# Merge intent — C `sync-rxdb-browser-delivery-spike`

Branch: `card/c-sync-rxdb-browser-delivery-spike` off `overnight-20260726` @ `c9dc440`
(which already carries card A's fold and the orchestrator's post-A review notes).

**Written BEFORE implementation**, per §15ad.65. Card B (`f1-workflow-*`, backend-only)
is running **concurrently** in a sibling worktree tonight. We share **zero code files**;
this note exists so that stays true through the merge.

---

## Files I own outright (new, nobody else writes them)

- `vendor/**` — **new directory.** The committed generator script and the committed
  pre-built ESM bundle(s) it emits. Nothing in the repo has ever written here; there
  is no prior owner and no concurrent writer.
- `.night-crew/qa/spike-supabase/browser/**` — the browser spike harness: its **own**
  `package.json` (Playwright driver + fixtures), its `node_modules/` (gitignored), the
  dedicated service-worker-enabled Playwright config, the spike page(s), and the
  proof scripts for the five legs.

Card B touches neither. Card A is already merged into my base.

---

## Shared files touched

| File | Owner | Why I touch it | What must survive |
|---|---|---|---|
| `build-sw.js` | repo-shared — **this is my one real shared surface, and I own it tonight** | The card requires `vendor/**` be added to Workbox's `globPatterns` so the vendored bundle is precached like every other local asset. That is the whole change: **one array entry.** | Every existing `globPatterns` / `globIgnores` entry, the `runtimeCaching` NetworkFirst API block with its offline-JSON `handlerDidError` fallback, `writeVersionJson()`, and all four Workbox flags (`skipWaiting`, `clientsClaim`, `cleanupOutdatedCaches`, `maximumFileSizeToCacheInBytes`). If a merge shows any diff in this file beyond the added glob, that is a defect in my card. |
| `.night-crew/qa/spike-supabase/README.md` | W1 (half 1) / W2 (half 2) | Card asks for **runbook half 3**, append-only, plus the **T-22 decision 53 rider** (the integrity-claim repair) which by construction touches text *above* my seam. | **All of W1's half 1 and W2's half 2 substance.** The rider is the one sanctioned exception to "append-only": it repairs six presentation blocks and/or narrows the two integrity claims at `:32-34` and `:724-727`. It does **not** revise any finding, any verdict, or any measured number. W1's GO stands and is not mine to revise. |
| `.night-crew/knowledge/designs/sync-rxdb-feasibility-spike.md` | W1 (stack verdict) / W2 (RxDB verdict) | Card asks me to **append this card's GO or NO-GO**. | W1's stack/JWT verdict and W2's RxDB-replication verdict **in full**. My verdict sits after both as a separate, clearly-headed browser-delivery verdict. **A NO-GO from me is a completed card, not a failed one**, and does not retract either prior GO. |
| `.night-crew/knowledge/roadmap.md` | run-shared | Per-change mechanics: card status flip **in the same change set as the work**. The `sync-rxdb-browser-delivery-spike` entry exists under Activity 1 (added at slating, currently `SLATED`); I flip it. | Every other card's entry, and the Activity 1 preamble including the fan-out note. I edit exactly one card's status paragraph. |
| `.night-crew/runs/2026-07-26-autonomous/timings.log` | run-shared | Leg instrumentation (`C_*`). | Card A's legs and the orchestrator's blocks. **Append at EOF only**; never rewrite or reflow. A conflict here is resolved by **keeping both sides in chronological order**, never by picking one. Card B appends `B_*` legs concurrently — expect this file to conflict and expect the resolution to be "both". |
| `sw.js`, `version.json` | generated | Regenerated by `node build-sw.js` as a consequence of the `globPatterns` change. | **Nothing.** See "safe to drop" below. |

Possible additional file, only if I hit a park trigger:

- `.night-crew/runs/2026-07-26-autonomous/DECISIONS-NEEDED.md` — append a new
  `## C — …` section at EOF. **Never overwrite**: it already carries D-1/D-2/D-3
  from card A's review, and card B may append concurrently.

---

## What must survive any merge

1. `vendor/**` in full — **both** the generator script and the committed bundle output.
   The whole point of the vendored-bundle decision is that the *output* is committed, so
   there is no build step in the deploy path. A merge that keeps the generator and drops
   the bundle destroys the decision it implements.
2. The single `vendor/**` entry in `build-sw.js`'s `globPatterns` — without it the bundle
   is not precached and the offline story the decision exists to protect does not hold.
3. The whole `browser/` harness including its **own lockfile** — the harness is the
   artifact the operator re-runs; without the lockfile the versions in the verdict are
   unreproducible.
4. The runbook half-3 append, and the rider's repair to the integrity claims. The rider is
   a **correctness fix to the record**, resolved as T-22 decision 53. Dropping it re-opens
   a resolved fork.
5. The GO/NO-GO verdict append in the design note. It is the card's deliverable and it
   gates `sync-rxdb-schema-and-replication`.
6. The pinned `esbuild` version recorded in the generator script and in the runbook. The
   pin is load-bearing: it is what let this card hold the root-`package.json` line.

## What is safe to drop

- **`sw.js`** — fully regenerable with `node build-sw.js` (or `task sw`). Content-hashed
  precache manifest; a merge conflict here should be resolved by **regenerating, not by
  hand-merging**. It is a build product that happens to be tracked.
- **`version.json`** — likewise regenerated by `build-sw.js`; it is also gitignored, so
  it should not appear in my diff at all.
- **`.night-crew/qa/spike-supabase/browser/node_modules/`** — gitignored, never committed,
  reinstallable with `npm ci` from my lockfile.
- Any transient log / screenshot / trace file written while capturing evidence. Every
  piece of evidence that matters is transcribed into runbook half 3 with its observed
  output; scratch files are not load-bearing.

Explicitly: **there is nothing else safe to drop.** Every other byte in my diff is
either the delivery mechanism, the harness, or the evidence.

---

## FOUR HARD BLAST-RADIUS CONSTRAINTS — per-item attestation

1. **`backend/go.mod` — UNTOUCHED.** This card adds no Go dependency and builds no Go
   code. Its only Go usage is `go run ./mintjwt` inside `.night-crew/qa/spike-supabase/`,
   which has its **own separate `go.mod`** (W1's) and is invoked read-only to mint spike
   tokens. `backend/go.sum` likewise untouched.
2. **Root `package.json` AND root `package-lock.json` — UNTOUCHED, and this one is mine
   to protect specifically.** They are the Playwright environment for **every** card and
   **every** worktree tonight, and card B is live right now — a card that "just adds one
   devDep" breaks concurrent dispatch for the whole night. The bundler is invoked as
   **`npx esbuild@<pinned-version>`**, which resolves into npx's own cache and writes
   nothing into the repo's dependency graph. `rxdb` / `@supabase/supabase-js` are consumed
   from `.night-crew/qa/spike-supabase/rxdb/`'s **own** lockfile (W2's), not installed at
   the root. **I never run `npm i` / `npm ci` from the repo root** — every install is
   preceded by a `cd` into a spike subdirectory. The root pair must remain byte-identical
   to `overnight-20260726` @ `c9dc440`.
3. **`docker-compose.nc.yml` — UNTOUCHED.** The night-crew ephemeral stack is unrelated
   to this spike. My Playwright gate leg uses the ordinary local Postgres with
   card-C-scoped database names (`hq_test_c1`, …) on `TEST_PORT=8299`.
4. **Root `Taskfile.yml` — UNTOUCHED.** The operator-runnable path is a runbook of real
   commands, deliberately not wrapper targets. I add no task. In particular I do **not**
   wire the vendor generator into `task sw` — doing so *is* the rejected "real build step"
   candidate and is an operator decision, not a card's (PARK trigger (i)).

## Fifth and sixth lines — the two things I consume but must not change

5. **`docker-compose.supabase.yml` — UNMODIFIED.** W1 owns it. I **consume** the stack
   W1 left running (`-p spike-supabase`, up ~10 h, db `:46011` / rest `:46233` /
   realtime `:46355`), resolving host ports via `docker compose … port`. **I do not tear
   it down** — teardown is an explicit, deliberately unexecuted runbook step, and **card B
   needs the same stack concurrently tonight.**
6. **`playwright.config.js` — UNMODIFIED.** `serviceWorkers: 'block'` at `:60` is
   load-bearing for 500+ existing tests. The service-worker leg is proved in a **dedicated
   config/driver under `.night-crew/qa/spike-supabase/browser/`**, never by relaxing the
   repo-wide setting. If the leg turns out to be unprovable without editing this file,
   that is **PARK trigger (ii)** — park, do not edit.

Also untouched, for completeness: **no production frontend code.** No `workflows.html`,
no `sync.js`, no other `*.html`, nothing under `backend/internal/**`, nothing under
`tests/**`. This card proves a delivery path; **it does not adopt one.**

The orchestrator can confirm all of the above with:

```
git diff --stat overnight-20260726..HEAD -- backend/go.mod backend/go.sum \
  package.json package-lock.json docker-compose.nc.yml Taskfile.yml \
  docker-compose.supabase.yml playwright.config.js \
  backend/internal tests ':(top,glob)*.html' ':(top)sync.js' ':(top)ptr.js'
```

which must print **nothing** — verified 2026-07-26, it does.

> **Corrected after the fact (G6 finding F7).** This command originally ended
> `... tests '*.html' sync.js ptr.js`. A bare `'*.html'` pathspec **matches at
> any depth**, so run verbatim it reported
> `.night-crew/qa/spike-supabase/browser/spike.html | 333 +++` — this card's own
> spike-harness page — and an orchestrator following this note would have seen an
> **apparent production-HTML violation at merge.** The `:(top,glob)` magic fixes
> both halves: `top` anchors at the repo root, and `glob` stops `*` from matching
> `/`. **No production `*.html` was ever touched**; the note's checker was wrong,
> not the diff.

---

## Merge order note

Card A is already folded into my base, so nothing of A's is at risk from me. Card B is
**concurrent and code-disjoint**: it is backend-only, I am frontend/vendor/spike-only.
The only files where a genuine concurrent-append shape exists are
`.night-crew/runs/2026-07-26-autonomous/timings.log` and (if either of us parks)
`DECISIONS-NEEDED.md`. Both resolve by **keeping both sides**, never by picking one.

`sw.js` is the one file where a mechanical conflict is plausible if any other card ever
regenerates it. Resolution is always: take either side, then run `node build-sw.js` and
commit the regenerated output. Do not hand-merge a precache manifest.

## Empty fields — stated explicitly rather than left blank

- **Database migrations:** none. This card creates no table, no column, no index.
- **API surface changes:** none. No route added, changed, or removed.
- **Production behaviour changes:** none. Nothing this card commits is loaded by any
  shipped page; `vendor/**` is precached but never imported by production code.
- **New root-level runtime dependencies:** none — see constraint 2.
- **Red-first:** **N/A, deliberately, and stated rather than left blank.** QA KR2's
  red-first requirement does not apply to a card whose deliverable is a **verdict**:
  there is no behaviour to hold red, because the card's output is a GO/NO-GO finding
  plus a runbook, not a production behaviour change. This is recorded here, in runbook
  half 3, and in the roadmap card entry — an empty field and a considered "N/A" must
  not look alike.
