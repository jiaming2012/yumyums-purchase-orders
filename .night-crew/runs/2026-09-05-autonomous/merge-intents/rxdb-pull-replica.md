# Merge intent — Card 2 · `rxdb-pull-replica` (run 20260905)

Branch: `wo-rxdb-pull-replica` (cut from overnight base @ 73c36bc — includes Card 1's
landed marketing.html shell + permission seed). Card authority: slate-20260905 Card 2.
Footprint: rxdb replica (`marketing/` NEW) + the vendor-surface widening below.

## Shared files touched

- `marketing/sync/pull-replication.js` (NEW) — the generic bounded/checkpointed pull
  mechanism (keyset checkpoint `{updated_at, id}`, PostgREST URL building with the
  spike's URL-encoding build-fact, RESYNC-on-Realtime wiring per §7.3). Dependency-
  injected: zero imports, so the same file runs under the Node harness (QA rxdb) and
  the browser (vendored bundle) unchanged.
- `marketing/sync/replicas.js` (NEW) — the two contract replicas over `public.codes`
  (Activity A's one-table schema call, roadmap Activity B note): codes/redemption-state
  (`expires_at > now() - 2 days`, §5.3) and offers (`expires_at > now()`, keyed on
  customer hash = `token_hash`, §10), plus `resolveOffers()` — the offline, local-only
  offer lookup. NOT wired into marketing.html tonight (see below).
- `marketing/sync/harness/` (NEW) — the standalone gate harness (B-345 precedent):
  `run.sh` + `harness.mjs`, driven against the spike-supabase substrate via
  `supabase/verify/lib.sh`. Red modes (`red-gap1`, `red-window`) demonstrate the naive
  cursor's silent miss and the unbounded pull; `green` is the PRIMARY gate.
- `vendor/src/rxdb-hq-entry.mjs` + `vendor/rxdb.bundle.js` — **surface widened**:
  `replicateRxCollection` (rxdb/plugins/replication) and `Subject` (rxjs) exported;
  bundle regenerated by `bash vendor/build-vendor.sh` against the SAME lockfile (pins
  unchanged — rxdb 17.4.0 / supabase-js 2.109.0 / esbuild 0.28.1, so the
  `sync-rxdb-client.spec.js` three-way version pin holds). Why this card and not
  Cards 5/6: the card mandates the replicas run on the vendored engine (R1), and the
  bundle exported neither the replication primitive nor an observable Subject —
  wiring later tonight would force a mid-run 500 KB bundle regen inside a page-touching
  card. Widening here, where the mechanism lands, is the smaller merge surface.
- `sw.js` — regenerated AFTER the vendor/marketing commits (build-sw.js reads git
  HEAD). `vendor/rxdb.bundle.js` is ALREADY precached (workflows.html →
  sync-rxdb/bootstrap.js → bundle), so its revision hash moves. **Precache count stays
  32 — deliberate: no asset added or removed**; the `marketing/sync/*` modules are
  unreferenced tonight and build-sw.js skips them by design (reachability, not
  completeness — B-37). CLAUDE.md's precache line is Card 1's ("currently 31" was
  already outdated by Card 1's 32; Card 1 owns that line's move — this card does not
  touch it because the count does not move again).
- **Wiring choice (stated per the slate): UNWIRED.** No module references are added to
  `marketing.html`; Cards 5/6 wire `marketing/sync/replicas.js` into the page and
  state their own precache moves. Nothing in this card's diff touches
  `marketing.html`, `index.html`, or any `tests/*.spec.js`.
- `.night-crew/knowledge/roadmap.md` — one-line status flip of the `rxdb-pull-replica`
  card (PLANNED → DRAFTING (overnight)), Card 1's flip convention. No other line moves.
- `.night-crew/knowledge/spikes/activity-b-offline-first-replica/rxdb-pull-replica.md`
  — GAP-1's `validated:` line appended under `## Comebacks` (the card's owed
  validation run), nothing else edited.
- `.night-crew/runs/2026-09-05-autonomous/merge-intents/rxdb-pull-replica.md` — this
  file (amended as evidence lands).
- `.night-crew/runs/2026-09-05-autonomous/card2-*.log` — committed whole gate logs,
  each ending in its `EXIT=` line.
- `night-crew.toml` — **nothing here.** `marketing/` and `vendor/` are deliberately
  undeclared paths → full-suite fallback, which this card runs anyway (vendor bundle
  is page-referenced). A `marketing` seam entry stays Card 6's call.
- `backend/`, `supabase/`, `sync-rxdb/`, `sync-schema/`, `package.json`,
  `version.go` — **nothing here** (no Go change, no schema change, no version bump;
  spike dirs are read-only inputs except the one Comebacks line above).

## What must survive any merge

- The `marketing/sync/` module files and their exported API
  (`makePullHandler` / `wireRealtimeResync` / `startCodesReplica` /
  `startOffersReplica` / `resolveOffers` / the two schemas) — Cards 5/6 wire exactly
  these; renaming or re-shaping them breaks the cards that follow tonight.
- The vendor bundle's widened export list (`replicateRxCollection`, `Subject`) and the
  regenerated `vendor/rxdb.bundle.js` — dropping either makes the replicas undrivable
  in the browser. If a later card regenerates the bundle on a newer HEAD with the same
  entry file, the regenerated copy supersedes this one (same pins ⇒ same semantics).
- `sw.js` regenerated on a HEAD that CONTAINS the new bundle. A merge that restores an
  older sw.js ships a stale bundle hash → the truck caches the old engine.
- The keyset checkpoint shape `{updated_at, id}` and the server-side OR predicate —
  reverting to a bare `gt` cursor reopens GAP-1 (the silent missed-redemption class).
- The GAP-1 `validated:` line in the spike ledger's `## Comebacks`; the roadmap flip;
  the `card2-*.log` evidence; this intent.

## What is safe to drop

- `marketing/sync/harness/` symlinked `node_modules` (runtime-created, gitignored).
- Nothing else in this branch is scratch.

## Red-first

Captured to `card2-red.log` (committed `cf2df08`) BEFORE the production modules
existed in the tree (they land in the NEXT commit, `5b8a486` — git history is
the chronology). The probes are the spike's naive handler shape, inline in the
harness, never the production code:

- **red-gap1** — `bash marketing/sync/harness/run.sh red-gap1` → **EXIT=1**.
  The naive `updated_at=gt.<ts>`-only cursor at batchSize 2 against the 5-row
  same-`updated_at` tie group: replica held **2 of 5**; the 3 silently missed
  ids are enumerated in the log. This is GAP-1's exact silent-miss class (a
  redemption that never reaches device B).
- **red-window** — `bash marketing/sync/harness/run.sh red-window` → **EXIT=1**.
  With no `expires_at` bound, both out-of-window rows landed (the per-run
  expired-5d code AND seed fixture …0003), named in the log.
- **Green**: the same harness, `green` mode, after the modules landed —
  `card2-harness.log`, EXIT=0 first attempt (below).

## Gate evidence (run in this worktree; full logs committed beside this file)

- **PRIMARY (standalone harness, B-345 precedent)** —
  `bash marketing/sync/harness/run.sh green` → **EXIT=0, first attempt**
  (`card2-harness.log`, committed `15b0f6d`). Substrate coordinates printed
  read-only by lib.sh before any write (spike-supabase reconcile; db/rest/
  realtime ports Docker-assigned). Every done_when clause held, enumerated:
  - codes replica initial sync = EXACT expected set (12 docs: 4 live seed
    fixtures + IN1 + target + offer-customer + 5 tie rows); out-of-window rows
    (expired-5d per-run + seed …0003) did NOT land; offers replica = EXACT
    11-doc set with IN1 (expired 1d) absent — the two windows provably differ.
  - GAP-1: tie group stamped with one shared `updated_at`; batchSize 2 forced
    boundaries inside it; **2 pull requests observably resumed MID-GROUP**
    from compound `(ts, id)` cursors; **5/5 tie rows landed**. Request log
    enumerated (9 codes requests, keyset cursors shown).
  - §7.3: the SUBSCRIBED refetch appeared in the request log resuming from the
    real checkpoint (never epoch); Realtime frames nudge-only.
  - done_when core: `POST /rpc/redeem` as device-a → `{ok:true}`; device-b's
    RUNNING replicas showed `redeemed_by=device-a` in **102 ms** (codes AND
    offers) — no restart, no manual reSync.
  - Offline: replication cancelled + channel removed, then
    `resolveOffers(customer-hash)` → exactly the customer's live offer;
    burned hash → []; redeemed seed hash → []; unknown hash → [] (the
    embedded-offer fallback signal); recently-expired code still visible to
    the codes replica offline.
- **G4** — `card2-g4.log`. Pass 1 on committed HEAD d4aa372: EXIT=0, **32
  files precached** (count unchanged — deliberate; only the vendor bundle's
  revision moved, every other url/revision pair identical). Pass 2 after the
  sw.js commit (917a3be): tree clean — idempotent. Version parity
  1.6.2 ≡ 1.6.2 ≡ 1.6.2 (version.go Frontend / package.json / version.json).
  Two environment notes recorded in the log: the npm 10.8.2 "Exit handler"
  bug left node_modules/.bin missing after a clean `npm ci` (repaired with
  `npm rebuild`; playwright 1.59.1 + bddgen resolve locally), and the
  TOOLCHAIN FINDING below.
- **G2 (Playwright, FULL suite)** — vendor bundle + sw.js are page-referenced,
  so no subset confinement. Two runs, per-failure verdicts:
  - **Run 1** (`card2-e2e.log`, HEAD 917a3be): EXIT=1 — **828 passed /
    9 failed / 6 skipped (39.1m)**, exactly one summary block (grep-verified).
    Notably SLOW (Card 1's runs: 26.6–28.1m) — consistent with a concurrent
    Track-A leg on this box. The 9, judged:
    1. `onboarding.spec.js:696` — slate-named known flake.
    2. `onboarding.spec.js:2268` — slate-named known flake.
    3. `sw-api-cache-partition` B1-XT-02 — known pre-existing (base-proven by
       Card 1's G6).
    4. `sw-api-cache-partition` B1-XT-05 — known pre-existing (ditto).
    5. `onboarding.spec.js:2233` — 30s-timeout shape, same helper + same
       describe as known-flake 2268 → **flake, proven below**.
    6. `persistence.spec.js:715` OFF-06 (timer-tick retry) → **flake, proven**.
    7. `sync.spec.js:135` SYN-03 → **flake, proven**.
    8. `sync.spec.js:928` SYN-09 (cursor race) → **flake, proven**.
    9. `users.spec.js:480` (30s timeout; users.html untouched by this diff —
       no mechanism connects it) → **flake, proven**.
    The slate's other two recurring reds — B1-XT-01 and workflows DBL-05 —
    were GREEN in this run (they flap; further evidence for the shape).
  - **Run 2** (`card2-e2e-2.log`, SAME tree, serial targeted re-run of exactly
    the five non-baseline reds): **5 passed (21.0s), EXIT=0** — every one
    green, at normal durations (1.1–3.4s vs 9.6s–30s timeouts under load).
    Two logs disagreeing on identical code = flake, per Card 1's judging
    precedent. **No failure in either run is attributable to this diff**; the
    four remaining reds are the slate's pre-existing set.
- **G1 / G2 (Go)** — n/a: `backend/` untouched (verified by the diff).

## Toolchain finding (triage-worthy, discovered by this card)

The committed `sw.js` has always been generated by the MAIN checkout's STALE
node_modules (workbox-build **7.3.0** / rollup 2.80.0 / terser 5.47.1 —
predating the 2026-07-17 lockfile pin `3b1be67`). A clean `npm ci` in this
worktree yields the LOCKFILE-TRUE toolchain (workbox-build **7.4.1** / rollup
4.62.2 / terser 5.49.0), whose output reorders the precache manifest array
(content-equivalent, same 32 url/revision pairs) and emits the inert
`//# sourceMappingURL=sw.js.map` comment twice (*.map is gitignored, the
reference was already unserved). This card committed the lockfile-true output
— the reproducible one — and states it here because the next card to
regenerate sw.js on the stale main-checkout env will see a spurious
whole-file reshape. The committed artifact should not depend on which
machine's stale install built it; recommend triage aligns the main checkout
(`npm ci`) or re-pins the lockfile deliberately.

## Engineering calls (recorded for the merge record)

1. **Checkpoint shape — id tiebreak, not over-fetch:** checkpoint is
   `{updated_at, id}`; the pull predicate is the PostgREST keyset tree
   `or=(updated_at.gt."TS",and(updated_at.eq."TS",id.gt."ID"))` with
   `order=updated_at.asc,id.asc`. Chosen over the one-row over-fetch because a
   same-`updated_at` group LARGER than one batch defeats `gte`+client-skip entirely
   (the server returns the same first rows forever → zero new docs while rows remain
   — a livelock, not just a re-read). The keyset walks any tie group deterministically.
   Both spike build-facts honored: cursor components URL-encoded (`+00:00`), values
   double-quoted inside the logic tree.
2. **One table, two replicas:** the roadmap defers one-vs-two tables to "Activity A's
   schema call"; Activity A built ONE table (`public.codes` — no entitlements table in
   the committed migrations). So the offers replica is the SAME table pulled with the
   `expires_at > now()` bound, keyed locally on `token_hash` — which under §10's
   identity-code model IS the customer hash the device computes from the scanned QR.
   `resolveOffers(hash)` returns every live (unredeemed, unexpired) row for that hash:
   length 0..1 tonight, length N unchanged when the identity-code card lands. NO
   schema change was needed → the PARK note did not trigger.
3. **Redemption state is NOT server-filtered out of the offers pull** (no
   `redeemed_at=is.null` filter): a filtered-out redemption would never reach the
   replica and the offline offer list would stay stale-positive. Redeemed rows flow
   through the pull as updates; `resolveOffers` excludes them locally.
4. **Module layout — dependency injection, not bundle imports:**
   `marketing/sync/*.js` import nothing; the RxDB primitives, fetch, and the Realtime
   client arrive as parameters. The Node harness injects the QA rxdb
   (memory storage + dev-mode + ajv validation); the browser injects the vendored
   bundle (Dexie). Identical mechanism code runs in both — which is what makes the
   standalone harness a real gate on the shipped modules.
5. **Vendored-surface widening lives here** (rationale in Shared files above).
