# DECISIONS-NEEDED — run `20260901`

Operator-only forks and parks surfaced during the run. Empty at launch. If this file
still says "No parks, no operator forks" at closeout, the night resolved everything
within its authority.

**Slate's three named PARK notes** (the discoveries that would be the operator's, per
the slate) — none triggered unless noted below:
- **Card 3 `toast-ingest-resurrection`:** park only if the recorded fix shape proves
  impossible (the SFTP key cannot ride an untracked file and would have to enter the
  image or the repo) — where a prod credential lives is the operator's.
- **Card 8 `app-slug-association`:** park only if the association-home turns product-fork
  (a new operator-facing concept or app-behavior choice). The home decision itself is
  the run's to make under standing authority.
- **Card 11 `deploy-hygiene-honesty`:** park if making one generator authoritative would
  remove/relocate a committed artifact from the release flow in a way that changes what
  deploys. A byte-identical unification is the run's to make.

---

## Open forks / parks

_None yet._
