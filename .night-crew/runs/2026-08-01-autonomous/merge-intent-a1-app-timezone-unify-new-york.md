# Merge intent — Card A1 `app-timezone-unify-new-york` (RESUME)

Run: `overnight-20260801`
Branch: `card/a1-app-timezone-unify-new-york`
Base: `overnight-20260801`, merged into the preserved card branch at `8da3ded`
(merge commit `524482b`).

**What this card does, in one line:** replaces every hardcoded `America/Chicago`
with the app's ruled timezone `America/New_York` (ledger T-26 decision 83,
T-28 decision 93), sourced from the single existing constant
`users.DefaultTimezone`, moves the three UTC "today" boundaries in the frontend
onto the same zone, **and edits HQ's half of the published two-repo contract**.

> ⚠️ **B-11 maintenance rule for this file.** If a repair round changes what a
> section below says, **re-read the whole note and strike the contradicted line**
> — do not merely append. Every "NOT touched" claim below is load-bearing.

---

## 0. What is NEW in the resume (vs the 07-29 note)

The 07-29 attempt parked at merge because it landed a two-repo decision it lacked
authority to land. That authority now exists (decision 93). The preserved work is
kept whole; three things are ADDED on top of it:

1. **Both `*-SALES-PROCESSOR-CONTRACT.md` documents and assumption A5.** HQ
   *publishes* `America/Chicago` to sales-processor at
   `21-SALES-PROCESSOR-CONTRACT.md:27`, `:67`, `:319` (A5) and
   `999.2-SALES-PROCESSOR-CONTRACT.md:30`. These are edited to New York and A5 is
   rewritten to record the coordinated release. **This is half of a two-repo
   agreement — a document handed to another maintainer, not a refactor.**
2. **`backend/internal/inventory/trends.go` `trendsWindow`** — a bare `time.Now()`
   with no `LoadLocation`, which the 07-29 attempt missed. Left as-is it would
   leave two 12-week COGS windows on two different zones.
3. **`inventory.html` badge-reset save + `tests/inventory.spec.js`** — decision 94.
   The Setup-tab Badge Reset wrote the *browser's* zone; it now writes the app
   zone. The test named *"badge reset saves with browser timezone, not hardcoded
   value"* was **asserting the defect** and is **rewritten**, not worked around.

🛑 **NOTHING SHIPS TO PROD from this card.** Until sales-processor's matching
change is ready, one repo is wrong. Merging tonight is correct; deploying is not.

---

## 1. Shared files touched (outside this card's own packages)

Everything below is a file another card tonight may also open. One line each on
*why* it is touched, so the orchestrator can resolve a 3am conflict without
reading the diff.

| File | Why A1 touches it |
|---|---|
| `sync.js` | `currentSubmitPeriod()` (was `new Date().toISOString().slice(0,10)`, i.e. **UTC**) now returns the app-timezone calendar date. Adds `APP_TIMEZONE` + `appDateString()` and exports both on `window`. |
| `workflows.html` | **Region: the period/timezone comparisons only.** `isCurrentPeriodEntry()` UTC fallback, plus the three pre-existing "already submitted today" comparisons (list row, runner, fill-body click handler). Adds a local `appDateString` fallback because `workflows.html` must not hard-depend on `sync.js` load order. **Disjoint from C1 (import + client construction) and C2 (conflict banner/sheet). Landing order A1 → C1 → C2.** |
| `purchasing.html` | `saveCutoff()` was **actively writing** `timezone: 'America/Chicago'` into cutoff config, overwriting the backend's New York default. Now writes `America/New_York`. |
| `inventory.html` | (a) comment-only fix at `costWindowLabel` (~`:1697`); (b) **NEW in the resume** — the Setup-tab Badge Reset save handler (~`:2713`) and its disabled display input (~`:2681`) stopped writing `Intl.DateTimeFormat().resolvedOptions().timeZone` and now write the app zone (decision 94). |
| `sw.js` | Regenerated by `node build-sw.js` because the HTML/JS files above changed. **Pure build artifact.** |
| `backend/internal/inventory/{handler.go,trends.go,types.go}` | The 6 COGS/completeness `AT TIME ZONE 'America/Chicago'` casts + 1 doc comment, **plus (new) `trendsWindow` now takes its "now" in the app zone.** |
| `backend/internal/purchasing/{service.go,repurchase.go}` | `CurrentWeekStart()` (every purchasing week hangs off this Monday) and the `repurchase_reset_config` insert default. |
| `backend/internal/recipes/{cost.go,scheduler.go,handler.go}` | 12-week cost window, Monday-09:00 drift tick, and the Recipes-tab default 7-day window. |
| `backend/internal/db/migrations/0072_app_timezone_new_york.sql` | **NEW FILE.** Flips the `cutoff_config.timezone` and `repurchase_reset_config.timezone` column defaults (set by `0037` / `0042`) to New York and re-points existing rows. **`0072` is assigned to A1 by the slate; verified present and unclaimed on `overnight-20260801` (latest migration there is `0071`).** |
| `.planning/milestones/v3.1-phases/21-.../21-SALES-PROCESSOR-CONTRACT.md` | **NEW in the resume.** `:27` (date semantics), `:67` (the published `pending_review_ids` SQL expression) and `:319` (assumption A5). |
| `.planning/milestones/v3.1-phases/999.2-.../999.2-SALES-PROCESSOR-CONTRACT.md` | **NEW in the resume.** `:30` (date semantics) + a pointer to A5. |
| `.night-crew/knowledge/roadmap.md` | The `app-timezone-unify-new-york` bullet is flipped PLANNED/PARKED → DONE. **One bullet only** — a conflict here is a line-level merge, and every other card's bullet is untouched. |

**Carried B-11 disclosures from the 07-29 attempt (still true):**
- `backend/internal/purchasing/service.go` also carries a `weekStartNow` clock seam
  (`var weekStartNow = time.Now`). It landed in the RED commit, before the zone
  changed, because the zone is unobservable without a frozen clock. Production
  behaviour is unchanged by the seam alone.
- Two sites were fixed that the slate's site list did not name, both inside this
  card's own packages and both the same defect:
  `backend/internal/purchasing/repurchase.go` (insert-default `America/Chicago`)
  and `backend/internal/recipes/handler.go` (`chicagoWeekWindow` → `appWeekWindow`).

## 2. What MUST survive any merge

If a conflict resolution drops any of these, the card is **half-landed**, which
is exactly the bug it exists to fix (two boundaries disagreeing).

1. **`users.DefaultTimezone` stays the ONE source.** No new timezone constant is
   introduced anywhere in Go. Every changed site either imports
   `github.com/yumyums/hq/internal/users` and reads `users.DefaultTimezone`, or
   (in SQL) reads the derived fragment built from it. If a resolution
   re-introduces a literal `"America/Chicago"` or a literal `"America/New_York"`
   in **non-test** Go code under `backend/internal/{inventory,purchasing,recipes}`,
   the resolution is wrong. (Two places legitimately still spell a zone out:
   migration `0072`, where SQL cannot read a Go const, and test fixtures, which
   name Chicago on purpose to prove the boundary moved.)
2. **All sites or none.** The site list is §1 above plus §4's enumeration. A
   partial merge is worse than no merge here.
3. **Migration `0072` keeps its number** unless the orchestrator renumbers at
   merge (Card B2 was assigned `0073`, not A1). Its content — both `ALTER
   COLUMN ... SET DEFAULT` **and** both `UPDATE` statements — must survive; the
   defaults alone leave already-written Chicago rows in place.
4. **The changeover-date note** in the header of `0072_app_timezone_new_york.sql`
   and in the doc comment on `purchasing.CurrentWeekStart`. Fix-forward only —
   past COGS/payroll figures are NOT restated.
5. **`inventory.pendingPeriodDateExpr`** (the SQL fragment const in
   `backend/internal/inventory/handler.go`) is used by BOTH `handler.go` and
   `trends.go`. Keep them sharing the const.
6. **The two contract-document edits and A5.** 🛑 This is the reason the 07-29
   attempt was parked. A merge that keeps the code and drops the contract edits
   re-creates the exact rejected state: HQ silently amending a published
   cross-repo agreement. **If the contract hunks conflict, resolve them — do not
   drop them.**
7. **The rewritten `tests/inventory.spec.js` badge-reset test.** The old test
   asserted the defect (decision 94). A resolution that restores
   `expect(body.timezone).toBe('America/Chicago')` from a Central browser context
   is restoring a defect assertion.

## 3. What is SAFE to drop in a conflict

- The **comment-only** edits: `inventory.html` ~`:1697`, `inventory/types.go`
  ~`:158`, and the `America/Chicago` mentions inside doc comments in
  `inventory/handler.go`, `recipes/scheduler.go`, `recipes/cost.go`,
  `purchasing/service.go`. Dropping these leaves stale prose, not a bug.
- The **rename** `recipes.chicagoWeekWindow` → `recipes.appWeekWindow`. If the
  rename conflicts, keep the old name and just change the zone inside it.
- The **disabled display input** in `inventory.html`'s badge-reset form
  (cosmetic — it shows the zone that will be saved). The **save handler** is not
  droppable; the display input is.
- `sw.js` entirely — regenerate with `node build-sw.js` after the merge rather
  than resolving its hunks. It is generated output.

## 4. Files this card does NOT touch (assert, so a conflict here is a red flag)

- `backend/internal/users/**` — **read-only dependency.** `DefaultTimezone` is
  consumed, never modified.
- `backend/internal/sync/**` — **not touched.** That is Track B2's surface.
- `backend/Dockerfile` — **not touched.** That is Track C1's.
- `backend/internal/purchasing/handler.go` — its `America/New_York` fallback at
  ~`:645` was **already correct**; left exactly as-is.
- `backend/internal/purchasing/scheduler.go` — already reads
  `users.DefaultTimezone`; it is the precedent this card follows, not a site.
- `playwright.config.js` — already `TimeZone=America/New_York`. **Not edited.**
- `users.html` — its timezone picker already defaults to New York (and must keep
  offering Central as a *per-user* choice; the app zone and a user's display
  zone are different things). **Not edited.**
- `docker-compose.nc.yml` — already New York. **Not edited.**
- `backend/internal/db/migrations/0037_cutoff_config.sql` and
  `0042_repurchase_tracking.sql` — **deliberately NOT edited.** Applied
  migrations are immutable history; `0072` is the forward fix.
- `backend/internal/receipt/worker.go` — **NOT touched.** See §7.
- No `openspec/` scaffolding is created (preflight verdict: ABSENT).
- `tests/persistence.spec.js` — **not touched, and not required.** The
  `workflows.html` edits are read-side date comparisons ("was this submission
  from today?") and the offline queue's period stamp. They do not add or change
  a user-entered value, so they never cross the
  `autoSaveField → DRAFT_RESPONSES → hydrateFieldState` path.

## 5. Test files touched

- `tests/sync.spec.js` — **APPENDED to only** (new `describe` block at end of
  file, frozen-clock period-boundary reds). B-06's stale comment at `:1584` is
  **NOT touched**. 🛑 The armed red
  `list page progress decrements when another device unchecks a field [LST-17]`
  is **not touched** either.
- `tests/workflows.spec.js` — `[DBL-05]` (~`:1201`) asserted
  `fresh[0].period === new Date().toISOString().slice(0,10)` — the exact UTC
  expression this card removes, so the test was asserting the defect. **Two lines
  inside that one test changed**; no other test in the file is touched.
- `tests/inventory.spec.js` — **NEW in the resume.** The single test
  *"badge reset saves with browser timezone, not hardcoded value"* (~`:2022`) is
  **rewritten** per decision 94: it now sets a deliberately non-app browser zone
  and asserts the PUT body carries the **app** zone. Retitled. **One test; no
  other test in this ~4000-line file is touched.**
- `backend/internal/inventory/trends_window_test.go` — **NEW FILE**, no conflict
  surface.
- `backend/internal/inventory/period_summary_test.go` — **NEW in the resume.**
  One appended boundary test for `pendingPeriodDateExpr` (the money path had no
  red; the park note called this out). Existing tests untouched.
- `backend/internal/purchasing/service_weekstart_test.go` — **NEW FILE**.
- `backend/internal/recipes/cost_test.go` — the existing
  `TestCostWindow_TwelveCompleteISOWeeksChicago` fixture **flips**: its
  "Sunday 2026-07-19 23:30 Chicago" case is Monday 00:30 in New York. Renamed and
  re-pinned. A real behaviour change surfaced by an existing test.
- `backend/internal/purchasing/scheduler_prove_test.go`,
  `scheduler_cron_test.go` — **NOT touched.** Their `America/Chicago` is an
  arbitrary DST-stable *fixture* location for configs that carry an explicit
  timezone column; it does not encode the app default.

## 6. Deliberately left alone inside touched files

- `inventory.html:1011` and `inventory.html:3329` — `new Date(Date.now() - N*day)
  .toISOString().slice(0,10)`. These are **lookback range starts** for read-only
  API filters (7 days, 90 days), not day-boundary decisions about accountability
  or money. **NOT a park** — a park needs a product question, and there is none.
- `users.html:373` — the `America/Chicago` `<option>` in the per-user timezone
  picker. That is a user's *display* preference, deliberately not the app zone.

## 7. Anything else

- **`backend/internal/receipt/worker.go` `parseEventDate`** falls back to
  server-local `time.Now()` when Mercury's `CreatedAt` is unparseable, and that
  value becomes `pending_purchases.event_date` — a COGS period assignment. The
  park note flagged it as *"outside this card's footprint; file it."* It is
  **filed in `.night-crew/knowledge/bugs.md`, not fixed here.**
- **`appDateString()`'s UTC fallback is silent** (`sync.js`). It fires only if
  `Intl.DateTimeFormat` throws or omits parts — a small-ICU runtime or an
  embedded WebView without tzdata. It now `console.warn`s so the failure is
  observable instead of silently restoring the removed UTC boundary. The
  fallback itself is unchanged: throwing inside a submit path is worse.
- **`APP_TIMEZONE` (`sync.js`) vs `users.DefaultTimezone` (Go)** is a hand-copied
  literal held equal by a comment — the same mechanism that produced the bug this
  card fixes. A Playwright assertion in `tests/sync.spec.js` now states the value
  independently so a one-sided edit fails a test rather than silently splitting
  the app in two. **Nothing mechanical can close this fully** without an
  app-timezone field on `/api/v1/health`; that is a bigger change than this card.
- **Reviewer note, so nobody re-litigates it:** `go test ./...` against ONE
  database is not a valid run of this repo's Go suite. Packages run in parallel
  by default and every DB-coupled `TestMain` TRUNCATEs shared tables. **`-p 1`**
  — which `Taskfile.yml:108` uses — is required. The gate numbers reported for
  this card are from `go test ./... -p 1`.
- **`inventory.spec.js:883` is a known pre-existing red** (B-27, cross-spec
  pollution, masked by `retries: 1`). It appears at `--retries=0`. **Not this
  card's, and not evidence against it.**
