# Backlog — advisory items that ride future cards

> Durable parking lot for triage-surfaced items that aren't roadmap cards yet but must
> survive run-to-run (HANDOFF is rewritten each run). Format: `title · description · origin ·
> status`. Promote to a roadmap card with `promoted → <card>`; drop with `dropped — reason`
> (struck through, kept as record).

> **🏁 Milestone boundary — HQ hardening cycle closed 2026-07-16 (cycle-gate signed off, ledger
> T-14).** The `new` items below are now the **feedstock for the next cycle's `/nc-okr-session`**.
> Nothing here was resolved by the read-only closeout run (it touched no code). The cycle's carried
> residue in one place: the ~37–41 flaky/data-dependent + SW-blocked suite pool (declined "Stabilize
> the suite," 2026-07-15) and the ~18 vacuous-test remainder are captured by the test-hardening notes
> below; the 3 harness WOs, F-1, F-2, and the video-pipeline fixture are itemized as their own entries.
> Per T-10/T-12, promotion/scheduling of any item is a planner call, not an operator hand-pick.

- 🔺 **[PRIORITY] Resume GSD Phase 23 — wage-history / employee-fields / scheduling foundation (preserved worktree)** ·
  A 31-commit GSD workspace at branch `workspace/hq-scheduling-app` (worktree
  `/Users/jamal/gsd-workspaces/hq-scheduling-app/hq`), **~complete-but-unverified** (STATE.md:
  *"Phase 23 — ALL PLANS COMPLETE — READY FOR VERIFICATION"*, last commit 2026-06-09). Delivers
  wage-history + employee fields + scheduling foundation: migration `0066_scheduling_foundation.sql`,
  `backend/internal/users/{db,handler}.go` +720 lines, `users.html` +657 lines, `tests/wages.spec.js`
  764 lines (+4,681 total). **Never shipped** — Payroll & Scheduling tiles are still `tile soon` on
  dev; no `wage_history` / `wages.spec.js` on dev. **Not a clean merge:** 977 commits behind dev, and
  the `0066` migration slot is now `0066_mercury_category_on_pending_purchases` (dev at 0074) → resume
  = renumber to 0075+, reconcile with dev's independently-shipped `0047_employee_fields`/`starting_salary`
  work, and redo `users.html` against current. **Worktree + branch PRESERVED at operator direction
  (2026-08-09) — do NOT discard.** Next step: operator + planner scope the pickup (rebase-vs-reimplement).
  · origin: nc-run launch triage 2026-08-09 (worktrees-check finding; operator chose keep + priority
  backlog over discard/merge-now) · status: new — **PRIORITY (operator-flagged 2026-08-09)**, planning owed

- **Onboarding video-pipeline E2E fixture (prove FR-16 + NFR-4)** · Stand up a DO Spaces bucket +
  an `ffmpeg` binary on the E2E host so the video presign→PUT→FFmpeg transcode/thumbnail path
  (FR-16, `handler.go:540-640`, `video.go:22-206`) and the `503 video_storage_not_configured`
  fallback (NFR-4) can be exercised. Both are fully implemented and **waived** from the Eng-KR
  denominator at triage 2026-07-13 (D-5) as env-gated — this fixture is the preserved prove-path:
  when built, it flips FR-16/NFR-4 UNPROVEN→WORKING (or reddens them). Needs operator-supplied
  Spaces creds. ~4–5h test-infra build. · origin: triage 2026-07-13 (D-5, waive-now-but-preserve)
  · deferred 2026-07-16 — off-theme for the "nothing silently lost" cycle; needs operator creds; revisit next cycle (OKR-session routing)

- ~~**Users stale-E2E repair** · `tests/users.spec.js` two Access-tab tests navigating dead
  `#t3`/`#s3` + rename the misleading `s3` var in `renderAccess`.~~ **DONE — promoted →
  `users-stale-e2e-repair`, landed overnight-20260714 (`d32830d`, G6-PASS):** `#t3/#s3` → `#t2/#s2`
  repoint + `s3`→`accessEl` rename; `users.spec.js` 17/2 → 19/0. · origin: triage 2026-07-10 (D-4)
  · closed 2026-07-14

- **Ops NFR-3 — backend resubmit `require_photo` gate** · The field-level required-photo gate ships
  front+back (overnight-20260714, `ad105f7`), but the **rejection-driven resubmit** photo requirement
  is **frontend-only** — `SubmitChecklistInput` (`backend/internal/workflow/model.go`) carries no
  `submission_id`/rejection context, so `validateFailNotes` can't know a prior rejection set
  `require_photo` on a field. A direct-API resubmit can bypass it. Fix: plumb rejection context into
  the submit validation (a `submission_rejections` join keyed on the resubmitted submission) + a
  red-first test that a rejected-with-require_photo field blocks resubmit server-side. Small fix-card.
  **Scheduling delegated to the planning agents** (PjM `/nc-slate-plan` / PM `/nc-pm-session` / eng) —
  triage 2026-07-14 (ledger T-10) declined to hand-pick backlog-vs-slate placement; queue placement is
  a planner call, not an operator triage pick. Stays a ready candidate here until they promote it.
  · origin: overnight-20260714 ops-nfr3 (in-footprint deferral, G6-confirmed) · promoted →
  `ops-nfr3-resubmit-photo-gate` (roadmap Activity 2, OKR session 2026-07-16)

- **Users `users.html:122` orphaned `<div id="s3">` cleanup** · After `users-stale-e2e-repair`
  repointed the Access tests to `#s2` and renamed the `renderAccess` var, the dead `<div id="s3">`
  at `users.html:122` (3-tab→2-tab refactor leftover) is now fully orphaned — nothing references it.
  Trivial removal; fold into any future Users card. · origin: overnight-20260714 users-stale G6
  (optional cleanup) · promoted → `users-s3-orphan-cleanup` (roadmap Activity 2, OKR session 2026-07-16)

- **`/nc-status` non-determinism — same repo, two machines, two reports** · Running `/nc-status`
  on separate machines yields diverging output, which defeats the skill's whole purpose (a single
  authoritative "you are here"). Root cause: the skill's §1 gather-commands hardcode paths that no
  longer match this repo's layout, so each machine's agent silently improvises a *different*
  substitute. Confirmed drift: (a) `grep -oE '"bg":"#..."' usm/roadmap.txt` — **no `usm/` dir
  exists**; the roadmap is `.night-crew/knowledge/roadmap.md` (markdown, not TextUSM), so the
  card-color signal returns empty and each agent re-derives progress differently. (b) `ls
  reference/slate-*.md` — actual path is `.night-crew/knowledge/reference/slate-*.md`. (c) root
  `BACKLOG.md` / `DECISIONS-NEEDED.md` don't exist — backlog is `.night-crew/knowledge/BACKLOG.md`
  and DECISIONS-NEEDED files nest per-run under `.night-crew/runs/<date>/`, so one machine reported
  "no DECISIONS-NEEDED in tree" while the resolved fork actually lives at
  `.night-crew/runs/2026-07-15-autonomous/DECISIONS-NEEDED.md`. (d) the DONE count read 28 on one
  machine vs 27 on the other because `grep -oE '\bDONE\b' roadmap.md` counts the **status-legend
  line** (`- **Status:** \`DONE\` · …`) as a card. Fix: repoint the §1 gather block to the real
  `.night-crew/` layout; make the color/progress signal read `roadmap.md`'s status tokens by
  **card row, not word token** (exclude the legend line); and resolve DECISIONS-NEEDED by globbing
  `.night-crew/runs/*/DECISIONS-NEEDED.md` and honoring the inline `> RESOLVED` marker (so a
  triaged fork reads "present-and-resolved," never "absent"). Symmetry check: apply the same
  layout truth to `/nc-help` and any `nc-progress` variant so all status surfaces agree. The skill
  file lives outside this repo (`~/.claude/skills/nc-status/`), so this is a **framework/tooling
  WO**, not a product card — but it must land or every future `/nc-status` cross-check is unreliable.
  · origin: 2026-07-15 cross-machine `/nc-status` diff (operator-observed) · deferred 2026-07-16 —
  framework/tooling outside this repo; stays in backlog until a night-crew dogfood pass picks it up
  (operator decision, OKR-session routing)

- **Ops P0 — template-edit data loss: field IDs churn on every `updateTemplate` (REPRODUCED)** ·
  Editing a template (e.g. cutting a task from the Friday checklist) while crew devices have the
  checklist open silently discards all their subsequent work. Mechanism, confirmed by deterministic
  E2E repro: `replaceTemplate` (`backend/internal/workflow/repository.go:99-220`) deletes and
  re-inserts every field with **new IDs** on any edit (drafts are remapped old→new at edit time,
  but only once); open devices keep rendering the old IDs, and every later check/uncheck writes a
  draft under a dead field id — accepted **silently** because the field_id FK was dropped
  (migrations 0051/0053/0054). Optimistic UI shows the check; reload loses it; a device that opened
  after the edit never sees the other's ops. Repro spec: `tests/repro-cut-task.spec.js` (untracked
  on dev — asserts the DESIRED behavior, so it fails until fixed and then becomes the regression
  test; baseline pre-edit sync passes, post-edit reload-persistence fails on both devices). Fix
  stage 1 of 3 (operator-ratified direction 2026-07-16): (a) preserve field IDs on update — honor
  the ids the Builder already sends (`toApiTemplate` includes them) via diff/upsert instead of
  delete-reinsert; (b) make dead-id saves loud — `saveResponse` rejects unknown field ids via an
  app-level existence check scoped to drafts (`submission_id IS NULL`; do NOT restore the FK —
  submitted responses reference snapshot ids by design). Red-first: flip the repro spec in.
  · origin: operator report 2026-07-16 (Friday checklist, two devices), reproduced + root-caused
  same session · promoted → ~~`stage1-*`~~ → **final (2026-07-16 grill-back, frozen-at-submit):**
  `editprop-stable-field-identity` (roadmap Activity 5) — the stage-1 work as the *permanent*
  architecture, not an interim

- **Ops — template-updated broadcast: open devices re-render on edit** · Stage 2 of the
  template-edit robustness roadmap. `SAVE_TEMPLATE` ops are already emitted on template edits and
  already flow through both live WS and `wsCatchUp` replay — clients simply ignore them. Handle
  them in `applyOp` (sync.js): re-fetch the template, re-render the open checklist with the new
  shape/ids, preserve any in-progress input. Closes the staleness gap stage 1 leaves (devices
  still rendering a cut field until reload; mixed old/new-device live sync) and covers offline
  devices on reconnect for free via catch-up. Depends on stage 1 (stable ids make the re-render
  a remap instead of a reset). Mind the silent-replay rule from `42eeb39`: a template re-render
  triggered by catch-up must not toast. · origin: fix-direction session 2026-07-16 (stage 2 of 3)
  · promoted → ~~`stage2-template-updated-broadcast`~~ → **final (2026-07-16 grill-back,
  frozen-at-submit):** `editprop-broadcast-rerender` (roadmap Activity 5) — live re-render on
  edit is the chosen *permanent* semantic, not interim relief (briefly demoted mid-grill when
  versioning was the plan; revived when frozen-at-submit won the head-to-head)

- **Ops architecture — immutable template versions, run-pinned (stable field identity)** · Stage 3
  end-state that deletes the stage-1/2 compensations: fields get client-generated UUIDs honored by
  the server forever (identity as fact, not row artifact); every edit creates an immutable template
  version with "the template" a head pointer; a checklist RUN pins the version current when the run
  started (crews finish the run they started — mid-shift edits take effect next run, resolving
  "what does cutting a task mean for a half-done checklist" by rule); responses key on
  (run, field-uuid) so the existing op-log/Lamport sync layer can never target a dead id. Extends
  the existing submit-time `template_snapshot` (LC-02) upstream to edit-time. Phase-sized (schema
  migration + workflow backend rework + runner load path); slots naturally ahead of sync Phase 11 /
  the reactive-store direction. · origin: fix-direction session 2026-07-16 (stage 3 of 3,
  operator asked for the robust-architecture answer) · ~~promoted → `versioning-*`~~ **demoted
  back to backlog at the 2026-07-16 evening grill-back:** weighed head-to-head against
  frozen-at-submit (edits live on unsubmitted checklists; submit freezes) — the operator delegated
  the semantic to the PM with a multi-device-sync bar, and frozen-at-submit won: the operator is
  the editor and wants corrections live; a 1–5 person single-kitchen crew doesn't need
  fleet-auditor run-pinning; and the chosen shape needs no schema migration. Kept as the future
  evolution if a fleet-style crew ever materializes (ledger G-2). · deferred 2026-07-16

- **Inventory — prod ghost catalog item check + cleanup** · The receipt pipeline auto-created a
  `''`-description `purchase_items` row when Claude parsed a receipt with unnamed line items
  (description is UNIQUE → every future unnamed line merges into the one ghost item; it renders as
  a blank first row in the review picker). All creation paths are guarded as of `42eeb39`
  (validate.go Check 0 routes unnamed receipts to review; worker.go never auto-creates from an
  empty name), but the guard shipped AFTER real receipts flowed through prod — check prod:
  `SELECT count(*) FROM purchase_items WHERE trim(description)='';` plus its linked
  `purchase_line_items`, then rename or unlink+delete. **Prod data mutation — needs operator
  sign-off on the handling, not just the WO.** Small card. · origin: 2026-07-16 test-failure
  root-cause (empty item found in `hq_test_ui` from live Mercury ingest; prod exposure inferred)
  · promoted → `prod-ghost-item-rename` (roadmap Activity 7, OKR session 2026-07-16 — operator
  chose rename-keep-links handling)

## Activity-4 fix-cards (from Activity-2 confirm-absence graduations — code-fix + regression-test WOs)

> Distinct from test-only prove-UNPROVEN WOs: these are **confirmed-BROKEN** flows where a cited
> line proves the behavior absent. Each = code fix (front+back) + red-first regression test.

- ~~**Operations FR-4 — yes/no "No" corrective-action enforcement** · A "No" answer never blocked
  submit (`evaluateFailTrigger` handled only `out_of_range`; submit short-circuited on
  `!f.fail_trigger`; server `validateFailNotes` checked only `out_of_range`).~~ **DONE — promoted →
  `ops-fr4-no-enforcement`, landed overnight-20260714 (`2287947`, G6-PASS, red-first):** a failing
  "No" now requires a corrective fail note front+back (`isYesNoNo` + trigger in `validateFailNotes`,
  mirroring the temperature path). · origin: overnight-20260712 ops-confirm-absence · closed 2026-07-14
- ~~**Operations NFR-3 — photo-required-at-submit enforcement** · No photo gate on submit/resubmit;
  `PhotoURL` storable (`model.go:92`) but never required.~~ **DONE (field-level) — promoted →
  `ops-nfr3-photo-required`, landed overnight-20260714 (`ad105f7`, G6-PASS, red-first):** submit now
  blocks until a required photo field carries a valid `https://` value, front+back (`isHTTPSPhotoValue`
  iterates template fields, not just responses). **Residual → F-1 below:** the rejection-driven
  *resubmit* photo requirement stays frontend-only (no `submission_id`/rejection context in the submit
  input). · origin: overnight-20260712 ops-confirm-absence · partially closed 2026-07-14
- ~~**Onboarding NFR-5 — reopen/reject of a video-led section is a silent no-op** · Video progress
  keyed by `ob_video_parts.id`, but `ReopenSection` deleted by parent `ob_items.id` → zero rows,
  section stayed complete.~~ **DONE — promoted → `onboarding-nfr5-video-reopen`, landed
  overnight-20260714 (`5d73b96`, G6-PASS, red-first):** `ReopenSection` resolves the first video part
  for `video_series` items and deletes THAT progress, reverting the section to active. Covers FR-9
  (crew reopen) + FR-15 (manager reject). · origin: overnight-20260712 onboarding-confirm-absence ·
  closed 2026-07-14
- ~~**Purchasing FR-18 — History tab is a static stub (frontend build WO)** · `#s4` hardcoded stub;
  no `renderHistory`, no `GET /shopping/history` call; backend `GetShoppingListHistory` never wired
  to the UI.~~ **DONE — promoted → `purchasing-fr18-history`, landed overnight-20260714 (`4cb57b7`,
  G6-PASS, red-first):** `renderHistory` built + wired to `GET /shopping/history`; 5 History tests
  pass (fixture via existing API, no SQL/migration). · origin: PRD-purchasing-hardening Activity 1 ·
  closed 2026-07-14

## Test-hardening notes (from Activity-3 test-audits — ride the downstream prove-UNPROVEN/test WOs; not reclassifications)

- **Operations FR-15 builder-UI coverage gap** · The six builder field types are proven only via
  `POST /createTemplate` API calls, not the builder Add-Field UI; only 4/6 types covered (photo = 0,
  sub-steps thin). Flow stays WORKING (real assertions exist; not statically vacuous) but is
  under-proven vs. its claim ("add each type *via the builder*"). Add builder-UI Add-Field coverage
  for all six types incl. photo. · origin: overnight-20260712 ops-test-audit (G6-passed) · promoted →
  rides `vacuous-tests-18-to-0` (roadmap Activity 6, OKR session 2026-07-16)
- **Operations FR-10/FR-12 vacuous reject test** · `reject item with comment`
  (`tests/workflows.spec.js:485-508`) wraps its whole body in `if (await flagBtn.isVisible())` with
  no `expect` — genuinely vacuous. Maps to FR-10/FR-12 (already UNPROVEN). Use as the starting point
  for the FR-12 test-only WO (the assertion to add). · origin: overnight-20260712 ops-test-audit ·
  promoted → rides `vacuous-tests-18-to-0` (roadmap Activity 6, OKR session 2026-07-16)
- **Onboarding 6 conditional-skip guard sites** · `tests/onboarding.spec.js` has 6 guard/`return`
  sites that skip an assertion when the seed shape differs: `:991` (FR-13, PRD-flagged), `:148-151`
  (FR-3), `:250-259`+`:826-841` (FR-5), `:304-306` (FR-2), `:700-745` (FR-13 UI), `:2104-2107`+
  `:2136-2139` (FR-15). All flows STAY WORKING (each has an unconditional sibling assertion) — these
  are hardening flags, not drops. Replace each guard with a self-seeded fixture so a shape mismatch
  reddens instead of silently skipping. Rides the Onboarding Activity-4 test-hardening WO.
  · origin: overnight-20260712 onboarding-test-audit (G6-passed) · promoted → rides
  `vacuous-tests-18-to-0` (roadmap Activity 6, OKR session 2026-07-16)
- ~~**Purchasing FR-7 vacuous shopping-tab test (dropped WORKING→UNPROVEN)** · `Shopping tab shows
  stub when no active list exists` ended in `expect(text.trim().length).toBeGreaterThan(0)` — a
  generic-content tautology.~~ **DONE — promoted → `purchasing-fr7-retest`, landed overnight-20260714
  (`958a176`, G6-PASS):** real assertions replace the tautology — (a) exact empty-state stub copy with
  no list, (b) grouped vendor sections + per-item check buttons + thumbnails + aisle locations when
  populated. (Note: the old test was baseline-RED — it targeted a nonexistent `#shopping-content` —
  not merely vacuous.) FR-7 UNPROVEN → WORKING. · origin: overnight-20260712 purchasing-test-audit ·
  closed 2026-07-14
- ~~**Inventory NFR-1 double name-normalization gap** · Two surfaces persist un-normalized text while
  the rest title-case: (1) `UpdateItemHandler` writes `input.Description` raw — item *edit* skips
  `normalizeItemName`; (2) `ConfirmPendingPurchaseHandler` upserts the **vendor** raw while receipt
  line-items ARE normalized.~~ **DONE — promoted → `inventory-nfr1-normalize-fix`, landed
  overnight-20260715 (`748463c`, G6-PASS, red-first):** the E4 prove card committed the NFR-1 RED
  first, then the fix added `normalizeItemName` to BOTH surfaces (`handler.go:660` vendor + `:1130`
  description) — RED→GREEN on pristine-vs-fixed binaries. Both named gaps closed in one 2-line fix;
  FR-4's "vendor upserted title-cased" PRD text is now accurate (no correction needed). Eng KR-1 +1 → 0.
  · origin: overnight-20260712 inventory-confirm-absence · closed 2026-07-15
- **Inventory ~40 data-dependent test guards (cleanup)** · `tests/inventory.spec.js` carries ~40
  `if (await X.count() > 0) return;` / `if(count>0){…}` guards that silently pass when a seed is a
  no-op (PRD §Verification). None drops a WORKING flow (each guarded assertion is backstopped by a
  guaranteed real seed or an unguarded sibling), but they're QA-KR-1 cleanup: convert to unguarded
  (or self-seed) so a seed miss reddens. Representative: FR-2 tax/grand-total at `:1039-1042,
  1058-1061`. Rides the Inventory Activity-4 test-hardening WO. · origin: overnight-20260712
  inventory-test-audit (G6-passed) · promoted → rides `vacuous-tests-18-to-0` (roadmap Activity 6,
  OKR session 2026-07-16)

## Pass-2 enumeration finds (from the 2026-07-16 evening PM session — `PRD-data-integrity` §Routing)

> The data-integrity PRD's two-pass surface sweep found 10 loss-mode candidates; 5 folded into
> the PRD (FR-9/FR-10/FR-11 + 2 subsumed), these 5 routed here. Full dispositions in
> `prds/PRD-data-integrity.md` §Routing.

- ~~**Sync op-log durability — `EmitOp` is fire-and-forget**~~ · After a successful business
  write, the op row that tells other devices about it is inserted in a goroutine with errors
  logged only (`backend/internal/sync/ops.go:245-264`). **PROMOTED same evening at the grill-back:**
  under the operator's delegated UX bar ("devices are always in sync"), delayed propagation IS a
  loss — folded into `PRD-data-integrity` FR-5 (transactional op emission), rides
  `editprop-broadcast-rerender`. · origin: pm-session 2026-07-16 pass-2 sweep · promoted →
  `editprop-broadcast-rerender` (grill-back 2026-07-16)

- **Runner — failed photo upload leaves a partial saved value** · The photo-upload `.catch` blocks
  (`workflows.html:1564-1572`, `:1635-1647`) render a retry UI but don't clear the partial value
  from `FIELD_RESPONSES`/`DRAFT_RESPONSES`; a stale value can linger under the field. No durable
  crew work is lost (the photo never existed server-side; submit validation blocks required-photo),
  so this is stale-state hygiene, not a loss mode. Small frontend fix + persistence test.
  · origin: pm-session 2026-07-16 pass-2 sweep · deferred 2026-07-19 (PM-session routing — off-theme / harness-dependent; reason per item in PRD-prove-and-surface §Routing items 10 & 12)

- **Offline submit idempotency under IndexedDB failure (suspected, unverified)** · If the offline
  submit queue's `idempotency_key` is lost to an IndexedDB write failure, reconnect drain
  (`sync.js:466-494`) could duplicate a submission. Untestable without the offline harness — rides
  `WO-offline-indexeddb-harness` (deferred this cycle) when it's built. · origin: pm-session
  2026-07-16 pass-2 sweep · deferred 2026-07-19 (PM-session routing — needs the offline-IndexedDB harness, not built this cycle; PRD-prove-and-surface §Routing item 11)

- **Lamport clock corruption → catch-up gap (suspected, unverified)** · If the stored Lamport
  clock in IndexedDB is cleared/corrupted, the next `wsCatchUp` (`sync.js:303-315`) may skip or
  refetch ops, staling the UI. Same harness dependency — rides `WO-offline-indexeddb-harness`.
  · origin: pm-session 2026-07-16 pass-2 sweep · deferred 2026-07-19 (PM-session routing — off-theme / harness-dependent; reason per item in PRD-prove-and-surface §Routing items 10 & 12)

- ~~**Unsubmit → fast resubmit fail-note staleness (suspected, unverified)**~~ · If a user
  unsubmits (`workflows.html:2372-2380`) and resubmits before the re-render, stale `FAIL_NOTES`
  state may duplicate or drop fail notes. **UPGRADED same evening at the grill-back:** the operator
  named submit/unsubmit explicitly in the convergence bar — becomes an FR-7 matrix cell
  (`editprop-convergence-matrix`) instead of a queued suspect. · origin: pm-session 2026-07-16
  pass-2 sweep · promoted → `editprop-convergence-matrix` (grill-back 2026-07-16)

## Prove-sweep PARK fix-WOs (from overnight-20260715 — production refactor / new harness, beyond same-footprint test-only)

> Surfaced by the prove-UNPROVEN sweep as the *fixes* the parked flows imply. Each needs a
> production seam or a new test harness, so per the slate they were NOT graduated the same night.
> Scheduling is the planners' call (queue placement per T-10). Unblocks the §A parks in
> `runs/2026-07-15-autonomous/DECISIONS-NEEDED.md`.

- **WO-cron-clock-seam** · Add a `now time.Time` (or package `nowFn`) seam to the 4 `run*Check`
  funcs — `runReminderCheck`/`runCutoffCheck`/`runLowStockCheck` (`internal/purchasing/scheduler.go:54/167/247`)
  + `runRepurchaseResetCheck` (`repurchase.go:129`), each currently reads `now := time.Now().In(loc)`
  inline — then add real cron-decision unit tests (seed config + past-cutoff → assert transition, no
  15-min wait). Adjacent pure logic already proven GREEN in `scheduler_prove_test.go`. **Unblocks P-6
  (Purchasing FR-19/20/21/22).** · origin: overnight-20260715 purchasing-prove-state-auth-scheduler
  (PARK) · promoted → `carried-fix-wos-sweep` (roadmap Activity 6, OKR session 2026-07-16 — pure
  code seam, no creds/harness dependency)
- **WO-photo-s3-harness** · A way to exercise photo presign→PUT→public-URL in E2E (mock S3 or a test
  DO-Spaces bucket); the ephemeral stack sets no `SPACES_*` so `UploadHandler` returns 503. **Unblocks
  P-1 (Onboarding FR-18), P-2 (Inventory FR-27), P-3 (Operations NFR-2 PUT leg)** — 3 flows in one
  harness. Related to (but distinct from) the Onboarding video-pipeline fixture above (video vs item/
  receipt photo). · origin: overnight-20260715 (A4/E3/D2 PARKs) · deferred 2026-07-16 — needs new
  harness infrastructure (mock S3 / test bucket + creds), same class as the deferred video fixture;
  revisit next cycle (OKR-session routing)
- **WO-offline-indexeddb-harness** · A dedicated Playwright project with the service worker +
  IndexedDB enabled (`playwright.config.js:29` blocks SWs today) to test offline sync/queue/conflict
  + draft-persist-across-redirect. **Unblocks P-4 (Operations NFR-5), P-5 (Operations NFR-7 draft
  leg).** · origin: overnight-20260715 ops-prove-cross (PARK) · deferred 2026-07-16 — needs a new
  SW+IndexedDB Playwright project (structural harness build), off-theme for this cycle; revisit
  next cycle (OKR-session routing)

## Editprop follow-ups (from overnight-20260717 morning triage — same-pattern tidy-ups, planner-scheduled)

> Surfaced as out-of-footprint follow-ups on the editprop build; operator routed to BACKLOG at
> triage 2026-07-17 (no competing option — queue placement is a planner call per T-10/T-12). The
> substantive convergence-cell item (F-A) was NOT backlogged — it went straight to roadmap card
> `editprop-convergence-cell-hardening` (Activity 6). The `workbox-build` gap (F-D) was fixed at
> triage (`3b1be67`), not backlogged.

- **Transactional op emission for Create/Archive (INV-1 parity)** · W-2 moved `updateTemplate`'s
  op emission into the write txn (`EmitOpTx`), but `CreateTemplateHandler` and
  `ArchiveTemplateHandler` still use the fire-and-forget `EmitOp` goroutine. Convert both to
  `EmitOpTx` (mirrors W-2's pattern; no schema change) for full INV-1 "0 accepted writes whose op
  is not durably queued" parity. Small follow-up card. · origin: triage 2026-07-17 (F-B, W-2
  follow-up) · deferred 2026-07-19 (PM-session routing — editprop tidy-up, stays BACKLOG not a KR this cycle; PRD-prove-and-surface §Routing item 6)
- **Fail-note conflict live-render on the `applyOp`/409 path (`_fail_note` unpack)** · The W-6b
  conflict-coverage sweep (`editprop-convergence-cell-hardening`) hardened the LWW-409/`applyOp`
  render for 4 answer types (yes_no, temperature, sub-step, checkbox) but could NOT reach the 2
  fail-note types: `applyOp`'s `SET_FIELD` branch (`sync.js:405`) unpacks only `value`/`sub_steps`,
  never `_fail_note` — that bundle is unpacked solely by `hydrateFieldState` (`workflows.html:1480`)
  on load/reopen. So on a two-device conflict where a fail-note (value+note+severity, or photo URL)
  loses LWW, the losing device shows a stale/malformed fail card until the next reopen (data never
  lost server-side; a live-render staleness window on a rare concurrent path). Production card:
  extend `applyOp` `SET_FIELD` to unpack `_fail_note` on the incoming-op/409 path, then land the 2
  parked W-6b cells — own design/footprint/G6, NOT test-debt. **Bundle candidate with F-B above**
  (both touch the op-emission/apply path). · origin: triage 2026-07-18 (D-1, overnight-20260718 —
  operator chose accept + track over graduate-now) · deferred 2026-07-19 (PM-session routing — out-of-footprint editprop tidy-up, needs `_fail_note` unpack on apply path; PRD-prove-and-surface §Routing item 7)
- **Atomic approval + feedback (`approveSubmission` tx)** · `approveSubmission` commits
  `status='approved'` *before* the feedback loop, so a feedback-persist failure correctly returns
  500 `feedback_persist_failed` (W-4's goal, requirement MET) but leaves the submission already
  `approved` — a partial commit. Thread a `tx` through `approveSubmission` (repository.go) so
  status + feedback commit atomically; also removes the less-specific `internal_error` a retrying
  approver sees on the 2nd attempt (idempotent via the `status='pending'` guard). · origin: triage
  2026-07-17 (F-C, W-4 follow-up) · deferred 2026-07-19 (PM-session routing — editprop tidy-up, not a KR this cycle; PRD-prove-and-surface §Routing item 8)
- **Onboarding persistence tests: `waitForResponse` over fixed flush wait** · Two converted
  persistence tests in `tests/onboarding.spec.js` use `waitForTimeout(1500)` instead of
  `waitForResponse('/saveProgress')`. The post-reload assertion is still the load-bearing proof, so
  the guard isn't weakened — but a fixed wait is a small flake-surface. Switch to `waitForResponse`
  on the save POST in a future test-hardening pass. Low priority. · origin: triage 2026-07-17 (F-E,
  T-2 minor) · deferred 2026-07-19 (PM-session routing — low-priority test-hardening; PRD-prove-and-surface §Routing item 9)

## Waiver-#1 last mile (from overnight-20260719 cycle gate — operator chose "graduate" 2026-07-19)

- **`suite-isolation-approved-checklist` — formally retire waiver #1** · The 2026-07-19 cycle gate
  ran the deterministic stack green on an isolated pg16 (Go units exit-0; Playwright 450 pass · 1
  fail · 0 flaky · 6 skip) EXCEPT one red: `tests/workflows.spec.js › approved checklist shows
  Approved badge and cannot be resubmitted [LST-08 RUN-08]` — `expect(locator('#toast')).toBeVisible()`
  gets `hidden` **in the full suite but PASSES in isolation** (fresh single-test DB, `--retries=0` →
  `1 passed`; cycle-closeout-20260719.md §1). It is a **cross-test DB-pollution / test-isolation
  defect, not a product defect**: some earlier spec sharing `hq_test` leaves approval/`#toast` state
  that this test's assertion trips over. This single red is the ONLY thing keeping literal
  `task test` from exit-0, so waiver #1 is currently **substantially retired (38 reds → 1) but not
  formally**. **Fix:** isolate this test's state dependency (identify the polluting predecessor —
  likely an approval/submission-rejection row or a `#toast` left visible — and either scope its
  fixture teardown or make the assertion order-independent), then re-run the full suite to confirm
  **literal `task test` exit-0**, which **formally retires carried waiver #1** (Eng KR5 PASS). Small
  test-hardening WO (no production change; test-only footprint). Red-first: the full-suite red is the
  baseline; green = literal exit-0. · origin: overnight-20260719 cycle gate (Eng KR5 PARTIAL) ·
  **operator chose (a) graduate 2026-07-19** (DECISIONS-NEEDED §C) · promoted → `waiver1-isolation-fix` (Activity 3; PM-session routing 2026-07-19, PRD-prove-and-surface §Routing / FR-10)
- **Per-card wall-clock instrumentation as a standing build-run output** · The 2026-07-19 gate could
  not compute a this-cycle Delivery median (KR4 PARTIAL) because the 07-17 run's 9 build cards were
  not per-card timed — only 07-18's single card was measured. `-0718` already re-adopted the
  harness-measured table; make it the **invariant** for every build run so the ledger stays measured,
  not narrated, and the next gate can compute a real median vs the T-14 baseline (N=23/22m28s).
  · origin: overnight-20260719 cycle gate (Delivery KR4 PARTIAL, fix-forward) · promoted → `percard-timing-instrumentation` (Activity 3; PM-session routing 2026-07-19, PRD-prove-and-surface §Routing / FR-11)
- **Gate run-mechanics: `CI=1` + explicit pre-migration by default** · Two run-to-run wall-clock
  losses (07-18 G6, 07-19 gate) came from the same `:8199` `reuseExistingServer` foreign-server
  latch and (07-19) an unmigrated isolated DB. Bake into the gate/G6 run-mechanics: always run the
  suite with `CI=1` (forces `reuseExistingServer:false` → own webServer + teardown) AND pre-migrate
  the isolated pg16 via a throwaway app boot before Go units. Run-mechanics doc/skill fix, not a code
  change. · origin: overnight-20260719 cycle gate (card-actuals 8th-slate obs) · folded → rides `percard-timing-instrumentation` / cycle-gate run-mechanics (PM-session routing 2026-07-19, PRD-prove-and-surface §Routing / FR-11)

## Escaped defect + QA gap (found 2026-07-17 on dev, operator play)

- **Cross-user live-sync access matrix + `sync`-package unit coverage** · An escaped defect
  (live-sync ops fanned out only to a checklist's assignees, excluding non-assignee editors incl.
  admins/superadmins → the operator's own edits never reached their 2nd device; fixed red-first,
  `sync/ops.go` `ResolveEntityAccess` unions admins + `listener.go` always includes the author,
  regression test `sync/access_test.go`) revealed a QA hole: **every convergence/live-sync test
  drives the assignee editing their own checklist**, so the recipient-resolution for a *non-assignee
  editor* was never tested, AND the whole `sync` package had **zero Go tests**. WO: add a cross-user
  access matrix ({who views}×{who edits}×{who observes}×{role/assignment}, asserting access AND
  live-op propagation) + `sync`-package unit coverage for `ResolveEntityAccess` across all
  role/assignment combos. Full write-up: `reference/qa-gap-20260717-live-sync-access.md`.
  · origin: 2026-07-17 operator-found live-sync bug (dev play, post cycle-gate) · promoted → `sync-pkg-unit-coverage` + `convergence-matrix-systematic` (Activity 3; PM-session routing 2026-07-19, PRD-prove-and-surface §Routing / FR-7·FR-8·FR-9)

- **Live approval-state convergence coverage** · Two MORE escaped defects of the SAME class found
  2026-07-18 in continued operator play: an approve/reject op reached the receiving device but the
  client re-rendered from a **stale `MY_SUBMISSIONS` cache** instead of reconciling the changed
  submission status. Symptoms: (1) a manager's rejection reason never reached the submitter's other
  device live (`applyOp` `REJECT_ITEM` only refreshed the Approvals tab — a no-op for a non-approver),
  and (2) an observer's list count stayed frozen on the pre-rejection submission **snapshot**
  (`getProgress` counts `submission.responses` while status is pending/submitted/approved). Fixed
  red-first (broad refresh-on-op: `applyOp` routes `APPROVE_ITEM`/`REJECT_ITEM` through
  `loadMyChecklists` so every receiving device reconciles submission status → correction banner,
  edit-vs-readonly mode, and list count all converge live). Regression tests: `tests/sync.spec.js`
  `RJT-LIVE-01/02/03`. **QA hole this widens:** the convergence matrix tested only `SET_FIELD`
  ops — never the *submission-lifecycle* ops (submit/approve/reject) cross-device, and never asserted
  that a status change reconciles the observer's list count / an open runner. Fold into the WO above:
  the access matrix must also vary the **op type** (field edit vs submit/approve/reject) and assert
  **live convergence of derived views** (banner, readonly, progress count), not just the field value.
  Full write-up: `reference/qa-gap-20260717-live-sync-access.md` (§ 2026-07-18 addendum).
  · origin: 2026-07-18 operator-found approval-sync bugs (dev play) · promoted → `convergence-matrix-systematic` (Activity 3; PM-session routing 2026-07-19, PRD-prove-and-surface §Routing / FR-8·FR-9 ESC-2)

- **Rejection feedback on SUB-STEPS (fixed) + the require-photo dead-end (open).** Operator play
  (dev, 2026-07-18) found rejecting a **sub-step** (e.g. "Cut the check → Do B") stored the comment
  + require_photo but rendered them NOWHERE — the runner drew the correction banner only at the
  PARENT field level (`REJECTION_FLAGS[parentId]`), sub-step rows had none, and `hydrateFieldState`'s
  field-id filter (`tplFieldIds`) never even included sub-step ids. Fixed red-first: shared
  `correctionBannerFor(id, resp)` helper renders on parent AND sub-step rows, and `tplFieldIds` now
  includes sub-step ids (both open paths). Test: `tests/workflows.spec.js` `APR-SUBSTEP-0718`.
  **RESOLVED 2026-07-18 — the require-photo DEAD-END + correction-photo slot.** A non-photo field
  rejected with `require_photo=true` used to show "📷 Photo required before resubmit" with NO capture
  control, while both gates demanded the field's *value* be an `https://` URL — impossible for a
  checkbox (its value is boolean). Built a dedicated **correction-photo slot** (`CORRECTION_PHOTOS`),
  persisted by bundling `_correction_photo` into the saved value (separate from the answer, mirroring
  `_fail_note`): capture UI on any non-photo require-photo field, unpacked on hydrate (draft +
  submission) and in the manager review view, and honored by the frontend gate + a new backend
  `hasResubmitPhoto`. Also fixed a latent **hard-block**: a sub-step `require_photo` matched the
  backend resubmit gate but was unsatisfiable (sub-steps aren't sent as top-level responses) → the
  gate now excludes `parent_field_id` fields (sub-step require-photo is advisory). Tests: backend
  `TestResubmit_..._SucceedsWithCorrectionPhoto` + `..._SubStepRequirePhoto_NotBlocked`; E2E
  `APR-DEADEND-0718`; persistence `FLD-CORRECTION-PHOTO`. Parked-by-convention: the presign+PUT camera
  plumbing itself (injected in tests, like onboarding FR-18).
  · origin: 2026-07-18 operator-found sub-step rejection bug (dev play) · resolved 2026-07-18

> **Triage 2026-07-20 (overnight-20260721, ledger T-18) graduations.** §B items were routed at
> triage (B2+B3 → `replay-fetchstorm-gate` card; B4 ratified; B5 folded into
> `inventory-tab-gating`) and do NOT appear below. These are the run's §C durable observations
> that ride future cards:

- **`checklist_submissions.status` never set for `requires_approval:false` submissions** ·
  Defaults `'pending'` and `submitChecklist` never updates it, so no-approval submissions read
  `'pending'` server-side forever. ~~Harmless today (UI derives from other fields)~~ — **that
  premise was DISPROVEN by run 20260725: the stuck `'pending'` was load-bearing.** It hit the
  `isPending` branch and rendered wrong-but-present copy, so removing it without a client change
  left the checklist editable and re-submittable. · origin: overnight-20260721 A1 impl (§C) ·
  **promoted → `workflow-submission-status-default`** (server half, DONE 2026-07-25) **+
  `workflow-submission-status-client-half`** (split out at morning triage 2026-07-25, ledger
  T-22 decision 49)
- ~~**Rejected-field hydrate quirk: new answer visually clears on reload until resubmission**~~ ·
  Answering a rejected field then reloading blanks the new answer visually
  (`workflows.html:1544` hydrate branch prefers the rejection snapshot); device-local, LOW.
  **dropped — superseded by the RxDB/Supabase migration** (roadmap Activity 1): a symptom of the
  manual-hydrate mechanism being replaced, not an independent fix. · origin: overnight-20260721
  A1 G6 (§C) · dropped, `/nc-roadmap-round` 2026-07-25
- **`sync.spec.js` de-flake: `:1198` + `:525`** ·
  `:1198 temperature answer converges` is **flaky — confirmed by controlled reproduction, at
  ~16% overall (4 red / 25 `--retries=0` legs), ~20% (4/20) under a concurrent Playwright
  suite** (investigation 2026-07-21, `investigate/1198-under-load-20260721`). The flake is
  REAL; the numbers previously carried here were not. **Two corrections to the prior claim:**
  (a) "red 1-of-2 legs at load 0.84" **misattributed the load** — its own source table
  (`runs/2026-07-22-autonomous/DECISIONS-NEEDED.md` §S1 tail) shows load 0.84 on the leg that
  **PASSED**; the red leg started at load **3.96** on a rising box. (b) The implied ~50% rate is
  refuted — two parties' ~20 consecutive greens are consistent with ~16-20% *conditional on
  contention* (p(0 red in 20 | 20%) ≈ 1.2%, and they sampled the quiet condition).
  **Failure mode is NOT what the card assumes.** All 4 reds share one signature:
  `page.waitForResponse` timeout at **`sync.spec.js:1119`** — the `POST /ops` **commit** wait —
  not `CONVERGE_TIMEOUT` and not a convergence assert. The autosave debounce is **400ms**
  (`workflows.html:278`), so a 12s timeout means the op **never fired**, not that it was slow;
  ops-journal deltas confirm it (green legs +5 rows, red legs +3 — no `SET_FIELD` row).
  **Therefore raising any timeout is the wrong fix.** The likely mechanism is the same stray WS
  catch-up `loadMyChecklists` re-render the de-flake comment names: it detaches the temperature
  input between `fill()` and `dispatchEvent('change')`, so the change handler never arms
  `debouncedSaveField` and no POST is ever issued. The de-flake **relocated** that race (from
  "typed value clobbered to empty" to "save never arms") rather than closing it — gating on the
  `POST /ops` 2xx cannot be race-free against a re-render that prevents the POST existing.
  Fix direction: make answer-entry re-render-safe (settle the runner before entering, and/or
  re-assert value + re-dispatch if no POST is observed). Test-side; no production change needed.
  **Scope must also include `:525 FLD-LIVE-02`**, which G6
  found fails 3/3 in isolation *and at the pre-gate baseline* — a pre-existing
  order-dependent test. The flake surface is broader than "just `:1198`". ·
  origin: overnight-20260722 S1 PARK (b) + quiet streak ·
  **evidence re-derived 2026-07-21** — see `reference/1198-flake-reproduction-20260721.md` ·
  **promoted → `syncspec-deflake` (D1, slate-20260720c)** — cleared the §15k architecture-blocking
  bar: the `cycle-gate` card promises no-retry suite-green attestation, which cannot pass while
  `:1198` is proven-flaky and `:525` reds at baseline.
- ~~**Replay fetch-storm class is NOT fully closed**~~ ·
  S1 gated `SUBMIT_CHECKLIST`, but G6's enumeration of every branch in `applyOp` found
  `loadPendingApprovals()` and `loadTemplates()` still fire an **ungated per-op re-fetch** —
  a catch-up with N APPROVE ops still storms the approvals queue, N SAVE_TEMPLATE ops still
  storm the Builder list. Same root cause, same one-line fix pattern, deliberate-by-omission
  (the in-code comments say "always refreshes"). **dropped — superseded by the RxDB/Supabase
  migration** (roadmap Activity 1): this is the same fetch-storm class the migration retires.
  **Architectural argument for why it cannot recur:
  `designs/fetchstorm-replay-class-superseded.md`** (written 2026-08-02 for Eng KR1, which admits
  one reviewed note per item where no test is constructible — `sync.js` is deleted by
  `sync-hard-cutover`, taking the trigger and the observable with it).
  · origin: overnight-20260722 S1 G6 · dropped, `/nc-roadmap-round` 2026-07-25
- **`sync.js api()` fetch-abort guard (S1 sub-move (d), deliberately skipped)** ·
  Suite-teardown noise (`loadMyChecklists error: Failed to fetch`) originates from a `catch`
  in **`workflows.html:389`**, outside S1's footprint. A sync.js-only fix would require
  `api()` to never reject during unload, altering the error path of every workflow API call
  including the offline-queue fallback — poor risk/benefit unattended. Needs re-scoping as
  its own card **with `workflows.html` in footprint**. · origin: overnight-20260722 S1 (d) · new
- **`.gitignore` lets a `node_modules` symlink into the index** ·
  The line is `node_modules/` (trailing slash) — matches a directory but **not a symlink**.
  Worktrees have no `node_modules`, so symlinking the main install is the natural move and it
  slips straight past into `git add -A`. One implementer already hit it and reverted its own
  instance. One-char fix: drop the slash. · origin: overnight-20260722 S1 flag · new
- **`:8199` port latch recurred (third run)** ·
  A `go run` child survives `kill` of its parent and holds the port. Standing recipe should
  kill the **listener PID** (`ss -ltnp`), not the `go run` parent, and concurrent tracks
  should be assigned distinct `TEST_PORT`s up front. · origin: overnight-20260722 S1 flag · new
- **`-p 1` is load-bearing for Go suites — write it into standing run mechanics** ·
  Verified at base commit (no card code present): default parallel `-p` reddens four packages
  (`inventory` 6, `purchasing` 4, `receipt` 9, `recipes` 5+) via concurrent `TRUNCATE`s on a
  shared DB. `-p 1` is green. This makes every card's build/vet signal unreliable until
  discovered per-card. · origin: overnight-20260722 F1 G6 · new
- **Money is `float64` end-to-end in the inventory/recipes path** ·
  Pre-existing repo convention (`period-summary` too), not a card regression. JSON can emit
  `23.099999999999998`, and it compounds per-bucket rounding drift. Cents-as-int or a decimal
  string is the repo-wide correct fix. · origin: overnight-20260722 F1 G6 · new
- **`git stash` prohibition needs a mechanical guard** ·
  A subagent ran `git stash` in a worktree (forbidden, 07-15 hazard), self-disclosed, and
  recovered cleanly; the operator's own stash entry was verified intact. But the rule lives
  only in prose. Consider a guard refusing `stash` when `git rev-parse --git-common-dir`
  differs from `--git-dir`. · origin: overnight-20260722 F4 incident · new
- **Dangling standing-rules pointer in every slate since 07-15** ·
  Slates inherit gates G1–G6 "unchanged by reference from
  `reference/overnight-run-plan-20260707.md`" — **that file does not exist.** Real origin is
  `runs/2026-07-09-attended/slate-20260710.md` §"Run mechanics" plus the app-code adaptation
  in `slate-20260714.md`. A dangling inherit in the one document defining the run's gates is a
  latent single point of failure. · origin: overnight-20260722 orchestrator · new
- **PRODUCT THREAD: the tabs show single numbers where the operator wants comparisons** ·
  Two triage answers converged on the same shape. (1) Food cost should be a **long-term
  average with a direction of travel**, not a fixed-12-week snapshot — this dissolves the
  0%-food-cost bug rather than patching it. (2) **Margin with and without discounting** —
  blocked today: `daily_menu_sales` stores only `units_sold` + `gross_amount`, no discount or
  comp field, so it needs Toast sync to capture them first. Not cleanup; belongs in a PM
  session. · origin: overnight-20260722 triage T-19 · new

---

## Graduated / added at morning triage T-20 (2026-07-21, overnight-20260720c)

- **Prod, dev and test share ONE Postgres cluster, one role, one password** ·
  `yumyums-dev-pg` holds database `yumyums` with schema `public` (dev, 50 tables) AND schema
  `production` (prod, 48 tables), plus databases `hq_test_go` / `hq_test_e2e`. Prod is separated
  from dev by nothing but a client-supplied `search_path=production` in a connection string — not
  an enforced boundary, no privilege separation, same `yumyums` role for both. Omit the parameter
  and you land in `public`. This is the sharpest instance of audit surface #4 and was found while
  answering the operator's "should never be writing to the live db!". Correct fix is a genuinely
  separate cluster (or at minimum separate roles with `REVOKE`), not more `search_path` discipline.
  **Cross-ref: the enforcement prerequisite for NF-7** (`design-findings-nightcrew.md`
  §"Enforcement is capability, not detection") — a coordinate guard (the `:5433` refusal) cannot
  substitute for a real prod/dev credential boundary; while dev and prod are the same role on one
  cluster there is no separate prod credential to hide from the agent, the operator's stated correct
  fix (2026-08-09). · origin: T-20 triage investigation · **new, HIGH**
- **`dotenv: ['backend/.env']` injects 21 LIVE credentials into every task from the main checkout** ·
  Root `Taskfile.yml:3`. A dev server (PID 75921) ran three days holding live Mercury *production*,
  Anthropic, Zoho Cliq and SMTP keys with `E2E_DISABLE_SCHEDULERS` unset — receipt worker, cutoff,
  drift and alert queue all armed against real external services. Triage blanked the Cliq/SMTP vars
  for the *Playwright* server (decision 40), which does NOT address `task dev` or any other target.
  Proper fix: invert the default so alerts require an explicit `ALERTS_ENABLED=1` and forgetting a
  flag fails safe. **Cross-ref: the enforcement prerequisite for NF-7** (`design-findings-nightcrew.md`
  §"Enforcement is capability, not detection") — broadcasting live prod credentials into every `task`
  is the opposite of "prod creds hidden from the agent"; capability-based prod isolation cannot exist
  while `dotenv` injects them universally (operator direction, 2026-08-09). · origin: T-20 / audit
  surface #5 · **new, HIGH**
- **`task backend:db-start` is vestigial and now conflicts** · It creates a second HQ Postgres
  container (`yumyums-pg`, which does not exist today) and, after decision 39 templated its host
  mapping to `{{.DB_PORT}}`, would collide with `yumyums-dev-pg` on 5433. Either delete the target
  or point it at a genuinely separate port. Not redesigned at triage. · origin: T-20 · new
- **Stale server processes squat ports across sessions** · PID 83061 holds `:18484` from a worktree
  (`hq-worktrees/sync-units`) that has been **deleted**, pointed at a dead Postgres port. Same shape
  as the `:8199` latch (audit #2, fixed by decision 41) with a different number. A general reaper /
  port-ownership convention is the durable fix. · origin: T-20 · new
- **`stash@{0}` holds unattributed WIP in a slot shared by five worktrees** ·
  `WIP on dev: acd2c7f refactor: migrate all server logging from log to slog NDJSON output`.
  `refs/stash` is shared across all worktrees, which is why `git stash` is already prohibited here.
  Confirm ownership before anything touches it. Still present after triage. · origin: audit #6 · new
- **Two config files carry independent copies of the same default** · `backend/Taskfile.yml` and
  `playwright.config.js` each define their own `DB_PORT` / `dbPort` default. Changing one silently
  leaves the other — which happened at T-20 and cost a full E2E leg. Both now cross-reference each
  other in comments, but the duplication itself is the defect. Audit for other duplicated defaults.
  · origin: T-20 reviewer error · new
- ~~**`sync.spec.js` de-flake: `:1198` + `:525`**~~ · **promoted → `syncspec-deflake` (re-aimed at
  T-20)**. Scope widened to four tests: `:1198`, `:525`, LST-17, GATE-04. Fix is test-side; no
  production change; no timeout increase helps. **Shipped overnight-20260724 (S1). Line anchors
  are now dead** — G1/S1 moved the tests; locate by title (`-g "temperature answer converges"`,
  FLD-LIVE-02 by name). T-21.
- ~~**Cost-tab gate: confidentiality or tidiness?**~~ · **promoted → `grant-enforcement-parity`**
  (T-20 decision 36). Answer was confidentiality, and the gap is 9 of 11 grants, not 2 routes.
- **`/photos/*` key-binding gate (G1-a)** · The photo presign/upload endpoints are the sole
  authenticated-only routes — any logged-in user with a photo key can read any stored photo URL
  regardless of grants. A union-of-app-grants gate was rejected as cosmetic (T-21 decision 42);
  the durable fix binds photo keys to their owning app/record so per-app grants actually gate
  reads. Needs a small design pass (key namespace vs owner-record join). The documented
  exception in `tests/grant-enforcement-parity.spec.js` stands until this ships. · origin:
  overnight-20260724 G1 park, T-21 decision 42 · new
- ~~**`sync.js` catch-up fetch-storm gate**~~ · `applyOp`'s SAVE_TEMPLATE branch re-fetches
  `myChecklists` per replayed op whenever a runner is open (`sync.js` ~491) — a device catching
  up on a large journal fires an un-awaited fetch per op (the storm behind every FLD-LIVE-02
  red). A one-line gate/debounce fixes the app-level behavior, but it is a production `sync.js`
  change and RE-ARMS the attended two-device convergence check — schedule it with that cost
  priced in. **dropped — superseded by the RxDB/Supabase migration** (roadmap Activity 1):
  `sync.js` is retired by `sync-hard-cutover`, not patched.
  **Architectural argument for why it cannot recur:
  `designs/fetchstorm-catchup-gate-superseded.md`** (written 2026-08-02 for Eng KR1, same admission
  — and it records the subtlety this entry's one-liner hides: the branch *is* gated, on view state
  rather than op liveness, so the runner-open arm at `sync.js:491` never consults `silent`).
  · origin: overnight-20260724 S1
  observation (reported, not fixed) · dropped, `/nc-roadmap-round` 2026-07-25
- **`onboarding.spec.js` second-run carried-DB failures** · Two tests fail when the full suite
  runs twice against the same un-reset DB (carried hire/training state). Fine under the
  clean-DB-per-leg rule, but the suite is non-idempotent — either reset state in `beforeAll` or
  document clean-DB as a hard precondition. · origin: overnight-20260724 S1 (pre-existing, out
  of scope) · new
- **Gated-tab grants: granular-overrides-umbrella semantics** · Reverse the T-18 umbrella
  rider for slug-bearing tabs, per the operator's play-test ruling (T-21a decision 45,
  verbatim): "If there is a granular permission for a tab and it does not exist, the tab
  should not be visible. If no granular permission exists, then the tab should be visible by
  default." Concretely: `inventory-trends`/`inventory-cost` require their explicit grant —
  the `inventory` app grant no longer implies them; un-slugged tabs stay app-grant-covered.
  Red-first regression test (Jim B scenario: app grant, no tab grant → tab absent + endpoint
  403) BEFORE the fix; then drop the umbrella arg from the two RequirePermission mounts, drop
  the `'inventory'` disjunct in `hasTabGrant` (inventory.html), flip the umbrella-direction
  tests (incl. G6's grant-parity pairs). Ships as a patch release. NOT urgent — no crew
  accounts hold prod grants today. · origin: operator play-test 2026-07-24, T-21a decision
  45 · new
- **Cross-user checklist hydration divergence (approved-vs-rejected ghost state)** · Two users
  viewing the SAME checklist render different states as a function of their own last
  submission: a rejected submission resurrects as the viewer's current 2/2 state while the
  other user (whose copy was approved) sees fresh 0/2 with clicks silently no-oping (no POST,
  no toggle, no feedback). Reproduced headlessly in fresh contexts — deterministic hydration
  logic (`MY_SUBMISSIONS`-driven), NOT network/cache/gating/the 07-22 sync.js change; server
  state verified byte-identical for both users. The E2E convergence matrix misses the cell:
  it never seeds an asymmetric approved-for-A/rejected-for-B history before reopening.
  ~~Needs a product ruling first (what SHOULD each user see on a new cycle after a split
  approve/reject?), then a red-first cell + fix.~~ **RULED 2026-07-26 — the product half is
  cleared; what remains is a red-first cell + fix.** Full evidence + repro:
  `reference/sync-crossuser-hydration-20260724.md`. · origin: operator play-test 2026-07-24,
  reproduced at triage · **ruled 2026-07-26 (operator-decided, ledger T-24 decision 67)** —
  *"A new cycle starts fresh for every user — 0/2 for both A and B."* Three requirements, and
  they are separable: (1) B's rejected submission does **NOT** resurrect as current state — it
  is archived and remains visible as **history**; the rejection and its fail notes are a record,
  not a live draft. (2) A's fresh 0/2 **must accept clicks** — *"the silent no-op is a bug, not
  intended behavior — it is part of what this ruling requires fixed, not a separate concern."*
  (3) The E2E convergence matrix still needs its missing cell seeded: an asymmetric
  approved-for-A / rejected-for-B history, reopened as both users, asserting 0/2 for both **and**
  asserting that A's clicks POST — a test that only checks the counter would pass against a dead
  UI. Rationale, verbatim: *"rejection means redo"* — a rejected checklist that carries forward
  as 2/2 lets unreviewed work look complete, which is precisely the failure the engine exists to
  prevent. Ruling defines the target state, **not** the work: the build is unwritten and is
  slated as Night B card **P4** (`reference/slate-20260802.md`), not yet authored. Unblocks the
  product half of `sync-hard-cutover`'s double block (Product KR2); that card remains blocked on
  `sync-rxdb-schema-and-replication` landing.
- **Cost tab 0%-food-cost anomaly investigation** · F2's open note (T-19 decision 33 residue,
  disposition T-21f decision 48): a menu item can show 0% food cost. Lead hypothesis: sales
  without any recipe allocation → ingredient_cost_total = 0 → a technically-correct but
  misleading 0% — wants an explicit "unallocated" marker distinct from genuine 0%, not a
  formula fix (the margin math is fixture-proven). Investigate once prod sales sync lands and
  Cost carries real rows. · origin: F2 open note, routed T-21f · new
- **`workflows.html` sync: migrate to RxDB + self-hosted Supabase, unify autosave + live
  broadcast into one store** · The hand-rolled WebSocket + Postgres LISTEN/NOTIFY +
  Lamport-clock op log (`sync.js` + `backend/internal/sync/`) is the repeat offender behind
  this cycle's own fragility findings above (`sync.js` catch-up fetch-storm gate,
  cross-user checklist hydration divergence, the retired-but-recurring `sync.spec.js`
  flakiness) — a "fetch storm" bug class (T-18) where reconnecting clients replay their full
  missed-op history and naive per-op refetches (`loadPendingApprovals`, `loadTemplates`)
  fire redundant requests and clobber optimistic UI mid-edit, plus a standing bug where
  manager rejections never reload into client state on refresh. Decision (operator explore
  session 2026-07-24): replace BOTH write paths in `workflows.html` — the field-level
  `autoSaveField`→`POST /saveResponse` draft path [🛑 **corrected 2026-08-04, B-65:** that is
  `debouncedSaveField`→`submitOp('SET_FIELD')`→`POST /ops`; `autoSaveField` is defined nowhere and
  no frontend code posts to `/saveResponse`. There is ONE frontend write path, not two — the
  "field-level draft path" and the "ops/broadcast layer" named here are the same channel]
  AND the live ops/broadcast layer — with a
  single RxDB client store replicated against a **self-hosted** Supabase stack (Realtime +
  PostgREST) running alongside the existing Postgres on the Windows box. Last-write-wins
  conflict resolution (checklist edit conflicts are rare; no custom conflict handler needed).
  Auth: bridge the existing bearer-token/session auth rather than adopt Supabase Auth/GoTrue —
  the Go backend mints its own HS256 JWTs (`role: authenticated`, `sub`, `exp`) signed with
  Supabase's configured `JWT_SECRET`, which self-hosted PostgREST/Realtime accept for RLS
  without GoTrue. Cutover: hard swap, no parallel run — `sync.js`, `backend/internal/sync/`,
  and `/saveResponse` retire entirely once live. Feasibility confirmed by research
  (2026-07-24): RxDB's Supabase replication plugin is Cloud-agnostic (talks only to
  `@supabase/supabase-js`, i.e. PostgREST + Realtime, both in the self-hosted Docker stack);
  self-hosted-specific gotcha is each synced table must be manually added to the
  `supabase_realtime` publication (no dashboard toggle) and needs a text PK + `_deleted` +
  `_modified` trigger + RLS enabled. Scope is `workflows.html` only — inventory, purchasing,
  onboarding, and users stay on the existing Go+Postgres REST API. Promotion should start
  with a feasibility spike (stand up self-hosted Supabase in Docker alongside the existing
  Postgres, prove the Go-minted-JWT → RLS bridge end-to-end) before sizing the full
  migration. · origin: operator explore session 2026-07-24 · **promoted → `sync-rxdb-feasibility-spike`,
  `sync-rxdb-schema-and-replication`, `sync-jwt-bridge-endpoint`, `sync-hard-cutover`** (roadmap
  Activity 1 "Sync foundation", `/nc-roadmap-round` 2026-07-25)

<!-- Handled entries start here. The 63 entries above predate the B-NN requirement and are why
     `night-crew backlog check --file` currently rejects this document (220 issues). New entries
     carry handles from 2026-07-25 forward; migrating the historical ones is B-02. -->

- **B-01 · Repair the W1 Supabase runbook's fabricated presentation** — `.night-crew/qa/spike-supabase/README.md` asserts twice (`:32-34`, `:724-727`) that its output blocks are "real captured output, not a reconstruction"; six blocks are hand-composed. All underlying facts re-verify and W1's GO stands, so this is a document-integrity repair, not a verdict change. · _morning triage 2026-07-25_ · new · lead: ten `HTTP nnn` annotations sit on `curl` calls carrying no `-w`/`-i`/`-D -`; `rtwatch` RECV lines are stripped of the `topic=` column `main.go:144` always prints; the `(5 more alice rows)` elision at `:821` implies 6 where the DB held 8 — re-run each block with real capture flags and paste what comes back.

- **B-02 · Migrate `BACKLOG.md` to B-NN handles so the validator does something** — 63 historical entries carry no handle, no one-line description and no lead, so `night-crew backlog check --file .night-crew/knowledge/BACKLOG.md` fails with 220 issues and gives us no signal at all. · _morning triage 2026-07-25_ · promoted → backlog-machine-migration · lead: run the checker, take its per-entry complaints as the worklist, and assign handles in file order from B-03 up; the "unrecognized status" complaints are mostly rich promoted-→ strings the grammar does not accept, so decide whether to simplify them or widen the grammar before bulk-editing.

- **B-03 · Route the run's gray areas through the decisions resolver** — `night-crew decisions audit --repo . --run 20260725` reports "no gray areas routed through the resolver yet", so preference coverage is undefined rather than low; the run parked its forks straight into `DECISIONS-NEEDED.md`. · _morning triage 2026-07-25_ · new · lead: the four forks this run parked are exactly the material the resolver wants — wire the park path to route through it so the coverage number has a denominator, otherwise the audit stays decorative every cycle.

- **B-04 · Watch `tests/purchasing.spec.js:1407` (FR-13)** — one flaky observation during run 20260725: died on `waitForLoadState('networkidle')` at the 30 s timeout, passed on retry, measured load 1.58 → 4.20. Deliberately not attributed — one observation is not an attribution. · _morning triage 2026-07-25_ · new · lead: if it reds again, re-run it in isolation on a quiet box before attributing; `networkidle` under a loaded box is the obvious suspect and is a different fix from a real product race.

- **B-05 · Humanize the History view's raw status token** — `workflows.html:2503` renders `escapeHtml(s.status || '')`, so a no-approval submission's history row shows the literal lowercase `completed` as user-facing copy. Cosmetic, and an improvement on the pre-card `pending`, but it is the eighth status-reading call site where the slate named seven. · _morning triage 2026-07-26 (T-23 decision 63)_ · new · lead: a small status→label map, folded into whichever card next touches `workflows.html`; the card's own framing "teach the client the DB's vocabulary" already covers it.

- **B-06 · Fix the stale comment at `tests/sync.spec.js:1584`** — reads *"requires_approval false → submit yields `'submitted'`"*; the server yields `'completed'`. It sits inside one of the status card's two acceptance specs. · _morning triage 2026-07-26 (T-23 decision 66)_ · promoted → `sync-rxdb-schema-and-replication` · lead: one line; fold into the next card that touches `sync.spec.js` rather than opening a card for it — assigned as that card's obligation 8 at morning triage 2026-07-27, after Card B did not touch the file and the item spent a second night waiting for "the next card".

- **B-07 · Re-measure leg 4 handover with `performance.now()`** — `browser/specs/leg4-leader-election.spec.js` uses `Date.now()` deltas on a box that steps its clock; G6's own run printed **−1545 ms** for the same measurement, so the quoted 47/65/87 ms are not measurements. The qualitative findings (one leader, follower silent, survivor replicates) are ordering facts and are unaffected. · _morning triage 2026-07-26 (T-23 decision 64)_ · new · lead: strike the three figures from card C's verdict and say "sub-second"; switch the spec to `performance.now()` before anyone quotes a number from it again.

- **B-08 · Merge-intents must disclose deviations at the time, not at triage** — card C's merge-intent promised `browser/` would carry its own `package.json`, `node_modules/` and lockfile; as built, `browser/` has zero dependencies and no lockfile and a new `vendor/package.json` + lockfile was created instead. The as-built design is better and is accepted (T-23 decision 65) — the defect is that the divergence was undisclosed, which devalues every future merge-intent. · _morning triage 2026-07-26_ · new · lead: process, not code — add a deviation line to the merge-intent template so "as-built differs from intent, here is why" is a field someone must fill rather than remember.

- **B-09 · `task test:` omits the `bdd:gen` dep, so every worktree silently runs 19 of 20 spec files** — reproduced at morning triage: a fresh worktree lists `Total: 559 tests in 19 files`, the main repo `560 in 20`; running `npx bddgen` regenerates `.features-gen/features/user-invite-onboarding.feature.spec.js` **byte-identical** and the count goes to 560/20. `task bdd:gen` already exists and `task bdd` / `task test:all` already depend on it — `task test:` (`Taskfile.yml:28-30`) is the one target that does not, so every card leg reported a full suite in good faith while a whole project contributed zero tests, with no error and no skip line. · _morning triage 2026-07-27 (T-25 decision 73)_ · **promoted → `test-harness-fail-loud`** (slate-planning session 2026-07-28, ledger T-27 decision 90 — §15k architecture-blocking bar: `sync-rxdb-row-visibility-rls`'s security gate cannot be trusted under a harness with this property). **✅ LANDED 2026-07-28 on `overnight-20260729-2`, merged to `dev` 2026-07-29 as `f35fa56`** — `bdd:gen` is in `test:`'s deps and the suite now resolves **21 spec files / 597 tests**, re-verified by adversarial re-execution at triage. 🛑 **Residual: the detection half is NOT closed — see B-23.** The floor that was supposed to announce the next silent-green is a hand-typed 20 and the tree is now at 21, so an empty Playwright project still reports PASS. The dep is fixed; the guard against its recurrence is not. · lead: one-line Taskfile dep, plus make an empty Playwright project a hard error so the next silent-green announces itself.

- **B-10 · The `await` on `clearApiCache()` has zero coverage** — dropping the `await` (keeping the call) leaves `tests/index.spec.js -g "logout deletes api-cache" --repeat-each=3` at 3 passed; deleting the call entirely reds 1. So the suite proves the call happens, not that it completes before `window.location.href` tears the page down — and both merge-intent A item 1 and the code comment name the await as "the entire disclosure". · _morning triage 2026-07-27 (T-25, adversarial finding F-1)_ · promoted → client-guard-coverage · lead: a test that resolves `caches.delete` slowly and asserts the redirect has not fired yet; fold into whichever card next touches `index.html` — likely `sync-rxdb-schema-and-replication` via decision 70.

- **B-11 · Merge-intent notes must be re-read WHOLE after a repair, not appended to** — Card B's note disclosed its `sync.js` edit correctly in *Late additions*, then left "**`sync.js` was NOT edited**" standing two lines below it and "**No change to `sync.js`** — not `drainQueue`, not the `duplicate_submission` arms" standing under *Not done, deliberately* — enumerating precisely the two things the repair changed. A merger resolving a `sync.js` conflict against that section would have dropped the `queuedAt` sort the note's own *Late additions* says to keep. This is B-08's lesson one notch down: the deviation WAS disclosed at the time; the contradicting claims elsewhere in the same note were not retracted. · _morning triage 2026-07-27 (T-25 decision 76)_ · new · lead: make the repair leg re-read the whole note and strike contradicted lines rather than only appending — a note that contradicts itself is worse than no note, because the merger trusts it unattended at 3am.

- **B-12 · hq's BACKLOG.md fails `night-crew backlog check` — 220 issues across 71 entries** — only B-01…B-08 use the validating shape (`**B-NN · Title** — description · _origin_ · status · lead:`); roughly 63 legacy entries use the retired `title · description · origin · new` shorthand, with missing handles, missing leads, and free-text statuses the validator rejects. Pre-existing and not caused by any one run, but it means the store cannot be machine-read and new items are being appended to a document that does not validate. · _morning triage 2026-07-27_ · promoted → backlog-machine-migration · lead: a mechanical migration card, not a triage edit — rewrite the legacy entries into the current shape, assign handles in file order, and gate it on `night-crew backlog check` exiting clean.

- **B-13 · CLAUDE.md says `task prod:deploy` runs `task sw`; the Taskfile does not** — `Taskfile.yml:174-210` does a `git reset --hard origin/main` then `docker compose build`, so the **committed** `sw.js` is what ships and nothing regenerates it on the box. Harmless while `sw.js` is always committed with its source, and load-bearing for B-09's sibling hazard: it is why a staged-but-uncommitted file baked into `sw.js` reaches production. · _morning triage 2026-07-27 (T-25 decision 67, aside)_ · promoted → P1 `build-deploy-manifest-integrity` (slate-20260802). **✅ LANDED 2026-08-01 on `card/p1-build-deploy-manifest-integrity` (`f635d98`), run `overnight-20260802`** — **the slate DECIDED the fork the lead names: the Taskfile is right and the doc is wrong**, under the delegation principle. `precache-manifest-from-head` made `build-sw.js` read git HEAD, and the prod clone's HEAD after `git reset --hard origin/main` **is** the tree being built, so regenerating on the box could only reproduce the committed file — the dep would be redundant by construction, and `sw.js` committed with its source is the correct contract. 🛑 **Flagged for ratification at triage; do not re-argue it.** The fix corrected four further false claims in the same block, none of them B-13's: there is no SSH and no Tailscale hop (prod runs on this box from a clone pinned to `origin/main`), `task prod:ssh` and `PROD_SSH` do not exist, and the container default is `yumyums-prod` not `yumyums-hq`. `task prod:rollback` and `PROD_COMPOSE` existed and were undocumented. · lead: fix the doc to match the Taskfile, or add the dep — but decide which is intended before editing either, since `precache-manifest-from-head` changes what "correct" means here.

- **B-15 · Build the SW-enabled two-context harness that shrinks the attended two-device check** — the attended check is carried on every run because nothing automated can see a *running* service worker: `playwright.config.js:60` sets `serviceWorkers: 'block'` repo-wide, no spec overrides it (`test.use(` appears in zero test files), and `sync-rxdb-feasibility-spike`'s leg 3 already rules that the repo-wide setting must **never** change. So SW install, SW update → `controllerchange` → `ptr.js` reload, and offline-shell serving are proven by exactly nothing — `tests/sw-manifest.spec.js` asserts the precache manifest as a *file* and never installs one. Meanwhile the app-layer half is strong and green: re-measured 2026-07-27 on `dev` @ `937543a` at `--retries=0`, fresh DB per leg — `sync.spec.js` **58/0** (6.4m, 29 two-context pairs), `broadcast-rerender.spec.js` **6/0** (2.7m), offline queue `-g "offline|DBL"` **6/0** (2.6m), both armed flakes (`:446` LST-17, `:1198`) green on a quiet box. The gap is transport-layer, not app-layer. · _operator session 2026-07-27, after re-measuring the automated half against the standing flag_ · new · lead: a **separate** Playwright project/config (own `serviceWorkers: 'allow'`, own port + DB, repo-wide block untouched) driving two contexts against a real `sw.js` — assert install, assert a rebuilt `sw.js` drives `controllerchange` → reload, assert the offline shell serves, then run the convergence + offline-drain cells through it. Two scheduling notes: (a) `sync-rxdb-feasibility-spike` **needs this same harness** for its leg 3 (a network-first API fallback answering a *replication* request with cached JSON), so build it once, there, rather than twice; (b) frame it transport-agnostically — the cycle's hard cutover retires `sync.js`, and a harness written against today's op-log transport would be half-invalidated by it, whereas an SW+convergence harness is exactly the regression net you want *across* the swap. Residual after this lands: iOS-standalone-on-prod only — procedure in `reference/attended-two-device-check.md`.

- **B-14 · The morning-triage G4 discipline greps are VACUOUS in hq and read as clean** — the ritual greps `internal/journal` and `internal/workorder`; hq's Go tree is `backend/{cmd,internal}` and **neither package exists**, so the greps return empty because there is nothing to find. Any run or triage reporting them "clean" reports a vacuum — the same silent-green class as B-09, one layer up in the tooling. **Reconfirmed at morning triage 2026-07-29 and reported as N/A rather than PASS** — the adversarial reviewer was briefed to say so explicitly and did; the triage merge commit records them as a vacuum. Third consecutive triage carrying this. · _morning triage 2026-07-27 (T-25 decision 75)_ · promoted → gate-ladder-completeness (roadmap round 2026-08-05) · **📝 RECORDED 2026-08-06 on `card/a4-gate-ladder` (run `20260806`) — and deliberately NOT fixed. This entry stays OPEN.** The card's mandate was to record it where the ladder's readers will see it, not to remedy it: the remedy binds the **night-crew clone**, and an hq run branch must never touch that repo. `.night-crew/knowledge/reference/gate-ladder.md` now carries §"The morning-triage G4 discipline greps are VACUOUS in this repo (B-14)" with the standing hq obligation (**write `N/A-VACUOUS`, never `clean`, never `PASS`**) and the clone-side destination named so it cannot be lost. 🛑 **Two corrections this entry did not have.** (1) The greps are not literally `grep internal/journal` — they search night-crew's journal/work-order **vocabulary** (`Outcome: "`, `journal.Entry{`, work-order status literals) across `internal cmd` while *excluding* those packages; hq has neither the vocabulary nor the excluded packages, so the vacuum is doubly deep. (2) **Verified by execution, both ways a triage might run them:** from the hq repo root, `ugrep: warning: internal: No such file or directory` / `cmd: …`, **exit 2** — the search paths themselves are absent (hq's are under `backend/`); from `backend/`, **empty, exit 1** — clean-looking and every bit as vacuous. Neither shape can be distinguished from a genuine clean by anything the ritual reads. **Destination, precisely:** the night-crew clone's `.claude/skills/nc-morning-triage/SKILL.md` — the G4 discipline step (three greps, ~`:139-142`) **and** the triage merge-commit template later in the same file (~`:201`), which emits the literal string `G4 discipline greps clean.` Fixing the greps without fixing the template leaves the false sentence in every triage commit. · lead: this binds the night-crew clone, not hq — carry it there; either make the greps assert the target packages exist first, or make them repo-conditional so an inapplicable gate reports N/A rather than PASS. **Both shapes must reach the merge-commit template too, not just the greps.**

- **B-16 · A reviewer dropping a card's test DB reads as a passing suite, not a broken one** — on `overnight-20260729` Card C's first G6 reviewer ran `DROP DATABASE hq_test_go_c` during its cleanup while the implementer was still using it; the implementer caught it only by questioning a suspiciously fast green. Mechanism, characterised by a later reviewer: `internal/sync/access_test.go:29,33` and `jwtbridge_test.go:169,173` `t.Skipf` on both connect *and* ping failure, `pgxpool.New` is lazy so a missing database surfaces at `Ping` — as a **skip** — and non-verbose output is a bare `ok ... 2.948s`, indistinguishable from a full run. Destroying the environment is therefore indistinguishable from passing it. This is B-09's silent-green class one layer down, in the harness rather than the task runner. Secondary: the host now carries ~30 `hq_test_*` databases from successive runs' reviewers, none cleaned up. · _overnight-20260729 closeout (incident during Card C review)_ · **(b) promoted → `test-harness-fail-loud`** (slate-planning session 2026-07-28, ledger T-27 decision 90). **(a) is NOT in the card** — it is standing G6 dispatch text, written directly into the `overnight-20260729-2` launch prompt, which is where this entry's own lead says it belongs. **(a) remains open as a standing-prompt obligation until it lands in the durable G6 dispatch text rather than one night's prompt.** Note: the skip-on-unreachable defect is **repo-wide**, broader than this entry states — measured 2026-07-28 across `recipes`, `workflow`, `inventory`, `receipt` and `sync`. **✅ (b) LANDED and independently re-verified at triage 2026-07-29:** all seven converted packages exit 1 under a set-but-unreachable `DB_TEST_URL` in three separate shapes (dead port, unroutable host, live server / missing DB), and skip-on-**unset** is preserved at exit 0 — so destroying the environment is no longer indistinguishable from passing it. Two things worth carrying: the admitted semantics change is real and complete (`internal/workflow` runs 35 tests live and **0** set-but-unreachable — a broken DB now yields *no* hermetic signal from those packages rather than partial signal), and *"zero DB-backed skips remaining"* is true **behaviourally** while **93 DB-gated `t.Skip` sites remain in source across 19 files** by design for the unset case — the phrase reads like deletion and is not. 🛑 **The guard over (b) is weak — see B-22.** **(a) remains OPEN.** · lead: two parts, and the second is the real one — (a) reviewer prompts must forbid dropping any database the reviewer did not create, which this run applied by hand from the second reviewer onward and which belongs in the standing G6 dispatch text rather than in each night's prompt; (b) make the Go suite **fail** rather than skip when `DB_TEST_URL` is set but unreachable, keeping skip only for *unset* — exactly the asymmetric gate Card C built for `HQ_SYNC_SPIKE_LIVE` in the same run (`proxy_live_test.go`, "flag set + port dead ⇒ FAIL"), so the pattern is already in-tree to copy.

- **B-17 · `build-sw.js` justifies a load-bearing flag with a claim that is empirically false** — `build-sw.js:39` (mirrored verbatim into `.night-crew/knowledge/roadmap.md:383`, so the false claim now lives in two places) says `--name-only` C-quotes any path containing "a space or a non-ASCII byte". G6 measured it in a throwaway repo: `git ls-tree -r --name-only HEAD` returns `a file with spaces.html` **unquoted** and only C-quotes the non-ASCII case — `core.quotePath` escapes non-ASCII and control bytes, not spaces. The `-z` flag is still correct and still necessary (for the non-ASCII case); only the stated reason is half wrong. Cosmetic today, but it is the comment a future reader will reason from when deciding whether `-z` can be dropped. Related and worth recording alongside it: `ls-tree HEAD` reads **local** HEAD while the image builds from `origin/main`, so committing a file locally, regenerating `sw.js`, and pushing only the `sw.js` commit still reproduces the 404 class — strictly tighter than the `git ls-files` it replaced, correctly out of `precache-manifest-from-head`'s scope, but the DONE record should not read as if the class is fully closed. · _overnight-20260729, G6 review of Card A (nits 1 and 2, non-blocking)_ · promoted → deploy-hygiene-honesty · lead: correct both copies of the sentence to name the real reason (`core.quotePath` escapes non-ASCII/control bytes, which is why `-z` is required), and add one line to the DONE card acknowledging the HEAD-vs-`origin/main` residual so it is not rediscovered as a regression.

- **B-18 · Two comments in `sync/proxy.go` describe code that does not exist or launder the evidence they record** — both surfaced by adversarial review of `sync-proxy-endpoint` after two repair rounds had already been spent, so they were carried out rather than fixed in-run. (a) `proxy.go:257-264` explains an `out.URL.RawPath = ""` assignment that **is not in the file** (`grep -n RawPath proxy.go` shows only reads). Harmless today because the stale RawPath disagrees with the prefix-trimmed Path, so `EscapedPath()` always falls back to re-escaping — but the hazard is the *obvious* fix: a refactor that trims RawPath in parallel with Path makes them agree again, `EscapedPath()` starts returning the caller's spliced bytes, and the wire path becomes attacker-controlled with the traversal check upstream none the wiser. (b) `proxy.go:203-205` logs the rejection with `r.URL.EscapedPath()` — the one function this card proved launders `%2f` — so an attacker sending `/sync/rest%2fadmin{` is recorded as `path=/sync/rest/admin%7B` and `reason=encoded_slash` is the only surviving signal. (c) Adjacent, deliberately deferred to protect that night's merge surface: `backend/cmd/server/main.go:436-438` reads as an all-clear ("inert until a deploy configures it") inches from the env-var names, with no "and do not configure it yet" — while setting them before row-visibility RLS lands grants every logged-in crew member read *and* write on the exposed schema. · _overnight-20260729, G6 final verification of Card C (nits N1-a, N1-b, F-3)_ · promoted → sync-doc-honesty · lead: (a) delete the comment or restore the assignment — do not leave a comment describing absent code in the one function whose false durable claim already cost a repair round; (b) log `r.RequestURI` or `r.URL.RawPath` alongside; (c) fold the `main.go` warning into whichever card next touches route registration.

- **B-19 · `unsubmitChecklist` orphans fail notes and nothing re-attaches them** — `repository.go:1281` detaches fail notes to `submission_id = NULL` on unsubmit; nothing ever re-attaches them and they carry no `answered_by`, so they leak permanently. Migration `0071`'s new unique index does **not** collide with them — Postgres treats NULLs as distinct — so the upsert work landed in `overnight-20260729` neither fixes nor worsens this. Confirmed genuinely pre-existing against `25fbc16` by adversarial review, i.e. not damage that run caused and reclassified. · _morning triage 2026-07-28 (T-26 decision 85, from Card B's disclosure)_ · promoted → P2 `workflow-unsubmit-failnote-reattach` (slate-20260802) · lead: decide the product question first — on unsubmit, should a corrective-action note survive to be re-attached on the next submit, or be discarded with the submission? The code currently does neither, which is the actual defect; fold into whichever card next touches `internal/workflow`'s submit/unsubmit path.

- **B-20 · `renderSyncBanner` paints the "Queued" badge onto Builder tab rows** — `sync.js:671` selects `[data-template-id]` document-wide, and the Builder tab renders those attributes too, so a template row in Builder gets a sync badge describing a queued *submission* it has nothing to do with. Reproduced in a browser by adversarial review (`builder=1 list=1 docwide=2`); byte-identical to base, so pre-existing. It is also what forced `[VOC-01]`'s locator to be scoped to `#checklist-list` during `overnight-20260729` — the test was fixed, the underlying selector was not. · _morning triage 2026-07-28 (T-26 decision 85, from Card B's disclosure)_ · promoted → P3 `sync-banner-builder-tab-scope` (slate-20260802) · lead: scope the selector to the checklist list rather than the document, then widen `[VOC-01]` back to document-wide so the test would catch a recurrence instead of being blind to it by construction. · 🛑 **DISPOSITION, run `20260803` card S1b `sync-hard-cutover` (which was to give B-20 its disposition — "fixed forward into whatever replaces the banner, or evaporated with the file"): NEITHER — it SURVIVES.** S1b **parked** and deleted nothing, so `sync.js:671` is byte-unchanged and the defect is exactly as filed. It did **not** evaporate with the file, and there is no replacement banner to fix it forward into. **Destination stays P3 `sync-banner-builder-tab-scope`**, which is the right home regardless — it is a two-line selector scoping that does not depend on the cutover and should not have been waiting on it.

- **B-21 · 14 of 33 commits on `overnight-20260729` carry a `Night-Crew-Card:` trailer git cannot parse** — a **blank line** between the trailer and `Co-Authored-By:` splits the trailer block, so `git interpret-trailers --parse` sees only the last paragraph. Affected: **all of Card C's commits**, **all four merge commits**, and the closeout — so `sync-proxy-endpoint` is entirely invisible to any trailer-parsing tooling, while all four merge-intent notes assert the trailer as a landed convention (textually true, mechanically false). Cards A and B each rewrote history mid-run specifically to fix this and Card C never did; the orchestrator's own merge commits then reproduced it. Triage declined to rewrite history — correcting four merge commits risks the record being preserved, for a gain that is cosmetic to tooling and zero to behaviour (T-26 decision 86). **Evidence 2026-07-29: the practice held for one night.** `overnight-20260729-2` has **0 of 32** commits whose trailer `git interpret-trailers --parse` cannot read, **all five merge commits included** — measured at triage, not by eye. 🛑 **Kept OPEN deliberately:** one clean night proves three agents got it right once, not that the emitter or the standing prompt text was fixed. The lead below is still unactioned. · _morning triage 2026-07-28 (adversarial finding F-1, undisclosed by the run)_ · new · lead: fix at the **emitter**, not the history — the trailer block must be emitted as one adjacent paragraph, and the standing card/G6/closeout prompt text should say so explicitly since three separate agents got it wrong the same way; optionally add a cheap post-merge check that every commit's trailer actually parses, because "the text is present" is what everyone verified and it is not the property that matters.

- **B-22 · `verify-test-harness.sh` Check B aggregates with OR, so six of seven packages can report `ok` on a dropped database and the gate still prints PASS** — the check runs one aggregate `cd backend && go test`, which is non-zero if *any* package fails, so reverting fail-loud in six of the seven converted packages (or in just `internal/inventory`) leaves `CHECK_B_AGGREGATE_EXIT=1` and the script reports harness OK with `ok github.com/yumyums/hq/internal/inventory 0.030s` sitting in a log it never reads. Only a 7-of-7 revert reds it. This is a stronger instance of the defect the card's own HANDOFF says it fixed in Check B2 — B2's unset direction genuinely aggregates AND across all seven. **The production fix is sound**: all seven packages exit 1 individually under three unreachable-DB shapes (dead port 110.7 s, unroutable host 109.7 s, live server / missing DB 0.02 s), so fail-loud does not depend on hanging. It is the guard that is weak. · _morning triage 2026-07-29 (T-28 decision 103a, adversarial finding F-1)_ · promoted → gate-harness-honesty (roadmap round 2026-08-05) · **✅ RESOLVED 2026-08-06 on `card/a2-harness-check-b` (commit `73547a0`, run `overnight-20260806`) — exactly the shape this lead specifies.** Check B now loops `for p in $DB_PKGS`, prints each package's exit status individually, and fails **naming every package that reported `ok`** — so the assertion is a conjunction over all seven rather than a disjunction that six reverts could satisfy. It **also asserts, as its own graded line, that it iterated `EXPECTED_DB_PKGS` (7)**, because `$DB_PKGS` is shared with Check B2 and silently trimming it would narrow both while both still printed PASS — the same defect one level up, so it is graded rather than trusted (`H1_DB_PKG_COUNT` overrides the constant to prove that assertion can go red). **Red demonstrated before the fix, not argued:** fail-loud was removed from six of the seven packages (`requested := dbURL != ""` → `requested := false` in the receipt/inventory/auth/purchasing `TestMain`s; `t.Fatal(testdb.Reason(…))` → `t.Skip(…)` in the recipes and sync helpers), leaving only `internal/workflow` honest; probing one by one read **1/0/0/0/0/0/0** while the aggregate check printed `PASS  go test exited 1`. The mutation was reverted — **no Go source is touched**, only `scripts/verify-test-harness.sh`. **Check B2 is deliberately unchanged** (the card's PARK trigger did not fire): its aggregate `-eq 0` is already a conjunction, so making B honest required no weakening of the over-correction guard. Cost is the live-server/missing-database DSN at ~0.02s per package — seven probes cost what one did; `H1_DEAD_PORT=1` stays opt-in. 🛑 **Status line written by `card/a4-gate-ladder`, not by A2** — A4 owns `BACKLOG.md` tonight, so A2 correctly left this entry alone; if A2's branch does not merge, this closure is false and must revert. · lead: make Check B per-package — `for p in $DB_PKGS; do go test ./$p || fail; done` — and assert the package count it iterated, so a shrinking `DB_PKGS` list announces itself; fold into whichever card next touches `scripts/verify-test-harness.sh`. · **resolved 2026-08-06**

- **B-23 · Check A2's spec-file floor of 20 lost its bite during the very run that added it** — the floor is a hand-typed constant with a comment reading "19 static under ./tests + 1 generated", and `dev` did have 19 static specs. B1 added `tests/sync-schema.spec.js` on the same run, making 20 static + 1 generated = **21**, and nobody ratcheted the floor. Measured: move `features/` away so `bddgen` exits 0 emitting nothing → `Total: 596 tests in 20 files` → **A2 PASS, exit 0**; delete one real spec → `588 tests in 20 files` → **PASS**. The check's own comment says it exists to catch "a moved features dir, a renamed glob, a generator that exits 0 on an empty input set" — it now catches none of them. B-09's detection gap is re-opened, one file wide. Check A (the mechanism check) is genuinely falsifiable: removing `bdd:gen` from `Taskfile.yml` deps reds it, verified. · _morning triage 2026-07-29 (T-28 decision 103b, adversarial finding F-2)_ · new · lead: derive the floor instead of typing it — `$(ls tests/*.spec.js | wc -l) + 1` — so the next card that adds a spec cannot silently spend the margin; a hand-typed floor is the same class of defect as the thing it guards.

- **B-24 · `shoot.mjs` measurement 6 walks `.plate` with no population floor, and the file's own header claims it has one** — `shoot.mjs:50-61` asserts "Six of the seven measurements now pin their population; m1 is not a population walk and has nothing to pin." False: m6 — the A-1 arithmetic check **added in the repair round** specifically to close the "value present but not right" hole — walks `document.querySelectorAll('.plate')`, prints `checked`, and gates only on `b6.bad.length`. Renaming `class="plate"` to `class="plateX"` in `mockup.html` yields `[light] A-1 arithmetic: 0 counting plates reconciled, 0 disagreeing -> PASS`, `self-verification PASS RAW_EXIT=0`. The other three repaired checks **are** falsifiable, each confirmed by mutation: deleting every `Undo` → `58 measured (expected >=62) -> FAIL` exit 1; demoting the error plate's `Retry` → `61 measured -> FAIL` exit 1; renaming a `Restore mine` into the non-destructive allowlist → `12 destructive controls (11 row/batch) -> FAIL` exit 1. · _morning triage 2026-07-29 (T-28 decision 103c, adversarial finding F-3)_ · new · lead: add an `EXPECTED_COUNTING_PLATES` pin to m6 and correct the "six of seven" sentence in the header — a false claim about which checks are pinned is worse than an unpinned check, because it stops anyone looking; rides the card that redraws the plates for A-3.

- **B-25 · Armed reds must be named by title/grep handle, never by line anchor** — `tests/sync.spec.js:1198` is `await p.waitForTimeout(400)` inside a helper's loop body: it names no test, and the test it used to name is now at `:1372`. Known dead since 2026-07-24 (`runs/2026-07-24-autonomous/HANDOFF.md:102` says so, `:164` files a migration item) and **still armed in `slate-20260729-2`'s preconditions table** — so every card told to "expect `:1198`" for five nights was told to expect something unobservable, and every report saying "it passed" was unfalsifiable. Two cards hit it independently on `overnight-20260729-2`. `:446` `[LST-17]` is live and correct. · _morning triage 2026-07-29 (T-28 decision 100, fork D-5)_ · new · lead: change the slate/launch-prompt convention to carry a grep handle (`-g "[LST-17]"`) rather than `file:line`, then sweep the other armed reds for line anchors — the anchor rot is silent by construction, so the fix has to be at the convention, not at this one line.

- **B-26 · The file every slate inherits gates G1–G6 from does not exist, and G5 has never been defined** — every slate since 07-15 and every launch prompt say the gates are "unchanged from `reference/overnight-run-plan-20260707.md`". Confirmed by `find` at triage: **that file is nowhere in the repo.** The contract was recoverable from practice and the runs used it (G1 build+vet, G2 Go+Playwright, G3 red-first re-verified by G6, G4 `sw.js` idempotence + version parity, G6 adversarial review), but **G5 has no definition in any sense the runs use** — it appears only in a PRD and a QA-coverage table, in two unrelated senses — and is not practiced. So every run this month has been graded against a ladder with no written definition. Worse than `DECISIONS-NEEDED.md` D-6 filed it, which read as a documentation nit. · _morning triage 2026-07-29 (T-28 decision 101, fork D-6)_ · promoted → gate-ladder-completeness (roadmap round 2026-08-05) · **✅ RESOLVED 2026-08-06 — the residual is closed on `card/a4-gate-ladder` (run `overnight-20260806`).** The first half landed earlier: `.night-crew/knowledge/reference/gate-ladder.md` was written at triage 2026-08-03 (T-34 decision 138), and slates now cite that path in this repo rather than the phantom clone file. **What it left open, and this card closed, is the half this entry's own title names second:** the file recorded G1–G4 and G6 and **said nothing at all about G5**, so the gap survived as a silent question a future slate would re-ask. The ladder now carries §"🛑 There is no G5. The number is retired, not missing." — the ladder is **G1, G2 (Go), G2 (Playwright), G3, G4, G6, and that is the whole ladder**; do not invent a G5, do not renumber G6 down. It also records **where the number does survive, so neither sighting is mistaken for a gate**: the three hardening PRDs use "G5" for a *second independent enumeration pass* over user flows, and `reference/qa-coverage-findings-20260718.md` uses it as a *coverage-gap number* for a missing behavioural-oracle layer — two unrelated senses, neither ever run as a gate. Recorded as **retired-never-defined rather than renumbered**, exactly as this lead directs and per ledger decision 101. 🛑 **Not a park:** decision 101 already made this call at triage; the card executed a standing decision rather than legislating a new gate. Adding a real fifth gate — a new obligation on every future card — remains an operator call and would get a **name, not the number 5**. B-26's sibling finding **B-14 is now recorded in the same file** (that entry stays open — its remedy is clone-side). · lead: write the file the prompts already point at, from practice, and record **G5 as never-defined** rather than renumbering — a retired number reads as history, a renumbered ladder hides that a gate was cited for a month and never existed. · **resolved 2026-08-06**

- **B-27 · `inventory.spec.js:883` fails from cross-spec pollution, unattributed** — `item modal pre-fills search with current line item text` fails `Expected "Special Sauce", Received "Test Item"`. **Proven pre-existing by reproduction:** G6 ran the preceding specs with B1's new spec file entirely absent and got the byte-identical failure, and `inventory.spec.js` alone on a fresh database passes 150/150. Source is one of `broadcast-rerender` / `grant-enforcement-parity` / `index`. **The first-proposed mechanism is wrong** and is recorded as wrong so it does not become folklore: a `.first()` collision over a shared `eventDate` cannot be it, because the pending list is `ORDER BY created_at DESC` with no re-sort, so `.first()` is the newest row and `event_date` is not in the sort key. Likelier: `seedPendingPurchase` swallowing a failed POST (`tests/inventory.spec.js:70`). `playwright.config.js` defaults to `retries: 1`, which is why the baseline reads green — cards at `--retries=0` see it. Did not surface in either the run's or triage's `--retries=0` full suite, consistent with load/ordering sensitivity. · _morning triage 2026-07-29 (T-28 decision 102, fork D-7)_ · new · lead: fix `seedPendingPurchase` to fail loud on a non-2xx POST first — that is a one-line change that either fixes this or eliminates the leading suspect; do not attribute the pollution until it reds again on a quiet box in isolation.

- **B-28 · `receipt/worker.go`'s `parseEventDate` stamps a COGS period from server-local time, outside the app timezone** — `backend/internal/receipt/worker.go:1056` ends `return time.Now().Format("2006-01-02")`: when Mercury's `CreatedAt` matches none of the accepted layouts, the fallback is the **server's** local date (UTC in the container), and that value is written to `pending_purchases.event_date` — a **COGS period assignment**, and the one field that WINS the `COALESCE` in `pendingPeriodDateExpr`. So the single path that is exempt from card A1's zone unification is the path that produces the value the zone would otherwise decide: a receipt ingested between 20:00 New York and midnight UTC is filed to tomorrow's period, permanently, with no boundary for A1 to move. Bounded by how often Mercury emits an unparseable `CreatedAt` — which is unmeasured, and silent when it happens (no log, no `reason`). · _flagged in the `overnight-20260729-2` park note for card A1; carried out of the 2026-08-01 resume as explicitly out of footprint (`backend/internal/receipt` is not in A1's packages)_ · promoted → receipt-worker-correctness · lead: two things, and the second matters more — (a) format in `users.DefaultTimezone` like every other site A1 moved; (b) the silent fallback is the real defect: an unparseable timestamp should set `reason` / log at WARN so a wrong period is *observable* rather than indistinguishable from a correct one. Fold into whichever card next touches `internal/receipt`; a one-line zone fix without (b) leaves the class intact.

- **B-29 · HQ changed what `/period-summary`'s completeness gate returns on 2026-06-06 and never told sales-processor — an undisclosed two-repo contract drift on the payroll gate** — Phase 21 published `completeness.pending_review_ids` as `pending_purchases.id` rows where `(created_at AT TIME ZONE 'America/Chicago')::date BETWEEN from AND to` (`21-SALES-PROCESSOR-CONTRACT.md:67`, archived in `875e26c` 2026-06-05), and that was **accurate to the code as it then stood**. On **2026-06-06** commit `cf959bd` — quick task `260606-0gh` (`.planning/quick/260606-0gh-completeness-gate-filters-pending-review/`), a task entirely separate from Phase 21 — replaced it with `COALESCE(event_date, (created_at AT TIME ZONE 'America/Chicago')::date)` **and did not touch the contract**. That task's own PLAN.md reasons explicitly about *"a sales-processor query for May 25–31"*, so the counterparty impact was in view at the time. **It is a behaviour change, not a clarification:** under the published expression a late-discovered receipt (the PLAN's own example — a May 29 purchase ingested June 2) was filtered on `created_at`, fell outside the May window, and did **NOT** block May payroll; under the shipped code its extracted `event_date` puts it inside the window and it **does**. So sales-processor may have been receiving an undocumented `ready:false` — a **blocked payroll run** — since June 2026, with no way to reconcile it against the contract it was handed. Card A1's `:67` amendment initially compounded this by asserting *"the `COALESCE` is not new"* / "carried since Phase 21", which reads to the counterparty as *"you were always getting this"* — the opposite of the truth; that text is corrected on `card/a1-app-timezone-unify-new-york`. 🛑 **This is a SECOND notice to sales-processor, independent of A1's timezone notice.** The operator decides what is told and when; A1's contract edit states the drift on the page but does not deliver it. · _adversarial review of card A1 on run `overnight-20260801`, finding F1 — verified at source against `875e26c` and `cf959bd`_ · done — disclosed + finalised 2026-09-01 as docs/contracts/NOTICE-sales-processor-2026-09-01.md (ledger T-48); §1 exposure window checked 2026-08-04, no past figure restated · lead: three parts, and the third is the general one — (a) the operator must decide whether sales-processor is told about this separately from the timezone change, and whether any past `ready:false` needs reconciling; (b) audit the other `:NN` rows of both `*-SALES-PROCESSOR-CONTRACT.md` documents the same way, by diffing each published expression against the code at HEAD rather than trusting the prose — this one survived eight weeks of reading because nobody diffed it; (c) a published cross-repo contract has no mechanical link to the code it describes, so any quick task is free to silently invalidate it — the durable fix is a check that fails when a contract's stated SQL no longer appears in the handler it names, or failing that, a rule that touching a handler named in a `*-CONTRACT.md` requires re-reading that document.

- **B-30 · `[A1-TZ-02]` is a NEW armed red — the offline-queue period test reds under whole-suite load and greens in isolation** — `tests/sync.spec.js` *"a queued submission still lends its idempotency_key at 7:30pm CT [A1-TZ-02]"* failed once in a full `--retries=0` suite (`Error: the queued entry must STILL lend its key at 7:30pm CT`, `Received: null` at the `A1_T3` assertion) and passed everywhere else it was run: green in isolation via `-g "A1-TZ"` (5/5), and green **at the identical suite position — test 478 of 603 —** on an immediate re-run of the whole suite. Both suite runs were valid (one summary block each) on a box carrying **three concurrent Playwright stacks** (this card's, plus card C1's and B2's). **The shape of the failure names the mechanism, and it is a harness race, not a product defect:** the test stamps the queue entry's `period` at `T1`, asserts a hit at `T2`, then asserts a hit at `T3`. The `T2` assertion PASSED and the `T3` one returned `null` — which is exactly what happens if `page.clock.setFixedTime` had not yet taken effect at `T1`/`T2` (so both the stamp and the `T2` comparison used the REAL date and agreed) and had taken effect by `T3` (so the comparison used 2026-07-15 and disagreed). A period mismatch caused by the *product* would have reddened `T2` as well, and it did not; `[A1-TZ-01]`, which exercises the same three instants through `isCurrentPeriodEntry` directly, passed in both suite runs. Note `tests/sync.spec.js` is the **only** spec file in the repo that uses `page.clock` (grepped), so this is not cross-spec clock interference. 🛑 **Non-reproduction does not retire it** — that is precisely the standing rule for `[LST-17]` and B-27, and this entry exists so the next run that sees it does not rediscover it as a novel regression. Named by grep handle, not line anchor, per B-25. · _fix round of card `app-timezone-unify-new-york`, run `overnight-20260801`, G2 Playwright run 1 of 2_ · new · lead: make the clock installation observable before the fixture depends on it — assert inside the page that `new Date().toISOString()` equals `A1_T1` immediately after each `setFixedTime` and before the IndexedDB write, so a clock that has not landed FAILS AT THE SETUP LINE naming the real cause instead of surfacing three assertions later as a mysterious `null`. That is a test-only change and it converts a silent race into a loud one; do it on whichever card next touches `tests/sync.spec.js`.

- **B-31 · `index.html`'s launcher hides tools a user can actually reach — it gates tiles on literal slugs, and per-tab grants are not literal slugs** — `filterTilesByPermissions` (`index.html:121-130`) builds `allowed` as the literal set of `slug` values returned from `hq_apps` and hides any tile whose slug is not in it. Under this repo's per-tab grant convention a user can hold **only** `inventory-trends` — a legitimate grant — in which case they can reach `/api/v1/inventory/trends` and the Trends tab, and the launcher shows them **no Inventory tile at all**. That is the launcher concealing a surface the user *can* reach, which is exactly the harm decision 56 / obligation 4 names, in the direction nobody checked. `sync-rxdb-replication-and-conflict-handler` discharged obligation 4 in the other direction only (umbrella `inventory` ⇒ `inventory-trends`, `inventory-cost`, exported and tested in `sync-rxdb/client.js`) and correctly did not touch `index.html`. **Pre-existing, NOT created by that card** — the gating predicate is byte-identical to base. · _overnight-20260801 G6 review of Card C1 (obligation 4 ruled satisfied-as-worded but under-delivering its stated purpose)_ · new · lead: the launcher needs the same slug relation the client helper already exports — a tile should show when the user holds the umbrella slug **or any slug under it**, so `inventory-trends` alone reveals the Inventory tile (landing on the tab the grant actually covers). Do not re-implement the relation: `expandGrantSlugs` in `sync-rxdb/client.js` is the tested one, and its inverse is what the launcher wants. Belongs to whichever card next owns `index.html` — it is a launcher change, not a sync change, and folding it into a sync card would repeat the footprint mistake that made it invisible.

- **B-32 · A FAMILY, not two bugs — load/scale-sensitive 30 s timeouts that redden at whole-suite scale and green in isolation, on specs the reddening card never touched** — Two more members surfaced in `overnight-20260801`'s G6 review of card C2, and they are filed as ONE entry because the interesting thing is the shape they share with **B-27** and **B-30**, not either individually. **(a) `tests/workflows.spec.js` *"submitted checklist survives builder edit with assignment change [LC-02]"*** — 30 s timeout. 🛑 **NOT C2's, and the proof predates C2 by five days:** the identical test reddened the same way on **2026-07-26**, on card B's leg, on a tree where `workflows.html` and `tests/workflows.spec.js` were proven **byte-identical to base** (`.night-crew/qa/spike-supabase/captures/gate-20260726-card-b.txt`, `.night-crew/runs/2026-07-26-autonomous/HANDOFF.md:166`). It was refused attribution then. G6's own run reproduced the *class* on a different, untouched spec. **(b) `tests/inventory.spec.js:2908`** *"Receipt sync button › reload mid-run shows Syncing… (state survives via GET /status)"* — 30 s `networkidle` timeout in G6's run. **Proven not C2's by byte-identity:** `inventory.html` contains **zero** references to `sync-rxdb`/`conflict-notice` (grepped), and neither it nor `tests/inventory.spec.js` appears anywhere in the card's diff (`git diff --name-only` over the card range returns no inventory file). **The family, stated as a family:** a long-running spec at whole-suite position reds on a wall-clock 30 s timeout and greens on the same box in isolation or at `--retries=1`. The captured signatures differ and are recorded rather than smoothed over: member (a)'s call log on 2026-07-26 read *"element was detached from the DOM, retrying"* — a re-render racing a click — while member (b) hung on `networkidle`. Same ceiling, same load sensitivity, different proximate wait. B-27 (cross-spec pollution in `inventory.spec.js:883`), B-30 (`[A1-TZ-02]` clock race) and both members here all have it. Every one of them has so far been reproduced-or-not on a box carrying **two to four concurrent Playwright stacks**, which is this project's normal overnight shape and is the leading suspect for the shared cause. 🛑 **Non-reproduction does not retire any member** — the standing rule for `[LST-17]`, B-27 and B-30 applies to this entry in full, and "rare, mechanism known" must not be laundered into "not flaky". Named by grep handle, not line anchor, per B-25, except where the line is the only handle. · _fix round of card `sync-rxdb-conflict-notice-ui`, run `overnight-20260801`, filing G6's two unattributed reds_ · new · lead: stop diagnosing members one at a time and measure the family instead — record per-test wall-clock duration and the suite position for every run into a durable artefact, then check whether the reddening tests are the ones already nearest the 30 s ceiling under concurrent-stack load. If they are, the fix is a harness one (raise the ceiling for the known-slow specs, or stop running four stacks against one Postgres) and it retires four backlog entries at once; if they are not, each member is a real and separate product bug and should be split back out. Do this before any further per-member investigation.

- **B-33 · The run-evidence oracle cannot see ANY hq slate run's closing evidence — every night reads `no-run-evidence` at the next launch** — `night-crew run-evidence check --repo . --run 20260801` returns **`no-run-evidence`** on a night that landed 4 of 4 with a committed closeout record, a full conflict log, HANDOFF and DECISIONS-NEEDED. Two independent path mismatches, either of which alone is sufficient: **(1)** the oracle reads `.night-crew/runs/<runid>/{summary.json,metrics.jsonl,journal.jsonl}` — i.e. `runs/20260801/` — but the launch prompt mandates the run directory be `.night-crew/runs/2026-08-01-autonomous/`, so those three can never be found regardless of whether they are written; **(2)** it reads `reference/conflicts-<runid>.md` relative to the **repo root**, and **hq has no root `reference/`** — this repo's reference home is `.night-crew/knowledge/reference/`, which is where the conflict log, the closeout record, the slate and the launch prompt all correctly live. `night-crew launch-prompt` searches BOTH homes and resolves correctly; `run-evidence check` does not, so the two verbs disagree about where this repo keeps its artifacts. **Consequence:** §1 of `/nc-run`'s "has this slate already run?" guard is **inert in every night-crew target repo scaffolded this way**. The next launch will read a completed night as never having run and offer to execute the slate again. The §2 run-branch guard is the only thing that catches it, and only while the run branch stays unmerged — once triage merges `overnight-20260801` to `dev`, **nothing** stops a re-execution of an already-landed slate. · _overnight-20260801 closeout; the closeout record was written per `/nc-run` §3a and the oracle still could not see it_ · dropped — night-crew tooling defect, tracked clone-side as B-346/B-347 · lead: this is a **night-crew clone** fix, not an hq one — teach `run-evidence check` the same two-home search `launch-prompt` already implements (scaffolded knowledge home first, then root `reference/`), and either resolve the run directory by glob (`runs/*<date>*`) or make the runid-keyed name the convention the launch prompt emits. Until it ships, treat §1's verdict in this repo as **unreliable** and lean on §2. Sibling of B-25/B-26 — a tool-side defect surfaced by a target repo.

- **B-34 · `purchasing.spec.js:1792` saw the server's clock run BACKWARDS 640 ms between two consecutive `now()` reads** — the FR-23 repurchase-reset test failed once during card B2's fix round on run `overnight-20260801`, then passed in isolation. The failure is not a timeout and not a fixture collision: two server-side `now()` reads taken in sequence returned times **640 ms apart in the wrong direction**, so a window computed from the first read excluded a row stamped by the second. 🛑 **Deliberately NOT folded into B-32.** B-32's family is wall-clock 30 s ceilings under concurrent-stack load — a *timeout* shape. This is a *monotonicity* shape, and the two have different fixes: B-32's is harness capacity, this one is either NTP slew on the host, a Postgres `now()`/`clock_timestamp()` mixup in the query under test, or a genuine ordering bug in the repurchase window. Filing them together would let the interesting one hide inside the boring one. · _morning triage 2026-07-31, run `overnight-20260801`; observed during B2's fix round, flagged by the run as "decide at triage whether it earns a number"_ · new · lead: first establish which clock moved — log both reads with `clock_timestamp()` alongside `now()` in the failing path, since `now()` is transaction-start time and two reads in **different** transactions can legitimately appear out of order under concurrent load while `clock_timestamp()` cannot go backwards. If `clock_timestamp()` is monotonic and `now()` is not, this is a transaction-boundary bug in the window computation and it is a real product defect, not a flake. Do this on whichever card next touches `internal/purchasing`.

- **B-35 · The standard gate command `go test ./...` DROPS a database it does not own — a B-16 trigger sitting inside the ladder itself** — `TestRowVisibilityRLS` drops and recreates its FDW fixture database on entry, and its default name is `hq_test_b2_fdw`. Any plain `go test ./...` in this repo therefore destroys `hq_test_b2_fdw` without asking, including a reviewer's or a concurrently-running card's. Reproduced at morning triage: the adversarial reviewer's first full `go test -p 1 -count=1 ./...` run silently dropped it before the reviewer switched to `HQ_RLS_TEST_DB=hq_adv_b2_fdw` for every subsequent probe. **This is exactly B-16's failure mode** — one agent destroying another's database, where the victim sees a broken suite rather than a stolen fixture — except that B-16 was about *reviewer cleanup* and this is baked into the **primary gate command every card runs**. Concurrent dispatch (three tracks, this project's normal overnight shape) makes collision the expected case, not the unlucky one. · _morning triage 2026-07-31; found by the adversarial re-execution of run `overnight-20260801`, disclosed by that agent against itself and not reported by the run_ · promoted → gate-harness-honesty (roadmap round 2026-08-05) · lead: the fixture database name must be unique per process, not a shared constant — derive it from the PID or require `HQ_RLS_TEST_DB` to be set and **fail loudly when it is not**, rather than defaulting to a name another agent may be using. Prefer failing over defaulting: a card that forgets the variable should get an error, not silently eat someone else's fixture. Sibling of B-16; fixing it there without fixing it here leaves the larger hole open. · ✅ **closed — attended re-gate 2026-08-28** (`card/a3-rls-fixture-own-regate`, ledger §T-45): the shared-constant default is gone — the fixture name derives per-process (`hq_rls_b2_fdw_p<pid>`) and must sit inside `^hq_rls_[a-z0-9_]+$`, so a plain `go test ./...` can no longer name, let alone drop, another leg's database; refuse-don't-drop pinned by test

- **B-36 · `internal/sync` prints `ok` and exits 0 while skipping B2's ENTIRE RLS attack suite — the row-visibility security gate cannot be distinguished from a gate that never ran** — `resolveSpikeConfig` locates the sync substrate by shelling out to `docker compose -p spike-supabase … port`. **Any** reason that command fails — docker absent, compose project down, ports not published, a different project name — is treated as "not configured" and becomes `t.Skip`, so the package reports `ok` and the ladder's recorded line **"G2 Go … 9 packages `ok`, 0 failed"** carries **zero** information about whether the security suite executed. Verified at triage by execution: with `PATH` stripped of docker, `go test -run TestRowVisibilityRLS ./internal/sync/` exits **0** with `ok`, and only `-v` reveals `--- SKIP` and the test's own honest line *"no sync substrate configured — skipping, and SKIPPED IS NOT PASSED"*. The test says the right thing; **nothing in the gate reads it.** 🛑 Distinct from **B-22**, which is about `verify-test-harness.sh` Check B aggregating with OR — that is the harness script; this is the suite's own skip path, and `verify-test-harness.sh` does not cover this axis at all (it grades only the `DB_TEST_URL` case). This is the repo's characteristic bug class — a check whose subject set can go empty — inside the card that exists to prove row visibility. · _morning triage 2026-07-31; reproduced by the adversarial re-execution of run `overnight-20260801`, finding F-1_ · promoted → gate-rls-skip-is-not-pass (roadmap round 2026-08-05) · lead: a security suite must not be allowed to skip silently in a gate context — gate `t.Skip` on an explicit opt-out (`HQ_SYNC_SUBSTRATE_OPTIONAL=1`) and `t.Fatal` otherwise, so an unreachable substrate reds the package instead of passing it. Until that lands, the standing triage rule recorded in ledger T-29 applies: gate evidence for `internal/sync` must include `-run TestRowVisibilityRLS -v` output showing subtests ran, because the package `ok` line provably cannot distinguish ran-and-passed from never-ran. **✅ RESOLVED — mechanism 2026-08-01, assertion run `20260806`.**

  🛑 **This entry sat OPEN for five days after it was fixed, and that is the more interesting half of its closure.** The mechanism this lead asks for — `t.Skip` gated on an explicit `HQ_SYNC_SUBSTRATE_OPTIONAL=1` and `t.Fatal` otherwise — **landed on 2026-08-01 in commit `4615661`** (*"fix(sync): B-36 — an unresolvable substrate is a FAILURE, not a silent skip"*), folded into card `sync-rxdb-write-policies` of run `overnight-20260802` rather than taken as a card of its own, on the reasoning recorded in **ledger §T-30's "Also folded, and stated as a planner call rather than a backlog promotion (§15k bar not invoked)" paragraph** (`ledger.md:2232-2239`): a second security gate must not be written into a package whose own gate cannot prove it ran. 🛑 **Not decision 111** — 111 (`ledger.md:2171`) is the RxDB *per-table write-policy* call ("push writes mirror the read rule, PER TABLE"), a different decision in the same triage section; the B-36 fold was stated as a planner call and never carried a decision number. The first draft of this closure cited 111 and is corrected here (fix-round finding F9). The reasoning quoted above is unchanged and was always accurate; only the handle was wrong. **Filed 2026-07-31, fixed 2026-08-01, and still carrying `promoted → gate-rls-skip-is-not-pass (roadmap round 2026-08-05)` on 2026-08-05** — i.e. the 08-05 roadmap round promoted it as live work four days after the code that closes it was on `dev`. Nobody re-verified the entry against the tree; the promotion was made against the entry's text. **This is an instance of B-38's channel gap, not a bookkeeping slip:** a fix that lands folded into another card's commit has no channel back to the backlog entry that motivated it, so the entry stays open by default and the next planning round reads it as work. The cost here was one wasted promotion; the cost of the same gap on a *security* entry is a second team re-fixing what is already fixed while believing coverage is absent.

  **Re-verified at slate time by execution in BOTH directions before this card was scoped**, against `dev`: docker stripped from `PATH` with no opt-out ⇒ `EXIT=1` and a hard `t.Fatalf`; `HQ_SYNC_SUBSTRATE_OPTIONAL=1` ⇒ `EXIT=0` and a deliberate `--- SKIP`. So the card was re-scoped away from re-implementing the mechanism and onto what was genuinely missing: **the mechanism was pinned only as a decision function, and the 59-subtest count was not pinned at all.**

  **What card A1 (`gate-rls-count-assertion`, run `20260806`, branch `card/a1-rls-count-assert`, commit `2b3a50f`) added, all of it in `backend/internal/sync/spikestack_gate_test.go`:**

  1. **`TestSubstrateGate_ExitCodeAsymmetry` — the property, end to end, rather than the decision function.** `TestSpikeResolution_OptOutIsTheOnlySkipDoor` (landed with the fix) pins `spikeResolution`, and that is necessary and not sufficient: between that pure function and the exit code the gate ladder actually reads sit `resolveSpikeConfig`'s switch, its `t.Fatalf`, and `rvConnect`'s skip. A `t.Skip` reintroduced on the fail branch, or an `if err != nil { return cfg, false }` added for "robustness", would leave every pre-existing test in the file green while the package went back to printing `ok` in 0.014s. The new test spawns a nested `go test -run '^TestRowVisibilityRLS$'` with a `docker` on `PATH` that exits 1 — one of this entry's own listed causes, reproduced rather than simulated — and asserts the two arms **differ**: no opt-out ⇒ non-zero, `HQ_SYNC_SUBSTRATE_OPTIONAL=1` ⇒ zero. Red-first against a tree with `spikeResolution`'s default branch reverted to `spikeGateSkip` (this entry's pre-fix behaviour): `EXIT=1`, arm 1 FAIL *"🛑 B-36 IS BACK"*, arm 2 PASS. Green on the shipped tree.

  2. **The 59 count asserted rather than inferred — Q-KR1's residual.** The standing triage rule in this lead's last sentence (ledger T-29, decision 108, kept with amendments by decision 116) requires citing `-v` output showing **59** subtests ran. That number lived only in prose: `grep -n '59'` across this package's tests returned a JWT secret and a hex literal. It was re-counted by a human off a log once a night, which is the same shape of check this entry itself is about. Two independent counters now assert it — **structural** (parses `rowvisibility_rls_test.go`, counts top-level `t.Run` registrations, expands `for … range []T{…}` by case-list length, refuses to descend into `t.Run` closures, and **reports** rather than guesses a case list whose length is not visible in source) and **execution** (re-runs the suite as a subprocess and counts the depth-1 subtests the runtime reported, reading depth off `go test`'s four-space indentation because these subtest names contain slashes on purpose). Measured: **59, by both, in agreement.** Red-first by deleting one case (`V20 hq_template_approvers`) from the `V15-17,20` list: both counters reported **58, want 59**, `EXIT=1`. 🛑 **The failure mode this closes is not "a subtest breaks"** — that reds the package and everyone sees it. It is a subtest that stops being *registered*, which leaves a wall of `--- PASS` and a package that says `ok`.

  3. **`TestRVTopLevelSubtestCount_CountsWhatItClaims`, the guard's own guard** — the walker is run against a synthetic function whose answer is 6 by construction and which contains every trap the real file contains (nested `t.Run`s, a nested loop, a registration inside a helper), plus an opaque case list it must report as unreadable. B-22/B-23/B-24's lesson applied to a counter.

  Additive only: `rowvisibility_rls_test.go` and `jwtbridge_rls_test.go` are byte-for-byte unchanged. **Standing obligation created:** any card that adds or removes a top-level subtest in `TestRowVisibilityRLS` must bump `wantRowVisibilitySubtests` in the same commit — the constant is the coverage claim, and a red there is the suite announcing it changed size. Deleting or relaxing the assertion to clear that red is not available. · **resolved run `20260806`**

- **B-37 · `build-sw.js` exits 0 while silently dropping shipping assets from the precache — warnings only, no failure** — reproduced at triage: with git HEAD at a pre-bundle commit and the tip's assets present in the working tree, `node build-sw.js` printed `skipped (not in HEAD): vendor/rxdb.bundle.js` plus six `sync-rxdb/` and `sync-schema/` modules, emitted **22 files / 1481.9 KB instead of 29 / 2111.1 KB**, and **exited 0**. `workflows.html` IS precached and loads `sync-rxdb/bootstrap.js` as a module, so the shipped service worker would cache a page whose dependencies it deliberately omitted — Obligation 5's production-outage mode. It bit for real during run `overnight-20260801`'s merge 3, where regenerating mid-merge produced a 24-file manifest; the run caught it and recorded the ordering rule in the conflict log, but **the rule is procedural and nothing mechanical enforces it**. 🛑 Distinct from **B-17**, which covers the wrong `--name-only` C-quoting comment and the HEAD-vs-`origin/main` residual — neither of those is the exit code. Reading from git HEAD is by design (card `precache-manifest-from-head`); exiting 0 after dropping a file another precached file imports is not. · _morning triage 2026-07-31; reproduced by the adversarial re-execution of run `overnight-20260801`, finding F-2_ · promoted → P1 `build-deploy-manifest-integrity` (slate-20260802). **✅ LANDED 2026-08-01 on `card/p1-build-deploy-manifest-integrity` (`fc768ff` guard, `9ea937a` the fix it demanded, `4c455ff` the guard on the guard), run `overnight-20260802`** — `build-sw.js` now runs `importReachabilityTransform` as the LAST `manifestTransform` and **throws**, so `sw.js` is never written on a failure rather than written-then-complained-about. **Red reproduced first, at exit 0:** with `sync-rxdb/bootstrap.js` dropped from HEAD and left in the working tree, the pre-fix `build-sw.js` printed the skip, emitted **28 files / 2132.6 KB**, and exited **0**, writing an `sw.js` that precached `workflows.html` without its module entry point. The guard exits **1** on the identical case and an unreferenced scratch `*.html` still exits **0** — the invariant is reachability, not completeness, exactly as this lead specifies. 🛑 **It then found the defect live on the merged tree with no synthetic case at all: `log.js` (all 7 precached pages) and `tab.js` (5) were referenced everywhere and in `globPatterns` nowhere**, since the ad-hoc commits that added them — `log.js` at `1ec8725` (2026-04-21), `tab.js` a week later at `cfe7edc` (2026-04-28), each first referenced in the same commit that introduced it. They ship via `COPY *.html *.js` so they have always worked online and always failed offline — and `tab.js` applies `#tab=N` before paint, so five of seven tools opened on a returning offline client with every tab section visible at once and no switching. **Precache count 29 → 31 (+2.0 KB); 31 is the new invariant.** Guard integrity: 18 files parsed, 30 references resolved, three anti-vacuity checks (zero files / zero refs / two canary edges), 7 tests in `tests/sw-manifest.spec.js`, and **4 mutations each red exactly one of them** (drop the transform → the end-to-end test; collapse the bare-specifier rule → the `ws` test *and* the real build; null the vacuity checks → the untrustworthy test; warn instead of throw → the end-to-end test). · lead: make the skip fatal when the skipped file is reachable from something already in the manifest — parse the precached HTML/JS for local `import`/`src` references and exit non-zero if any resolves to a path that was skipped. A blanket "fail on any skip" is wrong (skipping genuinely uncommitted scratch files is the feature); the actionable invariant is that **nothing precached may import something not precached**. Pairs with B-13, which is why an uncommitted-but-staged file reaches production in the first place.

- **B-38 · A card the cutover hard-depends on was written down three times overnight and never became a card — HANDOFF's "What is NOT done" has no route into the roadmap** — Card B2 wrote *"RxDB push replication is REFUSED until a follow-up card writes WITH CHECK policies. That card owns…"* into `sync-schema/sql/0003_rls_policies.sql:104` during run `overnight-20260801` (commit `06283b9`). The same deferral then appeared in that run's `DECISIONS-NEEDED.md:63` as contributing context under Fork 1, and a third time in `HANDOFF.md` under **"What is NOT done"**. HANDOFF also stated it as one of *"two things triage must rule on."* **Morning triage 2026-07-31 resolved decisions 104–110 and none of them is that one** — it was neither decided nor deferred-with-a-reason, it simply fell through. The consequence was invisible for a full day: `sync-hard-cutover`'s roadmap entry said it depended on **three** cards when it actually depended on **five**, and `slate-20260801.md:292`'s milestone remainder told the operator the milestone was *"2 cards short"* and *"slatable in one further night"* — **already false at the moment it was signed**, because B2's own deliverable was concurrently writing down a third card. Found at the next evening's `/nc-slate-plan` by grepping the SQL for `WITH CHECK`, **not** by reading the roadmap; authored as `sync-rxdb-write-policies` (ledger T-30 decision 111). 🛑 **The failure is structural, not anyone's inattention:** DECISIONS-NEEDED forks get a triage round and a recorded resolution; **HANDOFF's "What is NOT done" gets read once and then the file is archived.** A deferral written by a card about its own successor has no channel that ends in a roadmap card. · _slate planning 2026-07-31 evening, `/nc-slate-plan` for `overnight-20260801-2`; the operator caught the milestone-remainder discrepancy at sign-off and the trace was run from there_ · new · lead: two parts. (a) **Ritual fix, cheap:** `/nc-morning-triage` should treat every "What is NOT done" bullet the way it treats a fork — each one exits triage as a roadmap card, a backlog entry, or an explicit "no action, because…" recorded in the ledger. Three dispositions, none of them silence. (b) **Mechanical check, the durable half:** a card that writes "a follow-up card" / "a separate card" / "until a later card" into shipped source or into its own report is asserting a dependency that does not exist yet — grep the run's diff for that phrasing at closeout and fail the closeout until each hit is either an existing roadmap card name or a newly authored one. Sibling of B-33 in kind (an oracle that cannot see what the ritual produced), but this one needs no tooling change to fix the first half.

- **B-39 · Two runs stamp the implementer leg only, so 7 of 18 merged cards cannot enter an end-to-end median** — `overnight-20260729-2` records `+ 1 repair round` / `+ 2 repair rounds` with no minutes (its `timings.log` holds a single dispatch line), and `overnight-20260801` says so in terms: *"Per-phase G6/fix/land splits are NOT recorded for this run — `timings.log` captures dispatch and implementer-return only… sizing against implementer time alone under-prices every card by roughly half."* The consequence surfaced when Delivery KR3's median was finally computed (2026-08-02, `reference/card-actuals.md` § *Cycle median*): the number rests on **N=11** when **18** merged cards existed, and three cards of the two most recent runs — the ones nearest the next slate's shape — are exactly the ones missing. The `20260801` entry already asked for the fix (*"Ask the orchestrator to stamp G6-start / G6-return / fix-return per card"*) but it was a note inside a run write-up, not an item with a handle, so it had no channel — B-38's failure mode in a different file. · _Delivery KR3 median computation, pre-step of run `20260802`_ · new · lead: the orchestrator already writes `timings.log`; add three stamps per card (G6-start, G6-return, fix-return) next to the two it writes now, and have the closeout refuse to write a card row whose end-to-end cannot be derived. Cheap, and it makes the KR computable from the log instead of from prose. **Destination: NEXT milestone (run mechanics)** — it does not redden a named KR on the current OKR page, because D-KR3's median *was* computable from the 11 cards that are stamped.

- **B-40 · KR denominators written as literals go stale — "all 5 cards in this activity" was wrong within a week** — Delivery KR3 reads *"Per-card wall-clock timing is recorded for all 5 cards in this activity (4 sync + 1 independent fix)."* Activity 1 then fanned out repeatedly (`sync-rxdb-feasibility-spike` → W1 + W2 at slating; `sync-rxdb-schema-and-replication` → collections-and-table-contract, row-visibility-rls, replication-and-conflict-handler, browser-delivery-spike, proxy-endpoint, and the conflict-notice mockup/UI pair) and the cycle has produced **18 merged cards**, not 5. The KR is therefore either trivially satisfiable (5 of 18 are timed) or unsatisfiable (the named 5 no longer all exist as cards) depending on which reading is taken — and neither reading is what the KR meant. Sibling in kind to the prior cycle's P4 lesson already recorded at the top of `okrs.md` (*"write KR metrics that cannot be failed by desirable behavior"*): fanning a card out at slating is desirable behavior, and it broke this metric's denominator. · _Delivery KR3 median computation, pre-step of run `20260802`_ · new · lead: state KR denominators as a **query over an artifact** (*"every card merged in this cycle, per `reference/card-actuals.md`"*) rather than a count fixed at authoring time. Belongs in `/nc-okr-session`'s guidance next to the existing P4 lesson, not in a card. **Destination: NEXT milestone (`/nc-okr-session` guidance).**

- **B-41 · The ledger has two different decisions numbered 67** — `ledger.md:1337` is T-24's *"67. A new cycle starts fresh for every user — 0/2 for both A and B"* (the cross-user hydration product ruling, 2026-07-26); `ledger.md:1380` is T-25's *"Decision 67 — D-1: `build-sw.js` globs `git ls-tree -r --name-only HEAD`, not `git ls-files`"* (2026-07-27). The sequence runs 66 (T-23) → 67 (T-24) → **67** (T-25) → 68 (T-25): T-25 numbered from the last decision it had read rather than the last one written, and T-24 sits between them. Every downstream citation found so far disambiguates with its `T-NN` prefix (`roadmap.md:123` "T-24 decision 67", `roadmap.md:384` and `slate-20260729.md:70` "T-25 decision 67", `BACKLOG.md` B-13 "T-25 decision 67") — **except** `runs/2026-07-29-autonomous/merge-intent-a-precache-manifest-from-head.md:13`, which cites a bare *"decision 67"* and is resolvable only from surrounding context. Low severity today precisely because the `T-NN` habit held; it is a trap for any future tool or reader that treats the decision number as a key. · _Delivery/Product KR artifact pre-step of run `20260802`, found while transcribing T-24 decision 67_ · new · lead: do **not** renumber — the citations that exist are correct as written and renumbering would break them. Two cheap options: (a) make `T-NN` mandatory in citations and add a checker that reds a bare `decision NN`, or (b) note the collision inline at both ledger sites so a reader landing on either knows the other exists. Prefer (b) plus (a); the ledger is append-only evidence and editing history to tidy a number is the opposite of what it is for. **Destination: NEXT milestone (ledger/ritual hygiene).**
- **B-42 · `SYNC-REALTIME-SCOPE` — the pull is scoped per checklist but the live `postgres_changes` stream is not, so an out-of-scope row still reaches the phone when someone changes it** — 🛑 **RENUMBERED FROM `B-39` during the G6 fix round of run `20260802`: three legs that night independently claimed B-39, and the pre-step's B-39/B-40/B-41 were already merged to the run branch and keep those numbers. Content and destination are unchanged.** Card `sync-replication-scope-per-checklist` (run `20260802`, A1) gave every replicated collection a `pull.queryBuilder`, which is the only scoping seam `rxdb/plugins/replication-supabase` offers. It scopes the PULL. It does not scope the LIVE leg: the plugin opens `client.channel(replicationIdentifier).on('postgres_changes', {event:'*', schema:'public', table}, …)` with **no `filter` key**, and feeds every event it receives straight into the local store as a master change. So a `submission_responses` row belonging to a checklist this device never opened, **edited while the page is open**, still lands in IndexedDB — the pull scope does not evict it and nothing re-checks it. **Two seams were examined and both are genuinely closed, which is why this is a backlog item and not a missed line of code.** (a) `pull.modifier` DOES reach stream documents — RxDB's `masterChangeStream$` does `documents.map(m => modifier(m))` — but it cannot DROP one: there is no null filter on the downstream path (the push path has one, `newDocumentState===null?null:`; the pull path does not), so returning null injects a null document rather than skipping a row. (b) Realtime's own `postgres_changes` `filter` option accepts a **single** `column=op.value` clause, which can express `checklists`/`approvals`/`templates` but **cannot** express the `responses` scope, which is `or(submission_id.eq.X, and(submission_id.is.null, field_id.in.(…)))` — and `responses` is the unbounded collection the whole card is about. A partial fix that filters three collections and leaves the fourth is worth having but is a design call, not a mechanical one. **Bounded, and stated so the size is not overestimated:** the pull is the unbounded leg (all history, every page load); the stream is bounded by what other people change while this crew member is looking at their phone. The card's ~23 s figure is on the pull and the pull is now scoped. 🛑 **The unbounded-phone-storage consequence is IMPROVED, NOT FIXED — reworded during the G6 fix round (finding F-6), which caught the original wording claiming otherwise.** RxDB's downstream only adds; nothing evicts, and there is no retention sweep for the four replicated collections. A phone still accumulates every checklist it has **opened** — the bound moved from *all history* to *opened checklists*, which is a large improvement and is not the same as bounded. · _run `20260802`, card A1 `sync-replication-scope-per-checklist`, found while measuring the vendored plugin's pull construction against `vendor/rxdb.bundle.js`; recorded in that file's `REPLICATION SCOPE` header block and in `sync-rxdb/client.js` rather than only here_ · new · **destination: `sync-hard-cutover`** — that card owns the write path and is the moment `HQ_SYNC_REST_URL` is first set in a real deploy, which is the first moment the live leg carries anything at all. Until then the stream is connected to a door that answers 503. Lead: three options in ascending cost — (i) apply the single-clause Realtime `filter` to the three collections where it is expressible and accept `responses`; (ii) reshape the `responses` scope so one clause suffices (e.g. a queryable checklist key on the row, which is a SCHEMA change and therefore a different card's); (iii) fork the plugin's live subscription. 🛑 Do not treat this as reopening C-2: the preference is about pulling a collection whole, and after A1 nothing is pulled whole.

- **B-43 · The replication scope model has no shape for HQ's two ENTRY POINTS — "My Checklists" and "Approvals" are LISTS over many submissions, and the scope is singular by construction** — Card `sync-replication-scope-per-checklist` (run `20260802`, A1) made `scope.checklistId` **mandatory and singular** and scoped `checklists` to `id.eq.<one>`, `approvals` to `submission_id.eq.<one>`, and `responses` to that one submission plus its own field ids. That is exactly right for the checklist-fill view, which is the view preference `architecture/C-2` names. **But it is not the view a crew member lands on.** `workflows.html`'s 3-tab layout opens on **My Checklists**, a list of every submission assigned to this user, and its second tab is **Approvals**, a list of every rejection awaiting this user — both are lists over MANY submissions, and neither can name a single `checklistId`. So under `sync-hard-cutover` there are only two outcomes and nobody has chosen between them: either **those two pages cannot be RxDB-backed at all** (they stay on the REST path, and the cutover is partial by design rather than by accident), or **someone invents a widening** at the moment the page loads — which is a C-2 widening and therefore needs a recorded decision, taken deliberately rather than discovered while wiring. · _run `20260802`, G6 adversarial review of card A1 (finding F-7), confirmed against `sync-rxdb/client.js`'s `normalizeScope` (throws without a single `checklistId`) and `scopeFilterFor` (`checklists` → `id.eq`)_ · promoted → activate-list-views-or-state-they-stay-rest (roadmap round 2026-08-05) · **destination: `sync-hard-cutover`** — that card owns the write path and the page wiring, and is the first moment any page actually calls `startHQReplication`. 🛑 **This is NOT the same gap as `B-42 SYNC-REALTIME-SCOPE`**: B-42 is about the live `postgres_changes` leg being unscoped; this one is about the PULL scope having no expressible shape for a list view. B-42 could be fixed completely and this would still be open. Lead: three options in ascending cost — (i) leave the two list views on the existing REST endpoints and scope RxDB to the fill view only, stating it (cheapest, and arguably what C-2 already implies since a list needs summaries, not every response row); (ii) admit a list scope — `checklists: assigned_to.eq.<userId> AND status.in.(…)` plus a date floor — as an explicitly recorded C-2 widening with a bound that is not "all history"; (iii) a summary projection/view the list reads instead, which is a SCHEMA change and therefore a different card's. · **partial ruling 2026-08-07 (ledger T-43, slate 20260808-2):** Approvals stays on re-fetch (recorded); the My Checklists read path is deliberately kept OPEN by the operator — no card may decide it; entry stays open on that named remainder only
- **B-44 · The same `'submitted'`/`'completed'` doc rot ledger decision 66 filed for `tests/sync.spec.js:1584` ALSO sits at `tests/repro-cut-task.spec.js:169`, and no obligation names it** — 🔢 _renumbered B-39 → B-44 at the B1 fix round, run `overnight-20260802`: three legs of this run each independently claimed `B-39`. The pre-step's B-39/B-40/B-41 merged first and keep those numbers; A1 took B-42/B-43; this leg's two entries move to B-44/B-45 and its new one to B-46._ — obligation 8 of `sync-cache-and-identity-hygiene` was written against one line anchor, so the fix landed on one line. `tests/repro-cut-task.spec.js:169` carries the identical claim (`requires_approval false → 'submitted', freezes the snapshot`) and the server sets `'completed'` (`backend/internal/workflow/repository.go:715-716`). Not folded in under the standing scope freeze: it reddens no key result, and `tests/repro-cut-task.spec.js` is outside B1's footprint and outside the card's obligations. · _run `overnight-20260802`, card B1, found while discharging obligation 8_ · new · lead: one word, and the same one. The reusable lesson is the shape, not the line: **an obligation written as a file:line anchor fixes an instance, not a class** — decision 66 recorded "the stale comment at `tests/sync.spec.js:1584`" when what it had found was a claim about submit status that the codebase repeats. A grep-defined obligation (`grep -rn "requires_approval false" tests/`) would have caught both and cost nothing more. Sibling of B-25 in kind (line anchors rot); this is the version where the anchor was not merely stale but too narrow from the day it was written.

- **B-45 · The full suite's single failure is drawn from a ROTATING POOL — three runs of one tree, three different failures, none of them the documented baseline** — 🔢 _renumbered B-40 → B-45 at the B1 fix round, run `overnight-20260802`, for the number collision recorded in B-44. **Strengthened at the same time with a third corroboration**, which changed the finding from "it moved once" to "the baseline is a distribution."_ — **Three** full `npx playwright test --retries=0` runs of the identical `overnight-20260802` B1 worktree produced **one failure each, and a DIFFERENT one each time**:
  1. `list page progress decrements when another device unchecks a field [LST-17]` (armed, expected) — card leg, isolated stack `TEST_PORT=8305` / `hq_n802_b1i_e2e`.
  2. `Users UI + Access hardening › FR-18/19: adding then removing an individual user grant persists across reload` (`tests/users.spec.js:1185`) — `page.waitForResponse` for `PUT /api/v1/apps/{slug}/permissions` never resolved, 30.4s timeout; **passes in 1.7s** re-run in isolation immediately afterwards. Same stack, DB dropped and recreated before the run.
  3. `tests/inventory.spec.js:2994 › Receipt sync button › manual sync chip shows Synced from {date} using lookback_days` — a 30s `networkidle` timeout; **passes in 1.4s** in isolation. Observed by B1's **G6 adversarial reviewer**, a fresh context on its own stack, which makes it an independent third sample rather than a repeat of the card's own environment.

  4. **(B1 fix round, same night.)** The same tree run **concurrently with another leg's full suite** produced **27 failures in 56.7m**; re-run **alone** on a fresh database it produced **0 failures in 24.2m** — 741 passed, 6 skipped, one summary block. **Nothing in the pool failed at all, including all three armed reds and B-27.** 24 of the 27 contended failures were timeout-class (`Test timeout exceeded`, `waitForResponse`, `toBeVisible`), concentrated in the live cross-device `sync.spec.js` convergence matrix.

  🛑 **Run 4 identifies the MECHANISM, and it is not flakiness in any test.** The 2.3x wall-clock difference on an identical tree is CPU starvation: two 750-test serial suites, each with its own `go run` server, on one box. Under that load the live-sync tests miss their propagation windows. **The pool is contention-driven, so the ritual's per-card verdict depends on what OTHER legs happen to be running** — which no card can see, control, or report. That is the actionable core of this entry.

  Each run's other two titles passed. **B-27 itself (`tests/inventory.spec.js:883`) was green in ALL THREE**, so the documented baseline red did not reproduce once. 🛑 **The point is not any of the three tests.** A 743-test serial suite on one shared `go run` server + one shared database has a ~1-failure-per-run floor drawn from a rotating pool — so **every card's G2 verdict is judged against a baseline that is not stable between runs of the same commit**, while the ritual's own rule (judge against the B-27 baseline, name every survivor by full title) assumes it is. In the reviewer's words: **the baseline is a distribution, not a list**, and the ritual currently has no way to express that. · _run `overnight-20260802`, card B1 G2 leg (runs 1–2) and the B1 G6 adversarial review (run 3)_ · new · lead: **do not chase any of the three tests first.** Establish the distribution: run the suite N≥5 on an untouched `dev` and record which titles fail each time. If the pool is real, two things follow — (a) the actionable fix is the config comment's own escape hatch, `PW_WORKERS` with per-worker `TEST_DB_NAME`+`TEST_PORT`, because all three observed failures are `waitForResponse`/`networkidle`/`toContainText` timeouts under contention on the single server rather than assertion mismatches; and (b) the RITUAL needs a second verdict shape — "one unexplained timeout-class failure, re-run in isolation and passed" must be expressible as a pass-with-note, or every honest card will keep having to argue its baseline. Sibling of B-27 in kind (a red the baseline names) but larger. Related: run 1's Playwright process **hung after printing its summary** (webServer child not reaped; it held :8305 for 35 minutes until killed by hand), and that run's failure stack frame printed a path from **another card's worktree** (`a1-sync-replication-scope-per-checklist`) while the clean re-run printed the correct one — worth confirming the shared `/tmp/playwright-transform-cache-1000` is not serving cross-worktree sourcemaps.

- **B-46 · With no token the launcher paints NOTHING — no message, no spinner, no affordance, permanently** — `index.html`'s fail-closed identity branch is correct about what it refuses (it must not paint the previous user's tiles), but it has no visual half. With no verified `/api/v1/me` and no device identity token, `readCachedApps(deviceId)` returns `null` because `deviceId` is null, `filterTilesByPermissions` is therefore never called, and `.grid` stays at the inline `visibility:hidden` it ships with. The crew member sees the wordmark and empty space. There is no "you're offline", no "sign in to load your tools", no retry — and nothing on screen distinguishes it from a broken app. It **self-heals** on the next `index.html` load that reaches the network, which is why it is not blocking and why it did not surface as a defect during the card. 🛑 It is nonetheless the ordinary first-launch-offline experience on a truck. · _run `overnight-20260802`, card B1 G6 adversarial review, NON-BLOCKING-6; filed rather than built under the run's standing scope freeze — a UI for this is a design decision, not a fix round's call_ · new · **destination: next milestone** · lead: this is a UI-SPEC-shaped item, not a one-liner — the State Enumeration Table needs an "identity unverifiable" row, and the copy has to distinguish "offline, we'll load your tools when signal returns" from "you are signed out" without claiming to know which. Pairs naturally with any card that touches the launcher's empty/loading states; the mechanism side is one `else` branch in `checkAuth()`.

- **B-49 · `submission_rejections` just became the FOURTH collection B-42's unscoped live leg applies to — and it is the one that has never carried a single event** — Card `sync-rxdb-write-policies` (run `20260802`, A2) gives `submission_rejections` a SELECT policy for the first time (ledger T-30 decision 111 consequence 1). The table has been in the `supabase_realtime` publication with `REPLICA IDENTITY FULL` since `sync-schema/sql/0001_sync_tables.sql` shipped it, but with **zero policies it was deny-all**, so Realtime evaluated RLS against every row and delivered nothing — the subscription existed and was inert. It is no longer inert. **B-42 (`SYNC-REALTIME-SCOPE`) was written and scoped against three collections and now applies to four**, and the fourth is the approvals stream, which is the one a crew member's phone is most likely to receive while looking at a different checklist. 🛑 **This is a widening of B-42's blast radius, not a new mechanism** — the seam analysis in B-42 (Realtime's `filter` option accepts a single `column=op.value` clause; `pull.modifier` cannot drop a row) is unchanged and applies verbatim. `approvals` IS expressible as a single clause (`submission_id=eq.X`), so it falls in B-42's option (i) bucket rather than the hard `responses` bucket. Bounded the same way B-42 is: the live leg carries only what other people change while this crew member is looking at their phone. · _run `20260802`, card A2 `sync-rxdb-write-policies`, found while writing `0004_write_policies.sql` §5d — noticed because 0001 enrols the table in the publication and 0003 §5d explains at length why it has no policy, and the two facts only compose once the policy exists. Filed as B-46 and renumbered to B-49 mid-run: the B1 leg merged first and kept B-46, and B-50 was reserved for the substrate-isolation finding. The renumbering note previously sat inside the handle itself, which made the entry unparseable to `night-crew backlog check` — moved here at triage 2026-08-02_ · new · **destination: `sync-hard-cutover`** — the same destination B-42 already has, and for the same reason: that card owns the write path and is the first moment `HQ_SYNC_REST_URL` is set in a real deploy, which is the first moment any live leg carries anything. Lead: fold into B-42's option (i) — `approvals` is single-clause expressible, so it costs nothing extra to include when that fix is made. Do NOT file this as a separate fix; it is a line item on B-42's ticket.

- **B-47 · `sync-schema/sql/` has no applier, no manifest and no ordering test — the only thing that applies the four files in order is a hard-coded list inside a Go test** — `sync-schema/sql/` now holds four files (`0001_sync_tables`, `0002_hq_fdw`, `0003_rls_policies`, `0004_write_policies`) that MUST be applied in that order: 0003's policies call `hq_jwt_claim` which 0003 defines, 0004's `hq_can_approve_field` calls `hq_can_see_field` which 0003 defines, and both depend on foreign tables 0002 creates. There is **no goose, no migration table, no numbered-runner script and nothing that records what a given substrate has had applied.** The ordering exists in exactly two places, both of them incidental: the filename prefixes (a convention, unenforced) and `rvConnect` in `backend/internal/sync/rowvisibility_rls_test.go`, which names all four paths in a fixed sequence. 🛑 **The consequence is that adding `0005_*.sql` requires editing a Go test file to make it apply at all**, and a substrate applied by hand in the wrong order fails with a `function does not exist` error inside a policy at request time — 0002's own header calls that failure mode out for the user mapping and it is the same shape here. Not urgent while nothing is deployed (`HQ_SYNC_REST_URL` is unset everywhere) and while the only consumer is a test that hard-codes the list. It becomes urgent the moment a second environment exists, because there will then be no way to ask a substrate which files it has. · _run `20260802`, card A2 `sync-rxdb-write-policies`, found while adding the fourth file and having to edit `rvConnect` to apply it_ · new · **destination: NEXT milestone (substrate operability).** Lead: cheapest is a `sync-schema/apply.sh` that globs `sql/*.sql | sort` and applies each inside a transaction, plus a one-row `hq_sync_schema_version` table so a substrate can be asked. A test asserting the glob order matches what `rvConnect` applies would close the drift between the two lists without inventing a runner at all, and is worth doing first.

- **B-48 · Nothing STATIC guards `checklist_templates`' deliberate write deny-all — the only thing asserting it needs a live Docker substrate** — Ledger T-30 decision 111 row 1 is an **absence**: `sync-schema/sql/0004_write_policies.sql` deliberately writes no INSERT or UPDATE policy for `checklist_templates`, because the builder keeps the REST path and no phone writes a template definition. Three tables in the same file each got a write policy; this one did not. 🛑 **"Completing the set" is a four-line change that reads as tidying**, and the only thing in the repository that would catch it is variants W1/W2/W3 of `TestRowVisibilityRLS` — which need the Supabase spike stack up, and which (before this card fixed B-36) would have SKIPPED on a machine without it. `tests/sync-schema.spec.js` already demonstrates the missing pattern: it statically asserts `readSqlStatements()` does **not** match `/create\s+policy/` for `0001_sync_tables.sql`, on every machine, with no substrate. The equivalent assertion for 0004 — no `create policy … on public.checklist_templates` that is `for insert`/`for update`/`for all` — costs one test and holds everywhere. Same argument applies to the absence of any DELETE policy in the file (decision 111's table has no DELETE column; RxDB replication is soft-delete only per 0001 contract item 2). · _run `20260802`, card A2 `sync-rxdb-write-policies`, found while writing 0004 §1(a) and noting how much prose was needed to defend an absence_ · new · **destination: NEXT milestone (test hygiene).** Lead: extend `tests/sync-schema.spec.js` with a `0004_write_policies.sql` describe block asserting the two absences — no `create policy … on public.checklist_templates` that is `for insert`/`for update`/`for all`, and no `for delete` policy anywhere in the file.

  🛑 **RE-SPECIFIED at the G6 fix round of run `20260802` — the original lead named the WRONG GUARD, and it would have guarded a NON-DEFECT.** It proposed asserting that `with check` is PRESENT on both UPDATE policies, calling that "the highest-value one, since Postgres itself will never warn about it". Finding F3 measured the opposite: **PostgreSQL substitutes the `using` expression for an omitted `with check`**, so on rows 2 and 3 — where `using`, `with check` and the table's own SELECT policy are all the same expression — omitting the clause changes nothing the database does. Mutation M1 (`with check` deleted from both UPDATE policies, `pg_policies.with_check` NULL) leaves the live suite fully GREEN, correctly. A static guard demanding that clause would have failed a refactor that changed no behaviour, and would have said nothing about the defect that IS reachable.

  **The real static guard, and it is a different shape: on any `for update` policy in this file, the `with check` expression must not be WIDER than the `using` expression, and must not be wider than that table's own SELECT policy in 0003.** That is the case-(C) defect — the only UPDATE mutation any client can observe — and in this file it exists on exactly one policy, `submission_rejections_update`, whose `hq_can_approve_field` is strictly narrower than the table's SELECT rule `hq_can_see_field`. Mutation M1c (`with check ( true )` there) handed an approver the power to move a rejection onto work she is merely ASSIGNED to, and survived the entire live suite until variants WP8/W16 were added for it. Statically, the cheap and honest version is a **literal-text guard**: the `with check` expression of every `for update` policy must be BYTE-IDENTICAL to its own `using` expression, and no `with check` in the file may be `true`. That catches M1b/M1c without a substrate and without a semantic comparator, and it fails loudly the day someone widens one on purpose — which is the moment a human should be reading 0004 §5 anyway. **Add a second guard for the INSERT policies while there: no INSERT `with check` may be `true`** — by finding F3's probe (D) a widened INSERT check IS reachable by any push-only client, since the SELECT policy reaches an inserted row only through RETURNING.

- **B-51 · The "two independent gates refuse a hard DELETE" claim was measurably false — there is ONE gate, because Supabase's default privileges already granted `authenticated` ALL and a `grant` revokes nothing** — `sync-schema/sql/0001_sync_tables.sql` carries `grant select, insert, update on … to authenticated` on all four replicated tables, and both `0004_write_policies.sql` §1(b) and variant W12's comment read that as a privilege gate standing independently of the absent DELETE policy. **It never did.** A `GRANT` is additive; Supabase's own `alter default privileges` had already handed `authenticated` ALL on every table in `public` before 0001 ran. MEASURED on the live substrate, identically on all four tables: `authenticated` holds `DELETE, INSERT, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE` — and under `SYNC_RLS_SKIP_POLICIES=1` all three of W12's DELETEs returned **HTTP 204 with the rows gone**. 🛑 **Behaviour is green today** — the one real gate, RLS enabled with no DELETE policy, is deny-all and holds — **but the defense-in-depth was imaginary and two files stated it as fact.** The prose in both is now corrected and W12 logs the measured grant set rather than asserting a gate it does not have. The fix that would make the second gate real is one statement: `revoke delete, truncate on public.{checklist_templates,checklist_submissions,submission_responses,submission_rejections} from authenticated`. It was NOT applied by the fix round that found it, for two reasons worth carrying: the grants live in **0001, another card's file**, and a revoke written into 0004 instead would red W12 in **both** red modes (each withholds 0004), silently changing the property merge-intent item 8 rests on — that under `SYNC_RLS_SKIP_WRITE_POLICIES=1` every refusal still passes. A substrate privilege change is not an unattended fix round's to make. · _run `20260802`, card A2 `sync-rxdb-write-policies` G6 fix round, finding F4 — found by a fresh-context adversarial reviewer querying `information_schema.role_table_grants` instead of reading 0001_ · new · **destination: `sync-hard-cutover`** — it owns the write path and is the first card that sets `HQ_SYNC_REST_URL` in a real deploy, which is the first moment a stray DELETE privilege can be reached by anything but a test. Lead: put the revoke in **0001**, next to the grant whose wording implied it, so it applies in every mode including both reds and W12's grant assertion can be a hard one. While there: `TRUNCATE` and `REFERENCES` are also held and also unwanted.

- **B-50 · `HQ_RLS_TEST_DB` isolates only the HQ-side FDW source database — the Supabase `public` schema and the single PostgREST have NO isolation variable, so one leg's policies red another leg's suite** — B-35's rule ("every leg MUST set a unique `HQ_RLS_TEST_DB`, `TEST_PORT` and DB prefix") was followed to the letter by every leg on run `20260802`, and it was **not sufficient**. `rvHQDatabase()` moves only the throwaway HQ database that the FDW reads *from*. The substrate the policies actually live on — `spike-supabase-db-1`'s `public` schema — and the single PostgREST in front of it are **shared by every concurrent leg with no override at all**. 🛑 **Observed, not theorised:** card B1's fix round hit `TestRowVisibilityRLS/V18` FAILING in a worktree whose entire diff touches **zero** files under `backend/` and **zero** under `sync-schema/`. Cause: the concurrently-running A2 leg had created `submission_rejections_select/_insert/_update` on the shared substrate, while B1's tree still carried the pre-rewrite V18 asserting those policies are absent. It reproduced with the substrate otherwise quiet, so it was persisted residue rather than a live race. **This is strictly worse than B-35 in one respect: it reds cards that touch no Go whatsoever, so the card that gets blamed is never the card that caused it.** B1 correctly refused to "fix" it — dropping another leg's policies mid-run is the B-16 incident, where a dropped test database read as a passing suite. A2's fix round removed the *residue* half by registering a `t.Cleanup` so `tearDownWritePolicies` runs after **green** runs too (previously only the two red modes tore down, so every successful run left its policies resident); the **race** half is untouched and is what this entry is for. Two further consequences worth carrying: a mutation that "survives" because another leg's policies were resident **reads as a survival**, so mutation-testing evidence taken under concurrency is only as good as its mechanism proof; and `tearDownRowVisRLS` drops a hard-coded list of policies, so it silently goes stale every time a card adds one. · _run `20260802`, found by card B1's G6 fix round while diagnosing a Go failure in a card with no Go in its diff; number reserved by the orchestrator_ · new · **destination: NEXT milestone (run mechanics / test isolation)** — it does not redden a named key result on the current OKR page, so it routes forward under this slate's scope freeze. Leads, in ascending cost: (i) give the substrate a per-leg schema or database and an `HQ_SYNC_SUBSTRATE_DB`-style override, mirroring what `HQ_RLS_TEST_DB` does for the HQ side; (ii) derive the teardown list from the SQL files rather than a hard-coded list, so it cannot go stale; (iii) serialise substrate-touching legs at dispatch, which is the control loop's lever and costs wall-clock rather than code. 🛑 **Until one of these lands, a slate that puts two substrate-touching cards on concurrent tracks is buying a class of unattributable red** — which is exactly what `slate-20260802` §5 warned would surface if gate evidence looked contended. · 🛑 **aggravated 2026-08-28 (A3 re-gate):** Activity 5 made the shared substrate PERSISTENT — `task sync:dev:up` and the suites' default resolution name the same `spike-supabase` project — so residue is now permanent by design: `TestJWTBridgeRLS` reds on `spikec-*` dev rows on ANY tree, and the blamed card can never be the cause. Same tree green against an isolated `-p spike-regate` stack via `SPIKE_DB_URL`/`SPIKE_REST_URL`, so lead (i) works by hand today; the defect is that the DEFAULT resolves the dev stack
- **B-52 · The import-reachability guard is blind to any script reference that is not a literal** — `build-sw.js`'s `checkImportReachability` parses literal `src="…"` attributes and literal ES module specifiers. A page that builds a URL at runtime (`s.src = base + name`), or a module that `import()`s a computed specifier, is invisible to it, and so is a `<link rel="stylesheet">`, an `<img src>` and a CSS `@import` — the guard is deliberately scoped to the **module and script graph**, which is where absence breaks the tool rather than the favicon. **Nothing in the tree does any of this today** (measured: 30 local references across 18 precached files, all literal, all script-or-module), which is exactly why it is filed rather than built — a resolver for computed URLs is a static-analysis project, not a build hook, and it would be guarding a case that does not exist. The honest statement of the guard's reach is "every literal script edge", not "every edge", and this entry exists so the next person to widen it knows which half is missing. · _run `overnight-20260802`, card P1 `build-deploy-manifest-integrity`, found while bounding the parser's scope_ · new · **destination: NEXT milestone** · lead: do not reach for an AST parser first. The cheap 80% is a lint that reds on any assignment to `.src` or a template literal inside `import(` in a precached file, forcing the author to either make it literal or add an explicit allow-list entry — the same shape `tests/sw-manifest.spec.js` uses when it refuses a Dockerfile `COPY` form it cannot model rather than guessing.

- **B-53 · `favicon.ico`, `favicon-32x32.png` and `apple-touch-icon.png` are referenced by every page, committed, and copied into the image by NOTHING — they 404 in prod ONLINE** — all seven HTML pages carry `<link rel="apple-touch-icon" href="apple-touch-icon.png">`, `<link rel="icon" href="favicon.ico">` and `<link rel="icon" sizes="32x32" href="favicon-32x32.png">`. All three files are in HEAD. `backend/Dockerfile` stages `*.html *.js manifest.json`, then `icons`, `lib`, `vendor`, `sync-rxdb` and `sync-schema` — **no root image files at all** — so the embedded FS (`//go:embed all:public`) has never contained them and the browser gets a 404 on every page load, with signal, not just offline. 🛑 **Distinct from B-37 and NOT caught by its guard**, deliberately: the import-reachability check is scoped to the script/module graph, where absence breaks the tool. A missing favicon is cosmetic and lives on the other side of that line — widening the guard to cover it would have coupled a production-outage invariant to a decoration. Note `icons/logo-square.png` IS served (it is under `icons/`), so the PWA install icon is fine; this is the browser-tab and iOS-home-screen icon only. · _run `overnight-20260802`, card P1 `build-deploy-manifest-integrity`, found while bounding the parser's scope (B-52's sibling — same boundary, opposite side)_ · new · **destination: NEXT milestone** · lead: one Dockerfile line each side (`COPY favicon.ico favicon-32x32.png apple-touch-icon.png ./` and the matching `cp`), and then the question worth asking is whether they should ALSO be precached — they are ~4 KB total and a tab icon that vanishes offline is the kind of thing that reads as "the app is broken". Doing both makes the precache count 31 → 34; doing only the Dockerfile half leaves them online-only, which is the status quo minus the 404.

- **B-54 · The precache count `CLAUDE.md` calls the B-37 tripwire (**31**) is pinned by nothing** — `CLAUDE.md:108` tells a human "Precache count is currently **31**; if it moves without an asset being deliberately added or removed, that is the silent drop (B-37) coming back". **No test asserts 31, and `node build-sw.js` does not either.** Measured: dropping `manifest.json`, `version.json` or `icons/**/*.png` from `globPatterns` lowers the count and exits **0** — none of the three is a script or module edge, so the reachability invariant is structurally incapable of seeing them. This is not a defect in the guard: "reachability, not completeness" is the deliberate scope (merge-intent P1 §2.1) and a blanket completeness rule is explicitly what the card refused to build. **It is a defect in the doc**, which promises a tripwire that only exists in a reader's head. · _run `overnight-20260802`, card P1 `build-deploy-manifest-integrity`, G6 fix round — filed, NOT fixed (out of the fix round's four-item scope)_ · new · **destination: `sync-hard-cutover`** — it is the next card to edit `build-sw.js`, and if it adds or removes a precached asset it is the first card that will legitimately move the number, which is exactly when a pin has to be written and re-based in one place. Lead: two honest options, pick one, do not do both by halves. (a) Pin it — `expect(urlsFrom(fs.readFileSync('sw.js')).length).toBe(31)` in `tests/sw-manifest.spec.js`, with the number's justification in the same comment, so a deliberate change is a one-line edit and an accidental one is a red. (b) Soften `CLAUDE.md:108` to say the count is *informational* and the mechanical guard covers the script graph only. (a) is preferred: the count is cheap, stable, and the failure it catches is a production outage.

  🛑 **PROMOTED at morning triage 2026-08-02 — this stopped being hypothetical within one hour of being filed.** Merging `docs/claude-md-night-crew` (5 commits from 2026-07-26, approved at triage) moved the manifest **31 → 32 files, 2139.2 → 2358.8 KB** — **+219 KB precached onto every crew phone** — and `node build-sw.js` **exited 0 in silence**. The added file was `backlog-round.html`, a 225 KB static roadmap-round viewer that no page links to and that **decision 52 (ledger T-22, FORK 2) had already disposed of** as "the operator's own render, never tracked in any branch, left untracked and undeleted". That branch swept it into a `.planning/` cleanup and committed it. 🛑 **The mechanism this exposes is the one worth carrying: `committedOnlyTransform` reads git HEAD (B-37 / decision 67), so it excludes untracked strays *by construction* — which is exactly the protection decision 58 was written to give. A stray that gets COMMITTED walks straight past it, and nothing downstream notices.** The guard is sound; its unstated precondition is that nobody commits the file. Caught at triage only because a human had just read R2 and knew the number was 31 — which is precisely the "enforced by nothing" this entry describes, and the strongest possible argument for option (a). Fixed in place (`git rm --cached` + `.gitignore`, count restored to 31); the **pin is still not written.**

- **B-55 · The reachability parser fires on NON-edges — a specifier inside a comment or a string literal is read as a reference** — measured against `build-sw.js`'s exported `collectLocalRefs`: `'// import x from "./commented.js";'` yields the ref `./commented.js`, and a specifier embedded in a string literal yields one too. The regexes are line-oriented and have no notion of lexical context. **It fails CLOSED** — a phantom ref that resolves outside the precache throws, so no `sw.js` is written and the build is loud — which makes it a nuisance rather than an outage, and is why it is filed rather than fixed. 🛑 **The realistic trigger is not hand-written code.** `vendor/rxdb.bundle.js` is precached, 495 KiB, and **machine-generated** by `vendor/build-vendor.sh`; a future regeneration whose output happens to emit a relative specifier inside a comment banner or a string breaks the build with a message pointing at a file nobody edited and a "reference" that does not exist. 🛑 **This is the false-POSITIVE half. Already-filed B-52 records only the false-NEGATIVE half** (computed `.src`, computed `import()`), and the two want opposite fixes — B-52 wants the parser to see more, this wants it to see less. Anyone widening one should read the other first. · _run `overnight-20260802`, card P1 `build-deploy-manifest-integrity`, G6 fix round — filed, NOT fixed_ · new · **destination: NEXT milestone, with B-52** — same file, same function, one sitting. Lead: do not reach for an AST parser to fix a nuisance. The cheap correct step is to strip `//` line comments and `/* */` blocks before matching (a tokenizer-lite pass, ~10 lines), which kills the comment case outright; the string-literal case is rarer and can stay, because a false positive that fails loudly against a machine-generated file is still better than a silent miss. If it does bite `vendor/rxdb.bundle.js` before then, the one-line unblock is a `SKIP_PARSE` allow-list keyed on the URL — but that trades this nuisance for a genuine blind spot on the single largest precached asset, so it is a last resort and must be filed when used.

- **B-56 · `tests/sw-manifest.spec.js` is the only test in the repo that creates a git commit, and a kill mid-test strands one on the card branch** — verified: `grep -rn "commit" tests/ features/` returns exactly one site, `tests/sw-manifest.spec.js:484`, where the end-to-end reachability test commits a probe page so `committedOnlyTransform` can drop the entry point it imports (B-37's exact shape — the test genuinely needs HEAD to differ). **The teardown is already correct**: a `finally` that captures the SHA up front and `reset --soft`s to it, never `reset --hard HEAD~1`, so it destroys neither a real commit nor a developer's uncommitted work. 🛑 **What `finally` cannot cover is a kill between the `commit` and the `finally`** — Ctrl-C, a harness timeout, an OOM. What survives is a card branch carrying a stray `TEMP reachability probe` commit **and** a regenerated `sw.js`: precisely the class of mid-run mess B-37 bit on during `overnight-20260801`'s merge 3, arriving on a branch a morning triage is about to merge. · _run `overnight-20260802`, card P1 `build-deploy-manifest-integrity`, G6 fix round — filed, NOT fixed_ · new · **destination: NEXT milestone (test-harness hardening, with the H1 `test-harness-fail-loud` lineage)** · lead: the commit does not have to happen in the card's own repo. Run the probe in a throwaway clone under the OS temp dir (`git clone --no-hardlinks --depth 1 . $tmp`, commit there, `node build-sw.js` there, `rm -rf`), and the worst outcome of any kill is an orphaned temp dir. If that proves too slow, the cheap 80% is a `git worktree add` on a detached HEAD — same isolation, no object copy. Either way the invariant to state in the comment is **"no test may ever write to the checked-out branch's HEAD"**, which is currently true only by the grace of a `finally`.

- **B-57 · A residual false deploy path survives in an agent-facing doc — `save-project/SKILL.md` still claims prod deploys over Tailscale to a Windows box** — `.claude/skills/save-project/SKILL.md:231` reads *"To deploy to prod (Windows box via Tailscale → Docker rebuild + restart)"*. That is the **same false SSH/Tailscale claim** card P1 correctly removed from `CLAUDE.md` under B-13: prod actually builds from a separate clone pinned to `origin/main` on **this** box under Docker Desktop (`Taskfile.yml:178-221`), with no SSH and no Tailscale anywhere in the path. 🛑 **Worse than a stale comment, because this file is read by an agent, not a human** — `/save-project` is invoked at the end of a session, which is exactly the moment someone acts on a deploy instruction. `README.md:160` was checked and is already correct, so this is the last known copy. Outside P1's footprint, which is why the fix round filed it instead of taking it. · _run `overnight-20260802`, card P1 `build-deploy-manifest-integrity`, G6 fix round — filed, NOT fixed_ · new · **destination: NEXT milestone; take it opportunistically on any card that touches deploy docs — it is a one-line edit with no test surface** · lead: replace the parenthetical with the real mechanism ("prod clone → `git fetch` + `reset --hard origin/main` → `docker compose build` → `up -d`") and, while there, carry over the B-13 consequence that actually changes behaviour: **`task prod:deploy` does NOT run `task sw`**, so an `sw.js` you did not commit does not deploy — which is a thing `/save-project` is in a position to get wrong.

- **B-58 · `submission_rejections_update`'s `USING` clause is pinned by nothing — three separate comments assert a guard that measurement says is not one** — `sync-schema/sql/0004_write_policies.sql:483` reads `-- 🛑 approve, NOT see. See (2) above; W11 is the variant.`; §5d(2) states that copying the SELECT policy's `using` here *"is exactly what lets W11 through"*; and `backend/internal/sync/rowvisibility_rls_test.go:1922` reads `🛑 THIS VARIANT TESTS THE using CLAUSE AND ONLY THE using CLAUSE`. **All three are false.** Measured at triage by adversarial mutation: `using ( public.hq_can_approve_field(field_id) )` → `using ( public.hq_can_see_field(field_id) )` — precisely the substitution all three comments name — leaves `ok github.com/yumyums/hq/internal/sync 6.566s`, **all 54 subtests green, W11 among them** (also V18, W9, W16, WP7, WP8). 🛑 **Not a live vulnerability**: the refusal is still delivered by the narrow `with check`, so the table stays safe and no client can reach the widened read. What fails is the *proof*. This is the same defect class card A2 was chartered to remove — a suite that cannot tell its own policy from a mutant — surviving inside A2's own deliverable, and it is the one clause E-KR2's "the suite can now discriminate" claim does not cover. · _morning triage 2026-08-02, adversarial reproduction of run `20260802`; not disclosed by the run — its own mutation set did not include this clause_ · new · **destination: `sync-hard-cutover`** — it owns the write path and is already carrying B-42/B-49/B-51, and it is the next card to read 0004 §5 · lead: the cheap discriminating variant is a rejection on a field the approver can SEE but cannot APPROVE, asserted refused on UPDATE — that is the only shape the two functions disagree on, and it is what W11 was believed to be doing. Pairs with **B-48**'s re-specified static guard, which independently wants `with check` byte-identical to `using` on every `for update` policy in this file; do both in one sitting and the clause is pinned statically and dynamically.

- **B-59 · The guard-attribution comment at `build-sw.js:455-458` names two tests; only one of them fails when the guard is deleted** — the comment above the `api-cache` `cacheKeyWillBeUsed` hook reads *"Deleting this hook re-opens the cross-tenant read in full and nothing else notices: `build-sw.js` still exits 0, the precache is still 29 files, the SW still installs. Only `[B1-XT-01]` and `[B1-XT-02]` fail."* Measured at triage by deleting the hook and rebuilding: **`[B1-XT-02]` RED, `[B1-XT-01]` GREEN.** `[B1-XT-01]` — the test that actually demonstrates the roster leak — is closed by `login.html`'s `purgeDeviceIdentity()`, not by the cache-key partition, so it does not discriminate this hook at all. The stated precache count is also stale (**31**, not 29, since P1 landed in the same run). 🛑 **The run's own `merge-intent-b1-…md` mutation table row M1 records the correct result** — so the finding is not that anyone was fooled, but that the honest measurement landed in the merge-intent note and never reached the comment at the mutation site, which is the artifact the next engineer reads *before deciding it is safe to delete the hook*. Same class as B-58 and as the defect B1 was chartered to remove. · _morning triage 2026-08-02, adversarial reproduction of run `20260802`; not disclosed by the run_ · new · **destination: NEXT milestone (comment/guard hygiene), or opportunistically on any card that touches `build-sw.js`'s runtime-caching block — it is a comment-only edit with no test surface** · lead: replace the two-test claim with the measured one (`[B1-XT-02]` alone, plus which test closes the `login.html` half and why they are different mechanisms), and drop the hard-coded precache number rather than re-pinning it to 31 — a count in a comment is exactly what **B-54** says nothing enforces.

- **B-60 · `night-crew backlog check` has never validated this file, and the divergence is grammar, not rot** — running the deployed CLI against `.night-crew/knowledge/BACKLOG.md` reports **249 issues across 119 entries**. Measured at triage: `dev`'s pre-run copy already failed **222 across 101**, so tonight's 19 entries added ~27 issues at the same rate as the previous 100 — **this is a standing format divergence, not a regression any run introduced.** The validator (`openspec/specs/backlog-store/spec.md`) wants a status of exactly `new` / `promoted → <card>` / `dropped — reason` plus a separate `lead:` field; this file writes `· new · **destination: <where>** · lead: <line>`, and often folds a multi-sentence lead into the destination clause. 🛑 **The house style carries strictly more information than the grammar it fails** — a destination is a routing decision the validator has no field for, and destinations are what makes HANDOFF's "every bullet names its destination" (B-38) checkable. Do not migrate 119 entries to satisfy a parser. · _morning triage 2026-08-02, step 4.5 backlog validation_ · new · **destination: NEXT milestone (tooling), and it is a night-crew-clone change rather than an hq one** · lead: the cheap correct move is to teach the validator the `destination:` field as a first-class alternative to `promoted → <card>`, since they encode the same thing at different confidence; failing that, treat this file as out of the validator's scope and say so in the spec, so a triage that runs the check does not read 249 issues as 249 defects. Until one of those lands, **step 4.5 of `/nc-morning-triage` produces noise rather than signal on this repo** — which is its own small instance of a check that cannot fail usefully.

- **B-61 · The RxDB-backed "My Checklists" list is NARROWER than the REST list it replaces — HQ's `myChecklists` returns every submission since `current_date` for EVERYONE, and RLS admits only the caller's own assigned templates** — measured at source, not inferred: `backend/internal/workflow/repository.go`'s submissions query is `FROM checklist_submissions s ... WHERE s.submitted_at >= current_date` with **no per-user predicate at all**, and the comment above it states the rule as a product decision — *"Today's submissions — checklists are team objects, all members see all submissions."* The sync substrate cannot reproduce that: `checklist_submissions_select` is `hq_can_see_template(template_id)` (0003 §5b), so a crew member replicating the list sees submissions on **their own assigned templates only**. 🛑 **This is a product-visible behaviour change at cutover and it is NOT caused by the list scope** — the client-side scope could be widened to every template and the rows still would not arrive, because the refusal is the server's. Widening the SELECT policy to match is explicitly out of bounds: decision 111 authorises four rows and this would be a fifth. 🛑 **Neither is it a security finding** — the substrate is strictly tighter than the REST path, not looser. What it is: a list that will visibly lose rows the day `HQ_SYNC_REST_URL` is first set, on a truck where two crew members split a shift and each expects to see the other's completed opening checklist. · _run `20260803`, card S1a `sync-cutover-list-scope`, found while looking for the `assigned_to` column the slate's list scope names — there is none, and reading `myChecklists` to find out what the page actually lists is what surfaced this_ · **dropped — resolved at morning triage 2026-08-02 (ledger decision 127): the narrowing never occurs.** F-2 was answered "nothing changes" — both list tabs stay on REST as a consequence of F-1's read/write split (decision 126), so no list is rendered from RxDB and no row goes missing. Kept as record. Note for the successor card: HQ's team-wide visibility rule is still written down nowhere but a code comment in `repository.go` (`myChecklists`), and should become a recorded product decision before anything re-opens it** · lead: three options, and the choice is the OPERATOR's because it is a product rule, not a mechanism — (i) accept the narrowing and say so in the release note (cheapest; arguably the more defensible rule, since "all members see all submissions" was never an authorization decision anyone took deliberately); (ii) keep the two list tabs on the existing REST path and RxDB-back only the fill view, which is B-43's rejected framing and would re-open it; (iii) a fifth policy row, which needs decision 111 re-opened and is not a card's to invent. 🛑 **Do not resolve this by widening `scope.templateIds`** — that is the one move that looks like a fix and changes nothing.

- **B-62 · The Realtime `filter` B-42 option (i) now emits is proved at the CONFIG, never against a live Realtime server — nothing in the tree shows the substrate honours it** — card S1a wires a single `column=op.value` clause into the plugin's `postgres_changes` binding through a shim on `client.channel` (`sync-rxdb/client.js`), and `tests/sync-rxdb-client.spec.js` [SCOPE-04] proves the clause is correct, is one clause, reaches the binding config under the right channel name, and is absent on `responses`. **Every one of those assertions is about the object handed to the library.** No test connects to `spike-supabase-realtime-1`, publishes an out-of-scope row and observes that it does not arrive — so a filter that is syntactically perfect and semantically ignored (wrong column name for the publication's `REPLICA IDENTITY FULL` payload, an operator Realtime's filter parser rejects, a version that silently drops unknown keys) would pass the whole suite. 🛑 **The failure mode is the quiet one this repo keeps re-finding:** the filter is a NARROWING, so if it is ignored the system behaves exactly as it did before the card — B-42's unscoped live leg, still open, but now with three files saying it is closed. 🛑 **Not reachable today, which is why it is filed rather than built:** `HQ_SYNC_REST_URL` is unset in every deploy, the door answers 503, and nothing has ever opened a live subscription through it — a test would be proving a property of a connection this milestone does not yet make. · _run `20260803`, card S1a `sync-cutover-list-scope`, found while bounding what [SCOPE-04] is evidence for_ · promoted → spike-d-realtime-live (roadmap round 2026-08-05) · **done — answered GREEN by card D, run `20260807-2` (T-41)**: the live Realtime server honours the filter in all three production clause shapes (`gte`, `eq`, `in.(…)`), suppression attributable via a same-socket unfiltered control, and the `responses` unfiltered residual is now measured, not asserted. The recorded destination (`sync-hard-cutover`, "first moment this is testable") was pessimistic in a useful way — the property was testable against the spike stack with `HQ_SYNC_REST_URL` never set and the interlock armed; triage verdict re-executed live (green exit 0 AND red `--no-filter` exit 1) by the adversarial review · lead: the shape already exists in this repo — `TestRowVisibilityRLS`'s WITHOUT/WITH pairs. Subscribe with the filter, `INSERT` one in-scope row and one out-of-scope row through the pool as the substrate superuser, assert the in-scope one arrives (or the test is vacuous — an ignored filter and a dead subscription look identical) and the out-of-scope one does not. Pair it with `responses`, where the filter is deliberately absent, as the negative control: **both** rows must arrive there, which is what turns "we chose not to filter it" from a comment into a measurement.

- **B-63 · A crew member opening a checklist FROM the list will have TWO replications live over the same four local collections, and nothing says whether that is the design** — after S1a there are two scope shapes, and they mint different `replicationIdentifier`s by construction (the fingerprint carries a `list:`/`fill:` identity term), so a page that starts a LIST replication on load and a FILL replication when a checklist is opened gets **two live `replicateSupabase` states, two Realtime channels, and two pull loops writing into `db.templates` / `db.checklists` / `db.responses` / `db.approvals` simultaneously.** 🛑 **That may well be correct** — the list must stay live while a checklist is open, which is the whole of the operator's 2026-08-02 decision — but `sync-rxdb/client.js`'s standing instruction is the opposite one (*"🛑 CALLERS MUST CANCEL BEFORE RE-SCOPING"*, written when only one scope existed), and a reader following it literally would cancel the list replication on open and silently break the live list the widening was bought to keep. Nothing in the tree resolves the contradiction, and no test drives two concurrent scopes. **What is NOT at risk:** the checkpoints, which are per-identifier and therefore already independent (that is F-1's fix doing its job on a case F-1 never contemplated). **What is:** the local collections, which both replications write to, and the conflict handler, which will now see master states arriving from two pulls with different scopes. · _run `20260803`, card S1a `sync-cutover-list-scope`, found while writing the checkpoint-identity test — the same property that makes two USERS safe is what makes two SCOPES concurrent_ · promoted → activate-list-views-or-state-they-stay-rest (roadmap round 2026-08-05) · **destination: `sync-hard-cutover`** — S1a is forbidden from touching `sync-rxdb/bootstrap.js` and `workflows.html`, and this is entirely a page-lifecycle question, which is S1b's by charter · lead: decide it explicitly and write it into `startHQReplication`'s banner, replacing the now-ambiguous cancel instruction: one list replication for the session, one fill replication per open checklist, cancelled on close, and the cancel rule restated as *"cancel before RE-SCOPING THE SAME SHAPE"* rather than *"before re-scoping"*. Then drive both at once in `tests/sync-rxdb-client.spec.js`'s existing recorder — it already captures per-collection options, so two `startHQReplication` calls and an assertion that the eight identifiers are pairwise distinct is a handful of lines. · **ruled 2026-08-07 (ledger T-43, slate 20260808-2):** concurrent fill replications ARE the design — operator product requirement (setup + food-prep checklists worked simultaneously); one per open checklist, cancelled on close; cancel banner to be restated as "cancel before re-scoping THE SAME shape" (rides cards C3/S1) · 🟢 **CLOSED JOINTLY** — run `20260808-2`, cards **C3 `activate-fill-view-reads`** (branch `card/c3-activate-fill-view-reads`, commit `61d13d0`) and **S1 `list-views-decision-recording`** (branch `card/s1-list-views-decision-recording`, commit `35cb917`). The behavior half: C3's `HQFillSync` / `FILL_SYNC_SCOPES` is a MAP, not a slot — opening a second checklist's fill scope does not cancel the first, `close()` cancels exactly one — and `tests/sync-rxdb-client.spec.js`'s `[SCOPE-05]` block asserts two concurrent fill scopes mint pairwise-distinct identifiers (the regression guard for the property this ruling makes load-bearing) plus the shared-phone case (F-2). The documentation half: S1 restated the banner in `sync-rxdb/bootstrap.js` (the `startReplication` comment) and `sync-rxdb/client.js` (both the `startHQReplication` docblock and the older REPLICATION SCOPE design-block tail) as "cancel before re-scoping THE SAME shape" — a caller holding several DIFFERENT shapes at once is the design, not a leak.

- **B-64 · `sync-rxdb/bootstrap.js`'s scope banner went stale the moment S1a landed, and S1a is FORBIDDEN from fixing it** — `sync-rxdb/bootstrap.js:80-86` documents the public surface every page reaches replication through, and after card `sync-cutover-list-scope` it is wrong in two ways at once. It says *"REQUIRES a scope — `startReplication(db, client, {scope:{checklistId, templateId, fieldIds}})` … `checklistId` AND `templateId` are both mandatory"*, which is now **one of two shapes** — the LIST scope is `{mode:'list', userId, since, templateIds}` and names none of those three keys, so a reader who trusts this banner concludes the list views cannot be replicated at all, which is the exact conclusion the operator's 2026-08-02 decision overturned. And it says *"🛑 CANCEL the previous states before starting a re-scoped replication"*, which under two scope shapes reads as an instruction to cancel the LIST replication when a checklist is opened — **silently breaking the live list the widening was bought to keep** (that half is B-63's, filed separately because it is a design decision and this is a comment). 🛑 **Filed rather than fixed, and the reason is the interesting part:** `sync-rxdb/bootstrap.js` is one of two files S1a's card names HARD-UNTOUCHED at `done_when:` level, because S1b `sync-hard-cutover` owns the wiring. So the card that made this comment stale is structurally barred from correcting it — **a footprint boundary drawn between a behaviour and its own documentation.** Worth carrying as a shape, not just an instance: the same split would recur any time a card owns an API and another card owns its only caller. · _run `20260803`, card S1a `sync-cutover-list-scope`, found while checking that no HARD-untouched file needed to change_ · new · **destination: `sync-hard-cutover`** — it owns the file, it is cut immediately after S1a merges, and it has to open `bootstrap.js` anyway to wire the two scopes · lead: replace the banner with the two shapes and the corrected cancel rule (*"cancel before re-scoping THE SAME SHAPE"* — see B-63), and take the opportunity to point at `sync-rxdb/client.js`'s LIST SCOPE block rather than restating it, since a second copy of a rule is a second thing to go stale. **Do this in the same commit as B-63's decision**, not before it — the comment cannot be correct until the lifecycle question it describes is answered. · 🟢 **CLOSED** — run `20260808-2`, card **S1 `list-views-decision-recording`**, branch `card/s1-list-views-decision-recording`, commit `35cb917`. Found the banner had moved (not fixed) since this entry was filed: C2 added ~250 lines above the slate-cited `bootstrap.js:80-86`, so the stale comment is now the one on the `startReplication` property inside the `HQSync` object literal. Rewritten to name both shapes (pointing at `client.js`'s `startHQReplication` docblock as the shape of record rather than restating fields, per this entry's own lead), state what's live today (C2's flagged dev surface; C3's per-open-checklist fill scopes), state what stays REST (both list views; Approvals BY RULING — ledger T-43(a)), state My Checklists as OPEN (ledger T-43(b), no outcome predicted), and restate the cancel rule as "cancel before re-scoping THE SAME shape" (B-63). Same commit also restates the two client.js banners carrying the pre-B-63 wording (the `startHQReplication` docblock C3's merge-intent named, and the older REPLICATION SCOPE design-block tail) without touching C3's FILL-shape line. Docs-only diff; RF n/a (non-code deliverable, reason recorded in the card's merge-intent) — B-63's behavior half is proven by C3's own tests, cited jointly there.

- **B-65 · `autoSaveField` is called at `workflows.html:2219` and is defined NOWHERE — a live `ReferenceError` on the fail-note-photo path** — the card charter, the roadmap bullet, `sync-rxdb/bootstrap.js:9` and `sync-rxdb/conflict-notice-ui.js:23` all name `autoSaveField` → `POST /saveResponse` as the live per-field write path. Measured: `function autoSaveField`, `autoSaveField =` and `autoSaveField:` return **zero hits** repo-wide. The only executable reference is `workflows.html:2219` — `autoSaveField(fldId, FIELD_RESPONSES[fldId] ? … : '')`, reached after a fail-note photo upload presigns successfully — which throws `ReferenceError` the moment it runs. The real path is `debouncedSaveField` (`workflows.html:389`, ~18 call sites) → `submitOp('SET_FIELD', …)` (`:404`) → `POST /ops` → `workflowOpRouter` (`main.go:62`) → `workflow.SaveResponseFunc`. 🛑 **The interesting part is why nothing caught it:** `tests/sync-rxdb-client.spec.js:1460` asserts `expect(src).toContain('autoSaveField')`, which passes on the *comment* text alone — a test that reads as coverage of a function that does not exist. So four files document a write path the tool does not have, and one test appears to guard it. · _run `20260803`, card S1b `sync-hard-cutover`, found while mapping the two write paths the card was chartered to replace — there is one, and it is not this one_ · new · **destination: next milestone** (a small fix card — it is a real user-facing break on a real path, independent of the cutover) · lead: decide whether line 2219 should call `debouncedSaveField` or was meant to be deleted with an earlier refactor, then fix the assertion at `sync-rxdb-client.spec.js:1460` to match a symbol rather than a substring, since substring-matching source is what let a phantom function look tested. **✅ RESOLVED 2026-08-04 on `card/a2-workflows-autosavefield-phantom` (run `overnight-20260804`, card A2, base `bb6ff80`).** Both halves of this entry were confirmed by execution, not by reading: `[FLD-16B]` in `tests/persistence.spec.js` drives the real capture path (presign + S3 PUT intercepted, hidden file input fed via Playwright's filechooser) and went RED with `Fail photo upload failed: ReferenceError: autoSaveField is not defined at http://localhost:8202/workflows.html:2219:9`. **The lead's first question — "call `debouncedSaveField` or delete the line?" — answers CALL:** deleting it would leave the photo unpersisted, which is the same user-visible bug. **The lead's second — the substring assertion — is fixed:** `tests/sync-rxdb-client.spec.js` now asserts on the invocation (`debouncedSaveField(`, `submitOp('SET_FIELD'`) rather than a bare identifier a comment could satisfy. 🛑 **Two things this entry did not know.** (1) The ARGUMENT was independently wrong: `resp.value || resp` on a fail card — where the answer is falsy by construction — evaluates to the whole response **object**, so a rename-only fix would have written garbage as the field's value. Shipped as `resp ? resp.value : null`, mirroring the correction-photo path at `:2151`. (2) **A second phantom rode along:** every one of those documentation sites also names the transport as `POST /saveResponse`, and no frontend code posts to it — the endpoint exists on the backend and both test suites drive it directly, but the op journal has been the single write channel (D-08) since the sync work landed. Corrected at eight sites, not four: `CLAUDE.md`, `docs/data-flow-audit.md` (all 7 save-path cells + the rule block + row 8, which still called photo capture "DEFERRED — Phase 12"), `README.md:49`, `.claude/skills/save-project/SKILL.md:53`, `sync-rxdb/bootstrap.js:9`, `sync-rxdb/conflict-notice-ui.js:23`, two banner comments in `workflows.html`, and `roadmap.md` twice.

- **B-66 · The RxDB Supabase push plugin recovers from a unique violation by re-fetching on the PRIMARY KEY, but draft-response uniqueness is on `(field_id, answered_by)` — two devices answering the same field throw `doc not found`** — measured in the shipped bundle (`vendor/rxdb.bundle.js`, `replicateSupabase`): on a `23505` the insert path calls a re-fetch that does `.select().eq(<primaryKey>, …).limit(1)` and, on an empty result, `throw new Error("doc not found " + id)`. The substrate's draft uniqueness is **not** the primary key — `sync-schema/sql/0001_sync_tables.sql:214-217` declares `submission_responses_draft_idx on (field_id, answered_by) where submission_id is null`, while `id` is a client-minted `text primary key`. Two devices (or one device across a reinstall) that answer the same field mint **different** `id`s, so the second push violates the partial index, the recovery looks for a row with *its own* id, finds none, and throws — a replication error rather than a merge. 🛑 **This is the offline-truck case, not an exotic one:** it is exactly a shared field answered by two crew members, which is the scenario the conflict handler exists for, and it fails *before* the conflict handler is reached. · _run `20260803`, card S1b `sync-hard-cutover`, found while establishing whether the push path is real (it is — not a stub)_ · new · **destination: the `sync-hard-cutover` successor card** — it is unreachable until replication is actually started, which F-1 gates · lead: two candidate shapes — make the client-minted `id` DETERMINISTIC for drafts (a v5-style hash of `field_id` + `answered_by`, so both devices mint the same id and the conflict becomes an ordinary update the handler can see), or add a push `modifier`/custom handler that resolves on the natural key. The first is smaller and turns the whole class into the LWW case the handler already covers.

- **B-67 · `sync-schema/sql/0001`–`0004` are applied by NOTHING outside the Go test harness — the substrate schema is not deployed anywhere** — the four files create the replicated tables, the FDW bridge, the read policies and the write policies, and they are the substance of three shipped cards. Measured: repo-wide grep for `0001_sync_tables|0003_rls_policies|0004_write_policies|sync-schema/sql` finds exactly **one** consumer — `backend/internal/sync/rowvisibility_rls_test.go:442-522`, which reads them off disk and applies them into a throwaway test database. `docker-compose.supabase.yml` mounts only `.night-crew/qa/spike-supabase/initdb/*`; there is no sync-schema mount, no Taskfile target, no migration runner, no ordering guard. 🛑 **The consequence is that the policies are proved and undeployable in the same breath:** `TestRowVisibilityRLS`'s 59 subtests are real evidence about the SQL, and there is no mechanism that would put that SQL onto a substrate a phone talks to. Whoever first sets `HQ_SYNC_REST_URL` against a live stack would be pointing at tables that either do not exist or carry whatever the spike's `initdb` left. · _run `20260803`, card S1b `sync-hard-cutover`, found while establishing what "set `HQ_SYNC_REST_URL`" would actually require_ · new · **destination: the `sync-hard-cutover` successor card** — it is a precondition of any deploy of the sync layer, and it must land before, not with, the first activation · lead: the four files are already idempotent and ordered, so this is a runner plus an application order, not a redesign. Prefer a Taskfile target that applies them against a named substrate over a compose `initdb` mount, because `initdb` runs once per volume and would silently skip on an existing stack — which is the same class of silent-skip this repo has been burned by.

- **B-68 · `backend/internal/sync/{ops,handler}.go` is the transport for ALL workflow business logic, not just the op-log — deleting them deletes the submit-time validators** — a SECOND footprint correction on the same package the slate already corrected once (for `jwtbridge`/`proxy`/`rowvisibility_rls_test.go`), and this one lands on the files marked DELETE. `backend/cmd/server/main.go:47-95`'s `workflowOpRouter` is the router behind `POST /ops`: `SET_FIELD` → `workflow.SaveResponseFunc` (with the `ErrUnknownField` → 422 path), and `SUBMIT_CHECKLIST` → `workflow.ValidateFailNotesFunc` → `ValidateResubmitPhotoFunc` → `SubmitChecklistFunc` (with `ErrTemplateArchived` → 409). RxDB pushes rows to PostgREST and can run **no Go validator at all**. 🛑 **Be precise about what does and does not survive**, because the two halves differ: `saveResponse`'s app-level field-existence guard — the *"churn-driven Friday P0"* check at `repository.go:795-807` — **does** have a substrate equivalent in the `submission_responses_insert` policy's `with check (hq_can_see_field(field_id))`, pinned by `TestRowVisibilityRLS/W8`. That one is safe. The **`SUBMIT_CHECKLIST` validators have no RLS counterpart whatsoever** — fail-note completeness, resubmit-photo requirement and archived-template rejection are business rules no row policy can express. · _run `20260803`, card S1b `sync-hard-cutover`, found while sizing the "delete the op-log half" instruction against what the files actually carry_ · new · **destination: the `sync-hard-cutover` successor card** — it is a `done_when:`-level footprint fact, the same severity as the correction the slate already carries · lead: the submit path is a genuinely different problem from the per-field write path and should probably be scoped out of any cutover — a checklist submission is a low-frequency, online, transactional act with server-side validation, which is the profile REST is good at and offline replication is bad at.

- **B-69 · `sync.js` is not separable as "the op-log" — it also holds `api()` (12 call sites), the offline SUBMISSION queue, and the app timezone** — the card footprint says `sync.js` (**deleted**) with a `done_when:` grep, by analogy with the backend's "delete the op-log half". Measured symbol by symbol, the 799 lines are three buckets and only one is the op-log. **OP-LOG/WS** (RxDB replaces): `LamportClock`, `LAMPORT_CLOCK`, `wsConnect`, `applyOp`, `submitOp`, `flashField`, `enqueueSyncToast`, `showSyncToast`, `renderFieldResponse`, `rerenderOpenChecklistAfterSave`. **SUBMISSION QUEUE** (RxDB does *not* replace — whole-checklist submit with `idempotency_key`, guarded by decision 60): `enqueueSubmission`, `drainQueue`, `renderSyncBanner`, `currentSubmitPeriod`, the four `idb*` helpers. **GENERAL INFRA** (unrelated to sync): **`api()` — `workflows.html`'s only fetch wrapper, 12 call sites**, plus `generateUUID`, `APP_TIMEZONE`/`appDateString` (card A1-TZ's standardisation), `store`, `SAVE_DEBOUNCE`, `updateSaveStatus`. 🛑 **Not a park reason on its own** — the honest resolution is to split the file, delete the op-log half outright and move the remainder under a truthful name, which satisfies the grep with no parallel run. Filed so the successor sizes itself correctly instead of discovering it mid-swap. Three symbols are already dead and can go free: `idbDelete`, `rerenderOpenChecklistAfterSave`, and the `Store` class (only the `store` singleton is used). · _run `20260803`, card S1b `sync-hard-cutover`_ · new · **destination: the `sync-hard-cutover` successor card** · lead: do the split as its own commit BEFORE any write-path change, so the cutover diff is a write-path diff rather than a 799-line move plus a write-path change.

- **B-70 · A raw NUL byte in `sync-rxdb/client.js` puts every `grep` into binary mode, so a gate grep returns nothing and reads as a pass** — `sync-rxdb/client.js` contains exactly one NUL (`U+0000`), at byte offset 50850, deliberately: it is a delimiter inside a template literal, `scopeFingerprint(` + backtick + `${scopeIdentity(s)}<NUL>${serialized}` + backtick + `)`, chosen so the separator cannot occur in either operand. The technique is sound and the code is correct. **The side effect is not.** `file(1)` reports the file as `data`, and GNU grep switches to binary mode — so `grep -n 'export' sync-rxdb/client.js` **prints nothing and exits 1**, on a file containing 29 occurrences of `export`. Reproduced live at the start of card S1b: an early survey concluded `client.js` had no exports and no push path, both false, and the error was caught only because the conclusion was implausible enough to re-check with `grep -a`. 🛑 **The failure class is the one this repo keeps arming against:** several `done_when:` rows in this milestone are *"`grep -rn … ` returns nothing"*, and a `grep` that silently returns nothing because of a NUL byte is indistinguishable from a `grep` that returns nothing because the work is done. Any future gate grep whose scope includes `sync-rxdb/` is currently unreliable in the passing direction. · _run `20260803`, card S1b `sync-hard-cutover`, found by being wrong first_ · new · **destination: next milestone** (tooling hygiene — cheap, and it protects gate evidence) · lead: two independent fixes, do both. (a) Replace the raw NUL with the escape sequence `\0` (backslash-zero) in the template literal — the same byte at runtime, but the source stays 7-bit clean so `file` and `grep` behave. (b) Add `-a` to any gate grep that can reach `sync-rxdb/`, or mark the path `-text` in `.gitattributes`, so the guarantee does not depend on remembering.

- **B-71 · A contract test that decodes into the handler's own struct cannot detect doc-vs-code drift — and both sales-processor contracts said those tests proved the opposite** — `backend/internal/inventory/period_summary_test.go` and `backend/internal/recipes/menu_cogs_test.go` unmarshal responses into `inventory.PeriodSummary` / `recipes.MenuCOGSResponse` — the same structs the handlers marshal. A JSON key the *contract document* gets wrong is therefore invisible to every test in both suites: test and handler agree with each other and both disagree with the published page. Both documents nevertheless claimed their tests were "the executable proof that the HQ side matches this contract", and that claim is what stopped anyone diffing for eight weeks. Measured on 2026-08-03: **four of nine `/menu-cogs` per-item field names have never matched the code**, through a permanently green suite. · _run `20260803`, card P6 `period-summary-contract-notice`_ · new · **destination: next milestone** (test-shape work, cheap, and it is the only mechanism that would have caught this class) · lead: add one golden-JSON test per service-token endpoint that asserts on **raw keys** — unmarshal to `map[string]any` and compare the key set against a checked-in fixture, so adding, renaming or `omitempty`-hiding a field fails a test instead of silently editing the contract. Two tests total. The fixture doubles as the response example the contract documents publish, which removes the transcription step that caused the original divergence.

- **B-72 · `/menu-cogs` omits menu items that sold but have no recipe, so the endpoint cannot name what it failed to cost** — `MenuCogsHandler`'s summary query inner-joins `menu_items` to the recipe allocation set (`JOIN alloc a ON a.menu_item_id = mi.id`) and LEFT-joins sales, so row membership is decided by **having a recipe**, not by having sales. A menu item that sold 30 units with no recipe row attached is absent from `menu_items` entirely. Its ingredient spend still lands in `unallocated_cogs` — with nothing naming the menu item — so the operator sees a residual dollar figure and no way to learn which seller is uncosted. The Phase 999.2 contract published the opposite behaviour as **State C** and gave sales-processor an acceptance test for it (**Scenario 2**) that could never have passed; both were withdrawn on 2026-08-03. There is no HQ test for the case — the nearest, `TestMenuCogs_UnitsSoldZero_PerUnitIsNull`, exercises the reverse (recipe, no sales). 🛑 **Not fixed on this card by design:** `/menu-cogs` is consumed by an external system, so changing row membership adds rows to every response and is a contract change, not a bug fix. · _run `20260803`, card P6 `period-summary-contract-notice`_ · new · **destination: next milestone, and it needs the operator's answer to §8 Q3 of `docs/contracts/inventory-menu-cogs.md` before it can be planned** · lead: `JOIN alloc` → `LEFT JOIN alloc`, which yields the documented shape (`ingredient_cost_total: 0.00`, per-unit `null`) for exactly these rows; pair with the golden-JSON test from B-71 and a row asserting a sold-but-uncosted item appears.

- **B-73 · `/period-summary` and `/menu-cogs` stopped reconciling on 2026-06-06 and the broken invariant is still published, unmonitored** — `docs/contracts/inventory-menu-cogs.md` published `SUM(menu_items[].ingredient_cost_total) + unallocated_cogs ≈ period-summary cogs_incl_tax`, within $0.10. It held when both endpoints were written and broke when the **sibling** changed: `a726029` (2026-06-05) gave `/period-summary` a `mercury_category = ANY(HQ_COGS_CATEGORY_ALLOWLIST)` filter that `/menu-cogs` does not apply, and `d41faef` (2026-06-06) folded unconfirmed `pending_purchases` into `/period-summary`'s COGS at full `ABS(bank_total)`, which `/menu-cogs` cannot see because pending rows have no `purchase_line_items` yet. **The divergence runs in both directions and is bounded by nothing** — its size depends on how much of a period is miscategorised and how much sits in the review queue, neither knowable from either response. Any sales-processor reconciliation check written against the published invariant fires spuriously. The invariant is now marked broken in the contract, but nothing on either side detects the divergence. · _run `20260803`, card P6 `period-summary-contract-notice`_ · new · **destination: next milestone, gated on the operator's answer to §8 Q4 of `docs/contracts/inventory-menu-cogs.md`** — it is a genuine product fork (re-align the two endpoints, or formally declare them non-reconcilable and retire the check) · lead: whichever way it goes, add an HQ-side test that computes both figures over one fixture and asserts the *chosen* relationship, so the next divergence fails a gate rather than a counterparty's spreadsheet.

- **B-74 · `HQ_COGS_CATEGORY_ALLOWLIST` silently restates every historical period, with no migration and no signal to the consumer** — `envOrDefault("HQ_COGS_CATEGORY_ALLOWLIST", "COGS")` (`backend/cmd/server/main.go`, from `a726029`) decides which Mercury `categoryData.name` values roll up into COGS. It gates four `/period-summary` response fields — `cogs_excl_tax`, `cogs_incl_tax`, `purchase_event_count` and `completeness.pending_review_ids` — and until 2026-08-03 it appeared in **neither** contract document, so sales-processor has been consuming a figure whose scope it was never told about. Two properties make it worse than an undocumented default: **(a)** editing it changes what every *past* period reports, retroactively, with no migration and no version marker, so two runs of the same week either side of an edit disagree with no artifact explaining why; **(b)** a miscategorised transaction is invisible to every COGS figure while remaining visible in `tracked_bank_tx_ids`, so the data to detect the miss exists but nothing correlates it. Now documented in §2 of `docs/contracts/inventory-period-summary.md`; still unversioned. · _run `20260803`, card P6 `period-summary-contract-notice`_ · new · **destination: next milestone** (operator question §8 Q3 of the period-summary contract asks whether it should be versioned at all; the answer decides the shape) · lead: cheapest useful step is to echo the effective allowlist in the `/period-summary` response so a consumer can see the scope of the number it was handed, and log it at startup (already done) plus on change; a fuller fix records it per-period so historical restatements are detectable.

- **B-75 · Assumption A6 of the menu-cogs contract still publishes a false claim about where HQ rounds, and the audit's SUPERSEDED verdict hid it** — `docs/contracts/inventory-menu-cogs.md` A6 reads *"HQ rounds at the Go-decode boundary (not in SQL)"*, and the sibling's `:122` invariant note repeated the same framing (*"Phase 21 rounds at the SQL level, this endpoint rounds at the Go-decode level"*). **Both endpoints round in SQL.** `MenuCogsHandler`'s summary query wraps every money expression in `ROUND(…::numeric, 2)` and the per-unit in `ROUND(…::numeric, 4)`; `loadBreakdown` and the unallocated query do the same; `PeriodSummaryHandler` likewise. Go does no rounding on either path — it scans an already-rounded numeric into a `float64`. The 2026-08-03 audit marked A6 **SUPERSEDED** (its $0.10 tolerance is moot now that the invariant is broken outright) and so never checked the rounding-locus half, which is a separate and still-live false code claim. Low blast radius — nobody's decoder changes — but it is the kind of unexamined sentence that survives inside a row that *looks* audited. · _run `20260803`, card P6 `period-summary-contract-notice`, found by the G6 fix round; **out of scope under the card's scope freeze, which was F1–F10 only**_ · new · **destination: next milestone** — a two-line documentary fix, no code change, no KR reddened · lead: correct A6's parenthetical and the `:122` note to say both endpoints round server-side in SQL, and add a `§0` row for the rounding-locus claim so the coverage stays real. Bundle with **B-71**'s golden-JSON fixtures, which will pin the rounded values anyway.

- **B-76 · The full-suite Playwright gate runs against a database that carries state between runs — a red can be an artifact of a previous run, and so can a green** — run `20260803`'s final gate returned one failure, `tests/onboarding.spec.js:689 › Manager tab › sign-off form requires readiness rating (notes optional)`, a 30s timeout waiting for `[data-action="view-training"][data-template-id=…]`. **The title is on no armed-reds list**, so it was investigated rather than absorbed. Measured, in order: tonight's run touches **zero** onboarding files (`git diff --name-only 60b9edb HEAD` matches nothing under `onboarding`); re-run in isolation on the merged tree against the shared `hq_test_e2e` → **FAILED** (so not the B-45 load-sensitivity class, which passes in isolation); the same test on the **base commit `60b9edb`** against the shared DB → **FAILED** (so not introduced by this run); base commit with a **fresh** DB → **PASSED**; merged tree with a **fresh** DB → **PASSED**. 🛑 **The behaviour is symmetric across the base and merged trees — dirty fails on both, fresh passes on both** — which is what rules out the run as the cause and identifies accumulated state as the mechanism. `playwright.config.js:25` defaults every run to the single database `hq_test_e2e` and `TEST_DB_NAME` is opt-in, so consecutive suite runs share one accumulating dataset; the config's own comment already argues for separate databases *between the Go and Playwright suites* and stops one step short of arguing for a fresh one *per run*. **The half that should worry us is not the false red — it is the false green.** A suite whose fixtures accumulate can pass because a prior run left a row behind, and every full-suite figure in this milestone — including tonight's 778 — was taken under that condition. This is the same defect class as B-16 (a dropped test database reading as a passing suite) approached from the opposite side. · _run `20260803`, final gate, found by the orchestrator when a non-baseline title failed and the four-way base/merged × dirty/fresh matrix was run to attribute it_ · new · **destination: NEXT milestone (test isolation / gate integrity), and it belongs with B-50 and B-35 as one sitting** — all three are "the harness cannot tell whose state it is looking at" · lead: the cheap correct move is to make the e2e database **per-run by default** — derive `testDbName` from a run id or a timestamp rather than defaulting to a shared constant, and drop it on exit — which costs one `createdb` per suite and removes the whole class. If per-run creation proves too slow, the 80% is a mandatory truncate-and-reseed in global setup, but note that is strictly weaker: it fixes accumulation and not cross-run collision. **Do not "fix" this by adding a retry to the onboarding test** — that converts a demonstrated isolation defect into a hidden one.
  **✅ RESOLVED 2026-08-04 — run `overnight-20260804`, Wave 0, card A1 `e2e-gate-database-isolation`, branch `card/a1-e2e-gate-database-isolation` (base `c9e9e9e`).** The reset now lives in `playwright.config.js`'s `webServer.command`, as the first link of the shell chain that starts the Go server: `node scripts/reset-e2e-db.js && cd backend && …`. It therefore fires on **every** `npx playwright test` invocation and cannot be skipped by a CLI argument — including `night-crew.toml:34`'s `subset = 'npx playwright test "{tags}"'`, which is the leg B-76 says has never reset anything. **No `night-crew.toml` change was needed** (the PARK trigger did not fire): the existing `[e2e]` lines reset as they stand, and that file is byte-unchanged. 🛑 **Deliberately NOT a `globalSetup`, and the reason is measured, not assumed:** `node_modules/playwright/lib/runner/tasks.js:100-110` has `createGlobalSetupTasks` append `createPluginSetupTasks(config)` — where the `webServer` plugin starts the server — **before** `config.globalSetups`, so a globalSetup would DROP the database out from under a server that had already connected, migrated and seeded it. Top-level config code is no good either: the config is re-required in every worker process (verified — the worker load carries `TEST_WORKER_INDEX=0`) and also under `npx bddgen` and `--list`. **DROP+CREATE, not truncate** — the lead's "80%" was declined because a truncate list cannot see anything created outside the migration set, and `tests/db-isolation.spec.js` pins the stronger semantics by leaving exactly such an object behind. The **per-run-database** variant the lead preferred was also declined at landing. 🛑 **CORRECTED 2026-08-04, A1 fix round — the reason recorded here for declining it was FALSE, and it is struck rather than reworded.** This sentence originally read: *"the collision half is already carried by `TEST_DB_NAME` + `TEST_PORT` (every card is issued its own pair), and a per-run name would defeat `assertTestDatabaseName`'s point-blank name check for no gain this repo can currently observe."* **Both halves are wrong.** (1) Cards are **not** issued a `TEST_DB_NAME` — the isolation stanza in `launch-20260803.md:97-98` names `HQ_RLS_TEST_DB`, `TEST_PORT` and a scratchpad directory, and tonight's own `launch-20260804.md` carries **no isolation stanza at all**; this card's implementer ran `TEST_PORT=8201` against the bare default `hq_test_e2e`. So every leg shares one database and the reset this entry celebrates now **destroys** it for any concurrent leg — see **B-80**, which is the defect this false premise concealed. (2) A per-card name does **not** defeat the guard: `TEST_DB_NAME=hq_test_e2e_a1fix` matches `/^hq_test(?:_[a-z0-9]+)*$/` and was measured resetting cleanly through the gate path on 2026-08-04. The remedy therefore costs nothing the guard objects to. **A resolved entry justifying itself with a false premise is how the next reader gets misled**, which is why the strike is recorded here rather than only in B-80. The DROP+CREATE-over-truncate decision above is unaffected and stands. **The guard:** `assertTestDatabaseName` refuses any `TEST_DB_NAME` outside `/^hq_test(?:_[a-z0-9]+)*$/` and aborts the run naming both the database and the pattern — verified with `TEST_DB_NAME=hq` (standalone, exit 1) and `TEST_DB_NAME=hq_dev` (through the gate path: `Process from config.webServer was not able to start`, server never started, zero tests ran). B-16/B-35's lesson in the other direction. **Red-first, on the SUBSET path, run twice:** invocation 1 passed and left an `e2e_isolation_marker` table; invocation 2 **FAILED** — `Expected: "" Received: "e2e_isolation_marker"` — with `hq_test_e2e` still holding `sessions=890 ops=717 template_assignments=288 checklist_fields=268 checklist_templates=190 checklist_submissions=121` from run `20260803` and earlier, after two consecutive subset runs. Post-fix the same invocation passed three times consecutively and those tables are gone; what remains is migration-seeded only. `Taskfile.yml`'s two inline `psql` reset blocks were deleted as a second source of truth for a destructive operation (`test:ui`'s also defaulted `DB_PORT` to **5432**, which has no `yumyums` role, so it could only ever have failed). · **resolved 2026-08-04**

- **B-77 · `run-evidence check` cannot see a scaffolded repo's closeout or conflict log — so every hq slate night reads `no-run-evidence`, and the next `/nc-run` is told the night never happened** — measured at closeout of run `20260803`: with `reference/closeout-20260803.md` **and** `reference/conflicts-20260803.md` both written and committed under `.night-crew/knowledge/reference/`, `night-crew run-evidence check --repo . --run 20260803` returns **`no-run-evidence` ("nothing says this run ever executed")**. Cause, read at source in the night-crew clone (`internal/runevidence/runevidence.go`): line **173** resolves the committed scorecard correctly as `.night-crew/knowledge/scorecard/<runid>.jsonl`, but lines **177**, **218** and **221** hardcode `filepath.Join(root, "reference", …)` for the two artifacts a slate run actually produces — the **closeout record** and the **conflict log**. hq has **no root `reference/`** (the `launch-prompt` verb reports it "directory not present" and resolves to the scaffolded knowledge home), so both slate-layer sources are looked for in a directory that does not exist and the oracle correctly concludes it found nothing. 🛑 **This is the B-130 defect class exactly, one verb later.** `launch-prompt` was taught to search *both* homes precisely because a scaffolded target repo keeps its reference material under `.night-crew/knowledge/reference/`; `run-evidence` was not given the same treatment, and the two verbs now disagree about where this repo's artifacts live. **The consequence is a launch-time hazard, not a reporting nit:** `/nc-run` §1 checks run-evidence *before* cutting a branch, and on the next launch it will report that slate `20260803` never ran — while §2's guard, once this branch is merged at triage, reads clean and says "proceed." That is the exact combination the §1 check was added to prevent, and it would re-execute an archived slate. It also means **every hq slate run from `20260721` onward has been invisible to the oracle** for the same reason, which is consistent with the skill's own note that those runs read `ran-did-not-close` before the closeout record existed — the record now exists and is still unread. · _run `20260803`, closeout; found by the orchestrator running the verb to confirm its own closeout record was legible, and reading `runevidence.go` when it was not_ · new · **destination: the night-crew CLONE, next milestone — this is a tooling change, not an hq one** (same routing as **B-60**) · lead: give lines 177/218/221 the same two-home resolution `launch-prompt` already has — prefer `.night-crew/knowledge/reference/` when it exists, fall back to root `reference/` — and add a case to `runevidence_test.go` that writes the closeout into the **scaffolded** home, since the existing test at `:335` writes it to root `reference/` and therefore passes while the real repo fails. 🛑 **Until it lands, do not trust a `no-run-evidence` verdict in this repo** — verify against `.night-crew/runs/<date>-autonomous/HANDOFF.md` and the closeout record by hand before letting a launch proceed.

- **B-78 · `night-crew.toml`'s footprint comment carries a false "re-verified at landing" claim — `sync` selects six spec files, not the four it names** — measured at morning triage of run `20260803` by an adversarial subagent: `night-crew.toml:59-62` asserts *"As a Playwright path regex these four tokens select exactly workflows / persistence / sync / repro-cut-task .spec.js and nothing else — no other spec filename in tests/ contains any of them (re-verified at landing)."* At HEAD the token `sync` matches **six** specs: `states-sync-rxdb-conflict-notice`, `sync-rxdb-client`, `sync-rxdb-conflict-notice`, `sync-rxdb-conflict`, `sync-schema`, `sync`. The **direction is safe** — over-inclusion means more tests run, never fewer — so no gate has been weakened and no regression escaped through it. The defect is that a **false factual claim sits inside a gate-configuration file, carrying an explicit "re-verified" stamp that invites trust**, and CLAUDE.md names a narrow footprint entry as exactly how a regression escapes a green gate. A future editor reading the comment as true could "correct" the tag back toward the four files it names and land in under-inclusion, which is the unsafe direction. · _morning triage 2026-08-02, run `20260803`; found by the adversarial verifier measuring the claim rather than reading it_ · new · **destination: next milestone, and it rides any card that touches `night-crew.toml`** — a two-line documentary fix, no behaviour change · lead: correct the comment to state what the tokens actually select and drop the "re-verified at landing" stamp rather than re-stamping it; if the four-file bound is genuinely wanted, anchor the regex instead of renaming the tokens, and add the six-file list to the comment so the next reader can check it in one grep

- **B-79 · S1a's unknown-`scope.mode` refusal is named "must survive" in its own merge-intent and is pinned by no test — replacing it with `if (false)` leaves the suite fully green** — measured at morning triage of run `20260803` by mutation: `sync-rxdb/client.js:654-660` refuses an unrecognised `scope.mode`, and `merge-intents/merge-intent-s1a-sync-cutover-list-scope.md` lists that refusal as **must-survive item 1**. Neutering the guard leaves `tests/sync-rxdb-client.spec.js` at **55/55 passing**; no test anywhere passes a `mode` value other than `'list'`. **Severity is low and should not be overstated** — the fill branch's `checklistId`/`templateId` refusals still catch a malformed scope, so a neutered guard cannot widen into a pull-whole, which is the failure that would actually matter (and which `architecture/C-2` exists to prevent). What makes it worth filing is the *class*: a merge-intent's "what must survive" list is the artifact a 3am merger resolves conflicts against, so an entry on that list which no test defends is a claim the next merger will trust and the suite will not enforce. Seven of S1a's other guards were mutation-tested green-to-red at triage; this is the one that was not. · _morning triage 2026-08-02, run `20260803`; found by the adversarial verifier mutating each must-survive claim in turn_ · new · **destination: the successor cutover card** — it is already editing this file and this scope model · lead: one test, roughly three lines — assert `normalizeScope({mode:'nonsense', …})` throws, so the refusal named in the merge-intent is enforced by something other than the merge-intent; then consider whether every "what must survive" bullet ought to name the test that pins it, which would make the gap visible at write time instead of at the next triage

- **B-80 · Every Playwright invocation now DROPs the shared default `hq_test_e2e`, and cards are not issued a `TEST_DB_NAME` — so two concurrent legs no longer coexist, the later one destroys the earlier one mid-suite** — B-76's fix put `DROP DATABASE IF EXISTS <name>; CREATE DATABASE <name>;` (`scripts/reset-e2e-db.js:111-131`) at the head of `webServer.command` (`playwright.config.js:109`), where it fires on **every** `npx playwright test` invocation, full or subset. `TEST_DB_NAME` defaults to the single shared `hq_test_e2e` (`scripts/reset-e2e-db.js:77`). The run's per-card isolation issues a unique `TEST_PORT` and a unique `HQ_RLS_TEST_DB` but **not** a unique `TEST_DB_NAME` (`launch-20260803.md:97-98` names `HQ_RLS_TEST_DB`, `TEST_PORT` and a scratchpad directory and nothing else; 🛑 **and tonight's own `launch-20260804.md` carries no per-card isolation stanza at all** — grep it for `unique`, `TEST_PORT`, `TEST_DB_NAME`, `HQ_RLS_TEST_DB` and every one is absent, as it is in `slate-20260804.md`. The `slate-20260803.md:444` stanza that did exist says "a unique database prefix", which reads as covering this and demonstrably did not: card A1's own implementer ran `TEST_PORT=8201` against the bare `hq_test_e2e`). **Two legs differing only in `TEST_PORT` therefore share one database, and the reset makes that fatal rather than merely dirty** — before B-76 they merely polluted each other; now the second leg's first link drops the first leg's database out from under a server mid-suite. G6 demonstrated it by execution: an in-flight `onboarding` subset on `:8211` collapsed to **3 failed / 27 did not run / 25 passed / exit 1** when a second leg's first link ran. 🛑 **B-76's resolution note in this same file justified declining the per-run-database alternative on the grounds that "the collision half is already carried by `TEST_DB_NAME` + `TEST_PORT` (every card is issued its own pair)" — that premise is false and has been struck in place in the B-76 entry as part of this filing** (see the CORRECTED 2026-08-04 sentence there). A resolved entry justifying itself with a false premise is how the next reader gets misled. · _G6 adversarial review of card A1 `e2e-gate-database-isolation`, run `overnight-20260804`, found by execution when a second leg was started alongside an in-flight one; the launch-prompt half re-measured in the A1 fix round_ · new · **destination: the LAUNCH-PROMPT isolation stanza, and it is a same-milestone fix — the hazard is live tonight, not next milestone** · lead: issue a unique `TEST_DB_NAME` per leg in the launch-prompt isolation stanza alongside `TEST_PORT` and `HQ_RLS_TEST_DB` — the guard already permits `hq_test_e2e_<card>` (`/^hq_test(?:_[a-z0-9]+)*$/`; `TEST_DB_NAME=hq_test_e2e_a1fix` was measured resetting cleanly through the gate path on 2026-08-04), so this costs one line in the prompt and no code change. Restoring the stanza to `launch-*.md` at all is the prerequisite — a rule that is only in last night's prompt is not a rule. Consider also making the default itself unshareable rather than relying on every prompt to remember.

- **B-81 · The reset banner went to stdout, which Playwright's `webServer` discards by default — so a gate log carried zero evidence the reset had run** — `scripts/reset-e2e-db.js`'s CLI branch announced its reset with `console.log`. It runs as the first link of `playwright.config.js`'s `webServer.command`, and the webServer plugin pipes the child's stdout **only** when `webServer.stdout === 'pipe'` (`node_modules/playwright/lib/plugins/webServerPlugin.js:126`), an option documented as defaulting to `'ignore'` (`node_modules/playwright/types/test.d.ts:10285-10289`). stderr **is** piped by default (same file, `:10281-10283`) — which is exactly why the Go server's slog lines appear in every gate log and the banner did not. On a successful DROP psql emits no NOTICE either, so **a gate log contained no evidence whatsoever that the database had been reset** — the precise property the line's own comment claims to deliver: *"a silent reset is indistinguishable from no reset at all, which is B-76's mechanism."* B-76's fix was real but unobservable from the artifact triage actually reads. · _G6 adversarial review of card A1, run `overnight-20260804`; confirmed by execution across five runs — the banner never appeared while `[WebServer]` lines did_ · **✅ RESOLVED 2026-08-04 — A1 fix round, commit `7addc92`, branch `card/a1-e2e-gate-database-isolation`.** One line: `console.log` → `console.error`. Chosen over adding `stdout: 'pipe'` to the `webServer` block for blast radius — the config switch would also un-silence the Go server's entire stdout stream on every leg in the repo, to fix one line that already lives in the file that owns the banner. Banner content is unchanged (it names the database and the host:port; that content is the evidence). **Verified by execution, not by inspection**, with an A/B control inside a *single* invocation — a temporary `console.log` control line carried alongside the `console.error` banner: `TEST_PORT=8221 TEST_DB_NAME=hq_test_e2e_a1fix npx playwright test "db-isolation" --retries=0` emitted `[WebServer] ── reset hq_test_e2e_a1fix on localhost:5433 ──` as **line 1** of Playwright's output while `grep -c CONTROL-STDOUT` over the same log returned **0**. Same process, same run: stderr reaches the log, stdout does not. Control line reverted; only the `console.error` change ships. Suite green (`1 passed`); G4 `node build-sw.js` twice → 31 precached both times, tree clean. · **resolved 2026-08-04**

- **B-82 · `tests/db-isolation.spec.js` never runs on a seam-confined subset leg — the test that guards the reset is blind to the reset's removal on exactly the cards most likely to remove it** — the subset positional regex is built by unioning the `[e2e.seams]` tags of the seams a card touched (`night-crew.toml:36-60`, expanded into `subset = 'npx playwright test "{tags}"'` at `:34`). The full tag vocabulary is `inventory`, `recipes`, `workflows`, `persistence`, `sync`, `repro-cut-task`, `onboarding`, `users`, `purchasing` (🛑 the G6 report listed only the first six; the other three exist — the conclusion is unchanged because **none of the nine matches `db-isolation`** as a path regex). So on any confined card the reset still fires structurally — it is in `webServer.command`, which no CLI argument can skip — but **the test that would catch its removal does not run**. A future card that "tidies" the reset out of `webServer.command` while touching only `backend/internal/inventory` would go green. Note the escape is narrow by luck, not design: `scripts/reset-e2e-db.js` and `playwright.config.js` are unmapped, so a card editing *them* de-confines to the full suite. The exposure is a card that removes the reset **as collateral** while confined elsewhere. **Secondary vacuous-pass path:** `--project=bdd` alone (`task bdd`) never runs the spec, so it leaves no marker behind — and the following run then passes trivially, since the assertion is "no marker present" and the marker's absence is indistinguishable from a successful reset. The test is cross-process by construction and a skipped invocation silently re-arms it. · _G6 adversarial review of card A1, run `overnight-20260804`; tag vocabulary re-measured against `night-crew.toml` in the A1 fix round_ · new · **destination: next milestone, with B-09/B-86 — all three are "a green gate that did not run the thing it claims to grade"** · lead: the cheap move is a `db-isolation` seam entry mapping the harness files to their own tag, plus adding the tag to every seam's list so the isolation check rides every confined leg — it is one 300ms test. The vacuous-pass half wants the spec to assert it actually wrote its marker, or to run in both projects.

- **B-83 · The reset guard is name-only — it checks the database name and never the host, port or credentials, so it would happily DROP a correctly-named database on a production server** — `assertTestDatabaseName` (`scripts/reset-e2e-db.js:93-104`) tests `TEST_DB_NAME` against `/^hq_test(?:_[a-z0-9]+)*$/` and nothing else. `resolveE2eDb` (`:71-91`) reads `DB_HOST`, `DB_PORT`, `DB_USER` and `DB_PASS` straight from the environment with no assertion of any kind. Therefore `DB_HOST=<prod-host> TEST_DB_NAME=hq_test node scripts/reset-e2e-db.js` drops `hq_test` **on a remote server** without complaint — the guard sees a well-formed test name and waves it through. This is **within the card's stated scope** as written ("refuse any database whose *name* is not a test database") and the card delivered that faithfully; the filing is that the scope is narrower than the property the surrounding prose implies — B-16/B-35's lesson is "the harness cannot eat the wrong database", and a name check alone does not establish that. Severity is low today (no prod Postgres is reachable from this box under these coordinates) and would rise the moment one is. · _G6 adversarial review of card A1, run `overnight-20260804`_ · new · **destination: next milestone, with B-16/B-35 — same sitting** · lead: extend the guard to refuse a non-loopback `DB_HOST` unless an explicit opt-in env var is set, and to name the host in the throw alongside the database. Keep it a throw, not a warning. 🛑 Do not widen `TEST_DB_NAME_PATTERN` while doing it — B-80's remedy needs `hq_test_e2e_<card>`, which the pattern already permits.

- **B-84 · With `NIGHTCREW_ENV_URL` set the reset does not run and the test that would say so `test.skip()`s — the one shape in which B-76's fix is inert AND reports nothing** — `playwright.config.js:65` makes `webServer` `undefined` when `NIGHTCREW_ENV_URL` is set, so `webServer.command` — and with it the entire reset — never executes. `tests/db-isolation.spec.js:48-51` then `test.skip()`s rather than failing. **Both halves are correct behaviour**, and this entry is not a request to change them: in that mode a provisioned ephemeral stack owns its own database, the localhost coordinates name a *different* database, and asserting against it would grade the wrong one — a worse outcome than not grading it. It is also demonstrably **not** the current gate path (`night-crew.toml`'s `[e2e]` legs run `npx playwright test` directly and A1's evidence was taken through `webServer`). What is worth filing is the shape: this is the single configuration in which the card's deliverable is **fully inert and silent** — no reset, no banner (B-81's fix included, since the banner is printed by the code that does not run), and no test noticing. If the run mode ever flips to provisioned envs, B-76 quietly stops holding and nothing in the suite says so. · _G6 adversarial review of card A1, run `overnight-20260804`_ · new · **destination: next milestone, and it rides whatever card first makes `NIGHTCREW_ENV_URL` the real gate path — it is not actionable before then** · lead: do not convert the skip into a failure (that grades the wrong database). Instead make the *ephemeral env* responsible for its own freshness and have the spec assert **which** database it graded, so a skip in a mode where the stack is supposed to be fresh is visible in the log rather than silent. A skipped isolation check should read as "unproven", not as "passed".

- **B-85 · `task bdd` points at `DB_PORT: '5432'`, which black-holes SYN on this box — pre-existing, but B-76's reset changed the failure's SHAPE from a fast connect error to a 60s stall** — `Taskfile.yml:82-86` sets `DB_HOST: localhost` / `DB_PORT: '5432'` for the `bdd` task. Host `:5432` is bound by `infra-postgres-1` (slack-trading), which has no `yumyums` role — every other site in the repo uses **5433** (`yumyums-dev-pg`), including `test:go` at `:92` and `test` at `:108`. **Broken before this card and broken after it** — the card neither caused nor fixed it, and no gate leg runs `task bdd`. The reason to file it is that the *symptom* moved: previously it failed when the Go server tried to connect; now `node scripts/reset-e2e-db.js` is the first link of `webServer.command`, so the failure lands on the psql reset instead and presents as a **hang until `webServer.timeout: 60000` expires** (`playwright.config.js:116`) rather than as a named connection error. Worth knowing before someone spends the 60s stall looking for it in the wrong place. Note this is the same 5432/5433 inconsistency the card *did* remove from `test:ui`'s deleted inline psql block — `bdd` is the copy that survived. · _G6 adversarial review of card A1, run `overnight-20260804`_ · new · **destination: next milestone; trivial, and it rides any card touching `Taskfile.yml`** · lead: change `DB_PORT: '5432'` to `'5433'`, or better, delete the `env:` override so `bdd` inherits the same default every other task uses — a per-task port override is what let this drift in the first place. While there, confirm no other task carries a 5432.

- **B-86 · `verify-test-harness.sh`'s A2 floor is stale at 20 while the repo resolves 27 — a silent drop of up to seven spec files still passes the check built to catch exactly that** — `scripts/verify-test-harness.sh:78` sets `MIN_SPEC_FILES="${H1_MIN_SPEC_FILES:-20}"`, and its own comment at `:74-75` says the floor is *"20 (19 static under ./tests + 1 generated under .features-gen/). It ratchets UP as spec files are added; it must never ratchet down silently."* Measured at HEAD: `ls tests/*.spec.js` returns **26**, plus the one generated BDD spec — **27 resolved**. Nobody ratcheted the floor as the seven specs were added, so A2 passes with as few as 20 files reaching the runner. **That gap IS the B-09 failure the check exists to catch**: a suite that silently resolves fewer spec files than the repo contains reports success for tests it never ran. The check is not broken — it is doing exactly what it says with a number that stopped being true, which is the more dangerous state because it reports PASS. 🛑 Note the check is also **self-invalidating on the wrong side**: because the floor is a `-ge` comparison, adding spec files never makes it fail, so nothing will ever force the ratchet. It has to be maintained deliberately or replaced. · _G6 adversarial review of card A1, run `overnight-20260804`; spec count re-measured at HEAD in the A1 fix round_ · new · **destination: next milestone, with B-09 and B-82 — one sitting on "a green gate that did not run what it claims to grade"** · lead: raise the floor to 27 now, then make it stop being a hand-maintained constant — derive the expected count from `ls tests/*.spec.js | wc -l` plus the generated spec and assert **equality**, so both directions are graded and the ratchet cannot be forgotten. `H1_MIN_SPEC_FILES` stays as the go-red override.

- **B-87 · A Playwright CLI path filter matches the ABSOLUTE path, so a worktree directory name containing a seam token silently turns a confined subset into the FULL suite — the gate over-runs and its "which tests ran" claim is wrong** — `night-crew.toml:34`'s `subset = 'npx playwright test "{tags}"'` passes the `[e2e.seams]` tags as bare positional args. Those become unanchored regexes tested against the **absolute** file path: `node_modules/playwright/lib/util.js:183` `forceRegExp` → `new RegExp(pattern, "gi")` (no anchors, matches anywhere in the string); `util.js:128` `createFileMatcher` → `re.test(filePath)`; `runner/projectUtils.js:147-161` `collectFilesForProject` collects from the **resolved absolute** `testDir`, and `runner/loadUtils.js:63-71` feeds those absolute paths to the filter. So a **directory component** of the checkout path can satisfy a token meant to name a spec file. Measured from `hq-worktrees/a2-workflows-autosavefield-phantom` with the `workflows.html` seam subset `["workflows","persistence","sync","repro-cut-task"]`: `--list --project=chromium workflows` selects **786 tests / 26 files — the entire chromium suite** — because the directory component `a2-workflows-…` matches the `workflows` regex on *every* file in the tree; the same invocation from `/home/jcole/projects/hq` selects **86 / 1**. 🛑 **The consequence first proposed for this finding was WRONG, and is recorded as wrong so it does not become folklore.** The candidate filed on `card/a2-workflows-autosavefield-phantom` claimed that a worktree named for a *different* app — say `b-inventory-…` — "would make `[e2e] subset` select that app's specs **INSTEAD of** the seam's and report a green subset that never ran the seam it was confined to." **That does not follow, and G6 refuted it at source.** CLI path filters are **OR'd**, not intersected: `createFileMatcherFromArguments` (`util.js:124-127`) folds **all** positional args into **one** matcher whose closure (`:141-160`) returns `true` on the first regex that hits; `runner/tasks.js:243` pushes that single matcher onto `config.loadFileFilters`, so `loadUtils.js:68`'s `loadFileFilters.every(…)` iterates a **one-element** array. A directory-component match can therefore only ever produce a **superset** — it can never exclude a spec that would otherwise match. A worktree named `b-inventory-…` running tokens `workflows persistence sync` selects exactly the intended files, because `inventory` is not one of the tokens. **The under-coverage failure mode is unreachable by this mechanism.** Confirmed empirically three ways: `--list --project=chromium workflows inventory` from `/home/jcole/projects/hq` → **238** = 86 + 152 (`inventory` alone = 152), which is the OR; and from the `a2-workflows-…` worktree, `persistence` **alone** → **32 / 1 file**, i.e. a token the directory name does *not* contain is not widened at all, while `workflows` alone → 786 / 26 = the same count as passing no filter whatsoever. **The real consequence:** a worktree directory name containing a filter token silently turns a confined subset into the **full suite**. The gate **over-runs** — its runtime, its isolation assumptions, and its "which tests ran" claim are all not what they say. A card budgeted for a 3-minute confined subset silently spends 21 minutes; a leg that believed it was touching only the `workflows` seam's fixtures touches every seam's; and the merge-intent line "gate ran the confined subset" is false while the gate is green. This is a sibling of **B-76** in kind — *the harness cannot tell you what it actually ran* — but it is **not** a silent-skip risk, and the confined-gate-misses-a-regression direction is unreachable this way. · _run `overnight-20260804`, card A2 `workflows-autosavefield-phantom`, found by the implementer when the confined gate returned 787 tests instead of a subset; the proposed consequence refuted by G6 at source and re-measured by execution in the A2 fix round_ · new · **destination: next milestone, with B-82 and B-86 — all three are "the confined gate does not run the set it claims to"**, though note this one errs toward more coverage, not less · lead: **anchor the `[e2e.seams]` tokens to `tests/…`** (e.g. `tests/workflows`) so they cannot match a directory component — the tokens are already file-name fragments, so the anchor costs nothing and is a `night-crew.toml`-only change. Second, **have the gate print the selected file list** (`--list` is cheap, or parse the reporter's `Running N tests in M files` header) and record it in the merge-intent, so "which tests ran" stops being an assumption. 🛑 Do not "fix" this by renaming worktrees — the naming convention is deliberate and the next token collision would be a different word.

- **B-88 · Rule 2's guard is bypassable and already bypassed — `workflows.html` reads RxDB through `window.HQSync.db`, which the three-symbol assertion does not watch, on an eagerly-mounted page-load path that runs offline** — `tests/sync-rxdb-client.spec.js:1468-1470` is three `expect(src).not.toContain(…)` assertions over the **source text** of `workflows.html`, naming exactly `HQSync.createDatabase`, `HQSync.startReplication` and `HQSync.client`. It is cited — in `designs/offline-ownership.md` before the A4 fix round, and in the C1/C2 card charters — as the enforcement of the rule *"nothing may read from RxDB on a code path that can execute offline."* Measured: `workflows.html:3588-3592` is `function defaultStore(){ const db = window.HQSync && window.HQSync.db; if (db && db.conflict_records) return createRxdbConflictStore(db.conflict_records); return createMemoryConflictStore([]); }`, mounted **eagerly at page load** (`workflows.html:3639`, `window.HQConflictNotice.instance = window.HQConflictNotice.mount()`), on a path that runs offline. **`window.HQSync.db` is a fourth route the guard does not name**, so the assertion is green today and would stay green. 🛑 **The rule is therefore gated by the database not existing, not by the test.** `defaultStore()` falls through to the empty in-memory store only because nothing calls `createHQSyncDatabase`, which is what leaves `window.HQSync.db` undefined — `sync-rxdb/bootstrap.js:82-83` exposes it as a deferred handle and no production page calls it. **Harmless today, and only today: the first card that creates a database — `sync-hard-cutover` is the obvious one — breaks the rule with no diff to anything the guard watches, and no gate reds.** The failure class is the one this repo keeps arming against and B-70/B-79 are siblings of: **a green assertion that does not cover the thing it is cited as covering.** · _G6 adversarial review of card A4 `offline-ownership-design-note`, run `overnight-20260804`; the read path re-verified at source in the A4 fix round. The design note's enforcement claim is struck in place and corrected there; the code-side gap is this entry._ · promoted → skeleton-offline-ownership-honesty (roadmap round 2026-08-05) · **destination: next milestone, with B-79 — both are "a named invariant is pinned by nothing that would notice its removal"** · lead: extend the assertion to `HQSync.db` as the cheap patch. **Better: stop asserting on source text at all.** A `not.toContain` over a string cannot enumerate the routes into an object, and the next route added will be missed the same way. Assert on the **object** instead — load the page and evaluate that `window.HQSync.db` is `undefined` (and that no RxDB collection is reachable) at the end of load — which grades the state the rule is actually about rather than the spelling of four identifiers. · 🟢 **CLOSED** — run `20260808-2`, card **C1 `skeleton-offline-ownership-honesty`**, branch `card/c1-skeleton-offline-ownership-honesty`, commit `69f6543`. The three `expect(src).not.toContain(...)` source-text assertions in `tests/sync-rxdb-client.spec.js` (formerly lines 1497-1499) are deleted; replaced with a browser test (`'nothing reads from RxDB on a code path that can execute offline — window.HQSync.db is undefined at end of load (B-88)'`) that logs in, loads `/workflows.html` for real, and asserts `window.HQSync.db === undefined` plus (defense in depth) that `indexedDB.databases()` contains no RxDB/Dexie-named database — exactly the lead's prescription. This is also the flag-off contract `skeleton-one-row-end-to-end` (C2) must keep green: with the sync flag off, page load must leave `window.HQSync.db` undefined. RF: Probe A showed the shipped guard's three assertions stay green while a hypothetical fourth (`not.toContain('HQSync.db')`) reds on the CURRENT tree (`workflows.html` already contains the literal text) — the shipped guard's blind spot, mechanically. Probe B showed the new object-level test is a real gate: red (exit 1) when `HQSync.db` is temporarily set in `sync-rxdb/bootstrap.js` (simulating an unguarded future card), green (exit 0) on the unmodified tree, before and after. Logs: `.night-crew/runs/2026-08-08-2-autonomous/c1-gates/{rf-probe-a,rf-newtest-baseline-green,rf-newtest-injected-red,rf-newtest-postrevert-green}.log`.

- **B-89 · `cachedGrantSlugs()` returns `[]` unconditionally on every real client — `index.html` writes `hq_apps` as an identity envelope `{uid, apps}` and `bootstrap.js` still `Array.isArray`-gates on the bare array that shape replaced** — 🛑 **a live code defect, found while re-verifying class row 5 for the A4 design note; out of that card's docs-only contract and NOT fixed there.** `index.html:232-235`'s `writeCachedApps` does `localStorage.setItem(APPS_CACHE_KEY, JSON.stringify({uid:deviceId, apps:apps}))`, and `readCachedApps` (`:221-231`) **deliberately discards** a bare array at `:228` (`if(!env||Array.isArray(env)||env.uid!==deviceId||…)return null`) as pre-card legacy whose owner is unknown — fail closed, obligation 7(a). That is correct and intended. But `sync-rxdb/bootstrap.js:62-71` was never moved to the envelope: it does `const apps = JSON.parse(raw); return Array.isArray(apps) ? apps.map((a) => a && a.slug).filter(Boolean) : [];` — **the envelope is an object, not an array, so the ternary takes the `[]` branch every time.** The cached grant list that decides **what to replicate** (obligation 4 / decision 56, the narrow claim + umbrella expansion at `expandGrantSlugs`) is therefore **always empty on a real client**, and there is no error, no warning and no visible symptom — the `catch` at `:68-70` is not even reached. 🛑 **No test catches it because the test plants the shape the app no longer writes:** `tests/sync-rxdb-client.spec.js:1385` does `localStorage.setItem('hq_apps', JSON.stringify([{slug:'inventory'},{slug:'operations'}]))` — a **bare array** — in an `addInitScript`, so the expansion assertions downstream pass on a fixture `index.html` would itself discard. **Sibling in kind to B-65**: a test that is green on a shape the application never produces, guarding a path that is dead in production. Latent in consequence *today* only because replication is never started; it arms with the same deploy that arms §4's reconciliation gap. · _run `overnight-20260804`, card A4 `offline-ownership-design-note`, found by the fix-round implementer while opening the row-5 citations; both shapes read at source_ · promoted → shipped-bug-sweep (roadmap round 2026-08-05) · **destination: next milestone, with B-65 — one sitting on "the test plants a shape production does not write"** · lead: two changes in one set. (1) Make `cachedGrantSlugs()` read the envelope — parse, reject a bare array the same way `readCachedApps` does, and take `.apps`; it should also verify `uid` against the identity token rather than trusting whatever is in `localStorage`, which is the whole point of the envelope. (2) **Fix the fixture in the same commit** — `tests/sync-rxdb-client.spec.js:1385` must plant `{uid:…, apps:[…]}`, and a red-first run should show the current code failing on it. Consider making `index.html` export the read/write pair so there is exactly one parser for this key instead of two that drifted. · 🟢 **CLOSED** — run `20260807`, card **A2 `shipped-bug-sweep`**, branch `card/a2-shipped-bug-sweep`, commit `08dad2d`. `cachedGrantSlugs()` now reads the `{uid, apps}` envelope, rejecting a missing envelope, a bare array, or a non-array `apps`, and verifies `uid` against the identity token (`hq-identity` cache / `/__hq_identity` — origin-scoped CacheStorage, reachable from `workflows.html` too, not launcher-only), mirroring `index.html:234-236`'s `readCachedApps()` exactly, including the uid-mismatch behavior (silent empty, not a thrown error — a decision `index.html` already shipped, not a new one; not a PARK). Since verifying the identity token requires an async CacheStorage read, `cachedGrantSlugs()` is now `async` and its call site `await`s it (top-level await — safe, `bootstrap.js` loads as `type="module"`). Fixture fixed in the same commit: `tests/sync-rxdb-client.spec.js`'s `window.HQSync is constructed…` test now plants the real envelope + matching identity-cache token instead of the bare array, freezing the post-login redirect so `index.html`'s own `establishIdentity()` can't clobber the seeded state. RF: red pre-fix (`state.surfaces === []`, not the expected 4-item array) → green post-fix; full spec file 55/55 passed. Logs: `.night-crew/runs/2026-08-07-autonomous/a2-logs/b89-{red,green,full-spec-green}.log`.

- **B-90 · `build-sw.js`'s own safety-argument comment says the precache is "still 29 files" when it is 31 — a reader checking the number against reality gets a false negative on the argument** — `build-sw.js:457` reads *"the precache is still 29 files"*, inside the `:455-458` comment block explaining why deleting the `api-cache` `cacheKeyWillBeUsed` hook is invisible to every gate (*"build-sw.js still exits 0, the precache is still 29 files, the SW still installs. Only [B1-XT-01] and [B1-XT-02] fail."*). Measured: `node build-sw.js` prints `SW built: 31 files precached`, and `CLAUDE.md` states the count is 31. The count moved when `vendor/rxdb.bundle.js`, the five `sync-rxdb/*.js` modules and `sync-schema/collections.js` were added to `globPatterns`; the comment did not. 🛑 **The damage is specific to where the number sits.** This is not a stale number in prose — it is a **load-bearing premise in the repo's own argument about what a gate would and would not notice.** A reader auditing that argument runs the build, sees 31 against a claimed 29, and has to decide whether the comment is stale or the invariant is broken; the honest reading of a mismatch is "something moved and nobody looked," which is exactly the alarm B-37's reachability guard exists to raise. A false alarm in the one comment that teaches people to trust that number is worse than no comment. **Same comment block as B-59**, which flags a different defect in the same four lines (it names two tests where only one actually fails) — the block should be fixed once, for both. · _G6 adversarial review of card A4 `offline-ownership-design-note`, run `overnight-20260804`; count re-measured by running `node build-sw.js` twice in the A4 fix round_ · new · **destination: next milestone, with B-59 — same comment block, fix in one sitting** · lead: the number should not be hand-maintained in a comment at all — that is how it went stale. Have `build-sw.js` interpolate the real count into whatever it emits, or drop the count from the sentence and let the argument stand on *"the precache count does not move"* (which is the claim that matters and is already graded by the reachability guard + the 31 check in `CLAUDE.md`). Fix B-59's test attribution in the same edit.

- **B-91 · Three sites in the tree still say `hq_offline_v1` holds ONE store — it has held two since the database went to version 2, and the citations they carry have all moved** — `designs/offline-save-honesty.md:13-14` states *"`submitOp` (`sync.js:695`) has no offline queue — IndexedDB `hq_offline_v1` holds one store, `submitQueue` (`sync.js:51`), and `drainQueue` (`sync.js:560`) drains only that"*; the **same file** repeats the singular a third time at `:142` (*"`submitQueue` in IndexedDB (`sync.js:51`) + `drainQueue`"*, in the today-vs-after-cutover table); and `workflows.html:384-386` repeats *"the IndexedDB queue that today holds submissions only (`sync.js:51`)"*. Measured: `sync.js:48` opens the database at **version 2** and `onupgradeneeded` creates **two** object stores — `submitQueue` at `sync.js:51-53` and `syncMeta` at `sync.js:54-56` (the Lamport clock's persisted state, `LamportClock` `sync.js:108-139`). The function citations have also drifted: `submitOp` is now `sync.js:781-797` and `drainQueue` is now `sync.js:632`. **The mechanism each site describes is unchanged and still correct** — an unsent field answer genuinely has no store — so this is not a wrong conclusion; it is a wrong inventory underneath a right conclusion. 🛑 **This is precisely the collapse card A4's non-negotiable #2 exists to prevent, already in the tree before A4 was written.** `submitQueue` and `syncMeta` have **different fates**: retire the op-log and `syncMeta` becomes dead weight while `submitQueue` survives untouched, so a sentence reading "`hq_offline_v1` holds one store" conceals a whole class from anyone who opens these files looking for where data went. `designs/offline-ownership.md` §1b names all three sites and publishes the correct split; these three sites still contradict it. · _run `overnight-20260804`, card A4 `offline-ownership-design-note`; two sites named in the deliverable at authoring time, the third (`:142`) found by G6 and the line numbers re-verified in the A4 fix round_ · new · **destination: next milestone — a small, contained docs+comment correction; can ride any card already opening `sync.js` or `workflows.html`** · lead: correct all three sites in one change set (they will drift apart again if done piecemeal) — name both stores, update `submitOp` → `sync.js:781-797` and `drainQueue` → `sync.js:632`, and cite `designs/offline-ownership.md` §1b rather than restating the split a fourth time. 🛑 Note `workflows.html:384-386` is a **code comment**, so this entry cannot be closed by a docs-only card.

- **B-92 · The in-worktree `npx playwright test` path never generates `version.json`, so that stack serves a static tree missing a file `sw.js` precaches and `index.html` now displays** — 🛑 **headline narrowed 2026-08-04 (A6 fix round, G6 finding 2).** It previously read *"The Playwright gate path never generates `version.json`"*, which is broader than the body below supports and broader than the code: `night-crew.toml:12-16` declares an **ephemeral** env built from `docker-compose.nc.yml` → `backend/Dockerfile:58-64`, which **does** generate the file, and in that mode `playwright.config.js:65` makes `webServer` `undefined` entirely so none of the machinery below is even reached. The gap was only ever the **in-worktree `npx playwright test`** path. A headline is what a future reader triages from, so it names the path that actually had the gap. — `version.json` is git-ignored on purpose (`.gitignore:13`): `build-sw.js`'s `writeVersionJson()` (`build-sw.js:291-297`) writes it locally from `package.json` `"version"`, and `backend/Dockerfile:57-64` regenerates it into the image from the authoritative `version.go` `Frontend` constant, because `sw.js` precaches it and a 404 aborts the whole service-worker install. `tests/sw-manifest.spec.js:15` already encodes the exception (`GENERATED_BUT_SHIPPED = ['version.json']`). **The gap is the test stack.** `task test` generates the file via its `sw` dep (`Taskfile.yml:39`), but `night-crew.toml:33-34` runs `npx playwright test` **directly** for both `suite` and `subset`, and `playwright.config.js`'s `webServer.command` serves `STATIC_DIR=../` — the bare worktree. In a worktree where `node build-sw.js` has never run, `GET /version.json` **404s**, and it was measured doing exactly that on this card: the first green run of `tests/version-badge.spec.js` had 4/5 passing with the unmocked test reporting `Expected: "v1.4.0" Received: "v—"`. 🛑 **Not a production defect** — the Dockerfile block covers prod, and the app's response is honest (`data-state="unknown"`, no API fallback). The defect is that a *missing build artifact* and a *broken version line* are indistinguishable at the gate, on the one leg where nothing regenerates the artifact. Sibling of **B-76** in kind: the night-crew gate path skips something `task test` does, and the omission presents as a test result rather than as a setup error. **Mitigated but not fixed in-card:** `tests/version-badge.spec.js`'s unmocked test now asserts `GET /version.json` is 200 first, with a failure message naming `node build-sw.js`, so the artifact gap fails loud instead of reading as a code defect. · _run `overnight-20260804`, card A6 `app-version-badge`; found by execution when the unmocked test rendered `v—` in a fresh worktree_ · new · **destination: next milestone — gate hygiene, one line in the `webServer.command` chain** · lead: generate `version.json` in `playwright.config.js`'s `webServer.command` alongside `node scripts/reset-e2e-db.js`, so it fires on the subset path too and cannot be skipped by a CLI argument — the same reasoning that put the DB reset there. 🛑 **Do NOT call `node build-sw.js` for this**: it reads git HEAD and rewrites `sw.js`, so a gate leg would dirty the tree mid-run and race the B-37 committed-artifact invariant. Write only the one small file, from the same source `writeVersionJson()` uses. Note also that the two generators disagree on source — `build-sw.js` reads `package.json` while `backend/Dockerfile:61` reads `version.go` `Frontend` — which is harmless only while the standing three-way parity holds, and would surface as dev and prod showing different numbers the moment it does not.
  **✅ RESOLVED 2026-08-04 — run `overnight-20260804`, card A6 `app-version-badge` fix round, commit `e215ef3`, branch `card/a6-app-version-badge` (base `0dcd8b4`).** Fixed **in-card rather than filed**, because A6 introduced it: a gate that hands every future card leg a red unrelated to its own change partially undoes what A1 landed the same night. The generator is now the second link of `playwright.config.js`'s `webServer.command` — `node scripts/reset-e2e-db.js && node scripts/write-version-json.js && cd backend && …` — for exactly B-76's reason: that command runs once, in the parent, strictly before any test and before the server exists, and **no CLI argument can skip it**, so it fires on `night-crew.toml:34`'s `subset` leg too. 🛑 **NOT `node build-sw.js`**, per this entry's own lead: `writeVersionJson()` was **extracted** out of `build-sw.js` into `scripts/write-version-json.js` (module + CLI, the same shape as `scripts/reset-e2e-db.js`, paths resolved against the repo root rather than `cwd`), so the E2E stack gets the file without the build script's git-HEAD read and `sw.js` rewrite. **Extraction was chosen over inlining a `node -e` in the config**: a second definition of a shipping artifact's payload is precisely the drift this entry already warns about between `build-sw.js` and `backend/Dockerfile`. The **source disagreement is NOT closed** — the new script still reads `package.json`, the Dockerfile still reads `version.go`; that half stands as written above and remains next-milestone work. **The reset stays the first link and still fires**, verified in the same invocations. **Red-first, by execution, on a fresh `git archive` of the branch with no `version.json` and no prior build** (the same experiment G6 used to prove the defect): pre-fix → `1 failed, 4 passed` with `Error: GET /version.json returned 404`; post-fix, same experiment → **`5 passed (14.5s)`**, with `── reset hq_test_e2e_a6fix on localhost:5433 ──` as output line 1 and `── wrote version.json frontend=1.4.0 ──` as line 3, and `version.json` present in the archive afterwards containing `{"frontend":"1.4.0"}`. `tests/db-isolation.spec.js` (A1's own spec, the shared-harness check) `1 passed` with both banners. `node build-sw.js` twice → 31 precached both times, `sw.js` byte-unchanged. Three-way parity untouched at **1.4.0**. The banner is `console.error`, not `console.log`, for B-81's reason — `webServer.stdout` defaults to `'ignore'`, so a `console.log` generator would leave a gate log with no evidence it ran. · **resolved 2026-08-04**

- **B-93 · The orchestrator's own final-gate capture was piped through `tail`, destroying the "exactly one summary block" check** — run `overnight-20260804`'s final G2 (Playwright) was invoked as `npx playwright test --retries=0 2>&1 | tail -30`, so the retained log holds only the last 30 lines. HQ's verification ladder specifies **"exactly one summary block; two blocks under one header = an invalidated run"** — a check that requires the *whole* log and cannot be performed on a tail. The gate was judged sound on indirect evidence instead (test indices consecutive through 792, `786 passed + 6 skipped + 0 failed = 792` reconciling exactly, exit 0), which is strong but is **not the specified check**. 🛑 This repo has already been bitten by this exact class: `card-actuals.md` records a `go build ./...` whose error was **masked by a pipe into `tail`** and printed a false green. Consequence: the one gate whose figures the whole run is reported on is the one gate that cannot evidence its own validity — the same shape as B-81 (a reset that could not be seen in a log) and B-92 (a gate that skipped a step `task test` performs). · _run `overnight-20260804`, found by the orchestrator against its own closeout_ · new · **destination: next milestone, with B-81/B-82/B-86/B-87 — the "a gate cannot tell you what it actually did" cluster** · lead: capture the full gate log to a file and `tail` the *file* for display (`… > gate.log 2>&1; tail -30 gate.log`), then run the block count over `gate.log`; better still, commit the log under `.night-crew/runs/<date>-autonomous/` so morning triage can audit the figures rather than inherit them — G6 on A6 noted the absence of any committed raw gate log as an explicit limit on what it could verify.

- **B-77 · DIAGNOSIS ADDED 2026-08-04 (run `overnight-20260804`)** — the blindness has a concrete, two-part mechanism, established by running the oracle after a night that demonstrably ran and closed. `night-crew run-evidence check --repo . --run 20260804` returns `no-run-evidence` and prints the paths it consulted: (1) it looks for `.night-crew/runs/**<runid>**/summary.json`, `metrics.jsonl` and `journal.jsonl` — i.e. a directory literally named `20260804` — but this repo's run directories are named `**2026-08-04-autonomous**`, so none of the three can ever be found here; and (2) it looks for `**reference/**conflicts-<runid>.md` at the **repo root**, while this repo is scaffolded and its conflict log lives at `**.night-crew/knowledge/reference/**conflicts-20260804.md`. 🛑 **This is the same class as B-130** — a verb resolving a knowledge path against the development clone's layout rather than a scaffolded target repo's — which was **fixed for `launch-prompt` and never for `run-evidence`**. Consequence, and it is the one that costs a night: a completed run reads `ran-did-not-close` or `no-run-evidence` at the next launch, and `/nc-run` §1 routes it to the unfinished-night path. Verified tonight in both directions: `no-run-evidence` **before** the run (correct — nothing had happened) and `no-run-evidence` **after** a 4-of-4 night with a committed `reference/closeout-20260804.md`, four merge-intent notes and a four-entry conflict log (wrong). · lead: resolve both path families the way `launch-prompt` already does — search the scaffolded knowledge home as well as the root, and match the run directory by run-id **prefix** rather than exact name. Until then the launch prompt's standing warning is the only mitigation, and it must keep being written into every slate.

- **B-131 · `[RUN-10] unsubmit returns checklist to editable draft` is a live flake on `dev` and was neither filed nor listed as an armed red** — an independent full-suite run on `overnight-20260804`'s final tree returned **785 passed / 1 failed / 6 skipped, exit 1**, not the closeout's `exit 0 / 786 passed`. The failure is `tests/workflows.spec.js:2823`: `locator('[data-action="submit"]')` not found at `:2865` after unsubmit, with attempt 1 dying earlier at `:2859` on `element was detached from the DOM, retrying`. **Proven not to belong to that branch:** isolated on `dev` at `008e3ad` with a freshly created database it went pass / pass / flaky over 3 runs, and a control tree (the branch carrying `dev`'s `workflows.html`) passed — the only executable `workflows.html` diff sits inside the fail-photo `.then()`, unreachable from RUN-10. 🛑 The real cost is epistemic: it has a documented unattributed history (`reference/card-actuals.md:540,562` — run 20260726, 30 minutes of investigation, "BOUND-NOT-EXONERATION, still UNATTRIBUTED") but **no BACKLOG handle and no place on the armed-red list**, so a triage reader has no way to know a red there is expected, and the closeout's headline figure was a lucky draw nobody could recognize as one. · _morning triage 2026-08-03, adversarial reproduction of run `overnight-20260804`_ · new · lead: give it a handle on the armed-red list first so a red stops reading as a regression, then diagnose with a trace-on-first-retry run plus instrumentation of the unsubmit re-render — the alternating detached-element / missing-locator signature smells like the re-render racing the click

- **B-132 · `workflows.html:708` throws an uncaught `IndexSizeError` on every completed checklist submission, orphaning a full-screen canvas over the app** — the guard at `:706` tests `if(p.life<=0)continue;`, then `:707` does `p.life-=0.015`, so a particle at `life=0.01` clears the guard, becomes `-0.005`, and `ctx.arc(p.x,p.y,p.size*p.life,…)` at `:708` gets a negative radius. `:709` clamps `globalAlpha` with `Math.max(0,…)`; the radius is not clamped anywhere. Observed **28 times in one full-suite run**, and once per isolated run, on **both** `dev` and the branch: `Uncaught IndexSizeError: … The radius provided (-0.0219237) is negative. at workflows.html:708`. **Consequence:** the throw escapes the `requestAnimationFrame` callback, so neither `requestAnimationFrame(animate)` (`:714`) nor `canvas.remove()` (`:715`) runs — the animation freezes ~1.1s in while bursts 2–5 are still bright, and the `position:fixed; z-index:10000` canvas appended to `document.body` is never removed, because `renderFillOut()` does not touch `document.body`. A frozen spray of confetti stays painted over the app until a page reload. `pointer-events:none` means taps still work. Fired from `:3171`, the `allDone` submit-success branch — the crew's most common success path — and each occurrence ships an ERROR-level entry to the server via `log.js`, polluting production error logs. Pre-existing, not branch-caused (`dev` carries the same code at `:673`/`:3155`). **Destination: next night, with B-89 (T-34 decision 137)** · _morning triage 2026-08-03, found by adversarial reproduction of run `overnight-20260804`_ · promoted → shipped-bug-sweep (roadmap round 2026-08-05) · lead: clamp the radius (`Math.max(0, p.size*p.life)`) or decrement before the guard; then screenshot 3s after a completed submit on a 393×852 viewport to settle how bad the frozen overlay actually looks, which is the one thing not established · 🟢 **CLOSED** — run `20260807`, card **A2 `shipped-bug-sweep`**, branch `card/a2-shipped-bug-sweep`, commit `e65deb6`. Clamped the radius: `ctx.arc(p.x,p.y,Math.max(0,p.size*p.life),0,Math.PI*2)`; guard order and every other particle-loop behavior unchanged. Measured tonight at **2482×** per full suite run, not the 28×/run figure above — that figure was stale. **The screenshot settles the open question, and the answer corrects this entry's own speculation:** "bursts 2–5 are still bright" does NOT hold. All of burst 1's particles share one `life` trajectory (same start value, same decrement), so they cross `life<=0` on the same frame; the throw fires on the FIRST particle processed that frame, immediately after `ctx.clearRect()` already wiped the previous frame and before any particle — burst 1 or later — gets redrawn. The canvas freezes fully **transparent**, not visibly frozen; screenshots 3s post-submit pre- and post-fix are visually identical (My Checklists list + toast, no confetti in either). The confirmed defect is a leaked, invisible `<canvas>` DOM node orphaned by the aborted `animate()` (never reaches `canvas.remove()`) — 1 pre-fix / 0 post-fix, asserted directly via `document.querySelectorAll('canvas').length`, not a visual glitch. Regression test added: `tests/workflows.spec.js` describe block `B-132 — fireworks confetti canvas`. RF logs + before/after screenshots: `.night-crew/runs/2026-08-07-autonomous/a2-logs/b132-{red,green}.log`, `b132-{before-frozen-overlay,completed-submit}-3s.png`

- **B-133 · `BACKLOG.md` fails `night-crew backlog check`, so triage §4.5's mandated gate cannot pass in this repo** — `night-crew backlog check --file BACKLOG.md` exits **1** with **290 issues across 156 entries**: 85 unrecognized status, 74 missing plain-language lead, 63 missing handle, 63 missing one-line description, 2 missing origin. The divergence is structural and predates any single run — HQ's convention carries a `destination: …` field the validator does not recognize as a status, and 63 entries predate handles entirely. T-34 decision 141 declines to mass-rewrite 156 historical entries to satisfy a checker (the entries are evidence, and rewriting them rewrites the record), so the verb is **advisory** for this file and new entries follow the validated shape so it converges forward. 🛑 Filed so that "the gate did not pass" is recorded rather than narrated as a passing mention — a skipped ritual step reads exactly like a clean one. **Destination: next milestone — tooling, alongside B-77/B-33** · _morning triage 2026-08-03_ · promoted → backlog-machine-migration · lead: either teach the validator the `destination:` status and a grandfather rule for pre-handle entries, or add a `--since` flag so it validates only entries newer than a given date; a repo whose backlog cannot be validated at all gets no signal when a genuinely malformed new entry lands

- **B-134 · `localhost:5432` is hard-coded as the unset-`DB_TEST_URL` fallback in 7 Go `TestMain` sites, costing 120s per package before a false `ok`** — B-85's class in different files, and B-85 names only `task bdd`. Sites: `backend/internal/auth/permission_test.go:42,346,384`, `purchasing/scheduler_cron_test.go:41`, `inventory/period_summary_test.go:30`, `workflow/stable_identity_test.go:27`, `receipt/worker_test.go:27`. Postgres on this box is on **:5433**, so with `DB_TEST_URL` unset each of these dials a port nothing listens on and waits out its timeout: measured `ok …/internal/workflow 120.070s` and `ok …/internal/receipt 120.168s` — two minutes per package, then `ok`, which is the CLAUDE.md warning about counts-not-`ok` wearing a 120-second disguise. **Destination: next milestone, with B-85 — same class, one sitting** · _morning triage 2026-08-03, adversarial reproduction of run `overnight-20260804`_ · new · lead: make the fallback fail loud instead of defaulting (the reset guard in `scripts/reset-e2e-db.js` is the model — it refuses rather than guesses), or default to 5433 and centralise the string in one test helper

- **B-135 · `version.json`'s two shipping generators disagree at the byte level, so in prod the precache manifest revision does not describe the bytes served** — `scripts/write-version-json.js:43` writes `JSON.stringify(payload) + '\n'` → **21 bytes**, md5 `d324802108f5dfda4cc7e8ac9f01681f`; `backend/Dockerfile:64` writes `printf '{"frontend":"%s"}'` → **20 bytes**, md5 `c21332703b735375e4f0d47637715e5e`. The committed `sw.js` precache entry carries `revision:"d324802108f5dfda4cc7e8ac9f01681f"` — the dev generator's hash — while the container serves the Dockerfile's bytes. **No user-visible consequence could be demonstrated**: Workbox does not verify precache content hashes, and a version bump still correctly changes the revision (verified: `9.9.9` → `6b785046…`). Recorded because the *source* disagreement (this script reads `package.json`, the Dockerfile reads `version.go`) is disclosed in B-92 while the *byte* disagreement is not, and a future Workbox integrity option would turn it live. Also three different line-range citations for the same Dockerfile block — `33-44` (`tests/sw-manifest.spec.js:12`, `roadmap.md:452`), `57-64` (`scripts/write-version-json.js:14`, `tests/version-badge.spec.js:203`), `58-64` (B-92) — while the actual block is `backend/Dockerfile:58-70` with the `printf` at `:64`; same class as B-90. **Destination: next milestone, with B-90/B-91 — the citation-drift cluster** · _morning triage 2026-08-03, adversarial reproduction of run `overnight-20260804`_ · promoted → deploy-hygiene-honesty · lead: have the Dockerfile invoke `scripts/write-version-json.js` instead of reopening the payload in `printf`, which closes the byte divergence and the source divergence in one change

- **B-136 · The correction-photo camera path had zero executing coverage — B-65's other half** — ✅ **RESOLVED 2026-08-03 at morning triage of run `overnight-20260804`, commit `70ea466`, branch `fix/correction-photo-coverage`.** `handleCorrectionPhotoCaptureClick` (`workflows.html:2129-2168`) is the byte-for-byte structural twin of the chain B-65 broke, and nothing executed it: `[FLD-CORRECTION-PHOTO]` covers the same slot but injects via `POST /saveResponse` — the transport bypass FLD-16B's own header names as "the blind spot B-65 lived in for months" — and `tests/workflows.spec.js:684-689` *reimplements* the production write inside `page.evaluate`, asserting against its own copy of the code. The suite's only `waitForEvent('filechooser')` was FLD-16B's, on the fail path. **Proved by mutation before the test existed:** planting the literal B-65 defect (`autoSaveField` at `:2154`) and running every photo/correction test returned **9 passed, rc=0**. `[FLD-16C]` now drives the real path (approver rejects with `require_photo` → presign + PUT stubbed at the network layer → hidden file input via filechooser) and reds on both mutations: `ReferenceError: autoSaveField is not defined`, and `TypeError: Cannot read properties of undefined (reading 'value')` for the `resp.value || resp` form. 🛑 **The argument guard asserts the persisted bundle (`{_v: null, _correction_photo: <url>}`), NOT a checkbox state, and that was measured rather than chosen:** a first draft asserted the answer survived a back-and-reopen and failed, because `hydrateFieldState:1809` does `delete FIELD_RESPONSES[rej.field_id]` on **every** hydrate of a rejected submission ("uncheck a top-level field so crew must redo") — the test was wrong, not the code. No production code changed; `docs/data-flow-audit.md` row 8b corrected from an unqualified "OK". Footprint gate (`tests/workflows.spec.js tests/persistence.spec.js`, the `night-crew.toml` seam for this file): 119 tests, **118 passed / 1 skipped, exit 0**, exactly one summary block in the complete log. · _morning triage 2026-08-03, T-34 decision 136_ · done — `[FLD-16C]` in `tests/persistence.spec.js`, mutation-verified in both directions · lead: the same twin-path question is worth asking of every remaining bundled-metadata write, since two of the nine persisted states now have a test that bypasses the very transport it means to cover

- **B-137 · Two ledger decisions and three runs' drafting treated sales-processor as an external counterparty; the operator maintains it themselves** — established by asking directly at morning triage 2026-08-03. Nothing in this repo ever recorded ownership: `docs/contracts/inventory-period-summary.md:3` says only *"hand this document to the sales-processor maintainer"* and `:5` that the work was kept in its own repo *"per the developer's decision"* — and the developer **is** the operator. The repo is not on this box (`/home/jcole/projects/` holds hq, night-crew, slack-trading, infra, ui-jury, claude-skills and nothing else), which reads as "external" and is really "not written yet". **Consequence:** `NOTICE-sales-processor-2026-08-03-UNSENT.md` is drafted as a second-person apology to an external maintainer, asking questions only that maintainer could answer; ledger **decision 106** (two notices, sent separately, June drift first and alone) and **decision 128** (amending it to one combined notice) both reasoned entirely about sequencing outbound comms, and neither premise held. Cost: drafting across runs `20260803` and `20260804` plus a triage morning, and a P6 fix-forward checklist item (B4) spent re-framing a sequencing question that had no counterparty. 🛑 **What is NOT diminished:** the 111-row / 45-wrong audit stands (the contract documents are the spec sales-processor is built against), §1 is an operational finding about the operator's *own* payroll gate rather than a courtesy, and the coordinated-release constraint is unchanged — owning both sides removes the negotiation, not the one-hour disagreement. · _morning triage 2026-08-03_ · **Destination: next milestone — record counterparty ownership in the contract documents themselves, so no future card infers "external" from "absent".** · done — reframed first-person as docs/contracts/NOTICE-sales-processor-2026-09-01.md + owner lines added to both contract docs (Card 10); finalised 2026-09-01 (ledger T-48) · lead: add an explicit owner line to both `docs/contracts/*.md` headers ("consumed by: sales-processor, maintained by the operator, repo not present in this tree"), and rewrite the notice as a first-person changelog + pre-deploy checklist with the apology and the counterparty questions stripped; the audit body needs no change

- **B-138 · The §1 "no missing weeks" evidence stops 16 days short, and two weekly periods are unaccounted for** — the consumer closed the payroll-gate question on the strength of an unbroken weekly run for every period **2026-05-31 → 2026-07-19**, reasoning correctly that its gate hard-fails before writing a report or dispatching a transfer, so a spurious `ready:false` would show up as a **missing week on disk**. Today is **2026-08-04**: the periods ending **2026-07-26** and **2026-08-02** are outside the window it checked. By the reply's own detection method those two weeks are exactly where a spurious block would sit undetected — the test is sound, it just was not run over the most recent data. Not a contradiction of the consumer's evidence; a gap in its coverage, found by reading the reply rather than by the consumer. 🛑 Also worth one glance while checking: HQ's own `.env.prod` was last modified **2026-07-19**, the same date the reported runs stop. Probably coincidence — prod simply has not been redeployed since — but if the two weeks turn out to be missing, that timestamp is the first place to look. **✅ RESOLVED 2026-08-04 — the consumer extended the check and both weeks are accounted for.** Neither period has a run at **any** pipeline stage (classify, PDF, CSV, transfer ledger all stop at 07-19): nothing was blocked because **nothing ran** — both were operator skips, the business was closed. HQ's `/period-summary` returns `ready: true`, zero pending, zero unlinked for both periods. No spurious block in the extended window, no restatement. 🛑 **The consumer volunteered the finding that outlasts the answer:** *"a week we skip is byte-for-byte indistinguishable on disk from a week your gate blocks — both leave no report and no ledger."* So "unbroken weekly run ⇒ no blocks" was only **conditionally** valid and nobody had noticed the condition; it holds for 2026-05-31 → 2026-07-19 only because the first reply asserted every period in that window is *present*, not merely that present ones completed. The consumer is changing its process to run payroll even on closed weeks. HQ's half of the same blind spot is filed as **B-139**. · _morning triage follow-up 2026-08-04, T-35 decision 142; resolved T-36 decision 146_ · done — accounted for by the consumer's extended check; §1 closed, no restatement · lead: list the consumer's report and transfer-ledger output for the periods ending 2026-07-26 and 2026-08-02; present ⇒ §1 is fully closed and the window in the contract audit should say so, absent ⇒ that is the spurious-block symptom the whole §1 investigation was looking for and it is live, not historical

- **B-139 · `/period-summary` logs no successful request and no `ready` verdict, so HQ cannot see a blocked payroll week at all** — `internal/inventory/handler.go` carries ten `slog.Error` calls on this path and **not one log line for a successful response**. Nothing records that a request arrived, for which period, or what `completeness.ready` came back as; there is no access log on the service-token routes either (`internal/auth/*.go` has no logging). **Consequence:** the only record that a payroll week was blocked is an *absence* of a report on the consumer's disk — and the consumer established 2026-08-04 that an absence is byte-for-byte indistinguishable from a week it simply skipped. So the single most consequential answer this endpoint gives is the one nothing retains. HQ spent a triage morning and two rounds of correspondence reconstructing from the consumer's filesystem what one log line would have stated outright. 🛑 **Same class as B-81** (a reset that could not be seen in a gate log), **B-82**, **B-86**, **B-93** (a gate whose own summary was destroyed by a pipe) — the "a check cannot tell you what it actually did" cluster. The consumer is fixing its half by running payroll even on closed weeks; that is discipline, and this half is mechanism, which is the more durable of the two. **Destination: next milestone, with the B-81/B-82/B-86/B-93 cluster.** · _morning triage follow-up 2026-08-04, found while recording the consumer's reply (T-36 decision 147)_ · promoted → pipeline-fail-loud · lead: one `slog.Info` at the end of the handler — `period from`, `to`, `ready`, `len(pending_review_ids)`, `len(unlinked_line_item_ids)` — which makes a blocked week greppable in prod logs regardless of what any consumer writes to disk; consider the same for `/menu-cogs` so a future consumer's first call is visible, since that endpoint currently has no reader at all and nothing would show if one appeared

- **B-140 · The stale `sync-rxdb-row-visibility-rls` activation gate survives at four more sites five days after that card merged, one of them inside `sync-rxdb/` and one in a shipped precached asset** — the card merged **2026-08-01** at `bbbfc64` (run `overnight-20260801`; roadmap flipped it DONE at `914536c`), and four live comments still name it as a precondition that has not landed: `sync-rxdb/conflict-notice-ui.js:26` (*"the /sync door answers 503 by design until row-visibility RLS lands"*), `workflows.html:3560` (*"answers 503 by design until `sync-rxdb-row-visibility-rls` lands"* — the only survivor that spells the slug), `workflows.html:329` (*"must not happen until row-visibility RLS lands"*), and `tests/states-sync-rxdb-conflict-notice.spec.js:30` (same paraphrase). Card `w0-repo-hygiene` retired the two it owned — `sync-rxdb/bootstrap.js:22` and `sync-rxdb/client.js:1106-1111` — and its merge-intent note then claimed the directory was clean ("both sites, not one"); `conflict-notice-ui.js` makes that false, so there were **five** sites, not two. **Consequence:** a later author reads a merged card as an open precondition and either waits for work that already shipped or re-does it. The genuinely open precondition is the **cutover itself** — no page calls `startHQReplication`, so setting `HQ_SYNC_REST_URL` today would start a replication nothing reads — and a gate naming the wrong card hides that one. 🛑 **Also, and this is the more useful half: `tests/repo-hygiene.spec.js`'s case 3 (`no sync-rxdb source gates activation on the merged row-visibility-rls card`) is spelling-shaped, not fact-shaped.** It scans only `sync-rxdb/*.js` for the literal string `sync-rxdb-row-visibility-rls`, so it is blind to every paraphrase and to the whole tree outside that directory — which is exactly why it passes today with four live gates standing, three of them in files it never opens. **Demonstrated, not theorised:** re-inserting a paraphrased gate ("until row-visibility RLS lands", no slug) into `bootstrap.js` left the suite at **4 passed**. A guard that can be defeated by rewording the sentence it guards is a guard against typing, not against the defect. **Why this is a card and not a triage edit:** `workflows.html:3560` sits in a precached shipped asset, so retiring it forces an `sw.js` regeneration (B-13 — an `sw.js` you did not commit does not deploy) and therefore a full-suite gate; that is a night's cost, not a same-footprint tidy-up. **Destination: next milestone — with the sync cutover work, since `sync-hard-cutover` rewrites `workflows.html:3560`'s surrounding block anyway.** · _found by G6 adversarial review on card `w0-repo-hygiene`, run `20260806`_ · promoted → sync-doc-honesty · lead: retire all four comments in one change set (they are the same sentence four times) and, in the same card, broaden `tests/repo-hygiene.spec.js` case 3 from slug-matching to fact-matching — scan the whole tree, not just `sync-rxdb/`, and assert on the *claim* (a comment that names any DONE roadmap card as a future precondition) rather than on one spelling; the roadmap already carries the card statuses the assertion would need to read

- **B-141 · 🛑 A destructive-DDL parameter is guarded by a four-item literal blocklist, and the list cannot contain the names that matter — this destroyed the production database on 2026-08-06** — `backend/internal/sync/rowvisibility_rls_test.go` takes `HQ_RLS_TEST_DB` from the environment and feeds it, unquoted, into `DROP DATABASE <name> WITH (FORCE)` against `defaultHQAdminURL = postgres://yumyums:yumyums@localhost:5433/postgres` — **which is the production cluster on this box**. Card `a3-rls-fixture-own` (run `20260806`) correctly removed the old shared *default* and then guarded the now-mandatory environment value with a blocklist of four literals: `hq_test_b2_fdw`, `hq_test_go`, `hq_test_e2e`, `postgres`. **Measured, one name per invocation:** `yumyums` → **accepted, dropped and recreated production**; `hq_test_g6a3_0806_go` (the running leg's own `DB_TEST_URL` database) → accepted, dropped; `hq_rls_a1_0806` (another card's live isolation name) → accepted, dropped. The card's own source comment recognises the exposure — *"the environment is now the ONLY source of an identifier that goes into destructive DDL"* — and then chooses the one shape that cannot be complete, because the dangerous names are **per-leg and per-environment** and therefore unknowable at authoring time. Two of the three are available to the process at runtime and free (`DB_TEST_URL`'s database, `TEST_DB_NAME`); the application database is in `backend/.env`/`DB_URL`. 🛑 **Aggravating:** the same card makes supplying the name **mandatory for every leg**, so it multiplies the number of times a human or an agent hand-types a database name straight into `DROP DATABASE … WITH (FORCE)` against the production cluster. **Consequence, realised:** during G6 adversarial review of that very card, a probe of the blocklist set `HQ_RLS_TEST_DB=yumyums`; the suite accepted it and dropped the production database. The `production` schema was destroyed with **no PITR (`archive_mode=off`), no dump on the box, and an empty alternate volume** — see B-143. Prod was rebuilt structurally empty by operator decision the same night. **This defect outlives card A3 and must be filed regardless of that card's disposition.** · _found by G6 adversarial review on card `a3-rls-fixture-own`, run `20260806`, by realising it_ · promoted → gate-rls-fixture-ownership (prefix guard) + test-cluster-separation (structural half; ledger §T-38 decision 155) · lead: replace the blocklist with a **required prefix** — `^hq_rls_[a-z0-9_]+$` — which is one line and refuses all three measured escapes above, including `yumyums`; a blocklist enumerates what must not be destroyed and can never be complete, a prefix enumerates what may be, and only the latter is a boundary. Then reconsider whether a test suite should hold admin credentials to the production cluster at all: the deeper defect is that `defaultHQAdminURL` points at prod, so *any* mistake in this file is a production mistake · 🟡 **STRUCTURAL HALF CLOSED** — run `20260807`, card **W0 `test-cluster-separation`**, branch `card/w0-test-cluster-separation` (decision 155 executed). The suites now run on their own container: **`yumyums-test-pg`** (`docker-compose.test.yml`, service `postgres-test`, project `yumyums-test`, host port **5434**, volume `yumyums-test-pgdata`, role **`hqtest`** — a *different role name*, not just a different port, so a stale default fails closed with `role "hqtest" does not exist` instead of authenticating against prod). `defaultHQAdminURL` is now `postgres://hqtest:hqtest@localhost:5434/postgres` and `defaultFDWPort` is `5434`; `grep` finds **no `yumyums:yumyums` and no `:5433` in any test-path default in the tree** (the remaining `:5433` sites are `backend/Taskfile.yml`'s dev/migrate/db-ping coordinates, which are *supposed* to name the dev cluster, and prose). The card also re-pointed three sites the promotion note did not name: `scripts/reset-e2e-db.js` (the single source of Playwright's coordinates, and the file that issues the `DROP DATABASE` on every `npx playwright test`), `Taskfile.yml`'s `backend:db-test` deps, and `test:all`'s post-run `psql` reset trap. 🛑 **THE GUARD HALF IS STILL OPEN** — `HQ_RLS_TEST_DB` still goes unquoted into `DROP DATABASE … WITH (FORCE)` behind the four-item blocklist; the `^hq_rls_[a-z0-9_]+$` prefix guard (and **B-142**) ride the attended `gate-rls-fixture-ownership` re-gate, which stays refused-and-preserved on branch `card/a3-rls-fixture-own`. What separation bought is that the blast radius of that still-live defect is now a throwaway container instead of the business's database — a structural impossibility argument where the guard is a correctness one. Do not close this entry until the prefix guard lands · ✅ **GUARD HALF CLOSED — entry closed, attended re-gate 2026-08-28** (`card/a3-rls-fixture-own-regate`, ledger §T-45): the blocklist is replaced by the required prefix `^hq_rls_[a-z0-9_]+$` (`rvFixtureDBNameCheck`), examined at the top of `rvConnect` before substrate resolution or any socket; the incident probe (`HQ_RLS_TEST_DB=yumyums`) is pinned refused by a subprocess test run RED against the pre-fix tree first

- **B-142 · Card `a3-rls-fixture-own`'s own new test blind-drops the exact database B-35 is about, and the card breaks `task test:go`** — held here because the card is **NOT merged** (G6 verdict DO NOT MERGE) and its branch `card/a3-rls-fixture-own` must not be the only record of what it still owes. Two outstanding fixes. **(a)** `rowvisibility_rls_test.go:2658-2665`: `dropVictim()` runs unconditionally **before** `CREATE DATABASE` and again in `t.Cleanup`, and `TestRVFixtureDatabase_AnUnnamedRunEatsNothing` never calls `rvConnect`, so it **never resolves `HQ_RLS_TEST_DB`** and cannot tell a database it created from one it found. Measured: a fully compliant leg (`HQ_RLS_TEST_DB=hq_rls_g6a3_0806`) destroyed `hq_test_b2_fdw` — a database it did not create and did not name — and left it dropped, `EXIT=0`. That is precisely the property the card exists to remove, reproduced inside the card. It matters during the transition window: every sibling worktree on this box is on a pre-A3 tree and still *defaults* to that name. The red-first design genuinely needs that name, so the fix is **not** to rename the victim but to refuse to run when it already exists — `t.Fatalf("someone is holding it")` — instead of dropping blind. **(b)** `Taskfile.yml:101` and `:132` both run `cd backend && go test -p 1 ./...` with **no `HQ_RLS_TEST_DB`**, so on merge `task test:go` and `task test:all` — the repo's two documented test targets, and the gate CLAUDE.md names — fail for every user on the first sync test. One `env:` line each. The card's merge-intent covers "any `go test ./...` a human types tomorrow" generically but never names the Taskfile, and lists what it did *not* touch as a virtue without noting that `Taskfile.yml` now *needs* touching. · _found by G6 adversarial review on card `a3-rls-fixture-own`, run `20260806`_ · promoted → gate-rls-fixture-ownership (attended re-gate; ledger §T-38 decision 155) · lead: do (a), (b) and **B-141's prefix guard** as one card, then re-gate; the card's mechanism and evidence are otherwise the strongest of that run — the refusal provably fires before any socket is opened, B-36's opt-out door is unchanged in both directions, A1's counters hold, and the green-run-eats-a-fixture red was independently reproduced · ✅ **closed — attended re-gate 2026-08-28** (`card/a3-rls-fixture-own-regate`, ledger §T-45): (a) the suite now CLAIMS its fixture (`rvClaimFixtureDatabase`) — an existing name is refused with EXIT≠0 and provably not destroyed, and the run drops only the database it created; (b) moot by design — the per-process default means `task test:go`/`test:all` need no env line and ran green unmodified

- **B-143 · 🛑 The production database has no backup of any kind, and this was discovered by needing one** — verified on 2026-08-06 while attempting recovery: `archive_mode=off` and `archive_command=(disabled)` on the `yumyums-dev-pg` cluster, so **no PITR**; no `pg_dump`/backup target in `Taskfile.yml` or any compose file; no `*.sql`/`*.dump`/`*.sql.gz` anywhere under `/home/jcole` or `/mnt/c/Users/jcole`; and the second volume `yumyums-pgdata` (created 2026-07-14) is **empty** (4.0K, verified read-only) while the live volume is `yumyums_yumyums-pgdata`. **Consequence, realised:** when the production database was dropped (B-141), there was nothing to restore from, and the only available action was to rebuild it structurally empty. Data re-derivable from upstream systems — Mercury receipts, DO Spaces, Toast sales — survives as a re-import problem. Data that existed **only** in Postgres is gone: workflow templates, submissions and responses, real users and sessions, stock count overrides, recipes. 🛑 **This is the finding that converted a bad night into an unrecoverable one.** B-141 is the proximate cause; this is why it was terminal. `task prod:deploy` documents a `prod:rollback` for the *image* and nothing at all for the *data*, which is exactly the asymmetry that made the image reversible and the database not. · _realised during recovery, run `20260806`_ · promoted → prod-backup-floor-and-pitr (ledger §T-38 decision 154) · lead: a nightly `pg_dump` of `yumyums` to a path outside the Docker volume is the floor and is a Taskfile target plus a cron line; enabling `archive_mode` with a local WAL archive gives PITR and is the ceiling worth having. Either one alone would have made 2026-08-06 a twenty-minute restore. Consider also that the *test* suites and *production* share one cluster on this box — separating them would have made B-141 survivable independently of any backup

- **B-144 · `verify-test-harness.sh` Check B2 costs ~14 minutes of wall clock and the script's own cost section is silent about it** — Check B2 (the unset-`DB_TEST_URL` leg) probes each of 7 packages whose unset fallback DSN black-holes ~120s before skipping (measured: `ok internal/workflow 120.065s` in the unset log; ~120s × 7 ≈ 14 min on this host), while the script's "measured, not estimated" cost comments document Check B's ~15–20s and say nothing about B2 — so a first-time invocation looks hung, and the triage adversarial review's first run was killed by a 7-minute timeout mid-B2 for exactly this reason. Semantics held everywhere observed; this is a cost-honesty defect in a script whose card (A2, run `20260806`) exists to make the harness honest. · _found by the triage adversarial review re-executing the harness on the merged tree, morning triage 2026-08-05_ · new · lead: document B2's measured per-package cost beside Check B's and print a one-line "Check B2: ~2 min per package, N packages — this is normal" banner when it starts

- **B-145 · 🛑 Production is running structurally empty — decide, per data class, what gets re-imported, what gets reconstructed, and what is accepted as lost, then execute it** — *As the owner whose crew uses this console daily, I want each class of the destroyed production data either restored, rebuilt, or explicitly written off, so that the business is not silently operating on an empty database while the gap compounds.* Follows directly from **B-141** (the drop) and **B-143** (nothing to restore from): after the 2026-08-06 incident, prod was rebuilt structurally empty by operator decision the same night, and nothing since has put data back. The classes split by where truth survives. **Re-derivable from upstream, a re-import problem:** Mercury receipts (and whatever the DO Spaces bucket still holds — uploaded receipt files live outside Postgres and should have survived; verify before assuming), Toast menu items and daily sales. Each needs its retention window measured *soon* — re-import gets harder or impossible as upstream windows close, which is what makes this urgent rather than merely important. **Existed only in Postgres, gone:** workflow templates, submissions and responses, real users and sessions, stock count overrides, recipes (`usage_pct` allocations). Templates, users and recipes are reconstructable by hand at known cost (templates re-authored in the Builder, users re-invited, recipe sliders re-set); submissions/responses and stock overrides are history and likely write-offs — but that is the operator's call to make explicitly, not a default. 🛑 **Two live consequences while this sits open:** the crew re-enters operating state by hand into an empty console, and the sales-processor contract endpoints (`period-summary` for weekly payroll COGS, `menu-cogs` for the weekly report) compute over a purchase history with a hole in it — the current payroll period's COGS is wrong until purchases are re-imported or the gap is declared to sales-processor's owner. **Decision and execution split:** the per-class rulings (restore / rebuild / write off, and what to tell sales-processor) are attended and the operator's own; the mechanical legs — a Mercury/Spaces backfill through the existing receipt pipeline, a Toast re-sync — are cardable once ruled. Sequencing note: `prod-backup-floor-and-pitr` (decision 154's card) should land **before** hours of manual reconstruction go in, so the rebuilt data is itself protected from day one. · _operator-directed filing, 2026-08-06, from HANDOFF run `20260806` next-action 1_ · **executed 2026-08-06** (attended; rulings = ledger T-39 decisions 156–159; handoff `reference/handoff-prod-data-recovery-20260806.md`) · **per-class status:** purchases + receipts **restored** — backfill to 2026-03-01 ran clean (149 processed, 58 auto-created events, 84 in pending review for the crew, 0 errors; a Mercury pagination defect blocked the first attempt and was fixed with tests — `internal/receipt/mercury.go`, offset→cursor, plus new `cmd/backfill-receipts`); item catalog + vendors **restored via the backfill** (263 items, 20 vendors); Toast menu + daily sales **restored** (34 menu items, 1 052 sales rows, 80 business days 2026-03-05→08-02; Spaces archive first made gapless through 20260805 from the SFTP tail); users **partially rebuilt** (superadmin live; crew re-invites are the operator's, open); recipes **open — operator hand-rebuild** in the Recipes tab, COGS attribution wrong until done; submissions + responses, stock count overrides, sessions, and everything outside the inventory app **written off** (decision 157, operator's explicit scope); sales-processor notice **held by the operator** (decision 158, no draft). Backup floor + PITR landed BEFORE reconstruction (decision 154 both halves: nightly dump/globals/basebackup at 03:30 proven by observed firing, WAL archiving live outside the pgdata volume, restore drill green)

- **B-146 · Prod's Toast sync has been silently dead since the 2026-07-28 image rebuild — the SFTP key never ships** — `TOAST_SFTP_KEY_PATH=./id_rsa` resolves inside the container to `/app/id_rsa`, and that file does not exist there: the key lives only in the dev repo at `backend/id_rsa` (git-ignored, so the prod clone never receives it via `git reset --hard origin/main`), the image build does not COPY it, and `docker inspect yumyums-prod` shows **zero mounts**. Measured 2026-08-06 during B-145 Phase 0: the Spaces Toast archive's last date-directory is `toast/20260724` (last write 2026-07-26) and the container was built 2026-07-28 — sync worked until the rebuild replaced an image that evidently had the key, then stopped without any alert. Toast's SFTP side retains a ~27-day rolling window (measured: `20260710`–`20260805`), so each silent week burns real retention margin; the Spaces archive currently covers `20260305`–`20260724` and together they are still gapless, but only because this was caught inside the window. Fix shape: put the key beside `.env.prod` in the prod clone as an untracked file and bind-mount or COPY it in (the `.env.prod` secrets pattern already survives the hard reset), and give the sync loop a fail-loud path — a sync that cannot even open SFTP should surface in `/api/v1/health` or the Cliq alert, not log-and-continue. · _found during B-145 recovery Phase 0, 2026-08-06_ · promoted → toast-ingest-resurrection · lead: fix before or together with the B-145 Toast re-import (Phase 4.2) — the re-import can run from the dev repo where the key exists, but prod's ongoing daily sync stays dead until the key ships

- **B-147 · 🛑 The permanent B-132 regression test rewrites this run's committed evidence on every suite run** — `tests/workflows.spec.js:4242-4244` hardcodes its screenshot path into `.night-crew/runs/2026-08-07-autonomous/a2-logs/b132-completed-submit-3s.png`, so every future full-suite execution overwrites a file the run committed as evidence (it already did once; the bytes happened to be identical). Confirmed from the test source by the triage adversarial review. · _run `20260807` A2/G6-1, morning triage 2026-08-06_ · done — fixed same day, commit `05bba61` (screenshot re-pointed at gitignored `test-results/`; targeted test green) · lead: fixed before the next overnight run, as flagged

- **B-148 · 🛑 A red run of the spike-B script contaminates the shared substrate persistently, and its printed recovery command does not work** — the user-lane probe row (`probe-ok-<run>`, per-run timestamped id) escapes the restore manifest, so a run dying between insert and cleanup leaves it in `hq_sync_checklists` forever, which then reds `TestJWTBridgeRLS`; and the manual recovery command the script prints dies at import on the `SPIKE_B_HQ_CID` guard in `hq-bridge-env.js` — the advertised undo for exactly that failure is inoperable. Found by G6 on a deliberate red-path probe. · _run `20260807` S/G6-1+2 (paired), morning triage 2026-08-06_ · done — fixed same day, commit `05bba61` (probe ids written into the manifest before the load; CID guard moved from import time into srcPsql(), red-first proven: dev blob exit 2 → fixed exit 0). Note: the full red-path spike rehearsal was NOT re-run (needs the substrate stack); the fix is execution-verified on the recovery path and statically on the manifest · lead: fixed before the next overnight run, as flagged

- **B-149 · The uid-mismatch half of the B-89 fix is unguarded by any test** — `sync-rxdb/bootstrap.js`'s `cachedGrantSlugs()` verifies `env.uid` against the identity token (correct, confirmed by read), but deleting just that check leaves the shipped B-89 spec green: no test seeds a mismatched-uid envelope against bootstrap.js (`tests/index.spec.js` covers uid-mismatch for the launcher only). Reproduced by mutation at triage: remove the `env.uid !== deviceId` clause, run `tests/sync-rxdb-client.spec.js -g "window.HQSync is constructed"` → 1 passed. · _triage adversarial review, morning triage 2026-08-06_ · promoted → client-guard-coverage · lead: add one test seeding an `{uid: 'someone-else', apps: [...]}` envelope and asserting the sync client treats it as nothing-cached

- **B-150 · The composed `task test:*` targets' "db up first" ordering is unproven from cold** — Taskfile v3 runs `deps` in parallel, so nothing proves the test-DB container is up before the suite starts on a cold machine (fail-loud today: first invocation errors, second succeeds). · _run `20260807` W0/G6-1, morning triage 2026-08-06_ · new · lead: serialize (move db-test into `cmds` or hang it off `test:db:up`) and capture one cold run as evidence

- **B-151 · The armed `backend:db-test` dev-host guard fails OPEN on psql connection error** — the refusal checks `psql | grep -q 1`, so a connection failure (empty output) is indistinguishable from "host does not serve the dev DB" and the guard proceeds. · _run `20260807` W0/G6-3, morning triage 2026-08-06_ · new · lead: distinguish empty-result from non-zero psql exit; refuse on error

- **B-152 · Seven test fallback DSNs still carry `yumyums:yumyums@localhost:5432`** — in 5 files (`scheduler_cron_test.go:41`, `permission_test.go:42,346,384`, `stable_identity_test.go:27`, `period_summary_test.go:30`, `worker_test.go:27`), aimed at a live cluster that is not the hq-serving one but is real — prod's credential pair in test files pointed at a live system. Count and locations re-verified by the triage adversarial review. · _run `20260807` W0/G6-2, morning triage 2026-08-06_ · new · lead: re-point the fallbacks at `hqtest@localhost:5434` or delete them so `DB_TEST_URL` is required

- **B-153 · Spike-B's assertions are too loose to catch a dropped row** — the archived-exclusion check is `< total && >= 4` rather than `=== 6` with specific ids, hq-reset's seed-survival check is `> 0` not exact, and teardown's compose-down half is warn-and-continue — a transform bug dropping a legitimate row would be self-consistently green. · _run `20260807` S/G6-3, morning triage 2026-08-06_ · new · lead: pin the counts and ids the fixture makes knowable; matters before spike-C reuses the harness

- **B-154 · `night-crew.toml [e2e.seams]` has no row for the sync-rxdb paths** — undeclared paths de-confine to the full suite (safe default), but the run footprint rule exists precisely so the mapping is deliberate; A2 touched `sync-rxdb/bootstrap.js` and rode the default. · _run `20260807` A2 observation, morning triage 2026-08-06_ · promoted → client-guard-coverage (rider) · lead: add the row naming the spec subset `sync-rxdb/**` changes must run

- **B-155 · Gate logs carry no command/env attestation header as a convention** — A2's gate logs had no echo of the command and environment that produced them (results were corroborated by durations and content, but the format is thin); W0 and S both used an attestation header worth requiring. · _run `20260807` A2/G6 convention note, morning triage 2026-08-06_ · new · lead: make the W0/S-style header a slate requirement so every gate log states what produced it
- **B-156 · FR-11 — `tests/inventory.spec.js:3577` reds its UI half at whole-suite position; candidate new named flake, NOT B-32's shape** — failed in card C's full suite on run `20260807-2` (cardCount 0 at `:3626`, a 1.5 s assertion mismatch — not a 30 s timeout, so a distinct shape from the B-32 family) and passed at the identical suite position in card D's run the same night. On no armed list (B-27 is `:883`, B-32's inventory member is `:2908`). Neither card can have caused it: both diffs carry zero frontend/spec files — verified by the triage adversarial review, not just claimed. 🛑 Non-reproduction does not retire it (the standing rule for LST-17/B-27/B-30/B-32 applies in full). · _run `20260807-2` card C G6 note, morning triage 2026-08-07_ · new · lead: give it a grep handle per B-25 (the FR-11 tag already in the test title), arm it in the next slate's preconditions as expected-possible, and retire only by diagnosis (decision 100) — first check whether its assertion sits near a load-sensitive wait like the B-32 members
- **B-157 · One `/saveResponse` call fires any CDC trigger on `submission_responses` twice** — `workflow/repository.go:826` (the save) plus `sync/ops.go:148` (`EmitOp`'s `UPDATE … SET lamport_ts` LWW stamp) are two row writes per user action, and a row-level trigger cannot tell them apart — so any change-data-capture mechanism on this table (spike C's LISTEN/NOTIFY relay included) sees 2× the write volume a reader would predict. Harmless in the spike only because the projection is an idempotent upsert; recorded beside the code in `spikec_relay.go`. · _run `20260807-2` card C finding, morning triage 2026-08-07_ · promoted → cdc-single-fire · lead: the Activity 3 adoption card must either make the trigger fire once per logical change (e.g. a `WHEN` clause excluding lamport-only updates) or state idempotent-upsert as a load-bearing invariant of the projection — deciding neither is how the 2× quietly becomes a bug
- **B-158 · Spike-script hardening, one line each, on next touch** — (a) C's `rxdb/spike-c-read.js:165` checks `replErrors` only pre-write, so a post-write infra death could mislabel exit 1 "mechanism disproven"; (b) D's suppression leg would be backstopped by asserting rtprobe's `events==1` COUNT lines rather than riding event ids alone. Both flagged by the cards' own G6s. · _run `20260807-2` G6 notes, morning triage 2026-08-07_ · new · lead: one-line fixes each; do them on whichever cutover/Activity-3 card next touches either script, not as standalone work
- **B-159 · `spike-d-realtime.sh --fresh-substrate` is a destroy footgun inside a reconcile-only discipline** — it forwards `--fresh` to `env-up.sh`, which runs `down --volumes` on spike A's shared substrate; opt-in and never default, but the flag's name does not say it destroys, and the discipline every card runs under says reconcile-never-destroy. · _run `20260807-2` card D G6 note, morning triage 2026-08-07_ · new · lead: rename it to say what it does (`--destroy-substrate`) or gate it behind an explicit env ack (e.g. `SPIKE_DESTROY_OK=1`) so a hurried invocation cannot reach `down --volumes` by autocomplete
- **B-160 · HQ stores no template→app association — `app_slug` is a constant in every sync projection writer** — spike B's finding #1, resurfaced verbatim by spike C: the relay hardcodes `app_slug` because nothing in HQ's schema maps a template (or submission) to an app; labelled in `spikec_relay.go` beside the code. Every projected row claims the same app until this exists. · _run `20260807-2` card C finding (spike B finding #1 resurfaced), morning triage 2026-08-07_ · promoted → app-slug-association · lead: the cutover card needs a real mapping (a column on templates, or config) before the projection can serve more than one app — decide the source of truth there, not in the relay
- **B-161 · The disconnect/reconnect/catch-up recovery story is architecture-implied but proven by nothing** — spikes A–D proved the filter semantics (D) and the write→read round trip (C), but no test anywhere disconnects a replicating client, writes rows while it is dark, reconnects it, and asserts the rows arrive. The claim "a reconnecting client pulls forward from its checkpoint and re-reads state" rides on RxDB's replication protocol doing what its docs say against THIS substrate with THIS checkpoint shape — and the checkpoint clause is `submitted_at=gte.<iso>`, so a row *updated* without `submitted_at` advancing is exactly the kind of change a catch-up pull could miss (the same unreliable-watermark concern that disqualified `answered_at` for the polling relay). An ignored catch-up is the quiet failure shape this repo keeps re-finding: the system behaves identically until a crew member's phone sleeps through a write. · _operator question, post-triage session 2026-08-07_ · promoted → spike-e-reconnect-catchup · **done — answered GREEN by card E, run `20260808` (T-42)**: a severed client recovered all three dark-window changes including the in-place UPDATE, via checkpoint pull riding the trigger-stamped substrate `_modified` (strict `gt` + id tie-breaker, minted at the projection boundary independent of any HQ business timestamp) — the feared `gte.<iso>` weak spot does not exist on this path. The green is CONDITIONAL on the relay staying trigger/NOTIFY-driven: a polling relay on a business watermark reintroduces the miss (`submitted_at` measured never advancing after INSERT — a creation timestamp wearing a watermark's name). Red `--no-pull` exit 1 proved the assertion set non-vacuous; both legs re-executed live at triage with teardown byte-verified · lead: spike-shaped and cheap — reuse spike C's harness (real write path + relay) and spike D's substrate discipline: subscribe an RxDB client, sever its connection, write N rows through `/saveResponse` (including at least one UPDATE to an existing row), reconnect, and let the exit status say whether all N arrive by pull within a bound; the natural red is realtime-only recovery (pull leg disabled) which must miss the dark-window rows or the assertion set is vacuous
- **B-162 · `tests/receipt-carousel.spec.js:123` reds at whole-suite position in B-32's family shape, on no armed list** — failed in card E's full suite on run `20260808` (30 s `networkidle` timeout, whole-suite position — B-32's exact shape) and passed at triage's re-run of the identical tree (1.5 s). Provably not the card's: E's diff carries zero frontend/spec/Go files. Not a named member of any armed entry (B-27 is `inventory:883`, B-32's members are `workflows [LC-02]` and `inventory:2908`), so per the B-156/FR-11 precedent it is filed rather than absorbed silently. 🛑 Non-reproduction does not retire it — the standing rule for LST-17/B-27/B-30/B-32 applies in full. · _run `20260808` G6 finding 1, morning triage 2026-08-07_ · new · lead: give it a grep handle per B-25, arm it as expected-possible in the next slate's preconditions, and treat it as a candidate B-32 family member (c) — fold it into B-32's per-test duration/position measurement rather than diagnosing it alone; retire only by diagnosis (decision 100)
- **B-163 · Spike-E harness maps infra failures to RED and its `task` wrapper lies about the exit code** — three latent exit-code conflations, all confirmed by execution-adjacent reading at triage (none fired in any recorded run): (a) unguarded `$(srcpsql …)` assignments at `spike-e-reconnect.sh:411,463-464,552-554` under `set -euo pipefail` die with psql's own exit code — an infra failure can read as RED (exit 1), which would falsely instruct Activity 3 to add a resync step; (b) `:564` — `CLIENT_RC=0` with `B_ROWS≠1` calls `red` where the contract's honest code for "green was vacuous, no verdict" is 2; (c) an uncaught exception outside the try blocks in `rxdb/spike-e-reconnect.js` exits Node 1 → RED. Plus: `task spike:reconnect:red` exits **201** (go-task's failing-command code), not the script's 1 — anyone gating on the wrapper misreads the RED-FIRST capture; gate on the script directly. **✅ RESOLVED 2026-08-09 on run `20260809`, branch `card/c2-spike-exit-code-honesty` (card C2).** (a) The seven bare `VAR="$(srcpsql …)"` assignments that die under `set -euo pipefail` with psql/docker's own code (`TABLES`, `HQ_USER_ID`, `SUBMIT_TRG`, `RESP_TRG`, `B_ROWS`, `A_ROWS`, `C_ROWS` — a superset of the four cited; the printf-arg and `mapfile < <(…)` substitutions do NOT die under `set -e` and are gated by their own count checks, left unchanged) now carry `|| cannot_run …` → exit 2. (b) The `CLIENT_RC=0` + `B_ROWS≠1` branch (was `:564`, now `:591`) calls `cannot_run` (exit 2 — vacuous green, no verdict), not `red`. (c) `rxdb/spike-e-reconnect.js` installs top-level `uncaughtException`/`unhandledRejection` handlers mapping any escape from the unprotected main flow to `die(SETUP)` → exit 2. (d) `Taskfile.yml` `spike:reconnect:red` now carries a comment note on the 201 trap (gate on the script, not `task`). Red-first proven for each of (a)/(b)/(c) with deliberately-missing-coordinate probes (each exit 1 → exit 2), captured in the run's merge-intent. Note: like B-148, the full live spike was NOT re-run — B-163 states the recorded verdict does not depend on any of these; the fixes are execution-verified via exit-code probes and static/`node --check` review. · _run `20260808` G6 findings 2–4 + triage adversarial review, morning triage 2026-08-07; resolved run `20260809` card C2_ · done — all four (a–d) landed on `card/c2-spike-exit-code-honesty`, red-first proven 1→2 per path · lead: none outstanding
- **B-164 · A bare `backend:*` task invocation still resolves :5433 defaults — standing rule 1 has no mechanical guard above the per-task one** — during run `20260808` the implementer ran bare `task backend:db-test`; the task's own guard refused (read-only `SELECT 1 FROM pg_database`, zero writes, immediately re-run with :5434 coordinates) but standing rule 1 forbids ANY :5433 contact including read-only, so the night could not claim a clean no-contact record. The guard fired correctly; the defect is that the dangerous default is reachable by one bare word at all. · _run `20260808` disclosed near-miss, morning triage 2026-08-07_ · new · lead: fail closed before any socket — make `backend:db-test` (and any `backend:*` target that resolves DB coordinates) refuse to run without explicit coordinates, or repoint its default at :5434's test cluster; and carry the rule in every future launch prompt: never invoke `backend:*` bare, `task test:targets` is the read-only coordinate source
- **B-165 · `npx bddgen` is load-bearing for the Playwright gate and undiscoverable — skipping it silently runs 797 of 798 tests** — the `[bdd]` project (`playwright.config.js:41-43,147`, `features/user-invite-onboarding.feature`) has no spec until `bddgen` generates one, and `npx playwright test` without it reports a clean, full-looking summary one test short. Triage's own adversarial re-run initially fell into exactly this hole before catching it; nothing in `package.json` scripts names bddgen. A silently-shrunk denominator is the B-37 class one level up. · _triage adversarial review of run `20260808`, morning triage 2026-08-07_ · new · lead: make the miss loud, not remembered — either wire `bddgen` into the suite's own startup (webServer command or a globalSetup that runs it) or make the `[bdd]` project fail when its generated spec dir is empty; until then every gate recipe must carry the `npx bddgen` line explicitly
- **B-166 · [FILL-01]'s fixture-precondition guard reds in shared-DB subset legs — composition artifact, not a card defect** — pairing `persistence.spec.js` before `tests/sync-fill-view.spec.js` in one per-leg DB leaves 1 REST response (`submitChecklist` moves drafts onto the new submission), so FILL-01's guard ("the fixture must carry NO REST responses, or an answered field proves nothing", `sync-fill-view.spec.js:190`) refuses to render a verdict — the behavior assertions never run. Full 811-suite unaffected; isolated run 9/9 green. Reproduced independently at triage (same guard message, 1 failed/109 passed in the same 5-spec combination as the run's `final-g2-pw-r2.log`). · _run `20260808-2` closeout + triage adversarial review, morning triage 2026-08-08_ · new · lead: a known-shape red, not a defect — any confined leg pairing those two specs must expect this exact guard message and not re-diagnose; the durable fix is per-spec DB isolation in confined-leg recipes or FILL-01 seeding its own fixture DB
- **B-167 · G6 carried observations from run 20260808-2 — none blocking, bundled to ride the next sync-surface card** — (a) C2 F-4: the `hq_sync_read` URL override persists to localStorage by design — the accidental-enable vector once the sync door opens for real; (b) C3 O-1: `overlayKeys()` naming; (c) C3 O-2: FILL-04 covers the negative half only — no positive teammate-answered-row-of-the-open-submission case at page level (G6's re-verify Probe-2 covered it manually); (d) S1 F-2: "many live at once" is the lifecycle's contract while the tap path serializes — precision note; (e) C3's own stated bound: `RXDB_FILL_RESPONSES` needs a checklist dimension if one template ever has two concurrent fills; (f) triage adversarial observation: the flag's default-OFF is guarded ONLY by `sync-one-row.spec.js:261` + `sync-fill-view.spec.js:264` — C1's B-88 object-level guard stays green with the default forced ON (correct per its contract, but it is not a default guard). · _run `20260808-2` G6 reports + triage adversarial review, morning triage 2026-08-08_ · promoted → sync-doc-honesty · lead: fold into S2 `demo-sync-target` or the next Activity-5 sync card as checklist items, not standalone work; (f) belongs to whichever card next touches the flag surface · **✅ DISCHARGED-BY-CONVERSION on `sync-doc-honesty` (run 20260901).** Card `sync-doc-honesty` is comment-only footprint (it does not touch the flag surface, the fill-response store, or the client's live-lifecycle code), and B-167's own lead names the destination as "the next sync card as checklist items, not standalone work" — so this card CONVERTS each observation into a durable checklist item rather than mis-scoping a code change into a doc card. All six verified still-live against tonight's tree and re-homed to the two next sync-surface cards. **Per-item disposition (checklist for the receiving card):** (a) `hq_sync_read` URL-override persists to localStorage by design — the accidental-enable vector; verified still true at `sync-rxdb/bootstrap.js:152` (`?hq_sync_read=on` … PERSISTS it). → **flag-surface card** (add a "clears itself once the door is real / never persists in prod" decision + guard when the cutover wires a caller). (b) `overlayKeys()` naming — verified still at `workflows.html:2648`; cosmetic. → **fill-store card** (rename when that store is next edited; no standalone rename). (c) FILL-04 covers the negative half only — no page-level positive teammate-answered-row case (`tests/sync-fill-view.spec.js` drives the OFF vacuity + ON scope, but the teammate-answered-of-the-open-submission positive is G6-manual only). → **`activate-fill-view-reads` successor / fill card** (add the positive E2E when the flag-ON page path is next exercised — needs the ON fixture, out of a comment card's footprint). (d) S1 F-2 "many live at once" is the lifecycle contract while the tap path serializes — precision note; no code change. → **client-lifecycle card** (tighten the doc when `startReplication`'s scope banner is next edited). (e) `RXDB_FILL_RESPONSES` needs a checklist dimension if one template ever has two concurrent fills — a real structural bound, but only bites a not-yet-shipped feature. → **fill-store card** (add the dimension when concurrent fills become reachable). (f) the flag's default-OFF is guarded ONLY by `sync-one-row.spec.js:261` + `sync-fill-view.spec.js:264`; C1's B-88 object-level guard stays green with default forced ON. → **flag-surface card** (`app-slug-association` or the next flag card — add a default-OFF guard that is not defeated by forcing the flag ON, alongside the (a) persistence fix, since both live at the flag). None was in this card's footprint; none is lost — each now names its receiving card.
- **B-168 · BACKLOG.md itself fails `night-crew backlog check` — 296 legacy shape issues across 193 entries** — the store spec (`openspec/specs/backlog-store/spec.md` in the night-crew clone) wants `handle · title — description · origin · status · lead:`; ~40 pre-scheme entries carry no handle/lead, B-77 is assigned twice (any B-77 citation is ambiguous), and rich statuses like B-161's multi-sentence "done — answered GREEN…" don't parse. New entries since the scheme (B-141+) validate; the debt is the old body. Found because morning triage 2026-08-08 ran the mandated check and it exits 1 on the whole document — every triage before it filed into the same failing state without saying so. · _morning triage 2026-08-08_ · promoted → backlog-machine-migration · lead: a one-time mechanical grooming pass (assign handles to the pre-scheme entries, renumber the second B-77, compress prose statuses into the grammar with the prose moved into the description) so the check can gate future edits instead of being permanently red
- **B-169 · `task backend:db-test` bare-invocation falls back to `:5433` (the prod/dev cluster)** — the `backend:db-test` helper defaults its DB coordinates to `:5433` and relies on every caller passing `:5434` explicitly; the four wrapper targets (`test:go`, `test:`, `test:ui`, `test:all`) all do, and Taskfile.yml:120-123 documents the hazard — but a direct `task backend:db-test` (or any new caller that forgets the vars) creates test databases on `yumyums-dev-pg`, the cluster serving hq.yumyums.kitchen (dev AND prod). Surfaced by triage's adversarial reviewer, which invoked it bare and hit `:5433` (failing only because that cluster was down in the review environment; in the real box it is up). B-141/B-143 class (decision 155 — a `:5433` probe destroyed the prod DB on 2026-08-06). Orthogonal to run 20260809's cards (no Go touched). · _morning triage 2026-08-08_ · new · lead: harden `backend:db-test` to refuse or fail loud when handed no explicit DB_PORT (or default it to the `:5434` test cluster) so the safe path is the default, not a convention every caller must remember
- **B-170 · Bare `npx playwright test` in the spike scripts falls through to a foreign PATH `playwright`** — Card 2 (run `20260810`) burned two could-not-run rounds because a fresh worktree's `node_modules/.bin/playwright` symlink was missing (npm's "Exit handler never called" skipped bin-linking), so bare `npx playwright test` resolved to `/Users/jamal/miniconda3/bin/playwright` (no `test` subcommand → `unknown command 'test'`) and BOTH the red-first and armed legs failed identically — a fake asymmetry that reads as a card RED. Card 2's own harness (`sync-app-proof.sh`) is now hardened (resolves `node node_modules/@playwright/test/cli.js` + a preflight `test`-subcommand assertion), but `spike-f-browser-live.sh`, `sync-dev-proof.sh` and the other spike scripts still use bare `npx playwright test` and carry the same vulnerability (they got lucky with a populated `.bin`). Kin to B-165 (bddgen silent-skip) and B-158 (spike-script hardening) — the foreign-binary-on-PATH class. · _run `20260810` card 2 DECISIONS-NEEDED advisory #1 + morning triage 2026-08-09_ · new · lead: apply Card 2's deterministic-CLI-resolution pattern (`node node_modules/@playwright/test/cli.js` + a preflight assertion the resolved binary has a `test` subcommand) across `spike-f-browser-live.sh`, `sync-dev-proof.sh` and the remaining spike scripts, on whichever Activity-3/cutover card next touches them — not standalone work
- **B-171 · One-command `task sync:dev` (data plane + dev server) — PARKED pending a real credential boundary** — a single command to bring the persistent sync data plane up and launch the dev server for `dev-complete-attestation` was designed (scope confirmed by the operator: `sync:dev:up` + `backend:dev:tailscale` + print the URL) but PARKED 2026-08-09. Its only proposed safety gate was a `:5433` coordinate guard / interactive ack, and the operator ruled that detection-at-the-coordinate is the wrong layer: the right system hides prod credentials from the agent so a test/probe cannot reach prod at all, regardless of whether the operation carries a destructive statement (NF-7 enforcement — `design-findings-nightcrew.md` §"Enforcement is capability, not detection"). The command should NOT ship more port-guard machinery; its safety must ride on credential scoping. · _post-triage design session 2026-08-09_ · promoted → sync-dev-one-command · lead: unblock only after the two HIGH prerequisites land (a genuinely separate prod cluster/role + stop `dotenv`-injecting live creds into every task); then build `task sync:dev` inheriting the dev-scoped credential, with prod unreachable by construction rather than by an ack
- **B-172 · Canceled DO Spaces account is invisible — startup logs a false-positive "presigner initialized", health says ok, storage death surfaces only as broken uploads/images** — operator report 2026-08-22: the DigitalOcean account backing Spaces is canceled, yet the HQ app starts with no error anywhere. By construction nothing can notice: `backend/cmd/server/main.go:336-354` checks env-var *presence* only; `photos.NewSpacesClient` (`backend/internal/photos/spaces.go:24-31`) builds an offline S3 client from static creds (no network call); and `NewSpacesPresigner` has no error path at all — so with dead creds startup still logs `DO Spaces presigner initialized` (main.go:350), a false positive. Presigning is local HMAC, so the presign endpoints keep returning 200 URLs whose subsequent browser PUT fails against DO; the receipt worker's Spaces PUT failure only logs + falls back to Mercury URLs (`backend/internal/receipt/worker.go:384`); `/api/v1/health` (main.go:446-456) is a static `ok` with no storage field; and previously stored `digitaloceanspaces.com` public URLs (fail/correction photos, receipt images, onboarding videos) now render as broken media with no in-app surfacing. Data-exposure note: objects that lived only in the canceled bucket may already be unrecoverable — receipts persisted with the Mercury-URL fallback survive; direct Spaces uploads may not. · _operator report 2026-08-22 (found while scoping the Backblaze B2 swap)_ · resolved 2026-08-25 — probe + health field + launcher banner + STORAGE_* rename landed on `feat/b2-storage` 2026-08-22; cutover code landed 2026-08-24 (`9f86f41`: last two x-amz-acl headers dropped with a no-ACL regression test, `POST /inventory/purchases/recover-receipts` Mercury re-fetch endpoint, `STORAGE_REQUIRED` startup gate); buckets `yumyums-hq-dev`/`yumyums-hq-prod` provisioned on `us-east-005` (public, keep-only-last-version lifecycle, per-env CORS via API) and dev smoke green (health `storage:ok`, raw PUT + anonymous GET); `toast/` archive restored to the prod bucket from sales-processor (uploaded=576 date-dirs, 20230825→20260816) — **cutover deployed to prod 2026-08-25** (`211c251`, backend 0.4.0 / frontend 1.5.0, health `storage:ok` through the tunnel); `.env.prod` STORAGE_* filled by operator, `task prod:deploy` clean; `recover-receipts` run 1 recovered 95/95 (0 missing, 0 failed) back to 2026-01-28. Follow-ups spun out: B-173 (unrecoverable onboarding videos + fail/correction photos), B-174 (sw-api-cache test reds), B-175 (worker attachment misindex) · lead: fold into the Backblaze migration rather than patching DO-specific code — the storage layer gets (1) a startup reachability probe (HeadBucket with a short timeout) that logs loud on failure and (2) a `storage: ok|unreachable|unconfigured` field in `/api/v1/health` (surfaced by `task version` and the UI), so a dead account is explicit at boot instead of discovered per-upload; B2 is S3-compatible, so the swap itself is endpoint/region/creds + a stored-URL migration + an inventory of which existing objects are still recoverable
- **B-173 · Onboarding videos + fail/correction photos lost with the DO Spaces account — manual re-upload needed** — the B-172 recovery restores receipt images (Mercury still holds the attachments) and the Toast archive (sales-processor held local copies), but onboarding training videos (`videos/onboarding/…`, URLs in `ob_video_parts.url`/`thumbnail_url`) and workflow fail/correction photos had no second source — their `digitaloceanspaces.com` URLs are permanently dead and render as broken media. Deliberately left un-rewritten by the recovery (the stale URL is the only remaining evidence the object existed). Item catalog photos (`purchase_items.photo_url`, `items/{slug}.png`) can be re-seeded from the Notion export via `cmd/import-notion` if wanted. · _B-172 cutover 2026-08-24_ · promoted → media-recovery · lead: owner re-records/re-uploads the onboarding videos through the app (uploads now land on B2); fail/correction photos are historical and likely not worth recreating — decide whether to null the dead URLs or leave them as evidence
- **B-174 · `tests/sw-api-cache-partition.spec.js` B1-XT-01/-02/-05 fail deterministically on `feat/b2-storage`** — 3 of the spec's tests red on branch HEAD `35e715f` with the full suite otherwise green (814 passed). Verified pre-existing during the B-172 cutover: the same 3 fail identically on the untouched HEAD with zero working-tree changes (stash baseline), so the cutover commit `9f86f41` did not introduce them. Cause not yet diagnosed — candidates: the storage-banner changes to `index.html` + sw regen in `03ea295`/`1a9fd48`, or drift in the device-identity fixtures the spec relies on. Not present in any triage record, which means the branch's earlier commits shipped without a full-suite gate. · _B-172 cutover full-suite gate 2026-08-24_ · new · lead: diagnose on a branch off `dev` before the next release; check whether the same 3 red on `dev`/`main` HEAD to establish where they were introduced
- **B-175 · Receipt worker misindexes attachments after a failed download — ext/filename taken from the wrong attachment** — `runIngestCycle`'s download loop appends to `blobs` while skipping failed downloads, then the upload loop indexes `tx.Attachments[i]` by blob index (`worker.go` download/upload loops) — after any skip, every later blob pairs with the wrong attachment's `FileName`/`URL`, so extensions and Mercury-fallback URLs shift by one. Latent until a multi-attachment tx has a partial download failure. The B-172 recovery path (`recoverOneTx`) deliberately avoids the pattern (all-or-nothing per tx); the live ingest path still carries it. · _B-172 cutover code review 2026-08-24_ · promoted → receipt-worker-correctness · lead: track the attachment alongside its blob (single struct) instead of parallel slices; small fix + a worker test with one failing download among three attachments
- **B-176 · `tests/workflows.spec.js` [DBL-05] ("a queue entry from an earlier period does not lend its key to today") fails deterministically on clean `dev` HEAD** — red in the item-aliases full-suite gate 2026-08-28 (820 passed / 4 failed; the other 3 are B-174's known trio), then verified pre-existing: fails identically, including on retry, with the working tree stashed to untouched `dev` HEAD (`5e33f1a`). Assertion at `workflows.spec.js:1244` — offline-sync queue-key surface; no inventory/receipt file is in its path, so the aliases change cannot be the cause. Not present in any triage record — same class as B-174 (a red that shipped without a full-suite gate noticing). · _item-aliases full-suite gate 2026-08-28, stash-baseline reproduction_ · new · lead: diagnose on a branch off `dev`; first establish which commit introduced it (bisect against the DBL-05 spec, which landed green with `56a913d` in the sync-foundation cycle)
- **B-177 · `parseEventDate`'s PARSEABLE path stamps `event_date` from the timestamp's own zone (UTC), not the business zone — a parseable boundary receipt lands in the wrong COGS week** — surfaced by G6 on card `receipt-worker-correctness` (run 20260901). Card 1 fixed B-28's *unparseable fallback* (server-local → `users.DefaultTimezone`, red-first) but the parseable loop still does `return t.Format("2006-01-02")` with no `.In(loc)` (`backend/internal/receipt/worker.go:1100`): an RFC3339 UTC `CreatedAt` like `2026-09-01T02:00:00Z` (= 2026-08-31 22:00 America/New_York) stamps `event_date=2026-09-01`, and `event_date` WINS the COALESCE in `pendingPeriodDateExpr` (`inventory/handler.go:43`) so the zone-aware `created_at AT TIME ZONE` cast never runs on it → the purchase is filed to the adjacent period. This is the COMMON path (Mercury `CreatedAt` is normally parseable), but it is **pre-existing** (Card 1 did not change that line) and **entangled with the documented Chicago→NY changeover** (`handler.go:34-42` + migration `0072_app_timezone_new_york.sql`), whose comment asserts the boundary risk is bounded to rows with NO `event_date` — G6's finding is that the parseable-path `event_date` also carries it. Deliberately WAIVED from Card 1 (out of B-28's defined scope; changing a payroll path needs its own analysis against the changeover). NOTE: `TestParseEventDate_ParseableInputs_UnchangedAndSilent` (added by Card 1) currently ASSERTS the un-converted behavior — it must be revisited if this is fixed. · _G6 adversarial review, run 20260901, card receipt-worker-correctness_ · new · lead: decide whether `event_date` should be the business-zone date of `CreatedAt` (add `.In(loc)` to the parseable branch + a boundary-timestamp red-first test, and flip the Unchanged test) — SCOPE ALONGSIDE the 0072 changeover so the two zone decisions land coherently; confirm whether event_date's true source in the main flow is the receipt-content parser (business-local) vs parseEventDate(CreatedAt) before changing behavior
- **B-178 · `internal/sync/TestJWTBridgeRLS` RLS fixture is not isolated from the live Spike C relay's writes — 13 `spikec-*` rows contaminate the CONTROL assertion** — the JWT-bridge RLS suite seeds 4 fixture checklists (`chk-alice-*`/`chk-bob-*`) and its CONTROL subtest asserts service_role sees EXACTLY those; on run 20260901 it saw those 4 PLUS 13 `spikec-<uuid>` rows (all owner `d4b01f0a-bd5c-4b3a-aaa9-53e98678a10c`, `app_slug=operations`) written by the persistent Spike C relay (`.night-crew/qa/spike-supabase/.persistent-dev/spikec-relay`, pid 31802, connected to the live `100.70.200.55:5433` substrate over Tailscale). Deterministic, reproduces on base `0bfd1af` with zero concurrency; no `:5434` database persistently holds `hq_sync_checklists`, so the rows enter the test substrate via federation/seed from the relay's live writes at setup time. `internal/sync` is the ONLY red package — `internal/{receipt,inventory,recipes,workflow,auth,alerts,onboarding,photos,purchasing,toast}` all `ok`. NOT cleanable from an overnight run: the rows trace to `:5433` (dev AND production — hard-forbidden per B-141/B-143/decision 155), and stopping the relay would not remove already-written rows. Same family as the un-merged `gate-rls-fixture-ownership` (decision 155). Treated as a known-environmental red for run 20260901 (card gates judged as footprint-green + no NEW failures beyond the armed Playwright baseline + this). · _run 20260901 orchestrator diagnosis (Card 1 G2-Go red traced to relay contamination)_ · new · lead: isolate the RLS fixture from the live substrate (seed only the 4 fixtures; do not federate/copy live `hq_sync_checklists` into the test DB, OR filter the CONTROL assertion to the fixture's own owner ids), AND have the operator stop the Spike C relay + clean its `spikec-*` rows from the `:5433` sync substrate before the next sync-touching run; fold into `gate-rls-fixture-ownership`
- **B-179 · `node_modules` `workbox-build@7.3.0` drifts from `package-lock.json`'s pinned `7.4.1` — a `build-sw.js` regen can re-emit the workbox runtime chunk under a different name** — surfaced across run 20260901 (Cards 4/7/11): on this box the installed `workbox-build` is 7.3.0 while the lockfile pins 7.4.1, so a fresh `npm ci` would swap the workbox runtime chunk `build-sw.js` embeds. Card 11's G6 and Card 7's implementer both observed a regen emitting `workbox-0225851e`→`workbox-d4a0f5c1` (byte-identical but for the sourceMappingURL comment); each correctly KEPT the committed `0225851e` chunk rather than committing a runtime swap. On the final merged tree the regen was idempotent (zero delta) because the box's node_modules matches the committed chunk — but a fresh clone / CI running `npm ci` would regenerate against 7.4.1 and produce a different committed sw.js than dev, so `task prod:deploy`'s committed-sw.js-ships model could carry a chunk dev never validated. NOT a shipping defect tonight (committed sw.js is reproducible on the dev box; precache count 31 throughout). · _run 20260901 orchestrator + Cards 4/7/11 G6_ · new · lead: `npm ci` the dev box to 7.4.1 (attended), regenerate sw.js once, commit the 7.4.1 chunk deliberately, and confirm precache stays 31 — OR pin workbox-build to 7.3.0 in package.json to match the committed chunk; decide which version is authoritative, then make node_modules + lockfile + committed sw.js agree
- **B-348 · `main` carries 3 deploy-tooling commits never back-merged to `dev`** — the triage worktree sweep (2026-09-01, `night-crew worktrees check`) found `feat: add prod backend Docker stack with task wrappers` (`b89c202`), `fix(deploy): hard-sync prod clone to origin/main instead of ff-pull` (`572f370`), and `fix(deploy): pass GIT_SHA/BUILT_AT build-args so prod reports its commit` (`4cd81c3`) present on `main` but patch-absent from `dev` (`git cherry`). Pre-existing (predates run 20260901) and low severity — `main` already carries them so prod is unaffected — but a deploy-tooling change started from `dev` begins without them, and the PLANNED `ship-dev-to-main` promotion must reconcile rather than assume `dev` is a superset of `main`. Orthogonal to run 20260901 (the run branched from `dev` and touched none of these). · _morning triage 2026-09-01_ · new · lead: before/at `ship-dev-to-main`, diff `dev`↔`main` deploy tooling (Taskfile prod targets, `backend/Dockerfile` build-args, prod compose) and confirm `dev` already carries equivalent logic OR cherry-pick these 3 back to `dev`, so the promotion merge reconciles intent rather than silently dropping main-only fixes
