# Slate — run `20260904` (night of 2026-09-03 → morning 2026-09-04)

**Run branch:** `overnight-20260904` — the bare date `20260904` is free (no branch of any
suffix exists; no unmerged run branch of any date — run `20260901` merged at triage T-47).
No collision, no suffix.
**Sign-off:** GRANTED 2026-09-04 (operator, this session) — "Yes — sign it" at the §4 batch
sign-off, with the milestone remainder, attention budget, and spike-skips sections in view.
4 cards. **Dispatch: SERIAL, one lane** — chosen by the operator at the §5 question with
both projections side by side (concurrent: same 4 cards, ~2¼–3h, ~2–3× burn while tracks
overlap; serial: same 4 cards, ~3¾–5¼h, lowest burn). Coverage identical; the operator
chose the lower burn rate. Order: 1→2→3→4.
**Forks resolved:** **#5 (`requires_online` face-value threshold) — resolved this sitting,
operator's own answer: "Make $ amount configurable."** The schema card therefore carries a
settings surface (`marketing_settings.requires_online_threshold_cents`, single-row, seeded
default **$20.00**) instead of a hardcoded policy; per-campaign `requires_online` stays a
column (as designed §4/§8) whose value derives from face value vs the threshold at campaign
creation, and the threshold changes without a migration. The seeded default is an
engineering call, stated: $20 keeps typical $5–15 free-item welcome offers overridable
offline. Recorded in the roadmap's open-decisions table in this commit. **#8
(welcome-offer definition + expiry window) — scoping call, stated:** tonight seeds *test*
campaigns only (one `requires_online = true`, one `false` — the RLS and race tests need
both); the production welcome-offer definition lands at Activity E where the roadmap also
homes it. **#11 (campaign-admin half)** stays open, homed at Activity A/D by the roadmap —
it does not touch tonight's schema (the arbiter is Supabase either way, R2). No other card
carries an operator question: Activity G's two cards were fork-checked at the roadmap round
(carried whole from last cycle) and their spikes settled the remaining unknowns.
**Workflow-preflight verdict (ran clean — `night-crew workflow preflight --repo .`, exit 0):**
`openspec: absent` (no `openspec/` tree). `gsd: detected` (`.planning/` present — the
deliberate telltale; real state is `.night-crew/knowledge/`). **The launch prompt asserts
UNIVERSAL mechanics only** — red-first evidence, atomic commits, the `Night-Crew-Run:
20260904` trailer, the roadmap/status flip — and creates **no** OpenSpec scaffolding. This
is NOT the `undetermined` STOP case: the verb read every path and returned `absent` at
exit 0, matching every prior run in this repo.
**Spike gate (per-goal, §1.4):** the `night-crew spikes gate` verb cannot drive on this
product-mode target — it reads the dogfood-only `usm/roadmap.txt` story map (exit 1, "no
story map to gate against"), the same wall every slate since `20260810` records and the
convention the spike ledgers themselves state (per the `activity-5-dev-complete`
precedent). **The gate's substance was verified from the artifacts it would read:**
- **No orphan cards:** Activities A and G each carry exactly their two goal cards — the
  goals ARE the cards, 1:1; no card of either activity sits outside a goal.
- **Every goal has exit-zero spikes:** 7/7 spikes `passed` across the 4 goals
  (`supabase-schema-and-rls` ×3, `redeem-rpc-race-proof` ×1 five-leg,
  `backlog-machine-migration` ×2, `team-records-from-hand-runs` ×1), hand-run 2026-09-03.
- **Corrections reviewed and signed:** operator batch sign-off 2026-09-03 ("Sign off all
  three") covering the 4 agent-reached corrections; `team-records-from-hand-runs` had 0
  corrections so no review was owed.
- **Extraction records written and valid:** 4/4 pass `night-crew spikes record-check
  --record <path>` (the verb's direct-path form, which needs no story map).
**Spike skips: none** — every authored spike ran to exit 0; no skip was recorded for any
goal. Stated so an absent section and a considered "nothing skipped" don't look alike.
**Agreement check (§3b — `night-crew okr compare-slate … --expect-cards 4`, exit 0):** all 4
declared cards read by the parser; no contradiction of the three kinds it reads (opposed
disposition, delivery denied, a slate-excluded card a target claims). Coverage lines — "15
targets name no card", "4 slate cards no target names" — are **structural to a target-repo
OKR page** (each KR names its measurement *artifact*, not a card slug; same shape
slate-20260901 recorded). Read substantively the alignment is direct: **E-KR1 ↔ Card 2**
(the race proof is the KR, verbatim), **D-KR3/P-KR2's schema halves ↔ Card 1** (both
measured by `supabase/`), **Q-KR3 ↔ Card 3** and **Q-KR4 ↔ Card 4** (verbatim, the two
carried KRs). The check's live value was the `--expect-cards 4` parse assertion and the
three contradiction detectors (none fired).
**Precondition flags:** standing armed reds untouched by tonight's footprints — Playwright
baseline **B-174** (×3) + **B-176** (×1) (no card touches app code, so no Playwright leg
runs); **B-178** sync-RLS environmental red (the Spike C relay → live `:5433`) — no card
runs `internal/sync` tests; the attended relay cleanup stays recommended, unowed by this
run. Supabase substrate images are already local (the spikes ran 2026-09-03). No attended
sandbox re-arm is owed.
**Prod safety (decision 155's habit, applied to the new arbiter):** Cards 1–2 run against
the committed LOCAL substrate only (`.night-crew/qa/spike-supabase/`, brought up via
`env-up.sh` in reconcile mode — never `--fresh`); each prints its resolved target
coordinates read-only before any write. Never `:5433`, never a hosted Supabase project
(none exists yet — Activity 0 creates it, attended).

## What this night is

**The attribution spine gets built, and the planning surface stops lying.** Activity A
turns the spike-proven drafts into the in-repo `supabase/` migration + the race-proof
`redeem()` the whole cycle rides on — the single conditional UPDATE that is the only thing
enforcing single use (§6, E-KR1). Activity G retires the two KRs that reddened last
cycle's close through no fault of their own: the backlog becomes machine-true (Q-KR3) and
the scorecard finally sees the four roles (Q-KR4). Both activities are disjoint-footprint
by design — the roadmap authored G as overnight-parallel with everything.

Spike leverage is the sizing story tonight: all four cards inherit proven fixtures
(`sql/qr-schema.sql`, `sql/redeem-fns.sql`, the reshape loop, the scorecard JSONL shape) —
these are re-export-class cards more than from-scratch builds, priced accordingly.

## Shared surfaces and serialization

| Surface | Cards | Rule |
|---|---|---|
| `supabase/` (NEW dir, in-repo migrations) | Cards 1, 2 | same track (A), serial; slate order = landing order (Card 2 applies Card 1's migration before creating functions) |
| `.night-crew/qa/spike-supabase/` env harness | Cards 1, 2 (read/reuse) | reconcile mode only, never `--fresh`; edits not expected — same-track serial covers any |
| `.night-crew/knowledge/BACKLOG.md` | Card 3 only | exclusive; iterate on a scratch copy via `check --file`, land once green |
| `.night-crew/knowledge/scorecard/` + closeout template | Card 4 only | exclusive |
| Hand-run ritual docs (`COMMANDS.md` / triage + closeout ritual text) | Cards 3 (arms the triage §4.5 gate) and 4 (adds the closeout scorecard step) | cross-track, bounded — different ritual moments; resolve any collision at merge against both merge-intents (§15ad.65); log every merge in the conflict log |
| App code / `sw.js` / `night-crew.toml` seams | **none** | no card touches an `[e2e.seams]` key or any precached asset — no Playwright leg is owed; gates are per-card standalone harnesses (the B-345 precedent) and `n/a — no code change` where docs-only |

## Model routing (§3c — class-derived, no per-card override, process/C-7)

A card needing a different tier is **reclassified here**, not exempted. All classes route
to the **session model** tonight: Cards 1–2 are the cycle's arbiter (the most load-bearing
mechanism of the milestone), Card 3 mass-rewrites the planning surface of record under
content-preservation proofs, and Card 4 is small but too cheap for a downgrade to buy
anything. (Sizing classes per `card-actuals.md` live in the estimates table's Basis
column; they price, this section routes.)

| Class | Model | Cards |
|---|---|---|
| new-mechanism | session | supabase-schema-and-rls, redeem-rpc-race-proof |
| followup-batch | session | backlog-machine-migration |
| small-additive | session | team-records-from-hand-runs |
| G6 adversarial review | session | all cards, always (standing row — not a per-card class route) |

<!-- routing:slate -->
new-mechanism session
followup-batch session
small-additive session
supabase-schema-and-rls new-mechanism
redeem-rpc-race-proof new-mechanism
backlog-machine-migration followup-batch
team-records-from-hand-runs small-additive
<!-- /routing:slate -->

## Estimates and critical path (wall-clock ONLY)

🛑 **Sized against the calendar and nothing else.** No `--night` was given, so there is no
cut line — these estimates exist for the attention budget, the dispatch projection, and
the stretch discipline. The account's token quota — tier, ceiling, current consumption,
reset window — was **NOT measured and cannot be measured from here.** The run may exhaust
quota before the clock or the clock before quota; neither validates the other. Spend is
governed by the run's own stop conditions; a rate-limit stall degrades gracefully (finish
the in-flight card, closeout; under concurrent dispatch, degrade to serial — never sit
blocked).

End-to-end = implement + G6 + any fix round + merge. Basis: `reference/card-actuals.md`.

| # | Card | Track | Routing class | Estimate | Basis (sizing class per card-actuals) |
|---|---|---|---|---|---|
| 1 | `supabase-schema-and-rls` | A | new-mechanism | **40–70m** | re-export class (~21–25m, run 20260809 — "price against the harness it wraps": the spike fixture + named-object assertions ARE the working draft) pulls down; NEW in-repo surface (`supabase/` layout, the #5 settings table, seed policy) + G6 repriced 15–45m for code cards pulls up |
| 2 | `redeem-rpc-race-proof` | A | new-mechanism | **35–60m** | same re-export logic (`redeem-fns.sql` v2 + the 5-leg race script are proven); adds the GAP-1 re-validation run + `validated:` ledger line; race rounds cost real but small wall-clock (spike ran them in minutes) |
| 3 | `backlog-machine-migration` | G1 | followup-batch | **60–110m — risk-flagged, use 110** | no at-scale analog (first doc-migration card); 20260901's followup-batch (`sync-doc-honesty`, ~40m) pulls down; ~297 issues / ~207 entries across ≥3 defect classes (the spike proved the loop on 1 entry of 1 class), whole-doc token-multiset proof, SIGPIPE-safe tooling requirement pull up |
| 4 | `team-records-from-hand-runs` | G2 | small-additive | **20–40m** | QA/harness class ~21–25m actuals (run 20260809); template + ritual step + emitting tonight's own run record at closeout |

**Critical path:**
- **Concurrent (3 tracks: A = 1→2 serial · G1 = 3 · G2 = 4):** longest track is A ≈
  75–130m vs G1's 60–110m; + per-merge minutes + ~30m closeout → **mid ≈ 2h15m, high ≈
  3h05m**. Burn ≈ up to 3× serial while three tracks overlap (briefly — G2 finishes fast).
- **Serial (one lane, order 1→2→3→4):** sum ≈ 155–280m + ~30m closeout → **mid ≈ 3h40m,
  high ≈ 5h10m**.
- **The card lists are IDENTICAL under both modes** — all four cards fit either way with
  margin; the choice is burn rate vs wall-clock, not coverage. No stretch tier exists
  tonight (nothing is cut).

## Track A — the attribution spine (Wave 0: none; nothing cross-cutting precedes)

### Card 1 · `supabase-schema-and-rls` — the arbiter's schema, RLS, publication, and the #5 settings surface

Turns the spike's proven fixture into the in-repo migration the cycle builds on. Create
`supabase/` (migrations + README stating apply order and target discipline), starting from
`.night-crew/spikes/activity-a-attribution-spine/supabase-schema-and-rls/sql/qr-schema.sql`
(the candidate seed — the card's own design pass decides what to adopt, NFR-6): the
`campaigns` / `codes` / `scan_attempts` schema (§4, incl. F2's `unverified_code boolean`),
`token_hash` never storing the raw token, the `codes(updated_at)` checkpoint index, the
`(pos_business_date, pos_order_number)` join-key index, RLS per the spike-proven shape
(SELECT-for-authenticated on campaigns/codes; INSERT-only on `scan_attempts` with
`with check (device_id = jwt sub)`, no SELECT — push-only made structural), and
`public.codes` in the `supabase_realtime` publication. **Plus the #5 resolution:** a
single-row `marketing_settings` table carrying `requires_online_threshold_cents`
(seeded **2000**), documented as the value campaign-creation consults to derive
`requires_online` — configurable without a migration. Seeds two *test* campaigns (one
`requires_online = true`, one `false`) + codes for Card 2 and the RLS legs (#8's
production welcome-offer stays at Activity E). **Red-first (greenfield):** the named-object
assertion harness runs RED against a bare substrate (objects don't exist), then GREEN
after the migration applies — twice, fresh and warm. done_when: migration applies clean
twice; every structural claim enumerated by name (the spike's assertion set + the settings
table); the RLS six-leg discrimination holds through PostgREST; a server-side UPDATE
arrives at a second authenticated Realtime subscriber inside the window. Footprint:
supabase arbiter (`supabase/` NEW + spike harness read-only). **PARK note (narrow):**
needing a new operator-facing policy concept beyond the configurable threshold (a campaign
category, a new permission meaning) → park. Table layout, index shape, seed values,
directory layout are the night's to decide and record in the merge-intent.

### Card 2 · `redeem-rpc-race-proof` — the atomic arbiter, race-proven, GAP-1 closed

Builds `redeem(p_code, p_device)` into the `supabase/` migration **with the v2 body**
(operator-signed at the 2026-09-03 batch review): explicit `already_used` / `expired`
arms, `coalesce(…, 'not_found')` — never the handoff §6 text verbatim, whose NULL reason
for a nonexistent code would make a forged code read as a system outage downstream (§18
edge-case 3). Candidate source:
`.night-crew/spikes/activity-a-attribution-spine/redeem-rpc-race-proof/sql/redeem-fns.sql`.
Ships the race test as a repeatable harness beside the migration: 20 rounds × 2 concurrent
clients (each its own connection — the spike's `docker exec psql` pattern), exactly one
`ok=true` per round, 0 double-wins, every loser `already_used`; **red-first** against the
naive check-then-update analog (proven to double-win on demand); the expired leg and the
`not_found` leg ride along. **Closes GAP-1:** runs the spike (or its successor harness)
once against the BUILT migration and appends the `validated:` line naming GAP-1 to the
spike ledger's `## Comebacks` — in this card's sitting, not later. done_when: race test
passes 20× with 0 double-wins against the in-repo migration; `not_found` returned for an
unknown uuid; GAP-1's `validated:` line recorded. Footprint: supabase arbiter (extends
Card 1's migration — serial after it). **PARK note (narrow):** needing a redemption
reason or policy beyond the §9/§19 taxonomy (`not_found` / `already_used` / `expired`),
or a change to what the atomic UPDATE enforces → park. Harness shape, round counts,
timing, SQL organization are the night's.

## Track G1 — planning surface honest

### Card 3 · `backlog-machine-migration` — ~207 entries to canonical form, content-preserved, gate armed

Closes **B-02**, **B-168**, **B-12**, **B-133** (Q-KR3's producer, carried two cycles).
Reshape the legacy-shape entries until `night-crew backlog check --repo .` exits 0,
working the spike-proven loop: iterate on a **scratch copy** via `check --file <copy>`
(instrument proven to read the copy; the real document's sha only moves at the final
land), reshape mechanically per defect class — fold-don't-delete proven for the "extra
segment" class (the largest); the other classes ("missing origin", "missing
plain-language lead") get per-class treatment this card designs, each preserving every
token. **Content preservation is proven mechanically:** token-multiset containment over
the WHOLE document (every alphanumeric token present before is present after), recorded
in the merge-intent as Q-KR3 requires. Handles assigned above the current max (B-39→B-44
collisions have happened). **done_when (re-anchored by the spike):** `check` exit 0 AND
`backlog list` count == the checker's own "across M entries" parse (baseline pinned:
297 issues / 207 entries vs list's 208 — anchor on the checker, never a hand grep) AND
the whole-doc preservation proof green. Then **arm the triage §4.5 gate** so the document
cannot drift back (ritual step + check invocation at triage). **Tooling note (spike
build-fact):** the document's multi-KB single-line entries SIGPIPE any early-exit pipe
consumer under `pipefail` — full-readers only. Footprint: planning docs (BACKLOG.md
exclusive + triage ritual text). **PARK note (narrow):** an entry whose reshape cannot
preserve meaning mechanically (the reshape would change what the entry says) with no
citation-floor answer → park with the entry's handle — NEVER delete or paraphrase content
to green the checker. A defect class seeming to need a new canonical form the rubric
doesn't define → route through `night-crew decisions log`; act on the printed verdict.

## Track G2 — planning surface honest (disjoint)

### Card 4 · `team-records-from-hand-runs` — the scorecard sees the four roles

Q-KR4's producer, carried two cycles. The spike settled the OR-arm: the installed CLI
(v3.4.0+1) **already reads** hand-authored per-run records — no CLI change needed; this is
pure target-side ritual work. Ship: (1) the record **template** (candidate = the spike's
fixture: one `{"schema":1,"kind":"run-scorecard",…}` line + one `{"kind":"team",…}` line
per role — product / delivery / engineering / qa) committed where the ritual can find it;
(2) the **closeout-ritual step** that emits `.night-crew/knowledge/scorecard/<run-id>.jsonl`
per hand-run night, written into the closeout template/ritual docs; (3) **first consumer
proves the producer:** THIS run's closeout emits `20260904.jsonl` using the new template,
so `night-crew scorecard` renders record-backed rows for all four roles tomorrow morning.
Roster note: no `team.toml` is required (spike-proven); seeding one is optional build
detail the card may skip. **Red-first analog (docs/ritual card):** the Q-KR4 red is
already pinned by the spike (real repo renders "No runs to show."); the card records
`n/a — no code change` + cites that pinned red as its before-state. done_when: template +
ritual step landed; tonight's closeout record renders all four roles record-backed under
`night-crew scorecard --repo .`. Footprint: planning docs (scorecard/ + closeout ritual
text). **PARK note:** none expected — the spike settled the mechanism; if rendering
provably fails against the built template, that finding is the card's recorded OR-arm
outcome (filed clone-side, target half closed), not a park.

## Explicitly NOT in this slate

- **Activity 0, both cards** (`redemption-unknowns-spike`, `external-accounts-provision`) —
  **attended by design** (Toast field observation during service; accounts/billing are the
  operator's). Tonight's Activity A lands against the LOCAL substrate; applying the
  migration to the hosted project is a ~15m attended follow-up once Activity 0 provisions
  it. The A2P/10DLC registration inside `external-accounts-provision` has a 1–3 week
  external lead and gates Activity E — worth scheduling soon.
- **Activities B–F** — build order, and the spike-gate discipline: B is gated on Activity
  0's #6 field observation (it may shrink B outright); C opens with its own operator-called
  spike (XState vs hand-rolled); D–F sit behind A→B→C. None has an exit-zero spike goal,
  so none may be slated tonight.
- **`media-recovery` (B-173)** — open, unrelated to this cycle, carries forward.
- **The 46 CLI-visible `[new]` backlog items** — deliberately unwalked (the roadmap
  round's scope call). The dedicated backlog walk becomes *possible* after Card 3 makes
  the legacy entries machine-visible — a natural next attended sitting, not tonight's.
- **Backlog promotions: none** — nothing currently blocks future architecture (§15k bar);
  the backlog's destiny is the walk after Card 3, then the next roadmap round.

## Milestone remainder (live roadmap state, read this sitting)

Tonight closes **4 of 17** roadmap cards. Still white after a clean night — **13 cards**:
- Activity 0 ×2: `redemption-unknowns-spike`, `external-accounts-provision` (both attended)
- Activity B ×3: `rxdb-pull-replica`, `scan-attempts-push-conflict`, `clock-offset-on-sync`
- Activity C ×3: `marketing-tile-and-page`, `camera-scanner-decode`, `redemption-submit-flow`
- Activity D ×1: `gstate-arbitration-machine`
- Activity E ×2: `identity-code-and-qr`, `mms-send-on-signup`
- Activity F ×2: `smtp-toast-ingest`, `reconciliation-view`

The slate leaves the milestone 13 cards short, plus the close bar's three attended legs
(P-KR1) — expected: this is the cycle's first build night and the spine comes first.
**Recommended additions: nothing worth adding** — every remainder card is attended-by-design,
gated on Activity 0's answers, opens with its own spike, or sits behind un-spiked goals the
gate discipline refuses.

## Execution notes

- **Single-session serial order:** 1 → 2 → 3 → 4 (Track A first — the spine is the cycle's
  point; G's cards are independent and safe anywhere in the order).
- **Two-session split:** session 1 = Track A (1→2); session 2 = Track G (3, 4).
- **Concurrent:** Track A (1→2 serial within), G1 (3), G2 (4) — at most one in-flight card
  per track; orchestrator alone merges, in slate order.
- Run mechanics inherited by reference from `reference/overnight-run-plan-20260707.md`
  (standing rules, gates G1–G6) — not restated here. Gate legs adapt as the shared-surfaces
  table notes: no Playwright is owed; Cards 1–2 gate on their own harnesses' exit codes
  (B-345 precedent), Cards 3–4 on the CLI verbs' exit codes + preservation proofs.
- Commit trailer: `Night-Crew-Run: 20260904` (+ the card's slug in the body), every commit.
- Merge-intents at `.night-crew/runs/2026-09-04-autonomous/merge-intents/<card-slug>.md`,
  first commit of every card.
