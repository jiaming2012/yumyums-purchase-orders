Tonight's autonomous run. Execute the signed slate at
.night-crew/knowledge/reference/slate-20260804.md.

Setup: you are on branch dev.

Before any git write, CHECK FOR UNMERGED RUN BRANCHES. Run this EXACTLY as written — it contains
no placeholder to substitute, and it asks about every date, because an unfinished night from any
date is a night's work sitting unmerged:

    git branch --no-merged dev --format='%(refname:short)' | grep -E '^overnight-[0-9]{8}(-[0-9]+)?$'

STATE THE RESULT OUT LOUD BEFORE PROCEEDING — either "no unmerged run branches" or the list. You
may only proceed after saying which one you saw. An empty result is the "go" signal, so a command
that failed or ran wrong looks exactly like a clean repo unless you have named what you found.

- No unmerged run branch → say so, then cut this run's branch. Compute its name from the BARE
  DATE, since you are looking for every branch of that date:

      git branch --format='%(refname:short)' | grep -E '^overnight-20260804(-[0-9]+)?$' | sort -V

  No match → overnight-20260804. Otherwise the next free suffix (-2, -3, …). A MERGED branch's
  name is still taken, so a date whose run merged this morning yields -2 — cut it and proceed
  without asking; nothing is at risk.
- ANY unmerged run branch exists → 🛑 STOP. A previous night's work is sitting on it. Report what
  is there (git log --oneline dev..<branch>, and whether DECISIONS-NEEDED.md carries an open
  section for it), then ASK THE OPERATOR to choose: RESUME that branch, or RECUT at the next free
  suffix. Do NOT pick for them — resuming mixes two nights on one branch, recutting walks away
  from a night's work, and you cannot tell from here which is right. This is NOT covered by the
  batch sign-off: the yes is scoped to the signed specs, and what to do with an unfinished night
  is not one of them. Whatever they answer, the existing branch is left untouched — never delete,
  reset, force, or rename it.

If the run id gains a suffix, EVERY run-scoped name takes it with them: the conflict log
.night-crew/knowledge/reference/conflicts-<runid>.md, and the closeout's references to the run.
The slate keeps the name it was signed under.

🛑 `night-crew run-evidence check` is BLIND in this repo (B-77) and will report `no-run-evidence`
for 20260804 even after this night has run and committed. Do not treat that as evidence the night
did not happen, and do not offer to re-run a slate on the strength of it. Same root cause makes
`night-crew okr grade` and `milestone export` blind here — do not quote them.

Do all work on the run branch. Never push, never touch main. Hard prohibitions in CLAUDE.md apply.

Authority: batch sign-off was given by the operator on 2026-08-03 — the slate document is the
record. Do NOT pause for per-change sign-off. Every operator fork is already resolved: F-1/F-2/F-3
in ledger §T-32 (decisions 126–128), and tonight's own calls in ledger §T-33 (decisions 132–134).
You never choose a fork — what to build, what a milestone means, anything only the operator can
settle.

Park is NOT the default answer to a gray area. Route it through `night-crew decisions log` and act
on the verdict it PRINTED, never on an inference from the disposition. Two verdicts, four producing
states (CLAUDE.md §"Slate workflow" step 5 is the doctrine of record):
  - `verdict: decide` under an adopted delegation policy — recorded delegated,
    `ratification: pending`, back at triage. Deciding is not settling.
  - `verdict: decide` on the citation floor with NO policy involved — owes no ratification. You
    will meet this; do not read `decide` as "a policy covered it".
  - `verdict: park` with the decree read — prints the verdict and exits 0. An escalation is a
    correct outcome, not a command failure.
  - park because the decree could not be READ — fails, exits non-zero, renders `park:` inside its
    error, and prints NO `verdict:` line at all. Grepping for `verdict: park` misses this one;
    read the exit code.
An escalation and a park are the same act, which is why there are two verdicts and not three. A
delegation converts a question that WOULD have escalated into one that resolves; it is not the only
way a question resolves — never restate this as "decide only under a policy, otherwise park".

Rules of engagement:

- Standing rules and verification gates G1–G6 are inherited unchanged from
  reference/overnight-run-plan-20260707.md.
- 🛑 export PATH="/usr/local/go/bin:$PATH" before ANY Go or Playwright leg. The non-interactive
  shell does not carry Go; Playwright's webServer dies with `go: not found` / exit 127, which LOOKS
  like a test failure and is not. Postgres is on :5433. DB credentials on this box are
  yumyums:yumyums, NOT postgres:postgres — the 20260803 run lost a 15-minute gate to that. Go
  suite: `go test ./... -p 1` from `backend/` (the module root — `./...` from the repo root matches
  NO module and prints a false green if you pipe it into `tail`). Set DB_TEST_URL or the Go suite
  exits 0 while skipping every DB-coupled test: check COUNTS, not just `ok`.
- Dispatch model: you are the control loop, not the implementer. For each card: create a worktree
  off overnight-20260804, spawn a FRESH subagent scoped to exactly that card (paste its slate
  entry, footprint, and spec references into the subagent prompt; it drafts, implements, runs
  G1–G4 in its worktree, and returns a compact report), then spawn a SEPARATE fresh subagent for
  the G6 adversarial review, then merge to the run branch and archive yourself. Never implement a
  card in your own context; your context holds reports, not diffs. Subagents cannot spawn
  subagents — G6 dispatch is always yours. A failed card PARKS per the slate rules (worktree
  preserved, DECISIONS-NEEDED.md) — dispatch the next card. Editing a file outside the card's
  footprint is NOT a park (§15ad.65) — see the conflict rules below.
- Order: SERIAL, one card at a time, in slate order.
  **A1 `e2e-gate-database-isolation` strictly first and alone.** Its acceptance test: a SUBSET
  invocation of the Playwright suite demonstrably resets the e2e database — red-first, showing the
  current subset path leaving rows behind before showing it clean. Every later card's gate depends
  on A1's result, so do not begin A2 until A1 has merged or parked.
  Then A2 `workflows-autosavefield-phantom`, then A4 `offline-ownership-design-note`, then — ONLY
  if all three landed clean and 45–75m plus the 30m closeout still fit — the stretch card A6
  `app-version-badge`.
- Per-change mechanics — the slate's recorded workflow-preflight verdict for this repo is
  `openspec: absent`. Do NOT assert OpenSpec mechanics and do NOT create openspec/ scaffolding in
  this repo. Assert only the universal mechanics: red-first evidence, atomic commits, a
  change-identifying commit trailer on every commit, and the roadmap/status flip in the same change
  set. Which per-change discipline this repo should adopt is an operator question (B-105) — never
  encode another detected convention's mechanics in its place. G3 is therefore N/A; re-confirm the
  verdict at launch rather than inheriting it.
- Merge-intent note (REQUIRED, §15ad.65): each subagent writes its merge-intent note in its FIRST
  commit, before implementing, at
  `.night-crew/runs/2026-08-04-autonomous/merge-intents/merge-intent-<card>.md` — shared files
  touched (each file outside its own packages, one line of why), what must survive any merge, and
  what is safe to drop. Empty fields say "nothing here" explicitly. Paste this requirement into
  every implementer prompt.
- 🛑 NEW THIS RUN — the merge-intent note MUST carry a `## Red-first` section (reworded Q-KR2,
  ledger T-33 decision 132): the named test, the tree or commit it was captured RED against, and
  the green after. A card whose deliverable is documentation or an audit — A4 is one — writes
  `n/a — no code change` explicitly. An ABSENT section is a defect, not a pass; "no section" and
  "not applicable" must not look alike. This is the first run under the reworded KR.
- 🛑 Cite diffs against a base that does not move underneath you (T-32 decision 130a). A
  merge-intent or closeout claiming "no production code" must cite a diff against the card's own
  base commit, not against the run branch HEAD — a later card's merge makes the latter
  unreproducible at triage, and these SHAs get quoted.
- Backlog numbers are allocated UP FRONT: A1 closes B-76, A2 closes B-65, A4 opens nothing by
  default. Any NEW finding takes B-80 onward, in dispatch order. Do not let a card guess a number.
- Conflicts, not fences (§15ad.65): the footprint is planning information, not a boundary — a card
  MAY edit any file, and doing so is not a breach and not a park. Collisions surface as ordinary
  git conflicts. YOU resolve them at merge by reading BOTH sides' merge-intent.md and resolving
  against intent, not text; then re-run G1+G2. PARK the later card ONLY when, having read both
  intents, you still cannot determine which behavior is correct. An unplanned lifecycle row, a
  change to the escalation rules, a ceiling hit, a top-severity vote, or any other operator-only
  question still PARKS. Never improvise forks.
- Conflict log (REQUIRED, §15ad.66): append an entry to
  .night-crew/knowledge/reference/conflicts-20260804.md for EVERY merge, clean or conflicted — the
  cards involved, files and hunks, the intents you read, the resolution you took, and the gate
  result after it. Clean merges get a one-line entry, so an empty log never reads as "no conflicts"
  when it means "the logging never ran". Commit it with the merge. Summarize it in the closeout;
  morning triage audits it.
- 🛑 `sw.js` is a committed artifact and `build-sw.js` reads git HEAD, not the working tree. A2 and
  A6 both edit HTML: run `node build-sw.js` AFTER the merge commits (B-37) and commit the result.
  Precache count is currently 31 — if it moves without an asset being deliberately added or
  removed, that is the silent drop coming back. A6 legitimately does not move it (version.json is
  already precached).
- 🛑 Do NOT change `playwright.config.js:60`'s repo-wide `serviceWorkers: 'block'` (B-15). A1 edits
  that file and is the card most likely to be tempted.
- 🛑 Do NOT set `HQ_SYNC_REST_URL` in any environment. No card here needs it.
- Gate honesty: judge against "green except the armed reds", never against green. B-27 and the
  three other armed reds stay ARMED — passing retires nothing (decision 100 / T-31 decision 120).
  Until A1 lands, every full-suite figure is measured against an accumulating database (B-76): a
  red can be an artifact and SO CAN A GREEN. Say which you are looking at.
- Budget discipline: prefer a clean early exit over starting a card you cannot finish cleanly. A
  park with evidence is a better night than a merge with a caveat — 20260803's S1b spent 20 minutes
  to save a 4h30m card and produced the night's most valuable finding.

Closeout (ALWAYS, leave ~30 min): update
.night-crew/runs/2026-08-04-autonomous/HANDOFF.md (per-card outcomes table, gate evidence on the
final tree, next actions) and DECISIONS-NEEDED.md (anything parked), commit as docs. Record
per-card wall-clock stamps in card-actuals.md — implement start→end, G6 start→return, fix rounds —
because the reworded D-KR3 grades on a defensible median with its exclusions listed. The morning
reader is the operator — write for them.

Your FINAL CHAT MESSAGE is a hand-off, so CLAUDE.md §"Context checkpoints at hand-offs" binds it.
Do not end on a results summary. End with, in this order:
1. Next steps, led by morning triage — what is waiting, what is parked, what the operator has to
   decide. Name the two attended items this slate creates: `task prod:deploy` → `task version` →
   read the version line on a RETURNING client (closes D-KR2a and D-KR2b, ~15 min, only if A6
   landed), and the P6 fix-forward checklist before that notice goes anywhere.
2. The context-checkpoint verdict with its reason. For a finished run it is 🔄 Clear now
   (recommended) — /nc-morning-triage is designed to start cold and reviews the run branch on its
   merits; nothing in your context is load-bearing because HANDOFF.md, DECISIONS-NEEDED.md, and the
   conflict log carry it all. Say ⛔ instead only if something real is still in flight.
3. The paste-ready next command: /nc-morning-triage
Writing HANDOFF.md does NOT discharge this. The file and the closing message are different surfaces
read at different moments, and the operator reading the last thing on screen at 7am is the one this
is for.
