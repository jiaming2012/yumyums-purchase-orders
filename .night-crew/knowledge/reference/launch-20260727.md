Tonight's autonomous run. Execute the signed slate at
`.night-crew/knowledge/reference/slate-20260727.md`.

Setup: you are on branch dev.

Before any git write, CHECK FOR UNMERGED RUN BRANCHES. Run this EXACTLY as written — it contains
no placeholder to substitute, and it asks about every date, because an unfinished night from any
date is a night's work sitting unmerged:

    git branch --no-merged dev --format='%(refname:short)' | grep -E '^overnight-[0-9]{8}(-[0-9]+)?$'

STATE THE RESULT OUT LOUD BEFORE PROCEEDING — either "no unmerged run branches" or the list. You
may only proceed after saying which one you saw. An empty result is the "go" signal, so a command
that failed or ran wrong looks exactly like a clean repo unless you have named what you found.

- No unmerged run branch → say so, then cut this run's branch. Compute its name from the BARE
  DATE, since you are looking for every branch of that date:

      git branch --format='%(refname:short)' | grep -E '^overnight-20260727(-[0-9]+)?$' | sort -V

  No match → `overnight-20260727`. Otherwise the next free suffix (-2, -3, …). A MERGED branch's
  name is still taken, so a date whose run merged this morning yields -2 — cut it and proceed
  without asking; nothing is at risk.
- ANY unmerged run branch exists → 🛑 STOP. A previous night's work is sitting on it. Report what
  is there (`git log --oneline dev..<branch>`, and whether DECISIONS-NEEDED.md carries an open
  section for it), then ASK THE OPERATOR to choose: RESUME that branch, or RECUT at the next free
  suffix. Do NOT pick for them — resuming mixes two nights on one branch, recutting walks away
  from a night's work, and you cannot tell from here which is right. This is NOT covered by the
  batch sign-off: the yes is scoped to the signed specs, and what to do with an unfinished night
  is not one of them. Whatever they answer, the existing branch is left untouched — never delete,
  reset, force, or rename it.

If the run id gains a suffix, EVERY run-scoped name takes it with them: the conflict log
`.night-crew/knowledge/reference/conflicts-<runid>.md`, and the closeout's references to the run.
The slate keeps the name it was signed under.

Do all work on the run branch. Never push, never touch main. Hard prohibitions in CLAUDE.md apply.

Authority: batch sign-off was given by the operator on 2026-07-26 — the slate document is the
record. Do NOT pause for per-change sign-off. Every operator fork is already resolved in
`ledger.md` T-23 decisions 55–66, including D-6, which the operator confirmed directly at sign-off
(the per-tab granularity convention GOVERNS umbrella slugs; decision 56 stands as written). The
run never decides, it executes.

Rules of engagement:

- Standing rules and verification gates G1–G6 are inherited unchanged from
  `reference/overnight-run-plan-20260707.md`.

- Dispatch model: you are the control loop, not the implementer. For each card: create a worktree
  off `overnight-20260727`, spawn a FRESH subagent scoped to exactly that card (paste its slate
  entry, footprint, and spec references into the subagent prompt; it drafts, implements, runs
  G1–G4 in its worktree, and returns a compact report), then spawn a SEPARATE fresh subagent for
  the G6 adversarial review, then merge to the run branch and archive yourself. Never implement a
  card in your own context; your context holds reports, not diffs. Subagents cannot spawn
  subagents — G6 dispatch is always yours. A failed card PARKS per the slate rules (worktree
  preserved, DECISIONS-NEEDED.md) — dispatch the next card. Editing a file outside the card's
  footprint is NOT a park (§15ad.65) — see the conflict rules below.

- Order: **Card A `pwa-cache-and-build-hygiene` strictly first, dispatched ALONE** — acceptance:
  `caches.delete('api-cache')` runs on logout, `checkAuth`'s offline branch fails CLOSED on
  identity, `build-sw.js` globs `git ls-files` rather than the working tree, and the vendored
  bundle is excluded from the precache. `sw.js` is regenerated and committed before any other card
  starts. THEN dispatch one card at a time, SERIAL, in slate order: Card B
  `workflow-offline-double-submit`, then Card C `sync-rxdb-schema-and-replication`. Card C's
  worktree is cut only after Card B has landed on the run branch — both own `workflows.html`, and
  cutting C late is what makes the overlap free.

- Per-change mechanics — the slate records the workflow-preflight verdict as **ABSENT** (the
  subcommand does not exist in the deployed night-crew CLI), and hq has no `openspec/` directory.
  Therefore: **do NOT assert OpenSpec mechanics and do NOT create `openspec/` scaffolding in this
  repo.** Assert only the universal mechanics: red-first evidence, atomic commits, a
  change-identifying commit trailer on every commit, and the roadmap card flip in the same change
  set. Which per-change discipline this repo should adopt is an operator question (B-105) — never
  encode another detected convention's mechanics in its place.

- Merge-intent note (REQUIRED, §15ad.65): each subagent writes
  **`.night-crew/runs/2026-07-27-autonomous/merge-intents/<card>.md`** in its FIRST commit, before
  implementing — shared files touched (each file outside its own packages, one line of why), what
  must survive any merge, and what is safe to drop. Empty fields say "nothing here" explicitly.
  **Note the path: it is NOT `openspec/changes/<slug>/merge-intent.md`** — this repo has no
  `openspec/` tree and one must not be created. Paste this requirement, with this path, into every
  implementer prompt.

- Conflicts, not fences (§15ad.65): the footprint is planning information, not a boundary — a card
  MAY edit any file, and doing so is not a breach and not a park. Collisions surface as ordinary
  git conflicts. YOU resolve them at merge by reading BOTH sides' merge-intent note and resolving
  against intent, not text; then re-run G1+G2. PARK the later card ONLY when, having read both
  intents, you still cannot determine which behavior is correct. Any operator-only question still
  PARKS. Never improvise forks.

- Conflict log (REQUIRED, §15ad.66): append an entry to
  `.night-crew/knowledge/reference/conflicts-20260727.md` for EVERY merge, clean or conflicted —
  the cards involved, files and hunks, the intents you read, the resolution you took, and the gate
  result after it. Clean merges get a one-line entry, so an empty log never reads as "no
  conflicts" when it means "the logging never ran". Commit it with the merge.  Summarize it in the
  closeout; morning triage audits it.

- **Suite discipline — do NOT take a seam-confined subset for Cards B or C.** T-22 decision 54
  widened the workflow seam, and the ledger records a green `workflows|persistence` subset buying
  false confidence on this exact file — it shipped a regression because neither failing spec was
  in the subset. Run the full Playwright suite for any card touching `workflows.html`.

- **Card C park triggers, explicit:** park if the `_modified` semantics call needs an operator
  answer rather than a defensible engineering call; park if the origin-shape declaration
  (obligation 2) cannot be made without an operator decision on hosting; park if anything pressures
  you to vary `assignment_role` or the unconditional `admin`/`superadmin` arm in obligation 1 —
  that is a separate card by construction. Card C's five T-23 obligations are all binding; the
  origin-shape declaration is its FIRST spec line.

- **Root `package.json` and `package-lock.json` are HARD-untouched.** Card C pins
  `@supabase/supabase-js` through the vendored-bundle path established by
  `sync-rxdb-browser-delivery-spike`, never by editing the root manifest — that file is the
  Playwright environment for every card.

- Budget discipline: prefer a clean early exit over starting a card you cannot finish cleanly.
  Card C is the riskiest and is last by design; if the clock or quota is tight when B lands, stop
  cleanly rather than opening C. Note that the slate carries **no cut line** — no `--night` budget
  was given, and quota was never measured.

- **Record actuals.** `card-actuals.md` has no row for run `overnight-20260726`; do not repeat
  that. Capture per-card implement / G6 / land / cycle timings as you go and write the run's rows
  at closeout.

Closeout (ALWAYS, leave ~30 min): update HANDOFF.md (per-card outcomes table, gate evidence on the
final tree, next actions) and DECISIONS-NEEDED.md (anything parked), commit as docs. The morning
reader is the operator — write for them.

Your FINAL CHAT MESSAGE is a hand-off, so CLAUDE.md §"Context checkpoints at hand-offs" binds it.
Do not end on a results summary. End with, in this order:

1. Next steps, led by morning triage — what is waiting, what is parked, what the operator has to
   decide.
2. The context-checkpoint verdict with its reason. For a finished run it is 🔄 Clear now
   (recommended) — `/nc-morning-triage` is designed to start cold and reviews the run branch on its
   merits; nothing in your context is load-bearing because HANDOFF.md, DECISIONS-NEEDED.md, and the
   conflict log carry it all. Say ⛔ instead only if something real is still in flight in this
   session.
3. The paste-ready next command: `/nc-morning-triage`

Writing HANDOFF.md does NOT discharge this. The file and the closing message are different
surfaces read at different moments, and the operator reading the last thing on screen at 7am is
the one this is for.
