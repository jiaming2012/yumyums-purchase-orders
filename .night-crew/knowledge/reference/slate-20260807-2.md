# Slate — run `overnight-20260807-2`

> **Activities:** Activity 2 — *"Prove the route before the nights spend it"* — the two
> remaining spikes, C and D. Target confirmed by the operator at planning, 2026-08-06 (evening).
> **Run branch:** `overnight-20260807-2` — suffixed because tonight is the evening of
> 2026-08-06 and run `20260807` already ran **today in daylight** (launched mid-morning by the
> operator, triaged the same afternoon, merged; T-40 carries the date-label correction). A
> merged branch's name is still taken. `git branch --no-merged dev` returned **no unmerged run
> branches** at planning.
> **Sign-off:** batch sign-off given by the operator on 2026-08-06 (evening session), with the
> milestone remainder and attention budget presented as recorded below. This document is the
> record.
> **Dispatch:** **SERIAL** — operator's choice after sign-off. Card lists were identical under
> both modes; the choice was burn rate + shared-stack interference risk vs at most ~1.5h of
> margin, and serial won on a night with no `--night` clock pressure.
> **Cards:** 2 — `spike-c-round-trip` (load-bearing) then `spike-d-realtime-live`. No stretch:
> every other non-attended card in the milestone is an Activity 3–5 build card hard-gated
> behind spike C's green, which cannot exist before the run starts.
> **Forks resolved:** none open. `DECISIONS-NEEDED.md` for run `20260807` closed with zero open
> sections (ledger §T-40); the one triage fork became decision 160 (worktree rule, pending
> candidate `operations/C-3`). Neither card carries an open operator decision: spike C's
> mechanism question **is the spike's deliverable**, and spike D executes B-62's already-written
> test shape.
> **Workflow preflight verdict:** `openspec: absent` · `gsd: detected` · exit 0 (re-run at this
> planning). The **universal-mechanics** variant is asserted in the launch prompt; no OpenSpec
> scaffolding is to be created (B-100 guard).
> **Precondition flags:** (1) 🛑 Working tree must be clean at launch — verified clean at
> planning (`7bbc60f` on `dev`). (2) **B-147 and B-148 — the two fix-before-next-run items from
> T-40 — are DONE** on `dev` (`05bba61`, merged `cd2c6fd`). Residual carried as a risk note:
> B-148's fix is execution-verified on the recovery path, but the **full red-path spike
> rehearsal was never re-run** — tonight's spikes must not assume the red path is
> battle-tested. (3) Spike A's stack scripts are on `dev` and merged
> (`.night-crew/qa/spike-supabase/env-up.sh` + `spike-b-migration.sh` beside it).
> **Gate ladder:** `.night-crew/knowledge/reference/gate-ladder.md` — G1, G2(Go),
> G2(Playwright), G3 (N/A — openspec absent), G4, **RF** (red-first), G6.
> **Night budget:** none given (no `--night`). The slate is sized by its cards, not cut to a
> clock: **serial critical path ~2.5–4.5h + ~30m closeout ≈ 3–5h.** **Quota was not measured
> and cannot be** — the account's token ceiling is invisible to the planner (DESIGN §15w); the
> run may exhaust quota before the clock or the clock before quota, and spend is governed by
> the run's own stop conditions, never by these hour figures.

---

## 🛑 The three standing rules of this night

1. **Production stays untouched.** No leg of this run contacts the production container, the
   `yumyums` database, or **anything on the shared :5433 cluster** (`operations/P-1`; decisions
   154–155; the 2026-08-06 destruction is why). Test suites run on **:5434
   (`yumyums-test-pg`, role `hqtest`)** — real since run `20260807` W0. Spike infrastructure is
   spike A's own stack plus scratch containers created and destroyed per run (the spike-B
   discipline).
2. **The run git-operates only from a dedicated run worktree, never the main checkout**
   (decision 160, T-40 — ruled after run `20260807`'s first merge landed on `dev` because a
   concurrent attended session moved the main checkout). Create the worktree right after
   cutting the branch; all run git ops happen there.
3. **The global suite lock:** one test suite in flight, ever — no two Playwright runs, no Go
   suite alongside a Playwright run. `sync.spec.js` is load-sensitive (B-27/B-30/B-32 armed).
   **Corollary for tonight: one spike at a time on spike A's live stack** — spike C and spike D
   both consume it in reconcile mode, and run `20260806` already showed a compose op recreating
   spike containers and moving their ephemeral ports mid-leg.

---

## What verification found before this slate was written

Every load-bearing claim re-checked at source tonight (the slate-20260806 discipline).

1. **B-147/B-148 are genuinely fixed on `dev`** — `05bba61` re-points the B-132 screenshot
   into gitignored `test-results/` and writes probe ids into the spike-B manifest before the
   load, with the CID guard moved into `srcPsql()` (red-first proven at fix time: exit 2 →
   exit 0). The B-148 caveat above stands: the full red-path rehearsal was not re-run.
2. **Spike A + B artifacts are on `dev`**: `env-up.sh`, `spike-b-migration.sh`, `initdb/`,
   `sql/`, `mintjwt/`, `rtwatch/`, `rxdb/` all present under `.night-crew/qa/spike-supabase/`.
   Spike B's GREEN stands as signed (48 assertions, G6 re-executed; triage recorded the runtime
   figure unverified-but-structurally-checked).
3. **The write path is where the roadmap says**: `backend/internal/workflow/handler.go:616`
   `SaveResponseHandler` (POST `/api/v1/workflow/saveResponse`). The frontend writes via
   `POST /ops` (D-08), but the suites and the close bar's demo drive `/saveResponse` directly —
   spike C uses the real endpoint, per the card.
4. **`backend/internal/sync` already contains `listener.go`, `hub.go`, `ops.go`** — there is
   existing HQ-side plumbing (LISTEN/NOTIFY-shaped) that spike C's mechanism probe must
   inventory before concluding "nothing carries rows forward." Decision 126's measurement was
   about the FDW bridge (permissions, not data) and the substrate→HQ direction; the
   HQ→substrate forward leg is exactly what the spike establishes.
5. **Both cards pay the full suite.** `[e2e.seams]` still declares only the three
   inventory/recipes rows; `.night-crew/qa/**`, `Taskfile.yml`, `backend/internal/sync`,
   `sync-rxdb/**` are all undeclared → de-confine to full run. Full-suite baseline on the new
   container: **20.9–26.1m** (re-baselined by run `20260807`; do not re-arm on figures in that
   band).
6. **No unmerged run branches; tree clean at `7bbc60f`.**

## Gate cost *(required by Q-KR4 — exactly once, and never backfilled)*

- **Full-suite baseline: ~21–26m on the :5434 container** (run `20260807`, three runs:
  26.1/25.1/20.9m). 🛑 Inherited, not re-measured tonight.
- **Both cards pay the full Playwright suite + full Go suite** (paths undeclared in seams).
  ~42–52m of Playwright across the night, dispatch-invariant under the global lock.
- **Go suite:** `-p 1` load-bearing; `DB_TEST_URL` must be set or DB-coupled tests silently
  skip while printing `ok` — check counts, not just `ok`. Expect workflow=35-shaped counts.
- `export PATH="/usr/local/go/bin:$PATH"` on every leg; `go: not found` exit 127 is a PATH
  failure, not a test failure.

---

## Shared surfaces and serialization

| Surface | Cards | Rule |
|---|---|---|
| `.night-crew/qa/spike-supabase/**` | **C, D** | Each card adds its own sibling script (`spike-c-*.sh`, `spike-d-*.sh`) beside spike A/B's. Slate order = landing order (C → D). Shared helpers (`hq-bridge-env.js`, compose files) are read-mostly; an edit there goes in the merge-intent's what-must-survive. |
| `Taskfile.yml` | **C, D** | Disjoint new stanzas (`spike:roundtrip`, `spike:realtime`). Slate order = landing order. 🛑 Do not touch the `prod:backup` stanza or the `test:*` blocks W0 re-pointed. |
| `backend/internal/sync/**` | **C** (probe/prototype only) | Sole owner tonight. 🛑 The preserved `card/a3-rls-fixture-own` branch edits `rowvisibility_rls_test.go` — **that branch and its worktree stay untouched** (attended re-gate, decision 155). |
| `sync-rxdb/*.js` | **D** (only if the live-filter proof needs the shim surfaced) | Sole owner tonight. Changes here must keep `tests/sync-rxdb-client.spec.js` green — the config-level assertions B-62 credits remain true even once the live leg exists. |
| **Spike A's live substrate stack (runtime)** | **C, D** | Consume in **reconcile mode, never destroy** (spike-B discipline). 🛑 One spike on the stack at a time — standing rule 3's corollary. Scratch containers (an HQ-shaped source Postgres, if C needs one) are per-run, created and destroyed. |
| **The :5434 test container (runtime)** | both (suite legs) | One suite at a time under the global lock. Never :5433 (standing rule 1). |
| **The Playwright/Go suites (runtime)** | both | Global lock; trivially satisfied under serial dispatch. |

---

## Track — serial order

### C · `spike-c-round-trip` — 90–150m, planned at the high end 🛑 LOAD-BEARING · **3rd of 4 D-KR1 spike verdicts**

**Scope.** *Spike C — from the governing handoff §5, carried into the roadmap verbatim.* One
row written through the **real** write path (`POST /api/v1/workflow/saveResponse`) must appear
in an **RxDB-served read** within bounded time. The spike's job is to establish whether the
HQ-Postgres → substrate → RxDB-read path exists **at all, and by what mechanism** — this is
precisely the premise decision 126 measured false after nine nights (the FDW bridge is
one-directional and carries permissions, not data). Inventory what exists first
(`backend/internal/sync`'s `listener.go`/`hub.go`; spike B's PostgREST service-identity lane),
then prove ONE candidate mechanism end-to-end by script, against spike A's stack + a scratch
HQ-shaped source Postgres (never the shared cluster, never the :5434 test container's
databases). 🛑 **The verdict is the script's exit status, never prose** (the spike↔script
rule; B-345 clone-side). 🛑 **A red here is a successful spike, not a failed card** — record
the mechanism finding with the script's output and stop; do not grind for green. If C cannot
go green, the roadmap's own rule fires: no Activity 3–5 build card may be cut — **that
STOP binds the next slate, not this run**; tonight simply records the verdict.

**Footprint.** Owns: `.night-crew/qa/spike-supabase/` (new `spike-c-roundtrip.sh` + any
helper), `Taskfile.yml` (a `spike:roundtrip` target), `backend/internal/sync/**` (probe or
minimal prototype code only, clearly marked). Touches: nothing else. **Pays the full suite.**

**Estimate basis.** First-of-kind spike class: spike A actual **106m28s** (est 60–150m, full
fix round), spike B actual **~65m** (same est, low end), browser-delivery spike **145m** (the
hardest spike observed). Risk-flagged (load-bearing + mechanism-establishing, not
mechanism-verifying) → planned at the class's high end, 150m.

**Risk.** The mechanism may not exist — that is the point. The failure shape to avoid is a
**silent no-op green** (the defect class this milestone exists to retire): every leg of the
script must fail loud, and "could not run" must exit distinct from "ran red" (the demo-target
rule applies to spikes too). B-148 residual: the red path of the spike-B harness was never
re-rehearsed — if C reuses that harness, treat its recovery path as unproven.

**PARK note.** Park only on a genuinely operator-only discovery: the sole viable mechanism
would **abandon decision 126's shape** (writes moving off `/saveResponse`/REST), would require
**touching production or the shared :5433 cluster**, or would require a **paid/external
service or credential** the project has not adopted. Choosing *which* candidate mechanism to
prove (LISTEN/NOTIFY relay, polling relay, logical replication, PostgREST forward-writer) is
the spike's own call — that finding is the deliverable. A red verdict is not a park.

### D · `spike-d-realtime-live` — 60–120m · **4th of 4 D-KR1 spike verdicts · closes B-62**

**Scope.** Drive the Realtime replication `filter` against a **live** Realtime server — today
it is proved at the config only (`tests/sync-rxdb-client.spec.js` [SCOPE-04] proves the clause
reaches the binding; nothing proves the substrate honours it). B-62's entry already writes the
test shape: subscribe with the filter against spike A's live stack, `INSERT` one in-scope row
and one out-of-scope row as the substrate superuser, assert the in-scope row **arrives** (an
ignored filter and a dead subscription look identical — the positive leg is what makes the
negative leg evidence) and the out-of-scope row does not; pair with `responses` (filter
deliberately absent) as the negative control — both rows must arrive there. 🛑
`HQ_SYNC_REST_URL` unset in deploys is **the interlock working, not evidence** — the spike
connects to the spike stack directly and does not touch deploy config. Verdict = exit status.
**A filter the live server ignores is a RED verdict with the finding recorded** — that is
B-62's payoff, not a park.

**Footprint.** Owns: `.night-crew/qa/spike-supabase/` (new `spike-d-realtime.sh` + helper,
`rtwatch/` may be extended), `Taskfile.yml` (a `spike:realtime` target). Touches:
`sync-rxdb/client.js` only if surfacing the channel shim for the live proof requires it —
config-level spec assertions must stay green. **Pays the full suite.**

**Estimate basis.** Spike B actual **~65m** — the closest shape (drives spike A's existing
live stack in reconcile mode, no new infra); W1/W2 infra spikes ~69–72m. Not risk-flagged →
mid-class range, 60–120m.

**Risk.** Realtime's filter semantics vs `REPLICA IDENTITY FULL` payloads is exactly the
quiet-failure surface B-62 names — the assertion set must make "ignored" and "dead
subscription" distinguishable (the positive-arrival leg is mandatory, or the spike is
vacuous). Runs after C under standing rule 3's corollary; if C left the stack reconciled
dirty, reconcile first, never recreate.

**PARK note.** Park only if proving the filter would require **changing the sync contract's
schema or RLS policies in a way that alters what a grant means** (a permissions-boundary
judgment), or a new `night-crew.toml` key / preference category. A RED verdict (filter
ignored, subscription semantics unusable) is the deliverable, not a park.

---

## Explicitly NOT in this slate

- **`gate-rls-fixture-ownership` (A3)** — BLOCKED, attended by ruling (decision 155): its
  preserved branch `card/a3-rls-fixture-own` and worktree stay untouched tonight.
- **All Activity 3–5 build cards** (`skeleton-*`, `activate-*`, `demo-sync-target`) —
  hard-gated: no build card may be cut until spike C is green, and tonight is the night that
  verdict gets made. Even a green C tonight does not un-gate them mid-run — cutting cards is
  the next slate's act, on the recorded verdict.
- **`dev-complete-attestation`** — attended, the operator's own act, by definition.
- **B-149** (uid-mismatch half of B-89 unguarded by test) and **B-150–B-155** — real, filed,
  none architecture-blocking (§15k bar); they wait for a roadmap round or an attended slot.
- **The attended queue** — A3 re-gate, decision-156 Mercury backfill, B-146 SFTP key,
  decision-159 `archive_mode` residual, decision-158 sales-processor message, B-145 recovery
  Phase 1. Standing rule 1 keeps tonight out of all of them.
- **The observability cluster (B-139, B-81, B-82, B-86, B-93)** — left `new` at the roadmap
  round, off the critical path at the operator's direction.
- **B-131 and the load-sensitive flake family (B-27, B-30, B-32)** — stay armed; retired by
  diagnosis, never by passing once (decision 100; T-31 decision 120).

---

## Milestone remainder — recorded as the operator saw it at sign-off

Derived from the **live roadmap** (`.night-crew/knowledge/roadmap.md` card statuses read
tonight), not from HANDOFF prose. **18 cards; 9 DONE** (Activity 0 both, Activity 1 five of
six, Activity 2 spikes A and B). **This slate takes 2. If both land, the milestone stands
11 DONE / 7 remaining.** Reported in cards, not key results — with the one KR that bears on a
card choice: tonight completes **all four D-KR1 spike verdicts**, whichever way they land.

Still not-done after tonight:

| Activity | Cards |
|---|---|
| **1 — Gate integrity** | `gate-rls-fixture-ownership` 🛑 BLOCKED — attended re-gate (decision 155) |
| **3 — Walking skeleton** | `skeleton-one-row-end-to-end`, `skeleton-offline-ownership-honesty` |
| **4 — Activate reads** | `activate-fill-view-reads`, `activate-list-views-or-state-they-stay-rest` |
| **5 — Dev complete** | `demo-sync-target`, `dev-complete-attestation` 🛑 attended, the operator's own act |

**Recommended additions: nothing worth adding.** Every remaining non-attended card is an
Activity 3–5 build card gated behind spike C's green — a verdict that does not exist until
tonight runs. The only ungated candidates are backlog items (B-149 the strongest), and none
clears the §15k architecture-blocking bar. Spike C deserves the night's headroom, not
company.

---

## Execution notes

**Dispatch: SERIAL** (operator's choice at sign-off, 2026-08-06 evening).

    C  spike-c-round-trip     ← first; load-bearing, gets the night's headroom
    D  spike-d-realtime-live  ← after C lands (merged or verdict-recorded)

At most one card in flight. The orchestrator alone merges to the run branch, resolving
conflicts against both sides' `merge-intent.md`, logging every merge (clean ones included) in
`reference/conflicts-20260807-2.md`.

**Why serial is recommended (both projections for the operator):** serial fits both cards in
**~3–5h wall-clock**; concurrent projects **~2.5–3.5h at roughly 2× burn** — and buys less
than it appears, because the two cards' full-suite legs (~42–52m) serialize under the global
lock anyway, and both spikes drive spike A's single live substrate stack, where run
`20260806` already saw a compose op move container ports mid-leg. The card lists are
identical under both modes — the choice is burn rate + stack-interference risk vs at most
~1.5h of margin, on a night with no `--night` clock pressure.

**Two-session split, if the night is broken up:** Session 1 = C (the load-bearing verdict).
Session 2 = D. Clean split — the sets share only sibling files and disjoint Taskfile stanzas.

**D runs regardless of C's colour.** C red is a recorded mechanism finding and stops nothing
in Activity 2; the Activity 3–5 gate is the next slate's business.

**Run mechanics** are inherited by reference from
`.night-crew/knowledge/reference/gate-ladder.md` — gates G1–G6 + RF, the per-leg isolation
stanza, the capture-the-whole-log rule, and B-87's `tests/`-anchored Playwright filter form.
Not restated here. 🛑 This repo's ladder file is the ONLY mechanics source —
`reference/overnight-run-plan-20260707.md` has never existed in this repo (B-26, decision
138).
