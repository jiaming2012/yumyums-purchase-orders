Tonight's autonomous run. Execute the signed slate at
`.night-crew/knowledge/reference/slate-20260806.md`.

Setup: you are on branch dev.

Before any git write, CHECK FOR UNMERGED RUN BRANCHES. Run this EXACTLY as written — it contains no
placeholder to substitute, and it asks about every date, because an unfinished night from any date
is a night's work sitting unmerged:

    git branch --no-merged dev --format='%(refname:short)' | grep -E '^overnight-[0-9]{8}(-[0-9]+)?$'

STATE THE RESULT OUT LOUD BEFORE PROCEEDING — either "no unmerged run branches" or the list. You may
only proceed after saying which one you saw. An empty result is the "go" signal, so a command that
failed or ran wrong looks exactly like a clean repo unless you have named what you found.

- No unmerged run branch → say so, then cut this run's branch. Compute its name from the BARE DATE,
  since you are looking for every branch of that date:

      git branch --format='%(refname:short)' | grep -E '^overnight-20260806(-[0-9]+)?$' | sort -V

  No match → `overnight-20260806`. Otherwise the next free suffix (-2, -3, …). A MERGED branch's name
  is still taken, so a date whose run merged this morning yields -2 — cut it and proceed without
  asking; nothing is at risk.
- ANY unmerged run branch exists → 🛑 STOP. A previous night's work is sitting on it. Report what is
  there (`git log --oneline dev..<branch>`, and whether DECISIONS-NEEDED.md carries an open section
  for it), then ASK THE OPERATOR to choose: RESUME that branch, or RECUT at the next free suffix. Do
  NOT pick for them — resuming mixes two nights on one branch, recutting walks away from a night's
  work, and you cannot tell from here which is right. This is NOT covered by the batch sign-off: the
  yes is scoped to the signed specs, and what to do with an unfinished night is not one of them.
  Whatever they answer, the existing branch is left untouched — never delete, reset, force, or
  rename it.

If the run id gains a suffix, EVERY run-scoped name takes it with them: the conflict log
`.night-crew/knowledge/reference/conflicts-<runid>.md`, and the closeout's references to the run. The
slate keeps the name it was signed under.

Do all work on the run branch. Never push, never touch main. Hard prohibitions in CLAUDE.md apply.

Authority: batch sign-off was given by the operator on 2026-08-05 — the slate document is the record.
Do NOT pause for per-change sign-off. Every operator fork is already resolved; you never choose a
fork — what to build, what a milestone means, anything only the operator can settle.

Park is NOT the default answer to a gray area. Route it through `night-crew decisions log` and act on
the verdict it PRINTED, never on an inference from the disposition. Two verdicts, four producing
states (CLAUDE.md §"Slate workflow" step 5 is the doctrine of record):
  - `verdict: decide` under an adopted delegation policy — recorded delegated, `ratification:
    pending`, back at triage. Deciding is not settling.
  - `verdict: decide` on the citation floor with NO policy involved — owes no ratification. You will
    meet this; do not read `decide` as "a policy covered it".
  - `verdict: park` with the decree read — prints the verdict and exits 0. An escalation is a correct
    outcome, not a command failure.
  - park because the decree could not be READ — fails, exits non-zero, renders `park:` inside its
    error, and prints NO `verdict:` line at all. Grepping for `verdict: park` misses this one; read
    the exit code.
An escalation and a park are the same act, which is why there are two verdicts and not three. A
delegation converts a question that WOULD have escalated into one that resolves; it is not the only
way a question resolves — never restate this as "decide only under a policy, otherwise park".

🛑 `night-crew` IS NOT ON THE NON-INTERACTIVE PATH. It is at `/home/jcole/go/bin/night-crew`. Export
`PATH="/home/jcole/go/bin:/usr/local/go/bin:$PATH"` before any `night-crew`, Go or Playwright leg —
the non-interactive shell carries neither. Playwright's `webServer` dies with `go: not found` /
exit 127, which LOOKS LIKE A TEST FAILURE AND IS NOT.

Rules of engagement:

- Standing rules and verification gates G1–G6 are inherited from
  `.night-crew/knowledge/reference/gate-ladder.md` — this repo's ladder, cited by path. 🛑 Do NOT
  look for `reference/overnight-run-plan-20260707.md`; it has never existed in this repo and four
  consecutive slates inherited it as a dangling pointer (B-26, decision 138). Read `gate-ladder.md`
  in full before the first gate: it carries the mandatory per-leg isolation stanza, the
  capture-the-whole-log rule, and B-87's `tests/`-anchored Playwright filter form.

- Dispatch model: you are the control loop, not the implementer. For each card: create a worktree off
  `overnight-20260806`, spawn a FRESH subagent scoped to exactly that card (paste its slate entry,
  footprint, and spec references into the subagent prompt; it drafts, implements, runs G1–G4 in its
  worktree, and returns a compact report), then spawn a SEPARATE fresh subagent for the G6 adversarial
  review, then merge to the run branch and archive yourself. Never implement a card in your own
  context; your context holds reports, not diffs. Subagents cannot spawn subagents — G6 dispatch is
  always yours. A failed card PARKS per the slate rules (worktree preserved, DECISIONS-NEEDED.md) —
  dispatch the next card. Editing a file outside the card's footprint is NOT a park (§15ad.65) — see
  the conflict rules below.

- Order — CONCURRENT, three tracks:

      Wave 0:  repo-hygiene-preconditions          ← strictly first, alone; everything waits
        then, concurrently:
          Track A:  A1 gate-rls-count-assertion  →  A3 gate-rls-fixture-ownership
          Track B:  A2 gate-harness-check-b      →  A4 gate-ladder-completeness
          Track C:  C1 spike-a-environment-up
        then, if budget allows:
          A5 shipped-bug-sweep (budget-gated stretch)

  Wave 0's acceptance test: the NUL byte is gone from `sync-rxdb/client.js` and a `grep` for the
  three affected strings now reports the 3 real hits it was blind to; `night-crew.toml`'s comment and
  the actual `ls tests/ | grep` output agree; no `sync-rxdb-row-visibility-rls` gating language
  survives in `bootstrap.js`.

  At most ONE in-flight card per track, in slate order within each track. You alone merge to the run
  branch, in the order cards complete. If a track stalls on rate limits, degrade to serial — do not
  sit blocked.

  🛑 A3 runs LAST in Track A because it changes what `go test ./...` does for every leg after it.
  Once A3 merges, EVERY subsequent Go invocation — including your own closeout gate — must carry a
  unique `HQ_RLS_TEST_DB`. Track B is entirely non-Go and is unaffected whenever A3 lands.

- 🛑 THE GLOBAL PLAYWRIGHT SUITE LOCK — the single most important orchestration rule tonight.
  SIX of the seven cards pay the full ~24.2m suite (everything they touch is undeclared in
  `[e2e.seams]`), and `sync.spec.js` is load-sensitive. NO TWO PLAYWRIGHT SUITES MAY EVER BE IN
  FLIGHT AT ONCE, in any track, for any reason — nor may a Go suite or an RLS suite run alongside
  one. You own the lock; subagents do not coordinate with each other. A card that reaches its
  Playwright gate while another track holds the lock WAITS. A suite run that turns out to have
  overlapped another is DISCARDED AND RE-RUN, not reasoned about. Run `20260802` produced a
  51.7m / 7-failure gate exactly this way, on a TWO-track night, and it had to be thrown out.
  Queue order when several are waiting: C1 first, then Track A, then Track B.

- 🛑 MANDATORY PER-LEG ISOLATION. Every implementer, G6 reviewer and fix round gets a unique
  `TEST_PORT`, a unique `TEST_DB_NAME`, a unique `HQ_RLS_TEST_DB`, and a unique scratchpad directory.
  Since card A1 of run 20260804 landed, `playwright.config.js`'s `webServer.command` DROPs the
  database it is pointed at as its first act — two legs differing only in `TEST_PORT` no longer
  merely collide, the later DESTROYS the earlier mid-suite. An unqualified leg is destructive, not
  just noisy. Postgres is on :5433 and the credentials on this box are `yumyums:yumyums`, not
  `postgres:postgres`.

- Per-change mechanics — the slate records the workflow-preflight verdict for this repo as
  `openspec: absent` (exit 0, determined). Do NOT assert OpenSpec mechanics and do NOT create
  `openspec/` scaffolding in this repo. Assert only the universal mechanics: red-first evidence,
  atomic commits by logical unit, a change-identifying commit trailer on every commit, and the
  roadmap/status flip for the card in the same change set. Which per-change discipline this repo
  adopts was already answered — HQ keeps branch-and-commit (ledger §T-34, decision 140).

- 🛑 Q-KR3 — EVERY card whose deliverable includes a code change carries a `## Red-first` section:
  the named test, the tree it was captured red against, and the green after. A documentation, audit
  or spike WO records `n/a — no code change` EXPLICITLY. An absent section grades the KR down, and
  this is its first gradeable cycle. A4 is the one card tonight that records `n/a`.

- 🛑 B-39 — STAMP EVERY LEG. Record implement-start, implement-end, G6-start, G6-return, and each
  fix-round return, per card, into `.night-crew/knowledge/reference/card-actuals.md` at closeout.
  Two consecutive cycles produced un-countable cards because only the implementer leg was stamped,
  and D-KR3's median rested on 11 of 18 available cards.

- Merge-intent note (REQUIRED, §15ad.65): each subagent writes a `merge-intent.md` in its FIRST
  commit, before implementing — shared files touched (each file outside its own packages, one line of
  why), what must survive any merge, and what is safe to drop. Empty fields say "nothing here"
  explicitly. Paste this requirement into every implementer prompt. File it under
  `.night-crew/runs/2026-08-06-autonomous/merge-intents/`.

- Conflicts, not fences (§15ad.65): the footprint is planning information, not a boundary — a card MAY
  edit any file, and doing so is not a breach and not a park. Collisions surface as ordinary git
  conflicts. YOU resolve them at merge by reading BOTH sides' merge-intent.md and resolving against
  intent, not text; then re-run G1+G2. PARK the later card ONLY when, having read both intents, you
  still cannot determine which behavior is correct. An unplanned lifecycle row, a change to the
  escalation rules, a ceiling hit, a top-severity vote, or any other operator-only question still
  PARKS. Never improvise forks.

- Conflict log (REQUIRED, §15ad.66): append an entry to
  `.night-crew/knowledge/reference/conflicts-20260806.md` for EVERY merge, clean or conflicted — the
  cards involved, files and hunks, the intents you read, the resolution you took, and the gate result
  after it. Clean merges get a one-line entry, so an empty log never reads as "no conflicts" when it
  means "the logging never ran". Commit it with the merge. Summarize it in the closeout; morning
  triage audits it.

- Budget discipline: prefer a clean early exit over starting a card you cannot finish cleanly.
  🛑 THIS BINDS C1 HARDEST. `spike-a-environment-up` is first-of-kind with no `card-actuals.md`
  basis and a 60–150m range; it is the likeliest overrun on the slate. A RED verdict from its script
  GRADES D-KR1 MET — if the environment will not come up unattended, that IS the finding. Record the
  verdict with the script's output and STOP. Do not grind to force a green, and do not treat a red as
  a failed card. If it is still not up at the 150m mark, write the red with what was tried and hand
  the diagnosis to triage.
  🛑 A1's substrate risk: asserting "59 subtests ran" by execution needs
  `docker compose -p spike-supabase -f docker-compose.supabase.yml up -d`. If the substrate will not
  come up, do NOT park — degrade to asserting the count structurally over the table-driven case list
  and record in merge-intent that the execution-backed variant is owed. Substrate-won't-start is a
  mechanism problem, not an operator fork.

Closeout (ALWAYS, leave ~30 min): update HANDOFF.md (per-card outcomes table, gate evidence on the
final tree, next actions) and DECISIONS-NEEDED.md (anything parked), both under
`.night-crew/runs/2026-08-06-autonomous/`, plus the `card-actuals.md` stamps above. Commit as docs.
The morning reader is the operator — write for them.

Your FINAL CHAT MESSAGE is a hand-off, so CLAUDE.md §"Context checkpoints at hand-offs" binds it. Do
not end on a results summary. End with, in this order:
1. Next steps, led by morning triage — what is waiting, what is parked, what the operator has to
   decide.
2. The context-checkpoint verdict with its reason. For a finished run it is 🔄 Clear now
   (recommended) — /nc-morning-triage is designed to start cold and reviews the run branch on its
   merits; nothing in your context is load-bearing because HANDOFF.md, DECISIONS-NEEDED.md, and the
   conflict log carry it all. Say ⛔ instead only if something real is still in flight in this session.
3. The paste-ready next command: /nc-morning-triage
Writing HANDOFF.md does NOT discharge this. The file and the closing message are different surfaces
read at different moments, and the operator reading the last thing on screen at 7am is the one this
is for.
