# Closeout — run `20260810`

- **Run:** `20260810` (slate `reference/slate-20260810.md`, signed 2026-08-09). Branch
  `overnight-20260810` off `dev` @ `03c3e06`. Serial, subagent-per-card dispatch. Not merged to
  `dev` (morning triage's act).
- **Closeout written:** 2026-08-09 11:52 AST.
- **Changes that landed (2 of 2 cards, both G6-passed, both merged clean):**
  - **Card 1 `sync-live-in-dev-substrate`** (Activity 5, legs 1+2 + FDW persistence) — merge
    `bd03059`. Persistent dev data plane: `sync:dev:*` task family, 4× `HQ_SYNC_*` wired into dev
    targets **only** (proxy.go:78 guard honored), persistent FDW→HQ pointing SQL, relay-as-service
    bring-up + `sync-dev-proof.sh`. Both done_when **GREEN** (door 503→200; relay carries a
    `/saveResponse` write into the substrate in ~267ms). Roadmap flipped → DEV-COMPLETE. G6
    PASS-WITH-ISSUES, one comment-only must-fix applied (`167bc7e`), no fix round.
  - **Card 2 `sync-live-in-dev-app-proof`** (Activity 5, leg 3) — merge `489145e`. Standalone
    red-first harness (`sync-app-proof.sh` + `app-proof/` spec+config) drives the REAL
    `workflows.html` (no stub) against Card 1's live substrate — one `/saveResponse` field surfaces
    via RxDB in the app (`#sync-one-row → served`, ~454–508ms); the SAME spec fails relay-down.
    Verdict **GREEN** (gated on the script exit, B-163). No `workflows.html` edit needed (spike held).
    `night-crew.toml` = comment-only footprint note (no key/token). Roadmap flipped → DEV-COMPLETE.
    G6 PASS — independently re-ran the red-first asymmetry.
- **Final gate result (merged tree `489145e`):** G1 `go build`+`go vet` exit 0; G2(Go)
  `go test -p 1 -count=1 ./...` on `:5434` → **9 packages ok, 0 FAIL** (`HQ_SYNC_SUBSTRATE_OPTIONAL`
  + `HQ_SYNC_GATE_CHILD` attested unset); G2(Playwright) N/A-by-footprint (Card 2 standalone harness
  IS its verdict); G3 N/A (openspec absent); **G4 precache 31** N/A-by-footprint (no HTML/JS changed);
  G4 discipline greps N/A-VACUOUS (B-14); RF + G6 passed both cards.
- **Conflict log:** `reference/conflicts-20260810.md` — 2 merges, both clean, both logged.
- **Parked:** none. Advisory items in `DECISIONS-NEEDED.md` (spike-script `npx playwright test`
  hardening backlog candidate; two cosmetic G6 nits). No operator forks.
- **Environment:** left clean — substrate up (never destroyed), FDW restored to
  `:5434/hq_test_b2_fdw`, zero `spikec-`/`appproof-` rows, no scratch-HQ leak. No `:5433` command
  ran the entire night (B-164).
- **Milestone:** NOT closed by design — `dev-complete-attestation` (the operator's own attestation
  run in `dev:tailscale`) is the remaining attended act; the capability it attests is now LIVE and
  usable in the persistent dev environment, with the round trip proven red-first and automated.
