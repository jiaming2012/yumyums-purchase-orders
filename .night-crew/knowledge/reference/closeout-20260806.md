# Closeout — run `overnight-20260806`

**Closeout written:** 2026-08-06T02:05Z · **Ran:** 2026-08-05T18:31:12Z → 2026-08-06T02:05Z (~7h34m)
**Branch:** `overnight-20260806` · **Slate:** `slate-20260806.md`, batch sign-off 2026-08-05
**Dispatch:** concurrent, three tracks, under a global Playwright suite mutex

## What landed

**5 of 7 dispatched cards merged**, all clean, each logged in `conflicts-20260806.md`:

| Card | Merge |
|---|---|
| W0 `repo-hygiene-preconditions` | `6f91863` |
| C1 `spike-a-environment-up` (verdict GREEN) | `76dc12b` |
| A1 `gate-rls-count-assertion` | `9b63958` |
| A2 `gate-harness-check-b-per-package` | `b75ac53` |
| A4 `gate-ladder-completeness` | `c2a7e5c` |

**A3 `gate-rls-fixture-ownership` — NOT MERGED.** G6 verdict DO NOT MERGE; branch and worktree
preserved. **A5 `shipped-bug-sweep` — CUT on budget**, never dispatched.

Milestone effect: Q-KR1's residual closed, Q-KR2 closed, B-22 / B-26 / B-36 closed, B-14 recorded
with its clone-side destination. D-KR1 has one GREEN spike verdict of the four it requires.

## Final gate result

Run on the merged tree at closeout:

- **G4** — `node build-sw.js` `EXIT=0`, **31 files precached**, reachability 18 parsed / 30 resolved
  / 0 outside, **byte-idempotent** (tree clean on the second run). The count never moved all night.
- **Version parity** — `version.go Frontend 1.4.0` ≡ `package.json 1.4.0` ≡ `version.json 1.4.0`;
  Backend `0.3.0`. No bump, correctly.
- **G1 / G2 — NOT re-run at closeout, and this is stated rather than glossed.** Each merged card's
  gates are in its merge commit and merge-intent, and every Playwright gate this run was taken alone
  under the mutex. But **no full-suite gate was run against the final merged tree**; the last full
  suite (A3's — 791 passed + 6 skipped = 797, one summary block, 23.3m) was against a tree this
  branch does not hold, because A3 did not merge.

**Armed reds:** `sync.spec.js:446 [LST-17]` failed in C1's run, passed in A1's, A2's and A3's.
Recorded as observed; **armed status unchanged**.

## 🛑 The production incident

During G6 review of A3, a probe of that card's database-name guard set `HQ_RLS_TEST_DB=yumyums`; the
suite accepted it and ran `DROP DATABASE yumyums WITH (FORCE)` against the production cluster. No
PITR, no dumps, empty alternate volume — unrecoverable. The operator was stopped and asked, chose to
restart, and production was rebuilt structurally empty (goose 70, 48 tables, migration-seeded
reference data and one superadmin only).

Root cause was the orchestrator's, not the reviewer's: an adversarial review of a `DROP DATABASE`
guard was dispatched on a box where the test cluster **is** the production cluster, with a prompt
that invited probing using real database names and ruled none out of bounds.

Filed: **B-141** (a destructive-DDL parameter guarded by a blocklist that cannot contain the names
that matter), **B-142** (A3's residual fixes, held outside its branch), **B-143** (no backup of any
kind — the finding that made it terminal). Both structural questions are put to the operator as
**D-2** in `DECISIONS-NEEDED.md`.

## Open for triage

- **D-1** — what G3 is, and therefore whether red-first is a gate at all. Marked open **in the
  ladder itself**, not softened.
- **D-2** — the production posture: shared test/prod cluster, and no backups.
- **A3** — fix B-141's prefix guard plus B-142's two items as one card, then re-gate.
- **`task test:go` is broken** — shared `hq_test_go` corrupted (goose 73 applied, 72 absent).

## Nothing left polling

`night-crew workers check` re-run at closeout, 2026-08-06T02:06Z: **no pollers on any queue**,
both `night-crew` and `night-crew-env` clear, `EXIT=0`. A checked fact, not a habit.

Card A3's worktree at `/home/jcole/projects/hq-worktrees/a3-rls-fixture-own` (`ef2ef85`) is
**deliberately preserved** — it holds the refused card's full evidence.
