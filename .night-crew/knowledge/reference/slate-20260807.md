# Slate — run `overnight-20260807`

> **Activities:** Activity 0 — *"The floor under everything"* (`test-cluster-separation`; its
> sibling card was **done attended the same morning**, see the amendment below), **plus
> `shipped-bug-sweep` from Activity 1 and `spike-b-migration-rehearsal` from Activity 2 as
> budget-gated stretch** — the shape the operator chose at target confirmation, 2026-08-06.
> **Run branch:** `overnight-20260807` — no suffix; no run branch exists for this date, and
> `git branch --no-merged dev` returned **no unmerged run branches**.
> **Sign-off:** batch sign-off given by the operator on 2026-08-06, **amended by the operator
> the same morning: card A1 (`prod-backup-floor-and-pitr`) dropped** — their own attended
> session had already built and proven it on `fix/b145-prod-backup-floor` (merged to `dev`,
> tip `0a764a9`) while this slate was being planned. This document is the record of both acts.
> **Dispatch:** **SERIAL** — operator's choice at sign-off (both modes' card lists were identical;
> the choice was burn rate vs ~1h of margin, and serial won on quiet-box evidence quality).
> **Cards:** 3 — Wave 0 + A2, with S (spike-b) as budget-gated stretch. (The A1 label is
> retired by the amendment, not renumbered — A2 and S keep the names they were signed under.)
> **Forks resolved:** none open. `DECISIONS-NEEDED.md` for `overnight-20260806` is closed
> (ledger §T-38, decisions 153–155), and the B-145 recovery's Phase 3 rulings are recorded
> (ledger §T-39, decisions 156–159, attended 2026-08-06). Wave 0 IS decision 155 — the
> operator has already ruled what it builds; tonight builds it.
> **Workflow preflight verdict:** `openspec: absent` · `gsd: detected` · exit 0. Recorded here
> because the launch prompt's per-change mechanics clause is conditioned on it (B-100). The
> **universal-mechanics** variant is asserted; no OpenSpec scaffolding is to be created.
> **Precondition flags:** (1) 🛑 **The working tree must be clean at launch** — the launch
> prompt checks and refuses a dirty tree. (At amendment time an uncommitted Mercury
> cursor-pagination fix sat in the tree; the recovery session committed it as `4efd265` —
> `fix(receipt)`, with `cmd/backfill-receipts` — minutes later, so the tree is clean as of
> this slate's commit. The check stays: that session was still live when this slate landed.)
> (2) `hq_test_go` was repaired at morning
> triage 2026-08-06 (recreated green by `task test:go` — HANDOFF `20260806` header note).
> Moot after Wave 0 lands, since test databases move to the new container.
> **Gate ladder:** `.night-crew/knowledge/reference/gate-ladder.md` — G1, G2(Go), G2(Playwright),
> G3 (N/A — openspec absent), G4, **RF** (red-first, named gate since decision 153), G6.
> **Night budget:** none given (no `--night`). The slate is sized by its cards, not cut to a
> clock: full cards project **~2.7–4.25h** serial critical path + ~30m closeout; the stretch's
> 60–150m rides only if that lands with room. **Quota was not measured and cannot be** — the
> account's token ceiling is invisible to the planner (DESIGN §15w); the run may exhaust quota
> before the clock or the clock before quota, and spend is governed by the run's own stop
> conditions, never by these hour figures.

---

## 🛑 The two standing rules of this night

1. **Production stays untouched — the operator's explicit ruling at target confirmation
   (2026-08-06).** No leg of this run contacts the production container, the `yumyums` database,
   or **anything on the shared :5433 cluster**. Every mechanism tonight is built and proven
   against **scratch containers** or the new test container Wave 0 creates. (The backup floor
   already exists — the attended session's `task prod:backup` runs nightly at 03:30 via the
   Windows task `YumyumsProdBackup`, proven by an observed firing. Tonight's run has no reason
   and no permission to go near it.)
2. **Wave 0 lands before any other card dispatches, and before any test suite runs.** Until
   `test-cluster-separation` merges, running the Go or Playwright suite means touching the
   shared cluster — which rule 1 forbids. After it merges, every leg's test databases live on
   the new test-only container. Consequence: **tonight's first full-suite run is Wave 0's own
   proof**, executed against the container it created.

Both rules exist because of 2026-08-06: a G6 probe set `HQ_RLS_TEST_DB=yumyums` and the RLS
suite dropped the production database (B-141), with no backup to restore from (B-143). The
adopted preference **operations/P-1 — "Tests never share a cluster with production"** — is
Wave 0's mandate.

---

## What verification found before this slate was written

Every load-bearing claim was re-checked at source tonight (the slate-20260806 discipline).

1. **The shared-cluster wiring is exactly where the roadmap says.** `Taskfile.yml:92,108-110`
   default `DB_TEST_URL`/`DB_PORT` to :5433; `rowvisibility_rls_test.go:221` hardcodes
   `defaultHQAdminURL` to `localhost:5433/postgres` and `:226` `defaultFDWPort = "5433"`;
   `scripts/verify-test-harness.sh:207` builds its DEAD_URL probe on :5433. Those four sites
   plus the `task test:*` env blocks are Wave 0's re-point surface.
2. **⚠️ AMENDED — this claim was true on `dev` at planning time and false an hour later.** The
   planner verified "no `prod:backup` target, no ledger decision past 155" against the working
   tree before 08:25 and slated card A1 on it. **Concurrently, the operator's attended recovery
   session** (branch `fix/b145-prod-backup-floor`, checked out 08:24, discovered when this
   slate's commit landed on it at 09:01) **executed the B-145 handoff's Phases 0–3**: `task
   prod:backup` built and proven against real prod (restore drill green, 96 tables; globals
   dump added after 5 measured restore errors without it), Windows scheduled task proven by an
   observed firing then set nightly 03:30, PITR base via `pg_basebackup` + WAL pruning, Phase 3
   rulings recorded as **ledger §T-39 decisions 156–159**, and **B-146** filed (prod Toast sync
   silently dead since the 07-28 rebuild — SFTP key never ships). **The operator amended the
   signed slate to drop A1** and merged the branch to `dev` (`0a764a9`) under this slate.
   Lesson recorded for triage: two attended sessions against one checkout is how a slate gets
   signed on stale premises — the deciding-moment re-check caught it only because the misplaced
   commit made the collision loud.
3. **Both shipped bugs are still live.** B-89: `index.html:235` validates the `{uid, apps}`
   envelope it writes; `sync-rxdb/bootstrap.js:70-73` still `Array.isArray`-gates the raw parse,
   so `cachedGrantSlugs()` returns `[]` on every real client. B-132: `workflows.html:705`
   guards `p.life<=0` before the decrement and `:711` calls `ctx.arc` with an unclamped
   `p.size*p.life`. Neither changed since slate-20260806 §5 verified them (A5 was never
   dispatched).
4. **Spike A's environment is on `dev` and merged.** `.night-crew/qa/spike-supabase/env-up.sh`
   exists with the compose stack — spike-b builds on it as designed.

## Gate cost *(required by Q-KR4 — exactly once, and never backfilled)*

- **Full-suite baseline: ~24.2m on a quiet box** — 785 tests, `--retries=0` (run `20260803`,
  corroborated 24.5m). 🛑 **Inherited, not re-measured tonight.**
- **All three cards pay the full suite.** Every path this slate touches is undeclared in
  `[e2e.seams]` — `Taskfile.yml`, compose files, `scripts/`, `backend/internal/sync`,
  `sync-rxdb/**`, `.night-crew/qa/**` — and an undeclared path de-confines the card to the full
  run. (~72m+ of Playwright across the night, dispatch-invariant.)
- 🛑 **The global Playwright suite lock is in force** — `sync.spec.js` is load-sensitive
  (B-27/B-30/B-32 armed); no two suites in flight, no Go suite alongside a Playwright run.
  Serial dispatch makes the lock trivially satisfied, which is part of why serial won.
- **First full-suite run tonight doubles as Wave 0's proof** and re-baselines the suite on the
  new container. If its wall-clock moves materially off ~24m, record the new figure — do not
  discard the run for being slow.
- **Go suite:** `-p 1` load-bearing; `DB_TEST_URL` must be set or DB-coupled tests silently
  skip while printing `ok` — check counts, not just `ok`.

---

## Shared surfaces and serialization

| Surface | Cards | Rule |
|---|---|---|
| `Taskfile.yml` | **W0, S** | Slate order = landing order (W0 → S). W0 edits `test:*` env defaults; S adds a `spike:migration` target. 🛑 Both rebase over the attended session's new `prod:backup` stanza (merged at `0a764a9`) — do not touch it. Disjoint stanzas — conflicts, if any, resolve at merge against merge-intents. |
| Compose files (`docker-compose.nc.yml` or a new test-compose) | **W0** | Sole owner — defines the test-only Postgres service. |
| `backend/internal/sync/rowvisibility_rls_test.go` | **W0** (+ the preserved `card/a3-rls-fixture-own` branch, attended) | W0 re-points `defaultHQAdminURL`/`defaultFDWPort`. 🛑 The blocked A3 card's preserved branch edits the same file — **do not touch that branch or its worktree tonight.** W0's merge-intent must state the re-pointed defaults as what-must-survive, so the attended A3 re-gate rebases correctly. |
| `scripts/verify-test-harness.sh` | **W0** | Sole owner tonight (DEAD_URL re-point). |
| `sync-rxdb/bootstrap.js`, `workflows.html`, `tests/sync-rxdb-client.spec.js` | **A2** | Sole owner. |
| **The shared :5433 cluster (runtime)** | — | 🛑 **NO card, no gate leg, no probe touches it tonight.** Standing rule 1. |
| **The new test container (runtime)** | all cards after W0 | W0 names its service, port and volume in its merge-intent; every later leg inherits them. One suite at a time under the global lock. |
| **The Playwright full suite (runtime)** | all four cards | Global lock, trivially serial under serial dispatch. |

---

## Wave 0

### W0 · `test-cluster-separation` — 90–150m 🛑 first-of-kind (infra) · **executes decision 155, closes B-141's structural half**

**Scope.** Test suites get their own Postgres container; no test file holds admin credentials to
the cluster serving `hq.yumyums.kitchen`. Stand up a test-only Postgres service (its own port —
not 5433, not 5432 — its own volume), then re-point every default found at verification: the
`task test:*` env blocks (`Taskfile.yml:69-110`), the RLS suite's `defaultHQAdminURL` and
`defaultFDWPort` (`rowvisibility_rls_test.go:221,226`), and the harness probe URL
(`verify-test-harness.sh:207`). Red-first (gate RF): before the re-point, show one command that
demonstrates a test-path default resolving to :5433 (read-only evidence — print the resolved
URL, do **not** run the suite against it); after, show the same command resolving to the new
container, then prove the whole harness green against it (full Go suite with counts + full
Playwright suite — this is tonight's first suite run and the card's proof).

**Why Wave 0.** Standing rule 2: every later leg's gates depend on this landing first, and the
operator's prod-untouched ruling is only satisfiable once tests have somewhere else to run.

**Footprint.** Owns: `Taskfile.yml` (test env blocks), compose file(s),
`backend/internal/sync/rowvisibility_rls_test.go` (the two default constants only),
`scripts/verify-test-harness.sh:207`. Touches: `.night-crew/knowledge/BACKLOG.md` (B-141
structural-half status). **Pays the full suite** — which is also its proof.

**Estimate basis.** Run `20260806` merged cards ran 67–196m end-to-end (median 106m28s, every
card took a fix round). Harness-mechanism cards sat at the low-middle of that range; the
first-of-kind container work flags the high end. Risk-flagged → planned at the high end.

**Risk.** Playwright's `webServer` and the Go suite both read the re-pointed defaults — a missed
site fails loud (connection refused), which is the acceptable failure shape. `export
PATH="/usr/local/go/bin:$PATH"` on every leg; `go: not found` exit 127 is a PATH failure, not a
test failure.

**PARK note.** If separation turns out to require changing **production's** compose/deploy
topology (anything `task prod:deploy` reads), or a new `night-crew.toml` key — operator
question, park. Choosing the new container's name/port/volume is the card's own call.

---

## Track — serial, after Wave 0

### ~~A1 · `prod-backup-floor-and-pitr`~~ — **DROPPED by operator amendment, 2026-08-06 morning**

The card was signed at 60–120m and then discovered **already done, attended, and better than
slated** — the concurrent recovery session built it against real prod (not scratch), proved the
restore drill (96 tables), proved a scheduled firing, and added the PITR base
(`fix/b145-prod-backup-floor`, merged to `dev` at `0a764a9`; ledger §T-39). What remains of
decision 154/159 is attended-only: `archive_mode` enablement on the live cluster (decision 159,
needs a brief cluster restart with the operator present) if the session has not already done it.
**Nothing for tonight.** The roadmap card is flipped DONE in this slate's change set.

### A2 · `shipped-bug-sweep` — 70–105m · **closes B-89 and B-132**

**Scope.** Carried verbatim from slate-20260806's A5 (cut at 21:40Z, never dispatched; both
bugs re-verified live tonight). (a) **B-89** — `cachedGrantSlugs()` must read the `{uid, apps}`
envelope `index.html` writes, reject a bare array the way `index.html:235` itself does, and
verify `uid` against the identity token. 🛑 **Fix the fixture in the same commit**:
`tests/sync-rxdb-client.spec.js:1385` plants a bare array — a shape production never writes —
so the current test is green on a lie. RF must show current code failing against the real
envelope. This must land before Activity 4 arms replication; while nothing calls
`startHQReplication` it is latent, and Activity 4 is the card that changes that. (b) **B-132**
— clamp the radius (`Math.max(0, p.size*p.life)`) or decrement before the guard; then
screenshot 3s after a completed submit at 393×852 to settle how the frozen overlay actually
renders, which B-132 never established. Fires 28× per suite run today — fixing it cleans every
later gate reading.

**Footprint.** Owns: `sync-rxdb/bootstrap.js`, `tests/sync-rxdb-client.spec.js`,
`workflows.html`. Touches: nothing else. **Pays the full suite** (`sync-rxdb/` undeclared).

**Estimate basis.** Carried from slate-20260806 (70–105m); still unmeasured — the card was
never dispatched. Two-defect fix-card class, run-20260806 median 106m as the sanity ceiling.

**PARK note.** If reading the envelope correctly requires deciding **what a grant means** when
`uid` does not match the identity token — silently empty, or a hard error the crew sees — that
is a product judgment about a permissions boundary and it parks. Do not improvise it.

---

## Budget-gated stretch

### S · `spike-b-migration-rehearsal` — 60–150m 🛑 first-of-kind · **feeds D-KR1 (2nd of 4 spike verdicts)**

**Start only if** W0 + A1 + A2 have landed clean **and** S's estimate plus the ~30m closeout is
still in hand — computed, not vibes (the A5 cut on 20260806 is the precedent: decide early).

**Scope.** *Spike B — the operator's own, from the governing handoff §5.* Create one Postgres
whose schema mimics HQ's with a small subset of fields, add a data fixture, stand up fresh
Supabase + RxDB instances (building on spike A's `env-up.sh`), and **migrate the fixtured data
across** — proving HQ-shaped data actually lands in the substrate and surfaces in RxDB. This is
the leg nine nights were built on without ever testing. 🛑 **The verdict is the script's exit
status, never prose** (the spike↔script rule; B-345 clone-side). **A red verdict is a
successful spike** — record it with the script's output and stop; do not grind for green.

**Containers.** Fresh scratch containers only — its "HQ-shaped Postgres" is its own container,
never the shared cluster (standing rule 1) and never the new test container (W0's proof
substrate stays quiet).

**Footprint.** Owns: `.night-crew/qa/spike-supabase/**` (new `spike-b` script beside spike A),
`Taskfile.yml` (a `spike:migration` target). Touches: nothing else. **Pays the full suite**
(both paths undeclared) — queue it last under the lock.

**Estimate basis.** C1 `spike-a-environment-up`, the same first-of-kind spike class, planned
60–150m on run 20260806. Same wide range carried; still no measured spike actual to narrow it.

**PARK note.** If the migration mechanism requires a credential, paid account, or a decision
about **which substrate topology the project standardises on** — park. Debugging compose files
or the migration script is neither. A red verdict is not a park — it is the deliverable.

---

## Explicitly NOT in this slate

- **`gate-rls-fixture-ownership` (A3)** — BLOCKED, attended by ruling: decision 155 requires
  the B-141 prefix-guard + B-142 fixes re-gated **attended** (its G6 probe destroyed
  production). Its preserved branch `card/a3-rls-fixture-own` and worktree are **untouchable
  tonight** — W0's merge-intent carries the rebase contract instead.
- **The B-145 recovery's remaining execution** — the decision-156 Mercury backfill to
  2026-03-01 (waiting on the uncommitted cursor-pagination fix in the tree), the Toast
  re-import, **B-146**'s fix (the SFTP key that never ships in the prod image), any remaining
  decision-159 `archive_mode` enablement, and the sales-processor message (decision 158: held
  by the operator, no draft). All attended or prod-facing — standing rule 1 keeps tonight out
  of every one of them.
- **Spikes C and D, and all Activity 3–5 build cards** — spike-c is load-bearing and unrun;
  D-KR1 requires all four spike verdicts before any build card dispatches. Tonight adds at most
  the second verdict.
- **The observability cluster (B-139, B-81, B-82, B-86, B-93)** — left `new` at the roadmap
  round, off the critical path at the operator's direction.
- **B-131 and the load-sensitive flake family (B-27, B-30, B-32)** — stay armed; retired by
  diagnosis, never by passing once (decision 100; T-31 decision 120).

---

## Milestone remainder — recorded as the operator saw it at sign-off

Derived from the **live roadmap** (`.night-crew/knowledge/roadmap.md` card statuses read
tonight), not from HANDOFF prose.

**The milestone is 18 cards as written (16 original + Activity 0's 2, added at triage
2026-08-05). 6 are DONE — including `prod-backup-floor-and-pitr`, done attended this morning
and flipped in this change set. This slate takes 2 full cards + 1 stretch. It leaves the
milestone 9 cards short if the stretch runs, 10 if it is cut.** Reported in cards, not key
results.

Still not-done after tonight:

| Activity | Cards |
|---|---|
| **1 — Gate integrity** | `gate-rls-fixture-ownership` 🛑 BLOCKED — attended re-gate (decision 155) |
| **2 — Prove the route** | `spike-b-migration-rehearsal` (only if the stretch is cut), `spike-c-round-trip` 🛑 load-bearing, `spike-d-realtime-live` |
| **3 — Walking skeleton** | `skeleton-one-row-end-to-end`, `skeleton-offline-ownership-honesty` |
| **4 — Activate reads** | `activate-fill-view-reads`, `activate-list-views-or-state-they-stay-rest` |
| **5 — Dev complete** | `demo-sync-target`, `dev-complete-attestation` 🛑 attended, the operator's own act |

**Recommended additions: nothing further worth adding.** Four full-suite legs already queue
~96m+ of Playwright behind the global lock and project 4.2–6.75h before the stretch;
`spike-c-round-trip` — the only card whose promotion would change the milestone's critical path
— is load-bearing enough that a red must be read carefully, which deserves headroom, not a 3am
squeeze after two infrastructure cards. It leads the next slate instead.

---

## Execution notes

**Dispatch: SERIAL** (operator's choice at sign-off, 2026-08-06).

    W0  test-cluster-separation      ← alone; everything waits (standing rule 2)
    A2  shipped-bug-sweep
    S   spike-b-migration-rehearsal  (stretch — start only with estimate + closeout in hand)

At most one card in flight, ever. The orchestrator alone merges to the run branch, resolving
conflicts against both sides' `merge-intent.md`, logging every merge (clean ones included) in
`reference/conflicts-20260807.md`.

**Both modes' projections, as presented at sign-off (pre-amendment):** serial fit all four
signed cards in ~4.2–6.75h; concurrent fit the same list in ~3.5–5.5h at roughly 2× burn.
**Card lists identical — the choice was burn rate vs margin**, and the operator chose serial:
no clock pressure without `--night`, and the night rewires the test harness itself, where
quiet-box gate evidence is worth more than an hour of wall-clock. **The amendment only
strengthens serial** — with A1 dropped, W0 → A2 is a strict chain (standing rule 2) and there
is nothing left to parallelize before the stretch.

**Two-session split, if the night is broken up:** Session 1 = W0 (decision 155 executed).
Session 2 = A2 → S. Clean split: the sets share only `Taskfile.yml`, in disjoint stanzas.

**Run mechanics** are inherited by reference from
`.night-crew/knowledge/reference/gate-ladder.md` — gates G1–G6 + RF, the mandatory per-leg
isolation stanza, the capture-the-whole-log rule, and B-87's `tests/`-anchored Playwright
filter form. Not restated here. 🛑 This repo's ladder file is the ONLY mechanics source —
`reference/overnight-run-plan-20260707.md` has never existed in this repo (B-26, decision 138).
