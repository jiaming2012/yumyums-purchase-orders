# Slate — 20260724 (overnight run for the morning of 2026-07-24)

> **Run branch:** `overnight-20260724` (cut from `dev`; **never pushed, `main` untouched**). No
> date collision — no `overnight-20260724*` branch exists.
> **Sign-off:** batch sign-off given by the operator **2026-07-23** — this document is the record.
> **Target:** **Activity 3 tail + Activity 5 lead-in** — inferred as next-in-roadmap-order
> (`--night` given, spill rule §1): `syncspec-deflake` (Activity 3's last overnight-buildable
> card) + `grant-enforcement-parity` (Activity 5, **URGENT — blocks go-live**, T-20 decision 36),
> plus `cycle-gate`'s **computable legs as condition-gated stretch** (operator choice, this
> session).
> **Forks resolved: none open.** All `runs/*/DECISIONS-NEEDED.md` are RESOLVED (latest T-19/T-20).
> `grant-enforcement-parity` carries its operator ruling verbatim in the card (T-20 decision 36:
> grants are a DATA boundary); `syncspec-deflake` was re-aimed at T-20 with the mechanism proven
> (`reference/1198-flake-reproduction-20260721.md`). The two open items from T-19 (F2-a
> 0%-food-cost; red-negative margins) are investigations, not forks, and touch neither card.
> **Precondition flags:** **SATISFIED — DB is the ephemeral Docker env** (`docker-compose.nc.yml`,
> postgres:16, Docker-assigned port; `hq_test_go`/`hq_test_e2e` separation landed `22d85c4`).
> Baseline suite is exit-0 **with the known load-sensitive intermittents S1 exists to kill**
> (`:1198`, `:525`, LST-17, GATE-04) — no-new-reds gates stay baseline-relative. The attended
> two-device convergence check re-arms **only if S1 touches production `sync.js`** (its aim is
> test-side only). Tooling converged to night-crew **v3.0.0** (main) 2026-07-23 — the run uses
> only main-deployed features.
> **Night budget: 8h** (operator choice; `--night` was given without a duration and resolved in
> session). Serial critical path **~3h50m–6h incl. ~30m closeout** → **margin ~2h–4h10m**. The
> cut line falls **below the full queue** — nothing is budget-cut; the stretch leg is
> **condition-gated** (S1 merged clean, ≥1h in hand), not merely budget-gated.
> **🛑 Sizing disclosure (§15w):** this budget is a **wall-clock cut line sized against the
> calendar only**. The account's token quota is invisible to the planner and was **not
> measured** — the run may exhaust quota before the clock or the clock before quota; neither
> validates the other. Spend is governed by the run's own stop conditions, never by the 8h.
> **Dispatch mode (operator choice 2026-07-23): SERIAL** — G1 → S1 → (ST). Both modes carried an
> identical card list; serial was chosen because S1's deliverable **is** a flake proof and the
> 07-22 ledger lesson is explicit ("concurrency cost S1 its determinism proof for the entire
> run — serialize it or reserve the quiet window up front"). Concurrent (~3h20m–3h50m at ~2×
> burn) recorded as reference only; a rate-limit stall has nothing to degrade — the run is
> already serial.
> **Ledger basis:** `reference/card-actuals.md` — estimates + basis in §Estimates.
> **Standing instrumentation (Delivery KR3):** every card emits harness-measured per-card
> wall-clock (impl + G6 + merge legs, epoch-stamped `timings.log`), tabled in the HANDOFF.

## Target

Serial, one queue, three entries:

1. **G1 — `grant-enforcement-parity`** (Activity 5 lead-in, URGENT): migrate the 9 unenforced
   Users-tab grants onto `RequirePermission`; the grant model becomes a data boundary
   everywhere, not just Trends/Cost. → Eng KR1 · QA KR3.
2. **S1 — `syncspec-deflake`** (Activity 3 tail): test-side kill of the four named flakes;
   deliverable is a `--retries=0` streak on a **measured-quiet** box. → Eng KR5 · QA ·
   unblocks `cycle-gate`'s no-retry hard gate.
3. **ST — `cycle-gate` computable legs** (stretch, condition-gated): per-KR scorecard +
   suite-green attestation with the no-retry gate, on the tree S1 just made eligible.
   Read-only; the boundary decision itself stays attended.

## Estimates & critical path

Per-card cycle = implement + G6 + merge. ~30m closeout reserved off the top. G6 priced at the
repriced 15–65m app-code band (the 07-22 "order of magnitude" lesson), and **a revision round is
budgeted for every code card** — both 07-20c revision rounds were load-bearing (one caught a
rounding bug, one a live auth bypass).

| # | Card | Class · basis | Impl | G6 | Cycle |
|---|---|---|---|---|---|
| G1 | `grant-enforcement-parity` | authz L — basis F5 `inventory-tab-gating` (07-20c: 60m impl + 50m revision, 65m G6, ~124m cycle). This card is migration onto an EXISTING proven mechanism (no new middleware), but across 5 live app surfaces + a parity guard spec | 60–100m | 30–65m (incl. ~15m revision budget) | **~100–170m** |
| S1 | `syncspec-deflake` | de-flake L, test-side — basis D1 (07-20c: 180m on the *discovery* path, now done — diagnosis is on file) + `editprop-convergence-cell-hardening` (07-18: ~73m impl + ~16m G6). Priced with full-suite legs (~20m each) + a possible revert (D1 lesson) | 60–100m | 30–60m (streak legs dominate) | **~100–160m** |
| ST | `cycle-gate` computable legs | read-only compute — basis 07-16 gate (~5m36s subagent legs) + 07-19 gate (~4m11s scorecard); padded for the no-retry full-suite attestation leg (~20m toll) | 15–30m | — | **~15–30m** (stretch) |

**Serial critical path = G1 + S1 + closeout ≈ 3h50m–6h.** Stretch inside the 8h margin.
Concurrent reference line: ~3h20m–3h50m (identical cards, ~2× burn, streak legs still serialize).

## Shared surfaces & serialization

| Surface | Cards | Rule |
|---|---|---|
| `tests/workflows.spec.js` | S1 (GATE-04) · G1 (possible per-app gating E2Es) | Serial landing order G1 → S1; S1 implements against G1's merged tree |
| Machine load during suite legs | S1 streak vs everything | S1's determinism legs run under a **measured** load precondition (below); no other card's suite/Docker legs may overlap them |
| `backend/internal/auth` middleware (`RequirePermission`) | G1 owns | S1 must not touch backend; if it thinks it needs to, that is a park-trigger question, not an edit |
| `sync.js` / `task sw` | S1 only-if-unavoidable | Any production `sync.js` edit is declared in merge-intent, triggers `task sw`, and **re-arms the attended two-device check** (flag in HANDOFF) |

Footprint is planning information, not a fence (§15ad.65) — collisions resolve at merge against
both merge-intents; hq run precedent keeps merge-intent notes at
`.night-crew/runs/2026-07-24-autonomous/merge-intents/<card>.md` (hq has no `openspec/`).

## Cards

1. **G1 · `grant-enforcement-parity`** *(authz L, ~100–170m)* — **The Users tab offers 11
   grants; the backend enforces 2** (`inventory-trends`, `inventory-cost` — both F5's).
   (a) **Prove it live first** (red-first): enumerate every `RequirePermission` call site
   (source enumeration is the evidence), then reproduce with a real ungranted account — today it
   reads `/inventory/purchases`, `/items`, `/stock`, `/recipes`, `/menu-items` at 200.
   (b) **Migrate app-by-app** onto `RequirePermission` using F5's proven semantics (umbrella
   grants, fail-closed on DB error): `inventory` itself, `operations`, `purchasing`,
   `onboarding`, `users`. Roles remain a separate axis; the operator ruling (T-20 d.36,
   verbatim in the roadmap card) is the contract: *no grant → no view, no tab, no data.*
   (c) **Parity guard spec** (the `EXCEPTIONS`-style shape from `tests/ops-authz-coverage.spec.js`):
   derive the app list from `SeedHQApps`, assert every slug is enforced somewhere; the four
   placeholder slugs (payroll/scheduling/hiring/bi — no endpoints) are recorded **N/A-with-reason**,
   never silently skipped. Plus with-grant/without-grant E2E pairs per migrated app.
   Footprint — Owns: `RequirePermission` wiring in `backend/internal/{workflow,inventory,onboarding,users,me}`
   handler registration. Touches: new parity spec + per-app gating tests; `users.spec.js`.
   **PARK triggers:** (i) an endpoint whose grant mapping is genuinely ambiguous (which grant
   governs a cross-app read) → park **that app** with the question in DECISIONS-NEEDED, continue
   the others — the card is separable app-by-app; (ii) migration reddens an existing flow in a
   way that implies a contract question (a flow that legitimately serves ungranted users) → park
   that surface, never improvise the fork. → Eng KR1 · QA KR3.

2. **S1 · `syncspec-deflake`** *(de-flake L test-side, ~100–160m — riskiest, deliberately
   last)* — Kill the four named flakes **without touching production code**. The mechanism is on
   file (`reference/1198-flake-reproduction-20260721.md`): a stray WS catch-up re-render detaches
   the input between `fill()` and `dispatchEvent('change')`, so the op **never fires** (journal:
   +3 rows on red vs +5 on green; no `SET_FIELD`). No timeout increase can fix this.
   (a) `:1198` — settle the runner before entry; after acting, **assert the POST was observed
   and re-dispatch if not** (the fix shape the evidence doc names). (b) `:525` FLD-LIVE-02 —
   fix **as order-dependence** (fails 3/3 in isolation against a carried journal), not by
   reordering around it. (c) LST-17 and (d) GATE-04 — harden the two passed-on-retry
   observations from T-20, same discipline. Red-first on each (capture the flake mode where
   reproducible).
   **Deliverable is a `--retries=0` streak proof on a MEASURED-quiet box, not merely a green
   run** — see the load precondition in Execution notes; a streak under load proves nothing and
   must not be claimed (07-20c D1 lesson: serial dispatch ≠ idle machine).
   Footprint — Owns: `tests/sync.spec.js`, `tests/workflows.spec.js` (GATE-04). Touches:
   `sync.js`/`workflows.html` **only if a determinism seam is unavoidable** — declared in
   merge-intent, re-arms the attended two-device check.
   **PARK triggers:** (i) the journal shows the fix *relocated* the clobber again rather than
   closing it → revert to net-zero and park with the evidence (D1 precedent — the honest path);
   (ii) a production seam becomes load-bearing and raises any contract question → park.
   → Eng KR5 (determinism) · QA · unblocks `cycle-gate`'s no-retry hard gate.

3. **ST · `cycle-gate` computable legs** *(read-only stretch, ~15–30m; runs ONLY if S1 MERGED
   clean with ≥1h in hand)* — Compute, do not close: per-KR scorecard; suite-green attestation
   on the isolated deterministic stack **with the no-retry hard gate** (eligible only tonight,
   via S1); median WO cycle vs the T-14 baseline (N=23 / 22m28s, Delivery KR3). The
   **prod-parity leg (Delivery KR2) is computed honestly as PENDING-deploy** — prod frontend is
   1.0.3, `task version` will show local ahead; report it, never fabricate it (07-19 gate
   precedent: "median not computable → PARTIAL, none fabricated"). The roadmap card stays
   **PLANNED** — the milestone-boundary close-out (operator judgment on PARTIAL/pending legs +
   the prod-parity ship) is attended and belongs to triage. Footprint: read-only + one
   scorecard doc under `runs/2026-07-24-autonomous/`.

## Explicitly NOT in this slate

- **`prod-alert-dup-guard`** — an over-the-cycle Cliq-channel observation, not an
  overnight-buildable card (slate-20260721/22 precedent). Resolves at cycle end in the ledger.
- **`cycle-gate` boundary close-out + prod-parity ship** — needs an attended prod deploy
  (`/save-project` → `task prod:deploy`) and operator judgment; only the computable legs run
  tonight, as stretch.
- **F2-a 0%-food-cost and red-negative margins** — open *investigations* (T-19), blocked on
  discount data; no fork, no card.

## Execution notes

- **Serial order: G1 → S1 → (ST).** G1 first — authz work tolerates a loaded box, and early
  evening is when concurrent operator sessions are likeliest (the 07-20c D1 window was eaten by
  exactly that). S1 last — riskiest card (§2 rule), and its streak legs land in the small
  hours, the quietest window. ST only after S1 merges clean with ≥1h in hand.
- **Two-session split (if needed):** session 1 = G1 alone (backend + specs, independently
  mergeable); session 2 = S1 + ST. Either half stands on its own.
- **🛑 Measured load precondition for S1's streak legs (new, 07-20c lesson):** before any
  determinism claim, measure the box — 1-min load average (`uptime`) and a scan for concurrent
  heavy processes (other Claude/node/docker/playwright sessions). Loaded → wait or re-run the
  streak later in the night; **the claim requires the measurement in the HANDOFF**. "Serial
  dispatch guarantees this run is serial, not that the machine is idle."
- **Per-change mechanics — hq adaptation (T-20 decision 37, B-100):** hq has no `openspec/`.
  Keep what survives translation: red-first where a defect is named, atomic commits, flip the
  roadmap card in the same change set. **Merge-intent note stays REQUIRED** (§15ad.65), written
  to `.night-crew/runs/2026-07-24-autonomous/merge-intents/<card>.md` in the card's first
  commit: shared files touched, what must survive any merge, what is safe to drop.
- **Suite run-mechanics (standing):** `CI=1` on every full-suite / G6 leg; ephemeral pg16 via
  `docker-compose.nc.yml` (no host-port publish — scratchpad `ports:` override);
  `hq_test_go`/`hq_test_e2e` separation is landed — use it. Never background long runs
  (07-21 A1 lost ~25–30m); foreground ≤10m, detach+`tail --pid` beyond. `task sw` after any
  HTML/JS change. `git stash` is forbidden in worktrees.
- **Budget discipline (tokens ≠ calendar; quota was not measured):** prefer a clean early exit
  over starting a card that cannot finish cleanly. Drop from the tail: ST first, then S1's
  streak-extension — **never half-claim the proof**: either the measured-quiet streak completed
  or the determinism claim is not made and `cycle-gate` stays blocked; say which.
- **Run mechanics (standing rules, gates G1–G6)** inherited unchanged by reference from
  `reference/overnight-run-plan-20260707.md` — not restated here.
