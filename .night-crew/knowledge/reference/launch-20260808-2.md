Tonight's autonomous run. Execute the signed slate at
.night-crew/knowledge/reference/slate-20260808-2.md.

Setup: you are on branch dev.

FIRST, before anything else: `git status --porcelain` must be CLEAN. A run branch cut
over a dirty tree smuggles foreign work into the night's commits — if the tree is
dirty, 🛑 STOP and tell the operator what is sitting in it; it is not yours to commit,
stash, or discard.

Then, before any git write, CHECK FOR UNMERGED RUN BRANCHES. Run this EXACTLY as written —
it contains no placeholder to substitute, and it asks about every date, because an
unfinished night from any date is a night's work sitting unmerged:

    git branch --no-merged dev --format='%(refname:short)' | grep -E '^overnight-[0-9]{8}(-[0-9]+)?$'

STATE THE RESULT OUT LOUD BEFORE PROCEEDING — either "no unmerged run branches" or the
list. You may only proceed after saying which one you saw. An empty result is the "go"
signal, so a command that failed or ran wrong looks exactly like a clean repo unless
you have named what you found.

- No unmerged run branch → say so, then cut this run's branch. Compute its name from
  the BARE DATE, since you are looking for every branch of that date:

      git branch --format='%(refname:short)' | grep -E '^overnight-20260808(-[0-9]+)?$' | sort -V

  The bare name overnight-20260808 is TAKEN (merged, run of 2026-08-07 morning) — a
  merged branch's name is still taken, so expect overnight-20260808-2; if that is also
  taken, the next free suffix. Cut it and proceed without asking; nothing is at risk.
- ANY unmerged run branch exists → 🛑 STOP. A previous night's work is sitting on it.
  Report what is there (git log --oneline dev..<branch>, and whether
  DECISIONS-NEEDED.md carries an open section for it), then ASK THE OPERATOR to choose:
  RESUME that branch, or RECUT at the next free suffix. Do NOT pick for them — resuming
  mixes two nights on one branch, recutting walks away from a night's work, and you
  cannot tell from here which is right. This is NOT covered by the batch sign-off: the
  yes is scoped to the signed slate, and what to do with an unfinished night is not
  part of it. Whatever they answer, the existing branch is left untouched — never
  delete, reset, force, or rename it.

If the run id gains a further suffix, EVERY run-scoped name takes it along: the
conflict log .night-crew/knowledge/reference/conflicts-<runid>.md, the run directory
.night-crew/runs/<date-with-suffix>-autonomous/, and the closeout's references to the
run. The slate keeps the name it was signed under (20260808-2).

Do all work on the run branch. Never push, never touch main. Hard prohibitions in
CLAUDE.md apply.

🛑 TONIGHT'S THREE STANDING RULES (inherited from runs 20260807-2/20260808; slate
§"Precondition flags"):
1. PRODUCTION STAYS UNTOUCHED. No leg contacts the production container, the yumyums
   database, or ANYTHING on the shared :5433 cluster — no test, no probe, no psql,
   read-only included (the 2026-08-06 destruction was a G6 probe; run 20260808's
   near-miss was a bare `task backend:db-test`). 🛑 NEVER invoke any `backend:*` task
   bare — its defaults resolve :5433 (B-164). `task test:targets` is the read-only
   coordinate source. Test suites run on :5434 — yumyums-test-pg, role hqtest,
   `task test:db:up`. Isolation DB names minted in TEST_DB_NAME_PATTERN's shape:
   ^hq_test(?:_[a-z0-9]+)*$ (T-42 convention).
2. GIT-OPERATE ONLY FROM A DEDICATED RUN WORKTREE, never the main checkout (decision
   160, ledger §T-40). Immediately after cutting the run branch, create the worktree
   (e.g. under ../hq-worktrees/run-20260808-2) and do ALL run git ops there.
3. ONE SUITE IN FLIGHT, EVER — no two Playwright runs, no Go suite alongside a
   Playwright run (sync.spec.js is load-sensitive; armed reds expected: B-27
   inventory:883, [LST-17] sync:446, B-162 receipt-carousel:123 — an occurrence is
   flake-trail evidence, not a card failure; retire only by diagnosis, decision 100).
   Spike A's substrate, if any leg touches it, is consumed in RECONCILE MODE, NEVER
   DESTROY — 🛑 `--fresh` / `--fresh-substrate` are NEVER passed (B-159).
Also: the preserved branch card/a3-rls-fixture-own and its worktree are UNTOUCHABLE —
they are the attended re-gate's evidence (decision 155).

Authority: batch sign-off was given by the operator on 2026-08-07 (attended evening
session) — the slate document is the record. Do NOT pause for per-change sign-off.
Every operator fork this slate needed is resolved in ledger §T-43: (a) the Approvals
tab stays on re-fetch; (b) 🛑 the My Checklists read path is DELIBERATELY OPEN — the
operator declined to rule it, and NO CARD MAY DECIDE IT; a card that cannot proceed
without that answer PARKS; (c) concurrent multi-checklist fill work is a RECORDED
PRODUCT REQUIREMENT (setup + food-prep checklists worked simultaneously — multiple
live per-checklist fill replications ARE the design). You never choose a fork — what
to build, what a milestone means, anything only the operator can settle.

Park is NOT the default answer to a gray area. Route it through
`night-crew decisions log` and act on the verdict it PRINTED, never on an inference
from the disposition. (The binary lives at /home/jcole/go/bin/night-crew — the
non-interactive shell's PATH does not carry it.) Two verdicts, four producing states
(CLAUDE.md §"Slate workflow" step 5 is the doctrine of record):
  - `verdict: decide` under an adopted delegation policy — recorded delegated,
    `ratification: pending`, back at triage. Deciding is not settling.
  - `verdict: decide` on the citation floor with NO policy involved — owes no
    ratification. You will meet this; do not read `decide` as "a policy covered it".
  - `verdict: park` with the decree read — prints the verdict and exits 0. An
    escalation is a correct outcome, not a command failure.
  - park because the decree could not be READ — fails, exits non-zero, renders `park:`
    inside its error, and prints NO `verdict:` line at all. Grepping for
    `verdict: park` misses this one; read the exit code.
An escalation and a park are the same act, which is why there are two verdicts and not
three. A delegation converts a question that WOULD have escalated into one that
resolves; it is not the only way a question resolves — never restate this as "decide
only under a policy, otherwise park".

Rules of engagement:
- Standing rules and verification gates are inherited unchanged from
  .night-crew/knowledge/reference/gate-ladder.md — this repo's ladder: G1, G2(Go),
  G2(Playwright), G3 (N/A — openspec absent), G4, RF (red-first, decision 153), G6;
  plus the mandatory per-leg isolation stanza, the capture-the-whole-log rule, and
  B-87's tests/-anchored Playwright filter form. 🛑 That file is the ONLY mechanics
  source — reference/overnight-run-plan-20260707.md has never existed in this repo.
- Run mechanics that cost time to rediscover (CLAUDE.md §"Run mechanics"):
  export PATH="/usr/local/go/bin:$PATH" before ANY Go or Playwright leg; go test with
  -p 1; DB_TEST_URL must be SET or DB-coupled Go tests silently skip while printing ok
  — check counts, not just ok/FAIL (expect workflow=35-shaped counts);
  HQ_SYNC_SUBSTRATE_OPTIONAL and HQ_SYNC_GATE_CHILD attested UNSET in gate logs. 🛑
  `npx bddgen` BEFORE every Playwright gate leg (B-165 — without it the suite silently
  runs 797 of 798 and looks clean). Full-suite baseline is ~21–26m; a wall-clock in
  that band is normal, do not re-arm on it. Gate-log hygiene from T-41: capture the
  command + env attestation IN the log artifact.
- Model routing (carrying the 20260807-2/20260808 pattern; planner's call, stated):
  the orchestrator (you) and the G6 adversarial reviewer run on Fable. Implementers:
  Opus for C2 and C3 (first-of-kind wiring and the concurrency requirement — not a
  bounded-scope lane); Sonnet acceptable for C1, S1, S2 (bounded, well-anchored).
- Dispatch model: you are the control loop, not the implementer. For each card:
  create a worktree off the run branch, spawn a FRESH subagent scoped to exactly that
  card (paste its full slate entry, footprint, PARK note, and — for C2/C3/S2 — spike
  E's condition verbatim: trusted checkpoint catch-up is valid ONLY while the relay
  stays trigger/NOTIFY-driven; a polling relay on a business watermark reintroduces
  the missed-UPDATE hazard. The subagent drafts, implements, runs G1–G4 + RF in its
  worktree, and returns a compact report), then spawn a SEPARATE fresh subagent for
  the G6 adversarial review, then merge to the run branch and archive yourself. Never
  implement a card in your own context; your context holds reports, not diffs.
  Subagents cannot spawn subagents — G6 dispatch is always yours. A failed card PARKS
  per the slate rules (worktree preserved, DECISIONS-NEEDED.md) — dispatch the next
  card. Editing a file outside the card's footprint is NOT a park (§15ad.65).
  🛑 G6 probes obey tonight's standing rule 1 absolutely: no probe may resolve any
  database name against :5433. The 08-06 incident was a G6 probe.
- Order: SERIAL — dispatch one card at a time in slate order:
  C1 skeleton-offline-ownership-honesty (the object-level guard lands FIRST — its
  acceptance: with the sync flag off, page load leaves window.HQSync.db undefined,
  asserted against the RUNNING page, and the three source-text assertions are
  replaced, closing B-88) →
  C2 skeleton-one-row-end-to-end (first production call site; decision 126's shape
  verbatim and cited; behind an explicit flag; must satisfy C1's guard with the flag
  off) →
  C3 activate-fill-view-reads (per-open-checklist scope, decision 105; 🛑 T-43c's
  concurrency requirement with the two-concurrent-fill regression test) →
  [STRETCH GATE] S1 list-views-decision-recording →
  [STRETCH GATE] S2 demo-sync-target.
  Stretch gate arithmetic (compute and LOG it at the decision moment, 20260806's
  A5-cut discipline): start a stretch card only if its HIGH estimate (S1 80m, S2
  110m) plus the 30m closeout still fit inside the 8h line. Prefer a clean early
  exit over a card you cannot finish cleanly.
- Per-change mechanics — conditioned on the slate's recorded workflow-preflight
  verdict for this repo (openspec: absent): do NOT assert OpenSpec mechanics and do
  NOT create openspec/ scaffolding in this repo. Assert only the universal mechanics:
  red-first evidence (gate RF — the failing test captured BEFORE the code that passes
  it, exit code in the log), atomic commits, the commit trailer
  `Night-Crew-Run: 20260808-2` on every commit, and the roadmap/status flip for the
  card in the same change set. Which per-change discipline this repo should adopt was
  answered: branch-and-commit, no OpenSpec (decision 140).
- Persistence rule (CLAUDE.md, binding on C2/C3): every user-entered value flows
  debouncedSaveField → submitOp('SET_FIELD') → POST /ops. There is NO autoSaveField
  (B-65). Tonight's cards change READS only — /saveResponse and /submit keep owning
  writes (decision 126) — and the back-and-reopen persistence tests must stay green.
- Merge-intent note (REQUIRED, §15ad.65): each subagent writes
  .night-crew/runs/2026-08-08-2-autonomous/merge-intents/<card-slug>.md in its FIRST
  commit, before implementing — shared files touched (each file outside its own
  packages, one line of why), what must survive any merge, what is safe to drop, AND
  the card's `## Red-first` section (gate RF reads it there). Empty fields say
  "nothing here" explicitly. C1's must state the flag-off contract C2 will satisfy;
  C3's must state what must survive in workflows.html against S1's banner edits.
  Paste this requirement into every implementer prompt.
- Conflicts, not fences (§15ad.65): the footprint is planning information, not a
  boundary — a card MAY edit any file, and doing so is not a breach and not a park.
  Collisions surface as ordinary git conflicts; serial dispatch should make them
  rare. YOU resolve any at merge by reading BOTH sides' merge-intent.md and resolving
  against intent, not text; then re-run G1+G2. PARK the later card ONLY when, having
  read both intents, you still cannot determine which behavior is correct. An
  unplanned lifecycle row, a change to the escalation rules, a ceiling hit, ANY
  answer to the My Checklists read path (T-43b), or any other operator-only question
  still PARKS. Never improvise forks.
- Conflict log (REQUIRED, §15ad.66): append an entry to
  .night-crew/knowledge/reference/conflicts-20260808-2.md for EVERY merge, clean or
  conflicted — the card, files and hunks, the intents you read, the resolution, and
  the gate result after it. Clean merges get a one-line entry, so an empty log never
  reads as "no conflicts" when it means "the logging never ran". Commit it with the
  merge. Summarize it in the closeout; morning triage audits it.
- Budget discipline: the slate is sized against the CLOCK ONLY (8h wall; quota was
  not measured and cannot be). Each card's diff in workflows.html costs a full ~26m
  Playwright suite — priced in, once per card. B-39 stamps are MANDATORY: log
  implement-start/end, G6-start/return, fix-return, merge per card in timings.log so
  card-actuals stays measured, not narrated.

Closeout (ALWAYS, leave ~30 min): update HANDOFF.md (per-card outcomes table, gate
evidence on the final tree, next actions) and DECISIONS-NEEDED.md (anything parked),
in .night-crew/runs/2026-08-08-2-autonomous/, commit as docs. The morning reader is
the operator — write for them. Name explicitly: whether the skeleton exists (C2 —
the milestone's spine), whether the fill view reads from sync with the two-checklist
concurrency test green (C3), what the stretch gate decided and its arithmetic, that
the My Checklists read path remains the operator's open question (T-43b), and the
attended work still waiting: the A3 re-gate (decision 155), the decision-156 Mercury
backfill, B-146's SFTP key fix, the decision-159 archive_mode residual, the
decision-158 sales-processor message, B-145 recovery Phase 1, and — once the demo
target exists — the operator's own `task demo:sync` attestation run (the close bar).

Your FINAL CHAT MESSAGE is a hand-off, so CLAUDE.md §"Context checkpoints at
hand-offs" binds it. Do not end on a results summary. End with, in this order:
1. Next steps, led by morning triage — what is waiting, what is parked, what the
   operator has to decide. State how far the skeleton chain got and what that means
   for the demo/attestation cards.
2. The context-checkpoint verdict with its reason. For a finished run it is
   🔄 Clear now (recommended) — /nc-morning-triage is designed to start cold and
   reviews the run branch on its merits; nothing in your context is load-bearing
   because HANDOFF.md, DECISIONS-NEEDED.md, and the conflict log carry it all. Say
   ⛔ instead only if something real is still in flight in this session.
3. The paste-ready next command: /nc-morning-triage
Writing HANDOFF.md does NOT discharge this. The file and the closing message are
different surfaces read at different moments, and the operator reading the last
thing on screen at 7am is the one this is for.
