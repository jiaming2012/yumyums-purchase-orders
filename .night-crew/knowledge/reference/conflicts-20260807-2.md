# Conflict log — run `overnight-20260807-2`

Every merge to the run branch gets an entry, clean ones included — an empty log must never
read as "no conflicts" when it means "the logging never ran".

## §1 — C · `spike-c-round-trip` — CLEAN merge

- **Merge:** `card/c-spike-c-round-trip` → `overnight-20260807-2` at `76801aa` (--no-ff, ort), zero conflicts — 16 files, all additions (8261 insertions, no modified shared files except `Taskfile.yml`, which gained only the disjoint `spike:roundtrip` / `spike:roundtrip:red` stanzas the slate assigned).
- **Intents read:** C's merge-intent (`merge-intents/spike-c-round-trip.md`) — first merge of the night, no sibling intent to weigh.
- **Verdict carried:** **GREEN** — LISTEN/NOTIFY relay mechanism proven, round trip 248 ms (implementer) / 136 ms (G6 re-execution) against a 20 000 ms bound. 3rd of 4 D-KR1 spike verdicts.
- **G6:** MERGE WITH NOTES, verdict survived adversarial re-execution (red `--no-relay` exit 1, green exit 0, could-not-run exit 2/64 all reproduced). Notes carried to HANDOFF: (1) `tests/inventory.spec.js:3577 FR-11` failed on its UI half at whole-suite position and is on **no** armed list — candidate new flake handle for triage (card cannot have caused it: diff has zero frontend/spec files); (2) the `HQ_SYNC_SUBSTRATE_OPTIONAL`/`HQ_SYNC_GATE_CHILD`-unset evidence line lives only in a commit message, not a log artifact; (3) `card-c-g2-bddgen.log` is zero bytes; (4) `rxdb/spike-c-read.js:165` checks `replErrors` only pre-write — a post-write infra death could mislabel exit 1 "mechanism disproven"; inherit to the cutover card.
- **Gate result after merge:** G4 re-run on merged HEAD (see below); G1+G2 not re-run — clean merge, no conflicted hunks, rule requires re-run on conflict only.
