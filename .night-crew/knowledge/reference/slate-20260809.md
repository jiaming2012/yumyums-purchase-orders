# Slate — run `20260809` (night of 2026-08-08 → morning 2026-08-09)

**Run branch:** `overnight-20260809` — the bare date `20260809` is free (no branch of any
suffix exists; no unmerged run branch on any date). No collision, no suffix.
**Sign-off:** GRANTED 2026-08-08 evening (operator, this session) — 2 cards (demo +
B-163 companion). See §Sign-off record.
**Forks resolved:** none newly required. The sync read-path rulings this milestone runs on
are already recorded — ledger **T-42** (trusted-checkpoint catch-up valid only while the
relay stays trigger/NOTIFY-driven) and **T-43(a)–(c)** (Approvals stays REST; My Checklists
read path deliberately OPEN; concurrent multi-checklist fill is the design). The demo's
read-surface choice (fill view vs the C2 `#sync-one-row` dev surface) is an **engineer-level
decision, taken here, not an operator fork** — see the card scope. **Dispatch mode: SERIAL**
(operator, this session — demo first, B-163 priority-gated behind it; both share the Spike A
substrate stack, so concurrent bring-up risks moving ephemeral ports underneath each other).
**Workflow-preflight verdict (by DIRECT inspection — this repo's `night-crew` binary lacks
the `workflow preflight` verb; recorded per the slate-20260808-2 precedent):**
`openspec: absent` — no `openspec/` directory; `openspec list` errors *"No OpenSpec changes
directory found."* `.planning/` present (gsd telltale — deliberate, real state is
`.night-crew/knowledge/`). **The launch prompt asserts universal mechanics only** — no
OpenSpec scaffolding, no GSD mechanics; per-change discipline adoption remains B-105, an
operator question. This is NOT the `undetermined` STOP case: absence is established by two
independent probes, not by a failed path read, and every prior run in this repo (15+) carried
the same `absent` verdict.

## What this night is

**The milestone close-bar night.** After run `20260808-2` the "Sync, dev complete" milestone
has exactly one unattended-buildable card left: **`demo-sync-target`** (Activity 5), skipped
last night on stretch-gate arithmetic alone (140m needed > 134m remaining, logged 20:22:59).
Everything else in the milestone is DONE, attended-by-design (`dev-complete-attestation`), or
BLOCKED on an attended re-gate (`gate-rls-fixture-ownership`, decision 155). This slate builds
the close-bar deliverable so the only thing between here and milestone close is the operator's
own `task demo:sync` run.

**Second card (companion, same footprint): `spike-exit-code-honesty` (B-163).** The demo
card's own charter carries a 🛑 hard requirement — *"could not run" must render as an outcome
DISTINCT from "ran and failed."* That is precisely the B-163 contract (exit 2 vs 1 vs 0), and
B-163 fixes the three latent exit-code conflations still live in the sibling `spike-e` script.
Landing both in one night gives the whole spike+demo family **one consistent honest exit-code
contract** rather than the new script honoring it while the old one still lies. B-163 is below
the §15k architecture-blocking promotion bar and is pulled forward deliberately — see
§Explicitly NOT / recommended-additions for the honest framing and the operator's call at
sign-off.

## Estimates and critical path (wall-clock ONLY)

🛑 **Sized against the calendar and nothing else.** No `--night` was given, so there is no cut
line — these estimates exist for the attention budget and the companion-card priority gate.
The account's token quota — tier, ceiling, current consumption, reset window — was **NOT
measured and cannot be measured from here.** The run may exhaust quota before the clock or the
clock before quota; neither validates the other. Spend is governed by the run's own stop
conditions. A rate-limit stall degrades gracefully (finish the in-flight card, closeout).

End-to-end = implement + G6 + any fix round + merge.

| # | Card | Class | Estimate (end-to-end) | Basis |
|---|---|---|---|---|
| 1 | `demo-sync-target` | 🅢 spike-script + integration, **first-of-kind demo** | **60–110m** — risk-flagged, use 110 | 🅢 class actuals 59–65m (spikes C/D/E, runs 20260807-2 / 20260808) + Taskfile `demo:` integration + first-of-kind demo margin + one fix-round allowance |
| 2 | `spike-exit-code-honesty` (B-163) | S, red-first exit-code fix, spike-only footprint | **35–60m** | small red-first fix cards ~15–30m impl (07-14/07-17 W-4/5/6) + spike-harness familiarity + 3 distinct exit-code probes; no full Playwright suite (spike-only footprint, verdict = exit codes) |

**Critical path (serial):** demo 60–110m → B-163 35–60m → ~30m closeout. High end ≈ **200m
(~3h20m)**; mid ≈ **~150m (~2h30m)**. Comfortable on any reasonable night. **B-163 is
priority-gated behind the demo** (the demo is the milestone deliverable; B-163 is the
companion): dispatch B-163 only after the demo card completes and only if its high estimate +
the 30m closeout are still in hand — the standard stretch-gate arithmetic, computed at the
decision moment.

## Precondition flags (carried from slate-20260808-2, still in force)

- `task sandbox:e2e` — **no such target exists in hq**; the template's attended-sandbox
  precondition is N/A here, stated rather than silently skipped.
- 🛑 `npx bddgen` before every Playwright gate leg (B-165 — without it the suite silently runs
  one short and looks clean).
- 🛑 **Never invoke any `backend:*` / `test:*` task bare against a default coordinate** (B-164 —
  defaults resolve `:5433`, the **production** cluster; that is how prod was destroyed
  2026-08-06). `task test:targets` is the read-only coordinate source; all test coordinates are
  **:5434** (`yumyums-test-pg`, role `hqtest`).
- Isolation DB names minted in `TEST_DB_NAME_PATTERN` shape: `^hq_test(?:_[a-z0-9]+)*$`
  (`scripts/reset-e2e-db.js`; T-42 convention).
- `export PATH="/usr/local/go/bin:$PATH"` before any Go or Playwright leg (non-interactive
  shell drops Go; `go: not found` looks like a test failure and is not).
- `HQ_SYNC_SUBSTRATE_OPTIONAL` and `HQ_SYNC_GATE_CHILD` attested UNSET in any G2 log.
- **The spike/demo stack is stateful — `spike:down` is the ONLY data-destroying target
  (Taskfile note at :301).** The demo brings up Spike A's environment in reconcile mode
  (`spike:up` / `env-up.sh`), never `spike:down`, so a re-run never destroys a prior stack.
- Armed reds EXPECTED in any full Playwright run — `tests/inventory.spec.js:883` (B-27),
  `tests/sync.spec.js:446` ([LST-17], armed per T-42), `tests/receipt-carousel.spec.js:123`
  (B-162). An occurrence is flake-trail evidence, not a card failure; retire only by diagnosis
  (decision 100).

## Shared surfaces and serialization

| Surface | Cards touching it | Rule |
|---|---|---|
| `Taskfile.yml` | 1 (`demo:sync` target, new); 2 (`spike:reconnect:red` wrapper note) | Bounded overlap — different stanzas. Serial landing (demo first) resolves cleanly; each writes its own target/note. |
| `.night-crew/qa/spike-supabase/` | 1 (new `demo-sync.sh` + any `rxdb/` helper); 2 (`spike-e-reconnect.sh` + `rxdb/spike-e-reconnect.js`) | Disjoint at file level — card 1 adds files, card 2 edits existing `spike-e` files. No overlap. |
| `workflows.html` | 1 (READ only — the demo drives the running app; no source edit) | No source change ⇒ no precache delta (stays 31) and, absent a source edit, no `night-crew.toml` seam trigger for the full suite. Check the footprint map per card (standing rule). |
| Spike A env (`env-up.sh`, `spike:up` stack) | 1 (consumes, reconcile mode) | Never `spike:down`. Idempotent bring-up. |

## Wave 0 / serial order

No Wave 0 — no cross-cutting refactor. Two cards, serial, priority order: the deliverable
first, its companion second.

### Card 1 — `demo-sync-target` (Activity 5) · the milestone close bar

**Scope (2–4 lines):** Ship `task demo:sync` as a **first-class deliverable**: a scripted-fresh
environment (Spike A's stack, `env-up.sh` / `spike:up`, reconcile mode), **one field written
through the real write path** (`POST /api/v1/workflow/saveResponse`), surfacing in an
**RxDB-served read on one real checklist**, round-trip. Reuses Spike C's round-trip harness
(LISTEN/NOTIFY relay → PostgREST service-identity write → RxDB Realtime pull, proven at 248ms).
Non-zero exit on any failed leg; 🛑 **"could not run" renders as an outcome DISTINCT from "ran
and failed"** — exit **2** (infra/precondition failure, no verdict) vs exit **1** (ran and the
round-trip failed) vs exit **0** (green). Gate on the **script directly**, not the `task`
wrapper (which returns go-task's 201 on a failing command — B-163's lesson, applied here from
the start).
**Read-surface decision (engineer-level, recorded):** exercise the **real fill-view RxDB read
path** (C3, `hq_sync_read` flag ON for the demo run) on one real checklist — that is what the
operator's acceptance criterion means (*"the sync capability running in my dev environment"*).
**Fallback:** if scripting the full fill view proves too heavy for a clean demo, fall back to
C2's purpose-built `#sync-one-row` dev surface (also behind `hq_sync_read`) — both satisfy the
roadmap's close-bar letter ("one real checklist", "RxDB-served read"). The fallback is an
in-card engineering choice, not a park.
**Carries verbatim at the call site:** decision **126** (RxDB serves READS; `/saveResponse` +
`/submitChecklist` keep owning ALL writes), decision **105** (scoped read, never whole), and
spike E's condition **T-42** (no polling, no business-watermark resync — the relay stays
trigger/NOTIFY-driven).
**Footprint:** owns `.night-crew/qa/spike-supabase/demo-sync.sh` (+ any `rxdb/` helper) and the
`Taskfile.yml` `demo:sync` target; touches `workflows.html` (READ — drives the app), consumes
the Spike A stack. Check `night-crew.toml` seams: a no-source-edit-of-`workflows.html` card
likely needs no full Playwright suite — the demo's own green/red run IS the verdict — but honor
whatever the seam map returns.
**Risk notes:** first-of-kind demo deliverable (new script class combining Spike A env + Spike
C round-trip + a real checklist + the tri-state exit contract). The milestone rests on it —
budget the high end and a fix round.
**PARK note (narrow):** needs a scope widening beyond decisions 105/126, a substrate schema
change, or an answer to the My Checklists read path (operator kept it OPEN, T-43(b)) → park.
A failed demo leg is a **finding, not a park** — the whole point of the tri-state exit is that
"ran and failed" is a first-class recordable outcome.

### Card 2 — `spike-exit-code-honesty` (B-163) · companion, same footprint

**Scope (2–4 lines):** Fix the three latent exit-code conflations in `spike-e-reconnect.sh` so
an infra failure can never read as "ran and failed": (a) guard the unguarded `$(srcpsql …)`
assignments (`:411,463-464,552-554`) under `set -euo pipefail` with `|| die cannot_run` →
exit **2**; (b) `:564` — `CLIENT_RC=0` with `B_ROWS≠1` currently calls `red` (exit 1) where the
honest code for "green was vacuous, no verdict" is exit **2**; (c) wrap the client leg's main
in `rxdb/spike-e-reconnect.js` in a catch that exits **2** on an uncaught exception (else Node
exits 1 → false RED). Plus document that gating must be on the **script**, not
`task spike:reconnect:red` (which exits 201, not the script's code).
**Red-first (the test IS the exit code):** force each path and capture the WRONG code before
the fix — an infra failure (missing psql / bad coordinate) exiting 1, a vacuous-green
(`B_ROWS≠1`, `CLIENT_RC=0`) exiting 1, an uncaught JS exception exiting 1 — then GREEN (each
exits 2) after. The recorded spike-E verdict does **not** depend on these (per B-163), so no
re-run of the full live spike is required for correctness — the exit-code probes are the
regression.
**Footprint:** owns `.night-crew/qa/spike-supabase/spike-e-reconnect.sh` and
`.night-crew/qa/spike-supabase/rxdb/spike-e-reconnect.js`; touches `Taskfile.yml`
(`spike:reconnect:red` wrapper note only). Spike-only footprint — no full Playwright suite.
**PARK note (narrow):** none beyond the standing set. If forcing a path red-first turns out to
need the live substrate stack (the assignments read from `srcpsql`), the probe may be scripted
against a deliberately-missing coordinate rather than a full stack bring-up — that is the
red-first, not a park.

## Explicitly NOT in this slate

- **`gate-rls-fixture-ownership`** (Activity 1) — BLOCKED, **attended re-gate by ruling**
  (ledger decision 155; its G6 probe destroyed the production DB on 2026-08-06). Branch
  `card/a3-rls-fixture-own` + worktree preserved; the run must not touch them. Off the sync
  close-bar critical path — it is Activity-1 gate hygiene, not part of the demo close bar.
- **`dev-complete-attestation`** (Activity 5) — attended and the operator's own act, by the
  roadmap's definition. No overnight run can perform it. It is the close bar itself.
- **Deciding the My Checklists read path (T-43(b))** — the operator explicitly kept it open.
- **Other backlog promotions: none beyond B-163.** The observability cluster (B-139, B-81/82/86/93)
  stays off the critical path at the operator's direction (roadmap round 2026-08-05); the
  [FILL-01] fixture-pollution artifact (HANDOFF 20260808-2 §Next-actions 2) is a B-entry filing,
  not a card; the carried G6 observations (C2 F-4 URL-override persistence, C3 O-1/O-2, S1 F-2,
  C3's `RXDB_FILL_RESPONSES` checklist-dimension bound — HANDOFF §Next-actions 3) are all below
  the §15k bar and are next-roadmap candidates.

## Milestone remainder (as shown at sign-off — from LIVE roadmap state, not HANDOFF prose)

**Milestone cards still white/blocked after this slate, by name:**
- `gate-rls-fixture-ownership` — BLOCKED, attended (excluded above); not part of the sync close bar.
- `dev-complete-attestation` — attended, the operator's own `task demo:sync` run + ledger line.

**Plain statement:** this slate does **not** close the milestone, and cannot by design — the
close bar is the operator personally running `task demo:sync` and seeing it pass (roadmap
lines 36–41). **Best case (both cards land): the milestone is left ONE card short — 
`dev-complete-attestation`, attended-by-design — and the demo it runs will exist.** After
tonight, the *only* thing standing between here and milestone close is the operator's own
attestation run (plus the separately-tracked A3 attended re-gate, which is Activity-1 gate
hygiene, not the sync close bar). Reported in cards, not KRs, per the sign-off rule.

**Recommended additions:** `spike-exit-code-honesty` (B-163) — **already in this slate as
card 2**, pulled forward deliberately. Honest framing: it is a hardening item BELOW the §15k
architecture-blocking bar, whose normal home is the next roadmap round. It is pulled forward
here because (1) it is the exact tri-state exit contract the demo card must implement anyway,
so landing both gives the spike+demo family ONE consistent contract; (2) it is in the identical
footprint (`.night-crew/qa/spike-supabase/` + `Taskfile.yml`); (3) the §15k re-sequencing
concern is moot at a milestone boundary — there is no downstream build order for its insertion
to disrupt; and (4) explicit operator feedback flags under-packing as the standing failure
mode. The operator confirms or drops it at sign-off (§5 assigns a backlog promotion to the
operator). **Beyond B-163: nothing worth adding** — every other remaining card is
attended-by-design, blocked on an attended re-gate, or below the promotion bar with a home in
the next roadmap.

## Execution notes

- **Single-session serial order:** demo → [stretch gate] B-163 → closeout. Stretch-gate
  arithmetic on B-163: dispatch it only if its HIGH estimate (60m) + 30m closeout fit before
  any operative time line, computed and logged at the decision moment (20260806 A5-cut
  discipline).
- **Two-session split, if ever needed:** session 1 = demo alone (it is the deliverable and
  leaves a coherent tree — `task demo:sync` exists, default flag OFF, nothing else touched);
  session 2 = B-163. The seam is after the demo lands.
- **Run mechanics inherited by reference** from `reference/gate-ladder.md` — this repo's ladder
  (G1, G2(Go), G2(Playwright) *where a seam requires it*, G3 N/A, G4, RF, G6) and its isolation
  stanzas. 🛑 `reference/overnight-run-plan-20260707.md` has never existed in this repo (the
  launch-20260808 prompt records this); the ladder file is the only mechanics source.
- **G4 discipline greps: write `N/A-VACUOUS`, never `clean`/`PASS`** (B-14 — neither
  `internal/journal` nor `internal/workorder` exists in hq).
- **No idle-waiting on background jobs** (HANDOFF 20260808-2 process note): foreground legs
  ≤10m; detach + `tail --pid` beyond; a turn ending kills a backgrounded process (S1 lost ~15m
  to exactly this).

## Sign-off record

Presented per CLAUDE.md Slate workflow step 3 (four plain-English headings + parallel plan +
attention budget + milestone remainder) in the planning session of 2026-08-08 evening;
operator answered **"Yes — 2 cards"** (demo close-bar + B-163 companion). Fork resolutions:
none newly required (T-42/T-43 already recorded; demo read-surface decided at engineer level
above). Dispatch mode: **SERIAL**, chosen with the recommendation and its one-line reason on
screen.
