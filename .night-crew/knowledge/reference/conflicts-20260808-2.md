# Conflict log — run 20260808-2

Every merge gets an entry, clean or conflicted, so an empty log never reads as "no
conflicts" when it means "the logging never ran" (§15ad.66).

## C1 — card/c1-skeleton-offline-ownership-honesty → overnight-20260808-2

- **Merge:** CLEAN (no conflicts). Card branched from the run branch tip (549e83a); run
  branch had not moved. `--no-ff` merge.
- **Files:** tests/sync-rxdb-client.spec.js; roadmap.md + BACKLOG.md (B-88 closure);
  merge-intent + 16 gate logs under runs/2026-08-08-2-autonomous/.
- **Intents read:** C1's only (first merge of the night; no counterparty).
- **G6 verdict:** PASS-with-findings (F1 evidence-quality: RF probe B proved an
  impossible synchronous violation shape — G6 re-proved the realistic async violation
  reds 3/3 via the IndexedDB assertion; F2 storage-scoped guard, note for C2 charter;
  F3 "the sync flag" mechanism defined nowhere yet — C2 must define it; F4 G4
  tree-clean was asserted not evidenced, verified true by G6's own run). Orchestrator
  ruling: merged WITHOUT a fix round — acceptance met and independently re-proven by
  execution; F1–F3 carried into C2's dispatch prompt. Full G6 report:
  runs/2026-08-08-2-autonomous/c1-g6-review.md.
- **Gate result after merge:** content identical to the card tip the gates ran on
  (fast-forward-shaped merge, no divergent hunks) — G1/G2 evidence carries; G4
  re-verified on the merged tree (idempotent, precache 31).

## C2 — card/c2-skeleton-one-row-end-to-end → overnight-20260808-2

- **Merge:** CLEAN (no conflicts). Card branched from run tip fdfd867 (C1 merged); run
  branch had not moved. `--no-ff` merge, 19 files.
- **Files:** sync-rxdb/bootstrap.js (flag `hq_sync_read` + first production call site),
  workflows.html (additive dev surface `#sync-one-row`), tests/sync-one-row.spec.js
  (new), night-crew.toml (roll-call comment only, verified 0 non-comment lines),
  tests/repo-hygiene.spec.js (count 9→10), sw.js, roadmap flip, merge-intent + 10 gate
  logs.
- **Intents read:** C2's; C1's flag-off contract cross-checked — C2's synchronous
  flag-off throw satisfies it (B-88 guard re-run green by G6 on the card tree).
- **G6 verdict:** PASS-with-findings (F-1 moderate: rejected databasePromise/openScopes
  entry cached forever, no eviction — PLAUSIBLE by code reading; F-2 moderate: fill
  scope carries no user identity, shared-device checkpoint hazard — pre-existing shape
  made reachable; F-3 minor: RF red leg ran a ~7-line-earlier spec revision, mitigated
  by final-tree green; F-4 observation: URL override persists by design). Orchestrator
  ruling: merged WITHOUT a fix round — no acceptance defect; **F-1 and F-2 are carried
  into C3's dispatch prompt as requirements** (C3 owns the open/cancel lifecycle both
  sit on). Full G6 report: runs/2026-08-08-2-autonomous/c2-g6-review.md.
- **Gate result after merge:** content identical to card tip (fast-forward-shaped);
  G1/G2 evidence carries; G4 re-verified on merged tree below.

## C3 — card/c3-activate-fill-view-reads → overnight-20260808-2

- **Merge:** CLEAN (no conflicts). Card branched from run tip 04c6703 (C1+C2 merged);
  run branch had not moved. `--no-ff` merge, 34 files.
- **Files:** workflows.html (fill-view RxDB overlay + HQFillSync lifecycle),
  sync-rxdb/client.js (F-2 userId on fill scope), sync-rxdb/bootstrap.js (F-1
  rejection eviction), tests/sync-fill-view.spec.js (new, FILL-01..05),
  tests/sync-rxdb-client.spec.js (SCOPE-05), mechanical userId updates to two C2
  specs, night-crew.toml roll-call 10→11 (comment-only) + repo-hygiene count, sw.js,
  roadmap flip, merge-intent + 21 gate logs.
- **Intents read:** C3's (includes the required S1-facing clause naming the banner
  lines and workflows.html hunks that must survive S1's edits); C2's cross-checked —
  C3 discharged C2's G6 F-1 (rejection eviction, tested via seam) and F-2 (userId in
  fill scope fingerprint, a narrowing — decision 111's four rows untouched).
- **G6 round 1:** PASS-with-findings — F-A CONFIRMED TOP (fill overlay applied
  resident rows from a DIFFERENT submission; probe forced "1 of 1 complete" on a
  blank checklist), F-B (show(2)/show(3) missed close), F-C (close-then-fast-reopen
  race, PLAUSIBLE), F-D (RLS-is-the-gate prose oversold for drafts).
- **Fix round (orchestrator-ordered, the slate priced one per app-code card):**
  red-first 276068b (3 failed pre-fix, "2 of 2 complete" on blank checklist captured),
  fix 3e4397d (acceptedFillDocs: only this submission's rows + my NULL drafts;
  close on every tab switch; handle-identity cancel guard; prose corrected), docs
  a4d1ccc. Full suite on final tree: 811/0/6 EXIT=0 — fully clean; GLB-01
  (workflows:3909) passed, retiring round 1's single red as the flake it was reported
  as.
- **G6 re-verification (same reviewer, original probe re-run):** PASS. Probe-1
  (foreign-submission resident row) now renders 0-of-1; Probe-2 (REST parity —
  teammate-answered row of the OPEN submission still renders) passes. Two
  observations only (O-1 overlayKeys naming; O-2 FILL-04 positive-half coverage),
  riding this log, not a card.
- **Gate result after merge:** content identical to card tip (fast-forward-shaped);
  G1/G2 evidence carries; G4 re-verified on merged tree below.
