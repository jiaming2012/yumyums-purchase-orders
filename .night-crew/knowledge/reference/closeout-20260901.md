# Closeout — run `20260901`

**Closed:** 2026-08-31 20:29 EDT (closeout written in the same commit as the HANDOFF finalization).
**Run branch:** `overnight-20260901` (off `dev` @ `55aa6f8`; final tip after this commit). NOT merged to `dev` — triage's act.

## What landed — 11 of 11 cards, 0 parked, 0 operator forks

Concurrent 3-track dispatch, subagent-per-card + fresh-context G6 per card, orchestrator-only merges.
Merge order (landing order) and SHAs:

1. Card 9 `period-summary-visibility` — `4c387cb`
2. Card 1 `receipt-worker-correctness` — `0e988ce`
3. Card 10 `counterparty-notice-prep` — `4c8f431`
4. Card 2 `toast-sync-fail-loud` — `d2120c8`
5. Card 4 `client-guard-coverage` — `5d96274`
6. Card 11 `deploy-hygiene-honesty` — `8dcf506`
7. Card 3 `toast-ingest-resurrection` — `4bbb7dc`
8. Card 5 `cdc-single-fire` — `3359dee`
9. Card 6 `sync-dev-one-command` — `3b90709`
10. Card 7 `sync-doc-honesty` — `29793e9`
11. Card 8 `app-slug-association` — `809e15d`

Roadmap: all 11 DONE; `pipeline-fail-loud` DONE (both split halves); `counterparty-combined-notice`
stays PLANNED (closes on the operator's notice SEND).

## Final gate result (on `809e15d`)

- **G1:** `go build ./...` + `go vet ./...` exit 0.
- **G2-Go:** every test-bearing package `ok` EXCEPT `internal/sync/TestJWTBridgeRLS` = the B-178
  Spike-C-relay contamination env red (reproduced on base, no card touches the fixture).
- **G2-Playwright:** one summary block, **823 passed / 8 failed / 6 skipped**. 4 failures = documented
  deterministic baseline (B-174 ×3 + B-176); 4 = load-flakes all proven GREEN in an isolated re-run.
  No real new red.
- **G4:** `node build-sw.js` idempotent on the merged tree, precache **31**, version parity 1.5.0.
- **G6:** all 11 cards PASS (Card 1 PASS-WITH-ISSUES, finding waived → B-177). No parks.

## Findings filed
B-177 (parseEventDate parseable-path COGS zone, waived from Card 1), B-178 (sync RLS fixture not
isolated from the live relay), B-179 (workbox-build 7.3.0 vs lockfile 7.4.1).

## Morning acts (see HANDOFF.md "DO THESE FIRST")
1. Review + SEND the counterparty notice (`reference/counterparty-notice-20260901-draft.md`) — BEFORE the deploy (P-KR3).
2. Triage + merge `overnight-20260901` → `dev`.
3. Post-deploy attended: place the Toast SFTP key (`reference/toast-archive-gap-20260901.md`); kill-drill; toast prod-proof.

Concurrent dispatch verdict: **held up, recommend again** (details in HANDOFF.md).
