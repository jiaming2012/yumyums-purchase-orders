# Slate — 20260721 (overnight run for the morning of 2026-07-21)

> **Run branch:** `overnight-20260721` (cut from `dev`; **never pushed, `main` untouched**).
> **Sign-off:** batch sign-off given by the operator **2026-07-20** — this document is the record.
> **Target:** **Activity 3 (Trust track)** — the fork-free, non-design-gated, unattended set of the
> "Prove & surface" cycle — **plus a draft-only Activity-2 design card** (operator scope choice
> 2026-07-20: "Trust track + design draft"). The Trust track has **no design-gate dependency** and
> may start as soon as the PRD lands (PRD signed 2026-07-19, roadmap NFR-5).
> **Forks resolved:** **none open.** All `runs/*/DECISIONS-NEEDED.md` are RESOLVED; `grep -c '· new'
> BACKLOG.md` = 0 (routing complete, PRD §Routing). The PRD is the signed intent contract
> (`prds/PRD-prove-and-surface.md`, `prd: valid`, signed 2026-07-19). **The Activity-2 design
> SIGN-OFF is a separate, second attended gate** and is explicitly **not** attempted tonight — the
> draft card leaves the open A4 decision (slug vs sub-permission column) for the operator (PRD A4,
> queued; sign-off.md 2026-07-19). No card carries an open operator decision the run must make.
> **Precondition flags:** **SATISFIED — DB is the ephemeral Docker env** (`docker-compose.nc.yml`,
> postgres:16, Docker-assigned host port), the canonical local path (Wave-0
> `hq-infra-docker-standardize`, landed `overnight-20260714`). No host Postgres to arm. Baseline
> suite is now **clean-ish (~1–5 reds, was ~37–41)** after this cycle's paydown (07-19 gate:
> Go exit-0 + Playwright 450·1·0·6) — the 1 known red is the LST-08 cross-test-pollution artifact
> this slate's `waiver1-isolation-fix` retires. Red-first proofs assert the NEW test flips
> red→green and adds **no new reds** — NOT a globally clean suite (except `waiver1`, whose bar
> *is* literal `task test` exit-0).
> **Night budget:** none given (no `--night`). This slate is **coverage-complete, not
> budget-limited**: it is the entire fork-free, non-design-gated, unattended-dispatchable set
> tonight. Activity 2 sign-off is attended; Activity 4 (Feature) is blocked on it; Activity 5 (gate)
> is premature. Nothing remains to spill into (design-findings NF-1: don't manufacture busywork).
> **Ledger basis:** `reference/card-actuals.md`. Convergence/de-flake-hardening ≈ 60–90m impl (own
> size class, 07-18 measured ~73m; the de-flake *proof* is the non-compressible cost) + ~5–16m G6;
> Go-only unit-seam ≈ 20–30m (07-15 B4 / 07-17 clock-seam); test-isolation repair ≈ 10–15m but this
> one adds a **full-suite exit-0 proof run** (~16–20m Playwright wall, per 07-19 gate Card 1);
> design-draft (docs-only) has no direct analog — sized wide off the Activity-1 PRD-draft cards
> (~8–12m subagent) uplifted for the multi-package read (me/users/auth/inventory/recipes); G6 ≈
> 2–5m; merge ≈ 2–3m. NOT inflated first-of-kind hour guesses (NF-1).
> **Standing instrumentation (FR-11 / Delivery KR3):** **every card emits harness-measured per-card
> wall-clock** (impl + G6 + merge), tabled in the HANDOFF — this cycle's per-card timing is now the
> **invariant build-run output** (fixes the 07-17 gap the 07-19 gate flagged; `percard-timing-
> instrumentation` is this run-mechanic, not a separate card).

## Target

**Activity 3 — Trust track** (3 build cards) **+ a draft-only Activity-2 design card**. Three
strands, mapped to the signed PRD:

1. **Sync-trust E2E** (Track A) — turn the escaped live-sync defect class into systematic coverage:
   `convergence-matrix-systematic` (FR-8/FR-9 — the {op-type}×{editor}×{derived-view} matrix with
   the ESC-1/2/3 would-have-caught cells) → `waiver1-isolation-fix` (FR-10 — retire waiver #1 to
   literal `task test` exit-0). Serial within the track; `waiver1` lands **last** so its exit-0
   proof runs against the settled tree.
2. **Sync unit coverage** (Track B, disjoint Go) — `sync-pkg-unit-coverage` (FR-7 — `ResolveEntityAccess`
   across the full {role}×{assignment} cartesian; ESC-1 regression kept red-first vs pre-fix).
3. **Design draft** (Track C, disjoint docs) — `prove-surface-design-draft`: draft the Activity-2
   design (gating model + enforcement path, the two aggregation endpoints, the convergence-coverage
   contract shape) as a markdown design doc, **A4 left OPEN** for the operator's morning sign-off.
   Draft-only — the run authors, the operator signs. (FR-0 direction; **not** the FR-0 sign-off.)

Traceability: Track A → Eng KR4 · Product KR2 · Eng KR5 · QA · INV-D; Track B → QA KR1 · ESC-1;
Track C → Delivery KR1 (design-signed-before-build) as **input to** the attended gate, not the gate.

## Estimates & critical path

Per-card cycle = implement + G6 + merge. **Serial** wall-clock = card sum; ~30 min closeout
reserved off the top. **No cut line** (no `--night`) — all 4 cards are the complete unattended set.

| Track | Cards (serial order within track) | Est. (impl · basis) | G6 |
|---|---|---|---|
| **A · Sync-trust E2E** (SERIAL — shared full-suite / `hq_test` baseline) | A1 `convergence-matrix-systematic` (**XL**, ~70–90m · 07-18 de-flake-hardening class, high end for risk) → A2 `waiver1-isolation-fix` (S, ~30–40m · isolation repair ~10–15m **+ full-suite exit-0 proof ~16–20m**) | ~100–130m | ~7–18m |
| **B · Sync Go units** (disjoint — `sync/access_test.go`) | B1 `sync-pkg-unit-coverage` (S–M, ~20–30m · 07-15 B4 / 07-17 clock-seam) | ~20–30m | ~2m |
| **C · Design draft** (disjoint — docs only) | C1 `prove-surface-design-draft` (M, ~15–25m · Activity-1 PRD-draft uplifted for multi-pkg read) | ~15–25m | ~2–5m |

- **Serial critical path (default):** ~90 + ~40 + ~25 + ~20 = **~175m ≈ 2h 55m** + ~30m closeout ≈
  **~3h 25m** (using high-end estimates). Coverage-complete; no card is budget-gated stretch.
- **Concurrent (reference):** Track A is the long pole (~130m; the two E2E cards can't overlap —
  `waiver1` needs A1's settled tree for its exit-0 proof). B + C tuck alongside → critical path ≈
  Track A + closeout ≈ **~2h 40m**, saving ~45m. **Not recommended** — see §Dispatch mode: the XL
  convergence card's whole cost is *proving zero-flake*, and concurrent Docker/Playwright load
  (07-15: 1.5–2× per-card latency under 3-way contention) is the worst place to add CPU/IO strain.
  Same 4 cards either way → the choice is burn/risk, not coverage.

## Shared surfaces (serialization rules)

| Surface | Cards | Rule |
|---|---|---|
| Full Playwright suite + shared `hq_test` / ephemeral pg16 baseline | A1 convergence-matrix (adds E2E cells), A2 waiver1 (fixes cross-test pollution **and** proves literal full-suite exit-0) | **The collision that forces serial within Track A.** `waiver1` lands **after** `convergence-matrix` so its `task test` exit-0 proof runs against the **settled** tree (new cells already landed) — otherwise the exit-0 baseline shifts under its feet. |
| `tests/{sync,repro-cut-task,broadcast-rerender}.spec.js` | A1 only (owns) | A1 extends the convergence matrix here. A2 = `tests/workflows.spec.js` (LST-08 at `:2762`) — different file, but the shared full-suite baseline keeps both in one serial track. |
| `sync.js` / `workflows.html` (+ `sw.js` regen) | A1 **only if a determinism seam is needed** (FR-8/roadmap) | Prefer test-only. If A1 must touch production for determinism, `task sw` is **orchestrator-only**, once per HTML-touching merge. A2 is test-only (no production). |
| `backend/internal/sync/access_test.go` (Go, test-only) | B1 only (owns) | **Fully disjoint** — Go unit test file; touches no E2E surface, no production. Safe anywhere. |
| Design doc under `.night-crew/knowledge/designs/` | C1 only (owns) | **Fully disjoint** — docs only. No production code, no `openspec/` scaffolding (see C1 constraint). |
| Ephemeral Docker env (`docker-compose.nc.yml`) | A1, A2, B1 | Per-card worktree brings up its own pg16 (Docker-assigned host port). **Serial = one env at a time**, zero contention. C1 needs no env. |

## Tracks

`waiver1` (A2) sits last by hard dependency (exit-0 proof needs the settled tree), so the riskiest
card (`convergence-matrix`, A1) leads Track A rather than trailing it — its PARK trigger below
contains the risk.

### Track A — Sync-trust E2E (2 cards, SERIAL) · owns `tests/{sync,repro-cut-task,broadcast-rerender}.spec.js`, `tests/workflows.spec.js` (A2)

1. **`convergence-matrix-systematic`** *(XL, ~70–90m; riskiest — leads by dependency)* — PRD
   FR-8/FR-9, AC-8/AC-9, §Escaped-defect closure. Extend the convergence E2E matrix from
   `SET_FIELD`-only to **{op-type ∈ field / submit / approve / reject}** × **{editor ∈ assignee /
   non-assignee-admin}** × **{derived-view ∈ field-value / correction-banner / edit-vs-readonly-mode
   / list-progress-count}**, each cell **red-first then green across ≥2 devices** (two Playwright
   contexts, one DB) — target **0 matrix cells red at cycle end**. **Escaped-defect closure:**
   ESC-1 ({SET_FIELD}×{non-assignee admin}×{2nd device}×{field value}), ESC-2a
   ({REJECT_ITEM}×{approver}×{submitter 2nd device}×{correction banner + readonly mode}), ESC-2b
   ({REJECT_ITEM→SET_FIELD}×{observer}×{list progress count}), ESC-3 ({REJECT_ITEM on a **sub-step**}
   ×{submitter}×{sub-step correction banner}) each map to a named cell that reddens on the pre-fix
   build (A6: satisfied by the recorded historical red-first runs — `access_test.go` / `RJT-LIVE-*`
   / `APR-*` — not a fresh revert unless cheap). Test-only footprint; `sync.js`/`workflows.html`
   **only if a determinism seam is needed**. → Eng KR4 · Product KR2 · QA · INV-D.
   **PARK trigger (two kinds):** (a) a new cell that proves **no-retry-flaky** (the 07-17/07-18
   timing sensitivity) — land the cells that green deterministically (the ESC-1/2/3 would-have-caught
   cells are the priority and MUST land green), and **PARK the de-flake of the specific flaky cells**
   to a follow-up with the named list (do **not** hold the whole card on a de-flake tail — that was
   its own 07-18 card, ~73m). (b) a cell needing plumbing beyond a `sync.js`/`workflows.html`
   determinism seam (a new op type, a schema change) → **footprint breach → PARK that cell** with
   the specific list. Either way the rest of the matrix still lands.
2. **`waiver1-isolation-fix`** *(S, ~30–40m; lands LAST in the run)* — PRD FR-10, AC-10;
   BACKLOG "Waiver-#1 last mile". Fix the 1 isolation-confirmed cross-test DB-pollution red
   (`tests/workflows.spec.js › approved checklist shows Approved badge and cannot be resubmitted
   [LST-08 RUN-08]` at `:2762` — `expect(#toast).toBeVisible()` gets `hidden` in the full suite,
   **passes in isolation**) by isolating its state dependency: identify the polluting predecessor
   (an approval/submission-rejection row or a `#toast` left visible in shared `hq_test`) and either
   scope its fixture teardown or make the assertion order-independent. **Then re-run the FULL suite
   to confirm literal `task test` exit-0** — this is the card's acceptance bar (not "no-new-reds").
   Red-first: the full-suite red is the recorded baseline; green = literal exit-0, **formally
   retiring carried waiver #1** (Eng KR5 PARTIAL → PASS). Test-only footprint (no production change).
   → Eng KR5. **PARK trigger:** if literal exit-0 is blocked by a *second* independent pollution
   source uncovered mid-fix (not LST-08), fix LST-08, record the residual red with its isolation
   evidence, and PARK the "formally retired" claim to DECISIONS-NEEDED (waiver #1 stays reduced, not
   retired) rather than claim a false exit-0. Honesty over a clean scorecard (07-19 gate pattern).

### Track B — Sync Go units (1 card, disjoint) · owns `backend/internal/sync/access_test.go`

1. **`sync-pkg-unit-coverage`** *(S–M, ~20–30m)* — PRD FR-7, AC-7; BACKLOG "Cross-user live-sync
   access matrix + `sync`-package unit coverage". Extend `ResolveEntityAccess` unit coverage to the
   **full {role}×{assignment} cartesian** (recipient set = admins/superadmins ∪ assignees; the
   author is added by the listener, `listener.go:63-72` — assert the resolver's own contract, note
   the listener union). **Baseline (PRD-corrected):** the package is **not** 0-tests —
   `sync/access_test.go` already holds 2 DB-gated tests
   (`TestResolveEntityAccess_AdminReceivesLiveOps` = the ESC-1 red-first regression, +
   `…_EmptyWhenNoMatchingUsers`), both skipping without a test DB. Keep the **ESC-1 regression proven
   red against the pre-fix code** and green after. DB-gated skip is documented
   (`DB_TEST_URL`/`TEST_DATABASE_URL`). Go-only, test-only footprint — fully disjoint. → QA KR1 ·
   ESC-1. **PARK trigger:** none expected (pure Go unit extension on an existing file); if a combo
   needs a production seam in `ResolveEntityAccess` itself → footprint breach → PARK the combo.

### Track C — Design draft (1 card, disjoint, docs-only) · owns `.night-crew/knowledge/designs/`

1. **`prove-surface-design-draft`** *(M, ~15–25m; DRAFT-ONLY — the run authors, the operator signs)*
   — PRD FR-0 (the *direction*, **not** the FR-0 sign-off). Draft the Activity-2 design as a
   markdown design doc `.night-crew/knowledge/designs/prove-surface-gating-and-endpoints.md`
   (mirroring `designs/editprop-frozen-at-submit.md`), covering: **(a) the gating model + enforcement
   path** — how a per-tab grant is represented in `app_permissions`, the net-new
   `RequirePermission`-style check on the cookie-auth Trends/Cost endpoints (none exists today — only
   `auth/middleware.go` logged-in-vs-not + ad-hoc `isAdmin()`), the `/me` `MeAppsHandler`-style
   resolver extension (`backend/internal/me/handler.go`) that drives per-tab visibility, the Users
   admin-UI grant surface (`backend/internal/users/`), and `inventory` slug registration; **(b) the
   two aggregation endpoints** — by-week×by-group spend (tax-proration copying `recipes/handler.go:74-77`;
   the signed NULL-`purchase_item_id` exclude+"Unlinked $X" rule, FR-6b) and the margin join (add
   `SUM(daily_menu_sales.gross_amount)` revenue to the `menu-cogs` shape; `margin`, `food_cost_%`,
   the zero-revenue NULL rule, both movers orderings); **(c) the convergence-coverage-contract shape**
   (which matrix cells, which derived views — the {op-type}×{editor}×{derived-view} contract). Read
   surfaces: `inventory.html` (`#s5/#s6`, `renderTrends`/`renderCost`), `me/handler.go`,
   `users/handler.go`+`db.go`, `auth/middleware.go`, `inventory`/`recipes` handlers. → Delivery KR1
   (as **input** to the attended gate).
   **HARD CONSTRAINTS (this is a draft, and the run never decides):**
   - **A4 stays OPEN.** Present **both** representations — (i) two dedicated per-tab slugs
     (`inventory-trends`, `inventory-cost`; **no migration**) vs (ii) a per-tab sub-permission column
     (**a migration → a down-migration + up→down→up proof + pre-deploy backup under NFR-3/INV-E**) —
     with trade-offs and a **recommendation**, but **do NOT choose**. A4 is the operator's morning
     sign-off call (PRD A4, queued). The observable per-tab rule (FR-5) is fixed regardless.
   - **Docs only.** No production code, no test changes, no schema, no `task sw`.
   - **No `openspec/` scaffolding.** This repo has **no `openspec/` directory**; the prior cycle's
     design gate shipped as a markdown design doc (`designs/editprop-frozen-at-submit.md`), so the
     draft follows that precedent. **Flag** in the doc that formalizing it as an *OpenSpec change*
     (per the roadmap/PRD wording) would require `openspec init` — a structural repo decision — and
     leave that to the attended session; do not scaffold openspec unattended.
   - **Not the gate.** The doc is the operator's signable input; its landing does **not** authorize
     any Feature build WO (Activity 4 stays blocked until the operator signs the design).

## Explicitly NOT in this slate

- **Activity 2 design SIGN-OFF** — a separate, second **attended** gate (it carries the open A4
  decision). Tonight only **drafts** the design (C1); the signature is the operator's, in a separate
  session. (sign-off.md 2026-07-19; PRD A4.)
- **Activity 4 — Feature build** (`trends-*`, `cost-*`, `inventory-tab-gating`) — **blocked** on the
  Activity-2 design sign-off (FR-0 / NFR-5). 0 Feature WOs dispatch tonight (auditable from ledger).
- **Activity 5 — cycle gate** — premature; nothing this cycle is built/verified yet.
- **`prod-alert-dup-guard` (FR-12)** — an **over-the-cycle prod observation** (watch the Zoho Cliq
  channel for duplicate Mercury/alert-queue messages; delegated fallback = disable the **dev-side**
  emission, PRD A5), not an overnight build card. Record observations in the ledger over the cycle.
- **`openspec init` scaffolding** — a structural repo decision; if the operator wants the design as a
  formal OpenSpec change, that's the attended session's call (C1 constraint).
- **The deferred backlog** — editprop tidy-ups (F-B/F-C/D-1), photo-S3 / offline-IndexedDB harnesses,
  onboarding video fixture — all routed `deferred` at the PM session (PRD §Routing 6–12); none clears
  the §1.5 architecture-blocking promotion bar.

## Execution notes

- **Serial (default):** one in-flight card across all tracks. Order: **A1
  `convergence-matrix-systematic` → B1 `sync-pkg-unit-coverage` → C1 `prove-surface-design-draft` →
  A2 `waiver1-isolation-fix` LAST.** `waiver1` is strictly last so its literal `task test` exit-0
  proof runs against the fully-settled tree (all new E2E cells already landed). The orchestrator runs
  `task sw` once per HTML-touching merge (only if A1 needed a determinism seam), in landing order.
- **Two-session split (if run attended in two sittings):** session 1 = A1 (the XL convergence matrix
  — the priority) + A2 (`waiver1`, after A1); session 2 = B1 + C1 (Go units + design draft), which
  are footprint-disjoint and can run any time. Clean boundary on Track A vs Tracks B/C.
- **A PARK is not a stop.** A failed/footprint-breaching card PARKs (worktree preserved,
  DECISIONS-NEEDED.md) and dispatch continues. If A1 PARKs a de-flake tail, A2 still runs (its exit-0
  bar is about LST-08 pollution, independent of the new convergence cells' flakiness — but note any
  new red A1 leaves so A2's exit-0 claim stays honest). B1 and C1 are independent of A entirely.
- **Budget discipline (tokens are a SEPARATE constraint from calendar):** prefer a clean early exit
  over starting a card that can't finish cleanly. With no `--night` calendar pressure the run should
  complete all 4; if **quota** tightens, drop from the tail — C1 (design draft) first, then B1 —
  never abandon A1 mid-matrix or leave A2's exit-0 proof half-run.
- **Run mechanics (standing rules, gates G1–G6)** are inherited unchanged by reference from
  `reference/overnight-run-plan-20260707.md` — not restated here.
- **Suite run-mechanics (bake in from the start — 07-18/07-19 lost ~10–15m twice to this):** run any
  full-suite / G6 leg with **`CI=1`** (forces `reuseExistingServer:false` → own webServer + teardown,
  avoids the `:8199`/`:8199`-style foreign-server latch) **and** provision the isolated pg16 with an
  **explicit pre-migration boot** (throwaway app boot → goose) before Go units. This is `waiver1`'s
  exit-0 proof environment and A1's two-context env — get it right on the first pass.
