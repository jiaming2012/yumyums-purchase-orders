# Slate — 20260717 (overnight run for the morning of 2026-07-17)

> **Run branch:** `overnight-20260717` (cut from `dev`; never pushed, `main` untouched).
> **Sign-off:** batch sign-off given by the operator **2026-07-16** — this document is the record.
> **Dispatch mode:** **serial** (operator choice 2026-07-16). §5 criteria fail for concurrent:
> the workflow-engine super-track and the test-debt track are **not** additive-only — Super-track W
> rewrites `repro-cut-task.spec.js` / `workflows.spec.js` / `sync.spec.js`, and Track T's
> `vacuous-tests-18-to-0` rewrites vacuous tests across those same spec files. Concurrent would
> collide on shared `.spec.js` surfaces; with ~5.5h of margin under the 12h budget there is no
> time pressure to take that risk. Concurrent (safe-hybrid) projection noted below for reference.
> **Forks resolved:** none open. The **FR-1 edit-propagation design is operator-signed
> 2026-07-16** (`.night-crew/knowledge/designs/editprop-frozen-at-submit.md`) — the Activity-4
> gate that authorizes Activity-5's three `editprop-*` build cards. The **C5 day-boundary fork**
> resolved to **warned live removal** (design §5). No card carries an open operator decision.
> **Precondition flags:** **SATISFIED — DB is the ephemeral Docker env** (`docker-compose.nc.yml`,
> postgres:16), the canonical local path (Wave-0 `hq-infra-docker-standardize`, landed
> `overnight-20260714`). No host Postgres to arm. HQ carries ~37–41 pre-existing E2E reds as the
> known baseline — red-first proofs assert the NEW test flips red→green and adds **no new reds**,
> NOT a globally clean suite.
> **Night budget:** **12h, treated as a FLOOR not a ceiling** (operator directive 2026-07-14,
> `design-findings-nightcrew.md` NF-1). This slate is **coverage-complete, not budget-limited**:
> the 9 cards are the entire fork-free, non-gated, unattended-dispatchable set for this cycle.
> Nothing remains to spill into — Activity 7 (prod ops) is operator-gated, Activity 8 (cycle gate)
> is premature, and the backlog's sizeable items are harness/creds-blocked (design-findings NF-1
> discipline: don't manufacture busywork to fill a floor). The ~5.5h of slack is the shape of a
> cycle whose critical path ran through an attended design gate — now signed.
> **Ledger basis:** `reference/card-actuals.md`. App-fix cards ≈ 15–30m impl (overnight-20260714);
> multi-surface / new-fixture cards ≈ 30–50m; big E2E-matrix / core-path-replacement cards ≈
> 50–80m (first-of-kind for editprop — sized on the high end per risk); Go-only unit-seam cards ≈
> 20–30m; G6 ≈ 1–3m; merge ≈ 2–3m. NOT the inflated first-of-kind hour guesses (NF-1).

## Target

**Activities 2 + 4-signed-5 + 6** — the first build slate of the "Nothing silently lost" cycle,
now that the FR-1 design gate is signed. Three strands:

1. **Activity 5 — the edit-propagation structural fix** (unlocked tonight by the signed design):
   `editprop-stable-field-identity` → `editprop-broadcast-rerender` → `editprop-convergence-matrix`.
   The core of the cycle theme — field identity stable across edits, edits propagate live to open
   devices with surviving answers intact, submit freezes forever, multi-device convergence proven.
2. **Activity 2 — the two engine-trust fixes + carried small fixes** the semantic doesn't subsume:
   `engine-approval-feedback-loud` (FR-8), `engine-conflict-refetch` (FR-9),
   `ops-nfr3-resubmit-photo-gate` (carried T-10), `users-s3-orphan-cleanup` (hygiene).
3. **Activity 6 — test-debt retirement** (independent, any-time): `vacuous-tests-18-to-0`,
   `carried-fix-wos-sweep` (the `WO-cron-clock-seam`).

Traceability: Activity-5 cards → Eng "stable identity / loud rejection / edit propagation /
convergence matrix" + Product KR-2 + Delivery "repro red→green pair"; Activity-2 fixes → QA
"red-first"; Activity-6 → QA KR1 (vacuous 18→0) + KR2 (cron seam red-first).

## Estimates, critical path, cut line

Per-card cycle = implement + G6 + merge. **Serial** wall-clock = card sum. ~30 min closeout
reserved off the top. **No cut line** — all 9 cards fit with ~5.5h margin; nothing is
budget-gated stretch (the budget exceeds the available fork-free work).

| Track | Cards (serial order within track) | Track time |
|---|---|---|
| **W · Workflow engine + sync** (SERIAL — shared `internal/workflow` + `sync.js` + `workflows.html` + workflow specs) | stable-field-identity (~50m) → broadcast-rerender (~70m) → convergence-matrix (~80m) → approval-feedback-loud (~19m) → resubmit-photo-gate (~20m) → conflict-refetch (~31m) | **~270m ◄ long pole** |
| **U · Users** (disjoint — `users.html`) | s3-orphan-cleanup (~11m) | ~11m |
| **T · Test-debt + scheduler** (`internal/purchasing` + `tests/*.spec.js`) | cron-clock-seam (~28m) → vacuous-tests-18→0 (~53m) | ~81m |

- **Serial critical path (chosen):** 270 + 11 + 81 = **~362m ≈ 6h 02m** + 30m closeout ≈
  **6h 32m**. Fits the 12h budget with **~5h 28m margin**. All 9 cards land.
- **Concurrent (safe-hybrid, reference only):** Track U + `cron-clock-seam` (both disjoint) run
  alongside Super-track W; `vacuous-tests-18→0` must still serialize **after** W's spec-touching
  cards (collision). Critical path ≈ W long pole (~270m) + the vacuous tail that can't overlap ≈
  **~4h 50m** + closeout. Saves ~1.5h, adds the `.spec.js` collision-management burden — **not
  worth it** at 5.5h margin. Same 9 cards either way → the choice was burn/risk, not coverage.

## Shared surfaces (serialization rules)

| Surface | Cards | Rule |
|---|---|---|
| `internal/workflow` (`handler.go`, `repository.go`) | all 6 Super-track W cards | One track, serial. `editprop-stable-field-identity` **deletes** the `replaceTemplate` reinsert path (`repository.go:99-219`) and adds the 422 existence check — it lands **first**; every later W card builds on stable identity. |
| `sync.js` + `sync/ops.go` | broadcast-rerender, conflict-refetch, (stable-identity touches op path) | Serial within W. `broadcast-rerender` adds `SAVE_TEMPLATE` handling in `applyOp` + moves op emission into the write txn (`sync/ops.go:245-264`); `conflict-refetch` edits the 409 path (`sync.js:26-28`) — lands **after** broadcast-rerender so both sync.js edits serialize cleanly. |
| `workflows.html` (runner + Builder) + `sw.js` regen | stable-identity (runner 422 surfacing), broadcast-rerender (re-render + Builder discard warning), users-s3 (Users) | **`task sw` is orchestrator-only, once per HTML-touching merge**, in landing order. Subagents never hand-edit `sw.js`. |
| `tests/*.spec.js` (`repro-cut-task`, `workflows`, `sync`, `persistence`) | W cards write red-first tests here; `vacuous-tests-18→0` rewrites vacuous tests here | **The collision that forces serial.** In serial there is no contention (one card at a time). `editprop-convergence-matrix` **rewrites `repro-cut-task.spec.js`** to the frozen-at-submit semantic (the recorded red→green baseline pair, Delivery KR). |
| `internal/purchasing` (`scheduler.go`, `repurchase.go`) | `carried-fix-wos-sweep` only | Fully disjoint — the `now` clock-seam in the 4 `run*Check` funcs + Go unit tests. No other card touches `internal/purchasing`. |
| Ephemeral Docker env (`docker-compose.nc.yml`) | all cards | Per-card worktree brings up its own pg16 env (Docker-assigned host port). **Serial = one env at a time** — zero contention. |

## Tracks

Riskiest card in a track goes **last** where dependencies allow; the editprop chain's internal
dependency order fixes its sequence (identity → broadcast → matrix), so the riskiest editprop card
(`convergence-matrix`) sits at W-3 with a PARK note, and the independent fixes (W-4…W-6) follow it.

### Super-track W — Workflow engine + sync (6 cards, SERIAL) · owns `internal/workflow`, `sync.js`, `sync/ops.go`, `workflows.html`, `tests/{repro-cut-task,workflows,sync,persistence}.spec.js`

Authorized by the signed design `designs/editprop-frozen-at-submit.md` (§ references below).

1. **`editprop-stable-field-identity`** *(L, ~50m; lands first — the foundation)* — design §2+§3.
   `updateTemplate` diff/upserts by the Builder-sent field IDs (update kept / insert new / delete
   removed; conditions remap for new fields only); **delete** the `replaceTemplate` delete-reinsert
   path (`repository.go:99-219`). A write naming a field absent from the current template → **422
   `{"error":"unknown_field"}`** via an app-level existence check (NOT a restored FK); the runner
   surfaces it (no optimistic checkmark survives). Red-first: the rewritten `repro-cut-task.spec.js`
   post-edit identity checks + a Go test for the 422. → Eng "stable identity" + "loud rejection",
   FR-2/FR-3, INV-2/INV-4.
2. **`editprop-broadcast-rerender`** *(XL, ~70m)* — design §4+§5+§6. Handle `SAVE_TEMPLATE` in
   `applyOp` (`sync.js:401-449`): re-fetch + re-render the open unsubmitted checklist with all
   surviving answers intact (per the 7 field types + sub-steps), **silent on catch-up replay**
   (`42eeb39` no-toast rule). Builder **discard warning** naming the count before a save that cuts a
   field **or drops today from the schedule** (design §5, C5 = warned-live-removal). Plus
   **transactional op emission**: the op row commits in the same txn as the write (`sync/ops.go:245-264`).
   Red-first per sub-behavior. → Eng "edit propagation", FR-4/FR-5, INV-3/INV-6.
3. **`editprop-convergence-matrix`** *(XL, ~80m; riskiest editprop card)* — design §6. Red-first
   two-device E2E matrix: 7 field types + sub-steps + submit/unsubmit + list-view progress, **live +
   catch-up**, asserted on the observing device (converged within one op round-trip). Includes the
   ≥2 semantic acceptance tests via the rewritten `repro-cut-task.spec.js`: **AC-6a** (mid-run edit
   re-renders open devices, answers intact) + **AC-6b** (submitted record byte-identical after later
   edits). → Eng "convergence matrix", Product KR-2, Delivery "repro red→green pair".
   **PARK trigger:** a matrix cell needing plumbing beyond the design's surface (e.g. a new op type,
   a schema change) → PARK that cell with the specific list; the rest of the matrix still lands.
4. **`engine-approval-feedback-loud`** *(S, ~19m)* — FR-8. An approval-with-feedback only reports
   success if the comment is durably stored; today the `submission_rejections` insert swallows
   failure (`handler.go:615-617`, `ON CONFLICT DO NOTHING`, error logged not surfaced) while the
   approver sees "Approved". Red-first **Go test** forcing the failed insert (seam per A-6). → QA
   "red-first", INV-1.
5. **`ops-nfr3-resubmit-photo-gate`** *(S, ~20m)* — carried T-10. Plumb rejection context into
   submit validation (`SubmitChecklistHandler:511` / `validateFailNotes:54`) so a
   rejected-with-`require_photo` field blocks a **direct-API** resubmit server-side. Red-first Go
   test. → QA "red-first" (the deferred backend case from `ops-nfr3-photo-required`).
6. **`engine-conflict-refetch`** *(M, ~31m; last in W — the sync.js 409 path)* — FR-9. When a field
   write loses LWW (409, `sync.js:26-28` returns `{_conflict, winner}`), the losing device
   **re-fetches and renders the winning value** — the screen never keeps showing a value the DB
   rejected. Red-first two-context E2E asserting rendered value == stored value. → QA "red-first",
   INV-1. **PARK trigger:** if the winner value isn't in the 409 body and needs a new endpoint/op
   round-trip beyond the conflict path → PARK (footprint breach).

### Track U — Users (1 card, disjoint) · owns `users.html`

1. **`users-s3-orphan-cleanup`** *(XS, ~11m)* — remove the dead `<div id="s3">` at `users.html:122`.
   Hygiene, no KR, no behavior change → no red-first test required; verify no locator references
   `#s3` remain and `task sw` regenerates cleanly. Zero contention — safe anywhere.

### Track T — Test-debt + scheduler (2 cards) · owns `internal/purchasing`, `tests/*.spec.js`

1. **`carried-fix-wos-sweep`** *(M, ~28m)* — the `WO-cron-clock-seam`. Add a `now func() time.Time`
   seam to the 4 `run*Check` funcs (`scheduler.go:54/167/247` + `repurchase.go:129`) + **real
   cron-decision Go unit tests** with mock time (red-first). Unblocks P-6 / Purchasing FR-19–22
   (PARKed in `overnight-20260715` B4 for lack of a clock seam). The other two prove-sweep PARKs
   (photo-S3, offline-IndexedDB harness) stay deferred — harness class, off-theme. → QA KR2.
2. **`vacuous-tests-18-to-0`** *(L, ~53m; last — biggest, spec-file surface)* — each of the **18
   audited** conditional `test.skip()` / silent guard-returns becomes a real seeded assertion or is
   deleted. **Denominator is the audit's specific 18-item list**, NOT a blanket sweep of all 34
   `test.skip(` + 21 guard-returns in `tests/` (the env-gated SW/offline/harness skips are
   legitimately out). → QA KR1, retires carried waiver #2. **PARK trigger:** any of the 18 whose
   real assertion needs a fixture/harness that doesn't exist → convert what's fixture-available,
   PARK the rest with the specific list to DECISIONS-NEEDED.

## Explicitly NOT in this slate

- **Activity 7 — prod ops** (`prod-ghost-item-rename`, `prod-deploy-parity`) — operator-gated /
  prod data mutation; runs attended or with explicit go, never in an unattended run.
- **Activity 8 — cycle gate** (`cycle-gate`) — premature; nothing this cycle is built/verified yet.
- **Run-pinned immutable template versions** — weighed head-to-head at the grill-back and demoted to
  backlog (design §8, PRD §Out of scope). No versioning schema, no migration this cycle.
- **Backlog harness/creds-blocked items** — photo-S3 harness, offline-IndexedDB harness, onboarding
  video fixture. Deferred (cycle routing 2026-07-16); don't clear the §1.5 architecture-blocking
  promotion bar.
- **Historical stranded-row audit in prod** — no active users; FR-2/FR-3 stop new stranding.

## Execution notes

- **Serial (chosen):** one in-flight card across all tracks. Order: **Super-track W in full
  (W-1→W-6), then Track U, then Track T (T-1→T-2)** — see the two-session split for a natural
  boundary. `editprop-stable-field-identity` (W-1) is **strictly first** — everything in W builds on
  stable identity; its acceptance is the rewritten `repro-cut-task.spec.js` post-edit identity check
  going red→green. The orchestrator runs `task sw` once per HTML-touching merge, in landing order.
- **A PARK is not a stop.** A failed/footprint-breaching card PARKs (worktree preserved,
  DECISIONS-NEEDED.md) and dispatch continues with the next card. The editprop chain is dependency-
  ordered: if W-1 or W-2 PARKs, PARK the dependent editprop cards too (note it), but the independent
  fixes (W-4/W-5/W-6), Track U, and Track T **still run**.
- **Two-session split (if run attended in two sittings):** session 1 = Super-track W (the editprop
  build + engine-trust fixes — the priority); session 2 = Track U + Track T (hygiene + test-debt).
  The split is on the W/U-T boundary — U and T are footprint-disjoint from W, so the boundary is
  clean.
- **Budget discipline:** prefer a clean early exit over starting a card that can't finish cleanly.
  With ~5.5h margin the run should complete all 9; if quota (a SEPARATE constraint from the 12h
  calendar) tightens, drop from the tail — Track T first, then the W engine-trust fixes — never
  abandon a partially-applied editprop card mid-chain.
- **Run mechanics (standing rules, gates G1–G6)** are inherited unchanged by reference from
  `reference/overnight-run-plan-20260707.md` — not restated here.
