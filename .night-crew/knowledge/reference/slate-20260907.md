# Slate — run `20260907` (night of 2026-09-06 → morning 2026-09-07)

**Run branch:** `overnight-20260907` — no collision: `git branch | grep -E
'^overnight-20260907(-[0-9]+)?$' | sort -V` returned nothing this sitting, and
`git branch --no-merged dev` shows **no unmerged run branch of any date**.
**Sign-off:** GRANTED 2026-09-06 (operator, this session) — "Yes — sign it" at the §4
batch sign-off, with the milestone remainder, attention budget, spike-skips, and
agreement-check sections in view. 1 card, Activity B.
**Slate target:** the operator named **Activity B** in the invocation
(`/nc-slate-plan activity b`). Activity B has exactly one un-done card —
`sync-coordinates-provisioning` — so the card set is fully determined by the ask. No
inference; the Activity-0 skip is the operator's own standing sequencing (fourth
consecutive sitting slating past attended Activity 0, and this card was authored by the
operator's own decision 179/180 at the 2026-09-06 attended sitting). **1 card, Activity B.**
**Dispatch: SERIAL by definition** — a single card has no independent tracks, so the §5
concurrent-vs-serial question has no content tonight; nothing the operator would see
differs. Stated as the pick, not asked (the wording-check doctrine; slate-20260906 and
slate-20260906-2 precedent).
**Forks resolved:** none needed resolving this sitting. The card was authored at the
attended sitting of 2026-09-06 (ledger T-56, decisions 178–180) with the operator's own
"land provisioning first, rather than hand-seed the coordinates" call (decision 179)
already on record. The roadmap's two open questions both closed at the spike, at the
engineering level: **bearer lifetime/refresh DISSOLVES** (the proxy discards the client
bearer and re-mints per request from the session; the session row has `expires_at IS
NULL` — measured, a 30-day-aged session still mints), and **provisioning triggers at PAGE
INIT** (everything the writer needs is derivable from the session cookie every logged-in
page load already has; login-time would add a cross-page write for nothing). Both are
decided-and-stated mechanism calls, not operator forks. DECISIONS-NEEDED for run
`20260906-2`: resolved and closed — nothing parked, nothing awaiting ratification, zero
gray areas routed through the resolver (confirmed at T-55).

**Workflow-preflight verdict (ran clean this sitting — `night-crew workflow preflight
--repo /home/jcole/projects/hq`, exit 0):** `openspec: absent` (no `openspec/` tree);
`gsd: detected` (`.planning/` present — the deliberate telltale; real state is
`.night-crew/knowledge/`). **The launch prompt asserts UNIVERSAL mechanics only** —
red-first evidence, atomic commits, the `Night-Crew-Run: 20260907` trailer, the
roadmap/status flip — and creates **no** OpenSpec scaffolding. NOT the `undetermined`
STOP case: the verb read every path and returned `absent` at exit 0, matching every prior
run in this repo.

**Spike gate (per-goal, §1.4):** the `night-crew spikes gate` verb cannot drive on this
product-mode target — it reads the dogfood-only `usm/roadmap.txt` story map (re-run this
sitting: exit 1, "no story map … nothing to gate against"), the wall every slate since
`20260810` records. **The gate's substance was verified this sitting from the artifacts
it would read**
(`.night-crew/knowledge/spikes/activity-b-offline-first-replica/sync-coordinates-provisioning.md`,
committed `a642d56`):
- **No orphan cards:** Activity B's six cards are 1:1 with its six goal ledgers in the
  spikes directory; tonight's card IS its goal. The five DONE/LANDED siblings gated
  through at slates 20260905/20260906/20260906-2.
- **Every spike of tonight's goal has run to exit zero: 4/4 `passed`** —
  `coordinates-through-the-door` (exit 0 on attempt 2; attempt 1 COULD-NOT-RUN on a
  harness defect), `mint-supplies-the-coordinates` (first run), `push-lands-as-the-session-user`
  (exit 0 on attempt 3; two harness defects), `recovery-edge-for-b439` (first run).
  Hand-run 2026-09-06 per the no-story-map convention recorded in the ledger header.
- **Corrections reviewed and signed:** three agent-reached corrections, **all harness
  repairs, zero premise changes** — operator signed 2026-09-06 ("Review — signed:
  operator, 2026-09-06 — covers 3 correction(s)").
**Spike skips: none** — every authored spike ran to exit 0; no skip was recorded. Stated
so an absent section and a considered "nothing skipped" don't look alike.

**PRD gate:** ran FIRST this sitting (before the run id), exit 0 — 7 PRDs on file.
**Agreement check (§3b):** `night-crew okr compare-slate --expect-cards 1` run against
this document before sign-off; result recorded in the sign-off presentation.
**Clone freshness (B-435's remedy, run as step 0):** `git fetch origin` +
`git status -sb` this sitting — local `dev` is 1 ahead of `origin/dev` (the spike commit
`a642d56`, unpushed), 0 behind. Not the stale-clone failure mode; proceeding.

**Precondition flags:**
- Local `spike-supabase` compose substrate: **reconcile mode only, never `--fresh`** —
  harness legs run against the BUILT `supabase/` migrations applied by the committed
  helpers. Never `:5433`, never a hosted project (none exists yet — Activity 0 provisions
  it). If the compose project must be brought up, use `-p yumyums-test` (spike correction
  1 — a bare `up` fights the Taskfile's project and conflicts on the container name).
- `export PATH="/usr/local/go/bin:$PATH"` before any Go or Playwright leg; Playwright on
  `:5434` via `task test:db:up` — standing rules, decision 155. **The full-stack e2e (if
  the card takes that shape — build-fact 6) runs the real Go server the way spike 01
  did**: spike-owned `TEST_DB_NAME`, `HQ_SYNC_JWT_SECRET` = the compose file's committed
  throwaway secret, `HQ_SYNC_REST_URL` = the substrate's PostgREST. **Backend source is
  expected UNTOUCHED** — a diff touching `backend/` is scope drift and must be stated
  (roadmap footprint note); if it stays untouched, no Go suite leg is owed.
- `night-crew.toml` seams: every expected path (`marketing/scan-page.js`,
  `marketing/sync/*`, `tests/marketing.spec.js`) sits under the `marketing` prefix →
  `tests/marketing.spec.js`. **But the card edits precached files, so `sw.js` must be
  regenerated and committed in the same change set (content hashes move; B-13), and
  `sw.js` is an undeclared seam → the gate de-confines to the FULL Playwright suite**
  (the B-37 safe direction, deliberate). Price the full-suite leg (~20–28m quiet-box)
  into the card, not around it.
- **Precache count stays 43 unless a NEW file is added.** The provisioning writer is
  expected to live in existing files (`scan-page.js`, `marketing/sync/*`); if the card
  adds a new module, the count move is deliberate and stated, `sw.js` regenerated and
  committed in the same change set (B-37, B-13). If no new file, the count stays 43 and
  the card says so.
- 🛑 **The expected-red baseline is B-433's corrected set** (clean detached `dev` base,
  triage 2026-09-05): **four** base reds — `sw-api-cache-partition.spec.js:92 [B1-XT-01]`,
  `sync.spec.js:2976 [SYNC-FC-01]`, `sync.spec.js:3062 [SYNC-RF-01]`,
  `sync.spec.js:3136 [SYNC-RF-02]` — and DBL-05 PASSED on base. No-new-reds is judged
  against the four-red set; a red outside it is a finding, not baseline.
- Two untracked spike run logs sit in the working tree
  (`.night-crew/spikes/.../sync-coordinates-provisioning/.hq-server.log{,.degraded}`) —
  leftover server logs from this sitting's spike run. **The run must not sweep them into
  any commit**; they are local diagnostics, not artifacts.

## Shared surfaces and serialization

One card — no cross-card serialization. The surfaces below are shared with LANDED work;
the rules are what the implementer must not regress:

| Surface | Landed owner | Rule tonight |
|---|---|---|
| `marketing/scan-page.js` | runs 20260905/20260906 | gains the only `SYNC_KEY` writer in the tree (build-fact 1: mint → write `{restUrl:'/sync/rest', bearer}` → `startSync`, plus `deviceId = envelope.sub`); the shipped `if (sc) startSync(sc)` guard and today's no-sync degradation on 401/503 (write nothing) must survive |
| `marketing/sync/replicas.js` (`createCampaignPolicySource`) | run 20260906-2 card 1 | **the B-432 fail-closed predicate is untouchable** (a KNOWN code whose campaign is unresolved refuses — re-verified by mutation at triage); the B-439 fix clears `lastError` on successful-pull evidence (`clock.captures` is the proven witness, the ONLY candidate silent-while-erroring in both recovery shapes) — never on `error$`/`active$`/`remoteEvents$`, which fire on 503 cycles too |
| `marketing/sync/push-replication.js` | run 20260905 card 3 + 20260906-2 | `startScanAttemptsReplica` gains its first caller (inside `startSync`, `deviceId` from the mint `sub` — build-fact 4, decided scope); GAP-1's two belts and the F-2 divert path survive; 🛑 **`deviceId` is a correctness cliff, not a style choice** — a non-`sub` value = RLS 403 AFTER the RPC burn: stranded burn + poisoned queue (spike 03, measured) |
| `marketing/submit-flow.js` / `submit-machine.js` | run 20260905 card 6 | **zero new (state,event) pairs**; `policyUnresolvedFor`'s three arms unchanged — what changes is that `attach()` finally RUNS, making the shipped discriminator honest (B-438's whole mechanism) |
| `supabase/migrations/` | Activities A/B | **no new migration expected** — B-438/B-439 are client-side; any migration appearing in the diff is scope drift, stated |
| `sw.js` (committed artifact) | — | regenerated (`task sw`) + committed in the same change set as the client edits; count 43 unless a new file is deliberate and stated |
| Roadmap + knowledge docs | — | the card flips its own roadmap line; log the merge in `reference/conflicts-20260907.md` (clean or not — an empty log must mean "no merges", never "logging never ran") |

## Model routing (§3c — class-derived, no per-card override)

| Class | Model | Cards |
|---|---|---|
| new-mechanism | session | sync-coordinates-provisioning |
| G6 adversarial review | session | all cards, always (standing row) |

<!-- routing:slate -->
new-mechanism session
sync-coordinates-provisioning new-mechanism
<!-- /routing:slate -->

## Estimates and critical path (wall-clock ONLY)

🛑 **Sized against the calendar and nothing else.** No `--night` was given, so there is no
cut line — this estimate exists for the attention budget and the stretch discipline. The
account's token quota — tier, ceiling, current consumption, reset window — was **NOT
measured and cannot be measured from here.** The run may exhaust quota before the clock or
the clock before quota; neither validates the other. Spend is governed by the run's own
stop conditions; a rate-limit stall means finish the in-flight leg and close out — never
sit blocked.

End-to-end = implement + G6 + any fix round + merge. Basis: `reference/card-actuals.md`.

| # | Card | Track | Routing class | Estimate | Basis (sizing class per card-actuals) |
|---|---|---|---|---|---|
| 1 | `sync-coordinates-provisioning` | solo | new-mechanism | **60–110m — risk-flagged, use 110** | Two classes bracket it: new-mechanism actuals 35–90m (runs 20260905/20260906, the sibling `requires-online-replication` at 74m under the same spike-was-the-draft discount) and bug-fix-on-known-defect first actual ~73m (`refusal-holds-before-sync` — read as the class FLOOR). The discount applies at full strength — build-fact 1 fully specifies the writer, the push wiring is priced by spike 03's green drain leg, and the B-439 fix's witness is already chosen by measurement. Pulls up, and why the top is 110 not 90: **four done_when clauses that must all be proven UNSTUBBED** (the card-actuals lesson: price a "value is recorded" clause with an unstubbed end-to-end leg), the **first Playwright e2e needing a sync substrate** (build-fact 6 — no card-actuals entry for that stack shape, the wide-± component), the owed **wrapper-free spike-04 re-run in BOTH recovery shapes**, and the full-suite leg ~20–28m (sw.js de-confinement, priced above) |

**Critical path (serial, the only shape):** 60–110m + ~30m closeout →
**mid ≈ 1h55m, high ≈ 2h20m.** The high is insurance, not the expectation — every
spike-was-draft card to date has landed in or under band. No stretch tier exists tonight:
Activities E and F are refused by the per-goal spike gate (no spikes authored), and
Activity 0 is attended-by-design.

## The card

### Card 1 · `sync-coordinates-provisioning` — wire the caller so sync actually arms, and close-bar leg 3 becomes attestable

The leg-3 gating card (roadmap Activity B; authored at the attended sitting of
2026-09-06, ledger T-56, decisions 178–180), de-risked by four green spikes (goal ledger:
`spikes/activity-b-offline-first-replica/sync-coordinates-provisioning.md` — **binding**,
including its six build-facts). The defect, demonstrated not asserted: `SYNC_KEY`
(`hq_marketing_sync_v1`) has exactly one occurrence in the tree — its own declaration at
`scan-page.js:27` — so `readJson(SYNC_KEY)` is always null, `startSync` never runs,
`campaignPolicy.attach()` never runs, every local collection stays empty, every scan
resolves `unknownCode`, and Q-KR1's high-value-refusal branch is unreachable on real data
(decision 178). 🛑 **This is WIRING, not new mechanism** — both ends are landed
(`POST /api/v1/sync/token` at `main.go:633`; `internal/sync/proxy.go`; `startSync`
exported at `scan-page.js:410`); only the caller is missing. Except for one true addition
the spikes surfaced and priced: **the push replica is ALSO unwired**
(`startScanAttemptsReplica` has zero callers outside its own module — spike fact 4), and
arming sync without it ships a scanner that queues attempts forever.

**The build, fully specified by the spikes (binding build-facts):**
1. **The provisioning writer (build-fact 1):** on page init with a session,
   `POST /api/v1/sync/token` → write `{restUrl: '/sync/rest', bearer: envelope.token}`
   to `SYNC_KEY` → call the already-exported `startSync` — plus `deviceId =
   envelope.sub` for the push side. On 401/503, **write nothing**; the shipped
   `if (sc) startSync(sc)` guard keeps today's no-sync behavior. Re-visits re-provision
   idempotently (the `syncHandles` guard, measured).
2. **Push wiring (build-fact 4, decided scope):** start `startScanAttemptsReplica`
   inside `startSync`, `deviceId` **from the mint envelope's `sub`, never any
   device-local identity** — spike fact 3 measured the alternative both ways: a rogue
   `deviceId` draws RLS 403 AFTER `redeem()` has burned the code — a stranded burn that
   can never record, plus the F-2 throw-retry head-of-line poison.
3. **The B-439 fix (build-fact 5):** clear `lastError` on successful-pull evidence —
   `clock.captures` is the proven witness (increments on every HTTP-200 pull INCLUDING
   the zero-new-rows recovery, and only on success; `error$`/`active$`/`remoteEvents$`
   all fire on 503 cycles too and are disqualified by measurement). The card chooses the
   mechanism (compare captures across `error$` emissions, or a success callback on the
   pull-handler seam `replicas.js` already constructs); the edge is real either way.
4. **B-438 discharges as a consequence, not a separate fix:** `attach()` already sits in
   the right place inside `startSync` (spike 01 exercised the exact scan-page order
   through the door); what B-438 waited on was only that `startSync` runs. The
   discriminator's landed honesty was proven by the sibling ledger.

**done_when (roadmap verbatim, four clauses — ALL unstubbed):**
1. **The scanner holds real codes offline.** On a provisioned device with the network
   then cut, scanning a code that exists server-side resolves to that code rather than
   `unknownCode` — proven by an e2e that provisions **through the shipped path (never by
   writing `SYNC_KEY` from the test)**, kills the probe, and asserts `#scan-result`
   `data-kind`.
2. **The campaigns replica genuinely attaches.** `campaignPolicy.attached()` is `true`
   and `size()` non-zero after initial replication — asserted against the real source,
   **seam NOT stubbed**.
3. **B-438 discharged: `policy_unresolved` records what its DDL comment says.** With the
   campaigns replica made to fail **for real (kill/block the campaigns upstream at the
   network layer — decision 174's rule; never `setCampaignPolicy`)**, a
   genuinely-unknown-code override lands `policy_unresolved = true`; with it healthy, the
   same override lands `false`. 🛑 The proving test **may not call `setCampaignPolicy`**
   — today's clause-2 test injects `() => true` where the shipped function returns
   `() => false`, which is exactly how a clause passed while being unreachable (T-55,
   decision 174). If the new test stubs the seam, the clause is not met.
4. **B-439 discharged in the same pass** — after a pull failure and a full recovery,
   `unresolved()` returns to `false`; asserted by a test that errors, recovers, then
   reads it — **in BOTH recovery shapes** (with-docs and recovery-EMPTY, the shape that
   defeats any docs-based signal).

**The owed items, binding (goal ledger Comebacks + build-fact 6):**
- **One wrapper-free re-run of spike 04 against the shipped clear** in the same sitting —
  the latch must go `unresolved() = false` in BOTH recovery shapes where the spike
  measured it stuck `true` (the `refusal-run.sh` precedent: re-execute the spike against
  the shipped code, no wrapper).
- **The test-stack call (build-fact 6), decided and stated by the card:** the Playwright
  suite has no sync substrate. Either the clause-1 e2e runs the spike's stack shape (real
  server + `HQ_SYNC_*` env + PostgREST) or `/sync/rest/*` is route-intercepted **at the
  network layer** (decision-174-consistent — it fails the transport, not the seam), with
  the full-stack proof living in the harness the way `refusal-run.sh` does. The card
  states which it uses.
- Mark B-438 and B-439 dispositions in BACKLOG.md in the same change set.

**Red-first:** clause 1's red is structural and cheap — the e2e that provisions through
the shipped path fails on the pre-change tree because no code path writes `SYNC_KEY`
(every scan resolves `unknownCode`); name it and show it. The B-439 test reds against the
shipped latch (spike 04 already reproduced the stuck-`true` on shipped surface); the
B-438 test reds because `attach()` never runs pre-change.

**Stated, not in scope (from the goal ledger):** the unprovisioned-deploy degenerate case
stays — secret-less deploy → token 503, no sync starts, `attached()` false, a
genuinely-unknown override files `policy_unresolved=false`; that is **B-436-adjacent
territory (no policy source at all), outside this card's four clauses.** B-436 itself —
whether a device that cannot BUILD a policy source keeps or loses the override — is an
operator affordance call, untouched tonight (see PARK note).

Gate: standalone harness legs (exit codes: the spike-04 re-run + whatever full-stack
proof build-fact 6 selects) + `tests/marketing.spec.js` + the full Playwright suite
(sw.js de-confinement, stated above); no-new-reds vs the **B-433-corrected four-red base
set** (B1-XT-01, SYNC-FC-01, SYNC-RF-01, SYNC-RF-02 — DBL-05 is NOT baseline).

Footprint: `marketing/scan-page.js` (the provisioning call site) + `marketing/sync/`
(replicas latch, push wiring) + `tests/marketing.spec.js` + `sw.js` regeneration.
**Backend expected untouched; a diff touching `backend/` is scope drift and stated.**
**PARK note (narrow):** a **new terminal status** beyond the §9/§19 attempt taxonomy, any
**weakening of the `requires_online = true` refusal or of the B-432 fail-closed
predicate**, a new preference category / `night-crew.toml` key, or a build that turns out
to require **deciding B-436** (the no-source affordance call — the operator's, explicitly
deferred at T-55) → park. The provisioning writer's placement, the push-start mechanics,
the B-439 clear mechanism, and the build-fact-6 test-stack call are the night's.

## Explicitly NOT in this slate

- **Activity 0 (`redemption-unknowns-spike`, `external-accounts-provision`)** — attended
  by design: Toast settings/field facts and account/billing provisioning only the
  operator can do. The #6 field observation stays owed there; its answer can still
  re-scope what REMAINS of Activity B, which after tonight is nothing — tonight's card is
  the activity's last, and it is wiring the operator already sequenced first
  (decision 179).
- **Activity E (`identity-code-and-qr`, `mms-send-on-signup`)** — refused by the per-goal
  spike gate: no spikes authored for either goal. `mms-send-on-signup` additionally
  blocked on Activity 0's live sending number.
- **Activity F (`smtp-toast-ingest`, `reconciliation-view`)** — refused by the same gate,
  and its inputs (#1/#2/#3 Toast facts) sit in Activity 0.
- **Backlog promotions: none.** B-436 is an operator affordance call deliberately left
  open at T-55 — not architecture-blocking for this card (its degenerate case is stated
  and carried). B-440's fix predicate (`unverified_code && offline_override` divert)
  touches `push-replication.js`, which IS in tonight's diff — but its recorded lead says
  "before any second producer lands" and no second producer exists; folding it in would
  widen a four-clause card whose value is attestability, so it stays backlog. B-441 (the
  v0-seeded Dexie migration fixture) and B-442 (stranded card branches) are hardening
  class, below the §15k bar. B-437, B-423, B-429, B-173 unchanged.

## Milestone remainder (live state, this sitting)

**Milestone cards still white after tonight (assuming the card lands): 6** —
`redemption-unknowns-spike`, `external-accounts-provision` (Activity 0, attended);
`identity-code-and-qr`, `mms-send-on-signup` (Activity E); `smtp-toast-ingest`,
`reconciliation-view` (Activity F). **The slate leaves the milestone 6 cards short**,
plus the attended acts no overnight can close by design: the three operator-observed
close-bar legs (leg 3 becomes **genuinely attestable for the first time** if tonight
lands — currently unreachable per decision 178), and the still-armed ATTENDED live-camera
check from run 20260905's card 5. Derived from the live roadmap card states read this
sitting, not from HANDOFF prose.

**Recommended additions: nothing worth adding.** Every remaining overnight-able card
(E, F) is refused by the per-goal spike gate — no spikes authored, no waiver path. The
unlock is attended: Activity 0's facts + accounts, then spike sittings on E and F.
Tonight closes Activity B entirely; after this the overnight queue refills only through
the attended sittings.

## Execution notes

- **Serial, single card:** dispatch card 1, G6 it fresh-context, merge, closeout. The
  riskiest card is trivially last.
- **G6 attention note (carried from card-actuals, corrected count):** this is the
  **second** consecutive-run pattern where an APPROVE rested on a scope claim an
  independent reproduction later falsified (20260906's "Activity B closes 4/4";
  20260906-2's clause 2, stub-satisfied). The mechanism both times: **done_when clauses
  asserting a value is *recorded* were verified with stubs.** Tonight's G6 prompt must
  direct the reviewer to attack exactly that: does the clause-1 e2e provision through the
  shipped path or write `SYNC_KEY` itself? Does the clause-3 test fail the replica at the
  network layer or touch `setCampaignPolicy`? Was spike 04 re-run wrapper-free in BOTH
  recovery shapes? A green suite satisfies none of these by itself.
- **Two-session split:** not applicable — one card.
- Run mechanics inherited by reference from `reference/overnight-run-plan-20260707.md`
  (standing rules, gates G1–G6) — not restated here.
