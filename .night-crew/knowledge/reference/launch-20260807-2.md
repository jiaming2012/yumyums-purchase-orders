Tonight's autonomous run. Execute the signed slate at
.night-crew/knowledge/reference/slate-20260807-2.md.

Setup: you are on branch dev.

FIRST, before anything else: `git status --porcelain` must be CLEAN. A run branch cut
over a dirty tree smuggles foreign work into the night's commits — if the tree is
dirty, 🛑 STOP and tell the operator what is sitting in it; it is not yours to commit,
stash, or discard.

Then, before any git write, CHECK FOR UNMERGED RUN BRANCHES. Run this EXACTLY as written —
it contains no placeholder to substitute, and it asks about every date, because an
unfinished night from any date is a night's work sitting unmerged:

    git branch --no-merged dev --format='%(refname:short)' | grep -E '^overnight-[0-9]{8}(-[0-9]+)?$'

STATE THE RESULT OUT LOUD BEFORE PROCEEDING — either "no unmerged run branches" or the
list. You may only proceed after saying which one you saw. An empty result is the "go"
signal, so a command that failed or ran wrong looks exactly like a clean repo unless
you have named what you found.

- No unmerged run branch → say so, then cut this run's branch. Compute its name from
  the BARE DATE, since you are looking for every branch of that date:

      git branch --format='%(refname:short)' | grep -E '^overnight-20260807(-[0-9]+)?$' | sort -V

  No match → overnight-20260807. Otherwise the next free suffix (-2, -3, …). A MERGED
  branch's name is still taken — run `20260807` ran in daylight today and merged, so
  the expected answer tonight is `overnight-20260807-2`; cut it and proceed without
  asking; nothing is at risk.
- ANY unmerged run branch exists → 🛑 STOP. A previous night's work is sitting on it.
  Report what is there (git log --oneline dev..<branch>, and whether
  DECISIONS-NEEDED.md carries an open section for it), then ASK THE OPERATOR to choose:
  RESUME that branch, or RECUT at the next free suffix. Do NOT pick for them — resuming
  mixes two nights on one branch, recutting walks away from a night's work, and you
  cannot tell from here which is right. This is NOT covered by the batch sign-off: the
  yes is scoped to the signed specs, and what to do with an unfinished night is not one
  of them. Whatever they answer, the existing branch is left untouched — never delete,
  reset, force, or rename it.

If the run id gains a further suffix, EVERY run-scoped name takes it with them: the
conflict log .night-crew/knowledge/reference/conflicts-<runid>.md, the run directory
.night-crew/runs/<date-with-suffix>-autonomous/, and the closeout's references to the
run. The slate keeps the name it was signed under.

Do all work on the run branch. Never push, never touch main. Hard prohibitions in
CLAUDE.md apply.

🛑 TONIGHT'S THREE STANDING RULES (slate §"The three standing rules of this night"):
1. PRODUCTION STAYS UNTOUCHED. No leg contacts the production container, the yumyums
   database, or ANYTHING on the shared :5433 cluster — no test, no probe, no psql,
   read-only included (operations/P-1; the 2026-08-06 destruction was a G6 probe).
   Test suites run on :5434 — yumyums-test-pg, role hqtest, `task test:db:up` — real
   since run 20260807 W0. Spike infrastructure is spike A's own stack plus scratch
   containers created and destroyed per run.
2. GIT-OPERATE ONLY FROM A DEDICATED RUN WORKTREE, never the main checkout (decision
   160, ledger §T-40 — run 20260807's first merge landed on dev because a concurrent
   attended session moved the main checkout). Immediately after cutting the run
   branch, create the worktree (e.g. under ../hq-worktrees/run-20260807-2) and do ALL
   run git ops there.
3. ONE SUITE IN FLIGHT, EVER — no two Playwright runs, no Go suite alongside a
   Playwright run (sync.spec.js load-sensitive; B-27/B-30/B-32 armed). Corollary for
   tonight: ONE SPIKE AT A TIME on spike A's live substrate stack — both cards consume
   it in RECONCILE MODE, NEVER DESTROY (the spike-B discipline; run 20260806 saw a
   compose op recreate spike containers and move their ports mid-leg).
Also: the preserved branch card/a3-rls-fixture-own and its worktree are UNTOUCHABLE —
they are the attended re-gate's evidence (ledger §T-38 decision 155).

Authority: batch sign-off was given by the operator on 2026-08-06 (evening) — the
slate document is the record. Do NOT pause for per-change sign-off. No operator fork
is open (ledger §T-40; DECISIONS-NEEDED for run 20260807 closed empty), and you never
choose a fork — what to build, what a milestone means, anything only the operator can
settle. Each card's PARK note in the slate names its operator-only discoveries; 🛑 for
BOTH spikes, a RED VERDICT IS THE DELIVERABLE, NOT A PARK and NOT A FAILURE — record
the finding with the script's output and move on.

Park is NOT the default answer to a gray area. Route it through
`night-crew decisions log` and act on the verdict it PRINTED, never on an inference
from the disposition. (The binary lives at /home/jcole/go/bin/night-crew — the
non-interactive shell's PATH does not carry it.) Two verdicts, four producing states
(CLAUDE.md §"Slate workflow" step 5 is the doctrine of record):
  - `verdict: decide` under an adopted delegation policy — recorded delegated,
    `ratification: pending`, back at triage. Deciding is not settling.
  - `verdict: decide` on the citation floor with NO policy involved — owes no
    ratification. You will meet this; do not read `decide` as "a policy covered it".
  - `verdict: park` with the decree read — prints the verdict and exits 0. An
    escalation is a correct outcome, not a command failure.
  - park because the decree could not be READ — fails, exits non-zero, renders `park:`
    inside its error, and prints NO `verdict:` line at all. Grepping for
    `verdict: park` misses this one; read the exit code.
An escalation and a park are the same act, which is why there are two verdicts and not
three. A delegation converts a question that WOULD have escalated into one that
resolves; it is not the only way a question resolves — never restate this as "decide
only under a policy, otherwise park".

Rules of engagement:
- Standing rules and verification gates are inherited unchanged from
  .night-crew/knowledge/reference/gate-ladder.md — this repo's ladder: G1, G2(Go),
  G2(Playwright), G3 (N/A — openspec absent), G4, RF (red-first, decision 153), G6;
  plus the mandatory per-leg isolation stanza, the capture-the-whole-log rule, and
  B-87's tests/-anchored Playwright filter form. 🛑 That file is the ONLY mechanics
  source — reference/overnight-run-plan-20260707.md has never existed in this repo.
- Run mechanics that cost time to rediscover (CLAUDE.md §"Run mechanics"):
  export PATH="/usr/local/go/bin:$PATH" before ANY Go or Playwright leg; go test with
  -p 1; DB_TEST_URL must be SET or DB-coupled Go tests silently skip while printing ok
  — check counts, not just ok/FAIL (expect workflow=35-shaped counts). All test
  coordinates point at :5434 (`task test:targets` prints them read-only) — never
  :5433. Full-suite baseline on the new container is ~21–26m; a wall-clock in that
  band is normal, do not re-arm on it.
- Model routing (carrying the operator's run-20260807 pattern; planner's call,
  stated): the orchestrator (you) and every G6 adversarial reviewer run on Fable.
  Both implementer subagents run on Opus — C is first-of-kind (mechanism-establishing)
  and D is the first live-Realtime proof; neither fits the bounded-scope Sonnet lane.
- Dispatch model: you are the control loop, not the implementer. For each card:
  create a worktree off the run branch, spawn a FRESH subagent scoped to exactly
  that card (paste its slate entry, footprint, PARK note, and the red-verdict-is-
  the-deliverable rule into the subagent prompt; it drafts, implements, runs
  G1–G4 + RF in its worktree, and returns a compact report), then spawn a SEPARATE
  fresh subagent for the G6 adversarial review, then merge to the run branch and
  archive yourself. Never implement a card in your own context; your context holds
  reports, not diffs. Subagents cannot spawn subagents — G6 dispatch is always yours.
  A failed card PARKS per the slate rules (worktree preserved, DECISIONS-NEEDED.md) —
  dispatch the next card. Editing a file outside the card's footprint is NOT a park
  (§15ad.65) — see the conflict rules below.
  🛑 G6 probes obey tonight's standing rule 1 absolutely: no probe may resolve any
  database name against :5433. The 08-06 incident was a G6 probe.
- Order: SERIAL — C spike-c-round-trip first (load-bearing; its acceptance: the spike
  script's exit status IS the verdict, green or red, with the mechanism finding named
  in the report either way). Then D spike-d-realtime-live. 🛑 D RUNS REGARDLESS OF
  C'S COLOUR — C red is a recorded finding, not a stop; the Activity 3–5 build-card
  gate binds the NEXT slate, not tonight. Between cards, leave spike A's stack
  reconciled, never recreated.
- Spike discipline (both cards, from the slate): the verdict is the script's exit
  status, never prose (the spike↔script rule). Every leg fails loud; "could not run"
  exits distinct from "ran and failed" — a silent no-op green is the defect class
  this milestone exists to retire. Do not grind a red toward green past the card's
  estimate: record it. B-148 residual: the spike-B harness's red path was never
  re-rehearsed after its fix — if a spike reuses that harness, treat its recovery
  path as unproven and verify cleanup yourself.
- Per-change mechanics — conditioned on the slate's recorded workflow-preflight
  verdict for this repo (openspec: absent): do NOT assert OpenSpec mechanics and do
  NOT create openspec/ scaffolding in this repo. Assert only the universal mechanics:
  red-first evidence (gate RF — a failing test, probe, or captured failing command
  with its exit code, BEFORE the fix; for a spike, the script red on the unproven
  route before the mechanism work), atomic commits, a change-identifying commit
  trailer on every commit, and the roadmap/status flip for the card in the same
  change set. Which per-change discipline this repo should adopt was answered:
  branch-and-commit, no OpenSpec (decision 140).
- Merge-intent note (REQUIRED, §15ad.65): each subagent writes
  .night-crew/runs/2026-08-07-2-autonomous/merge-intents/<card>.md in its FIRST
  commit, before implementing — shared files touched (each file outside its own
  packages, one line of why), what must survive any merge, what is safe to drop, AND
  the card's `## Red-first` section (gate RF reads it there). Empty fields say
  "nothing here" explicitly. C's merge-intent must state any `backend/internal/sync`
  probe code as clearly-marked spike code; D's must state that
  tests/sync-rxdb-client.spec.js's config-level assertions stay green. Paste this
  requirement into every implementer prompt.
- Conflicts, not fences (§15ad.65): the footprint is planning information, not a
  boundary — a card MAY edit any file, and doing so is not a breach and not a park.
  Collisions surface as ordinary git conflicts. YOU resolve them at merge by reading
  BOTH sides' merge-intent.md and resolving against intent, not text; then re-run
  G1+G2. PARK the later card ONLY when, having read both intents, you still cannot
  determine which behavior is correct — the park ladder does NOT relax this stopping
  condition. An unplanned lifecycle row, a change to the escalation rules, a ceiling
  hit, a top-severity vote, or any other operator-only question still PARKS. Never
  improvise forks.
- Conflict log (REQUIRED, §15ad.66): append an entry to
  .night-crew/knowledge/reference/conflicts-20260807-2.md for EVERY merge, clean or
  conflicted — the cards involved, files and hunks, the intents you read, the
  resolution you took, and the gate result after it. Clean merges get a one-line
  entry, so an empty log never reads as "no conflicts" when it means "the logging
  never ran". Commit it with the merge. Summarize it in the closeout; morning triage
  audits it.
- Budget discipline: prefer a clean early exit over starting a card you cannot
  finish cleanly. C is the widest risk (mechanism-establishing, planned at its 150m
  high end) — but unlike an infra card, C struggling is NOT a reason to stop the
  night: a mechanism that will not come green inside the estimate becomes a RED
  verdict with the finding recorded, and D dispatches next. Only an actual park
  (PARK-note territory) or a gate failure parks the card.

Closeout (ALWAYS, leave ~30 min): update HANDOFF.md (per-card outcomes table, gate
evidence on the final tree, next actions) and DECISIONS-NEEDED.md (anything
parked), commit as docs. The morning reader is the operator — write for them. Name
explicitly: the four D-KR1 spike verdicts and their colours (A GREEN, B GREEN, C ?,
D ?), whether the Activity 3–5 build gate is now open or shut, and the attended work
still waiting: the A3 re-gate, the decision-156 Mercury backfill, B-146's SFTP key
fix, any remaining decision-159 archive_mode enablement, and the decision-158
sales-processor message.

Your FINAL CHAT MESSAGE is a hand-off, so CLAUDE.md §"Context checkpoints at
hand-offs" binds it. Do not end on a results summary. End with, in this order:
1. Next steps, led by morning triage — what is waiting, what is parked, what the
   operator has to decide. State C's verdict and what it means for the next slate
   (build cards un-gated, or a route re-plan).
2. The context-checkpoint verdict with its reason. For a finished run it is
   🔄 Clear now (recommended) — /nc-morning-triage is designed to start cold and
   reviews the run branch on its merits; nothing in your context is load-bearing
   because HANDOFF.md, DECISIONS-NEEDED.md, and the conflict log carry it all. Say
   ⛔ instead only if something real is still in flight in this session.
3. The paste-ready next command: /nc-morning-triage
Writing HANDOFF.md does NOT discharge this. The file and the closing message are
different surfaces read at different moments, and the operator reading the last
thing on screen at 7am is the one this is for.
