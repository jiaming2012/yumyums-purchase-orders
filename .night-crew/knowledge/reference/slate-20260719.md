# Slate — 20260719 (overnight run for the morning of 2026-07-19)

> **Run branch:** `overnight-20260719` (cut from `dev`; never pushed, `main` untouched).
> Labelled as the next increment after `overnight-20260718` (merged/triaged 2026-07-18).
> **Sign-off:** batch sign-off given by the operator **2026-07-17** — this document is the record.
> **Dispatch mode:** **serial** — trivially. Read-only closeout, one isolated env; there are no
> independent tracks and no app-code contention, so the §5 concurrent/serial question is degenerate
> (this matches the prior gate, `overnight-20260716`).
> **Forks resolved:** the one open operator decision — Activity 7 (prod ops) sequencing vs the gate —
> was resolved **2026-07-17** to **"Gate now, prod KRs pending"** (recorded in `ledger.md`
> §"Slate-plan resolutions (2026-07-17)"): the read-only gate runs tonight and attests the dev-side
> deterministic stack; the 2 prod-dependent KRs are scored **PENDING** with exact verify commands;
> Activity 7 runs attended AFTER, as the milestone's ship step, in correct order
> (attest-green-before-ship). The card itself carries no other operator decision.
> **Precondition flags:** **SATISFIED — DB is the ephemeral Docker env** (`docker-compose.nc.yml`,
> postgres:16), the canonical local path; no host Postgres to arm. The gate is judged **not** on a
> literally-clean suite but on **`task test` exit 0 over the defined deterministic stack** + **0 new
> uncategorized reds** vs HQ's documented ~37–41 flaky/data-dependent/SW-blocked baseline. The
> convergence suite is now a **no-retry adoptable hard gate** (operator rider retired at triage
> 2026-07-18) — this is the criterion this cycle adds over 07-16's waived substitute.
> **Night budget:** none supplied (`/nc-slate-plan` invoked with no `--night`). Coverage-complete,
> not budget-limited: the `cycle-gate` card is the entire remaining unattended-dispatchable work for
> this cycle (Activity 7 is operator-gated/attended by design). A thin read-only night here is honest
> (design-findings NF-1: "budget is a floor, don't manufacture busywork").
> **Ledger basis:** `reference/card-actuals.md` §"overnight-20260716 (the CYCLE-GATE closeout)" — the
> matching size class. Sizes on **1 full-suite run + a few-minute audit/compute per evidence leg**,
> NOT the build-card shape.

## Target

**Activity 8 — cycle gate (last, serialized):** `cycle-gate`. Activities 1–6 are DONE
(edit-propagation design + build + convergence matrix + test-debt retirement, all G6-verified);
the convergence-suite rider blocking a no-retry hard gate was **retired 2026-07-18**. Activity 7
(prod ops) is operator-gated/attended and, per the resolved fork, runs AFTER this gate.

Traceability: the gate closes out **all 16 KRs** across Product / Delivery / Engineering / QA
(`okrs.md`, signed 2026-07-16) with an evidenced per-KR scorecard, retires **carried waiver #1**
(`task test` exit 0 on the deterministic stack) and **waiver #2** (vacuous tests 18→0), records this
cycle's median WO cycle time against the **N=23 / 22m28s** baseline (T-14), and produces the closeout
doc + ledger append.

## Card split (mechanical — mirrors overnight-20260716)

The single `cycle-gate` roadmap card bundles four mechanisms (suite attestation · compute · scorecard
· closeout doc); per §4 it fans into **3 read-only cards + orchestrator-assembled closeout**. No
operator decision in the split.

## Estimate (no `--night` → no cut line; sizing for expectation only)

| Card | Class | Estimate (basis: 07-16 gate actuals) | PARK risk |
|---|---|---|---|
| `cycle-gate-suite-baseline` (Wave 0, gating) | read-only + 1 full-suite run | **~20–30m** (07-16: ~20m; +convergence no-retry subset re-run) | **Medium** — see trigger |
| `cycle-gate-attestation` | read-only audit (subagent) | **~4–6m** (07-16: ~3.5m; more to attest — editprop build + matrix) | Low |
| `cycle-gate-scorecard` | read-only compute (subagent) | **~3–5m** (07-16: ~2m; 16 KRs vs 9) | Low |
| **Critical path (serial)** | — | **~30–45m subagent/suite wall-clock + orchestrator assembly** | — |

~30 min closeout reserved off the top regardless. No cut line (no `--night`); all three cards are the
whole slate.

## Shared surfaces (serialization rules)

Read-only closeout ⇒ no app-code contention. Recorded for the footprint contract:

| Surface | Rule |
|---|---|
| Ephemeral Docker env (`docker-compose.nc.yml`, pg16) | One isolated env for the suite run (own Docker-assigned port; **must not touch** any host `:5432`). Serial ⇒ zero contention. |
| `okrs.md`, `roadmap.md`, `ledger.md`, `card-actuals.md`, run HANDOFF/DECISIONS-NEEDED | **Orchestrator-only writes** (roadmap card flip → DONE + milestone-boundary note; ledger append; closeout doc). Subagents are **read-only** and return their section as a report. |
| Production code / `sync.js` / `workflows.html` / backend | **Untouched.** This is a closeout; any diff here = out-of-footprint → PARK. No `task sw`. |

## Wave 0 / tracks

One track, three cards, strict serial order (2 and 3 may read in either order but are cheap; keep
serial for a clean single-env timeline). **Card 1 gates**: cards 2–3 append evidence to whatever it
attests.

### Card 1 — `cycle-gate-suite-baseline` *(Wave 0, gating; read-only + 1 suite run, ~20–30m)*

Orchestrator brings up an **isolated** pg16 env (own port; never touch host `:5432`) and runs the
deterministic stack: Go units (`go test ./...` — the `internal/receipt` `ANTHROPIC_API_KEY` red is a
known env-gated exclusion) + Playwright (`task test`). Then:

1. **Attest `task test` exit 0 over the defined deterministic stack** — the load-bearing new
   criterion this cycle (**retires carried waiver #1**). Define the stack explicitly: **excludes** the
   SW-blocked/offline tests (`serviceWorkers:'block'`, `playwright.config.js:29` — structurally
   unreachable) and the documented data-dependent/persona-fixture guards that need per-card seeds;
   **includes** the two-device convergence suite (`sync.spec.js`), now no-retry-adoptable per the
   retired rider. State the exact include/exclude set and the command in the WO record.
2. **Categorize every red** against the documented ~37–41 flaky baseline (SW-blocked/offline ·
   reload/tab-persistence · cross-test DB-pollution · data-dependent/persona guards). Run the
   **targeted isolation re-run** (fix-adjacent tests on a fresh single-test DB) to separate cross-test
   pollution from structural/seed causes — the 07-16 discipline: a single green run is not proof.
3. **Prove the convergence suite zero-flake under `--retries=0`** (repeat-run streak recorded) so the
   Eng "convergence matrix — 0 cells red" + Delivery "convergence proof (no-retry hard gate)" KRs rest
   on demonstrated determinism, not one pass.

**Footprint:** read-only + one isolated suite run. **PARK trigger:** **any uncategorized red** (a red
not attributable to a documented baseline cause) → PARK to DECISIONS-NEEDED with the specific test +
observed failure; do **not** hand-wave the deterministic-stack attestation. If exit-0 over the full
defined deterministic stack proves unreachable, attest the **substitute** ("0 new uncategorized reds
vs baseline") explicitly and mark the Eng waiver-#1 KR **PARTIAL**, not PASS — never silently.

### Card 2 — `cycle-gate-attestation` *(read-only audit subagent, ~4–6m)*

Fresh read-only subagent; returns its section as a report (orchestrator writes). Attests, each with a
cited artifact (git SHA / PRD line / spec test):

- **Product KR1** — 100% of `PRD-data-integrity` requirements trace to a reproduced failure or a named
  invariant (the PRD trace table, audited).
- **Product KR2 / Delivery repro-pair** — the edit semantic is recorded as 1 sign-off-ratified decision
  AND encoded as **≥ 2 passing acceptance tests** (mid-run edit re-renders open devices with surviving
  answers; a submitted checklist is unaffected by later edits) via the rewritten
  `repro-cut-task.spec.js`; **exactly 1 recorded red + 1 green** run in the WO record.
- **Engineering KRs** — stable identity (`updateTemplate` upsert, `replaceTemplate` reinsert deleted),
  loud rejection (distinct 422 envelope, 0 dead-id 200s), transactional op emission, convergence matrix
  0 red — each to its landed commit (`6a483d1`, `0d49f27`/`1c7c73c`, `72fffba`/`6c3aafb`, …).
- **QA KR2** — 100% of this cycle's **fix**-WOs carry red-run evidence (bug-fix protocol denominator).
  Note the squash-merge caveat (Activity-4/5 bundle test+fix, so the pre-fix failing commit is not
  always standalone-reconstructable — **carry, don't hide**, per T-14).

### Card 3 — `cycle-gate-scorecard` *(read-only compute subagent, ~3–5m)*

Fresh read-only subagent; returns the scorecard table (orchestrator writes). Computes:

- **Median WO cycle time this cycle vs baseline N=23 / 22m28s (T-14).** ⚠ **Instrumentation gap:** the
  07-17 run (9 cards) was **not** per-card timed; only 07-18's single card (~73m impl / ~16m G6) was
  measured. So a full median is **not computable** — mark the Delivery "median WO cycle ≤ baseline" KR
  **PARTIAL**, report what IS measured (07-18's card + the 07-14/07-15 tables), name the gap, and log a
  fix-forward (per-card instrumentation, already noted in `card-actuals.md`).
- **Full 16-KR scorecard** across Product (3) / Delivery (4) / Engineering (5) / QA (4). Expected
  dispositions to verify (not assume):
  - **PENDING (2, prod-gated → Activity 7 attended):** Delivery "prod parity" (`task version` prod ==
    local `version.go`), QA "prod ghost item resolved" (`SELECT count(*) FROM purchase_items WHERE
    trim(description)=''` → 0 in prod). Record the **exact verify commands** so the post-gate ship step
    flips them without re-deriving.
  - **N/A (1):** QA "down-migration proven + pre-deploy backup" — **no schema migration shipped this
    cycle** (frozen-at-submit deleted the versioning schema; `okrs.md` premise). Mark N/A with that
    reason, not PASS.
  - **PARTIAL (1):** Delivery median cycle time (above).
  - **Retires waiver #2:** QA "vacuous tests 18→0" (`vacuous-tests-18-to-0`, `3f68cc9`).
  - Remaining ~11 KRs: attest against Cards 1–2 evidence.

### Orchestrator closeout (control-loop, not a card)

Assemble `reference/cycle-closeout-20260719.md` from the three reports; flip the `cycle-gate` roadmap
card → **DONE** with a **milestone-boundary note** ("Nothing silently lost" cycle closed, prod KRs
PENDING Activity 7); append `ledger.md` (new T-entry: gate ratified, scorecard tally, waivers #1/#2
disposition, prod KRs pending); write HANDOFF (per-card outcomes + gate evidence) and
DECISIONS-NEEDED (the 2 PENDING prod KRs as the operator's attended follow-up, with commands).

## Explicitly NOT in this slate

- **Activity 7 — prod ops** (`prod-ghost-item-rename`, `prod-deploy-parity`) — operator-gated: prod DB
  mutation + operator-run `task prod:deploy`. **Runs attended AFTER this gate** (resolved fork
  2026-07-17): the gate attests the deterministic stack green, then you deploy the attested build +
  run the ghost cleanup, then a short parity-confirm flips the 2 PENDING KRs and formally closes the
  milestone. Correct order — never ship ahead of the green attestation.
- **The editprop follow-ups in BACKLOG** — F-B (transactional Create/Archive op emission), F-C (atomic
  approval+feedback tx), F-E (`waitForResponse` over fixed flush), D-1 (fail-note conflict live-render
  on the `applyOp`/409 path). None clears the §1.5 architecture-blocking promotion bar; all are
  next-cycle `/nc-okr-session` feedstock, not gate work.
- **Any app-code change.** This is a read-only closeout — a production diff = out-of-footprint → PARK.

## Execution notes

- **Serial, read-only, single isolated env.** Bring up one pg16 env on its own Docker-assigned port
  (**never** touch host `:5432`), run the suite, tear down `--volumes`. No two-session split.
- **No `task sw`** — no HTML/JS asset changes (closeout only).
- **A PARK here is an exit signal, not a stop mid-run:** the only PARK trigger is Card 1's uncategorized
  red. If it fires, PARK precisely (which test, observed failure) and still let Cards 2–3 produce their
  attestation/scorecard around the parked criterion (mark the affected KR PARTIAL/PARKED, not PASS).
- **Instrumentation fix-forward:** the scorecard must state the 07-17 per-card timing gap plainly and
  carry the fix-forward — don't fabricate a median.
- **Run mechanics (standing rules, gates G1–G6)** are inherited by reference from
  `.night-crew/runs/2026-07-09-attended/slate-20260710.md` → "Run mechanics" (the HQ baseline gates;
  the later-slate citation to `reference/overnight-run-plan-20260707.md` points at a file absent from
  this repo — same layout-drift class as the `/nc-status` backlog item). For a **read-only** gate the
  build gates G1–G4 are largely N/A; the load-bearing gate is the suite run itself + the isolation
  re-run discipline, exactly as 07-16.
