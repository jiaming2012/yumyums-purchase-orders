# Slate — run `20260808-2` (night of 2026-08-07 → morning 2026-08-08)

**Run branch:** `overnight-20260808-2` — the bare date `20260808` is taken by the merged
attended-daytime run launched 2026-08-07 morning immediately post-triage of `20260807-2`
(T-40-precedent pattern); a merged branch's name is still taken, so tonight takes `-2`.
**Sign-off:** GRANTED 2026-08-07 evening (operator, this session) — see §Sign-off record.
**Forks resolved:** ledger **T-43** (recorded this session, committed with this slate):
(a) Approvals tab stays on re-fetch — partial resolution of B-43; (b) **My Checklists read
path deliberately kept OPEN by the operator — no card may decide it**; (c) concurrent
multi-checklist fill work is a **recorded product requirement** (operator: setup + food-prep
checklists worked simultaneously) — resolves B-63's direction. Dispatch mode chosen at
§2b.3: **SERIAL** (operator, same session).
**Workflow preflight verdict (recorded, exit 0):** `openspec: absent` (no `openspec/`),
`gsd: detected` (`.planning/` present — deliberate, not corruption; real state is
`.night-crew/knowledge/`). **The launch prompt asserts universal mechanics only** — no
OpenSpec scaffolding, no GSD mechanics (per-change discipline adoption is B-105, an
operator question).

## Night budget (wall-clock ONLY — read this paragraph as part of the cut line)

`--hours 8` = 480m, minus ~30m closeout reserve = **450m usable**. 🛑 **This slate is sized
against the calendar and nothing else. The account's token quota — tier, ceiling, current
consumption, reset window — was NOT measured and cannot be measured from here.** The run may
exhaust quota before the clock or the clock before quota; neither validates the other. Spend
is governed by the run's own stop conditions, never by the 8h figure. If a rate-limit stall
hits, the run degrades gracefully (finish the in-flight card, closeout) rather than sitting
blocked.

**Precondition flags:**
- `task sandbox:e2e` — **no such target exists in hq**; the template's attended-sandbox
  precondition is N/A here, stated rather than silently skipped.
- Armed reds EXPECTED in any full Playwright run — `tests/inventory.spec.js:883` (B-27),
  `tests/sync.spec.js:446` ([LST-17], stays armed per T-42), `tests/receipt-carousel.spec.js:123`
  (B-162, candidate B-32 family). An occurrence is evidence for the flake trail, not a card
  failure; retire only by diagnosis (decision 100).
- 🛑 `npx bddgen` before every Playwright gate leg (B-165 — without it the suite silently runs
  797 of 798 and looks clean).
- 🛑 Never invoke any `backend:*` task bare (B-164 — defaults resolve :5433, the production
  cluster). `task test:targets` is the read-only coordinate source; all test coordinates are
  **:5434** (`yumyums-test-pg`, role `hqtest`).
- Isolation DB names minted in `TEST_DB_NAME_PATTERN` shape: `^hq_test(?:_[a-z0-9]+)*$`
  (`scripts/reset-e2e-db.js:115`; T-42 convention).
- `export PATH="/usr/local/go/bin:$PATH"` before any Go or Playwright leg.
- `HQ_SYNC_SUBSTRATE_OPTIONAL` and `HQ_SYNC_GATE_CHILD` must be attested UNSET in G2 logs.

## Shared surfaces and serialization

| Surface | Cards touching it | Rule |
|---|---|---|
| `workflows.html` | ALL of C1, C2, C3, S1; S2 reads it | Serial dispatch = landing order = slate order; every card's diff here re-runs the full Playwright suite (~26m, priced in) |
| `sync-rxdb/bootstrap.js` | C2 (call site), S1 (B-64 banner) | C2 lands first; S1 rewrites the banner only after the lifecycle it documents exists |
| `sync-rxdb/client.js` | C2, C3 (read/extend), S1 (cancel-rule banner) | Same landing order |
| `tests/sync-rxdb-client.spec.js` | C1 (guard rework), C3 (two-concurrent-fill test) | C1's object-level guard is the floor C2/C3 build on |
| `Taskfile.yml` | S2 only (`demo:sync` target) | No overlap |
| `night-crew.toml` seams | `workflows.html`/`sync-rxdb/*` → full suite | Check the footprint map per card; a narrow entry is how regressions escape (standing rule) |

## Per-card estimates — basis stated (§2b.1)

End-to-end = implement + G6 + one fix round + merge. The ledger's standing lesson (three
cycles running): implement-only estimates are roughly half the answer; budget a fix round for
every card of the app-code class. Anchors: sync-class end-to-end median **~103m** (Basis A,
N=11, "Sync foundation" close) and **106m28s** (run 20260806, N=5, every card took a fix
round); clean-path serial nights **~59–72m** (20260804/07/07-2); S-class test/guard cards
**~68–77m** (20260804 A2/A6); 🅢 spike-class scripts **~59–65m** actuals.

| # | Card | Class | Estimate (end-to-end) | Basis |
|---|---|---|---|---|
| C1 | `skeleton-offline-ownership-honesty` | S, test-guard rework | **60–90m** | 20260804 A2 (76m45s) / A6 (67m33s); test-only band |
| C2 | `skeleton-one-row-end-to-end` | first-of-kind app build (page wiring + sync client) | **100–150m** — risk-flagged, cut line uses 150 | sync-class medians 103m/106m; first-of-kind analog `sync-rxdb-browser-delivery-spike` 145m01s |
| C3 | `activate-fill-view-reads` | app build, same class | **90–140m** | sync-class medians 103m/106m; fix round priced in |
| S1 | `list-views-decision-recording` (reshaped `activate-list-views-or-state-they-stay-rest`) | S–M, docs+banner+test | **45–80m** | 20260804 A4 (31m, analysis pre-done) to A6 (68m); scope shrunk by T-43 |
| S2 | `demo-sync-target` | spike-script + integration | **60–110m** | 🅢 class actuals 59–65m (C/D/E) + Taskfile integration margin |

## The cut line (SERIAL — the chosen mode)

Cumulative at the **high** end: C1 90 → C2 240 → C3 **380m**, + 30m closeout = **410m of
480m — fits with ~70m margin.** S1 would take the high-end total to 460m > 450m usable →
**S1 and S2 are budget-gated stretch** (start a stretch card only if everything above landed
clean AND its own high estimate + the 30m closeout are still in hand — the computed
slate-20260709 card-6 rule). At mid estimates the night lands C1–C3 in ~315m and both
stretch cards are plausible; that is what stretch is for.

**The other mode, for the record (§2b.3):** concurrent (C1 ∥ C2 at the start only — the
dependency chain kills all other overlap) fits four cards above the line: max(90,150) + 140
+ 80 = 370m + 30m = 400m, ~50m margin — but both overlapped cards edit `workflows.html`, the
two full suites queue on the lock anyway, and 20260802's evidence is "concurrency cost more
than it bought." Operator chose **serial** with both projections on screen. Card lists
differed (3 vs 4 above the line); the choice was coverage-vs-cleanliness, and cleanliness won.

## Wave 0 / serial order

### C1 — `skeleton-offline-ownership-honesty` (Activity 3) · closes B-88

**Scope (2–4 lines):** Replace the three source-text `expect(src).not.toContain(…)`
assertions (`tests/sync-rxdb-client.spec.js:1468-1470`) with **object-level** enforcement of
the rule "nothing may read from RxDB on a code path that can execute offline": load the page
and assert `window.HQSync.db` is `undefined` (and no RxDB collection reachable) at end of
load. `workflows.html:3588-3592`'s `defaultStore()` reads `window.HQSync.db` — the fourth
route the current guard does not name. Runs FIRST so the invariant is real before C2 creates
the first database.
**Footprint:** owns `tests/sync-rxdb-client.spec.js` (+ sibling specs); touches
`workflows.html` (read-mostly; edits only if the guard needs a hook).
**Risk notes:** must define the flag-off contract C2 will satisfy: with the sync flag OFF,
page load leaves `HQSync.db` undefined. Coordinate via merge-intent.
**PARK note (narrow):** none expected — a discovery that the rule itself needs re-stating
(a new invariant category) is the operator's → park.

### C2 — `skeleton-one-row-end-to-end` (Activity 3) · the milestone's spine

**Scope:** Thread ONE checklist row from the real write path (`POST
/api/v1/workflow/saveResponse`) to an RxDB-served read in dev, behind an **explicit flag**,
both list views and the fill view still on REST. The **first production call site** of
`createHQSyncDatabase()` + `startHQReplication()` in the repo's history. Carries decision
126's shape **verbatim and cited** (reads on RxDB, `/saveResponse` + `/submit` keep owning
writes — a build card may not propose the split itself). 🛑 **Carries spike E's condition
verbatim (T-42):** trusted checkpoint catch-up with NO explicit resync step is valid ONLY
while the relay stays trigger/NOTIFY-driven (spike C's mechanism); a polling relay on a
business watermark reintroduces the missed-UPDATE hazard (`submitted_at` never advances
after INSERT). If the card contemplates polling, an explicit resync step comes back as a
requirement.
**Footprint:** owns `sync-rxdb/bootstrap.js` (call site), `workflows.html` (flag + wiring);
touches `sync-rxdb/client.js`, `tests/`.
**Risk notes:** riskiest card of the night by class (first-of-kind); must satisfy C1's
object-level guard with the flag off. Full suite mandatory (workflows.html seam).
**PARK note (narrow):** needs a scope widening beyond decisions 105/126, a new
`night-crew.toml` key, or any answer to the My Checklists read path (operator kept it OPEN,
T-43) → park. Everything else — including editing outside the footprint — is not a park.

### C3 — `activate-fill-view-reads` (Activity 4)

**Scope:** The checklist fill view reads from RxDB, scoped per-open-checklist (T-29 decision
105 — never all collections at once; no widening without a recorded decision). 🛑 **NEW HARD
REQUIREMENT (operator, T-43):** crew members work multiple checklists concurrently (setup +
food prep) — **multiple live fill replications at once ARE the design**: one per open
checklist, cancelled on close, checkpoints per-identifier (already independent). Regression
test drives TWO concurrent `startHQReplication` fill scopes and asserts the identifiers are
pairwise distinct (B-63's lead, in the existing recorder). Same spike-E trigger/NOTIFY
condition as C2, in the card text.
**Footprint:** owns `workflows.html` (fill view), `sync-rxdb/client.js`; touches
`sync-rxdb/bootstrap.js`, `tests/sync-rxdb-client.spec.js`, `tests/sync.spec.js`.
**Risk notes:** every user-entered value still flows `debouncedSaveField → submitOp →
POST /ops` — this card changes READS only; the persistence rule is untouched and its
back-and-reopen tests must stay green.
**PARK note (narrow):** concurrent-fill turns out to need a substrate schema/policy change
(decision 111 authorizes four rows; a fifth is the operator's) → park. A gray area the
citation floor covers → decide and file, not park.

### S1 (stretch) — `list-views-decision-recording` (Activity 4, reshaped by T-43)

**Scope:** Record what T-43 ruled, in the code that lies about it today: (a) Approvals tab
stays on re-fetch — recorded in the card's docs trail + `bootstrap.js`; (b) 🛑 **My
Checklists read path is OPEN — this card must NOT decide it**; state it as open in the
banner. (c) Fix B-64's stale scope banner (`sync-rxdb/bootstrap.js:80-86`) and restate the
cancel rule as *"cancel before re-scoping THE SAME shape"* (B-63's corrected wording) — only
after C3's lifecycle exists. Closes B-64; closes B-63 jointly with C3's test.
**Footprint:** owns `sync-rxdb/bootstrap.js` banner, docs; touches `sync-rxdb/client.js`
banner, `tests/`.
**PARK note (narrow):** if the recording cannot be written without deciding My Checklists,
park — that half is the operator's by their own choice tonight.

### S2 (stretch) — `demo-sync-target` (Activity 5)

**Scope:** Ship `task demo:sync` as a first-class deliverable: scripted-fresh environment
(spike A's stack), one field through `/saveResponse`, surfacing in an RxDB-served read (C2's
skeleton), one real checklist. Non-zero exit on any failed leg; 🛑 **"could not run" renders
as an outcome DISTINCT from "ran and failed"** (exit 2 vs 1 — B-163's contract; gate on the
script, not the `task` wrapper, which returns 201). Depends on C2 landing, not on C3.
**Footprint:** owns `.night-crew/qa/spike-supabase/` sibling script + `Taskfile.yml`
`demo:` target; touches `workflows.html` (read).
**PARK note (narrow):** none beyond the standing set — a failed demo leg is a finding, not
a park.

## Explicitly NOT in this slate

- **`gate-rls-fixture-ownership`** (Activity 1) — BLOCKED, **attended re-gate by ruling**
  (ledger decision 155; its G6 probe destroyed the production DB on 2026-08-06). Branch
  `card/a3-rls-fixture-own` + worktree preserved; the run must not touch them.
- **`dev-complete-attestation`** (Activity 5) — attended and the operator's own act, by the
  roadmap's definition. No overnight run can perform it.
- **Deciding the My Checklists read path** — operator explicitly kept it open (T-43).
- **Backlog promotions: none.** B-163/B-164/B-165 are hardening, below the §15k
  architecture-blocking bar; the observability cluster (B-139, B-81/82/86/93) stays off the
  critical path at the operator's direction (roadmap round 2026-08-05).

## Milestone remainder (as shown at sign-off)

**Milestone cards still white/blocked after this slate, by name (live roadmap state):**
- `gate-rls-fixture-ownership` — BLOCKED, attended (excluded above)
- `list-views-decision-recording` — in slate but below the cut line (stretch)
- `demo-sync-target` — in slate but below the cut line (stretch)
- `dev-complete-attestation` — attended, the close bar itself

**Plain statement:** this slate does NOT close the milestone, and cannot by design — the
close bar is the operator personally running `task demo:sync` and seeing it pass. Best
night (C1–C3 + both stretch land): the milestone is left **2 cards short**, both
attended-by-design. Cut-line night (C1–C3 only): **4 cards short**. Reported in cards, not
KRs, per the sign-off rule.

**Recommended additions: nothing worth adding.** The only fork-free, split-checked white
cards in roadmap order are already in the slate (as core or stretch); everything else
remaining is attended-by-design or blocked on an attended re-gate.

## Execution notes

- **Single-session serial order:** C1 → C2 → C3 → [stretch gate] S1 → [stretch gate] S2 →
  closeout. Stretch gate arithmetic: start a stretch card only if its HIGH estimate + 30m
  closeout fit before the 8h line, computed and logged at the decision moment (20260806's
  A5-cut discipline).
- **Two-session split, if ever needed:** session 1 = C1 + C2 (the skeleton exists, guard
  honest); session 2 = C3 + stretch. The seam is after C2 — the first session leaves a
  coherent tree (flag off by default; suite green).
- **Run mechanics inherited by reference** from `reference/gate-ladder.md` — this repo's
  ladder (G1, G2(Go), G2(Playwright), G3 N/A, G4, RF, G6) and its isolation stanzas.
  🛑 `reference/overnight-run-plan-20260707.md` has never existed in this repo (the
  launch-20260808 prompt records this); the ladder file is the only mechanics source.

## Sign-off record

Presented per CLAUDE.md Slate workflow step 3 (four headings + parallel plan + attention
budget + milestone remainder) in the planning session of 2026-08-07 evening; operator
answered **"Yes — sign off."** Fork resolutions taken in the same session: T-43 (a)–(c).
Dispatch mode: serial, chosen with both projections on screen.
