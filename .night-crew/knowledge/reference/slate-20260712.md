# Slate — 20260712 (overnight run for the morning of 2026-07-12)

> **Run branch:** `overnight-20260712` (cut from `dev`).
> **Sign-off:** batch sign-off given by the operator **2026-07-11** — this document is the record.
> **Dispatch mode:** **serial** (operator choice 2026-07-11). Card list is identical under concurrent; the night is quota-bound not clock-bound, so serial was chosen for lowest burn + simplest merge ordering. Concurrent line noted below for reference.
> **Forks resolved:** none open. Activity-1 forks D-1..D-4 all resolved at triage 2026-07-10 (`ledger.md` → "Morning-triage resolutions"). No card in this slate carries an open operator decision.
> **Precondition flags:** **localhost Postgres / E2E suite NOT required for this slate** — Activity 2/3 are static inspection (grep/read source + tests). The HANDOFF standing flag names "Activities 2–5"; the hard DB dependency bites at **Activity 4** (red-first proofs against a live DB), which is the *next* slate. Arming localhost Postgres is a follow-up before the Activity-4 slate, not tonight.
> **Night budget:** `--night 6` → 6h ceiling. See cut line below.
> **Ledger basis:** `reference/card-actuals.md` (one prior run: Activity-1 PRD drafts, 8–12 min subagent wall-clock each). These are first-cycle actuals — carry the *shape*, not the raw minutes.

## Target

**Activity 2 — Confirm-absence sweeps** (next activity in roadmap order; Activity 1 is 5/5 DONE) **+ Activity 3 — Audit the WORKING** (spilled into per `--night`, fork-free & light). Depth: **inspection + status reclassification only — no app code, no fixes, no new features.** Same depth class as the proven Activity-1 run.

Deliverable: every one of the 153 enumerated flows gets a **confirmed** status —
- **Activity 2 (confirm-absence, Eng KR-1):** each app's UNPROVEN flows are grep/inspected; anything confirmed absent/stubbed graduates UNPROVEN → **BROKEN** (cited line) and spawns an Activity-4 fix-card in the backlog.
- **Activity 3 (test-audit, QA KR-1):** each app's WORKING flows are audited for statically-hollow tests (`test.skip`, guard-return, empty assertion); a vacuous test drops the flow WORKING → **UNPROVEN**.

Output = an accurately-sized Activity-4 WO backlog. Traceability: Activity 2 → Eng KR-1 (0 known-broken denominator becomes exact); Activity 3 → QA KR-1 (vacuous-test count 23 → measured), plus the WORKING/UNPROVEN reclassifications feed Delivery's Activity-4 WO list.

## Shared surfaces (serialization rules)

| Surface | Cards | Rule |
|---|---|---|
| `.night-crew/knowledge/prds/PRD-<app>-hardening.md` (status marks + tally) | that app's 2 cards | **Same app-track, serialized.** confirm-absence and test-audit both edit the *same* app's PRD status marks → they land in landing order within the app-track (confirm-absence → test-audit). Distinct file per app → app-tracks are mutually disjoint. |
| `.night-crew/knowledge/roadmap.md` (Activity-2/3 status rows) | all | **Orchestrator-only.** The control loop flips each card's row PLANNED → DONE (or DRAFTING if held for triage) on that card's G6 pass, serially. Subagents never edit the roadmap. |
| `.night-crew/knowledge/BACKLOG.md` / new Activity-4 fix-cards | orchestrator | **Orchestrator-only.** Confirmed-BROKEN graduations and WORKING→UNPROVEN drops append Activity-4 backlog entries; the control loop records them, not subagents. |

No card touches another app's source. No shared *code* surface → **no worktrees** (nothing mutates shared source in parallel; PRDs are per-app docs).

## Tracks (5 app-tracks, no Wave 0)

No cross-cutting refactor precedes these → **no Wave 0.** Five independent app-tracks (disjoint per the roadmap's app-footprint table), each = `confirm-absence` then `test-audit` for that app. Serial dispatch order below; riskiest app (Inventory) ordered **last**.

### Track A — Operations  *(exemplar app; validates the reclassification shape first)*
- **`ops-confirm-absence`** — grep/inspect Operations' **17 UNPROVEN** flows against `workflows.html` + `backend/internal/workflow/*`; confirm-absent → BROKEN (cite line), else keep UNPROVEN with the confirm note. Two passes: pass 1 UI-flow, cross-check angled at backend-only surfaces (persistence contract, approval state machine). Footprint: reads Operations only, edits `PRD-operations-hardening.md` marks. Risk: low.
- **`ops-test-audit`** — audit Operations' **10 WORKING** flows in `tests/workflows.spec.js` + `persistence.spec.js` for `test.skip`/guard-return/empty-assert → drop vacuous to UNPROVEN. Footprint: reads Ops tests only, edits same PRD. Risk: low.

### Track B — Users
- **`users-confirm-absence`** — **16 UNPROVEN** against `users.html` + `backend/internal/users/*`; cross-check angled at auth *enforcement* (invite-token expiry, 403 role refusals, grant round-trip — where Users' single-pass recall was 73%). Risk: low.
- **`users-test-audit`** — **10 WORKING** in `tests/users.spec.js` + `multi-role.spec.js`. **Folds in the known BACKLOG item:** two Access-tab tests navigate dead `#t3`/`#s3` DOM (3-tab→2-tab refactor; Access now `#s2`) — mark UNPROVEN (stale-test), note the `#t3/#s3 → #t2/#s2` repoint for the Users Activity-4 WO. Risk: low.

### Track C — Onboarding
- **`onboarding-confirm-absence`** — **11 UNPROVEN** against `onboarding.html` + `backend/internal/onboarding/*`; cross-check angled at the sign→reject→reopen state machine + video pipeline + seed idempotency. Risk: low-med (state-machine transitions).
- **`onboarding-test-audit`** — **23 WORKING** (largest WORKING set) in `tests/onboarding.spec.js` (88 KB). Risk: low-med (volume of WORKING flows to audit).

### Track D — Purchasing
- **`purchasing-confirm-absence`** — **18 UNPROVEN** against `purchasing.html` + `backend/internal/purchasing/*`; FR-18 (History) already confirmed-BROKEN. Per **D-1 (resolved):** the 5 no-UI admin/cron endpoints (`simulate-cutoff`, order lock/unlock, 3× repurchase-reset) are **by-design API/scheduler-only** — confirm handler presence, keep UNPROVEN (test-only Activity-4 scope), do **not** mark BROKEN for absent UI. Risk: med (cron surface).
- **`purchasing-test-audit`** — **7 WORKING** in `tests/purchasing.spec.js` (31 KB). Risk: low.

### Track E — Inventory  *(largest surface — riskiest, ordered last)*
- **`inventory-confirm-absence`** — **19 UNPROVEN** against `inventory.html` + `backend/internal/inventory/*` (22 endpoints); cross-check angled at backend-only surfaces (sync-receipts, reprocess-pending, Monday drift cron, the two `HQ_INVENTORY_SERVICE_TOKEN` service-contract endpoints period-summary/menu-cogs). **Trends/Cost (FR-24/25) already waived-BROKEN — excluded from this sweep** (out of the Eng-KR denominator per D-3). Risk: **highest** (largest surface).
- **`inventory-test-audit`** — **19 WORKING** in `tests/inventory.spec.js` (154 KB) + `recipes.spec.js`. Risk: med-high (largest test file to audit).

## Per-card estimates (basis: `card-actuals.md`, Activity-1 PRD-draft actuals)

Confirm-absence/test-audit cards are **lighter** than the Activity-1 PRD drafts (8–12 min): they audit against an *existing* PRD + source rather than drafting a fresh two-pass enumeration. Estimate **~6–10 min subagent wall-clock** each; high end for big surfaces (Inventory, Purchasing).

| App-track | confirm-absence | test-audit | Basis |
|---|---|---|---|
| Operations | ~6–8 min | ~6–8 min | exemplar; smallest test surface |
| Users | ~7–9 min | ~7–9 min | auth-enforcement cross-check + backlog fold |
| Onboarding | ~7–9 min | ~8–10 min | 23 WORKING to audit (largest set) |
| Purchasing | ~8–10 min | ~6–8 min | cron surface; 18 UNPROVEN |
| Inventory | ~9–10 min | ~9–10 min | largest surface (154 KB tests, 22 endpoints) |

**Critical path (chosen mode = serial):** sum of all 10 cards ≈ **~1h 40m–1h 50m**, + ~30 min closeout reserved off the top ≈ **fits in ~2h 20m with ~3h 40m margin** under the 6h budget.
**Concurrent line (reference only):** longest app-track (Inventory, 2 cards) ≈ ~20 min + closeout ≈ ~50m. Card list identical to serial. The night is **quota-bound, not clock-bound.**

## Explicitly NOT in this slate

- **Activity 4** (`prove-&-fix-unproven` WOs) — depends on *this slate's* output (the confirmed backlog), needs card-splitting (ops = 17 flows), needs **localhost Postgres + E2E**, and writes app code. Not a whole/fork-free/split-checked card → cannot spill into tonight. Next slate.
- **Activity 5** (cycle gate) — serializes last, after all Activity-4 cards.
- **Fixing anything** — a card that edits app code or tests-for-real has breached its footprint → PARK. Reclassification marks the PRD/roadmap only.
- **Running the E2E suite / touching the DB** — static audit only. Runtime-vacuous tests (green without exercising the flow) are Activity 4's red-first job.
- **Inventory Trends/Cost** — waived-BROKEN (D-3), excluded from the confirm-absence sweep.
- **New features / new field types / dependencies** — hardening only; house build unchanged.

## Run mechanics

**Inherited by reference** from `.night-crew/runs/2026-07-09-attended/slate-20260710.md` → "Run mechanics" (the HQ baseline gates G1–G4, G6 — the first slate defined them inline; not restated here).

**Card-type adaptation for this slate** (inspection/reclassification, not drafting):
- **G3 — Honest-status gate.** confirmed-only-BROKEN still binds: a flow graduates UNPROVEN→BROKEN **only** on code inspection proving it absent/stubbed (cite the line); merely-suspect stays UNPROVEN. A test drops WORKING→UNPROVEN **only** on a cited `test.skip`/guard-return/empty-assertion.
- **G4 — Two-pass gate (non-negotiable).** confirm-absence runs a first pass + a cross-check angled at the per-track backend-only blind spot named above (the angle that recovered every sub-90% miss last run).
- **G6 — Adversarial review.** A **separate fresh subagent** reviews each card's reclassifications against ONLY: the app's signed PRD, this slate's card entry, and `okrs.md`. It re-checks every BROKEN citation at the cited line and every vacuous-test call. The roadmap row is not flipped until G6 passes or the gap is parked.
- Depth reminder: **no worktrees, no app run, no DB, no E2E.** Each subagent reads app source + tests and edits its app's PRD status marks; the orchestrator flips the roadmap and appends Activity-4 backlog entries.

## Execution notes

- **Single-session serial order:** ops-CA → ops-TA → users-CA → users-TA → onboarding-CA → onboarding-TA → purchasing-CA → purchasing-TA → inventory-CA → inventory-TA (smallest/exemplar first, riskiest Inventory last).
- **Two-session split:** session 1 = Operations + Users + Onboarding (6 cards); session 2 = Purchasing + Inventory (4 cards).
- **Card → roadmap:** on each card's G6 pass the orchestrator flips that card's Activity-2/3 row PLANNED → DRAFTING (DRAFTING → DONE happens at morning triage sign-off, not overnight). Subagents never touch the roadmap.
