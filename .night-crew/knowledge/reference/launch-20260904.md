Tonight's autonomous run. Execute the signed slate at reference/slate-20260904.md
(full path: .night-crew/knowledge/reference/slate-20260904.md).

Setup: you are on branch dev.

Before any git write, CHECK FOR UNMERGED RUN BRANCHES. Run this EXACTLY as written —
it contains no placeholder to substitute, and it asks about every date, because an
unfinished night from any date is a night's work sitting unmerged:

    git branch --no-merged dev --format='%(refname:short)' | grep -E '^overnight-[0-9]{8}(-[0-9]+)?$'

STATE THE RESULT OUT LOUD BEFORE PROCEEDING — either "no unmerged run branches" or the
list. You may only proceed after saying which one you saw. An empty result is the "go"
signal, so a command that failed or ran wrong looks exactly like a clean repo unless
you have named what you found.

- No unmerged run branch → say so, then cut this run's branch. Compute its name from
  the BARE DATE, since you are looking for every branch of that date:

      git branch --format='%(refname:short)' | grep -E '^overnight-20260904(-[0-9]+)?$' | sort -V

  No match → overnight-20260904. Otherwise the next free suffix (-2, -3, …). A MERGED
  branch's name is still taken, so a date whose run merged this morning yields -2 —
  cut it and proceed without asking; nothing is at risk.
- ANY unmerged run branch exists → 🛑 STOP. A previous night's work is sitting on it.
  Report what is there (git log --oneline dev..<branch>, and whether
  DECISIONS-NEEDED.md carries an open section for it), then ASK THE OPERATOR to choose:
  RESUME that branch, or RECUT at the next free suffix. Do NOT pick for them — resuming
  mixes two nights on one branch, recutting walks away from a night's work, and you
  cannot tell from here which is right. This is NOT covered by the batch sign-off: the
  yes is scoped to the signed specs, and what to do with an unfinished night is not one
  of them. Whatever they answer, the existing branch is left untouched — never delete,
  reset, force, or rename it.

If the run id gains a suffix, EVERY run-scoped name takes it with them: the conflict
log reference/conflicts-<runid>.md, and the closeout's references to the run. The slate
keeps the name it was signed under.

Do all work on the run branch. Never push, never touch main. Hard prohibitions in
CLAUDE.md apply. 🛑 Tests and probes run on :5434 (yumyums-test-pg) or the committed
LOCAL Supabase substrate only — :5433 is dev AND production and is hard-forbidden
(decision 155). The Supabase substrate comes up via
.night-crew/qa/spike-supabase/env-up.sh in RECONCILE mode — never --fresh. Every
card that writes to a database prints its resolved target coordinates read-only first.

Authority: batch sign-off was given by the operator on 2026-09-04 — the slate
document is the record. Do NOT pause for per-change sign-off. Every operator fork is
already resolved — #5 (configurable requires_online threshold) at the slate sitting,
recorded in the slate and the roadmap's open-decisions table — and you never choose a
fork: what to build, what a milestone means, anything only the operator can settle.

Park is NOT the default answer to a gray area. Route it through
`night-crew decisions log` and act on the verdict it PRINTED, never on an inference
from the disposition. Two verdicts, four producing states (CLAUDE.md §"Slate workflow"
step 5 is the doctrine of record):
  - `verdict: decide` under an adopted delegation policy — recorded delegated,
    `ratification: pending`, back at triage. Deciding is not settling.
  - `verdict: decide` on the citation floor with NO policy involved — owes no
    ratification. You will meet this; do not read `decide` as "a policy covered it".
  - `verdict: park` with the decree read — prints the verdict and exits 0. An
    escalation is a correct outcome, not a command failure.
  - park because the decree could not be READ — fails, exits non-zero, renders `park:`
    inside its error, and prints NO `verdict:` line at all. Grepping for
    `verdict: park` misses this one; read the exit code.
An escalation and a park are the same act, which is why there are two verdicts and not
three. A delegation converts a question that WOULD have escalated into one that
resolves; it is not the only way a question resolves — never restate this as "decide
only under a policy, otherwise park".

Rules of engagement:
- Standing rules and verification gates G1–G6 are inherited unchanged from
  reference/overnight-run-plan-20260707.md. Gate legs adapt per the slate's
  shared-surfaces table: NO Playwright is owed tonight (no card touches an
  [e2e.seams] key or app code); Cards 1–2 gate on their own harnesses' exit codes
  (the B-345 standalone-harness precedent), Cards 3–4 on the night-crew CLI verbs'
  exit codes + the preservation proof. Red-first is greenfield-shaped for Cards 1–2
  (the named assertion harness reds against a bare substrate before the migration
  exists); Cards 3–4 record their pinned before-state (297-issue check red; "No runs
  to show.") — the docs-card RF form.
- Dispatch model: you are the control loop, not the implementer. For each card:
  create a worktree off overnight-20260904, spawn a FRESH subagent scoped to exactly
  that card (paste its slate entry, footprint, and spec references into the subagent
  prompt; it drafts, implements, runs G1–G4 in its worktree, and returns a compact
  report), then spawn a SEPARATE fresh subagent for the G6 adversarial review, then
  merge to the run branch and archive yourself. Never implement a card in your own
  context; your context holds reports, not diffs. Subagents cannot spawn subagents —
  G6 dispatch is always yours. A failed card PARKS per the slate rules (worktree
  preserved, DECISIONS-NEEDED.md) — dispatch the next card. Editing a file outside
  the card's footprint is NOT a park (§15ad.65) — see the conflict rules below.
- Order: no Wave 0. SERIAL — dispatch one card at a time in slate order
  1 `supabase-schema-and-rls` → 2 `redeem-rpc-race-proof` (applies Card 1's landed
  migration; serial dependency, never reorder) → 3 `backlog-machine-migration` →
  4 `team-records-from-hand-runs`.
- Model routing (from the slate's routing section — strictly class-derived, no
  per-card override, process/C-7): dispatch each implementer subagent on the model
  its card's class routes to; G6 reviews always on the session model. The routing the
  run obeys, one <card> <class> <model> line per card, between the exact markers below
  (this block is what `night-crew routing check` compares against the slate — do NOT
  hand-edit it out of agreement with reference/slate-20260904.md; reclassify in the
  slate instead):

      <!-- routing:launch -->
      supabase-schema-and-rls new-mechanism session
      redeem-rpc-race-proof new-mechanism session
      backlog-machine-migration followup-batch session
      team-records-from-hand-runs small-additive session
      <!-- /routing:launch -->

- Per-change mechanics — the slate's recorded workflow-preflight verdict is
  `openspec: absent`: do NOT assert OpenSpec mechanics and do NOT create openspec/
  scaffolding in this repo. Assert only the universal mechanics: red-first evidence,
  atomic commits, the `Night-Crew-Run: 20260904` trailer on every commit with the
  card's slug in the body, and the roadmap/status flip for the card in the same
  change set. Which per-change discipline this repo should adopt is an operator
  question (B-105) — never encode another detected convention's mechanics in its
  place.
- Merge-intent note (REQUIRED, §15ad.65): each subagent writes
  .night-crew/runs/2026-09-04-autonomous/merge-intents/<card-slug>.md in its FIRST
  commit, before implementing — shared files touched (each file outside its own
  packages, one line of why), what must survive any merge, and what is safe to drop.
  Empty fields say "nothing here" explicitly. Paste this requirement into every
  implementer prompt. Card 3 additionally records its whole-document
  token-multiset content-preservation proof there (Q-KR3 names the merge-intent as
  the proof's home).
- Conflicts, not fences (§15ad.65): the footprint is planning information, not a
  boundary — a card MAY edit any file, and doing so is not a breach and not a park.
  Collisions surface as ordinary git conflicts. YOU resolve them at merge by reading
  BOTH sides' merge-intent.md and resolving against intent, not text; then re-run
  G1+G2. PARK the later card ONLY when, having read both intents, you still cannot
  determine which behavior is correct — the park ladder does NOT relax this stopping
  condition, because no adopted policy and no citation floor tells you which of two
  implementers was right (§15ad.65 as amended). An unplanned lifecycle row, a change
  to the escalation rules, a ceiling hit, a top-severity vote, or any other
  operator-only question still PARKS. Never improvise forks. The slate's PARK notes
  are the per-card stopping conditions — Card 3's content-preservation rule is hard:
  NEVER delete or paraphrase entry content to green the checker; park by entry handle.
- Conflict log (REQUIRED, §15ad.66): append an entry to
  reference/conflicts-20260904.md for EVERY merge, clean or conflicted —
  the cards involved, files and hunks, the intents you read, the resolution you took,
  and the gate result after it. Clean merges get a one-line entry, so an empty log
  never reads as "no conflicts" when it means "the logging never ran". Commit it with
  the merge. Summarize it in the closeout; morning triage audits it.
- Card-specific obligations (from the signed slate):
  - Card 1 seeds the #5 settings surface (marketing_settings.requires_online_threshold_cents,
    default 2000) + two TEST campaigns (one requires_online per value); the production
    welcome-offer (#8) is explicitly NOT tonight's.
  - Card 2 builds redeem() with the v2 BODY (not_found via coalesce — operator-signed),
    and closes GAP-1: run the race spike (or successor harness) once against the BUILT
    migration and append the `validated:` line naming GAP-1 to
    .night-crew/knowledge/spikes/activity-a-attribution-spine/redeem-rpc-race-proof.md
    under ## Comebacks, in the same sitting.
  - Card 3 anchors done_when on the CHECKER's own "across M entries" parse (baseline
    297 issues / 207 entries), iterates on a scratch copy via check --file, and uses
    full-readers only (the document's multi-KB lines SIGPIPE early-exit pipes).
  - Card 4 lands the template + ritual step; the CLOSEOUT then emits
    .night-crew/knowledge/scorecard/20260904.jsonl for THIS run using it (first
    consumer proves the producer) and verifies `night-crew scorecard --repo .`
    renders the four roles record-backed.
- Budget discipline: prefer a clean early exit over starting a card you cannot
  finish cleanly. Substrate images are already local (the spikes pulled them);
  no docker pull should be needed — if one is, note it in timings and proceed.

Closeout (ALWAYS, leave ~30 min): update HANDOFF.md (per-card outcomes table, gate
evidence on the final tree, next actions) and DECISIONS-NEEDED.md (anything
parked) in .night-crew/runs/2026-09-04-autonomous/, emit the Card-4 scorecard record
for this run (if Card 4 landed), commit as docs. The morning reader is the operator —
write for them.

Your FINAL CHAT MESSAGE is a hand-off, so CLAUDE.md §"Context checkpoints at
hand-offs" binds it. Do not end on a results summary. End with, in this order:
1. Next steps, led by morning triage — what is waiting, what is parked, what the
   operator has to decide.
2. The context-checkpoint verdict with its reason. For a finished run it is
   🔄 Clear now (recommended) — /nc-morning-triage is designed to start cold and
   reviews the run branch on its merits; nothing in your context is load-bearing
   because HANDOFF.md, DECISIONS-NEEDED.md, and the conflict log carry it all. Say
   ⛔ instead only if something real is still in flight in this session.
3. The paste-ready next command: /nc-morning-triage
Writing HANDOFF.md does NOT discharge this. The file and the closing message are
different surfaces read at different moments, and the operator reading the last
thing on screen at 7am is the one this is for.
