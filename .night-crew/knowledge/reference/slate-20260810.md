# Slate — run `20260810` (night of 2026-08-09 → morning 2026-08-10)

**Run branch:** `overnight-20260810` — the bare date `20260810` is free (no branch of any
suffix exists; no unmerged run branch on any date — `git branch --no-merged dev` is empty,
run `20260809` merged to `dev` this morning at `486d7b2`). No collision, no suffix.
**Sign-off:** GRANTED 2026-08-09 (operator, this session) — "sign off", after a clarifying
exchange on `gate-rls-fixture-ownership` / decision 155 (excluded, separately-tracked). 2 cards.
See §Sign-off record.
**Forks resolved:** none newly required. The only operator-awareness item this activity
carried — the demo's read surface being a Node RxDB client, not the app UI (run `20260809`
DECISIONS-NEEDED item 1) — was **resolved at morning triage as ledger §T-44 / decision 161**:
the operator's intent (*"the sync capability running in my dev environment … something I can
actually use"*) requires an **app-surface read**, so `sync-live-in-dev` was cut to deliver
exactly that. That is the card this slate builds. No open fork remains on it.
**Workflow-preflight verdict (ran clean — `night-crew workflow preflight --repo .`, exit 0):**
`openspec: absent` (no `openspec/` tree; `openspec list` errors *"No OpenSpec changes directory
found"*). `gsd: detected` (`.planning/` present — the deliberate telltale; real state is
`.night-crew/knowledge/`). **The launch prompt asserts UNIVERSAL mechanics only** — red-first
evidence, atomic commits, a change-identifying commit trailer, the roadmap/status flip — and
creates **no** OpenSpec scaffolding. Which per-change discipline this repo adopts is an operator
question (B-105), not this run's. This is NOT the `undetermined` STOP case: the verb READ every
path and returned `absent` at exit 0, matching every prior run in this repo.
**Spike gate:** SATISFIED via the goal's spike ledger
(`.night-crew/knowledge/spikes/activity-5-dev-complete/sync-live-in-dev.md`), which is the
authoritative "goal settled" record. The `night-crew spikes gate` VERB could not drive — it
reads the dogfood-only `usm/roadmap.txt` story map, absent in a product-mode target repo (the
ledger's own header records this) — so the substantive gate was read from the ledger it points
to: **leg 3 (browser vs real substrate) spiked GREEN** (exit 0, `spike-f-browser-live.sh`, run
`f20260808232119`); **legs 1 & 2 recorded "no spike needed"** with reasons (proven mechanics;
leg-2 door-check folded into leg 3's script); **zero agent-reached corrections** → no batch
review owed. Ledger verdict, verbatim: *"The goal is settled and slatable."*
**Agreement check (§3b — `night-crew okr compare-slate … --expect-cards 2`, exit 0):** no
contradiction of the three kinds it reads (opposed disposition, delivery denied, a slate-excluded
card a target claims). Its coverage lines — "15 targets name no card", "2 slate cards no target
names" — are **structural to a target-repo OKR page**: `okrs.md` traces objectives→KRs and each
KR names a measurement *artifact*, not a card slug, so there is no slug overlap by construction.
The check's live value here is the `--expect-cards 2` parse assertion (2 declared, 2 read) and
the three contradiction detectors (none fired).

## What this night is

**The corrected-close-bar night.** After run `20260809` the "Sync, dev complete" milestone has
exactly one unattended-buildable card left: **`sync-live-in-dev`** (Activity 5) — the work
decision 161 cut to make the RxDB sync capability *run in the operator's persistent dev
environment and be usable in the app*, not only inside `demo:sync`'s throwaway stack. Everything
before it is DONE; everything after it is attended-by-design (`dev-complete-attestation`) or a
separately-tracked attended re-gate (`gate-rls-fixture-ownership`).

**This slate splits `sync-live-in-dev` into two cards** (§1.5 split rule — the roadmap card
bundles two normal-change-sized mechanisms in disjoint file families, and an unattended run must
not discover mid-night that one card is two):

- **Card 1 — `sync-live-in-dev-substrate`** (roadmap legs 1 + 2 + the FDW-persistence finding):
  the data plane runs **persistently** in dev and the `/sync/*` proxy door **opens** (200, not
  503). Verified by curl + a real-write→substrate poll. Owns infra/config/Go-relay files.
- **Card 2 — `sync-live-in-dev-app-proof`** (roadmap leg 3): a **red-first Playwright spec**
  drives the real `workflows.html` against the live persistent substrate — no `page.route` stub
  — and asserts one field surfaces via RxDB in the app; the same spec fails when the relay is
  stopped. Owns the spec + its gate-harness integration.

The split maps 1:1 onto the roadmap card's own four `done_when` items: items 1–2 → Card 1,
item 3 → Card 2, item 4 (the attended attestation) → `dev-complete-attestation` (excluded, the
operator's own act). Card 2 **depends on** Card 1 (it needs the persistent substrate + FDW
live to drive against), so dispatch is a **serial chain, not two tracks** — see §Dispatch.

## Estimates and critical path (wall-clock ONLY)

🛑 **Sized against the calendar and nothing else.** No `--night` was given, so there is no cut
line — these estimates exist for the attention budget and the stretch gate. The account's token
quota — tier, ceiling, current consumption, reset window — was **NOT measured and cannot be
measured from here.** The run may exhaust quota before the clock or the clock before quota;
neither validates the other. Spend is governed by the run's own stop conditions; a rate-limit
stall degrades gracefully (finish the in-flight card, closeout).

End-to-end = implement + G6 + any fix round + merge.

| # | Card | Class | Estimate (end-to-end) | Basis |
|---|---|---|---|---|
| 1 | `sync-live-in-dev-substrate` | infra + config productionization of proven mechanics, w/ novel FDW persistence | **60–100m** — risk-flagged, use 100 | Build-class median **~82m** (run `20260808-2`, N=4) as the anchor; the substrate/relay bring-up is proven mechanics being *productionized* (cf. `demo-sync-target`'s re-export lesson, ~25m for wrapping a proven harness) which pulls toward the low end — but persistent FDW→HQ pointing (`hq_pg` server + `hq_sync_fdw` LOGIN role) is genuinely NEW, config spans 3 dev targets, and the proxy's ACTIVATION-ORDER guard demands care. One fix round allowed. |
| 2 | `sync-live-in-dev-app-proof` | novel-integration test (real browser vs live substrate), **first-of-kind repo spec** | **60–110m** — risk-flagged, use 110, wide ± | Between 🅢 spike-class (~59–64m, but those *included* full suites) and build-class (~82m). The harness already exists GREEN (`browser-live/workflows-live.spec.js` from spike-f) which pulls DOWN; promoting it to a repo red-first spec against the *persistent* substrate + the gate-harness integration decision + no card-actuals analog (first-of-kind) push UP and widen the band. |

**Critical path (serial — forced by Card 2 → Card 1 dependency):** Card 1 60–100m → Card 2
60–110m → ~30m closeout. High end ≈ **240m (~4h)**; mid ≈ **~185m (~3h)**. Comfortable on a
full night. 🛑 **Both cards carry Docker + Supabase-substrate bring-up risk** (docker pull, port
assignment, substrate reconcile) that can turn a "ran and failed" into a "could-not-run" — the
run must render that distinction, not bury it, and must prefer a clean stop over a rushed
landing. **Card 1 landing alone is a coherent partial outcome** (persistent substrate + door +
relay-carries-write banked; Card 2 retryable next night without redoing legs 1–2).

## Gate cost (Q-KR4 — required, authored at sign-off, never backfilled)

- **Expected full Playwright suite runtime: ~24m** (recent measured range 21.7m–23.0m across
  runs `20260808-2` / `20260809`; use 24m for planning). 🛑 `npx bddgen` before every Playwright
  leg (B-165) or the suite silently runs short and reads clean.
- **Which cards actually pay it:**
  - **Card 1** changes `backend/internal/sync/spikec_relay.go` (Go) + compose + Taskfile + FDW
    SQL — **no `tests/*.spec.js` change**. It pays the **Go suite** (`go test ./... -p 1`, all 9
    packages, ~fast) at G2(Go); G2(Playwright) is expected **N/A-by-footprint** (no seam key
    matches an infra/Go/compose path — same shape as run `20260809`). Verify against the seam
    map per the standing rule; do not assume.
  - **Card 2** adds a spec. If it lands under `tests/` matching the `sync` token it de-confines
    the **11-file sync subset** (which INCLUDES `sync.spec.js`, the load-sensitive one) → a
    partial-to-full Playwright run. If it lands as a standalone spike-style spec (its own config,
    gated on its own exit — the `browser-live/` precedent), G2(Playwright) is N/A-by-footprint
    and the spec's own live run IS the verdict. **Either way budget one ~24m suite** for Card 2.
- **Load-sensitivity risks tonight:**
  - `tests/sync.spec.js` is the load-sensitive spec that must not share a box (night-crew.toml
    note, run `20260726` precedent). The B-27 / B-30 / B-32 flake family stays **armed** —
    retire only by diagnosis, never by passing once (decision 100).
  - **Armed reds EXPECTED** in any full run: `inventory.spec.js:883` (B-27), `sync.spec.js:446`
    ([LST-17], T-42), `receipt-carousel.spec.js:123` (B-162). An occurrence is flake-trail
    evidence, not a card failure.
  - **The novel load this slate adds:** both cards depend on the Docker Supabase substrate being
    up; Card 2 additionally drives **headless Chromium against a live external substrate** — an
    infra-coupled path no prior repo spec has run, so a red there must be triaged as
    could-not-run vs ran-and-failed before it counts against the card.

## Precondition flags (carried from slate-20260809, still in force)

- `task sandbox:e2e` — **no such target exists in hq**; the template's attended-sandbox
  precondition is N/A here, stated rather than silently skipped.
- 🛑 `npx bddgen` before every Playwright gate leg (B-165).
- 🛑 **Never invoke any `backend:*` / `test:*` task bare against a default coordinate** (B-164 —
  defaults resolve `:5433`, the **production** cluster; that is how prod was destroyed
  2026-08-06). `task test:targets` is the read-only coordinate source; all test coordinates are
  **:5434** (`yumyums-test-pg`, role `hqtest`). 🛑 **This card family is exactly where B-164
  bites** — it stands up substrate + FDW + a scratch/real HQ; every psql, curl and FDW repoint
  must be against an explicit non-`:5433` coordinate, and the spike-f harness's snapshot→restore
  discipline is the model.
- Isolation DB names in `TEST_DB_NAME_PATTERN` shape `^hq_test(?:_[a-z0-9]+)*$`
  (`scripts/reset-e2e-db.js`; T-42 convention).
- `export PATH="/usr/local/go/bin:$PATH"` before any Go or Playwright leg (non-interactive
  shell drops Go; `go: not found` looks like a test failure and is not).
- `HQ_SYNC_SUBSTRATE_OPTIONAL` and `HQ_SYNC_GATE_CHILD` attested UNSET in any G2 log.
- **The spike/substrate stack is stateful — `spike:down` is the ONLY data-destroying target.**
  Bring the substrate up in reconcile mode (`env-up.sh` / `spike:up`), never `spike:down`, so a
  re-run never destroys a prior stack. Card 1 is turning that reconcile bring-up into a
  *persistent* service — it must inherit the same never-destroy-on-restart posture.
- 🛑 **Gate red-first captures on the SCRIPT's exit code, never on `task`** (B-163 — `task`
  returns go-task's 201 on a failing command, not the script's 0/1/2).

## Shared surfaces and serialization

| Surface | Cards touching it | Rule |
|---|---|---|
| `Taskfile.yml` (root) + `backend/Taskfile.yml` | 1 (new persistent-substrate `task` target + 4× `HQ_SYNC_*` in dev targets); 2 (a `task` wrapper for the app-proof spec, at most) | Bounded overlap — different stanzas. Serial landing (Card 1 first) resolves cleanly. Each writes its own target. Card 2's merge-intent states what of Card 1's Taskfile stanzas must survive. |
| `docker-compose.supabase.yml` / a new persistent-dev compose | 1 (owns) | Card 1 productionizes the Spike A substrate + relay into a persistent dev service. Card 2 consumes it read-only (drives against it). |
| `backend/internal/sync/spikec_relay.go` + `proxy.go` env plumbing | 1 (owns the relay-as-persistent-service wiring) | 🛑 The proxy's ACTIVATION-ORDER guard (`proxy.go:78`) forbids setting `HQ_SYNC_REST_URL` in ANY deploy that is not ready — Card 1 sets the 4 vars in **dev targets only**, never prod/`docker-compose.prod.yml`. Any change here re-runs G2(Go). |
| `night-crew.toml` `[e2e.seams]` | 2 (adds the app-proof spec to a seam OR documents a standalone home) | 🛑 Adding a **roll-call spec name** is the guard working as designed (repo-hygiene precedent, run `20260806`) — do it, and bump `tests/repo-hygiene.spec.js`'s count in the same commit. Needing a **new seam KEY or TOKEN** is a schema change → PARK (operator, per the `sync`-token history). |
| `workflows.html` / `sync-rxdb/*` | 2 (READ path — expected read-only; spike-f drove the shipped page unmodified) | If Card 2 finds the read path needs a source edit to reach `served` against the persistent substrate, that is a real change (precache + seam recheck) — but the spike evidence says it will NOT (the shipped `#sync-one-row` surface reached `served` in 673ms unmodified). |

## Wave 0 / serial order

No Wave 0 — no cross-cutting refactor. Two cards, **serial by dependency**: the persistent
substrate first (Card 1), then the app-surface proof that runs against it (Card 2).

### Card 1 · `sync-live-in-dev-substrate` — Activity 5 (legs 1+2 + FDW persistence), the data plane persistent

**Scope (2–4 lines):** Make the RxDB data plane **run persistently in the operator's dev
environment**. (a) Bring up the Spike A substrate (PostgREST + Realtime) and the LISTEN/NOTIFY
relay (`backend/internal/sync/spikec_relay.go`) as a **persistent dev service** — a compose
service + a `task` target — so the data plane stays up between runs instead of being spun up and
torn down per `demo:sync`. (b) Set the **four** required vars — `HQ_SYNC_REST_URL`,
`HQ_SYNC_REALTIME_URL`, `HQ_SYNC_JWT_SECRET`, `HQ_SYNC_REALTIME_HOST` (the spike proved TWO is
not enough — roadmap named only the first two) — in the `backend:dev` / `dev:tailscale` /
`dev:lan` env so the in-server `/sync/*` proxy **opens** (today it answers 503 everywhere).
(c) Arrange a **persistent FDW→HQ pointing**: the substrate's `hq_pg` server must resolve real
per-user RLS against the operator's live `dev:tailscale` HQ (carrying the `hq_sync_*` source
views + the `hq_sync_fdw` role given LOGIN) — NOT only against a scratch container as the spike
did by repoint+restore (spike ledger §"FDW entanglement").
**done_when (from the roadmap card, items 1–2):**
- Starting the dev stack opens the substrate door: substrate + relay up and `HQ_SYNC_*` set → a
  request to `/sync/rest/…` through the running dev server returns **200, not 503** — check:
  bring up the dev stack, `curl` the proxy path against an explicit non-`:5433` coordinate,
  assert non-503.
- The relay carries a real write to the substrate: a field written via `/saveResponse` appears
  in the substrate within the Spike-A convergence bound — check: write through the running dev
  server, poll the substrate, assert the row arrives.
**Carries verbatim:** decision **126** (RxDB serves READS; `/saveResponse` + `/submitChecklist`
own ALL writes), decision **105** (scoped read, never whole), spike E's condition **T-42** (no
polling, no business-watermark resync — the relay stays trigger/NOTIFY-driven).
**Footprint — OWNS:** the persistent-substrate compose service (`docker-compose.supabase.yml` or
a new persistent-dev compose), the root/`backend` Taskfile `task` target + the 4× `HQ_SYNC_*`
dev-env wiring, the `spikec_relay.go` persistent-service wiring, and the FDW/role SQL for the
persistent pointing (substrate-side setup under `.night-crew/qa/spike-supabase/sql/` or its
persistent equivalent). **TOUCHES:** `proxy.go` only if the env plumbing needs it (read the
ACTIVATION-ORDER guard first). **NO** `workflows.html` / `tests/*.spec.js` edit expected.
**Risk notes:** the FDW→persistent-HQ pointing is the novel piece the spike flagged as the
card's real work (bring-up + config are proven mechanics). The ACTIVATION-ORDER guard means the
vars go in **dev targets only** — a leak into `docker-compose.prod.yml` is the exact defect the
guard exists to prevent. Budget the high end + one fix round.
**PARK note (narrow — operator-only questions):** needs a new `night-crew.toml` key/token, a new
terminal status, a substrate **schema** change beyond decision 111's four rows, or a scope
widening beyond decisions 105/126 → park. Setting the FDW pointing, authoring the compose
service, and wiring the 4 vars into dev targets are proven-mechanics build work the run
**decides and files** — not parks. A door that stays 503 after wiring is a **finding** (record
what curl returned), not a park.

### Card 2 · `sync-live-in-dev-app-proof` — Activity 5 (leg 3), the app shows the round trip

**Scope (2–4 lines):** Prove the sync capability is **usable in the app**. Promote spike-f's
`browser-live/workflows-live.spec.js` into a repo **red-first** Playwright spec that drives the
real `workflows.html` (flag `hq_sync_read` ON, **no `page.route` stub**) against Card 1's live
persistent substrate, enters one field through the real `/saveResponse` path, and asserts it
surfaces via RxDB replication **in the app** (`#sync-one-row` → `data-state="served"` carrying
the field's value, within a bounded time). This replaces `demo:sync`'s Node RxDB read client
with the actual app surface — closing the read-surface gap T-44 recorded.
**done_when (from the roadmap card, item 3):**
- The **app** shows the round trip: the spec drives `workflows.html` against the live substrate
  (no `page.route` stub), enters one field, and asserts it surfaces via RxDB in the app —
  **red-first**: the same spec **fails when the relay is stopped** — check: run the spec with the
  relay up (pass) and down (fail). 🛑 Gate the red-first on the spec/script exit, never on `task`
  (B-163).
**Depends on:** Card 1 (needs the persistent substrate + open door + FDW pointing live). Do not
dispatch until Card 1 has merged.
**Gate-harness integration (engineer-level, the run decides and files):** the spec drives a
**live external substrate**, so it cannot run inside the standard `:5434` `task test` harness.
Resolve at build time between (a) a standalone spike-style spec with its own config gated on its
exit (the `browser-live/` precedent — cleanest, B-345-aligned), or (b) a `tests/` spec that
**self-skips** when the substrate is absent (the `HQ_SYNC_REST_URL`-unset-is-the-interlock
pattern) and runs green in the live path. Record the choice + reasoning in the merge-intent. The
roadmap asks for a `night-crew.toml` seam entry "so the app-surface test is in a card's
footprint" — honor that as a footprint-mapping entry; if it forces a new KEY/TOKEN, PARK.
**Footprint — OWNS:** the new red-first spec + its Playwright config, and its `night-crew.toml`
footprint/seam integration (+ `tests/repo-hygiene.spec.js` count bump if a roll-call name is
added). **TOUCHES:** `workflows.html` / `sync-rxdb/*` **only if** the read path needs a source
edit to reach `served` — the spike evidence says it will not (shipped surface reached `served`
unmodified). If it does: precache + seam recheck apply.
**Risk notes:** first-of-kind repo spec driving headless Chromium against a live external
substrate. The spike proved the integration works (673ms to `served`, red-first non-vacuous with
the carrier withheld), so the mechanism is de-risked — but the repo-harness form, the persistent
(not repoint/restore) substrate, and the standalone-vs-seam decision are new. Wide ± band.
**PARK note (narrow — operator-only questions):** needs a new `night-crew.toml` KEY or TOKEN (not
a roll-call name), a scope widening beyond decision 105, or an answer to the My Checklists read
path (operator kept it OPEN, T-43(b)) → park. A spec that cannot reach `served` against a
correctly-configured live substrate is a **finding** (capture the proxy/replication logs like
spike-f did — all four collections' HTTP codes), not a park. The standalone-vs-seam choice is an
engineer decision, not a park.

## Explicitly NOT in this slate

- **`dev-complete-attestation`** (Activity 5) — 🛑 **attended and the operator's own act**, by
  the roadmap's definition (decision 161): the operator opens `workflows.html` in `dev:tailscale`
  against the persistent substrate this slate delivers, sees a field sync in the app, and records
  the outcome in `ledger.md`. **No overnight run can perform it. It is the close bar itself.**
  This slate ENABLES it (Card 2 proves the same round trip red-first, automated) but does not
  substitute for it.
- **`gate-rls-fixture-ownership`** (Activity 1) — BLOCKED, **attended re-gate by ruling**
  (decision 155; its G6 probe destroyed the production DB on 2026-08-06). Branch
  `card/a3-rls-fixture-own` + worktree preserved; the run must not touch them. Off the sync
  close-bar critical path — Activity-1 gate hygiene, not the sync close bar.
- **Deciding the My Checklists read path (T-43(b))** — the operator explicitly kept it open; no
  card here touches it.
- **Backlog promotions: none.** The observability cluster (B-139, B-81/82/86/93) stays off the
  critical path at the operator's direction (roadmap round 2026-08-05) and is a next-roadmap
  candidate; the E-KR4 fetch-storm items (`sync.js` `:443-454` / `:475-479`) are carried as
  restated, not retired, this cycle. None clears the §15k architecture-blocking bar for
  mid-roadmap promotion.

## Milestone remainder (as shown at sign-off — from LIVE roadmap state, not HANDOFF prose)

**Milestone cards still white/blocked after this slate, by name:**
- `dev-complete-attestation` — attended, the operator's own act (open the app in `dev:tailscale`,
  see a field sync, record the ledger line). The close bar.
- `gate-rls-fixture-ownership` — BLOCKED, attended re-gate (excluded above); NOT part of the sync
  close bar (Activity-1 gate hygiene).

**Plain statement:** this slate does **not** close the milestone, and cannot by design — the
close bar is the operator personally seeing a field sync **in the app** in their own dev
environment (`dev-complete-attestation`, roadmap lines 541–547). **Best case (both cards land):
the milestone is left ONE card short — `dev-complete-attestation`, attended-by-design — and the
capability it attests will be LIVE and usable in the operator's persistent dev environment, with
the same round trip proven red-first and automated.** After tonight, the only thing between here
and milestone close is the operator's own attestation run (plus the separately-tracked A3
attended re-gate). Reported in cards, not KRs, per the sign-off rule.

**Recommended additions: nothing worth adding.** Every other remaining card is
attended-by-design (`dev-complete-attestation`), blocked on an attended re-gate
(`gate-rls-fixture-ownership`), or below the §15k promotion bar with its home in the next
roadmap (observability cluster, E-KR4 restatement). The standing feedback that slates under-pack
was weighed: the honest ceiling tonight is `sync-live-in-dev`'s two legs — the milestone's
remaining unattended-buildable work is exactly this card, and padding the night with off-critical
observability items would spend the run's Docker/substrate-heavy budget on work that does not
advance dev-complete. Two substantial, risk-flagged, infra-coupled cards is a full night.

**Spike skips carried to sign-off (§1.4):** leg 3 was **spiked GREEN** (no skip). Legs 1 & 2 were
gated through a **recorded "no spike needed"**: leg 1 (persistent substrate + relay) is proven
mechanics (Spike A GREEN + the round trip closed GREEN in spikes C/D/E); leg 2 (config wiring) is
not a falsifiable premise on its own and its live effect (the door opening) is asserted inside
leg 3's script. The operator signs with those skips in view.

## Dispatch

**SERIAL — and it is forced, not chosen.** Card 2 depends on Card 1 (it drives the real browser
against Card 1's persistent substrate + FDW pointing), so the two cards are a **linear
dependency chain, not two independent tracks**. Concurrent dispatch is structurally unavailable
here — there is nothing to run in parallel. This is reported, not put to the operator as a
serial-vs-concurrent choice, because no observable outcome differs (§5 / the wording check: when
nothing the operator sees differs, pick the honest option and report it). If a rate-limit stall
hits, finish the in-flight card and close out — never sit blocked.

## Execution notes

- **Single-session serial order:** Card 1 (persistent substrate) → [gate: Card 1 merged clean] →
  Card 2 (app-proof spec) → closeout (~30m reserved). Card 2 is NOT a stretch card — it is the
  deliverable's second half — but it does not start until Card 1 lands, and if Card 1's high
  estimate + Card 2's high estimate + closeout will not fit, prefer landing Card 1 clean and
  leaving Card 2 for a follow-up night (it is retryable in isolation).
- **Two-session split, if ever needed:** session 1 = Card 1 alone (it leaves a coherent tree —
  the persistent substrate exists, the door opens, the relay carries a write; default flag still
  OFF so nothing crew-facing changes); session 2 = Card 2. The seam is after Card 1 lands.
- **Run mechanics inherited by reference** from `reference/gate-ladder.md` — this repo's ladder
  (G1, G2(Go), G2(Playwright) *where a seam requires it*, G3 N/A, G4, RF, G6) and its isolation
  stanzas. 🛑 `reference/overnight-run-plan-20260707.md` has never existed in this repo; the
  ladder file is the only mechanics source.
- **G4 discipline greps: write `N/A-VACUOUS`, never `clean`/`PASS`** (B-14 — neither
  `internal/journal` nor `internal/workorder` exists in hq).
- **No idle-waiting on background jobs** (HANDOFF 20260808-2 process note): foreground legs ≤10m;
  detach + `tail --pid` beyond; a turn ending kills a backgrounded process. The substrate
  bring-up is the classic long leg here — background it, do not block a turn on it.
- **Conflict log** `reference/conflicts-20260810.md` REQUIRED — one entry per merge, clean or
  conflicted (an empty log must never read as "no conflicts" when it means "logging never ran").

## Sign-off record

**GRANTED 2026-08-09 (operator, this session).** Presented per CLAUDE.md Slate workflow step 3 —
four plain-English headings (What you get / What changes day-to-day / Trade-offs / Not covered
yet) + the serial plan (there is no parallel plan; Card 2 depends on Card 1) + the operator
attention budget (2 cards unattended, ~3h mid / ~4h high; then `dev-complete-attestation` ~15–30m
attended) + the milestone remainder (still white: `dev-complete-attestation`,
`gate-rls-fixture-ownership`; recommended additions: nothing worth adding) + the spike skips (leg 3
GREEN, legs 1 & 2 recorded "no spike needed"). The operator asked one clarifying question —
what `gate-rls-fixture-ownership` and decision 155 are — was answered (A3 is the BLOCKED,
attended RLS-fixture re-gate whose G6 probe destroyed prod 2026-08-06; decision 155 moved the test
suites off the production cluster, the structural half DONE as `test-cluster-separation`, the
prefix-guard half riding A3) — then answered **"sign off"**. Dispatch: SERIAL (forced by the
Card 2 → Card 1 dependency; reported, not chosen — no observable outcome differs). Fork
resolutions: none newly required (the read-surface item resolved at T-44 / decision 161 into this
card). Agreement check §3b: exit 0, no contradiction.
