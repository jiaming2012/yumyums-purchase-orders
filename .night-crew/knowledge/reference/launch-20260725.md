Tonight's autonomous run. Execute the signed slate at
`.night-crew/knowledge/reference/slate-20260725.md`.

Setup: you are on branch dev.

Before any git write, CHECK FOR UNMERGED RUN BRANCHES. Run this EXACTLY as written — it
contains no placeholder to substitute, and it asks about every date, because an unfinished
night from any date is a night's work sitting unmerged:

    git branch --no-merged dev --format='%(refname:short)' | grep -E '^overnight-[0-9]{8}(-[0-9]+)?$'

STATE THE RESULT OUT LOUD BEFORE PROCEEDING — either "no unmerged run branches" or the list.
You may only proceed after saying which one you saw. An empty result is the "go" signal, so a
command that failed or ran wrong looks exactly like a clean repo unless you have named what
you found.

- No unmerged run branch → say so, then cut this run's branch. Compute its name from the BARE
  DATE, since you are looking for every branch of that date:

      git branch --format='%(refname:short)' | grep -E '^overnight-20260725(-[0-9]+)?$' | sort -V

  No match → `overnight-20260725`. Otherwise the next free suffix (-2, -3, …). A MERGED
  branch's name is still taken, so a date whose run merged this morning yields -2 — cut it and
  proceed without asking; nothing is at risk.
- ANY unmerged run branch exists → 🛑 STOP. A previous night's work is sitting on it. Report
  what is there (`git log --oneline dev..<branch>`, and whether that run's
  `.night-crew/runs/*/DECISIONS-NEEDED.md` carries an open section), then ASK THE OPERATOR to
  choose: RESUME that branch, or RECUT at the next free suffix. Do NOT pick for them — resuming
  mixes two nights on one branch, recutting walks away from a night's work, and you cannot tell
  from here which is right. This is NOT covered by the batch sign-off: the yes is scoped to the
  signed specs, and what to do with an unfinished night is not one of them. Whatever they
  answer, the existing branch is left untouched — never delete, reset, force, or rename it.

If the run id gains a suffix, EVERY run-scoped name takes it with them: the run directory
`.night-crew/runs/2026-07-25[-N]-autonomous/`, the conflict log
`.night-crew/knowledge/reference/conflicts-20260725[-N].md`, and the closeout's references to
the run. The slate keeps the name it was signed under.

Do all work on the run branch. Never push, never touch main. Hard prohibitions in CLAUDE.md
apply. **This run provisions no infrastructure beyond local Docker containers, touches no
production database, and reaches no hosted Supabase project.**

Authority: batch sign-off was given by the operator on 2026-07-25 — the slate document is the
record. Do NOT pause for per-change sign-off. Every operator fork is already resolved (roadmap
Activity 1 + the BACKLOG explore-session entry, cited in the slate header); the run never
decides, it executes.

Rules of engagement:

- Standing rules and verification gates G1–G6 are inherited unchanged from
  `reference/overnight-run-plan-20260707.md`. **That file does not exist** (known dangling
  pointer, on the backlog since 07-22) — the real origin is
  `.night-crew/runs/2026-07-09-attended/slate-20260710.md` §"Run mechanics" plus the app-code
  adaptation in `.night-crew/knowledge/reference/slate-20260714.md`. Read those.
- Dispatch model: you are the control loop, not the implementer. For each card: create a
  worktree off `overnight-20260725`, spawn a FRESH subagent scoped to exactly that card (paste
  its slate entry, footprint, and spec references into the subagent prompt; it drafts,
  implements, runs G1–G4 in its worktree, and returns a compact report), then spawn a SEPARATE
  fresh subagent for the G6 adversarial review, then merge to the run branch and archive
  yourself. Never implement a card in your own context; your context holds reports, not diffs.
  Subagents cannot spawn subagents — G6 dispatch is always yours. A failed card PARKS per the
  slate rules (worktree preserved, DECISIONS-NEEDED.md) — dispatch the next card. Editing a
  file outside the card's footprint is NOT a park (§15ad.65) — see the conflict rules below.
- Order: SERIAL — dispatch one card at a time, in this order:
  **F1 `workflow-submission-status-default` → W1 `sync-spike-stack-and-jwt-bridge` →
  W2 `sync-spike-rxdb-replication`.**
  F1 first is deliberate: its footprint is confined to mapped `[e2e.seams]` so it runs the
  `workflows|persistence` SUBSET, which excludes `tests/sync.spec.js` — it therefore cannot be
  reddened by the known `:1198` flake, and it guarantees the night produces at least one merged
  card before the box fills with Supabase containers. **W2 runs ONLY if W1 landed with a GO and
  there is real time left** — see Tail discipline below.
- 🛑 **Four HARD blast-radius constraints, all three cards.** These are NOT footprint fences
  (which §15ad.65 made advisory) — each one, if broken, changes the build or test environment
  for every other card in the cycle. Touching any of them is a PARK-and-ask, never a merge-time
  resolution:
    1. `backend/go.mod` — untouched. The JWT mint is stdlib (`crypto/hmac`, `encoding/json`,
       `encoding/base64`); the Realtime client is `github.com/coder/websocket`, ALREADY a direct
       dependency. Do not add a JWT module.
    2. Root `package.json` — untouched. It is the Playwright environment for every card in the
       repo. RxDB and `@supabase/supabase-js` install into `.night-crew/qa/spike-supabase/rxdb/`
       under its OWN package.json + lockfile.
    3. `docker-compose.nc.yml` — untouched. Supabase goes in a NEW, separate
       `docker-compose.supabase.yml`. Extending the nc env would boot Supabase for every
       night-crew run in this repo forever.
    4. Root `Taskfile.yml` — untouched. The operator-runnable path is a README of real commands,
       deliberately not wrapper targets.
  Paste these four into every implementer prompt, and require each merge-intent to state
  explicitly that all four are untouched — that is the claim you check at merge.
- 🛑 **The operator-runnable requirement (W1 and W2) is a done_when condition, not a
  nice-to-have.** The operator asked to run this spike themselves, to learn from it. Deliverable:
  `.night-crew/qa/spike-supabase/README.md` — real copy-pasteable commands the card ACTUALLY RAN,
  with observed output (no reconstructed steps, no "should print"); each step carrying one or two
  sentences on what it demonstrates and which failure it would catch; idempotent bring-up;
  **teardown documented as its own step so the stack is NOT auto-destroyed at card end**;
  artifacts (compose file, Go mint program, RxDB harness) KEPT AND COMMITTED, not deleted after
  the proof; throwaway secrets inline under a loud banner (a deliberate decision for
  reproducibility — **G6 must not relitigate it**) plus a one-line warning never to reuse them
  anywhere real; and a local-only banner at the top. W1 writes half 1, W2 appends half 2 — **half
  1 must stand alone as complete and runnable if W2 never runs.**
- Per-change mechanics — hq adaptation (T-20 decision 37, B-100): hq has no `openspec/`. Keep
  what survives translation: red-first where a defect is named, atomic commits, flip the roadmap
  card in the same change set. **F1 → DONE. W1 and W2 flip to DONE on EITHER verdict — a written
  NO-GO is a completed spike, not a failed card.** A card that parks without a verdict stays
  PLANNED; if W2 never starts it stays PLANNED and is the next slate's lead card.
- Merge-intent note (REQUIRED, §15ad.65): each subagent writes
  `.night-crew/runs/2026-07-25-autonomous/merge-intents/<card>.md` in its FIRST commit, before
  implementing — shared files touched (each file outside its own packages, one line of why), what
  must survive any merge, what is safe to drop, and the four-HARD-constraints attestation above.
  Empty fields say "nothing here" explicitly. Paste this requirement into every implementer
  prompt.
- Conflicts, not fences (§15ad.65): the footprint is planning information, not a boundary — a
  card MAY edit any file, and doing so is not a breach and not a park. Collisions surface as
  ordinary git conflicts. YOU resolve them at merge by reading BOTH sides' merge-intent.md and
  resolving against intent, not text; then re-run G1+G2. PARK the later card ONLY when, having
  read both intents, you still cannot determine which behavior is correct. An unplanned lifecycle
  row, a breach of the four HARD constraints, or any operator-only question still PARKS. Never
  improvise forks.
- Conflict log (REQUIRED, §15ad.66): append an entry to
  `.night-crew/knowledge/reference/conflicts-20260725.md` for EVERY merge, clean or conflicted —
  the cards involved, files and hunks, the intents you read, the resolution you took, and the
  gate result after it. Clean merges get a one-line entry, so an empty log never reads as "no
  conflicts" when it means "the logging never ran". Commit it with the merge. Summarize it in the
  closeout; morning triage audits it.
- 🛑 **Non-attribution rule for Playwright reds.** W1 and W2 touch undeclared paths, so both
  de-confine to the FULL suite (~20m), which contains `tests/sync.spec.js:1198` — proven flaky at
  ~16% overall and ~20% under concurrent suite load
  (`reference/1198-flake-reproduction-20260721.md`). night-crew's gate parses Go per-test but
  collapses Playwright to a single failure atom, so it cannot distinguish "this card broke it"
  from "already red." If a known intermittent reds: report the **measured 1-min load average**
  (`uptime`) alongside the result and do NOT attribute it to the card without evidence. Per
  `preferences.md` P2/P3a, "the box is quiet" is a claim to be MEASURED, never assumed — serial
  dispatch guarantees the run is serial, not that the machine is idle. State the condition you
  sampled; a green on a quiet box bounds nothing about the loaded condition.
- Cold-start handling: the Supabase images are NOT cached on this box (verified 2026-07-25 — 24
  local images, none Supabase; ~1.5–2GB to pull, 931GB free). **W1's first act is the image pull,
  in the foreground, before writing any config**, so a network failure costs minutes not hours.
  **W2 opens with its `npm i` for the same reason.** A failed pull or install is an immediate
  PARK with the transcript — do not spend the night retrying a cold registry.
- Suite run-mechanics (standing): `CI=1` on every full-suite / G6 leg; ephemeral pg16 via
  `docker-compose.nc.yml` (no host-port publish — scratchpad `ports:` override);
  `hq_test_go`/`hq_test_e2e` separation is landed — use it. **`-p 1` is load-bearing for Go
  suites** (default parallel `-p` reddens four packages via concurrent `TRUNCATE`s on a shared
  DB). Never background long runs (07-21 A1 lost ~25–30m); foreground ≤10m, detach + `tail --pid`
  beyond. `git stash` is forbidden in worktrees. Two Docker stacks are live at once during W1/W2
  — neither publishes fixed host ports; resolve both via `docker compose port`. The `:8199` latch
  has recurred three times: kill the **listener PID** (`ss -ltnp`), never the `go run` parent.
- Standing instrumentation (Delivery KR3): emit harness-measured per-card wall-clock (impl + G6 +
  merge legs, epoch-stamped `timings.log`) and table it in the HANDOFF.
- 🛑 **Tail discipline** (no `--night` was given, so this is the stop rule instead of a cut line).
  Serial critical path is ~6h–12h; two first-of-kind cards in one queue is where the ledger has no
  signal, and the high end exceeds a night. Drop tail-first, never mid-proof:
    1. W2's optional legs — the LWW-behaviour observation and the licensing determination go
       before the core both-directions replication proof.
    2. W2 entirely. If W1 lands after the small hours or parks, DO NOT START W2. A
       started-and-abandoned W2 leaves a half-written runbook the operator may trust — worse than
       an unstarted one.
    3. **Never half-claim a verdict.** Either the go/no-go is evidenced or it is not made and the
       card parks with what it learned — say which, explicitly.
  **The cycle's gate is W1 alone.** W2 deepens the verdict and de-risks the next slate, but its
  absence blocks nothing. Prefer a clean early exit over starting work that cannot finish cleanly.
- Budget discipline: tokens ≠ calendar, and quota was NOT measured. Spend is governed by these
  stop conditions, not by a clock estimate.

Closeout (ALWAYS, leave ~30 min): update `.night-crew/runs/2026-07-25-autonomous/HANDOFF.md`
(per-card outcomes table, gate evidence on the final tree, per-card timings, next actions) and
`.night-crew/runs/2026-07-25-autonomous/DECISIONS-NEEDED.md` (anything parked), commit as docs.
The morning reader is the operator — write for them.

🛑 **Say this in the HANDOFF, plainly, because the next slate depends on it:** the three
downstream Activity-1 cards (`sync-rxdb-schema-and-replication`, `sync-jwt-bridge-endpoint`,
`sync-hard-cutover`) **cannot be dispatched until morning triage records W1's go/no-go verdict in
`.night-crew/knowledge/ledger.md`** — Product KR1 and Delivery KR1 measure the ledger timestamp
against run dispatch timestamps, and the ledger is an attended artifact this run does not write.
Recording that verdict is the single highest-value thing the operator does tomorrow.

Your FINAL CHAT MESSAGE is a hand-off, so CLAUDE.md §"Context checkpoints at hand-offs" binds it.
Do not end on a results summary. End with, in this order:

1. Next steps, led by morning triage — what is waiting, what is parked, what the operator has to
   decide. Lead with the ledger verdict (above) and, if the runbook landed, tell them it is ready
   to run and where.
2. The context-checkpoint verdict with its reason. For a finished run it is 🔄 Clear now
   (recommended) — `/nc-morning-triage` is designed to start cold and reviews the run branch on
   its merits; nothing in your context is load-bearing because HANDOFF.md, DECISIONS-NEEDED.md,
   and the conflict log carry it all. Say ⛔ instead only if something real is still in flight in
   this session.
3. The paste-ready next command: `/nc-morning-triage`

Writing HANDOFF.md does NOT discharge this. The file and the closing message are different
surfaces read at different moments, and the operator reading the last thing on screen at 7am is
the one this is for.
