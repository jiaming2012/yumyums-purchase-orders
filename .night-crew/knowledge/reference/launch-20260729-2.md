Tonight's autonomous run. Execute the signed slate at
.night-crew/knowledge/reference/slate-20260729-2.md.

Setup: you are on branch dev.

Before any git write, CHECK FOR UNMERGED RUN BRANCHES. Run this EXACTLY as written — it
contains no placeholder to substitute, and it asks about every date, because an unfinished
night from any date is a night's work sitting unmerged:

    git branch --no-merged dev --format='%(refname:short)' | grep -E '^overnight-[0-9]{8}(-[0-9]+)?$'

STATE THE RESULT OUT LOUD BEFORE PROCEEDING — either "no unmerged run branches" or the list.
You may only proceed after saying which one you saw. An empty result is the "go" signal, so a
command that failed or ran wrong looks exactly like a clean repo unless you have named what
you found.

- No unmerged run branch → say so, then cut this run's branch. Compute its name from the BARE
  DATE, since you are looking for every branch of that date:

      git branch --format='%(refname:short)' | grep -E '^overnight-20260729(-[0-9]+)?$' | sort -V

  No match → overnight-20260729. Otherwise the next free suffix (-2, -3, …). A MERGED branch's
  name is still taken, so a date whose run merged this morning yields -2 — cut it and proceed
  without asking; nothing is at risk. (Expected tonight: `overnight-20260729` exists and is
  merged, so this resolves to **overnight-20260729-2**, which is the branch the slate was
  signed against.)
- ANY unmerged run branch exists → 🛑 STOP. A previous night's work is sitting on it. Report
  what is there (git log --oneline dev..<branch>, and whether DECISIONS-NEEDED.md carries an
  open section for it), then ASK THE OPERATOR to choose: RESUME that branch, or RECUT at the
  next free suffix. Do NOT pick for them — resuming mixes two nights on one branch, recutting
  walks away from a night's work, and you cannot tell from here which is right. This is NOT
  covered by the batch sign-off: the yes is scoped to the signed specs, and what to do with an
  unfinished night is not one of them. Whatever they answer, the existing branch is left
  untouched — never delete, reset, force, or rename it.

If the run id gains a suffix, EVERY run-scoped name takes it with them: the conflict log
.night-crew/knowledge/reference/conflicts-<runid>.md, and the closeout's references to the run.
The slate keeps the name it was signed under.

Do all work on the run branch. Never push, never touch main. Hard prohibitions in CLAUDE.md
apply.

Authority: batch sign-off was given by the operator on 2026-07-28 — the slate document is the
record. Do NOT pause for per-change sign-off. Every operator fork is already resolved in
ledger.md (T-26 decisions 82–88, T-27 decisions 89–91); the run never decides, it executes.

Rules of engagement:

- Standing rules and verification gates G1–G6 are inherited unchanged from
  .night-crew/knowledge/reference/overnight-run-plan-20260707.md.

- Dispatch model: you are the control loop, not the implementer. For each card: create a
  worktree off overnight-20260729-2, spawn a FRESH subagent scoped to exactly that card (paste
  its slate entry, footprint, and spec references into the subagent prompt; it drafts,
  implements, runs G1–G4 in its worktree, and returns a compact report), then spawn a SEPARATE
  fresh subagent for the G6 adversarial review, then merge to the run branch and archive
  yourself. Never implement a card in your own context; your context holds reports, not diffs.
  Subagents cannot spawn subagents — G6 dispatch is always yours. A failed card PARKS per the
  slate rules (worktree preserved, DECISIONS-NEEDED.md) — dispatch the next card. Editing a
  file outside the card's footprint is NOT a park (§15ad.65) — see the conflict rules below.

- Order: CONCURRENT — three tracks, at most one in-flight card per track, in slate order within
  each track. You alone merge to the run branch.
    - Track A: H1 `test-harness-fail-loud` first, then A1 `app-timezone-unify-new-york` on H1's
      completion.
    - Track B: B1 `sync-rxdb-collections-and-table-contract` first, then B2
      `sync-rxdb-row-visibility-rls` on B1's completion — 🛑 **and not before H1 has MERGED.**
      B2's entire gate is an attack-variant Go suite in `internal/sync`, and under the pre-H1
      harness that suite reports `ok` when its database is unreachable. A security proof that can
      succeed on zero executed tests is not a weaker gate; it is a gate that lies. **If H1 parked
      or has not merged when B1 lands, dispatch B2 anyway but require it to state — in its report
      AND its merge-intent — that its gate ran under the unfixed harness, and re-run that gate on
      the final tree after H1 lands.**
    - Track C: C1 `sync-rxdb-conflict-notice-mockup-amendments` dispatches immediately; it is
      independent of everything and touches `.planning/` only.
    - If a track stalls on rate limits, degrade to serial — do not sit blocked. The dev harness
      has no quota-pause machinery.

- Per-change mechanics — the slate records the workflow-preflight verdict for this repo as
  **ABSENT** (`night-crew` is not on PATH; there is no `openspec/` directory). Therefore: do NOT
  assert OpenSpec mechanics and do NOT create `openspec/` scaffolding in this repo. Assert only
  the universal mechanics: red-first evidence, atomic commits, a change-identifying commit
  trailer on every commit, and the roadmap/status flip. Which per-change discipline this repo
  should adopt is an operator question (B-105) — never encode another detected convention's
  mechanics in its place.

- 🛑 Commit trailer adjacency (B-21, decision 86 — fixed at the emitter, not in history): the
  trailer block must be emitted as ONE ADJACENT PARAGRAPH. A blank line between
  `Night-Crew-Card:` and `Co-Authored-By:` splits the block and `git interpret-trailers --parse`
  sees only the last paragraph. On overnight-20260729 this silently broke 14 of 33 commits —
  all of Card C's, all four merge commits, and the closeout — while every merge-intent note
  asserted the trailer as a landed convention (textually true, mechanically false). Three
  separate agents got this wrong the same way, so state it in every implementer AND reviewer
  prompt, and apply it to YOUR OWN merge and closeout commits. After each merge, verify with
  `git log -1 --format=%B | git interpret-trailers --parse` that the trailer actually parses —
  "the text is present" is what everyone verified last time and it is not the property that
  matters.

- 🛑 Test-harness interim rules — BOTH defects are live at dispatch and are closed by Card H1
  DURING the run:
    - B-09: `task test` runs 19 of 20 spec files until H1 merges. **Every card must run
      `npx bddgen` before claiming a full suite, and must state the spec-file count it saw.**
      After H1 merges, cards must STILL state the count — the instruction is what proves the fix.
    - B-16(b): a dropped or unreachable database reads as a passing Go suite until H1 merges.
      **No card may infer a Go pass from a bare `ok`.**
    - 🛑 B-16(a) — STANDING G6 DISPATCH TEXT, paste into EVERY reviewer prompt: **a reviewer must
      never drop, truncate, or otherwise destroy a database it did not itself create.** On
      overnight-20260729 a G6 reviewer ran `DROP DATABASE hq_test_go_c` during cleanup while the
      implementer was still using it; the implementer caught it only by questioning a
      suspiciously fast green. Reviewers create their own database with a unique name, and clean
      up only what they created.

- Merge-intent note (REQUIRED, §15ad.65): each subagent writes a merge-intent note in its FIRST
  commit, before implementing — shared files touched (each file outside its own packages, one
  line of why), what must survive any merge, and what is safe to drop. Empty fields say "nothing
  here" explicitly. Paste this requirement into every implementer prompt. **After any repair
  round, the note must be re-read WHOLE and contradicted lines struck, not merely appended to
  (B-11)** — on overnight-20260727 a note disclosed a `sync.js` edit correctly in one section
  while leaving "`sync.js` was NOT edited" standing two lines below it. A note that contradicts
  itself is worse than no note, because the merger trusts it unattended at 3am.

- Conflicts, not fences (§15ad.65): the footprint is planning information, not a boundary — a
  card MAY edit any file, and doing so is not a breach and not a park. Collisions surface as
  ordinary git conflicts. YOU resolve them at merge by reading BOTH sides' merge-intent notes
  and resolving against intent, not text; then re-run G1+G2. PARK the later card ONLY when,
  having read both intents, you still cannot determine which behavior is correct. An unplanned
  lifecycle row or any operator-only question still PARKS. Never improvise forks.
  Known collision tonight: **the next migration number (`0072`)** — A1 takes it; B2 renumbers at
  merge if it created one. Ordinary conflict, resolve at merge, do not park.

- Conflict log (REQUIRED, §15ad.66): append an entry to
  .night-crew/knowledge/reference/conflicts-20260729-2.md for EVERY merge, clean or conflicted —
  the cards involved, files and hunks, the intents you read, the resolution you took, and the
  gate result after it. Clean merges get a one-line entry, so an empty log never reads as "no
  conflicts" when it means "the logging never ran". Commit it with the merge. Summarize it in
  the closeout; morning triage audits it.

- Full-suite cost and expected reds — state these before dispatching each card, per QA KR3:
  every card in this activity pays the FULL Playwright suite (the `[e2e.seams]` map leaves
  `sync` unmapped), ~23m on a quiet box but **~45m under tonight's concurrent dispatch**. Two
  `sync.spec.js` reds are ARMED, load-sensitive, and **concurrent dispatch is the condition that
  reds them**: `:446` [LST-17] (documented standing waiver, decision 44) and `:1198` (~16–20%
  flake). **Expect both; they are not regressions.** Card C1 is the one exception — `.planning/`
  only, zero production code, so it does not pay the suite unless it touches a file outside
  `.planning/`, in which case it pays it like everything else.

- Budget discipline: prefer a clean early exit over starting a card you cannot finish cleanly.
  The 11h night is a WALL-CLOCK cut line only — the token quota was not measured and is
  invisible to the planner. Do not read the 2h20m–4h50m margin as headroom to spend; it exists
  to absorb a G6 repair round. On 07-27 two cards ran 2–3.5× their estimates and cost a third
  card entirely; on 07-29 estimates built from the same ledger came in at 0.7–0.9×. Neither
  night validates the other in advance.

- Do NOT fold in opportunistic fixes. Specifically: **B-06** (`tests/sync.spec.js:1584` stale
  comment) belongs to `sync-cache-and-identity-hygiene` and no card tonight touches it — folding
  it in would widen a signed card. Same for B-17, B-18, B-19, B-20.

Closeout (ALWAYS, leave ~30 min): update HANDOFF.md (per-card outcomes table, gate evidence on
the final tree, next actions) and DECISIONS-NEEDED.md (anything parked), commit as docs. **Verify
the final-tree suite actually EXECUTED tests rather than reporting exit 0 on zero** — on
overnight-20260729 the orchestrator's own final suite reported exit 0 having run nothing, because
Playwright's `webServer` could not start and a `tail` pipeline masked the status. State the test
count you saw. The morning reader is the operator — write for them.

Your FINAL CHAT MESSAGE is a hand-off, so CLAUDE.md §"Context checkpoints at hand-offs" binds it.
Do not end on a results summary. End with, in this order:
1. Next steps, led by morning triage — what is waiting, what is parked, what the operator has to
   decide. Name explicitly: whether C1's revised plates are ready for the operator's signature
   (which is the only thing that unblocks `sync-rxdb-conflict-notice-ui`), and that the attended
   two-device convergence check is RE-ARMED if A1 landed (it touches `sync.js` and
   `workflows.html`, so `sw.js` regenerates) — runbook at
   .night-crew/knowledge/reference/attended-two-device-check.md.
2. The context-checkpoint verdict with its reason. For a finished run it is 🔄 Clear now
   (recommended) — /nc-morning-triage is designed to start cold and reviews the run branch on its
   merits; nothing in your context is load-bearing because HANDOFF.md, DECISIONS-NEEDED.md, and
   the conflict log carry it all. Say ⛔ instead only if something real is still in flight in this
   session.
3. The paste-ready next command: /nc-morning-triage

Writing HANDOFF.md does NOT discharge this. The file and the closing message are different
surfaces read at different moments, and the operator reading the last thing on screen at 7am is
the one this is for.
