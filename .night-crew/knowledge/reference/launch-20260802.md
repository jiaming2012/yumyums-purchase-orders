Tonight's autonomous run. Execute the signed slate at
`.night-crew/knowledge/reference/slate-20260802.md`.

Setup: you are on branch dev.

Before any git write, CHECK FOR UNMERGED RUN BRANCHES. Run this EXACTLY as written — it
contains no placeholder to substitute, and it asks about every date, because an unfinished
night from any date is a night's work sitting unmerged:

    git branch --no-merged dev --format='%(refname:short)' | grep -E '^overnight-[0-9]{8}(-[0-9]+)?$'

STATE THE RESULT OUT LOUD BEFORE PROCEEDING — either "no unmerged run branches" or the list.
You may only proceed after saying which one you saw. An empty result is the "go" signal, so a
command that failed or ran wrong looks exactly like a clean repo unless you have named what
you found.

- No unmerged run branch → say so, then cut this run's branch. Compute its name from the BARE
  DATE, since you are looking for every branch of that date:

      git branch --format='%(refname:short)' | grep -E '^overnight-20260802(-[0-9]+)?$' | sort -V

  No match → `overnight-20260802`. Otherwise the next free suffix (-2, -3, …). A MERGED
  branch's name is still taken, so a date whose run merged this morning yields -2 — cut it and
  proceed without asking; nothing is at risk. **At planning time this returned NOTHING, so the
  expected branch is `overnight-20260802`. Re-run it anyway — do not trust this sentence over
  the command.**
- ANY unmerged run branch exists → 🛑 STOP. A previous night's work is sitting on it. Report
  what is there (`git log --oneline dev..<branch>`, and whether that run's
  `.night-crew/runs/<rundir>/DECISIONS-NEEDED.md` carries an open section for it), then ASK THE
  OPERATOR to choose: RESUME that branch, or RECUT at the next free suffix. Do NOT pick for
  them. This is NOT covered by the batch sign-off. Whatever they answer, the existing branch is
  left untouched — never delete, reset, force, or rename it.

🛑 **`/nc-run` §1's "has this slate already run?" guard is INERT in this repo — B-33.**
`run-evidence check` cannot see any hq run's closing evidence (two independent path mismatches;
it looks for `.night-crew/runs/<runid>/` and a root `reference/`, and this repo has neither).
It will report `no-run-evidence` for a night that fully landed. **The branch check above is
your only real backstop. Do not read a `no-run-evidence` verdict as "this slate is fresh."**

🛑 **The `night-crew` binary on PATH may be STALE and missing this ritual's guard verbs.** At the
2026-08-01 launch the installed binary (Jul 23) had none of `launch-prompt`, `run-evidence`,
`worktrees check` or `workers check`, and a non-zero `launch-prompt` reads as "no signed night".
If a verb prints a usage line instead of a report, that is a stale BINARY, not a finding — build
main read-only rather than trusting the install, and never touch the night-crew clone's worktree:

    cd ~/projects/night-crew && git archive main | tar -x -C <scratch>/nc-main
    cd <scratch>/nc-main && go build -o <scratch>/nc ./cmd/nightcrew

If the run id gains a suffix, EVERY run-scoped name takes it with them: the run directory
`.night-crew/runs/2026-08-02-autonomous/`, the conflict log
`.night-crew/knowledge/reference/conflicts-20260802.md`, and the closeout record. The slate keeps
the name it was signed under.

Do all work on the run branch. Never push, never touch main. Hard prohibitions in CLAUDE.md
apply. **Nothing deploys tonight** — `task prod:deploy` is not a run action under any outcome.

Authority: batch sign-off was given by the operator on 2026-08-01 — the slate document is the
record. Do NOT pause for per-change sign-off. Every operator fork is already resolved in
`.night-crew/knowledge/ledger.md` (T-29 decisions 104–110; T-30 decisions 111–113; T-24 decision
67). The run never decides a PRODUCT fork; it executes.

## Rules of engagement

- **Standing rules and verification gates G1–G6: read them from the slate's own
  §"Run mechanics" pointer, NOT by inheritance.** 🛑
  `reference/overnight-run-plan-20260707.md` DOES NOT EXIST IN THIS REPO — it lives only in the
  night-crew clone and its ladder is night-crew's own. This is **B-26**. HQ's real ladder is
  written out in full in `slate-20260801-2.md` §"HQ's verification ladder" — read it from there
  and **paste that table into every implementer and every G6 prompt.**

- **Dispatch model: you are the control loop, not the implementer.** For each card: create a
  worktree off `overnight-20260802`, spawn a FRESH subagent scoped to exactly that card (paste
  its slate entry, footprint, and spec references into the subagent prompt; it drafts,
  implements, runs G1–G4 in its worktree, and returns a compact report), then spawn a SEPARATE
  fresh subagent for the G6 adversarial review, then merge to the run branch and archive
  yourself. Never implement a card in your own context; your context holds reports, not diffs.
  Subagents cannot spawn subagents — G6 dispatch is always yours. A failed card PARKS per the
  slate rules (worktree preserved, `DECISIONS-NEEDED.md`) — dispatch the next card. Editing a
  file outside the card's footprint is NOT a park (§15ad.65) — see the conflict rules below.

- **Order — CONCURRENT, 2 tracks, at most one in-flight card per track, slate order within each
  track. You alone merge to the run branch.**

  - **Track A (strictly serial — the serialization is load-bearing, both cards share the live
    Supabase substrate and A2 rewrites its policies):**
    **A1 `sync-replication-scope-per-checklist`** → *merge* → **A2 `sync-rxdb-write-policies`**.
    A2 is cut only AFTER A1 merges, so it develops against the merged state.
  - **Track B (serial within the track, concurrent with Track A throughout):**
    **B1 `sync-cache-and-identity-hygiene`** → *merge* → **P1 `build-deploy-manifest-integrity`**
    → *merge* → **P2 `workflow-unsubmit-failnote-reattach`** → *merge* →
    **P3 `sync-banner-builder-tab-scope`**.
    **P1 is cut only after B1 merges** (both own `build-sw.js`); **P3 only after P2 merges**
    (both own `workflows.html`).
  - **Track B is the critical path tonight and that is by design** — loading the bug work here is
    what leaves Night B clear for the cutover. Track A finishing first is correct, not waste.
  - If a track stalls on rate limits, **degrade to serial — do not sit blocked.**

- **🛑 B-35 — EVERY leg MUST set a unique `HQ_RLS_TEST_DB`, a unique `TEST_PORT`, and a unique
  database prefix: the implementer leg, the G6 leg, AND every fix round.**
  `TestRowVisibilityRLS` drops and recreates its FDW fixture database on entry, default name
  `hq_test_b2_fdw`, so a plain `go test ./...` destroys a database it does not own. With six
  cards in flight this is a near-certainty, not a hazard. This is not a suggestion.

- **Per-change mechanics — conditioned on the slate's recorded workflow-preflight verdict, which
  is `openspec: absent` (exit 0, the check read every path and concluded).** Do NOT assert
  OpenSpec mechanics and do NOT create `openspec/` scaffolding in this repo. Assert only the
  universal mechanics: **red-first evidence**, **atomic commits by logical unit**, **a
  change-identifying commit trailer on every commit**, and **the status flip in the same change
  set**. Which per-change discipline this repo should adopt is an operator question
  (B-105) — never encode another detected convention's mechanics in its place.
  🛑 **"Status flip" means different files for different cards here.** A1, A2 and B1 are milestone
  cards — flip their bullet in `.night-crew/knowledge/roadmap.md`. **P1, P2 and P3 are promoted
  backlog items with NO roadmap card** — flip their `BACKLOG.md` entry from
  `· promoted → <card> (slate-20260802) ·` to the landed form. Do not invent roadmap bullets for
  them, and do not skip the flip because no roadmap card exists.
  🛑 **This repo's trailers are `Night-Crew-Card:` / `Night-Crew-Run:`, NOT `Card:` / `Run:`.**
  That drift cost ten commits on 2026-08-01. Get it right on the seed commit.

- **Merge-intent note (REQUIRED, §15ad.65): each subagent writes it in its FIRST commit, before
  implementing.** 🛑 **`openspec/` does not exist here and must not be created** — write it to
  `.night-crew/runs/2026-08-02-autonomous/merge-intent-<card>.md`. Contents: shared files touched
  (each file outside its own packages, one line of why), **what must survive any merge**, and
  what is safe to drop. Empty fields say "nothing here" explicitly. Paste this requirement into
  every implementer prompt. **B1's is load-bearing tonight AND tomorrow** — P1 edits
  `build-sw.js` after it and S1 edits it again on Night B, so B1 must state exactly what its
  per-identity cache partition needs to survive.

- **Conflicts, not fences (§15ad.65):** the footprint is planning information, not a boundary — a
  card MAY edit any file, and doing so is not a breach and not a park. Collisions surface as
  ordinary git conflicts. YOU resolve them at merge by reading BOTH sides' merge-intent notes and
  resolving against intent, not text; then re-run G1+G2. PARK the later card ONLY when, having
  read both intents, you still cannot determine which behavior is correct. An operator-only
  question still PARKS. Never improvise forks.

- **Conflict log (REQUIRED, §15ad.66):** append an entry to
  `.night-crew/knowledge/reference/conflicts-20260802.md` for EVERY merge, clean or conflicted —
  the cards involved, files and hunks, the intents you read, the resolution you took, and the
  gate result after it. Clean merges get a one-line entry, so an empty log never reads as "no
  conflicts" when it means "the logging never ran". Commit it with the merge.

- **Budget discipline:** prefer a clean early exit over starting a card you cannot finish
  cleanly.

## 🛑 SCOPE FREEZE — a standing rule tonight, and the run applies it without asking

**Anything you discover tonight routes to the NEXT milestone unless it reddens a named key result
on the current OKR page.** Do not widen a card. Do not author a new card mid-run. Do not ask.

The milestone remainder has grown from work found inside cards that landed clean on each of the
last two nights, and discovery runs at roughly five findings per day with no sign of slowing.
Without this freeze the close reopens tomorrow, which is what happened to the last three slates.

**A discovery still gets a destination.** File it to `BACKLOG.md` with a handle, and name it in
the closeout under "What is NOT done" **with its destination stated** — that is B-38's rule. A
finding routed to the next milestone is disposed of; a finding merely mentioned is dropped.

## Briefings every implementer and every G6 reviewer gets

- **🛑 `dev` IS NOT GREEN. `tests/inventory.spec.js:883` (B-27) fails on a clean isolated re-run
  of the merged tree.** The 2026-07-31 adversarial re-execution refuted the prior closeout's
  exit-0 claim; the branch was merged anyway, deliberately, with the red named (T-29 decision
  104). **Every card starts from a base whose Playwright suite exits 1.** Judge G2 against *that*
  baseline. A card claiming "green" instead of "green except B-27" is reporting something false —
  and B-27 reddening is NOT the card's.

- **🛑 B-36 — `internal/sync` printing `ok` is NOT evidence.** `resolveSpikeConfig` turns any
  `docker compose … port` failure into `t.Skip`, so the package reports `ok` and exits 0 while
  skipping the entire RLS attack suite. **Gate evidence citing `internal/sync` MUST include
  `-run TestRowVisibilityRLS -v` output showing subtests executed.** Card A2 fixes this in the
  same change set — until it merges, the reporting rule binds every card. **E-KR2 cannot be
  evidenced at all until A2 lands**, which is why A2 is the key result and not just a card.

- **A1 has a governing PREFERENCE — paste it verbatim into A1's implementer prompt.**
  `architecture/C-2`: *"Any client-side fetch or replication over a collection that can grow
  without bound is batched and scoped — never pulled whole. Scope it to what the current view
  actually needs (for workflows, the open checklist). Widening the scope requires a recorded
  decision."* **If A1 hits a scoping edge, C-2 decides it — do not park on a question this
  preference already answers.**

- **P2 is bound by CLAUDE.md's Workflows Data Persistence Rule.** Fail-note text and fail
  severity are two of the seven persisted states. P2 MUST carry a back-and-reopen regression test
  in `tests/persistence.spec.js` — enter data → back to list → reopen → data still there. **The
  feature is not complete without that test.**

- **Guard integrity — B-22 / B-23 / B-24.** A guard printing PASS is not evidence until its
  subject set is shown non-empty. Brief every G6 reviewer. Applies with particular force to A2
  (whose deliverable is a guard) and to P1 (whose deliverable is a build-time check).

- **`go test` must run with `-p 1`.** `Taskfile.yml:108` already does it. Two agents hit the
  parallel-package failure mode independently on 07-29.

- **G4 ordering — B-37.** `build-sw.js` reads **git HEAD, not the working tree**, and exits 0
  after silently dropping assets. Regenerate the service worker **after** the merge commit, never
  mid-merge; on 2026-08-01 that produced a 24-file manifest instead of 27. **Check the file count
  against expectation, not just the exit code.** P1 is the card that makes this mechanical —
  until it merges, the procedural rule binds every card, P1's own gates included.

- **P1 carries a DECIDED question, not an open one.** B-13 asked whether the doc or the Taskfile
  is right. **The slate decided: the Taskfile is right, fix the doc.** `precache-manifest-from-head`
  made `build-sw.js` read git HEAD, and the prod clone's HEAD after `git reset --hard origin/main`
  IS the shipped tree, so regenerating on the box is redundant by construction. Implement that;
  do not re-open it. It is flagged for ratification at triage.

- **Armed reds, by grep handle and never by line anchor (decision 100):**
  `list page progress decrements when another device unchecks a field [LST-17]`
  (`tests/sync.spec.js`) — **STAYS ARMED**, T-29 decision 109; 🛑 the bare tag `[LST-17]`
  matches TWO tests, so the FULL TITLE is mandatory.
  `a queued submission still lends its idempotency_key at 7:30pm CT [A1-TZ-02]` — B-30.
  `submitted checklist survives builder edit with assignment change [LC-02]` — B-32.
  **Non-reproduction retires none of them.**

- **🔴 `HQ_SYNC_REST_URL` STAYS ARMED.** A1 and A2 both touch the replication path. **No card may
  set it in any deploy.** It disarms only at triage, on evidence, never by the run asserting it.

## Closeout (ALWAYS, leave ~30 min)

Update `.night-crew/runs/2026-08-02-autonomous/HANDOFF.md` (per-card outcomes table, gate evidence
on the final tree, next actions) and `DECISIONS-NEEDED.md` (anything parked), commit as docs. Also
write `.night-crew/knowledge/reference/closeout-20260802.md` in that same commit — run id, what
landed, final gate result, time written. **Write it only at the closeout**; it is the only closing
evidence a slate run produces, and an early write destroys the distinction between a night that
landed and one that stopped halfway.

**Report `dev`'s baseline honestly.** State the final tree's Playwright result as *"green except
B-27"* or name every survivor by FULL TITLE grep handle. Milestone close depends on a trustworthy
baseline, not a green one.

🛑 **Every "What is NOT done" bullet you write must name its destination** — a roadmap card, a
backlog entry, or an explicit "no action, because…". **B-38 is exactly the failure of not doing
this:** on 2026-08-01 a card wrote *"a follow-up card owns this"* into shipped SQL, repeated it in
DECISIONS-NEEDED and in HANDOFF's "What is NOT done", and it never became a card — which left
`sync-hard-cutover` blocked on nothing and told the operator the milestone was two cards short when
it was four. Do not reproduce that. Under tonight's scope freeze the destination will usually be
"next milestone", and saying so IS the disposition.

**Also flag for ratification at triage:** the B-13 call (doc-not-Taskfile) that this run
implemented rather than parked.

## Your FINAL CHAT MESSAGE is a hand-off

CLAUDE.md §"Context checkpoints at hand-offs" binds it. Do not end on a results summary. End
with, in this order:

1. **Next steps, led by morning triage** — what is waiting, what is parked, what the operator has
   to decide. **Lead with whether Night B is cuttable**: A2 must have landed for S1 to have
   policies to swap onto, and B1's merge-intent must state what its cache partition needs to
   survive `build-sw.js`'s next edit. If either failed, say so first — Night B's slate depends on
   it.
2. **The context-checkpoint verdict with its reason.** For a finished run it is
   🔄 **Clear now (recommended)** — `/nc-morning-triage` is designed to start cold and reviews the
   run branch on its merits; nothing in your context is load-bearing because HANDOFF.md,
   DECISIONS-NEEDED.md and the conflict log carry it all. Say ⛔ instead only if something real is
   still in flight in this session.
3. **The paste-ready next command:** `/nc-morning-triage`

Writing HANDOFF.md does NOT discharge this. The file and the closing message are different
surfaces read at different moments, and the operator reading the last thing on screen is the one
this is for.
