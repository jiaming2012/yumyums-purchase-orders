Tonight's autonomous run. Execute the signed slate at reference/slate-20260809.md.

Setup: you are on branch dev.

Before any git write, CHECK FOR UNMERGED RUN BRANCHES. Run this EXACTLY as written —
it contains no placeholder to substitute, and it asks about every date, because an
unfinished night from any date is a night's work sitting unmerged:

    git branch --no-merged dev --format='%(refname:short)' | grep -E '^overnight-[0-9]{8}(-[0-9]+)?$'

STATE THE RESULT OUT LOUD BEFORE PROCEEDING — either "no unmerged run branches" or the
list. You may only proceed after saying which one you saw. An empty result is the "go"
signal, so a command that failed or ran wrong looks exactly like a clean repo unless
you have named what you found.

- No unmerged run branch → say so, then cut this run's branch. Compute its name from
  the BARE DATE, since you are looking for every branch of that date:

      git branch --format='%(refname:short)' | grep -E '^overnight-20260809(-[0-9]+)?$' | sort -V

  No match → overnight-20260809. Otherwise the next free suffix (-2, -3, …). A MERGED
  branch's name is still taken, so a date whose run merged this morning yields -2 —
  cut it and proceed without asking; nothing is at risk.
- ANY unmerged run branch exists → 🛑 STOP. A previous night's work is sitting on it.
  Report what is there (git log --oneline dev..<branch>, and whether
  DECISIONS-NEEDED.md carries an open section for it), then ASK THE OPERATOR to choose:
  RESUME that branch, or RECUT at the next free suffix. Do NOT pick for them — resuming
  mixes two nights on one branch, recutting walks away from a night's work, and you
  cannot tell from here which is right. This is NOT covered by the batch sign-off: the
  yes is scoped to the signed specs, and what to do with an unfinished night is not one
  of them. Whatever they answer, the existing branch is left untouched — never delete,
  reset, force, or rename it.

If the run id gains a suffix, EVERY run-scoped name takes it with them: the conflict
log reference/conflicts-<runid>.md, and the closeout's references to the run. The slate
keeps the name it was signed under (slate-20260809.md).

Do all work on the run branch. Never push, never touch main. Hard prohibitions in
CLAUDE.md apply — in particular: NEVER invoke any `backend:*` or `test:*` task bare
against a default coordinate (B-164 — defaults resolve :5433, the PRODUCTION cluster;
that is how prod was destroyed 2026-08-06). `task test:targets` is the read-only
coordinate source; all test coordinates are :5434 (yumyums-test-pg, role hqtest). And
NEVER run `task spike:down` — it is the ONLY data-destroying spike target; the demo
consumes the Spike A stack in reconcile mode (spike:up / env-up.sh), never destroys it.

Authority: batch sign-off was given by the operator on 2026-08-08 — the slate document
is the record. Do NOT pause for per-change sign-off. Every operator fork is already
resolved (T-42, T-43(a)–(c) in the ledger; the demo's read-surface choice is decided at
engineer level in the slate); the run never decides, it executes.

Rules of engagement:
- Standing rules and verification gates G1–G6 are inherited unchanged from
  reference/gate-ladder.md — this repo's ladder (G1, G2(Go), G2(Playwright) WHERE A
  SEAM REQUIRES IT, G3 N/A, G4, RF, G6) and its isolation stanzas. NOTE:
  reference/overnight-run-plan-20260707.md has never existed in this repo; the
  gate-ladder file is the only mechanics source. G4 discipline greps: write
  N/A-VACUOUS, never clean/PASS (B-14 — neither internal/journal nor internal/workorder
  exists in hq).
- Preconditions for every leg: `export PATH="/usr/local/go/bin:$PATH"` before any Go or
  Playwright leg; `npx bddgen` before every Playwright gate leg (B-165); attest
  HQ_SYNC_SUBSTRATE_OPTIONAL and HQ_SYNC_GATE_CHILD UNSET in any G2 log; armed reds
  EXPECTED in any full Playwright run (inventory:883 B-27, sync:446 LST-17,
  receipt-carousel:123 B-162) — an occurrence is flake-trail evidence, not a card
  failure. No idle-waiting on background jobs: foreground legs ≤10m; detach +
  `tail --pid` beyond (a turn ending kills a backgrounded process — S1 lost ~15m to this
  on 20260808-2).
- Dispatch model: you are the control loop, not the implementer. For each card:
  create a worktree off overnight-20260809, spawn a FRESH subagent scoped to exactly
  that card (paste its slate entry, footprint, and spec references into the subagent
  prompt; it drafts, implements, runs G1–G4/RF in its worktree, and returns a compact
  report), then spawn a SEPARATE fresh subagent for the G6 adversarial review, then
  merge to the run branch and archive yourself. Never implement a card in your own
  context; your context holds reports, not diffs. Subagents cannot spawn subagents —
  G6 dispatch is always yours. A failed card PARKS per the slate rules (worktree
  preserved, DECISIONS-NEEDED.md) — dispatch the next card. Editing a file outside
  the card's footprint is NOT a park (§15ad.65) — see the conflict rules below.
- Order: SERIAL, one card at a time in slate order.
    1. `demo-sync-target` FIRST — the milestone close-bar deliverable. Acceptance: a
       new `task demo:sync` (script at .night-crew/qa/spike-supabase/demo-sync.sh)
       stands up the Spike A environment (reconcile mode), writes one field through the
       real `/saveResponse` path, shows it surfacing in an RxDB-served read on one real
       checklist, and exits with the TRI-STATE contract — 0 green / 1 ran-and-failed /
       2 could-not-run — proven RED-FIRST (each wrong-exit path captured before the
       fix, then correct after). Gate on the SCRIPT directly, not the `task` wrapper
       (which returns go-task's 201). Read surface: the real fill-view path (C3,
       hq_sync_read ON for the demo run), with the C2 `#sync-one-row` dev surface as an
       in-card fallback — an engineering choice, not a park.
    2. `spike-exit-code-honesty` (B-163) SECOND — PRIORITY-GATED behind the demo:
       dispatch it only after the demo card completes AND only if its high estimate
       (60m) + ~30m closeout are still in hand (stretch-gate arithmetic, computed and
       logged at the decision moment). Fix the three exit-code conflations in
       spike-e-reconnect.sh + rxdb/spike-e-reconnect.js per the slate; RED-FIRST is the
       exit code (force each wrong path exiting 1, then correct to 2). Spike-only
       footprint — no full Playwright suite.
- Per-change mechanics — the slate's workflow-preflight verdict for this repo is
  `openspec: absent` (no openspec/ directory; openspec list errors "No OpenSpec changes
  directory found"). Therefore: do NOT assert OpenSpec mechanics and do NOT create
  openspec/ scaffolding in this repo. Assert only the universal mechanics: red-first
  evidence, atomic commits, a change-identifying commit trailer on every commit, and the
  roadmap/status flip (flip the card in .night-crew/knowledge/roadmap.md in the same
  change set). Which per-change discipline this repo should adopt is an operator question
  (B-105) — never encode another detected convention's (e.g. .planning/ GSD) mechanics
  in its place.
- Merge-intent note (REQUIRED, §15ad.65): each subagent writes a merge-intent.md in its
  FIRST commit, before implementing — shared files touched (each file outside its own
  footprint, one line of why), what must survive any merge, and what is safe to drop.
  Both cards touch Taskfile.yml (card 1 adds the `demo:sync` target; card 2 the
  `spike:reconnect:red` wrapper note) — each must state that. Empty fields say "nothing
  here" explicitly. Paste this requirement into every implementer prompt.
- Conflicts, not fences (§15ad.65): the footprint is planning information, not a
  boundary — a card MAY edit any file, and doing so is not a breach and not a park.
  Collisions surface as ordinary git conflicts. YOU resolve them at merge by reading
  BOTH sides' merge-intent.md and resolving against intent, not text; then re-run
  G1+G2. PARK the later card ONLY when, having read both intents, you still cannot
  determine which behavior is correct. An operator-only question still PARKS. Never
  improvise forks.
- Conflict log (REQUIRED, §15ad.66): append an entry to
  reference/conflicts-20260809.md for EVERY merge, clean or conflicted — the cards
  involved, files and hunks, the intents you read, the resolution you took, and the gate
  result after it. Clean merges get a one-line entry, so an empty log never reads as "no
  conflicts" when it means "the logging never ran". Commit it with the merge. Summarize
  it in the closeout; morning triage audits it.
- Budget discipline: prefer a clean early exit over starting a card you cannot finish
  cleanly. The demo card is the milestone deliverable — if it parks, closeout rather
  than rush B-163 late. The demo consumes the shared Spike A substrate stack; bring it up
  with spike:up / env-up.sh (reconcile), NEVER spike:down.

Closeout (ALWAYS, leave ~30 min): update HANDOFF.md (per-card outcomes table, gate
evidence on the final tree, next actions) and DECISIONS-NEEDED.md (anything parked),
commit as docs. The morning reader is the operator — write for them.

Your FINAL CHAT MESSAGE is a hand-off, so CLAUDE.md §"Context checkpoints at hand-offs"
binds it. Do not end on a results summary. End with, in this order:
1. Next steps, led by morning triage — what is waiting, what is parked, what the
   operator has to decide (in particular: if the demo landed, the operator's own
   `task demo:sync` attestation run is the last thing before milestone close).
2. The context-checkpoint verdict with its reason. For a finished run it is
   🔄 Clear now (recommended) — /nc-morning-triage is designed to start cold and
   reviews the run branch on its merits; nothing in your context is load-bearing
   because HANDOFF.md, DECISIONS-NEEDED.md, and the conflict log carry it all. Say
   ⛔ instead only if something real is still in flight in this session.
3. The paste-ready next command: /nc-morning-triage
Writing HANDOFF.md does NOT discharge this. The file and the closing message are
different surfaces read at different moments, and the operator reading the last thing on
screen at 7am is the one this is for.
