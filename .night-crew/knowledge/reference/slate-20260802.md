# Slate — run `20260802` (Night A of a two-night milestone-close sequence)

**Run branch:** `overnight-20260802` · date is free (no branch of 2026-08-02 exists), no suffix.

**Sign-off:** ✅ **SIGNED by the operator 2026-08-01** — 6 cards, no stretch.
**Dispatch mode: CONCURRENT (2 tracks)**, chosen by the operator with both projections on screen
(concurrent 4h30m–6h20m vs serial 7h30m–10h35m; **card lists identical under both modes**, so the
choice was burn rate vs wall clock, not coverage). Concurrent burns roughly 2× serial.

🛑 **§5 criterion 1 was ARGUABLE and was signed anyway, knowingly.** The prior concurrent run
(`overnight-20260801`) landed 4 of 4, but the 2026-07-31 adversarial triage **refuted its headline
gate claim** (Playwright exit 1 on B-27 against a recorded exit 0), and `card-actuals.md` prices
that night's concurrency at **one invalidated full gate run, two shared-database collisions, and
one duplicate backlog number** at load 17 on 8 cores. The bet taken here is that **two** tracks is
a materially different load profile from three, and that B-35 — now known — is mitigated by the
per-leg isolation this slate mandates. **If gate evidence looks contended tonight, that is this
decision surfacing, and triage should say so.**

**Supersedes `slate-20260801-2.md`**, which was signed 2026-07-31 and **never launched**: the
launch session hit the §2 card-branch guard, the investigation that followed consumed the night,
and no branch was cut. Nothing of it ran; nothing is stranded. Its card content survives here,
re-sequenced around a goal it did not have.

**Goal, stated by the operator 2026-08-01:** the **fewest steps to milestone close**, where close
means **`dev` is bug-free and the OKRs are confirmed** — a definition that is deliberately *not*
"a release." Release is `dev → main`, cheap, and separate. This slate is Night A of two.

**Forks resolved:** every card here carries a resolved decision — ledger T-29 (104–110), T-30
(111–113) for the four white cards, and T-24 decision 67 for the newly-authored hydration card.
**No card in this slate carries an open operator decision.**

**Workflow preflight (recorded, per §1):** `openspec: absent` (exit 0 — the check read every path
and concluded), `gsd: detected`. Do **not** assert OpenSpec mechanics; do **not** create
`openspec/` scaffolding. Universal mechanics only. Trailers here are `Night-Crew-Card:` /
`Night-Crew-Run:`, **not** `Card:` / `Run:`.

---

## 🛑 Pre-step — 45 minutes, zero code, three KRs

**Do this before the run, attended or as a single agent leg. It is not a card.** Three key results
are unmet only because artifacts were never updated to match decisions already made.

| KR | Why unmet | Action |
|---|---|---|
| **P-KR2** | measured by *"the BACKLOG.md entry's disposition text at the time `sync-hard-cutover`'s WO is dispatched"* — the entry still reads `· new` | transcribe ledger **T-24 decision 67** into the "Cross-user checklist hydration divergence" entry **and** `roadmap.md:1770`, which still says *"needs a product ruling… not resolved this round"* six days after the ruling |
| **D-KR3** | per-card timings are recorded; the **median was never computed** against the prior cycle's N=12 / 94m baseline | compute and record it |
| **E-KR1** | requires *0 of the 2 superseded fetch-storm items reproduce* — and its own text allows **"exactly 1 reviewed architectural-argument note per item"** where no test is constructible. No test is constructible: `sync.js` is deleted by S1 | write the 2 notes into `.night-crew/knowledge/designs/` |

🛑 **If S1 is dispatched on Night B while P-KR2's entry still reads `new`, P-KR2 reddens** — on
text, not on substance. That is the whole reason this pre-step is first.

---

## Night budget and the cut line

**No `--night` was given, so this slate has no clock-derived cut line.** Estimates below come from
`reference/card-actuals.md` by size class, and are stated with their basis.

🛑 **The account's token quota is invisible to me** — plan tier, rate-limit ceiling, current
consumption and reset window are unreadable from here, and none of them is derivable from clock
time. This slate is sized against the **calendar only**. A run may exhaust quota before the clock
or the clock before quota, and **neither validates the other**. Spend is governed by the run's own
stop conditions.

### Night A card estimates

| Card | Class | Estimate | Basis |
|---|---|---|---|
| **A1** `sync-replication-scope-per-checklist` | client-layer modification + measurement leg, S–M | **1h15m–1h45m** | Modifies an existing module (`sync-rxdb/client.js`) rather than creating it (C1 was 112m creating it). Nearest same-shape actual `workflow-submission-status-client-half` (27m impl + 17m G6). High end because the re-measurement leg needs the live Supabase stack. |
| **A2** `sync-rxdb-write-policies` | security / RLS, SQL + Go attack suite | **1h45m–2h30m** | Same shape as `sync-jwt-bridge-endpoint` (82m impl + 10m fix + 10m G6 ≈ 102m); `sync-rxdb-row-visibility-rls`'s resume was 67m but **resumes are not cheap** (ledger), so a floor not a discount. High end: one new predicate, a SELECT policy, a 27-subtest suite extended red-first, **and B-36 fixed in the same package**. |
| **B1** `sync-cache-and-identity-hygiene` | small app fix + build script | **1h30m–1h50m** | Direct analog `pwa-cache-and-build-hygiene` — the same files — 50m impl + 41m G6 + 10m fix ≈ 101m with 8 G6 findings. Narrower obligations (decision 112) but red-first on a cross-tenant disclosure. |
| **P1** `build-deploy-manifest-integrity` (B-37 + B-13) | build script + doc, S–M, **no analog** | **1h00m–1h30m** | First-of-kind: parses precached HTML/JS for local imports and fails when one resolves to a skipped path. Priced against `precache-manifest-from-head` (the card that created this seam). **± wide.** |
| **P2** `workflow-unsubmit-failnote-reattach` (B-19) | backend + E2E, M | **1h00m–1h40m** | Data-loss fix with a red-first requirement; priced against `workflow-queue-period-and-failnote-upsert`, which touched the same fail-note surface. |
| **P3** `sync-banner-builder-tab-scope` (B-20) | small UI fix + test, S | **30m–50m** | Priced against the small end of the frontend fix class. |

- **Track A (serial)** = A1 → A2 = **3h00m–4h15m**
- **Track B (serial within track)** = B1 → P1 → P2 → P3 = **4h00m–5h50m**
- **Critical path (concurrent, 2 tracks)** = Track B = **4h00m–5h50m**, + ~30m closeout ⇒ **4h30m–6h20m**
- **Serial total (all six)** = 7h00m–10h05m, + ~30m closeout ⇒ **7h30m–10h35m**

**Track B is the critical path on Night A, deliberately.** Loading the bug work onto Night A is
what leaves Night B clear for the cutover, which is the card that must not be rushed.

---

## Shared surfaces and serialization

| Surface | A1 | A2 | B1 | P1 | P2 | P3 | Rule |
|---|---|---|---|---|---|---|---|
| **The live Supabase substrate** | **uses** | **uses** | — | — | — | — | 🛑 **HARD SERIALIZATION — this is why A1 and A2 are one track.** A2 rewrites its policies. Two cards mutating one substrate concurrently produces a security matrix nobody can attribute. |
| `sync-rxdb/client.js` | **owns** | — | — | — | — | — | No contention. |
| `sync-schema/sql/` | — | **owns** | — | — | — | — | No contention. |
| `backend/internal/sync` | — | **owns** (B-36) | — | — | — | — | No contention. |
| `build-sw.js` | — | — | **owns** | **owns** | — | — | 🛑 **The one real collision.** B1 lands its per-identity cache partition; P1 adds the import-reachability check. **B1 merges first**, P1 develops against the merged state. Both state what must survive in `merge-intent`. |
| `index.html` / `login.html` | — | — | **owns** | — | — | — | No contention. |
| `workflows.html` | — | — | — | — | **owns** | **owns** | P2 and P3 are same-track and serial — P2 merges before P3 is cut. |
| `backend/internal/workflow` | — | — | — | — | **owns** | — | No contention. |
| **`hq_test_b2_fdw`** | — | **owns** | **collides** | **collides** | **collides** | **collides** | 🛑 **B-35 — a plain `go test ./...` DROPS this database without asking.** Six cards in flight raises this from a hazard to a near-certainty. **Every leg — implementer, G6, and every fix round — MUST set a unique `HQ_RLS_TEST_DB`, `TEST_PORT` and DB prefix.** Non-negotiable. |

---

## Track A — the substrate pair, strictly serial

### A1 · `sync-replication-scope-per-checklist`

**Scope.** Scope RxDB replication to what the open checklist actually needs, so a device does not
hold rows for a checklist it never opened. Acceptance is a **red proving the current over-broad
scope**, then the fix. Authored at morning triage 2026-07-31 (T-29 decision 105) after G6 measured
the RLS path at ~23 ms/row and the operator's rider relocated the defect from substrate to scope.

**This card has an adopted-pending preference that governs it — `architecture/C-2`:** *"Any
client-side fetch or replication over a collection that can grow without bound is batched and
scoped — never pulled whole. Scope it to what the current view actually needs (for workflows, the
open checklist). Widening the scope requires a recorded decision."* **Paste C-2 into the
implementer prompt verbatim.** If the implementer hits a scoping edge, C-2 decides it — do not
park on a question this preference already answers.

**Footprint.** `sync-rxdb/client.js`, `tests/sync-rxdb-client.spec.js`.

**PARK if:** the scoping change requires a schema or policy change (that is A2's, and A2 has not
run yet).

### A2 · `sync-rxdb-write-policies`

**Scope.** WITH CHECK policies mirroring the read rule **per table** (decision 111's four-row
table), a SELECT policy, and the attack suite extended red-first. **Also fixes B-36 in the same
change set** — `resolveSpikeConfig` must not turn a `docker compose … port` failure into `t.Skip`;
gate the skip on an explicit `HQ_SYNC_SUBSTRATE_OPTIONAL=1` opt-out and `t.Fatal` otherwise.

**Why B-36 rides here and is not optional:** **E-KR2** requires *0 attack variants bypass RLS under
a suite comparable in rigor to `grant-enforcement-parity`'s 13 variants*. Today the package prints
`ok` and exits 0 while skipping the entire suite, so **E-KR2 cannot be evidenced at all** until
B-36 is fixed. This card is the KR.

**Footprint.** `sync-schema/sql/`, `backend/internal/sync`, the Go attack suite.

**Gate evidence rule while B-36 is unfixed — binds A2's own early legs and every other card
tonight:** any evidence citing `internal/sync` MUST include `-run TestRowVisibilityRLS -v` output
showing subtests **executed**. A package `ok` line provably cannot distinguish ran-and-passed from
never-ran.

**PARK if:** a write predicate is needed beyond decision 111's four rows (operator-only).

---

## Track B — hygiene and product bugs, serial within the track

### B1 · `sync-cache-and-identity-hygiene`

**Scope.** As re-specified 2026-07-31 (T-30 decision 112): one mechanism, a per-identity `api-cache`
partition, red-first on a **cross-tenant disclosure**.

**Footprint.** `build-sw.js`, `index.html`, `login.html`, `tests/sync.spec.js` (obligation 8).

🛑 **B1's `merge-intent.md` is load-bearing tonight and on Night B.** P1 edits `build-sw.js` after
it, and S1 edits it again tomorrow. B1 must state explicitly **what its cache partition needs to
survive**.

### P1 · `build-deploy-manifest-integrity` — promoted from B-37 + B-13

**Scope.** Two halves of one deploy-path defect, deliberately one card because fixing either alone
leaves the path broken.

- **B-37:** `build-sw.js` exits 0 after silently dropping shipping assets — reproduced at 22 files
  / 1481.9 KB instead of 29 / 2111.1 KB, and it **bit for real** during `overnight-20260801`'s
  merge 3 (24-file manifest). `workflows.html` is precached and imports `sync-rxdb/bootstrap.js`;
  a precached URL that 404s **fails the entire service-worker install for every returning client**.
  Fix the actionable invariant, not a blanket rule: **nothing precached may import something not
  precached.** Parse precached HTML/JS for local `import`/`src` references; exit non-zero if any
  resolves to a skipped path. Skipping a genuinely unreferenced scratch file still exits 0.
- **B-13:** CLAUDE.md says `task prod:deploy` runs `task sw`; `Taskfile.yml:178-190` does
  `git reset --hard origin/main` then `docker compose build`, so the **committed** `sw.js` ships.

🛑 **B-13 carried an embedded question — *"decide which is intended before editing either"* — and
this slate DECIDES it rather than parking it, under the delegation principle:**
**the Taskfile is right and the doc is wrong.** `precache-manifest-from-head` (DONE) made
`build-sw.js` read **git HEAD**, and the prod clone's HEAD after `git reset --hard origin/main`
**is** the shipped tree — so regenerating on the box is redundant by construction, and `sw.js`
committed with its source is the correct contract. **Fix the doc to match the Taskfile.**
Recorded here for **ratification at Night A's triage**; reverse it there if wrong.

**Why this clears the §15k promotion bar:** **D-KR2** requires prod parity verified on a
**returning** client. B-37 is precisely the defect that breaks returning clients. It does not merely
block a release — it blocks a named key result.

**Footprint.** `build-sw.js`, `CLAUDE.md`.

**Cut after B1 merges** — both own `build-sw.js`.

### P2 · `workflow-unsubmit-failnote-reattach` — promoted from B-19

**Scope.** `unsubmitChecklist` orphans fail notes and nothing re-attaches them. Red-first on the
orphaning, then re-attach. Data loss in the accountability path the workflow engine exists for.

**Footprint.** `backend/internal/workflow`, `workflows.html`, `tests/persistence.spec.js`.

🛑 **CLAUDE.md's persistence rule binds:** fail-note text and severity are two of the seven
persisted states. This card MUST carry a back-and-reopen regression test in
`tests/persistence.spec.js` — *"the feature is not complete without this test."*

### P3 · `sync-banner-builder-tab-scope` — promoted from B-20

**Scope.** `renderSyncBanner` paints the "Queued" badge onto Builder-tab rows, where it is
meaningless. Scope the badge to the rows it describes. Red-first.

**Footprint.** `workflows.html`, `tests/sync.spec.js`.

**Cut after P2 merges** — both own `workflows.html`.

---

## 🛑 Scope freeze — a standing rule for BOTH nights

**Anything discovered during Night A or Night B routes to the NEXT milestone unless it reddens a
named key result on the current OKR page.** The run applies this itself; it does not ask.

**Why this is in the slate and not left to judgment:** the milestone remainder has grown from work
found *inside* cards that landed clean on each of the last two nights (T-29 decision 105 →
`sync-replication-scope-per-checklist`; B2's own SQL → `sync-rxdb-write-policies`). Discovery runs
at roughly **5 findings per day and has not slowed in three weeks**. Without a freeze, the
close reopens after Night A — which is what happened to the last three slates.

**A discovery still gets a destination.** File it to `BACKLOG.md` with a handle and route it in the
closeout under "What is NOT done" **with the destination named**. That is B-38's rule; a finding
routed to the next milestone is disposed of, not dropped.

---

## Night B — committed sequence, slated after Night A's triage

**Recorded here so it is not re-litigated, but deliberately NOT given a launch prompt tonight.**
S1 must be cut against the base Night A leaves; slating its detail now would repeat the error the
superseded slate's stretch gate existed to prevent.

| | Card | Estimate |
|---|---|---|
| **Track A** | **S1** `sync-hard-cutover` — **COMMITTED, not stretch** | **3h–5h** |
| **Track B** | **P4** cross-user hydration fix (T-24 dec 67) | 1h30m–2h30m |
| | **P5** `receipt-event-date-app-timezone` (B-28) | 40m–1h10m |
| | **P6** `period-summary-contract-notice` (B-29) | 45m–1h15m |
| **Stretch** | B-05 (raw status token), B-31 (launcher hides reachable tools) | 45m–1h20m |

- Track A = **3h–5h**; Track B = **2h55m–4h55m**; critical path + closeout ⇒ **3h30m–5h30m**.
- **S1 is committed on Night B because three KRs require it** — P-KR3 (the no-parallel-run
  constraint carried into the WO verbatim), E-KR3 (one documented owner per offline data class
  *after cutover*), and D-KR2's precondition.
- **P4 is a card that does not exist yet and must be authored** — the ruling (T-24 dec 67) is
  signed and the work was never carded, which is B-38 exactly. Three requirements: a rejected
  submission must **not** resurrect as current state (archived, visible as history); a fresh 0/2
  **must accept clicks** — *"the silent no-op is a bug, not intended behavior"*; and the
  convergence matrix needs its missing asymmetric approved-for-A / rejected-for-B cell seeded.
- 🛑 **P6 is not only a code fix.** B-29 is a live contract break with sales-processor, ~8 weeks
  old. It needs an **operator-sent outbound notice**. The card fixes the code; the notice is
  yours, and D-KR2's "bug-free" claim is not honest without it.

---

## Explicitly NOT in this slate

| Item | Why |
|---|---|
| **B-27 / B-30 / B-32** (load-sensitive flake family) | `dev` is **not green** — `tests/inventory.spec.js:883` fails on a clean isolated re-run. **Judge G2 against that baseline; a card claiming "green" instead of "green except B-27" is reporting something false, and B-27 reddening is NOT the card's.** Eliminating the family is not a night's work; what the close needs is a *trustworthy baseline*, which the closeout provides by naming survivors with their grep handles. |
| **B-22 / B-23 / B-24** (guard integrity) | Real, and they make "green" weaker than it reads. None blocks a named KR. Under the scope freeze they go to the next milestone. |
| **B-33 / B-38** (night-crew tooling + ritual) | Not hq code. Transferred to night-crew's backlog as B-242 and B-245 on 2026-08-01. |
| **The `sync-jwt-bridge-endpoint` client half** | Card reads **DONE — BACKEND HALF ONLY**. S1 is expected to absorb the client half; **verify this explicitly when S1 is slated**, and if it does not, it is a fifth white card and the milestone does not close on Night B. |
| **The ~19 unrouted `· new` backlog items** | Milestone-boundary work for `/nc-roadmap-round`, per §15k. |

---

## The milestone remainder — derived from live card state, 2026-08-01

🛑 **The live OKR grader is blind in this repo.** `night-crew okr grade --repo .` returns *"no
metrics.jsonl found under .night-crew/runs"* (B-105 — hq's runs are hand-run slates that write no
metrics record). **This section is therefore reported in cards, from live roadmap state, and the
KR column below is read by hand from the OKR page — not from a grader.**

**Activity 1 holds 24 card bullets: 19 DONE, 1 DISSOLVED, and exactly 4 white.**

| White card | Night |
|---|---|
| `sync-replication-scope-per-checklist` | **A** |
| `sync-rxdb-write-policies` | **A** |
| `sync-cache-and-identity-hygiene` | **A** |
| `sync-hard-cutover` | **B** |

**All four are in this two-night sequence. On cards, the sequence closes the milestone.**

**On key results it does not, and the gap is one attended hour.** After Night B, **D-KR2** remains
— *prod parity with 0 drift **and** 2/2 tab screenshots verified on a **returning** client*. That
requires a deploy and an attended check. **Under the operator's own definition, milestone close
therefore contains a release**, whichever order they are done in.

**Hand-read KR ledger after each step:**

| KR | Pre-step | Night A | Night B | Attended |
|---|---|---|---|---|
| P-KR1, D-KR1 | already met | | | |
| **P-KR2** | ✅ closed | | | |
| **D-KR3** | ✅ closed | | | |
| **E-KR1** | ✅ closed | | | |
| **E-KR2** | | ✅ (A2 fixes B-36) | | |
| QA-KR1/2/3 | | measurable from run records | | verify |
| **P-KR3, E-KR3** | | | ✅ (S1) | |
| **D-KR2** | | | precondition | ✅ deploy + returning-client screenshots |

**Recommended additions beyond this slate: nothing worth adding.** Considered and rejected:
**B-22/B-23/B-24** (guard integrity — genuinely tempting because they make every other gate on
these two nights more trustworthy, rejected because none blocks a named KR and adding them makes
Night A's critical path the longest of the cycle); and **B-15** (automate the two-device check —
it would shrink D-KR2's attended hour, but it is a build of its own and would not pay back inside
this sequence).

---

## Run mechanics

Standing rules and gates **G1–G6 must be read from HQ's own ladder and pasted into every implementer
and every G6 prompt.** 🛑 `reference/overnight-run-plan-20260707.md` **DOES NOT EXIST IN THIS REPO** —
it lives only in the night-crew clone and its ladder is night-crew's own (**B-26**). Carry HQ's
ladder forward from `slate-20260801-2.md` §"HQ's verification ladder" verbatim.

- **`go test` must run with `-p 1`** (`Taskfile.yml:108` already does). Two agents hit the
  parallel-package failure mode independently on 07-29.
- **G4 ordering — B-37.** `build-sw.js` reads **git HEAD, not the working tree**, and exits 0 after
  silently dropping assets. Regenerate the service worker **after** the merge commit, never
  mid-merge. **Check the file count against expectation, not just the exit code.** P1 is the card
  that makes this mechanical — until it merges, the procedural rule binds every card.
- **Armed reds, by grep handle and never by line anchor** (decision 100):
  `list page progress decrements when another device unchecks a field [LST-17]` (`tests/sync.spec.js`)
  — **STAYS ARMED**, T-29 decision 109; 🛑 the bare tag `[LST-17]` matches **two** tests, so the
  FULL TITLE is mandatory. `a queued submission still lends its idempotency_key at 7:30pm CT [A1-TZ-02]`
  — B-30. `submitted checklist survives builder edit with assignment change [LC-02]` — B-32.
  **Non-reproduction retires none of them.**
- **🔴 `HQ_SYNC_REST_URL` stays armed.** A1 and A2 both touch the replication path. **No card may
  set it in any deploy.** It disarms only at triage, on evidence, never by the run asserting it.
- **Nothing deploys on Night A.** `task prod:deploy` is not a run action under any outcome.
