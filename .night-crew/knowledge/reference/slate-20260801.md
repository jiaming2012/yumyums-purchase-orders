# Slate — `overnight-20260801`

**Run branch:** `overnight-20260801` (no date collision — no prior run branch for 2026-08-01, and
`git branch --no-merged dev` returned **no unmerged run branches** at planning time).
**Planned:** 2026-07-31 (evening) · **Cycle:** "Sync foundation" · **Activity:** 1 — Sync
foundation: RxDB + self-hosted Supabase (the roadmap's only activity with unbuilt cards).
**Cards:** 4, across 3 tracks.

**Sign-off:** ✅ **SIGNED by the operator 2026-07-31** — all 4 cards, no night budget, no cut
line. **Dispatch mode: CONCURRENT** (3 tracks), chosen by the operator at sign-off after both
modes were put side by side. Recommendation basis: prior concurrent runs completed cleanly
(`overnight-20260726`, `overnight-20260729-2` across three tracks), and shared-surface overlap is
bounded rather than heavy — no two tracks rewrite the same call site. The burn-rate half
(≈3× serial) was the operator's to weigh; **quota was not measured and is not measurable from
here.** Serial order is recorded under §Execution notes as the fallback if a track stalls on rate
limits.

**Considered and not added at sign-off:** the B-23 spec-file-floor ratchet (see §Recommended
additions). The operator took the slate as presented; the mitigation is the G6 briefing that a
guard's PASS is not evidence until its population is shown non-empty.

**Forks resolved:** every card in this slate is fork-free at slating. Ledger T-28 decisions
**92** (fdw topology, decision 61 REVERSED), **93/94** (timezone two-repo ruling + badge reset),
**95** (removed question keeps its label — amendment A-3), **96** (retention window stays 30 days),
**97** (sheet caps at 10 groups), **98** (revision-2 plates SIGNED, card no longer
ATTENDED-BLOCKED). No card carries an open operator decision. `DECISIONS-NEEDED.md` for
`overnight-20260729-2` is fully resolved.

**Workflow preflight (recorded per §1):** `night-crew workflow preflight --repo /home/jcole/projects/hq`
→ **`openspec: absent`** (no `openspec/` tree), `gsd: detected` (`.planning/` is a directory).
Verdict is **`absent`**, not `undetermined` — the check read every path and concluded. The launch
prompt therefore asserts **universal mechanics only** and forbids creating OpenSpec scaffolding
here. Which per-change discipline HQ should adopt is an operator question (B-105), not this run's.

> **Tooling note, recorded because it nearly became a silent skip.** The installed
> `~/go/bin/night-crew` (built 2026-07-23) has **no `workflow` verb** — it printed usage. The verb
> exists on night-crew **`main` @ `258d723` (v3.0.2)**, so the preflight above was run from a
> binary built out of the pinned `night-crew-main` worktree into scratch. Nothing was rebuilt from
> `dev` and the operator's install was not modified. **The installed binary is stale and should be
> refreshed with `task nc:update`** before a ritual depends on a newer verb.

## Sizing

**No `--night` was given, so this slate has NO cut line** — every card below is committed, none is
budget-gated stretch. The figures are expectations for the closeout to measure against, not a
budget.

🛑 **What was and was not measured.** These are **wall-clock** estimates against
`card-actuals.md`. The account's **token quota is invisible to me** — plan tier, rate-limit
ceiling, current consumption and reset window are all unreadable from here, and none of them is
derivable from clock time. A run may exhaust quota before the clock or the clock before quota, and
neither validates the other. Spend is governed by the run's own stop conditions.

| Card | Class | Estimate | Basis |
|---|---|---|---|
| A1 `app-timezone-unify-new-york` | cross-cutting refactor (**resume**) | **1h–1h45m** | Its own 07-29 build took ~1h18m before G6 REJECT; that work is preserved at `8da3ded`. Remaining is 3 additions + re-verify, **plus an explicit orientation leg** — the 07-29 lesson: a card whose PARK trigger names an external contract must read the contract first (B2 parked in 15m by probing first; A1 spent 1h18m building and was rejected on evidence sitting in a committed document). |
| B2 `sync-rxdb-row-visibility-rls` | security / RLS (**resume**) | **1h30m–2h30m** | Prior slate priced it 2h45m–4h15m; it parked at ~15m during orientation. The mechanism is now decided and the `hq_can_see_template()` port is **already written out** in the park note and G6-verified as a faithful transposition. Schema-only work is systematically over-priced ~2.5× (B1: 2h15m est → 53m actual), but the attack-variant gate suite is real work and is not schema-only. |
| C1 `sync-rxdb-replication-and-conflict-handler` | integration / client, **first-of-kind** | **2h–3h15m** | No prior card puts RxDB into production. Nearest analogs are `sync-proxy-endpoint` (backend integration, 2 repair rounds) and the browser-delivery spike. Wide because first-of-kind; the `conflictHandler` is novel design work, not wiring. |
| C2 `sync-rxdb-conflict-notice-ui` | production UI + verifier gate | **1h30m–2h45m** | C1's mockup drew in 26m and then spent **two** repair rounds — "price a mockup card at draw + 2–3× draw for gating, and expect the findings to be about the checks rather than the artifact." This is production UI, so draw is larger and gating is the same shape. |

- **Critical path (concurrent)** = Track C's serial pair = 3h30m–6h, + ~30m closeout ⇒ **~4h–6h30m**.
- **Serial total** = 6h–10h15m, + ~30m closeout ⇒ **~6h30m–10h45m**.
- Reference point: `overnight-20260729-2` budgeted 11h and finished in **3h25m** across 5
  concurrent cards, every card at or under estimate. These estimates run hot.

## Preconditions and armed flags

| Flag | State for this run |
|---|---|
| 🔴 **`HQ_SYNC_REST_URL` must NOT be set** | **STILL ARMED.** `sync-proxy-endpoint` forwards every method with no row filtering; B2 is the card that adds filtering. B1's four tables are deny-all so the door is shut, but the proxy is unguarded. **Disarms only when B2 lands** — and only at triage, on evidence, not by the run asserting it. |
| 🔴 **`submission_fail_notes` duplicate check** | Re-arms before **every** `task prod:deploy`. **Nothing tonight deploys.** But tonight **adds two migrations** (`0072`, `0073`), so this is re-armed for the next deploy regardless of outcome. |
| 🔵 **Attended two-device convergence check** | **RE-ARMS TONIGHT.** A1, C1 and C2 all move frontend files and regenerate `sw.js`; the flag re-arms whenever the verify/merge path or any frontend file moves. This is a **morning attended follow-up**, not a run task. Runbook: `reference/attended-two-device-check.md`. Automating it is B-15. |
| 🔴 **Guard integrity — B-22/B-23/B-24** | Newly armed 2026-07-29. Three guards pass on empty or mis-scoped populations, **inside the checks last run added to cure exactly that**. **Every G6 reviewer is briefed: a guard printing PASS is not evidence until its subject set is shown non-empty.** Do not accept `verify-test-harness.sh` Check B, Check A2, or `shoot.mjs` measurement 6 at face value. |
| 🟡 **`go test` must run with `-p 1`** | Standing. `Taskfile.yml:108` already does it. Two agents hit the parallel-package failure mode independently on 07-29; brief every implementer. |
| ⚪ `task sandbox:e2e` | **Not applicable to HQ** — that is a night-crew dogfood precondition. HQ's attended analog is the two-device check above. Stated so the absence reads as considered, not skipped. |

**Armed reds — by grep handle, never by line anchor (decision 100).**

| Handle | File | Status |
|---|---|---|
| `list page progress decrements when another device unchecks a field [LST-17]` | `tests/sync.spec.js` | **LIVE and correct.** Use the **full title** — the bare tag `[LST-17]` matches **two** tests (`:446` decrements, `:1006` completes), so a tag-only handle is ambiguous. This slate is the first to say so. |
| ~~`tests/sync.spec.js:1198`~~ | — | **DEAD — DO NOT ARM.** Verified at planning: line 1198 is a bare `}`. It named no test for five nights and every "it passed" against it was unfalsifiable (decision 100, → B-25). |

## Shared surfaces and serialization

The footprint is **planning information, not a fence** (§15ad.65). A card MAY edit any file; doing
so is not a breach and not a park. Collisions surface as ordinary git conflicts and are resolved at
merge against both sides' merge-intent notes.

| Surface | Cards | Rule |
|---|---|---|
| **`workflows.html`** | A1, C1, C2 | Three-way, but **regionally disjoint**: A1 edits the period/timezone comparisons (`isCurrentPeriodEntry` ~`:1758-1762`, and the three "already submitted today" comparisons ~`:2274`, `:2308`, `:2674`); C1 adds **import + client construction only**; C2 adds the conflict banner/sheet component. **Landing order: A1 → C1 → C2.** A1 first because its edits are the most surgical and it is a resume. |
| **`sw.js`** (generated) | A1, C1, C2 | **NEVER merge the artifact.** On conflict, take either side and immediately regenerate: `task sw`. Re-run G4 (idempotence + version parity) after every merge that touches a precached file. |
| **`version.go`** | all 4 | Expect a conflict on nearly every merge. **Resolve per-constant**, not per-file — precedent `79fa7cd` (07-29). |
| **Migrations** | A1, B2 | 🛑 **Assigned here so the run never has to decide:** **A1 = `0072`** (the two column defaults; verified unclaimed — latest on `dev` is `0071`), **B2 = `0073`** (fdw / foreign tables). A card that finds its number taken **stops and reports**; it does not renumber unilaterally. |
| **`backend/internal/sync`** | B2 only | C1 touches `backend/Dockerfile`, **not** `internal/sync`. Disjoint. |
| **RxDB client layer** (new) | C1 creates, C2 extends | Heavy overlap by design ⇒ **same track, strictly serial.** C2 cannot start before C1 merges. |
| **`tests/sync.spec.js`** | C1 (likely) | B2's suite belongs in a new attack-variant spec file, not here. Bounded. |

## Wave 0

**None.** No card in this slate is a cross-cutting refactor that shrinks the others' footprints,
so nothing has to land before anything else globally. The only ordering constraints are Track C's
internal dependency and the `workflows.html` landing order above.

## Track A — `app-timezone-unify-new-york` (resume)

**Resume `card/a1-app-timezone-unify-new-york` @ `8da3ded`** — branch preserved, worktree intact,
work reusable. It was parked, correctly, because it landed a decision it lacked authority to land.

**Scope.** The operator has now ruled (decision 93): **both repos move to `America/New_York` in a
coordinated release.** So the card is **wider than A1 built it** and carries three additions on top
of the preserved work: **(1)** edit **both** contract documents and **assumption A5** —
`21-SALES-PROCESSOR-CONTRACT.md:27`/`:67`/`:319` and `999.2-SALES-PROCESSOR-CONTRACT.md:30` — this
is half of a two-repo agreement, not a refactor; **(2) `trends.go:89-98`**, which A1 missed and
which would otherwise leave two 12-week COGS windows on two different zones (the park note lists
two further sites to absorb); **(3)** the Setup-tab **Badge Reset** (`inventory.html:2713`) must
follow the app zone, not the browser's, and **`tests/inventory.spec.js:2022` is asserting the
defect** — rewrite it, do not work around it (decision 94).

🛑 **First leg is orientation, not code: read both contract documents before editing anything.**
This is the card's PARK trigger and the 07-29 lesson by name — A1 reported *"nothing parked, no
site turned out to be deliberately Chicago"*, which was false against the repo's own committed
artifacts.

🛑 **Nothing ships to prod until sales-processor's matching change is ready.** Building and merging
tonight is correct; **deploying is not**, and this card does not authorize a deploy. Until both
repos land, one is wrong — the disagreement is one hour at each period edge on rows with no
extracted `event_date`.

**Footprint.** `backend/internal/{inventory,purchasing,recipes}`, `sync.js`, `workflows.html`,
`purchasing.html`, `inventory.html`, `tests/inventory.spec.js`, migration **`0072`**, and the two
`.planning/phases/**/*-SALES-PROCESSOR-CONTRACT.md` documents.

**PARK if:** the contract edit turns out to require a sales-processor-side change this repo cannot
express alone, or the coordinated-release sequencing needs an operator call beyond decision 93.

## Track B — `sync-rxdb-row-visibility-rls` (resume)

**Resume the parked work.** B2 parked in ~15m during orientation — *before* writing SQL — because
it found decision 61's contract impossible: the projection and the mutation live in **two different
Postgres servers**, `max_prepared_transactions` is `0` at both ends, and `Sign()` is an allowlist
that can only emit `authenticated`. That park was correct and is now resolved.

**Scope (decision 92 — decision 61 REVERSED).** Build **foreign tables via `postgres_fdw`** from
the substrate onto HQ's live `template_assignments ⋈ users`, so **there is no projection to write**
and "same transaction" is vacuous — zero stale-permissive window. The extension is proven
installable at both ends by executing the C symbol. **Accepted standing cost, recorded at
sign-off: HQ's Postgres is on the network path of every RLS row check.**

🛑 **The full port of `ResolveEntityAccess` into `hq_can_see_template()` is ALREADY WRITTEN OUT** in
`.night-crew/runs/2026-07-29-2-autonomous/park-b2-sync-rxdb-row-visibility-rls.md`, and G6 verified
it as a character-for-character faithful transposition with both inherited properties preserved.
**Do not redo it.**

**Two inherited properties are knowing, not accidental** — the resolver never filters on
`assignment_role` (an `approver` sees what an `assignee` sees), and the
`roles && ARRAY['admin','superadmin']` arm is unconditional (every admin sees every template).
**Changing either is a SEPARATE card.** Do not vary substrate and permission semantics in one night.

**Gate: an attack-variant suite, red-first, modelled on the 16/16 `sync-jwt-bridge-endpoint` ran.**
Prove RLS **discriminating**, not merely responding — a 200 alone proves nothing, and a
`service_role` BYPASSRLS control is what rules out *"the table was empty."* Reuse the two findings
that card banked and do not rediscover them: `auth.uid()` is wrong for HQ (read the **plural** GUC
via `public.hq_jwt_claim`), and `public.hq_uid_trap` re-proves it every run.

**Footprint.** Sync DB policies/SQL, `backend/internal/sync`, migration **`0073`**, a new
attack-variant spec file.

**PARK if:** `postgres_fdw` cannot be made to work across the two servers as the port assumes, or
an operator-only question about the accepted network-path cost surfaces. Do **not** park merely
because the suite is large.

## Track C — the serial pair (frontend)

### C1 — `sync-rxdb-replication-and-conflict-handler`

**Scope.** Wire RxDB's Supabase replication client-side against **`/sync/rest/*` and
`/sync/realtime/*` on HQ's own origin** (decision 69, same-origin). Do **not** fetch
`/api/v1/sync/token` and attach a bearer — the proxy mints per request and injects it, and a
client-supplied `Authorization`/`apikey` is deliberately discarded. Realtime is reached at
`/sync/realtime/socket/websocket?vsn=1.0.0`; **do not add an `apikey` parameter, the door sets it.**
Inherits from `sync-jwt-bridge-endpoint` (decision 51, address moved only): the permanent
client-construction helper using `global.fetch` + `realtime.transport`, gateway-less, no Kong — and
the `@supabase/supabase-js` pin plus a smoke test that **fails loudly on upgrade**, since the
coupling is to how the library derives `<baseUrl>/rest/v1`, not to a public extension point.

**The `conflictHandler` is this card's real work (decision 50).** RxDB's default is unconditional
**master-wins** — not last-write-wins — no clock participates, and a strictly-later local write is
discarded silently (reproduced 4×). The decided policy is a **field-level three-way merge**:
different fields edited by different people all survive; only a genuine same-field clash falls back
to master-wins, **and then `conflict$` must surface it with the discarded value recoverable**.
Tractable because `assumedMasterState` is in `RxConflictHandlerInput`
(`conflict-handling.d.ts:10`) — diff fork-vs-assumed and master-vs-assumed to know who changed
what. It is **optional in the type**, so the rule needs a **defined fallback when it is absent**;
state that fallback explicitly rather than assuming presence. `conflict$` fires **per document**
and carries the document id (`upstream.js:333`). **Headless and testable is a requirement, not a
nicety** — C2's whole design assumes this handler's behaviour.

**Obligation 4 (decision 56).** The client-construction helper must expand umbrella slugs so the
launcher shows the per-tab surfaces the user can actually reach (`inventory` ⇒ `inventory-trends`,
`inventory-cost`).

🛑 **Obligation 5 (decision 59) AND ITS TRAP.** This is where a page finally imports
`vendor/rxdb.bundle.js`, so this is where the `globPatterns` precache entry is re-added.
**RE-ADDING THE GLOB ALONE BREAKS PRODUCTION, verified at source:** `backend/Dockerfile` COPY lines
are `21`, `25`, `26` (`icons`), `27` (`lib`), `30` (`backend`) — **`vendor/` is never copied into
the image.** A precached URL that 404s fails the **entire** service-worker install for every
returning client — the exact bug `pwa-cache-and-build-hygiene` fixed. **This card must add
`vendor/` to the Dockerfile in the same change set, or not re-add the glob.** There is no third
option and this is not a park trigger — it is a choice the card makes and states.

**Footprint.** New RxDB client layer, `workflows.html` (**import + construction only** — the
write-path swap belongs to `sync-hard-cutover`), `build-sw.js`, `backend/Dockerfile`, `vendor/`.

**PARK if:** the field-level three-way merge cannot be made headless-testable, or
`assumedMasterState`'s absent-case fallback turns out to be a product decision rather than a
mechanism the card can justify from decision 50.

### C2 — `sync-rxdb-conflict-notice-ui` (riskiest, goes last)

> 🛑 **READER'S NOTE — the card body in `roadmap.md` contains a stale
> `THIS CARD IS STILL ATTENDED-BLOCKED` block.** It is superseded by the card's own header and by
> **decision 98**. The card **IS signed and slate-ready.** The card text itself warns that an
> unattended merger reading top-down was previously able to take struck sentences as binding —
> **read the header and this slate, not the struck block.**

**Scope.** The user-visible half of decision 50: when a same-field clash falls back to master-wins,
`conflict$` must surface it to the crew member **with the discarded value recoverable** — not
silently dropped. Build against **revision 2** of the plates
(`.planning/phases/sync-rxdb-conflict-notice/mockup.html` + `UI-SPEC.md`, 16 plates / 32 renders).

**Build obligations — all settled, none reopenable by the run:**
- **A-1** — the banner carries **both** figures: what was overwritten in the retention window
  **and** how many rows are still to review. (This **overturned** the earlier single-count rule; a
  run that implements the struck rule "as drawn" reinstates the defect A-1 was filed against.)
- **A-2** — the confirm names the loss in its **title** (*"Replace 3 of Dana M.'s answers?"*), lists
  all server values struck through **with who saved each and when**, and labels its primary button
  **Replace**, not Restore.
- **A-3 (decision 95)** — **a removed question keeps its label, struck through and read-only.**
  This supersedes *both* readings of open decision (i). 🛑 **The committed plates `edge-removed`,
  `openq-count-a` and `openq-count-b` draw the raw field id `fld_prep_sink_temp` and must be
  REDRAWN**, and **the deviation from the approved mockup noted in SUMMARY.md** per CLAUDE.md's
  mockup rule. Buildable with no new schema: `template_snapshot` is `json.Marshal(tmpl)` of the
  whole template (`repository.go:695`) and `Field.Label` is on the marshalled struct
  (`model.go:44-57`), so the discarded document carries its own frozen label.
- **(b) STANDS** — handled rows **stay on the sheet**: a restored row and a kept-theirs row both
  collapse to a confirmation and keep an **Undo**, because a removed row cannot be undone. Rows
  leave only on Dismiss or expiry.
- **Decision 96** — retention window is **30 days**, read from **one named constant**; no surface
  restates the literal.
- **Decision 97** — the sheet **caps at 10 groups** with an "and N more" line, no date filter. Rows
  below the line are not dropped; the banner count still reports the true total.

**Dependency promoted by decision 95:** B1's recorded-not-fixed item **R-C** — `template_snapshot`
is `{type:'object'}` with no nested `properties` and nothing rejects a malformed value — moves from
an open question to a **dependency** of this card, because the snapshot's *shape* is now
load-bearing. Handle a malformed snapshot explicitly; do not assume well-formedness.

**Gates.** CLAUDE.md's UI discipline applies in full: State Enumeration Table, `done_when:` rows,
the self-verification ritual (screenshots read back with the Read tool, **not** described from the
spec), and the **verifier-subagent gate** between build and SUMMARY.md, whose inputs are ONLY the
UI-SPEC, the `done_when:` block, the diff and the screenshots.

**Footprint.** `workflows.html`, the RxDB client layer,
`.planning/phases/sync-rxdb-conflict-notice/` (the A-3 plate redraws).

**PARK if:** the verifier gate cannot pass a `done_when:` row without a design change the operator
owns, or A-3's redraw surfaces a counting consequence decision 95 did not settle. Triage's own
recorded reservation, worth watching in the built UI: `a1-banner` puts four figures on one screen
plus a batch button reading a fifth — all internally consistent and documented, but **a lot of
counting at 6am with wet hands.**

## Explicitly NOT in this slate

| Card / item | Why |
|---|---|
| `sync-cache-and-identity-hygiene` | **Dependency-blocked by C1** (this slate). The `api-cache` retirement is only correct once offline data actually comes from IndexedDB. Becomes slatable the morning after C1 lands. |
| `sync-hard-cutover` | **Dependency-blocked**, on B1 (done) + **B2** + **C1** (both in this slate) + jwt-bridge (done). Row-visibility RLS is not optional for this card specifically — the cutover is the moment `HQ_SYNC_REST_URL` gets set in a real deploy. Becomes slatable the morning after tonight, if both land. |
| `sync-rxdb-schema-and-replication` | **DISSOLVED 2026-07-28.** Not a card; its obligations were fanned out into the cards above. |
| **B-22 / B-23 / B-24** (guard defects) | Do **not** clear the §1.5 promotion bar — they are hardening, not architecture-blocking. No planned card is built wrong because of them. Carried instead as an **armed precondition** briefing every G6 reviewer (see the flags table). See §Recommended additions for the honest counter-argument. |
| **B-25 / B-26** (armed-red grep handles; the G1–G6 ladder has no written definition) | Both are **night-crew clone** concerns — slate mechanics and the gate ladder live there, not in HQ. B-25's substance is nonetheless **applied in this slate**: the preconditions table carries grep handles, and the dead `:1198` anchor is struck. |
| **B-27** (`inventory.spec.js:883` cross-spec pollution) | Hardening, not architecture-blocking. Proven pre-existing by reproduction, and masked by `playwright.config.js` `retries: 1`. **Cards running at `--retries=0` will see it** — implementers are briefed that it is known, attributed, and not theirs to fix. |
| Everything else in `BACKLOG.md` (~41 `· new`) | The backlog's normal destiny is the **next roadmap** at a milestone-boundary planning round, not this slate (§15k). |

## Milestone remainder

Derived from the **live roadmap state**, not from HANDOFF prose.

**Cards in Activity 1 not in this slate:** `sync-cache-and-identity-hygiene`, `sync-hard-cutover`.

**This slate does not close the milestone — it leaves it 2 cards short.** Both of those cards are
blocked **only** by cards inside this slate, so if tonight lands clean the milestone's entire
remainder becomes slatable in one further night. Reported in cards, not key results.

### Recommended additions

**Nothing worth adding — but one item was genuinely weighed, and here is the reason it lost.**

**B-23** (the `verify-test-harness.sh` Check A2 spec-file floor of 20 lost its bite) is a one-line
ratchet and it degrades *tonight's own evidence*: there are **20 static spec files** in `tests/` and
`bddgen` emits 1, so the floor already carries a file of slack, and **every card in this slate adds
spec files** — B2's attack-variant suite, C1's replication tests, C2's states spec — widening the
slack further. The argument for adding it is verification integrity, not architecture.

It loses on the §1.5 bar as written: the bar is *architecture-blocking*, and a blind floor does not
make any planned card build wrong. Mitigated instead by briefing every G6 reviewer that a guard's
PASS is not evidence until its population is shown non-empty. **If you would rather ratchet the
floor tonight, say so at sign-off** — it is an operator call, it is roughly a 10-minute card, and
it is the only remainder item that has any claim on this night.

## Execution notes

**Dispatch model.** Orchestrator + fresh subagent per card. The launch session is a **control
loop, never the implementer** — its context holds reports, not diffs. Per card: worktree off
`overnight-20260801` → fresh implementer subagent (scoped to exactly that card) → **separate** fresh
G6 subagent → orchestrator merges and archives. Subagents cannot spawn subagents; G6 dispatch is
always the orchestrator's.

**Single-session serial order:** A1 → B2 → C1 → C2.

**Two-session split** (if the night is interrupted): session 1 takes Track A + Track B (both are
resumes, both self-contained, both end at a mergeable state); session 2 takes Track C's pair, which
must run in order and shares a footprint with nothing in session 1 except `workflows.html` — by
then already landed.

**Merge-intent notes.** HQ has **no `openspec/` tree**, so the note goes at
`.night-crew/runs/2026-08-01-autonomous/merge-intent-<card>.md`, matching this repo's existing
convention (`merge-intent-b1-…`, `merge-intent-h1-…` from 07-29). Written in the card's **first**
commit, before implementing.

**Conflict log.** `.night-crew/knowledge/reference/conflicts-20260801.md` — an entry for **every**
merge, clean or conflicted, so an empty log never reads as "no conflicts" when it means "the
logging never ran."

**Run mechanics** — standing rules and gates **G1–G6** are inherited unchanged from
`reference/overnight-run-plan-20260707.md` and are not restated here.

> ⚠️ Recorded for the operator, not for the run: **decision 101 found that this inherited file does
> not exist anywhere in the repo.** The gate contract is recoverable from practice (G1 build+vet,
> G2 Go+Playwright, G3 red-first re-verified by G6, G4 `sw.js` idempotence + version parity, G6
> adversarial review) and the runs do use it, but **G5 has no definition in any sense the runs
> use.** Tonight inherits the same practiced ladder every prior run did — this changes nothing
> about tonight, and supplying the written definition is **B-26**, in the night-crew clone.
