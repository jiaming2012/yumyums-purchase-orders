# Slate — 20260715 (overnight run for the morning of 2026-07-15)

> **Run branch:** `overnight-20260715` (cut from `dev`; never pushed, `main` untouched).
> **Sign-off:** batch sign-off given by the operator **2026-07-14** — this document is the record.
> **Dispatch mode:** **concurrent** (operator choice 2026-07-14). §5 criteria all met: prior
> orchestrator-dispatch run (`overnight-20260714`) completed clean (14 legs, 0 crashes, 0 park);
> the 5 app tracks share **zero source files** (disjoint footprints per the roadmap App-footprints
> table); no card rewrites files another track reads. Degrade to serial on any rate-limit stall.
> Serial cut line noted below for reference.
> **Forks resolved:** none open. F-1 (Ops NFR-3 backend resubmit gate) stays in `BACKLOG.md`,
> scheduling delegated to the planning agents (`ledger.md` T-10) — NOT in this slate. Waivers
> (Inventory FR-24/25 D-3; Onboarding FR-16/NFR-4 D-5) exclude those flows. No card carries an
> open operator decision.
> **Precondition flags:** **SATISFIED — DB is the ephemeral Docker env** (`docker-compose.nc.yml`,
> postgres:16), now the canonical local path (Wave-0 `hq-infra-docker-standardize`, landed
> `overnight-20260714`). No host Postgres to arm. HQ carries ~37–41 pre-existing E2E reds as the
> known baseline — red-first proofs assert the NEW test flips red→green and adds no new reds, NOT a
> globally clean suite.
> **Night budget:** **6h treated as a FLOOR, not a ceiling** (operator directive 2026-07-14; see
> `design-findings-nightcrew.md` NF-1 + memory `feedback_budget_floor_not_ceiling`). Size to fill
> the night; graduated fixes below the prove-sweep cut line are the floor-filler, not deferred.
> **Ledger basis:** `reference/card-actuals.md` § overnight-20260714 — first app-code + test-only
> actuals exist. Estimates below are anchored on that run's central tendency (test-only card
> ≈ 6–13 min impl; +new-fixture ≈ 20–30 min; +new Go scaffold / multipart upload ≈ 35–45 min;
> G6 ≈ 0.5–2.5 min; merge ≈ 2 min). **NOT** the wide first-of-kind ranges, and NOT the scoping
> agents' inflated per-card hour guesses (NF-1: the estimator over-anchors on the high end).

## Target

**Activity 4 — Prove & fix the UNPROVEN** (the remaining bulk; the first build slate landed
`overnight-20260714`). This slate = the **test-only prove-UNPROVEN sweep across all 5 apps** —
every still-UNPROVEN flow gets a red-first assertion naming its observable DB/UI behavior — **plus
a first wave of the fixes that sweep surfaces** (budget-gated stretch).

Per the roadmap sign-off policy: **test-only WO first**; a flow whose assertion goes RED graduates
to a **fix WO** (same footprint). The scoping pass (5 parallel read-only agents, 2026-07-14)
forecasts **~34–40 of ~78 flows will go RED** — proving detonates a fix backlog; that is the
expected shape of "prove & fix." Tonight delivers the **full prove sweep (guaranteed)** + **as many
graduated fixes as the floor holds** (priority-first), parking the rest for the next slate.

Traceability: prove sweep → **QA KR-1** (vacuous/untested → asserted) + **Delivery KR** (flow
coverage); graduated fixes → **Eng KR-1** (known-broken → fixed). The sweep will *raise* the
visible known-broken count before the fix wave lowers it — honest reclassification.

## Estimates, critical path, cut line

Per-card cycle = implement + G6 + merge. Concurrent wall-clock = **longest track**, not card sum
(disjoint footprints run in parallel; the orchestrator serializes only its own merges, in slate
order). ~30 min closeout reserved off the top.

| Track (disjoint footprint) | Cards (serial within track) | Flows | Track time |
|---|---|---|---|
| A · Operations | checklists, approvals, builder, cross | 15 | ~120 min |
| B · Purchasing | order, po-approval, shopping, state-auth-scheduler | 18 | **~140 min ◄ crit path** |
| C · Users | security, ui-access | 16 | ~65 min |
| D · Onboarding | assignments, progress | 10 | ~55 min |
| E · Inventory | purchases, stock, setup, recipes-cross | 19 | ~130 min |

- **Concurrent prove sweep:** max track ≈ **140 min + 30 min closeout ≈ 2h50m** → all 16 prove
  cards land, ~78 flows proven. **Cut line: ~3h10m of the 6h floor remains for graduated fixes.**
- **Serial prove sweep (reference):** sum ≈ **510 min ≈ 8h30m** > 6h → only ~11 of 16 prove cards
  fit, **0 fixes**. Serial cannot complete the sweep — this is why concurrent was chosen.
- **Graduated fixes (budget-gated stretch, floor-filler):** start only after a track's prove cards
  land clean. Take RED flows in the priority order below until ~30 min closeout + the in-flight fix
  estimate is all that remains, then PARK the rest to HANDOFF with the full red list. Estimated
  ~10–15 fixes land concurrently in the remaining ~3h; the rest carry to the next slate.

**Fix priority (when graduating):** ① Inventory NFR-8 (slider reach-past-100 rollback, PRIORITY),
Stock FR-12/13/14 (aggregation/override) → ② Purchasing NFR-1/2 (state-machine + admin gates),
crons FR-19–22 → ③ Ops NFR-2 (photo), NFR-5 (sync/offline), FR-18 (skip logic) → ④ everything else
in flow-id order. A fix needing a migration / new field / schema change / operator question is a
**PARK**, not a fix — log to DECISIONS-NEEDED.

## Shared surfaces (serialization rules)

| Surface | Cards | Rule |
|---|---|---|
| App source files | within one track only | Each app owns its `*.html` + `internal/<app>` + `*.spec.js`. Cards **within** a track serialize (same spec file); **across** tracks they're disjoint → parallel. Verified: no file is owned by two tracks. |
| `sw.js` (Workbox regen after any HTML edit) | any card touching `*.html` | **Orchestrator-only, at merge.** `task sw` regenerates the precache once per merge in landing order; subagents never hand-edit `sw.js`. Cross-track merges serialize here (orchestrator merges one at a time). |
| Non-admin (`team_member`) session fixture | every 403-gate card (Ops FR-7, Purchasing NFR-2/FR-6/14, Users NFR-1, Onboarding FR-14/NFR-3, Inventory admin gates) | **Author inline per spec** — copy the invite-a-role pattern from `multi-role.spec.js` (`roles:['team_member']` → login as them). **Do NOT create a shared helper module** — none exists today (each spec has its own `login`/`apiCall`); a shared module would couple all 5 tracks and force a Wave 0. Inline keeps tracks disjoint. |
| Ephemeral Docker env (`docker-compose.nc.yml`) | all cards | Per-card worktree brings up its own env (Docker-assigned host port). **Concurrent = up to 5 envs at once** — the one resource-contention risk; if Docker strains or a port collides, that's a degrade-to-serial signal, not a park. |
| New Go test files (`internal/purchasing/*_test.go`) | purchasing-prove-state-auth-scheduler | New scaffold for the cron flows (FR-19–22) — owned solely by that card; no other card touches `internal/purchasing`. |

## Tracks

Riskiest card in each track goes **last** (its PARK note inline). Within a track, dispatch in the
listed order.

### Track A — Operations (4 cards) · owns `workflows.html`, `internal/workflow`, `tests/workflows.spec.js` + `persistence.spec.js`
1. **`ops-prove-checklists`** *(S, ~20m)* — FR-6 (idempotent submit no-dup), FR-7 (unsubmit auth 403), FR-8 (history last-50). Existing helpers (`createTestTemplate`, `submitChecklistViaAPI`). Likely GREEN except FR-8.
2. **`ops-prove-approvals`** *(M, ~25m)* — FR-10 (flag→reject status), FR-12 (reject+comments), FR-13 (rejection feedback render). Rewrite the vacuous flag test (`:485-508`). FR-10 likely RED.
3. **`ops-prove-builder`** *(M, ~30m)* — FR-16 (DOW schedule visibility), FR-17 (section visibility condition), FR-18 (skip logic show/hide). Needs template condition/skip fixtures. FR-18 likely RED.
4. **`ops-prove-cross`** *(L, ~45m; last — highest fixture cost)* — NFR-2 (photo presign→PUT→round-trip), NFR-5 (sync/offline queue+conflict+cleanup), NFR-6 (archived 409), NFR-7 (401 redirect+draft persist), FR-19 (template snapshot freeze), FR-20 (approval-gate). **PARK trigger:** S3/IndexedDB plumbing beyond a test fixture, or any change past validation/render. Several likely RED (NFR-2/5/7).

### Track B — Purchasing (4 cards) · owns `purchasing.html`, `internal/purchasing`, `tests/purchasing.spec.js`
1. **`purchasing-prove-order`** *(M, ~30m)* — FR-1 (draft roll-to-next-week when locked), FR-2 (qty stepper persist / qty-0 removes), FR-4 (suggestions + bulk-add), FR-6 (cutoff config admin + non-admin 403). Extend `seedShoppingList`. FR-1/FR-6 likely RED.
2. **`purchasing-prove-po-approval`** *(M, ~35m)* — FR-13 (locked PO read-only+badge), FR-14 (admin edit vs 403), FR-15 (approve button visibility), FR-16 (approve snapshot + both 409s). FR-14/15/16 likely RED.
3. **`purchasing-prove-shopping`** *(S/M, ~25m)* — FR-12 (vendor-complete cascade list→PO; rewrite vacuous tail), FR-17 (history endpoint shape), FR-24 (repurchase_log record on complete). FR-12 likely RED.
4. **`purchasing-prove-state-auth-scheduler`** *(L, ~50m; last — new Go scaffold)* — NFR-1 (state machine + optimistic-lock transitions/409), NFR-2 (admin-gate 5+ endpoints → 403), FR-19/20/21/22 (auto-lock / cutoff-reminder / low-stock / repurchase-reset **crons — new Go unit tests**, mock time), FR-23 (manual reset API). **PARK trigger:** a cron fix needing schema/migration. Most likely RED (crons + NFR-1).

### Track C — Users (2 cards) · owns `users.html`, `internal/users`, `tests/users.spec.js` + `multi-role.spec.js`
1. **`users-prove-security`** *(M, ~30m)* — NFR-1 (non-admin → 403 across 8 handlers), NFR-2 (invite token single-use + 7-day expiry), NFR-3 (grant write → `/me/apps` read round-trip), NFR-4 (notif-pref admin-or-self + ≥1 channel), NFR-5 (401 redirect). Needs the inline non-admin session. Guards are real → mostly GREEN; NFR-3 round-trip the likeliest RED.
2. **`users-prove-ui-access`** *(M, ~35m)* — FR-2 (load-fail error state), FR-4 (invite 422/409), FR-6 (reinvite fresh token+toast), FR-9 (email immutable readonly), FR-10 (channel 400), FR-11 (timezone IANA 400), FR-15 (inline get-invite-link), FR-16/17 (Access role toggles — the stale tests already repointed to `#t2/#s2`; add the persistence assertion), FR-18/19 (grant chip add/remove persist). Mostly GREEN; FR-15 edge the likeliest RED.

### Track D — Onboarding (2 cards) · owns `onboarding.html`, `internal/onboarding`, `tests/onboarding.spec.js`
1. **`onboarding-prove-assignments`** *(S, ~15m)* — FR-26 (unassign template idempotent + role-auto-assign survives). Seed is fixture-rich (`setupCompletedSignoffSection`). Likely GREEN.
2. **`onboarding-prove-progress`** *(M/L, ~40m; last)* — FR-10 (proof-photo round-trip), FR-14 (sign_off_role_required 403), FR-18 (custom-thumbnail upload), FR-21 (archived template disappears), FR-28 (seed idempotent re-seed — **NOT E2E-testable**; assert indirectly via `/templates` presence or note as untestable, do not force), FR-29 (manager vs hire progress % agree), NFR-3 (team_member 403 sweep). Needs inline non-admin session. FR-10/14/NFR-3 likely RED. **PARK trigger:** photo/thumbnail plumbing beyond a fixture.

### Track E — Inventory (4 cards) · owns `inventory.html`, `internal/inventory` (+`recipes`,`toast` read), `tests/inventory.spec.js` + `recipes.spec.js`
1. **`inventory-prove-purchases`** *(S, ~20m)* — FR-3 (item-picker selection persists across reload), FR-5 (discard sets discarded_at + row gone), FR-11 (vendor filter + pagination; convert ~8 data-dependent guards to real seed). FR-3/5 likely RED.
2. **`inventory-prove-stock`** *(M, ~30m)* — FR-12 (threshold classification seed→assert), FR-13 (COALESCE override wins: override POST→stock GET chain), FR-14 (manual count requires reason + persists), FR-15 (reorder-scroll / View-in-Setup nav). Convert count-guards. FR-12/13/14 likely RED (PRIORITY fixes).
3. **`inventory-prove-setup`** *(L, ~45m; last — photo + cross-pkg)* — FR-26 (items CRUD title-cased), FR-27 (photo upload → JPEG convert/resize), FR-28 (group thresholds low<high persist), FR-29 (vendors CRUD), FR-30 (tags list), FR-31 (repurchase-reset config — **crosses into `internal/purchasing`**; read-only expected, a write there is a footprint note). Many likely RED. **PARK trigger:** multipart/photo plumbing beyond a fixture; a write outside `internal/inventory` for FR-31.
4. **`inventory-prove-recipes-cross`** *(M, ~35m)* — FR-16 (Menu real handler, replace the `route.fulfill` stub), FR-23 (recipe cost math, not placeholder), NFR-1 (normalize on item-edit + confirm-vendor — the double-normalization gap; assert output, note the gaps as fix candidates), NFR-3 (401 redirect), NFR-8 (slider reach-past-100 → 422 → **rollback** E2E, PRIORITY), NFR-9 (live `/drift` fetch, not synthetic `DRIFT_BANNER`). NFR-8/9 + NFR-3 likely RED.
   - **Footprint note:** confirm the source-of-truth `inventory.html` before editing — the scoping agent found a served copy at `backend/cmd/server/public/inventory.html`; verify against the repo-root `inventory.html` and edit the source, not the embedded copy.

## Explicitly NOT in this slate

- **Waived flows** — Inventory **FR-24/25** (Trends/Cost `.coming-soon` stubs, D-3, unbuilt-future);
  Onboarding **FR-16/NFR-4** (video presign→FFmpeg + 503 fallback, D-5, env-gated). Excluded from
  the sweep, not proven, not counted.
- **F-1** — Ops NFR-3 backend resubmit `require_photo` gate. In `BACKLOG.md`; scheduling delegated
  to the planning agents (ledger T-10). A planner may fold it into a future Purchasing/Ops slate.
- **F-2** — orphaned `users.html:122` `<div id="s3">`. Trivial cleanup, backlogged; the
  `users-prove-ui-access` card may remove it opportunistically if in-diff, else leave it.
- **Onboarding FR-28 direct re-seed assertion** — boot-time concern, not E2E-drivable; the card
  proves it indirectly or records it untestable. No fix WO graduates from an untestable assertion.
- **Deep graduated fixes that PARK** — any RED flow whose fix needs a migration, a new field/type,
  a schema change, or an operator decision PARKs (worktree preserved, logged) — never improvise.

## Execution notes

- **Concurrent (chosen):** dispatch Tracks A–E in parallel, **≤1 in-flight card per track**, in the
  listed order within each track. The orchestrator alone merges to `overnight-20260715`, in slate
  order, running `task sw` once per HTML-touching merge. After a track's prove cards all land clean,
  that track's orchestrator loop begins graduating its RED flows to fix WOs (priority order above)
  until the floor's fix-budget is exhausted, then stops and reports the remaining red list.
- **Serial fallback (degrade trigger: rate-limit stall or Docker strain):** collapse to one
  in-flight card across all tracks, slate order A→B→C→D→E within the prove sweep; drop graduated
  fixes first (they're the stretch). Do NOT sit blocked on a rate limit.
- **Two-session split (if run attended in two sittings):** session 1 = Tracks A+B+C prove sweep;
  session 2 = Tracks D+E prove sweep + graduated fixes. Tracks are independent → any split on track
  boundaries is safe.
- **Run mechanics (standing rules, gates G1–G6)** are inherited unchanged by reference from
  `reference/overnight-run-plan-20260707.md` — not restated here.
