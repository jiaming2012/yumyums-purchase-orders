# Slate — 20260722 (overnight run for the morning of 2026-07-21)

> **Run branch:** `overnight-20260722` (cut from `dev`; **never pushed, `main` untouched**).
> **Sign-off:** batch sign-off given by the operator **2026-07-20** — this document is the record.
> **Target:** **Activity 3 remainder + Activity 4 (Feature build track)** — operator scope choice
> 2026-07-20 (AskUserQuestion, this session): "Activity 3 + Activity 4". The Activity-2 design was
> **SIGNED 2026-07-20** (ledger T-18; `designs/prove-surface-gating-and-endpoints.md` §8), so all
> 5 Feature WOs are unblocked; `replay-fetchstorm-gate` is Activity 3's only overnight-buildable
> remainder (`prod-alert-dup-guard` is an over-the-cycle prod observation, slate-20260721 precedent).
> **Forks resolved:** **none open.** All `runs/*/DECISIONS-NEEDED.md` are RESOLVED (latest: T-18,
> B1–B5). Two slate-time specifications were resolved by the operator **in this planning session
> (2026-07-20)** and are recorded HERE as the designated record:
> 1. **B5 role rule** (design §5 item 4: "specified at slate time"): approve/reject requires
>    **an approver assignment on the template ∨ admin ∨ superadmin**.
> 2. **UI contract for the new tabs:** build directly against the signed PRD State-Enumeration
>    tables + `done_when` (no separate mockup gate; purchasing-FR-18 precedent — the CLAUDE.md
>    mockup clause is satisfied by the signed PRD, operator-confirmed).
> **Precondition flags:** **SATISFIED — DB is the ephemeral Docker env** (`docker-compose.nc.yml`,
> postgres:16, Docker-assigned host port). Baseline suite is **exit-0 as of overnight-20260721**
> (waiver #1 formally retired) with **one known load-sensitive intermittent**
> (`sync.spec.js:1198`) — which S1 exists to kill; no-new-reds gates stay baseline-relative.
> ⚠ S1 changes production `sync.js` → **the attended two-device convergence check re-arms on
> land** (operator, morning).
> **Night budget:** none given (no `--night`). No cut line — this slate is the complete
> dispatchable set: every remaining card of the cycle except the over-the-cycle observation and
> the cycle gate (which needs tonight's cards landed first).
> **Dispatch mode (operator choice 2026-07-20): CONCURRENT — Track S ∥ Track F, one in-flight
> card per track**, orchestrator alone merges in slate order; S1's `--retries=0` streak legs
> must run in a quiet window or be re-run quiet before any determinism claim (see Execution
> notes). Serial (S1→F1→…→F5, ~5h–7h15m) recorded as the fallback — degrade to it on a
> rate-limit stall.
> **Ledger basis:** `reference/card-actuals.md` — per-card estimates + basis in §Estimates.
> **Standing instrumentation (Delivery KR3):** every card emits harness-measured per-card
> wall-clock (impl + G6 + merge legs, epoch-stamped `timings.log`), tabled in the HANDOFF.

## Target

**Two tracks, file-disjoint:**

1. **Track S — Sync trust** (Activity 3): `replay-fetchstorm-gate` — kill the reconnect fetch
   storm at its root (`sync.js:443`), harden the last known intermittent downstream of it, and
   close out the B4 contract flip. → Eng KR5 (determinism) · QA.
2. **Track F — Feature build** (Activity 4, serialized after the signed design): the two
   aggregation endpoints, the two tabs, then gating — landing order fixed by shared
   `inventory.html` + route surfaces. → Eng KR1/KR2/KR3 · Delivery KR2 · Product KR3 · QA KR3/KR4.

Traceability: every Track-F card implements a section of the SIGNED design
(`designs/prove-surface-gating-and-endpoints.md` §1/§2/§6) tracing to PRD-prove-and-surface
FR-1/FR-3/FR-5/FR-6; Track S implements the T-18 B2+B3 promotion.

## Estimates & critical path

Per-card cycle = implement + G6 + merge. ~30 min closeout reserved off the top. No cut line.

| # | Card | Class · basis | Impl | G6 | Cycle |
|---|---|---|---|---|---|
| S1 | `replay-fetchstorm-gate` | fix (S, in-file proven pattern) + **convergence/de-flake proof class** (07-18 measured ~73m; 07-21 A1 ~70–75m productive) | 60–90m | 10–20m (streak re-runs) | ~75–110m |
| F1 | `trends-spend-by-group-endpoint` | Go endpoint + red-first fixture test (07-15 B4 15m / 07-17 clock-seam M / 07-14 backend 21m; fixture authoring is the lever) | 25–40m | 2–3m | ~30–45m |
| F2 | `cost-margin-endpoint` | same class as F1 (join extension on existing `menu-cogs` shape) | 25–40m | 2–3m | ~30–45m |
| F3 | `trends-tab-frontend` | net-new UI + states-spec (analog: purchasing-fr18 27m impl + screenshot ritual) | 35–55m | 2–3m | ~40–60m |
| F4 | `cost-tab-frontend` | same class as F3 (table-heavier, chart-lighter) | 35–55m | 2–3m | ~40–60m |
| F5 | `inventory-tab-gating` | **first-of-kind** (net-new permission middleware, multi-surface) — budget ONE substantive revision round (07-17 XL lesson) | 45–75m | 3–5m | ~50–85m (+~15m revision budget) |

- **Serial (all 6, one in-flight):** Σ ≈ **265–405m (~4h25m–6h45m)** + 30m closeout ≈
  **~5h–7h15m**.
- **Concurrent (2 tracks, one in-flight per track):** critical path = Track F
  (F1→F2→F3→F4→F5 ≈ 190–295m) + closeout ≈ **~3h40m–5h25m**; S1 (~75–110m) tucks alongside.
  Saves ~75–110m — but S1's whole cost is *proving determinism* with `--retries=0` streak runs,
  and cross-track Docker/Playwright load is exactly what makes flake proofs lie (07-15: 1.5–2×
  latency under contention; 07-21 slate declined concurrency for the same reason).

## Shared surfaces (serialization rules)

| Surface | Cards | Rule |
|---|---|---|
| `sync.js` (production) + `tests/sync.spec.js`, `tests/workflows.spec.js`, `backend/internal/sync/access_test.go` (comment) | S1 only (owns) | **Fully disjoint from Track F.** Production JS change → `task sw` (orchestrator-only) on merge; re-arms the attended two-device check. |
| Full-suite / `--retries=0` streak baseline | S1 (determinism proof); F-cards run affected subsets only | S1's streak legs are the load-sensitive proof. Under concurrent dispatch they must run on a quiet machine or be re-run quiet before the claim is made (see Execution notes). |
| `inventory.html` (+ `task sw` regen) | F3, F4, F5 | **The collision that fixes Track-F landing order: F3 → F4 → F5.** One `task sw` per HTML-touching merge, orchestrator-only, in landing order. |
| Backend route registration (router wiring) | F1, F2, F5 | Additive one-line entries; merges serialize in slate order (F1 → F2 → F5). |
| `backend/internal/inventory` | F1 (owns), F2 (touches) | F2's join lives in `recipes`; any shared inventory helper lands via F1 first. |
| `backend/internal/users`, `me`, `auth` middleware, `users.html`, `backend/internal/workflow` handlers | F5 only (owns) | The gating card's net-new `RequirePermission` spine + B5 endpoint gate. No other card touches these. |
| Ephemeral Docker env (`docker-compose.nc.yml`) | all except none | Per-card worktree brings up its own pg16. Serial = one env at a time; concurrent = one per track (2 max — under the rolling-3 proven ceiling). |

## Tracks

### Track S — Sync trust (1 card) · owns `sync.js`, `tests/sync.spec.js`, `tests/workflows.spec.js`, `access_test.go` (comment)

1. **`replay-fetchstorm-gate`** *(S-fix + de-flake proof, ~60–90m impl)* — ledger T-18 (B2+B3
   promotion, operator: "promote it"). Four bundled moves, one root cause:
   (a) **Gate the ungated `SUBMIT_CHECKLIST` replay re-fetch** (`sync.js:443`) with the proven
   in-file pattern `(runner open) ∨ !silent` — exactly as the `APPROVE_ITEM`/`SAVE_TEMPLATE`
   branches already do. Red-first: reproduce the per-replayed-op `loadMyChecklists()` flood
   (fetch-count assertion on a catch-up with N queued SUBMIT ops) before gating.
   (b) **Harden the successor intermittent** `sync.spec.js:1198 › temperature answer converges`
   (pre-existing, load-sensitive, red 2-of-3 G6 legs on 07-21 — directly downstream of the storm):
   after (a), prove it deterministic with `--retries=0` streak runs (isolated + under-load, the
   07-18 recipe). (c) **Revert A2's test-side `checkAllWithRepair` to plain clicks**
   (`workflows.spec.js`) — dead code once the storm is gated. (d) **Optional:** fetch-abort guard
   in `sync.js api()` (silences suite-teardown noise, 07-21 §C) — take it only if (a)–(c) are
   green with time in hand. Plus: **flip the approver-inclusion pin comment**
   (`access_test.go:402-425`) from reviewer-NOTE to contract per signed B4 ("Everyone should see
   live ops"). Production `sync.js` change → `task sw` + attended two-device re-arm flag.
   → Eng KR5 · QA. **PARK trigger:** if `:1198` determinism demands plumbing beyond the gate +
   test-side deterministic waits (a new op type, schema, or a structural `sync.js` rework) —
   land (a)+(c)+(d), PARK the de-flake tail with the named streak evidence (do not hold the gate
   fix hostage to the tail; 07-18 precedent prices the tail as its own card).

### Track F — Feature build (5 cards, SERIAL in landing order) · owns `backend/internal/{inventory,recipes,users,me,auth,workflow}`, `inventory.html`, `users.html`, states specs

1. **`trends-spend-by-group-endpoint`** *(S–M, ~25–40m)* — design §2.2 (SIGNED). New
   `GET /api/v1/inventory/trends`: spend bucketed `date_trunc('week', event_date)` ×
   `purchase_items.group_id` over an 8–12-week window; tax-proration per §2.1 (the
   `recipes/handler.go:74-77` pattern); NULL-`purchase_item_id` lines → per-week `unlinked`
   array (rider (a), signed); linked-but-groupless → **"Ungrouped" pseudo-group** (D2, signed).
   Cookie-auth logged-in-only tonight — the grant gate arrives with F5 **same night**.
   Red-first Go test: every week×group cell = SUM of matching line-item spend on a
   ≥8-week/≥2-group seeded fixture, including an unlinked line and an Ungrouped line.
   Footprint: `backend/internal/inventory` (+ 1 route line). → Eng KR1 · Delivery KR2.
   **PARK trigger:** if proration can't reconcile with `period-summary` on the shared fixture
   (AC-6 identity breaks) → PARK with the failing reconciliation case; do not ship a
   number that disagrees with payroll's.
2. **`cost-margin-endpoint`** *(S–M, ~25–40m)* — design §2.3 (SIGNED). Extend the `menu-cogs`
   join (or a sibling endpoint per §2.3's choice) to select `SUM(daily_menu_sales.gross_amount)`
   revenue and compute `margin = gross − ingredient_cost_total`, `food_cost_% =
   ingredient_cost_total / gross` (zero-revenue → NULL rule per §2.3), plus both movers
   orderings. Red-first Go test matching a hand-computed fixture **to the cent**.
   Footprint: `backend/internal/recipes` (+ `inventory`), 1 route line. → Eng KR2 · Delivery KR2.
3. **`trends-tab-frontend`** *(M, ~35–55m)* — PRD Trends State-Enumeration table +
   `done_when` (SIGNED — the operator-confirmed UI contract; no mockup gate). Build `#s5`
   replacing the `renderTrends` stub (`inventory.html:993-995`): inline SVG/CSS
   weekly-spend-by-group chart + table, ~8–12-week window, Unlinked/Ungrouped rendered per the
   signed rules. Ships `tests/states-trends.spec.js` — empty / loading / error / populated /
   no-data rows forced by fixture, screenshots taken AND read back (multimodal) per the DoD
   self-verification ritual; the **without-grant row lands with F5** (gate doesn't exist yet).
   Footprint: `inventory.html` (`task sw` orchestrator-side). → Delivery KR2 · QA KR3.
4. **`cost-tab-frontend`** *(M, ~35–55m)* — PRD Cost State-Enumeration table + `done_when`
   (SIGNED). Build `#s6` replacing the cost stub (`inventory.html:997-998`): sortable
   per-menu-item food-cost table (units / revenue / ingredient cost / margin / food-cost %) +
   top/bottom movers highlight, inline SVG/CSS bars; **honest sparse/empty state** where Toast
   sales are absent (accept-sparse-prod, signed at OKR). Ships `tests/states-cost.spec.js`
   (same ritual; without-grant row lands with F5). Footprint: `inventory.html`.
   → Delivery KR2 · QA KR3.
5. **`inventory-tab-gating`** *(M–L, first-of-kind, ~45–75m — riskiest, lands LAST)* — design
   §1 (SIGNED: Option (i), umbrella semantics) + B5 fold-in. (a) Seed the two per-tab slugs
   `inventory-trends`/`inventory-cost` via `SeedHQApps` — **no migration** (QA-KR4
   down-migration clause N/A, signed). (b) Net-new `RequirePermission`-style check on the
   Trends/Cost data endpoints: passes on **(tab slug ∨ whole-app `inventory` slug ∨
   superadmin)** — umbrella semantics per the operator's rider. Without grant → **distinct 403**
   AND the tab does not render; with grant → 200 + tab. (c) `/me`-style resolver extension
   driving per-tab visibility + the Users admin-UI grant surface (nudge to co-grant per signed
   rider (c)). (d) **B5:** gate `ApproveSubmissionHandler`/`RejectItemHandler`
   (`workflow/handler.go:728-753, 793+`) with the role rule **specified in this slate
   (operator, 2026-07-20): approver assignment on the template ∨ admin ∨ superadmin.**
   Red-first with-grant/without-grant test pairs incl. the mixed Trends-only case (§1.6/§1.7);
   extends `states-trends/cost.spec.js` with the without-grant rows; 0 logged-in-only bypass.
   Footprint: `users.html`, `backend/internal/{users,me,auth,workflow}`, `inventory.html` tab
   render. → Eng KR3 · Product KR3 · QA KR4. **PARK triggers:** (i) if the B5 gate reddens
   existing workflow E2Es in a way that implies a *contract* question (a flow that legitimately
   has non-approver crew approving) → PARK the B5 leg to DECISIONS-NEEDED, land (a)–(c);
   (ii) if the `/me` resolver extension turns out to need a schema change → footprint breach →
   PARK the card whole.

## Explicitly NOT in this slate

- **`prod-alert-dup-guard`** — over-the-cycle prod observation (watch the Cliq channel), not an
  overnight build card (slate-20260721 precedent). Stays open on the roadmap until cycle end.
- **Activity 5 `cycle-gate`** — premature by definition: it needs tonight's cards landed,
  plus the attended prod deploy/parity checks (`task version`, screenshots on
  `https://hq.yumyums.kitchen`).
- **Prod deploy of the tabs** — attended (`task prod:deploy` from the operator's box), rides the
  cycle gate, not tonight.
- **The deferred backlog** — editprop tidy-ups (F-B/F-C/D-1), photo-S3 / offline-IndexedDB
  harnesses, onboarding video fixture, the two 07-21 §C observations — none clears the
  architecture-blocking promotion bar (§15k pattern); normal destiny is the next roadmap.
- **`openspec init` scaffolding** — structural repo decision, attended-only (07-21 C1 constraint).

## Execution notes

- **Serial (single-session order):** **S1 → F1 → F2 → F3 → F4 → F5.** S1 first — its
  `--retries=0` streak proofs get the quiet machine before any Docker-build load exists, and its
  production `sync.js` merge (+ `task sw`) lands before the night's HTML merges. Endpoints before
  tabs (F1/F2 → F3/F4) so each tab builds against a merged API; gating (F5) strictly last.
- **Two-session split:** session 1 = S1 + F1 + F2 (sync trust + both endpoints — backend-only,
  clean boundary); session 2 = F3 + F4 + F5 (both tabs + gating — all the `inventory.html`
  surface). Either session is independently mergeable.
- **Concurrent variant (if chosen at dispatch):** Track S ∥ Track F, at most one in-flight card
  per track; orchestrator alone merges, in slate order. **Constraint: S1's determinism-streak
  legs must not run while a Track-F Docker build/Playwright run is active** — either schedule
  them into a quiet window or re-run the streak quiet before claiming determinism (a streak
  under load proves nothing either way, 07-15/07-21 evidence). If a track stalls on rate
  limits, degrade to serial. `git stash` is forbidden in worktrees (07-15 hazard).
- **A PARK is not a stop** — worktree preserved, DECISIONS-NEEDED entry, dispatch the next card.
  F-track dependency note: if F1 PARKs, F3 cannot run (no API) — skip to F2/F4; if F3/F4 PARK,
  F5 still runs (it gates endpoints + render paths independently).
- **Budget discipline (tokens ≠ calendar):** prefer a clean early exit over starting a card that
  can't finish cleanly. If quota tightens, drop from the tail: F5 first (it's the
  next-morning-friendly card — endpoints ship logged-in-only for one day), then F4.
  Never leave S1's streak proof half-claimed or a tab merged without its states spec.
- **Suite run-mechanics (standing):** any full-suite / G6 leg runs with **`CI=1`** + explicit
  pre-migration boot on the isolated pg16 (the `:8199` foreign-server latch cost two prior runs
  ~10–15m each). Subagents never background long runs (07-21 A1 lost ~25–30m); foreground ≤10m,
  detach+`tail --pid` beyond. Compose postgres publishes no host port — use the scratchpad
  `ports:` override pattern.
- **Run mechanics (standing rules, gates G1–G6)** inherited unchanged by reference from
  `reference/overnight-run-plan-20260707.md` — not restated here.
