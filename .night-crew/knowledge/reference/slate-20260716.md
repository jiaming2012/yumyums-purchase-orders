# Slate — 20260716 (overnight run for the morning of 2026-07-16)

> **Run branch:** `overnight-20260716` (cut from `dev`; never pushed, `main` untouched).
> **Sign-off:** batch sign-off given by the operator **2026-07-15** — this document is the record.
> **Dispatch mode:** **serial** (only one card touches Docker; Cards 2 & 3 share the closeout doc +
> `roadmap.md` write surface, so concurrency is inapplicable — identical card list, no throughput
> win). See §"Estimates".
> **Target:** **Activity 5 — Cycle gate (closeout)**, the last activity. Single roadmap card
> `cycle-gate`, deps (all Activity-4 cards) DONE. Fanned out into 3 attestation/evidence cards
> (split rule — each leg rivals a normal change: a full-suite baseline run, a 5-app broken-flow
> audit, a 9-KR scorecard + metric).
> **Forks resolved:** the one gray area — *the gate cannot pass literally tonight* (criteria "full
> suite green / 0 pre-existing reds" and "vacuous 23→0" collide with HQ's ~37–41 documented flaky
> reds, the SW-blocked offline tests, and the test-hardening WOs the roadmap deferred to backlog) —
> was resolved with the operator **2026-07-15: ATTEST & WAIVE.** The gate runs as an attestation of
> the honest criteria; the two unmeetable criteria are formally **waived** and their work carried to
> the next cycle's roadmap (extends the D-3/D-5 waiver precedent). No card carries an open decision.
> **Precondition flags:** **SATISFIED — DB is the ephemeral Docker env** (`docker-compose.nc.yml`,
> postgres:16), the canonical local path since Wave-0 `hq-infra-docker-standardize`. Card 1 brings
> up the stack for one read-only full-suite run; Cards 2–3 are pure repo reads.
> **Night budget:** `--night 8h` given. **This slate is short by the operator's own choice, not
> under-scoping.** Activity 5 is the LAST activity — there is nothing downstream to spill into
> (§2b.5). The only 8h-filling work (flaky stabilization + the deferred test-hardening WOs) was
> offered as "Stabilize the suite" and **declined** in favour of "Attest & waive"; per the
> architecture-only promotion bar those backlog items are the *next* roadmap's feedstock, not
> tonight's floor-filler. The "6h floor not ceiling" directive (NF-1) assumes graduatable work
> exists below the cut line — at a milestone boundary it does not. Critical path ≈ **~2h**.
> **Ledger basis:** `reference/card-actuals.md`. These are a NEW card shape (read-only audit + one
> suite run) — closest analog is the Activity-2/3 audit cards (~2–5 min/card) plus a single full
> `task test:all` run (~15–30 min on the stack). Not app-code cards; no Docker build-per-card floor.

## Target

**Activity 5 — Cycle gate (closeout).** The OKR closeout across all five apps. The `cycle-gate`
card's own criteria:

    Gate = 0 known-broken flows · full E2E suite green on localhost Postgres, 0 pre-existing reds
         · vacuous tests 23 → 0 · every repaired flow carries a red-first proof
         · median WO cycle time recorded over ≥5 WOs (baseline)

**Resolved reading (operator 2026-07-15, "Attest & waive"):** three criteria are met and attested;
two are formally waived against documented reality and carried forward.

| Criterion | Verdict tonight | Basis |
|---|---|---|
| 0 known-broken flows | **ATTEST PASS** | denominator driven 4→0 in Activity 4 (+NFR-1 fix); D-3 (Inv FR-24/25) + D-5 (Onb FR-16/NFR-4) waived |
| Full E2E suite green, 0 pre-existing reds | **WAIVE** → attest "0 *new* uncategorized reds vs the documented ~37–41 flaky baseline" | ledger standing note: cards judged no-new-reds-vs-baseline, never clean-suite; `playwright.config.js:29` blocks SWs (offline flows can't run) |
| Vacuous tests 23 → 0 | **WAIVE** → attest the ~4–5 rewritten this cycle; the ~18 remainder are the deferred test-hardening WOs (still `new` in `BACKLOG.md`) | Ops FR-10/12, Onboarding 6 guards, Inventory ~40 guards, Ops FR-15 all deferred, never shipped |
| Every repaired flow red-first | **ATTEST PASS** | `card-actuals.md`: red-first load-bearing on all fix cards (ops-fr4/nfr3, purchasing-fr18, onboarding-nfr5, inventory-nfr1) |
| Median WO cycle time ≥5 WOs | **COMPUTE** | 38 WOs of data across 4 runs in `card-actuals.md` |

Traceability: attestation → **Eng KR-1/2, QA KR-1/2/3** closeout; scorecard → the cycle OKR retro.
This is a **milestone boundary** — on a clean gate the next step is `/nc-okr-session` for the next
cycle (which consumes the carried-forward backlog).

## Estimates, critical path, cut line

New card shape (read-only). Per-card cycle = implement (audit/run) + G6 + orchestrator merge.

| # | Card | Size | Impl est. | Basis |
|---|---|---|---|---|
| 1 | `cycle-gate-suite-baseline` (Wave 0) | M | ~30–40 min | one full `task test:all` on the pg16 stack (up/down ~5m + 170+ Playwright + Go units ~15–30m + red categorization ~5m) |
| 2 | `cycle-gate-attestation` | S | ~15–20 min | 5-PRD + git-log audit; Activity-2/3 audit-card shape (~2–5 min/leg × 5 apps) |
| 3 | `cycle-gate-scorecard` | S | ~15–20 min | metric compute + 9-KR scorecard + roadmap flip + ledger append |

- **Critical path (serial):** ~40 + ~18 + ~18 + G6 ×3 (~6m) + merges (~6m) + **~30m closeout** ≈
  **~1h58m**. Fits the 8h with **~6h margin**. **No stretch cards, no cut line** — all 3 land or the
  gate is incomplete; there is nothing below a cut line and nothing to spill into (Activity 5 is
  last).
- **Concurrent (reference):** inapplicable. Card 1 gates 2 & 3 (they cite its evidence); Cards 2 & 3
  both write `roadmap.md` + the closeout doc → shared function-body surface → serial by rule (§5).
  The card list is identical under both modes; concurrency yields no throughput and adds a shared-doc
  race. **Serial is chosen on merit, not as a fallback.**

## Shared surfaces (serialization rules)

| Surface | Cards | Rule |
|---|---|---|
| Ephemeral Docker env (`docker-compose.nc.yml`) | Card 1 only | One read-only stack for the full-suite run; torn down `--volumes` after. Cards 2–3 need no stack. |
| Closeout doc (`reference/cycle-closeout-20260716.md`) | 1 → 2 → 3 (each appends its section) | **Orchestrator-owned assembly.** Card 1 writes the evidence block, Card 2 the attestation, Card 3 the scorecard. Subagents return their section as a report; the orchestrator writes the doc in landing order. Serial removes any race. |
| `roadmap.md` (`cycle-gate` row flip) | Card 3 only | Orchestrator flips `PLANNED → DONE` with the waiver footnote at Card 3 merge — after the attestation (Card 2) and metric (Card 3) exist to justify it. |
| `ledger.md` (T-13 closeout append) | Card 3 only | Orchestrator appends the cycle-closeout ledger entry at final merge. |
| No app source | — | **Zero `*.html` / `internal/*` / `*.spec.js` edits this slate.** `task sw` never runs (no HTML touched). If any card proposes an app-source edit, that is a footprint breach → PARK. |

## Cards (serial, in order)

### Card 1 — `cycle-gate-suite-baseline` (Wave 0, gating) · read-only + one stack run

Bring up the ephemeral pg16 stack and run the **full** E2E suite once — `task test:all` (Playwright
`tests/*.spec.js` + `task test:go` units) against localhost Postgres. Capture exact **pass / red /
skipped** counts. Then **categorize every red** against a documented cause:
- the ~37–41 known flaky pool — offline-sync, tab-persistence, cross-test DB-pollution;
- SW-blocked (`playwright.config.js:29` blocks service workers → offline/IndexedDB flows);
- the 33 `test.skip`/`fixme` (27 in `purchasing.spec.js`) — expected-skipped, not red.

**Output (returned as a report; orchestrator writes it to the closeout doc):** the baseline evidence
table — *N passing · N red each mapped to a named documented cause · N expected-skip · **0
uncategorized reds***.

- **Footprint:** owns nothing; runs the stack read-only; **no source edits**. Writes only its report.
- **🅿 PARK trigger (the one real risk):** a red that maps to **no** documented category — i.e. a
  genuinely new broken product flow. The gate does **not** cleanly pass. PARK: preserve the run log,
  log the specific test + failure to the run's `DECISIONS-NEEDED.md`, and continue to Cards 2–3 (the
  attestation records the exception honestly rather than papering over it). Do **not** attempt a fix —
  a new broken flow is an operator/next-cycle decision, not a closeout task.

### Card 2 — `cycle-gate-attestation` · read-only audit

Cross-app audit across all five hardening PRDs (`.night-crew/knowledge/prds/PRD-*-hardening.md`) +
`git log`:
1. **0 known-broken product flows.** Enumerate every flow ever marked BROKEN; confirm each is
   GREEN-fixed (cite the landing commit) or explicitly waived (D-3 Inv FR-24/25; D-5 Onb
   FR-16/NFR-4). Produce the attestation with its waiver-adjusted denominator.
2. **Red-first proofs.** Confirm each repaired flow's fix card committed a failing test before the
   fix — `ops-fr4-no-enforcement`, `ops-nfr3-photo-required`, `purchasing-fr18-history`,
   `onboarding-nfr5-video-reopen`, `inventory-nfr1-normalize-fix`. Cite the red→green commit pair.
3. **Formal waiver record (criteria 2 & 3).** Citing Card 1's evidence, record the waiver of
   "full-green suite" and "vacuous 23→0", and enumerate the **carried-forward backlog**: the ~37–41
   flaky pool + SW-blocked flows; the ~18 vacuous remainder (Ops FR-10/12, Onboarding 6 guards,
   Inventory ~40 guards, Ops FR-15); the 3 harness WOs (`WO-offline-indexeddb-harness`,
   `WO-photo-s3-harness`, `WO-cron-clock-seam`); F-1 (Ops NFR-3 resubmit gate).

- **Footprint:** read-only (PRDs, `git log`, `BACKLOG.md`, `ledger.md`); writes only its report.

### Card 3 — `cycle-gate-scorecard` · read-only compute + closeout writes

1. **Metric (Delivery KR baseline):** compute **median WO cycle time** over the shipped WOs in
   `reference/card-actuals.md` (≥5 WOs — state N, the median, and the per-run spread). No pass/fail
   target this cycle; this establishes the baseline.
2. **OKR scorecard:** score every key result with pass / partial / miss + evidence + the honest gap —
   Product (KR-1 5/5 PRDs · KR-2 enumeration recall ≥90% · KR-3 ≥90% WOs trace to a PRD flow),
   Delivery (100% broken/unverified shipped · median cycle-time recorded), Eng KR-1 (known-broken
   4→0), QA KR-1 (vacuous→genuine — **PARTIAL**, cite the remainder + waiver).
3. **Closeout writes (orchestrator, at merge):** finalize `reference/cycle-closeout-20260716.md`
   (evidence + attestation + scorecard), **flip `cycle-gate` PLANNED → DONE** in `roadmap.md` with
   the waiver footnote, and append the **T-13** cycle-closeout entry to `ledger.md`.

- **Footprint:** read-only (`card-actuals.md`, `okrs.md`, `roadmap.md`, `ledger.md`); writes the
  scorecard section, the roadmap flip, and the ledger append.

## Explicitly NOT in this slate

- **Flaky-suite stabilization** (~37–41 reds) — the "Stabilize the suite" option, **declined**
  2026-07-15. Next cycle's roadmap via `/nc-okr-session`. Some reds are blocked on
  `WO-offline-indexeddb-harness` first anyway.
- **The deferred test-hardening WOs** (Ops FR-10/12 assertion, Onboarding 6 guards, Inventory ~40
  guards, Ops FR-15 builder-UI) — QA cleanliness, below the architecture-only promotion bar. Stay in
  `BACKLOG.md` as next-roadmap feedstock.
- **The 3 harness WOs** (`WO-offline-indexeddb-harness`, `WO-photo-s3-harness`, `WO-cron-clock-seam`)
  and **F-1** (Ops NFR-3 resubmit gate) — backlogged; planner-scheduled, not this closeout.
- **Waived flows** — Inventory FR-24/25 (D-3), Onboarding FR-16/NFR-4 (D-5): excluded from the
  denominator, not re-proven.
- **Any app-source edit** — this is a read-only closeout. An `*.html` / `internal/*` / `*.spec.js`
  change is a footprint breach → PARK.

## Execution notes

- **Serial (chosen on merit):** Card 1 → Card 2 → Card 3, one at a time. Card 1 gates: if it PARKs on
  an uncategorized new red, still run Cards 2–3 (they record the exception). The orchestrator writes
  the closeout doc in landing order and does the roadmap flip + ledger append at Card 3 merge.
- **No `task sw`:** no HTML is touched; the SW is never regenerated. A card proposing an HTML edit is
  breaching footprint.
- **Milestone-boundary note for the morning reader:** on a clean gate, tomorrow's move is
  `/nc-okr-session` (open the next cycle, consume the carried-forward backlog) — **not**
  `/nc-slate-plan`. `/nc-morning-triage` first, as always.
- **Run mechanics (standing rules, gates G1–G6)** are inherited unchanged by reference from
  `reference/overnight-run-plan-20260707.md` — not restated here.
