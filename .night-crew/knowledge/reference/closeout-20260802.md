# Closeout — run `20260802`

**Run id:** `20260802` · Night A of a two-night milestone close
**Branch:** `overnight-20260802`, cut off `dev` @ `812bf84`. **Not merged to `dev`. Nothing pushed. Nothing deployed.**
**Slate:** `.night-crew/knowledge/reference/slate-20260802.md`, signed by the operator 2026-08-01
**Closeout written:** 2026-08-01 21:15 EDT

---

## What landed

**4 of 6 cards, plus the pre-step. Nothing parked. No operator-only fork raised.**
**All three of Night A's milestone (white) cards landed.**

| | Card | Merge |
|---|---|---|
| pre-step | P-KR2, D-KR3, E-KR1 closed (docs only) | `9f444ff` |
| **A1** | `sync-replication-scope-per-checklist` | `2dc4eef` |
| **B1** | `sync-cache-and-identity-hygiene` | `8b6b3bd` |
| **A2** | `sync-rxdb-write-policies` | `de7d78c` |
| **P1** | `build-deploy-manifest-integrity` | `a9e2018` |

**Not started (budget):** P2 `workflow-unsubmit-failnote-reattach`, P3 `sync-banner-builder-tab-scope`.
Both carry destinations in `HANDOFF.md` under "What is NOT done".

Every card ran **implement → fresh-context G6 → fix round → merge**. Four G6 reviews; **three found
blocking defects**; **no card merged on its first submission.**

**Key results moved:** P-KR2, D-KR3, E-KR1 closed by the pre-step. **E-KR2 met** by A2 (it was
*not* met at the reviewer's bar until A2's fix round — the policies were correct, the proof was not).

---

## Final gate result

On the merged tree `a9e2018`, fresh isolated databases, **quiet box**:

- **G1** — `go build ./...` exit 0 · `go vet ./...` exit 0
- **G2 (Go)** — `go test -p 1 -count=1 ./...` **exit 0**, all 9 packages `ok`
- **G2 (`internal/sync`)** — **54 subtests RUN, 54 PASS, 0 SKIP**, `HQ_SYNC_SUBSTRATE_OPTIONAL` unset
- **G3** — N/A (`openspec: absent`); none created
- **G4** — **31 files precached**, idempotent, parity 1.4.0
- **G2 (Playwright)** — one summary block: **1 failed · 6 skipped · 762 passed (24.5m)** of 769

**Baseline, honestly: NOT "green except B-27".** B-27 **passed**, as in every run tonight — per
decision 100 that retires nothing and it stays armed. All four armed reds passed. The single
survivor by full title is `tests/sync.spec.js:1343 › Convergence matrix (W-3): surviving answers
converge across devices › yes/no answer converges (live + catch-up)`, which **passes in isolation at
4.1s** and is a sixth distinct title in the rotating family recorded as `B-45`. Not laundered as
"not flaky".

An earlier final-gate attempt returned 7 failures in 51.7m; it was **contended by the orchestrator**
and is discarded. Same tree: **24.5m quiet vs 51.7m contended** — the most controlled evidence yet
that this suite's distribution is moved by CPU starvation, not flakiness.

---

## For triage

`DECISIONS-NEEDED.md` carries **no open forks and no parks** — seven items for ratification. The two
substantive: **R1** (B-13 decided as the slate directed, plus four *further* false deploy claims in
`CLAUDE.md` that the slate never anticipated) and **R2** (P1's precache count moving **29 → 31**
under a scope freeze, verified independently by G6 and judged the right call).

**19 backlog entries filed: `B-39` … `B-57`, no duplicates.**

🛑 **`HQ_SYNC_REST_URL` remains ARMED and UNSET.** It disarms only at triage, on evidence.

**Night B is cuttable:** A2 landed so S1 has policies to swap onto; B1's merge-intent states what
its cache partition needs to survive, corrected after its G6 proved the original claims false.
