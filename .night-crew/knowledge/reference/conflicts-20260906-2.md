# Conflict log — run 20260906-2

Every merge to `overnight-20260906-2` gets an entry, clean or conflicted, so an empty log
never reads as "no conflicts" when it means "the logging never ran" (§15ad.66).

## Merge 1 — `card/c1-refusal-holds-before-sync` (Card 1, the night's only card)

- **Cards involved:** Card 1 only — first and only merge of the night. Run branch cut at
  the slate sign-off commit `1f7d384`; card branch merged at `15619b5`; merge commit
  `afc9e97` (`--no-ff`).
- **Files/hunks:** **clean merge, no conflicts.** Nothing on the run branch had moved
  since the branch point, so there was no second side to reconcile. 26 files:
  `marketing/sync/replicas.js` (the fail-closed predicate + the `error$` latch),
  `marketing/submit-flow.js` (policy seam + `policy_unresolved` capture/write),
  `marketing/sync/push-replication.js` (enqueue whitelist line, schema `version: 1`,
  landing-body lines), `marketing/scan-page.js` (latch attach ordering),
  `supabase/migrations/20260906000300_scan_attempts_policy_unresolved.sql` (NEW numbered
  file; landed migrations untouched — `git diff --stat -- supabase/` is addition-only),
  `vendor/rxdb.bundle.js` + `vendor/src/rxdb-hq-entry.mjs` (the schema-migration plugin —
  schema v1 throws without it), `marketing/sync/harness/` (new `refusal-harness.mjs` +
  `refusal-run.sh`; `f2`/`push`/`campaigns` harnesses adjusted),
  `tests/marketing.spec.js`, `sw.js` (revision-hash-only), roadmap + BACKLOG flips, the
  goal ledger's `validated:` line, merge-intent, and the card's evidence logs.
- **Intents read:** Card 1's merge-intent only — no other side exists on a first merge.
  Its must-survive items were each verified by G6 independently rather than accepted:
  zero new (state,event) pairs on `submit-machine.js` (`git diff --stat` on that file and
  `tests/machine/` is **empty**); GAP-1's two push belts intact; the F-2 land-unverified
  path and `scan_attempts_names_a_code` still hold (the constraint is *tightened* by
  rider (a), not removed); no landed migration edited.
- **Resolution:** none needed.
- **Gate result after merge:** G4 re-run at the merged HEAD — `node build-sw.js` EXIT=0,
  **43 files precached** (unchanged; only the vendor bundle's content hash moved),
  import reachability 0 outside the precache, working tree **clean on the second run**
  (idempotent). Post-merge `tests/marketing.spec.js` re-run on the final tree — see
  HANDOFF.md for its figures. The card branch's own full-suite gate:
  **860 passed / 4 failed / 6 skipped, 870 tests, 27.6m, exactly one summary block**
  (counted over the complete log, never a tail).
- **Baseline note, recorded because the count hides it:** the four reds are *not* the
  four-red baseline set. `SYNC-FC-01`, `SYNC-RF-01`, `SYNC-RF-02` reproduced as expected;
  **`B1-XT-01` PASSED**; **`inventory.spec.js:1404` failed and is outside the baseline**
  → filed **B-437**. Four in, four out — a swap that a bare count would have concealed.
  Characterization is in HANDOFF.md; it did not block the merge.
