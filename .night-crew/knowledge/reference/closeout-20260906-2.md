# Closeout — run 20260906-2

Written at the closeout, 2026-09-05T23:16Z (19:16 EDT), on `overnight-20260906-2`.

- **Landed (1 of 1):** `refusal-holds-before-sync` — merge `afc9e97`. B-432 closed: the
  §8 `requires_online = true` refusal now holds before the campaigns replica has
  delivered, fail-closed on a Map-membership predicate (`campaignId != null` + campaign
  unresolved), never on replica readiness — which is what also closes the
  codes-arrive-first window. Migration `20260906000300_scan_attempts_policy_unresolved.sql`
  (NEW numbered file) carries the discriminator; RxDB schema v1 + device migration is the
  first schema migration in the tree. GAP-1's validation debt paid — `validated:` line
  appended to the goal ledger's Comebacks. B-434 riders (a)(b)(c) all disposed. B-436 and
  B-437 filed.
- **Parked:** nothing. No card hit a park condition; none of the slate's three narrow PARK
  notes was approached. No gray area was routed through `night-crew decisions log`, so
  nothing awaits ratification.
- **Final gate state:** G1 EXIT=0 (zero `.go` files in the diff); G2 (Go) not run —
  `backend/` untouched, stated not implied; G2 (Playwright) full suite **860 passed /
  4 failed / 6 skipped**, 870 tests, 27.6m, **exactly one summary block**; G3 N/A
  (`openspec: absent`); G4 re-run at merged HEAD EXIT=0, **precache 43**, idempotent;
  RF satisfied twice. Post-merge `tests/marketing.spec.js` **33 passed EXIT=0** on the
  final tree. G6: **APPROVE-WITH-FINDINGS** (5 findings, F1 material, none blocking).
  Merge logged in `conflicts-20260906-2.md` (1 merge, clean).
- **🛑 The four reds are NOT the four-red baseline.** `B1-XT-01` passed; `inventory.spec.js:1404`
  failed and is outside the set (**B-437**). Four in, four out — a swap a bare count hides.
  Characterized as non-attributable: green in isolation on **both** the card tree and the
  base tree `1f7d384`, spec byte-identical, zero inventory/Go files in the diff. Stated
  honestly — non-attribution rests on isolation runs plus diff contents, **not** on a
  second full-suite base run.
- **Nothing left polling:** `night-crew workers check` re-run at 19:14 EDT — queues
  `night-crew`, `night-crew-env` clear, no pollers.
