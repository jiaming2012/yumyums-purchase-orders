# Closeout — run `20260809`

- **Run:** `20260809` (slate `reference/slate-20260809.md`, signed 2026-08-08). Branch `overnight-20260809`
  off `dev` @ `bdf9f5a`. Serial, subagent-per-card dispatch. Not merged to `dev` (morning triage's act).
- **Closeout written:** 2026-08-08 14:31 AST.
- **Changes that landed (2 of 2 cards, both G6-passed, both merged clean):**
  - **C1 `demo-sync-target`** (Activity 5, milestone close-bar) — merge `0fade6b`. `task demo:sync` +
    `demo-sync.sh`: one field through real `POST /saveResponse` → NOTIFY relay → PostgREST → RxDB-served read,
    tri-state exit 0/1/2. Verdict **GREEN** (round-trip 117 ms). Roadmap card flipped PLANNED → DONE.
    G6 PASS-with-findings (read-surface note carried to DECISIONS-NEEDED; no fix round).
  - **C2 `spike-exit-code-honesty`** (B-163) — merge `3e6cd5c`. Seven `srcpsql` guards + vacuous-green fix in
    `spike-e-reconnect.sh`, uncaught-exception handlers in `rxdb/spike-e-reconnect.js`, Taskfile 201-trap note;
    all three conflations proven red-first 1→2. **B-163 → RESOLVED**. G6 PASS (no over-correction; no fix round).
- **Final gate result (merged tree `3e6cd5c`):** G1/G2(Go) N/A-by-footprint (zero `.go` changed);
  G2(Playwright) N/A-by-footprint (no seam fires); G3 N/A (openspec absent); **G4 precache 31** (verified by
  execution, sw.js byte-identical to committed); G4 discipline greps N/A-VACUOUS (B-14); RF + G6 passed both cards.
- **Conflict log:** `reference/conflicts-20260809.md` — 2 merges, both clean, both logged.
- **Parked:** none. One operator-awareness item (demo read surface) in DECISIONS-NEEDED for the attestation run.
- **Milestone:** NOT closed by design — `dev-complete-attestation` (the operator's own `task demo:sync` run) is
  the remaining attended act; the deliverable it runs now exists.
