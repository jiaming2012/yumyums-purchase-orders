Tonight's autonomous run. Execute the signed slate at
`.night-crew/knowledge/reference/slate-20260726.md`. Read it in full before dispatching anything —
it carries the per-card scope, footprints, estimates, park triggers, and the verified line numbers
each card needs.

Setup: you are on branch `dev`.

Before any git write, CHECK FOR UNMERGED RUN BRANCHES. Run this EXACTLY as written — it contains
no placeholder to substitute, and it asks about every date, because an unfinished night from any
date is a night's work sitting unmerged:

    git branch --no-merged dev --format='%(refname:short)' | grep -E '^overnight-[0-9]{8}(-[0-9]+)?$'

STATE THE RESULT OUT LOUD BEFORE PROCEEDING — either "no unmerged run branches" or the list. You
may only proceed after saying which one you saw. An empty result is the "go" signal, so a command
that failed or ran wrong looks exactly like a clean repo unless you have named what you found.

- No unmerged run branch → say so, then cut this run's branch. Compute its name from the BARE
  DATE, since you are looking for every branch of that date:

      git branch --format='%(refname:short)' | grep -E '^overnight-20260726(-[0-9]+)?$' | sort -V

  No match → `overnight-20260726`. Otherwise the next free suffix (-2, -3, …). A MERGED branch's
  name is still taken, so a date whose run merged this morning yields -2 — cut it and proceed
  without asking; nothing is at risk.
- ANY unmerged run branch exists → 🛑 STOP. A previous night's work is sitting on it. Report what
  is there (`git log --oneline dev..<branch>`, and whether that run's `DECISIONS-NEEDED.md` carries
  an open section for it), then ASK THE OPERATOR to choose: RESUME that branch, or RECUT at the
  next free suffix. Do NOT pick for them — resuming mixes two nights on one branch, recutting walks
  away from a night's work, and you cannot tell from here which is right. This is NOT covered by
  the batch sign-off: the yes is scoped to the signed specs, and what to do with an unfinished
  night is not one of them. Whatever they answer, the existing branch is left untouched — never
  delete, reset, force, or rename it.

If the run id gains a suffix, EVERY run-scoped name takes it with them: the conflict log
`.night-crew/knowledge/reference/conflicts-<runid>.md`, the run directory
`.night-crew/runs/2026-07-26-autonomous/` (keep the date, note the suffix inside), and the
closeout's references to the run. The slate keeps the name it was signed under.

Do all work on the run branch. Never push, never touch `main`. Hard prohibitions in CLAUDE.md
apply. **No production database, no hosted Supabase project, no deploy, no infrastructure
provisioning beyond local Docker.**

Authority: batch sign-off was given by the operator on 2026-07-25 — the slate document is the
record. Do NOT pause for per-change sign-off. Every operator fork is already resolved (ledger T-22
decisions 49–54, plus the two planner calls the slate flags and the operator accepted at sign-off).
The run never decides, it executes.

---

## Rules of engagement

- Standing rules and verification gates G1–G6 are inherited unchanged from
  `reference/overnight-run-plan-20260707.md`.

- **Dispatch model: you are the control loop, not the implementer.** For each card: create a
  worktree off the run branch, spawn a FRESH subagent scoped to exactly that card (paste its slate
  entry, footprint, park triggers, and spec references into the subagent prompt; it drafts,
  implements, runs G1–G4 in its worktree, and returns a compact report), then spawn a SEPARATE
  fresh subagent for the G6 adversarial review, then merge to the run branch and archive yourself.
  **Never implement a card in your own context; your context holds reports, not diffs.** Subagents
  cannot spawn subagents — G6 dispatch is always yours. A failed card PARKS per the slate rules
  (worktree preserved, `DECISIONS-NEEDED.md`) — dispatch the next card. Editing a file outside the
  card's footprint is NOT a park (§15ad.65) — see the conflict rules below.

- **Order — Wave 0 serial, then concurrent (operator-chosen 2026-07-25):**

  **Card A `workflow-submission-status-client-half` runs STRICTLY FIRST AND ALONE**, on as quiet a
  box as this machine offers. Nothing else is dispatched until A is merged. Two reasons, both
  binding: (i) `dev` is currently RED on `tests/repro-cut-task.spec.js:153` and
  `tests/sync.spec.js:1581`, so until A lands every other card's "no new reds" baseline is
  polluted; (ii) **A's acceptance test IS those two specs going green, and one of them lives in
  `sync.spec.js`, the load-sensitive file.** A green sampled under concurrent load proves nothing —
  that is 20260722's S1 lesson by name. Do not overlap anything with A.

  **Acceptance for Wave 0:** both named specs green, the new "no-approval submitted state renders"
  test green, and the `night-crew.toml` seam expansion landed. Then merge.

  **Then dispatch Card C and Card B CONCURRENTLY**, at most one in-flight card per track:
  - Track C: `sync-rxdb-browser-delivery-spike`
  - Track B: `sync-jwt-bridge-endpoint` (backend half only)

  They share **zero code files** — that is what makes this safe. **You alone merge to the run
  branch, and you merge C before B** (C's `build-sw.js` change is the one with reach). If a track
  stalls on rate limits, **degrade to serial — do not sit blocked.**

- **Per-change mechanics — the slate's recorded workflow-preflight verdict is ABSENT.**
  `night-crew workflow preflight --repo <root>` exits 1 with a `usage:` banner; the installed CLI
  has no `workflow` subcommand. This repo also has **no `openspec/` directory**.
  **Therefore: do NOT assert OpenSpec mechanics and do NOT create `openspec/` scaffolding in this
  repo.** No `openspec validate`, no `tasks.md` flow, no `OpenSpec-Change` trailer.
  Assert only the universal mechanics:
  - **red-first evidence** (see the per-card note below — it differs by card, and one card is
    exempt for a stated reason);
  - **atomic commits**;
  - **a change-identifying commit trailer on every commit** — use `Night-Crew-Card: <card-name>`,
    the identifier this repo's runs already key on;
  - **the roadmap card status flip** in the same change set as the work it describes.

  Which per-change discipline HQ should adopt is an operator question (B-105). **Never encode
  another detected convention's mechanics in its place.**

- **Red-first, per card (QA KR2 — 100% of this activity's WOs carry red-run evidence):**
  - **Card A** — the red-first test already exists at commit `0b53d46` on
    `card/f1-workflow-submission-status-default`. **Keep it, do not rewrite it** — cherry-pick or
    rebase it in. Capture it failing on the unfixed tree, then green it.
  - **Card B** — REQUIRED and it is the card's real gate. Every attack variant must be captured
    **refusing** before the policy that refuses it is written. A 200 alone proves nothing; prove
    RLS **discriminating**, with a `service_role` BYPASSRLS control ruling out "the table was
    empty."
  - **Card C** — **N/A, and it must say so explicitly in the WO record rather than leaving the
    field blank.** C's deliverable is a verdict, not a behavior change. An empty field and a
    considered "N/A" must not look alike.

- **Merge-intent note (REQUIRED, §15ad.65):** each subagent writes
  `.night-crew/runs/2026-07-26-autonomous/merge-intents/<card>.md` in its FIRST commit, **before
  implementing** — shared files touched (each file outside its own packages, one line of why), what
  must survive any merge, what is safe to drop, and the four-HARD-constraints attestation below.
  Empty fields say "nothing here" explicitly. **Paste this requirement into every implementer
  prompt.**

- 🛑 **Four HARD constraints. Every merge-intent must attest all four untouched, and that is the
  claim you check at merge:**
  1. `backend/go.mod`
  2. root `package.json` **and** `package-lock.json` — this is the Playwright environment for every
     card and every worktree. **Card C's use of `npx esbuild@<pinned>` instead of a devDependency
     exists precisely to hold this line.** A card that "just adds one devDep" breaks concurrent
     dispatch for the whole night.
  3. `docker-compose.nc.yml`
  4. root `Taskfile.yml`

  A breach of any of these still PARKS.

- **Conflicts, not fences (§15ad.65):** the footprint is planning information, not a boundary — a
  card MAY edit any file, and doing so is not a breach and not a park. Collisions surface as
  ordinary git conflicts. YOU resolve them at merge by reading BOTH sides' `merge-intent.md` and
  resolving **against intent, not text**; then re-run G1+G2. PARK the later card ONLY when, having
  read both intents, you still cannot determine which behavior is correct. An unplanned lifecycle
  row, a breach of the four HARD constraints, or any operator-only question still PARKS. **Never
  improvise forks.**
  **Exception — `sw.js` is a GENERATED file.** Never hand-resolve a conflict in it. Take either
  side, then re-run `node build-sw.js` (or `task sw`) and commit the regenerated output.

- **Conflict log (REQUIRED, §15ad.66):** append an entry to
  `.night-crew/knowledge/reference/conflicts-20260726.md` for **EVERY merge, clean or conflicted** —
  the cards involved, files and hunks, the intents you read, the resolution you took, and the gate
  result after it. **Clean merges get a one-line entry**, so an empty log never reads as "no
  conflicts" when it means "the logging never ran". Commit it with the merge. Summarize it in the
  closeout; morning triage audits it.

- 🛑 **Non-attribution rule for Playwright reds.** All three cards pay a wide suite (A the expanded
  4-spec subset, C and B the full suite), and all of them contain `tests/sync.spec.js:1198` — a
  proven ~16–20 % flake, whose exposure card A's seam fix **deliberately raises**. Also watch
  `tests/purchasing.spec.js:1407` (FR-13), which flaked once unattributed on 20260725. **A card
  that cannot test whether a red is its own FLAGS THE CORRELATION AND REFUSES TO ATTRIBUTE IT.**
  On 20260725 exactly that refusal is what let 21 minutes of orchestrator measurement produce a
  proven root cause instead of a wrong fact in the record. Guessing "pre-existing" is as wrong as
  guessing "mine". Report measured 1-min load with every suite result — the box is never idle
  (~13 unrelated containers), so a green here bounds the loaded-ish condition, not a quiet one.

- **Run mechanics that have each cost real wall-clock before — apply them, do not re-learn them:**
  - `export PATH="/usr/local/go/bin:$PATH"` before any Playwright or Go leg. The non-interactive
    shell does not carry Go, and Playwright's `webServer` dies with `go: not found` / exit 127 —
    which **looks like a test failure and is not**. This box's PATH contains spaces
    (`/mnt/c/Program Files/…`), so always quote; a bare `env PATH=$PATH …` splits and fails.
  - `-p 1` is load-bearing for the Go suite (`go test ./... -count=1 -p 1`) — without it
    `backend/internal/workflow` reds with `checklist_templates_created_by_fkey` violations from
    cross-package `users` truncation on a shared `hq_test_go`.
  - `CI=1` on every suite and G6 leg — forces `reuseExistingServer:false`. The `:8199`
    foreign-server latch has cost wall-clock on two separate runs.
  - Separate `TEST_PORT` / `TEST_DB_NAME` per leg (8199 / 8299 / 8399 / …) — zero collisions across
    four legs on 20260725 with this discipline.
  - **Never `git stash`** in a worktree — worktrees share `refs/stash`, and one `stash pop` once
    pulled a concurrent card's WIP into the wrong tree. Baseline-first or `cp` instead.
  - **Never background a suite run naively.** `$!` after `nohup … &` yields a wrapper pid that
    exits immediately; `tail --pid` on it returns instantly and **looks like a finished run**. Find
    the real `node`/`go` pid with `pgrep -af` first. Foreground ≤10 m; detach + `tail --pid` beyond.
  - **The spike Supabase stack is currently RUNNING** (`docker compose -p spike-supabase -f
    docker-compose.supabase.yml`) and card B needs it. **Do not tear it down.** Teardown is an
    explicit, deliberately unexecuted runbook step. If it is down, bring-up is idempotent and was
    measured at 3 m 22 s.

- **Per-card timing is a standing output (Delivery KR3).** Record epoch-stamped impl / G6 / land
  wall-clock per card to `.night-crew/runs/2026-07-26-autonomous/timings.log`. Card C is
  first-of-kind with no ledger entry — its 100–240 m band is a range, not a forecast; **record what
  it actually cost**, which is the whole point of the ledger.

- **Budget discipline: prefer a clean early exit over starting a card you cannot finish cleanly.**
  Card-specific risks the slate priced:
  - **Card C is the riskiest.** Its sharpest edge is that `playwright.config.js:60` sets
    `serviceWorkers: 'block'` **repo-wide**, so the service-worker leg cannot be proven inside the
    normal suite — it needs its own context/config/harness. **Do NOT change the repo-wide setting;
    it is load-bearing for 500+ tests.** Park triggers: (i) the vendored bundle cannot be made to
    work and the only remaining path is a real build step — **that is an operator constraint
    change; PARK, do not adopt it**; (ii) proving the SW leg requires editing repo-wide
    `playwright.config.js` settings — PARK. Note (iii): Dexie proving unusable is a **NO-GO
    verdict, not a park** — write it and finish the card.
  - **Card B parks** if bridging HQ grants into Supabase claims requires a *new* grant/permission
    concept rather than mapping existing ones. That is a product question — **never invent a
    permission model.**
  - **Card A parks** if teaching the client `'completed'` turns out to require changing
    `backend/internal/workflow` — that reopens decision 49. Park, do not re-decide.

---

## Closeout (ALWAYS, leave ~30 min)

Update `.night-crew/runs/2026-07-26-autonomous/HANDOFF.md` (per-card outcomes table, gate evidence
on the final tree, per-card timings, standing flags, next actions) and
`.night-crew/runs/2026-07-26-autonomous/DECISIONS-NEEDED.md` (anything parked), commit as `docs:`.
Summarize the conflict log. **The morning reader is the operator — write for them.**

Standing flags to address explicitly in the closeout, because they are live tonight:
- **`dev` is RED on two E2E specs** — disarms when card A lands. Say whether it did.
- **The attended two-device convergence check** — carried since 2026-07-22 and **card A re-arms
  it**, because A changes the live submit/render path. Say so; the operator owes that check.
- **`tests/sync.spec.js:1198`** — report whether it red at any point, across every leg.
- **`tests/purchasing.spec.js:1407`** — watch it; one prior observation is not an attribution.
- **W1's runbook integrity claim** — disarms only if card C's rider actually repaired it.

---

## Your FINAL CHAT MESSAGE is a hand-off

CLAUDE.md §"Context checkpoints at hand-offs" binds it. **Do not end on a results summary.** End
with, in this order:

1. **Next steps, led by morning triage** — what is waiting, what is parked, what the operator has
   to decide. Name the operator-owned preconditions explicitly if they are still open: the mockup
   sign-off for `sync-rxdb-conflict-notice-ui`, the `/nc-pm-session` on cross-user hydration
   divergence that hard-blocks `sync-hard-cutover`, and the two-device convergence check card A
   re-armed.
2. **The context-checkpoint verdict with its reason.** For a finished run it is
   **🔄 Clear now (recommended)** — `/nc-morning-triage` is designed to start cold and reviews the
   run branch on its merits; nothing in your context is load-bearing because HANDOFF.md,
   DECISIONS-NEEDED.md, and the conflict log carry it all. Say ⛔ instead only if something real is
   still in flight in this session.
3. **The paste-ready next command:** `/nc-morning-triage`

Writing HANDOFF.md does NOT discharge this. The file and the closing message are different surfaces
read at different moments, and the operator reading the last thing on screen at 7am is the one this
is for.
