Tonight's autonomous run. Execute the signed slate at
`.night-crew/knowledge/reference/slate-20260803.md`.

Setup: you are on branch dev.

Before any git write, CHECK FOR UNMERGED RUN BRANCHES. Run this EXACTLY as written — it contains no
placeholder to substitute, and it asks about every date, because an unfinished night from any date
is a night's work sitting unmerged:

    git branch --no-merged dev --format='%(refname:short)' | grep -E '^overnight-[0-9]{8}(-[0-9]+)?$'

STATE THE RESULT OUT LOUD BEFORE PROCEEDING — either "no unmerged run branches" or the list. You may
only proceed after saying which one you saw. An empty result is the "go" signal, so a command that
failed or ran wrong looks exactly like a clean repo unless you have named what you found.

- No unmerged run branch → say so, then cut this run's branch. Compute its name from the BARE DATE,
  since you are looking for every branch of that date:

      git branch --format='%(refname:short)' | grep -E '^overnight-20260803(-[0-9]+)?$' | sort -V

  No match → `overnight-20260803`. Otherwise the next free suffix (-2, -3, …). A MERGED branch's
  name is still taken, so a date whose run merged this morning yields -2 — cut it and proceed
  without asking; nothing is at risk.
- ANY unmerged run branch exists → 🛑 STOP. A previous night's work is sitting on it. Report what is
  there (`git log --oneline dev..<branch>`, and whether `DECISIONS-NEEDED.md` carries an open section
  for it), then ASK THE OPERATOR to choose: RESUME that branch, or RECUT at the next free suffix. Do
  NOT pick for them — resuming mixes two nights on one branch, recutting walks away from a night's
  work, and you cannot tell from here which is right. This is NOT covered by the batch sign-off: the
  yes is scoped to the signed specs, and what to do with an unfinished night is not one of them.
  Whatever they answer, the existing branch is left untouched — never delete, reset, force, or
  rename it.

If the run id gains a suffix, EVERY run-scoped name takes it with them: the conflict log
`.night-crew/knowledge/reference/conflicts-<runid>.md`, and the closeout's references to the run. The
slate keeps the name it was signed under.

Do all work on the run branch. Never push, never touch main. Hard prohibitions in CLAUDE.md apply.

Authority: batch sign-off was given by the operator on 2026-08-02 — the slate document is the
record. Do NOT pause for per-change sign-off. Every operator fork is already resolved: ledger
§15x-equivalent records are `T-30 decision 113` (delete-vs-edit), `T-23 decisions 55 and 61`
(ownership; grant projection), `T-24 decision 67` (fresh cycle per user), and the **B-43 list-scope
widening decided inline at slate planning and recorded in the slate's §Forks** — *lists stay live,
the scope is widened, the date floor is mandatory*. The run never decides, it executes.

Rules of engagement:

- Standing rules are inherited from CLAUDE.md. 🛑 **The verification ladder G1–G6 is NOT inherited by
  reference** — `reference/overnight-run-plan-20260707.md` DOES NOT EXIST IN THIS REPO (B-26). HQ's
  ladder is written out IN FULL in the slate under §"HQ's verification ladder"; **paste it into every
  implementer and every G6 prompt verbatim.**
- Dispatch model: you are the control loop, not the implementer. For each card: create a worktree off
  `overnight-20260803`, spawn a FRESH subagent scoped to exactly that card (paste its slate entry,
  its `done_when:` block, its footprint and its spec references into the subagent prompt; it drafts,
  implements, runs G1–G4 in its worktree, and returns a compact report), then spawn a SEPARATE fresh
  subagent for the G6 adversarial review, then merge to the run branch and archive yourself. Never
  implement a card in your own context; your context holds reports, not diffs. Subagents cannot spawn
  subagents — G6 dispatch is always yours. A failed card PARKS per the slate rules (worktree
  preserved, `DECISIONS-NEEDED.md`) — dispatch the next card. Editing a file outside the card's
  footprint is NOT a park (§15ad.65) — see the conflict rules below.
- **Order — SERIAL, one card at a time, chosen by the operator at sign-off:**
  **S1a `sync-cutover-list-scope`** → *merge* → **S1b `sync-hard-cutover`** → *merge* → **[gate]** →
  **P6 `period-summary-contract-notice`**.
  S1b is cut AFTER S1a merges, so it develops against the merged scope model — B-50 makes that
  mandatory, not stylistic: `HQ_RLS_TEST_DB` isolates only the HQ-side FDW database, and the Supabase
  `public` schema and the single PostgREST have **no** isolation variable, so two substrate-touching
  cards in flight buy a class of unattributable red.
  🛑 **The P6 gate is evaluated ONCE, when S1b's merge completes** — P6's HIGH-END estimate (2h) plus
  30m closeout must still be in hand. No partial credit, and do not re-evaluate it optimistically
  later in the night. A card started at hour ten because "there might be time" is the failure this
  gate exists to prevent. P6 is stretch; the night is a success without it.
- Per-change mechanics — the slate's recorded workflow-preflight verdict is **`openspec: absent`**
  (established by direct tree inspection; the installed `night-crew` CLI has no `workflow` verb).
  **Do NOT assert OpenSpec mechanics and do NOT create `openspec/` scaffolding in this repo.** Assert
  only the universal mechanics: **red-first evidence, atomic commits, a change-identifying commit
  trailer on every commit (`Night-Crew-Card:`), and the roadmap/status flip in the same change set.**
  Which per-change discipline this repo should adopt is an operator question (B-105) — never encode
  another detected convention's mechanics in its place.
- Merge-intent note (REQUIRED, §15ad.65): each subagent writes a `merge-intent.md` in its FIRST
  commit, before implementing — shared files touched (each file outside its own packages, one line of
  why), what must survive any merge, and what is safe to drop. Empty fields say "nothing here"
  explicitly. Since this repo has no `openspec/changes/` tree, put it at
  `.night-crew/runs/2026-08-03-autonomous/merge-intents/merge-intent-<card>.md`. Paste this
  requirement into every implementer prompt.
- Conflicts, not fences (§15ad.65): the footprint is planning information, not a boundary — a card MAY
  edit any file, and doing so is not a breach and not a park. Collisions surface as ordinary git
  conflicts. YOU resolve them at merge by reading BOTH sides' merge-intent notes and resolving against
  intent, not text; then re-run G1+G2. PARK the later card ONLY when, having read both intents, you
  still cannot determine which behaviour is correct. An unplanned lifecycle row or any operator-only
  question still PARKS. Never improvise forks.
- Conflict log (REQUIRED, §15ad.66): append an entry to
  `.night-crew/knowledge/reference/conflicts-20260803.md` for EVERY merge, clean or conflicted — the
  cards involved, files and hunks, the intents you read, the resolution you took, and the gate result
  after it. Clean merges get a one-line entry, so an empty log never reads as "no conflicts" when it
  means "the logging never ran". Commit it with the merge. Summarize it in the closeout; morning
  triage audits it.
- **🛑 Per-card isolation is MANDATORY, not advisory.** A unique `HQ_RLS_TEST_DB`, a unique
  `TEST_PORT`, a unique database prefix **and a unique scratchpad directory** for every implementer,
  every G6 reviewer and every fix round. B-35 and B-16 bit on 20260801; on 20260802 shared scratchpads
  clobbered logs and produced a contamination report that had to be retracted.
- **🛑 Environment, and it costs a night to rediscover:** `export PATH="/usr/local/go/bin:$PATH"`
  before ANY Go or Playwright leg — the non-interactive shell does not carry Go, and Playwright's
  `webServer` dies with `go: not found` / exit 127, **which looks like a test failure and is not**.
  Postgres is on **:5433**, not 5432. `go test` runs with **`-p 1`** — load-bearing; packages share
  one test DB and each `TestMain` truncates.
- **Orchestrator discipline, from 20260802's own stated errors — read these as rules, not anecdotes:**
  **Allocate backlog numbers yourself, up front** (three legs each claimed `B-39`; two more both
  claimed `B-46`; cards cannot see each other's numbers — the next free number is **B-61**).
  **Never dispatch a fix round into a worktree whose implementer is still alive.**
  **Never assert a quiet box from a stale check, and never create the contention yourself** by running
  Go or RLS suites alongside a Playwright gate — the same tree at the same commit gave 24.5m/1 failure
  quiet vs 51.7m/7 failures contended.
  **Never write a wait loop whose `pgrep` pattern matches its own command line** — that idled ~2h20m
  on 20260802 while reporting queued.
  **Reserve ~25m of orchestrator investigation time**, or accept that it comes out of the last card.
- **Baseline honesty:** B-27 and three other armed reds are STILL ARMED and were NOT retired by
  passing on 20260802 (decision 100 / T-31 decision 120). Judge G2 against *"green except the armed
  reds"*, matched by FULL TITLE, never by line anchor. A card claiming "green" instead of "green
  except the armed reds" is reporting something false, and an armed red reddening is NOT that card's.
- **Scope freeze (standing, both nights of this close):** anything discovered tonight routes to the
  **NEXT milestone** unless it reddens a named key result on the current OKR page. Apply it yourself;
  do not ask. A discovery still gets a destination — file it to `BACKLOG.md` with a handle and route
  it in the closeout under "What is NOT done" **with the destination named** (B-38).
- Budget discipline: prefer a clean early exit over starting a card you cannot finish cleanly. S1b is
  the riskiest card of the cycle and is last in its track by design; its three PARK triggers are in
  the slate and in the roadmap bullet. **A park with evidence is a better night than a merge with a
  caveat.**

Closeout (ALWAYS, leave ~30 min): update `.night-crew/runs/2026-08-03-autonomous/HANDOFF.md`
(per-card outcomes table, gate evidence on the FINAL MERGED TREE run by you and not inherited from
card reports, next actions) and `DECISIONS-NEEDED.md` (anything parked), commit as docs. The morning
reader is the operator — write for them.

🛑 **Stamp G6-start, G6-return and fix-return per card, and record them in
`.night-crew/knowledge/reference/card-actuals.md`.** This is **B-39**, and it is the highest-value
process fix in the backlog on evidence from two consecutive cycles: D-KR3's median rested on 11 of 18
cards last cycle and on **0 of 4** the night before this one. A KR measured from a ledger nobody
stamps is measured from nothing. It is three timestamps per card and costs seconds.

Your FINAL CHAT MESSAGE is a hand-off, so CLAUDE.md §"Context checkpoints at hand-offs" binds it. Do
not end on a results summary. End with, in this order:
1. Next steps, led by morning triage — what is waiting, what is parked, what the operator has to
   decide. Name explicitly whether the milestone closed on cards, and that **D-KR2 still needs an
   attended deploy plus 2/2 tab screenshots on a RETURNING client** either way.
2. The context-checkpoint verdict with its reason. For a finished run it is 🔄 Clear now
   (recommended) — `/nc-morning-triage` is designed to start cold and reviews the run branch on its
   merits; nothing in your context is load-bearing because HANDOFF.md, DECISIONS-NEEDED.md and the
   conflict log carry it all. Say ⛔ instead only if something real is still in flight in this session.
3. The paste-ready next command: `/nc-morning-triage`

Writing HANDOFF.md does NOT discharge this. The file and the closing message are different surfaces
read at different moments, and the operator reading the last thing on screen at 7am is the one this
is for.
