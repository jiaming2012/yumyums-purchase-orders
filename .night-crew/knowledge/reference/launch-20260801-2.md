Tonight's autonomous run. Execute the signed slate at
`.night-crew/knowledge/reference/slate-20260801-2.md`.

Setup: you are on branch dev.

Before any git write, CHECK FOR UNMERGED RUN BRANCHES. Run this EXACTLY as written — it
contains no placeholder to substitute, and it asks about every date, because an unfinished
night from any date is a night's work sitting unmerged:

    git branch --no-merged dev --format='%(refname:short)' | grep -E '^overnight-[0-9]{8}(-[0-9]+)?$'

STATE THE RESULT OUT LOUD BEFORE PROCEEDING — either "no unmerged run branches" or the list.
You may only proceed after saying which one you saw. An empty result is the "go" signal, so a
command that failed or ran wrong looks exactly like a clean repo unless you have named what
you found.

- No unmerged run branch → say so, then cut this run's branch. Compute its name from the BARE
  DATE, since you are looking for every branch of that date:

      git branch --format='%(refname:short)' | grep -E '^overnight-20260801(-[0-9]+)?$' | sort -V

  No match → `overnight-20260801`. Otherwise the next free suffix (-2, -3, …). A MERGED
  branch's name is still taken, so a date whose run merged this morning yields -2 — cut it and
  proceed without asking; nothing is at risk. **At planning time this returned exactly
  `overnight-20260801` (merged to `dev` this morning), so the expected branch is
  `overnight-20260801-2`. Re-run it anyway — do not trust this sentence over the command.**
- ANY unmerged run branch exists → 🛑 STOP. A previous night's work is sitting on it. Report
  what is there (`git log --oneline dev..<branch>`, and whether that run's
  `.night-crew/runs/<rundir>/DECISIONS-NEEDED.md` carries an open section for it), then ASK THE
  OPERATOR to choose: RESUME that branch, or RECUT at the next free suffix. Do NOT pick for
  them — resuming mixes two nights on one branch, recutting walks away from a night's work, and
  you cannot tell from here which is right. This is NOT covered by the batch sign-off: the yes
  is scoped to the signed specs, and what to do with an unfinished night is not one of them.
  Whatever they answer, the existing branch is left untouched — never delete, reset, force, or
  rename it.

🛑 **`/nc-run` §1's "has this slate already run?" guard is INERT in this repo — B-33.**
`run-evidence check` cannot see any hq run's closing evidence (two independent path mismatches;
it looks for `.night-crew/runs/<runid>/` and a root `reference/`, and this repo has neither).
It will report `no-run-evidence` for a night that fully landed. **The branch check above is
your only real backstop. Do not read a `no-run-evidence` verdict as "this slate is fresh."**

If the run id gains a suffix, EVERY run-scoped name takes it with them: the run directory
`.night-crew/runs/2026-08-01-2-autonomous/`, the conflict log
`.night-crew/knowledge/reference/conflicts-20260801-2.md`, and the closeout's references to the
run. The slate keeps the name it was signed under.

Do all work on the run branch. Never push, never touch main. Hard prohibitions in CLAUDE.md
apply. **Nothing deploys tonight** — `task prod:deploy` is not a run action under any outcome,
and the two-repo sales-processor agreement is unmet regardless of what lands.

Authority: batch sign-off was given by the operator on 2026-07-31 — the slate document is the
record. Do NOT pause for per-change sign-off. Every operator fork is already resolved in
`.night-crew/knowledge/ledger.md` (T-29 decisions 104–110; T-30 decisions 111–113); the run
never decides, it executes.

## Rules of engagement

- **Standing rules and verification gates G1–G6: read them from the slate's own §"HQ's
  verification ladder", NOT by inheritance.** 🛑 `reference/overnight-run-plan-20260707.md` DOES
  NOT EXIST IN THIS REPO — it lives only in the night-crew clone and its ladder is night-crew's
  own. Last night's run had to reconstruct HQ's real ladder from a prior handoff. This is
  **B-26**; the slate writes the ladder out in full so nobody reconstructs it a third time.
  Paste that table into every implementer and every G6 prompt.

- **Dispatch model: you are the control loop, not the implementer.** For each card: create a
  worktree off `overnight-20260801-2`, spawn a FRESH subagent scoped to exactly that card
  (paste its slate entry, footprint, and spec references into the subagent prompt; it drafts,
  implements, runs G1–G4 in its worktree, and returns a compact report), then spawn a SEPARATE
  fresh subagent for the G6 adversarial review, then merge to the run branch and archive
  yourself. Never implement a card in your own context; your context holds reports, not diffs.
  Subagents cannot spawn subagents — G6 dispatch is always yours. A failed card PARKS per the
  slate rules (worktree preserved, `DECISIONS-NEEDED.md`) — dispatch the next card. Editing a
  file outside the card's footprint is NOT a park (§15ad.65) — see the conflict rules below.

- **Order — CONCURRENT, 2 tracks, at most one in-flight card per track. You alone merge to the
  run branch, in slate order.**

  - **Track A (strictly serial, and the serialization is load-bearing — all three cards share
    the live Supabase substrate):**
    **A1 `sync-replication-scope-per-checklist`** → *merge* →
    **A2 `sync-rxdb-write-policies`** → *merge* → **[STRETCH GATE]** →
    **S1 `sync-hard-cutover`**.
    Each card is cut only AFTER its predecessor merges, so it develops against the merged state.
  - **Track B (single card, concurrent with Track A throughout):**
    **B1 `sync-cache-and-identity-hygiene`**. It will finish well before Track A. **The track
    then idles, and that is correct, not waste** — S1's gate needs B1 settled before S1 touches
    `build-sw.js`. Do not backfill it with unslated work.
  - **A1 is Wave 0 in the sense that matters:** its acceptance test is a red proving a device
    does NOT hold rows for a checklist it never opened, and A2 writes policies against the
    substrate state A1 leaves.
  - If a track stalls on rate limits, **degrade to serial — do not sit blocked.** Note that
    degrading materially lowers S1's odds; that is expected and is not a reason to skip the
    gate's margin check.

- **🛑 THE STRETCH GATE — evaluate it ONCE, at the moment A2's merge completes. All four
  conditions, no partial credit:**
  1. A1 landed clean (merged, gates green against the B-27 baseline below).
  2. A2 landed clean. **If A2 parked, S1 does not start** — it would swap the write path onto
     policies that do not exist.
  3. **S1's full HIGH-END estimate (use 5h, not the midpoint) plus ~30m closeout still fits** in
     the remaining night.
  4. B1 has landed or been abandoned cleanly.

  **If any condition fails: do not start S1. Say why in the closeout and exit clean.**
  🛑 **Do NOT re-evaluate this gate optimistically later in the night.** A card started at hour
  eight because "there might be time" is the exact failure this gate exists to prevent, and a
  half-finished write-path swap is the single worst state to hand a morning reader.

- **Per-change mechanics — conditioned on the slate's recorded workflow-preflight verdict, which
  is `openspec: absent` (exit 0, the check read every path and concluded).** Do NOT assert
  OpenSpec mechanics and do NOT create `openspec/` scaffolding in this repo. Assert only the
  universal mechanics: **red-first evidence**, **atomic commits by logical unit**, **a
  change-identifying commit trailer on every commit**, and **the roadmap card flip in the same
  change set**. Which per-change discipline this repo should adopt is an operator question
  (B-105) — never encode another detected convention's mechanics in its place.
  🛑 **This repo's trailers are `Night-Crew-Card:` / `Night-Crew-Run:`, NOT `Card:` / `Run:`.**
  That drift was the orchestrator's own mistake last night and ten commits inherited it before
  it was caught. Get it right on the seed commit.

- **Merge-intent note (REQUIRED, §15ad.65): each subagent writes
  `openspec/changes/<slug>/merge-intent.md` in its FIRST commit, before implementing.**
  🛑 **`openspec/` does not exist here and must not be created** — write it instead to
  `.night-crew/runs/2026-08-01-2-autonomous/merge-intent-<card>.md`, matching last night's
  convention (`merge-intent-c2-sync-rxdb-conflict-notice-ui.md`). Contents: shared files touched
  (each file outside its own packages, one line of why), **what must survive any merge**, and
  what is safe to drop. Empty fields say "nothing here" explicitly. Paste this requirement into
  every implementer prompt. **B1's is load-bearing tonight** — S1 edits `build-sw.js` after it,
  so B1 must state what its per-identity cache partition needs to survive.

- **Conflicts, not fences (§15ad.65):** the footprint is planning information, not a boundary —
  a card MAY edit any file, and doing so is not a breach and not a park. Collisions surface as
  ordinary git conflicts. YOU resolve them at merge by reading BOTH sides' merge-intent notes
  and resolving against intent, not text; then re-run G1+G2. PARK the later card ONLY when,
  having read both intents, you still cannot determine which behavior is correct. An
  operator-only question still PARKS. Never improvise forks.

- **Conflict log (REQUIRED, §15ad.66):** append an entry to
  `.night-crew/knowledge/reference/conflicts-20260801-2.md` for EVERY merge, clean or
  conflicted — the cards involved, files and hunks, the intents you read, the resolution you
  took, and the gate result after it. Clean merges get a one-line entry, so an empty log never
  reads as "no conflicts" when it means "the logging never ran". Commit it with the merge.
  Summarize it in the closeout; morning triage audits it.

- **Budget discipline:** prefer a clean early exit over starting a card you cannot finish
  cleanly. That rule is at its sharpest at the stretch gate above.

## Briefings every implementer and every G6 reviewer gets

- **🛑 `dev` IS NOT GREEN. `tests/inventory.spec.js:883` (B-27) fails on a clean isolated re-run
  of the merged tree.** This morning's adversarial re-execution refuted last night's closeout
  exit-0 claim; the branch was merged anyway, deliberately, with the red named (T-29 decision
  104). **Every card starts from a base whose Playwright suite exits 1.** Judge G2 against
  *that* baseline. A card claiming "green" instead of "green except B-27" is reporting
  something false — and B-27 reddening is NOT the card's.

- **🛑 B-35 — every card MUST set a unique `HQ_RLS_TEST_DB`, a unique `TEST_PORT`, and a unique
  database prefix, for the implementer leg, the G6 leg AND every fix round.**
  `TestRowVisibilityRLS` drops and recreates its FDW fixture database on entry, default name
  `hq_test_b2_fdw`, so a plain `go test ./...` destroys a database it does not own. This is a
  CROSS-TRACK hazard tonight even though the two tracks' file footprints barely overlap: B1 can
  eat A2's fixture. This is not a suggestion.

- **🛑 B-36 / T-29 decision 108 — `internal/sync` printing `ok` is NOT evidence.**
  `resolveSpikeConfig` turns any `docker compose … port` failure into `t.Skip`, so the package
  reports `ok` and exits 0 while skipping the entire RLS attack suite. **Gate evidence citing
  `internal/sync` MUST include `-run TestRowVisibilityRLS -v` output showing subtests
  executed.** Card A2 fixes this in the same change set — until it merges, the reporting rule
  binds every card.

- **Guard integrity — B-22 / B-23 / B-24.** A guard printing PASS is not evidence until its
  subject set is shown non-empty. Brief every G6 reviewer. Applies with particular force to A2,
  whose whole deliverable is a guard.

- **`go test` must run with `-p 1`.** `Taskfile.yml:108` already does it. Two agents hit the
  parallel-package failure mode independently on 07-29.

- **G4 ordering — B-37.** `build-sw.js` reads **git HEAD, not the working tree**, and exits 0
  after silently dropping assets. Regenerate the service worker **after** the merge commit,
  never mid-merge; last night that produced a 24-file manifest instead of 27. Check the file
  count against expectation, not just the exit code.

- **Armed reds, by grep handle and never by line anchor (decision 100):**
  `list page progress decrements when another device unchecks a field [LST-17]`
  (`tests/sync.spec.js`) — **STAYS ARMED**, T-29 decision 109; 🛑 the bare tag `[LST-17]`
  matches TWO tests, so the FULL TITLE is mandatory.
  `a queued submission still lends its idempotency_key at 7:30pm CT [A1-TZ-02]` — B-30.
  `submitted checklist survives builder edit with assignment change [LC-02]` — B-32.
  **Non-reproduction retires none of them.**

- **🔴 `HQ_SYNC_REST_URL` RE-ARMS tonight.** Disarmed at triage this morning on evidence (T-29
  decision 107) with the re-arm condition *"whenever a card touches the sync proxy or
  replication path"* — A1, A2 and S1 all do. **No card may set it in any deploy.** It disarms
  again only at triage, on evidence, never by the run asserting it.

- **🛑 S1 ONLY — "delete `backend/internal/sync`" CANNOT be taken literally.**
  `sync-proxy-endpoint` lives in that package and the `/sync/*` proxy is load-bearing. Establish
  what must survive before deleting anything. Also: S1's decision-113 work uses **C2's existing
  conflict sheet** — introducing a NEW UI component would require mockup sign-off you cannot
  obtain, which parks the card. And **EXTEND** the pinned test
  `_deleted participates in the merge — an UNCONTESTED local delete survives`; do not replace
  it. It asserts the winner, the winner is unchanged, and the new assertion is that the
  annihilated edit is reported.

## Closeout (ALWAYS, leave ~30 min)

Update `.night-crew/runs/2026-08-01-2-autonomous/HANDOFF.md` (per-card outcomes table, gate
evidence on the final tree, next actions) and `DECISIONS-NEEDED.md` (anything parked), commit as
docs. The morning reader is the operator — write for them.

**If S1's gate did not open, say so explicitly and give the condition that failed with its
number.** "We ran out of time" is not a closeout line; "condition 3 failed — 2h15m remained
against a 5h high-end estimate plus closeout" is.

🛑 **Every "What is NOT done" bullet you write must name its destination** — a roadmap card, a
backlog entry, or an explicit "no action, because…". **B-38 is exactly the failure of not doing
this:** last night a card wrote *"a follow-up card owns this"* into shipped SQL, repeated it in
DECISIONS-NEEDED and in HANDOFF's "What is NOT done", and it never became a card — which left
`sync-hard-cutover` blocked on nothing and told the operator the milestone was two cards short
when it was four. Do not reproduce that.

Also: if any card writes "a follow-up card", "a separate card", or "until a later card" into
source or into its report, that is a dependency that does not exist yet. Name it in the closeout
as a card to author.

## Your FINAL CHAT MESSAGE is a hand-off

CLAUDE.md §"Context checkpoints at hand-offs" binds it. Do not end on a results summary. End
with, in this order:

1. **Next steps, led by morning triage** — what is waiting, what is parked, what the operator
   has to decide. If S1 landed, lead with the fact that **the milestone's cards are closed but
   nothing is deployed**, and that the attended two-device check is no longer deferrable.
2. **The context-checkpoint verdict with its reason.** For a finished run it is
   🔄 **Clear now (recommended)** — `/nc-morning-triage` is designed to start cold and reviews
   the run branch on its merits; nothing in your context is load-bearing because HANDOFF.md,
   DECISIONS-NEEDED.md and the conflict log carry it all. Say ⛔ instead only if something real
   is still in flight in this session.
3. **The paste-ready next command:** `/nc-morning-triage`

Writing HANDOFF.md does NOT discharge this. The file and the closing message are different
surfaces read at different moments, and the operator reading the last thing on screen at 7am is
the one this is for.
