Tonight's autonomous run. Execute the signed slate at
`.night-crew/knowledge/reference/slate-20260801.md`.

Setup: you are on branch dev.

Before any git write, CHECK FOR UNMERGED RUN BRANCHES. Run this EXACTLY as written — it
contains no placeholder to substitute, and it asks about every date, because an unfinished
night from any date is a night's work sitting unmerged:

    git branch --no-merged dev --format='%(refname:short)' | grep -E '^overnight-[0-9]{8}(-[0-9]+)?$'

STATE THE RESULT OUT LOUD BEFORE PROCEEDING — either "no unmerged run branches" or the list.
You may only proceed after saying which one you saw. An empty result is the "go" signal, so a
command that failed or ran wrong looks exactly like a clean repo unless you have named what
you found.

- No unmerged run branch → say so, then cut this run's branch. Compute its name from the BARE
  DATE, since you are looking for every branch of that date:

      git branch --format='%(refname:short)' | grep -E '^overnight-20260801(-[0-9]+)?$' | sort -V

  No match → `overnight-20260801`. Otherwise the next free suffix (-2, -3, …). A MERGED
  branch's name is still taken, so a date whose run merged this morning yields -2 — cut it and
  proceed without asking; nothing is at risk.
- ANY unmerged run branch exists → 🛑 STOP. A previous night's work is sitting on it. Report
  what is there (`git log --oneline dev..<branch>`, and whether that run's
  `.night-crew/runs/<rundir>/DECISIONS-NEEDED.md` carries an open section for it), then ASK THE
  OPERATOR to choose: RESUME that branch, or RECUT at the next free suffix. Do NOT pick for
  them — resuming mixes two nights on one branch, recutting walks away from a night's work, and
  you cannot tell from here which is right. This is NOT covered by the batch sign-off: the yes
  is scoped to the signed specs, and what to do with an unfinished night is not one of them.
  Whatever they answer, the existing branch is left untouched — never delete, reset, force, or
  rename it.

If the run id gains a suffix, EVERY run-scoped name takes it with them: the run directory
`.night-crew/runs/2026-08-01-<suffix>-autonomous/`, the conflict log
`.night-crew/knowledge/reference/conflicts-<runid>.md`, and the closeout's references to the
run. The slate keeps the name it was signed under.

Do all work on the run branch. Never push, never touch main. Hard prohibitions in CLAUDE.md
apply.

Authority: batch sign-off was given by the operator on 2026-07-31 — the slate document is the
record. Do NOT pause for per-change sign-off. Every operator fork is already resolved in
`.night-crew/knowledge/ledger.md` (T-28, decisions 92–103); the run never decides, it executes.

## Rules of engagement

- Standing rules and verification gates G1–G6 are inherited unchanged from
  `reference/overnight-run-plan-20260707.md`.

- **Dispatch model: you are the control loop, not the implementer.** For each card: create a
  worktree off `overnight-20260801`, spawn a FRESH subagent scoped to exactly that card (paste
  its slate entry, footprint, and spec references into the subagent prompt; it drafts,
  implements, runs G1–G4 in its worktree, and returns a compact report), then spawn a SEPARATE
  fresh subagent for the G6 adversarial review, then merge to the run branch and archive
  yourself. Never implement a card in your own context; your context holds reports, not diffs.
  Subagents cannot spawn subagents — G6 dispatch is always yours. A failed card PARKS per the
  slate rules (worktree preserved, `DECISIONS-NEEDED.md`) — dispatch the next card. Editing a
  file outside the card's footprint is NOT a park (§15ad.65) — see the conflict rules below.

- **Order: there is NO Wave 0 in this slate** — no card here shrinks another's footprint, so
  nothing has to land first globally. Dispatch **Track A, Track B and Track C concurrently**, at
  most one in-flight card per track, in slate order within each track:
    - **Track A** — `app-timezone-unify-new-york` (resume `card/a1-app-timezone-unify-new-york`
      @ `8da3ded`; branch and worktree are preserved and intact)
    - **Track B** — `sync-rxdb-row-visibility-rls` (resume)
    - **Track C** — `sync-rxdb-replication-and-conflict-handler`, then STRICTLY AFTER IT MERGES,
      `sync-rxdb-conflict-notice-ui`. C2 must not start before C1 has merged; its whole design
      assumes C1's conflict handler behaviour.

  **You alone merge to the run branch.** Landing order on the shared `workflows.html` is
  **A1 → C1 → C2**; if A1 is not ready when C1 is, merge C1 and let A1 rebase — the regions are
  disjoint and the order is a preference, not a correctness requirement. If a track stalls on
  rate limits, degrade to serial — do not sit blocked.

- **Per-change mechanics.** The slate records this repo's workflow-preflight verdict as
  **`openspec: absent`**. Therefore: do **NOT** assert OpenSpec mechanics and do **NOT** create
  `openspec/` scaffolding in this repo. Assert only the universal mechanics:
    - red-first evidence (reproduce a defect before fixing it; a failing test before the code
      that passes it)
    - atomic commits by logical unit
    - a change-identifying commit trailer on every commit
    - the roadmap/status flip for the card in the same change set

  Which per-change discipline this repo should adopt is an operator question (B-105) — never
  encode another detected convention's mechanics in its place.

- **Merge-intent note (REQUIRED, §15ad.65).** HQ has no `openspec/` tree, so each subagent
  writes `.night-crew/runs/2026-08-01-autonomous/merge-intent-<card>.md` in its FIRST commit,
  before implementing — shared files touched (each file outside its own packages, one line of
  why), what must survive any merge, and what is safe to drop. Empty fields say "nothing here"
  explicitly. **Paste this requirement into every implementer prompt.**

- **Conflicts, not fences (§15ad.65).** The footprint is planning information, not a boundary —
  a card MAY edit any file, and doing so is not a breach and not a park. Collisions surface as
  ordinary git conflicts. YOU resolve them at merge by reading BOTH sides' merge-intent note and
  resolving against intent, not text; then re-run G1+G2. PARK the later card ONLY when, having
  read both intents, you still cannot determine which behavior is correct. An unplanned
  lifecycle row or any operator-only question still PARKS. Never improvise forks.

  Three collisions are expected and pre-resolved by the slate — do not treat them as findings:
    - **`sw.js`** is GENERATED. Never merge the artifact. Take either side, then immediately
      regenerate with `task sw`, and re-run G4 (idempotence + version parity) after any merge
      touching a precached file.
    - **`version.go`** will conflict on nearly every merge. Resolve **per-constant**, not
      per-file — precedent `79fa7cd` (07-29).
    - **Migration numbers are ASSIGNED IN THE SLATE**: A1 = `0072`, B2 = `0073` (`0071` is the
      latest on `dev`, verified at planning). A card that finds its number taken **stops and
      reports** — it does not renumber unilaterally.

- **Conflict log (REQUIRED, §15ad.66).** Append an entry to
  `.night-crew/knowledge/reference/conflicts-20260801.md` for EVERY merge, clean or conflicted —
  the cards involved, files and hunks, the intents you read, the resolution you took, and the
  gate result after it. Clean merges get a one-line entry, so an empty log never reads as "no
  conflicts" when it means "the logging never ran." Commit it with the merge. Summarize it in
  the closeout; morning triage audits it.

## Briefings every implementer and every G6 reviewer gets

- 🔴 **Guard integrity — B-22/B-23/B-24, armed 2026-07-29.** Three guards pass on empty or
  mis-scoped populations, **inside the checks last run added to cure exactly that**. **A guard
  printing PASS is not evidence until its subject set is shown non-empty.** Do not accept
  `verify-test-harness.sh` Check B (one aggregate `go test` — six of seven packages can report
  `ok` on a dropped database), Check A2 (spec-file floor of 20, but 20 static + 1 generated = 21,
  so it has slack), or `shoot.mjs` measurement 6 (walks `.plate` with no population floor) at
  face value. This repo's characteristic bug is **a check whose subject set can go empty** — it
  is not yet cured.

- **Armed reds — by grep handle, NEVER by line anchor (decision 100).**
    - LIVE: `list page progress decrements when another device unchecks a field [LST-17]` in
      `tests/sync.spec.js`. Use the **full title** — the bare tag `[LST-17]` matches **two**
      tests, so a tag-only handle is ambiguous.
    - **DEAD, DO NOT ARM:** `tests/sync.spec.js:1198`. Verified at planning — that line is a bare
      `}`. It named no test for five nights and every "it passed" against it was unfalsifiable.

- **`go test` must run with `-p 1`.** `Taskfile.yml:108` already does it. Two agents hit the
  parallel-package failure mode independently on 07-29.

- **`inventory.spec.js:883` is a KNOWN pre-existing red**, proven by reproduction and attributed
  to cross-spec pollution (B-27). `playwright.config.js` defaults to `retries: 1`, which is why
  the baseline reads green; **cards running at `--retries=0` will see it.** It is not yours to
  fix and it is not evidence against your card.

- **🛑 `HQ_SYNC_REST_URL` MUST NOT BE SET** in any deploy, config, test fixture or compose file.
  The proxy forwards every method with no row filtering. Track B is the card that guards it, and
  **the flag disarms at morning triage on evidence, not by any card asserting it.**

- **🛑 NOTHING DEPLOYS TONIGHT.** No `task prod:deploy`, under any circumstances. Track A's work
  additionally cannot ship until sales-processor's matching change is ready — it is half of a
  two-repo agreement.

- **Track A's first leg is ORIENTATION, not code:** read both
  `*-SALES-PROCESSOR-CONTRACT.md` documents before editing anything. This is the card's PARK
  trigger. On 07-29 this card reported *"nothing parked — no site turned out to be deliberately
  Chicago"*, which was false against the repo's own committed artifacts, and it cost 1h18m of
  building before rejection. A card whose PARK trigger names an external contract reads the
  contract first.

- **Track B: do NOT redo the port.** The full transposition of `ResolveEntityAccess` into
  `hq_can_see_template()` is already written out in
  `.night-crew/runs/2026-07-29-2-autonomous/park-b2-sync-rxdb-row-visibility-rls.md` and G6
  verified it as character-for-character faithful with both inherited properties preserved.
  Changing `assignment_role` filtering or the unconditional admin arm is a SEPARATE card.

- **Track C1: re-adding the precache glob WITHOUT adding `vendor/` to `backend/Dockerfile`
  breaks production for every returning client.** `backend/Dockerfile` COPY lines are 21, 25, 26,
  27, 30 — `vendor/` is never copied into the image, verified at source. Add `vendor/` to the
  Dockerfile in the same change set, or do not re-add the glob. There is no third option and
  this is not a park trigger.

- **Track C2: the card body in `roadmap.md` contains a STALE
  `THIS CARD IS STILL ATTENDED-BLOCKED` block.** It is superseded by the card's own header and by
  **decision 98** — the card IS signed and slate-ready. The card text itself warns that an
  unattended reader going top-down was previously able to take struck sentences as binding.
  **Read the header and the slate, not the struck block.** Build obligations A-1, A-2, A-3 and
  decisions 96/97 are all settled and none is reopenable by the run; A-3 requires REDRAWING the
  `edge-removed`, `openq-count-a` and `openq-count-b` plates and **noting the deviation from the
  approved mockup in SUMMARY.md**.

- **Budget discipline:** prefer a clean early exit over starting a card you cannot finish
  cleanly. Track C's pair is the critical path — if C1 runs long, C2 is the card to leave
  unstarted rather than to rush through its verifier gate.

## Closeout (ALWAYS, leave ~30 min)

Update `.night-crew/runs/2026-08-01-autonomous/HANDOFF.md` (per-card outcomes table, gate
evidence on the final tree, next actions) and `DECISIONS-NEEDED.md` (anything parked), record
per-card wall-clock in `timings.log`, and commit as docs. The morning reader is the operator —
write for them.

Your FINAL CHAT MESSAGE is a hand-off, so CLAUDE.md §"Context checkpoints at hand-offs" binds
it. Do not end on a results summary. End with, in this order:

1. Next steps, led by morning triage — what is waiting, what is parked, what the operator has to
   decide. Include the two attended follow-ups this run creates: the **two-device convergence
   check re-arms** (any frontend file moved / `sw.js` regenerated), and the **sales-processor
   coordination** Track A's work depends on.
2. The context-checkpoint verdict with its reason. For a finished run it is 🔄 Clear now
   (recommended) — `/nc-morning-triage` is designed to start cold and reviews the run branch on
   its merits; nothing in your context is load-bearing because HANDOFF.md, DECISIONS-NEEDED.md,
   and the conflict log carry it all. Say ⛔ instead only if something real is still in flight in
   this session.
3. The paste-ready next command: `/nc-morning-triage`

Writing HANDOFF.md does NOT discharge this. The file and the closing message are different
surfaces read at different moments, and the operator reading the last thing on screen at 7am is
the one this is for.
