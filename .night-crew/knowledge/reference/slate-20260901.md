# Slate — run `20260901` (night of 2026-08-31 → morning 2026-09-01)

**Run branch:** `overnight-20260901` — the bare date `20260901` is free (no branch of any
suffix exists; run `20260810` merged 2026-08-09, cycle closed 2026-08-28). No collision, no
suffix.
**Sign-off:** GRANTED 2026-08-31 (operator, this session) — "Yes — sign it" at the §4 batch
sign-off, with the milestone remainder, attention budget, and spike-skips sections in view.
11 cards. **Dispatch: CONCURRENT, 3 tracks** — chosen by the operator at the §5 question with
both projections side by side (concurrent: all 11, ~4¾–7¼h critical path, ~3× serial burn,
first concurrent night in this repo; serial: ~6–7 cards, ~8–10h, rest stretch). See
§Sign-off record.
**Forks resolved:** none newly required. Every slated card's operator-level questions were
settled at the 2026-08-28 roadmap round (ledger T-46): `app-slug-association`'s
association-home is an engineer/PM-level call the roadmap text itself pre-authorizes ("decide
and record in the merge-intent under standing authority; park only if it turns product-fork");
`sync-dev-one-command`'s credential boundary was ruled satisfied-in-shape at the round (B-171's
park lifted — dev targets carry the 4 `HQ_SYNC_*` vars, prod compose does not, capability-not-
guards per the standing preference). DECISIONS-NEEDED from run `20260810`: no parks, no
operator forks. No open fork enters this slate.
**Workflow-preflight verdict (ran clean — `night-crew workflow preflight --repo .`, exit 0):**
`openspec: absent` (no `openspec/` tree). `gsd: detected` (`.planning/` present — the
deliberate telltale; real state is `.night-crew/knowledge/`). **The launch prompt asserts
UNIVERSAL mechanics only** — red-first evidence, atomic commits, the `Night-Crew-Run: 20260901`
trailer, the roadmap/status flip — and creates **no** OpenSpec scaffolding. Which per-change
discipline this repo adopts is an operator question (B-105), not this run's. This is NOT the
`undetermined` STOP case: the verb read every path and returned `absent` at exit 0, matching
every prior run in this repo.
**Spike gate:** the `night-crew spikes gate` verb cannot drive on this product-mode target —
it reads the dogfood-only `usm/roadmap.txt` story map (exit 1, "no story map to gate against"),
the same wall slate-20260810 recorded. The substantive state: **this cycle's roadmap round
(T-46) authored no spike-gated goals** — every slated card is a measured defect or a recorded
fix shape (B-146's fix shape is written in the backlog entry; B-139's lead names the log line;
B-28/B-175 are measured defects), not novel-integration work. There are no goals to gate, no
spikes run, and therefore **no spike skips to carry** — stated so an absent section and a
considered "nothing" don't look alike.
**Agreement check (§3b — `night-crew okr compare-slate … --expect-cards 11`, exit 0):** all 11
declared cards read by the parser; no contradiction of the three kinds it reads (opposed
disposition, delivery denied, a slate-excluded card a target claims). Coverage lines — "14
targets name no card", "11 slate cards no target names" — are **structural to a target-repo
OKR page** (each KR names its measurement *artifact*, not a card slug; no slug overlap by
construction — same shape slate-20260810 recorded). Read substantively, the alignment is
real: E-KR1↔Card 4, E-KR2↔Card 5, E-KR3↔Card 7, E-KR4↔Card 8, P-KR2↔Card 3, P-KR3↔Card 10,
Q-KR1↔Card 2 (mechanism half), B-139↔Card 9. The check's live value here was the
`--expect-cards 11` parse assertion (11 declared, 11 read) and the three contradiction
detectors (none fired).

## What this night is

**The build-everything-the-deploy-will-carry night.** The cycle's build order is
notice → ship → fix: Activity 1 (the counterparty notice) gates Activity 2 (the deploy), and
the deploy gates the fixes *reaching prod* — but nothing gates the fixes being *built on dev*.
Tonight builds every unattended-buildable card in Activities 1, 3 and 4 so the morning
becomes: review + **send the notice** (your act) → merge dev→main → `task prod:deploy` → the
deploy carries two months of fixes PLUS tonight's, and the kill-drill mechanism is already in
the tree waiting to be drilled.

Scope chosen by the operator at this session's scope question: **notice prep + prod fixes +
sync hardening** (Activity 5 explicitly declined for tonight; `media-recovery` excluded as
attended-assist).

**One §1.5 split:** roadmap card `pipeline-fail-loud` bundles two mechanisms in two file
families — (a) the Toast sync loop's fail-loud path (receipt worker + `/api/v1/health` + Cliq)
and (b) `/period-summary` request/verdict visibility (inventory endpoints, B-139's lead). It
fans out to **`toast-sync-fail-loud`** (Track A) and **`period-summary-visibility`** (Track C).
The roadmap card flips DONE only when **both** land; each split card's commits identify their
own slug.

## Shared surfaces and serialization

| Surface | Cards | Rule |
|---|---|---|
| `backend/internal/receipt/**` | all 3 Track A cards | same track, serial; slate order = landing order |
| `backend/internal/sync/**` | `sync-doc-honesty`, `app-slug-association` | same track (B), serial |
| `docs/contracts/*` | `period-summary-visibility` (if its verdict surface warrants a doc line), `counterparty-notice-prep` | same track (C), serial — the notice card runs AFTER the visibility card so the contract docs describe what prod will actually serve post-deploy |
| root `Taskfile.yml` | `sync-dev-one-command` (dev targets), `toast-ingest-resurrection` (prod-infra targets, if touched) | cross-track, DIFFERENT sections — bounded; resolve at merge against both merge-intents (§15ad.65), log every merge in the conflict log |
| `sw.js` + precache manifest | `sync-doc-honesty` (retires a comment in precached `workflows.html`), `deploy-hygiene-honesty` (changes the generator itself) | cross-track. Rule: every card touching a precached file runs `task sw` + commits `sw.js` in its own change set; **the orchestrator re-runs `task sw` on the merged tree after the LAST of these two lands** and commits the regeneration with the merge. Precache count is 31 — if it moves without a deliberate add/remove, that is B-37 coming back |
| `night-crew.toml` | `client-guard-coverage` only (B-154 rider) | first in Track B — it shrinks every later sync card's gate leg |
| `tests/*.spec.js` | most cards add distinct spec files | no shared file expected; collisions resolve at merge via intents |

## Model routing (§3c — class-derived, no per-card override, process/C-7)

A card needing a different tier is **reclassified here**, not exempted. Classes use the
routing vocabulary `routing check` reads (small-additive · new-mechanism · followup-batch ·
closeout). All classes route to the **session model** tonight — every card is either a
prod-defect fix or contract-facing work, and no class is downgrade-safe on this slate.
(Sizing classes — build/harness/docs per `card-actuals.md` — live in the estimates table's
Basis column; they price, this section routes.)

| Class | Model | Cards |
|---|---|---|
| small-additive | session | receipt-worker-correctness, client-guard-coverage, cdc-single-fire, sync-dev-one-command, period-summary-visibility, counterparty-notice-prep, deploy-hygiene-honesty |
| new-mechanism | session | toast-sync-fail-loud, toast-ingest-resurrection, app-slug-association |
| followup-batch | session | sync-doc-honesty |
| G6 adversarial review | session | all cards, always (standing row — not a per-card class route) |

<!-- routing:slate -->
small-additive session
new-mechanism session
followup-batch session
receipt-worker-correctness small-additive
toast-sync-fail-loud new-mechanism
toast-ingest-resurrection new-mechanism
client-guard-coverage small-additive
cdc-single-fire small-additive
sync-dev-one-command small-additive
sync-doc-honesty followup-batch
app-slug-association new-mechanism
period-summary-visibility small-additive
counterparty-notice-prep small-additive
deploy-hygiene-honesty small-additive
<!-- /routing:slate -->

## Estimates and critical path (wall-clock ONLY)

🛑 **Sized against the calendar and nothing else.** No `--night` was given, so there is no cut
line — these estimates exist for the attention budget, the dispatch projection, and the
stretch gate. The account's token quota — tier, ceiling, current consumption, reset window —
was **NOT measured and cannot be measured from here.** The run may exhaust quota before the
clock or the clock before quota; neither validates the other. Spend is governed by the run's
own stop conditions; a rate-limit stall degrades gracefully (finish the in-flight card,
closeout; under concurrent dispatch, degrade to serial — never sit blocked).

End-to-end = implement + G6 + any fix round + merge. Basis: `reference/card-actuals.md` —
build-class median **~82m** (run `20260808-2`, N=4: 66/66/98/113, bands calibrated);
harness/QA-class **~21–25m** (run `20260809`); test-hardening/convergence class **~73m**
(`editprop-convergence-cell-hardening`); docs-class PRD actuals **~8–12m** (run `20260710`).

| # | Card | Track | Routing class | Estimate | Basis (sizing class per card-actuals) |
|---|---|---|---|---|---|
| 1 | `receipt-worker-correctness` | A | small-additive | **70–110m** | build median ~82m; two measured defects in one file family (B-28 tz, B-175 misindex), red-first each — upper-mid band |
| 2 | `toast-sync-fail-loud` | A | new-mechanism | **60–100m** | build median; the Cliq alert pattern already exists (drift-check, Phase 999.2) — reuse pulls down |
| 3 | `toast-ingest-resurrection` | A | new-mechanism | **90–140m — risk-flagged, use 140** | build band top: first-of-kind prod-secret shipping pattern + B2 archive gap enumeration (P-KR2); no direct card-actuals analog, wide ± |
| 4 | `client-guard-coverage` | B | small-additive | **60–90m** | convergence-hardening class ~73m actual; + B-154 toml rider authoring; pays one full ~24m suite itself (rider lands in this card) |
| 5 | `cdc-single-fire` | B | small-additive | **45–80m** | F1/F2 Go-endpoint S–M class; single defect, trigger-count red-first |
| 6 | `sync-dev-one-command` | B | small-additive | **25–45m** | QA/harness class 21–25m actuals (run `20260809`); Taskfile target + existing spike scripts |
| 7 | `sync-doc-honesty` | B | followup-batch | **45–75m** | docs-diff is small, but B-140 demands the repo-hygiene case-3 broadening (spelling→fact matching) + a precached-asset edit → `task sw` + one full ~24m suite |
| 8 | `app-slug-association` | B | new-mechanism | **80–120m — risk-flagged, use 120** | build band top: migration + every projection writer + the association-home decision recorded in merge-intent |
| 9 | `period-summary-visibility` | C | small-additive | **45–75m** | Go-endpoint S–M class; B-139's lead is one-log-line-shaped, but red-first log assertion + the `/menu-cogs` sibling considered |
| 10 | `counterparty-notice-prep` | C | small-additive | **40–70m — wide ±, first-of-kind** | docs/PRD-class 8–12m actuals pull down; auditing both contract docs against live handlers + drafting an outward-facing notice pull up; no analog |
| 11 | `deploy-hygiene-honesty` | C | small-additive | **50–85m** | build low band; generator unification (B-135) + claim/flag correction (B-17), red-first byte-diff; B-37 precache-invariant care |

**Critical path:**
- **Concurrent (3 tracks):** longest track is B ≈ 255–410m; + ~30m closeout →
  **high ≈ 7h20m, mid ≈ 5h45m**. Tracks A (≈ 220–350m) and C (≈ 135–230m) finish earlier;
  merges serialize on the orchestrator but cost minutes each.
- **Serial (one lane):** sum ≈ 610–990m + closeout → **high ≈ 17h, mid ≈ 10h45m** — the full
  slate does NOT fit a night serially; roughly the first 6–7 cards land and the tail becomes
  budget-gated stretch (stretch rule: start a stretch card only with its high-end estimate +
  30m closeout still in hand).

**Coherent partial outcomes** (concurrent or serial): every card lands independently on dev;
no card's value is hostage to another's except the two `pipeline-fail-loud` halves (the
roadmap card stays un-flipped until both land — each half banked alone is still real).

## Gate cost (Q-KR4 — required, authored at signing, never backfilled)

- **Expected full Playwright suite runtime: ~24m** (measured 21.7–23.0m, runs `20260808-2` /
  `20260809`; use 24m). 🛑 `npx bddgen` before every Playwright leg (B-165) or the suite
  silently runs short and reads clean.
- **Who pays what:**
  - **Full ~24m Playwright legs: 3 expected** — B1 `client-guard-coverage` (sync footprint
    de-confines to full suite until its OWN rider lands — it pays the last full-price sync
    leg), B4 `sync-doc-honesty` (precached-asset edit → `task sw` + full suite), C3
    `deploy-hygiene-honesty` (generator change → full suite). ≈ **72m** of suite time + bddgen.
  - **Confined/subset legs:** later sync-footprint cards ride B1's new `[e2e.seams]` row —
    that is the rider's whole point; B5's Go-side change pays Go only.
  - **Go suite legs (`go test ./... -p 1`, on `:5434`, `DB_TEST_URL` set, counts checked not
    just `ok`): 7 cards** (A1–A3, B2, B5, C1, C3) ≈ **~5m each, ~35m** total.
  - **G6 adversarial review:** priced at the repriced **15–45m band** for app-code cards
    (card-actuals: "a slate that prices G6 at 2–5m will systematically under-budget"),
    ~7–12m for docs/harness — G6 time is inside the per-card estimates above.
  - **RF n/a rows:** C2 `counterparty-notice-prep` records `n/a — no code change` explicitly
    (decision 153).
- **Armed reds EXPECTED in any full run** (flake-trail evidence, not card failures, decision
  100): `inventory.spec.js:883` (B-27), `sync.spec.js:446` ([LST-17], T-42),
  `receipt-carousel.spec.js:123` (B-162). `tests/sync.spec.js` is load-sensitive and must not
  share the box — under concurrent dispatch, Playwright legs from different tracks must not
  overlap wall-clock; the orchestrator schedules gate legs, not just merges.

## Precondition flags (carried from slate-20260810, still in force)

- `task sandbox:e2e` — no such target in hq; the template's attended-sandbox precondition is
  N/A, stated rather than silently skipped.
- 🛑 **All test coordinates are `:5434`** (`yumyums-test-pg`, role `hqtest`); `task
  test:targets` is the read-only coordinate source. Never invoke a `backend:*`/`test:*` task
  bare against a default coordinate (B-164 — that is how prod was destroyed 2026-08-06).
- 🛑 **dotenv leaks real Mercury/Anthropic keys into test runs** (E2E gotchas, standing) —
  Track A's receipt-worker tests must run with test doubles/fixtures, never live creds; a
  live ingest into the test DB is a known failure mode.
- 🛑 **No prod credential enters the run.** `toast-ingest-resurrection` builds the shipping
  *mechanism* (compose mount pattern, path convention, docs); the actual key file is placed
  by the operator, attended, on the prod box. Capability isolation, not guards — the standing
  preference.
- **D-KR2 stamp discipline:** the per-card stamp block in `reference/card-actuals.md` is
  maintained **at each leg's return** (implementer-return, G6-return, fix-return) — never
  backfilled at close. The launch prompt carries this as a run mechanic.

## Wave 0

None. No cross-cutting refactor shrinks tonight's footprints; B1's B-154 rider is a
track-local ordering rule (first in Track B), not a global gate.

## Track A — prod ingest lives (Activity 3, receipt family, serial within track)

### Card 1 · `receipt-worker-correctness` — two measured worker defects, red-first

Closes **B-28**, **B-175**. `parseEventDate` (`backend/internal/receipt/`) stamps COGS
periods from server-local time instead of the business timezone (America/Chicago) — a
period-boundary purchase lands in the wrong week; a failed download leaves the next
attachment misindexed (ext/filename read from the wrong entry). **Red-first each**: one test
pinning a boundary-timestamp period assignment, one pinning index integrity across a failed
download. Footprint: receipt/toast worker.

### Card 2 · `toast-sync-fail-loud` — the sync loop cannot die silently

Split (a) of roadmap card `pipeline-fail-loud`; closes the B-146 silent-death class. A Toast
sync that cannot open SFTP or auth **surfaces in `/api/v1/health`** (last-sync
status/timestamp field) **and fires the Cliq alert** (reuse the Phase 999.2 drift-check alert
pattern) — never log-and-continue. Red-first: a test that breaks the SFTP path and asserts
the health field + alert dispatch. **This card builds what the close bar's kill-drill
proves** — the drill itself is attended, post-deploy. Footprint: receipt/toast worker
(+ health handler).

### Card 3 · `toast-ingest-resurrection` — the key ships, the gap is enumerated

Closes **B-146** (mechanism half). Ship the SFTP key into prod the way `.env.prod` secrets
already survive the hard reset (untracked file beside it, bind-mount or COPY — B-146's
recorded fix shape); resurrect the daily sync config; **enumerate the archive gap
day-by-day** (P-KR2): every date-directory missing since 2026-07-28 listed,
recovered-vs-aged-out dispositioned by name against Toast's ~27-day retention (days before
~2026-08-05 are expected casualties — record exactly which). Dev-provable half tonight:
mechanism + tests + a written attended-steps note (key placement, prod verification). **The
prod proof — "prod's next scheduled sync writes a current date-directory" — lands attended,
post-deploy.** Footprint: prod infra + receipt/toast worker. **Riskiest in track — goes
last. PARK note:** if the recorded fix shape proves impossible (the key cannot ride an
untracked file and would have to enter the image or the repo), where a prod credential lives
is the operator's — park.

## Track B — sync hardened past its demo (Activity 4, serial within track)

### Card 4 · `client-guard-coverage` — the unguarded guards get tests; the B-154 rider lands

Closes **B-149**, **B-10**; carries the **B-154 rider** FIRST IN TRACK: add the
`night-crew.toml [e2e.seams]` row for `sync-rxdb/` paths, authored deliberately against the
sync footprint — until it exists every sync card de-confines to the full ~24m suite. Then:
the uid-mismatch half of the B-89 fix gets its `cachedGrantSlugs()` envelope-verification
test; the `await` on `clearApiCache()` gets a test that reds when the `await` is dropped.
**Red-first both** (E-KR1). Footprint: sync client + `night-crew.toml`.

### Card 5 · `cdc-single-fire` — exactly one CDC fire per save

Closes **B-157**. One `/saveResponse` call fires any CDC trigger on `submission_responses`
twice (`workflow/repository.go`). Red-first: a trigger-count assertion that counts 2 on the
pre-change tree, 1 after (E-KR2). Footprint: backend workflow.

### Card 6 · `sync-dev-one-command` — `task sync:dev` for daily use

Closes **B-171**. One command (`task sync:dev`) brings up data plane + dev server together
for daily use. The credential boundary is already in shape (dev targets carry `HQ_SYNC_*`;
prod compose does not) — this card wires convenience, not policy. Footprint: root Taskfile +
spike scripts. Harden any script it touches against B-170's bare-`npx playwright`
fall-through while in the file.

### Card 7 · `sync-doc-honesty` — every comment in the sync surface tells the truth

Closes **B-140**, **B-18**, **B-167**. Retire all four stale `sync-rxdb-row-visibility-rls`
activation-gate comments (one sits in precached `workflows.html:3560` → `task sw` + full
suite); correct the two `sync/proxy.go` comment fictions; discharge or convert B-167's
carried G6 observations. **In the same card, broaden `tests/repo-hygiene.spec.js` case 3
from slug-matching to fact-matching** (scan the whole tree; assert on the claim — a comment
naming any DONE roadmap card as a future precondition — B-140's lead). RF for the comment
diff: `n/a — no code change` except the spec broadening, which is red-first (re-insert a
paraphrased gate, watch it caught). Footprint: backend sync + sync client comments +
`workflows.html` + `sw.js` (E-KR3 — site list named in the merge-intent so "0 remaining" is
checked, not hoped).

### Card 8 · `app-slug-association` — `app_slug` from a stored association, 0 constants

Closes **B-160** (E-KR4). HQ stores no template→app association; `app_slug` is a constant in
every sync projection writer. Decide where the association lives (engineer/PM-level,
**pre-authorized** — decide and record in the merge-intent under standing authority),
migrate, populate every writer from it, 0 constants remaining. Footprint: backend sync +
migrations. **Riskiest in track — goes last. PARK note:** park only if the home turns
product-fork — it would require a new operator-facing concept or an app-behavior choice the
operator would see. The home decision itself is the run's to make.

## Track C — visibility, the notice, honest tooling (serial within track)

### Card 9 · `period-summary-visibility` — a blocked payroll week is greppable

Split (b) of roadmap card `pipeline-fail-loud`; closes **B-139**. One `slog.Info` at the end
of the `/period-summary` handler — period from/to, `ready`, `len(pending_review_ids)`,
`len(unlinked_line_item_ids)` — so a blocked payroll week is greppable in prod logs
regardless of what any consumer writes to disk; same for `/menu-cogs` (its first-ever caller
should be visible). Red-first: a log-assertion test. Footprint: inventory endpoints.

### Card 10 · `counterparty-notice-prep` — the combined notice drafted, contract docs true

The preparable half of roadmap card `counterparty-combined-notice` (closes B-29/B-137 only
when the operator SENDS — the send is not this card's). Draft the combined notice to the
sales-processor maintainer covering: the 2026-06-06 `/period-summary` completeness-gate
change (B-29), migration `0072`'s changeover date (D-KR4's carried dependency), and every
statement in `docs/contracts/inventory-period-summary.md` + `inventory-menu-cogs.md` that is
no longer true — **audited against the live handlers on tonight's tree**, i.e. what prod
will serve after the deploy, including Card 9's new visibility if contract-relevant. Update
both contract docs in the same change; record the B-137 counterparty-process lesson line for
the ledger. Draft lands at
`.night-crew/knowledge/reference/counterparty-notice-20260901-draft.md` for the operator's
morning review + send (P-KR3: send BEFORE the deploy — ordering is the KR). RF: `n/a — no
code change`. Footprint: `docs/contracts/*` + the draft. Runs AFTER Card 9.

### Card 11 · `deploy-hygiene-honesty` — one authoritative generator, no false claims

Closes **B-135**, **B-17**. `version.json`'s two shipping generators disagree at the byte
level (prod's precache can carry a stale version artifact): make one authoritative, delete
or subordinate the other, red-first byte-diff test. `build-sw.js` justifies a load-bearing
flag with an empirically false claim: correct the claim or the flag. Precache count must
stay 31 unless a change is deliberate (B-37). Footprint: build tooling. **Riskiest in
track — goes last. PARK note:** park if making one generator authoritative would remove or
relocate a committed artifact from the release flow in a way that changes what deploys — the
release flow's shape is operator-visible; a byte-identical unification is not, and is the
run's to make.

## Explicitly NOT in this slate

- **`ship-dev-to-main`** (Activity 2) — attended by design; overnight runs never push, never
  touch main.
- **The notice SEND** — the outward-facing half of `counterparty-combined-notice`; the
  operator's own act (P-KR3).
- **`media-recovery`** (Activity 3) — attended-assist; the operator holds the originals.
  Nothing overnight can establish what survives.
- **`backlog-machine-migration`**, **`team-records-from-hand-runs`** (Activity 5) — offered
  at the scope question this session; the operator chose against for tonight.
- **The kill-drill and toast prod-proof** — the close bar's legs 2–3; attended, post-deploy.
- **Backlog promotions:** none. The round walked all 63 CLI-visible items 3 days ago;
  nothing new blocks architecture (§15k bar).

## Milestone remainder (recorded at signing — what the operator saw)

**Milestone cards still white and NOT in this slate** (live roadmap state, all 13 cards
currently PLANNED): `ship-dev-to-main` · `media-recovery` · `backlog-machine-migration` ·
`team-records-from-hand-runs` — plus the attended halves of two slated cards (the notice
send; toast's prod proof) and the attended close-bar legs (parity check, ingest-current
observation, kill-drill). **Plainly: this slate does NOT close the milestone.** It leaves 4
cards + the attended acts short; the attended acts are the close bar itself and cannot be
slated by design.

**Recommended additions: nothing worth adding.** Activity 5's two cards were offered and
declined at the scope question this session; `media-recovery` cannot run without the
operator's originals; everything else remaining is attended-by-design. (Estimates for the
declined pair, for reference: `backlog-machine-migration` ~45–90m harness-class;
`team-records-from-hand-runs` ~30–60m docs-class.)

**Spike skips: none** — no spike-gated goals exist this cycle (see header).

## Execution notes

- **Concurrent (3 tracks):** dispatch Tracks A, B, C concurrently, at most one in-flight
  card per track, slate order within each track. Orchestrator alone merges to the run
  branch; Playwright gate legs from different tracks must not overlap wall-clock
  (load-sensitive `sync.spec.js`). Rate-limit stall → degrade to serial.
- **Serial (one lane):** 1 → 4 → 9 → 10 → 2 → 5 → 11 → 6 → 3 → 7 → 8 (slugs in the
  estimates table) — defects and the notice first, risk-flagged cards last; cards past ~9h
  become stretch under the stretch-gate arithmetic (high-end estimate + 30m closeout in
  hand, run `20260808-2` precedent).
- **Two-session split (if the night is cut):** session 1 = Cards 1, 2, 4, 5, 9, 10 (the
  defects + the notice prep — the deploy-critical set); session 2 = Cards 3, 6, 7, 8, 11.
- **D-KR2:** stamp block per card in `reference/card-actuals.md` at each leg's return.
- Run mechanics inherited by reference from `reference/gate-ladder.md` — this repo's ladder
  (standing rules, gates G1–G6) — not restated here. G4 discipline greps write
  `N/A-VACUOUS`, never `clean`/`PASS` (B-14). No idle-waiting on background jobs (HANDOFF
  `20260808-2` process note). Conflict log `reference/conflicts-20260901.md` REQUIRED — one
  entry per merge, clean or conflicted.

## Dispatch

**CONCURRENT — 3 tracks, at most one in-flight card per track, slate order within each
track.** Operator-chosen at the §5 question (2026-08-31, this session) with both modes'
projections and card lists on screen. The §5 criteria as presented: the orchestrator +
subagent-per-card model has 8 clean prior nights (HANDOFF evidence, runs `20260804`–`20260810`)
— though tracks have never run concurrently in THIS repo, stated plainly; cross-track overlap
is bounded (root `Taskfile.yml` different sections; `sw.js` regeneration rule); every
implementer writes a merge-intent with an explicit "what must survive" field; burn rate ≈ 3×
serial was put to the operator, who chose it. **Rate-limit stall → degrade to serial** (the
dev harness has no quota-pause machinery); Playwright gate legs from different tracks must not
overlap wall-clock (load-sensitive `sync.spec.js`) — the orchestrator schedules gate legs.
Orchestrator alone merges to the run branch.

## Sign-off record

- **Scope question (this session):** operator chose "Notice prep + prod fixes + sync
  hardening" over lighter and maximal alternatives; Activity 5 explicitly declined for
  tonight.
- **Batch sign-off:** GRANTED 2026-08-31 — "Yes — sign it", with the four plain-English
  headings, parallel plan, attention budget, milestone remainder (both mandatory sections),
  spike-skips line, and wall-clock-only disclosure presented.
- **Dispatch:** CONCURRENT chosen over serial with both projections side by side (see
  §Dispatch).
- **Agreement check:** exit 0, `--expect-cards 11` satisfied, no contradictions (see header).
