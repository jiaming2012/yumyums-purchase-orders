# Slate — run `20260906` (night of 2026-09-05 → morning 2026-09-06)

**Run branch:** `overnight-20260906` — the bare date `20260906` is free (no branch of any
suffix exists; no unmerged run branch of any date — run `20260905` merged at triage T-53).
No collision, no suffix.
**Sign-off:** GRANTED 2026-09-05 (operator, this session) — "Yes — sign it" at the §4
batch sign-off, with the milestone remainder, attention budget, and spike-skips sections
in view. 1 card, Activity B.
**Slate target confirmed by the operator this sitting:** the inferred next activity is
Activity 0 by roadmap order, but both its cards are attended-by-design; the operator
confirmed slating Activity B's last card past it (the same call as the 20260905 sitting,
re-confirmed tonight). **1 card, Activity B.**
**Dispatch: SERIAL by definition** — a single card has no independent tracks, so the §5
concurrent-vs-serial question has no content tonight; nothing the operator would see
differs. Stated as the pick, not asked (the wording-check doctrine: a question where no
option differs is not put to the operator).
**Forks resolved:** none needed resolving this sitting. The card's one prior fork — D-1,
the `requires_online` unknown-policy default — was ratified by the operator at morning
triage 2026-09-05 (**decision 166**, ledger T-53): unknown→false stands as shipped, with
the rider that THIS card is REQUIRED before any real campaign is provisioned. Decision 167
named and scoped the card (flag replication + the F-2 landing path together — the two
failure surfaces arm at the same moment). No open fork touches tonight's card; the
extraction's one deliberately-open item (the server-side `status` taxonomy for an
unverified override) is carried as the card's narrow PARK note, not a pre-resolved fork.

**Workflow-preflight verdict (ran clean this sitting — `night-crew workflow preflight
--repo /Users/jamal/projects/yumyums/hq`, exit 0):** `openspec: absent` (no `openspec/`
tree); `gsd: detected` (`.planning/` present — the deliberate telltale; real state is
`.night-crew/knowledge/`). **The launch prompt asserts UNIVERSAL mechanics only** —
red-first evidence, atomic commits, the `Night-Crew-Run: 20260906` trailer, the
roadmap/status flip — and creates **no** OpenSpec scaffolding. NOT the `undetermined`
STOP case: the verb read every path and returned `absent` at exit 0, matching every prior
run in this repo.

**Spike gate (per-goal, §1.4):** the `night-crew spikes gate` verb cannot drive on this
product-mode target — it reads the dogfood-only `usm/roadmap.txt` story map (exit 1, "no
story map to gate against"), the wall every slate since `20260810` records. **The gate's
substance was verified this sitting from the artifacts it would read:**
- **No orphan cards:** Activity B carries exactly its 4 goal cards — the goals ARE the
  cards, 1:1 (`.night-crew/knowledge/spikes/activity-b-offline-first-replica/` holds all
  four); no card of the slated activity sits outside a goal. The three DONE cards gated
  through at slate-20260905.
- **Every spike of tonight's goal has run to exit zero:** 3/3 spikes `passed` —
  `flag-replicates-and-flips` (green first run), `refusal-arms-on-real-data` (green first
  run, no corrections), `f2-push-poison-and-guard` (red-first at the poison leg, then
  green) — spike-open sitting 2026-09-05, commit `f53e7fb` ("3 spikes green, review
  signed").
- **Corrections reviewed and signed:** `signed: operator, 2026-09-05 — covers 2
  correction(s)` in the goal ledger.
- **Extraction record written and valid:** `night-crew spikes record-check --record
  <path>` exit 0 (the direct-path form, which needs no story map), verified this sitting.
  Spike-close commit `ec300ed`.
**Spike skips: none** — every authored spike ran to exit 0; no skip was recorded. Stated
so an absent section and a considered "nothing skipped" don't look alike.

**PRD gate:** ran first this sitting, exit 0 — 7 PRDs on file. **Agreement check (§3b):**
`night-crew okr compare-slate --expect-cards 1` run against this document before
sign-off; result recorded in the sign-off presentation.

**Precondition flags:**
- Local `spike-supabase` compose substrate: **reconcile mode only, never `--fresh`** —
  the card's harness legs run against the BUILT `supabase/` migrations applied by the
  committed helpers. Never `:5433`, never a hosted project (none exists — Activity 0
  provisions it; B-419's guard rides that card).
- `export PATH="/usr/local/go/bin:$PATH"` before any Go or Playwright leg; Playwright on
  `:5434` via `task test:db:up` — standing rules, decision 155. No Go-tree changes are
  expected (the F-2 guard is client-side; the landing path is Supabase schema), so no Go
  suite leg is owed unless the footprint grows to touch `backend/`.
- `night-crew.toml` seams: the `marketing` seam now exists (added by card 6, run
  20260905) — `marketing/` paths select `tests/marketing.spec.js`, with
  `marketing/sync/`'s substrate mechanisms gated by their own standalone harnesses
  (B-345, graded on exit codes). **But this card also touches `supabase/` — an undeclared
  path — so the gate de-confines to the FULL Playwright suite** (the B-37 safe direction,
  deliberate). Price the full-suite leg (~23–28m quiet-box) into the card, not around it.
- Precache: the card is expected to extend `marketing/sync/replicas.js` +
  `pull-replication.js` + `push-replication.js` (already precached at count 43). **If any
  NEW precached file is added, the count move is deliberate and stated**, `sw.js`
  regenerated and committed in the same change set with the CLAUDE.md count line (B-37,
  B-13). If no new file, the count stays 43 and the card says so.

## Shared surfaces and serialization

One card — no cross-card serialization. The surfaces below are shared with LANDED work;
the rules are what the implementer must not regress:

| Surface | Landed owner | Rule tonight |
|---|---|---|
| `marketing/sync/pull-replication.js` (`buildPullUrl`) | card 2 (run 20260905) | the expiry bound becomes OPTIONAL, never removed — the codes/offers replicas keep their bounds; their harness legs must stay green |
| `marketing/sync/push-replication.js` | card 3 (run 20260905) | the F-2 guard lands BEFORE `redeem()`; GAP-1's two belts (burn-persist, own-device `already_used` = accepted) must survive — belt-2's block-on-unknown-winner is by design (build-fact 5), not a defect to "fix" |
| `marketing/submit-machine.js` policy source (`setCampaignPolicy`) | card 6 (run 20260905) | feed from the campaigns replica; the spike proved ZERO new undeclared (state,event) pairs — the 460-pair strictness proof must still hold after the swap |
| `supabase/migrations/` | Activity A cards (run 20260904) | any change is a NEW numbered migration file, never an edit to Activity A's; reconcile mode applies it to the substrate |
| Roadmap + knowledge docs | — | the card flips its own roadmap line; log the merge in `reference/conflicts-20260906.md` (clean or not — an empty log must mean "no merges", never "logging never ran") |

## Model routing (§3c — class-derived, no per-card override, process/C-7)

A card needing a different tier is **reclassified here**, not exempted. The one class
routes to the **session model**: this card arms the cycle's offline-safety policy (§8) on
real data and rewires the landed push path — the offline-correctness core, exactly the
class the 20260905 slate kept on the session model.

| Class | Model | Cards |
|---|---|---|
| new-mechanism | session | requires-online-replication |
| G6 adversarial review | session | all cards, always (standing row — not a per-card class route) |

<!-- routing:slate -->
new-mechanism session
requires-online-replication new-mechanism
<!-- /routing:slate -->

## Estimates and critical path (wall-clock ONLY)

🛑 **Sized against the calendar and nothing else.** No `--night` was given, so there is no
cut line — this estimate exists for the attention budget and the stretch discipline. The
account's token quota — tier, ceiling, current consumption, reset window — was **NOT
measured and cannot be measured from here.** The run may exhaust quota before the clock or
the clock before quota; neither validates the other. Spend is governed by the run's own
stop conditions; a rate-limit stall means finish the in-flight leg and close out — never
sit blocked.

End-to-end = implement + G6 + any fix round + merge. Basis: `reference/card-actuals.md`.

| # | Card | Track | Routing class | Estimate | Basis (sizing class per card-actuals) |
|---|---|---|---|---|---|
| 1 | `requires-online-replication` | solo | new-mechanism | **60–100m — risk-flagged, use 100** | new-mechanism actuals 35–90m (run 20260905); the spike-was-the-draft discount applies at full strength — the spike ran the campaigns replica ON the shipped pull module and proved the F-2 guard shape end-to-end (12-attempt/0-landing blast radius measured). Pulls up: the full-suite leg ~23–28m (supabase/ is an undeclared seam → de-confine, the 20260905 "price the gate separately" lesson), one new SQL migration touching `scan_attempts` + `campaigns`, and the branch-3 e2e flip from seam-injected to real data. Risk flag: the §9/§19 status-taxonomy re-read can park the card (narrow PARK note below) |

**Critical path (serial, the only shape):** 60–100m + ~30m closeout →
**mid ≈ 1h50m, high ≈ 2h10m.** Recent actuals ran well under these bands when the spike
was the draft (cards 3, 7 of run 20260905) — the high is insurance, not the expectation.
No stretch tier exists tonight: Activities E and F are refused by the per-goal spike gate
(no spikes authored, no waiver path — §15cf.260), so nothing sits below a cut line.

## The card

### Card 1 · `requires-online-replication` — arm the §8 refusal on real data; own the F-2 landing path

The decision-166 rider (ledger T-53), scoped by decision 167 and de-risked by three green
spikes (extraction:
`spikes/activity-b-offline-first-replica/requires-online-replication.extraction.md` —
binding). **Mechanism decided by the spike, with evidence:** a dedicated **campaigns pull
replica** on the shipped `marketing/sync/pull-replication.js` mechanism
(`replicateRxCollection` + `makePullHandler`, GAP-1 keyset checkpoint unchanged); the
codes-embed alternative is CLOSED — after a stamped flip the campaigns replica delivered
`true` on the next RESYNC while the embed still read `false` (`max(codes.updated_at)`
byte-identical either side), and a campaign downgraded while its codes sit still would
never re-deliver — arming the refusal *wrong* is worse than the honest unknown. The
policy lookup handed to `setCampaignPolicy` reads the replica instead of the shipped
`CAMPAIGN_POLICY = null` literal.

**The four owed items from the extraction, binding:**
1. **Optional expiry bound in `buildPullUrl()`** — `campaigns` has no `expires_at`; the
   shipped handler answers HTTP 400 on that table today. Optional means optional: the
   codes/offers replicas keep their bounds.
2. **`campaigns` becomes visible to replication** — publication membership
   (`supabase_realtime`) + a touch trigger, OR a fan-out of the codes channel's RESYNC
   into the campaigns replica (mechanics are the night's call); today an unstamped,
   unpublicized write is invisible to every checkpointed replica — 3s after a stamped
   write with no RESYNC the spike's replica still read the old value. New numbered
   migration, never an edit to Activity A's files.
3. **The F-2 guard, placed BEFORE `redeem()`** via the distinct landing path:
   `scan_attempts.code_id` nullable + a `token_hash` column + a check constraint; an
   unverified attempt skips `redeem()` and lands directly, carrying
   `offline_override=true, unverified_code=true`. The spike measured the shipped failure:
   `/rpc/redeem` refuses first (HTTP 400, `22P02` on `p_code uuid`) — 12 redeem attempts,
   ~12 retry cycles, **0 landing requests**, the legitimate attempt behind the poison row
   never left `pending`. *Skip-until-arbitration was run and REJECTED* — it drains the
   queue but the audit-flagged attempt never reaches the server, retroactively falsifying
   decision 166's own rationale.
4. **ONE validation run re-executing spike 03 against the shipped guard** (GAP-1) with its
   `validated:` line appended to the goal ledger's `## Comebacks` in this card's sitting;
   the harness **mints a fresh live code per leg** (codes are single-use; the push
   handler's belt-2 correctly BLOCKS on `already_used` with no codes-replica winner to
   name — build-fact 5, a design property, not a defect).

**Red-first (the spike proved both reds are real):** the branch-3 refusal e2e reds on the
pre-change tree — with `CAMPAIGN_POLICY = null` the $40 `requires_online=true` code is
overridable exactly like the $2 one, so the refusal is demonstrably unreachable today —
and greens on replica-fed policy. The F-2 leg reds on the shipped retry-poison path and
greens with the landing-path guard.

done_when (roadmap verbatim + extraction): a `requires_online=true` campaign's code,
scanned offline, shows "can't verify — try again" with **NO override even for an
entitlement holder** — the branch-3 e2e flips from seam-injected to real data; an
unknown-code override lands without poisoning the push queue; a post-flip campaign
downgrade re-delivers on the next RESYNC (the leg that killed the embed alternative).
Gate: standalone harness legs (exit codes) + `tests/marketing.spec.js` + the full
Playwright suite (undeclared `supabase/` seam → de-confine, stated above); no-new-reds vs
the dev baseline (sole standing hard red: B-421's DBL-05, base-proven).

**Residuals recorded, NOT this card:** GAP-2 — an unstamped campaign write is silently
invisible to every replica; arms when the §16 provisioning surface lands and belongs to
whichever card builds the campaign write path. GAP-3 — the embedded-offer path
(`submit-flow.js:304-309` maps `embeddedOffer` → `unknownCode`, no `campaign_id`) stays
policy-unknown by construction even after this card; it is Activity E's
`identity-code-and-qr` payload question and must not be mistaken for coverage when Q-KR1
is attested. B-423 (belt-2 cross-session double-serve) and B-429 (harness HTTP-failure
injection gap) stay backlog — hardening, not arming.

Footprint: rxdb replica (`marketing/sync/`) + supabase arbiter (`supabase/` migrations) +
the submit-flow policy seam + `tests/marketing.spec.js`. **PARK note (narrow):** a **new
terminal status** beyond the §9/§19 attempt taxonomy (the extraction leaves the
unverified-override `status` open — re-read §9/§19 before building on the spike's
`accepted`+flags shape; if the re-read demands a genuinely new status value, that is the
operator's, per card 3's precedent), or **any weakening of the `requires_online = true`
refusal** → park. Replica-vs-RESYNC-fan-out mechanics, migration shape, constraint form,
harness design are the night's.

## Explicitly NOT in this slate

- **Activity 0 (`redemption-unknowns-spike`, `external-accounts-provision`)** — attended
  by design: Toast settings/field facts and account/billing provisioning only the
  operator can do. Strictly next in roadmap order; the operator confirmed slating past it
  tonight (re-confirming the 20260905 call). The #6 field observation stays owed there.
- **Activity E (`identity-code-and-qr`, `mms-send-on-signup`)** — refused by the per-goal
  spike gate: no spikes authored for either goal. `mms-send-on-signup` is additionally
  blocked on Activity 0's live sending number.
- **Activity F (`smtp-toast-ingest`, `reconciliation-view`)** — refused by the same gate
  (no spikes authored), and its inputs (#1/#2/#3 Toast facts) sit in Activity 0.
- **Backlog promotions: none.** B-420..B-431 reviewed this sitting — hygiene, hardening,
  and gate-coverage class throughout; nothing clears the §15k architecture-blocking bar.
  The closest calls, stated: B-422 (checkpoint commit-order skew) and B-423 (belt-2
  cross-session double-serve) harden mechanisms tonight's card merely reuses — no
  rip-out risk if fixed later, so they do not qualify. `media-recovery` (B-173) stays
  open, unrelated.

## Milestone remainder (live state, this sitting)

**Milestone cards still white after tonight (assuming the card lands): 6** —
`redemption-unknowns-spike`, `external-accounts-provision` (Activity 0, attended);
`identity-code-and-qr`, `mms-send-on-signup` (Activity E); `smtp-toast-ingest`,
`reconciliation-view` (Activity F). **The slate leaves the milestone 6 cards short**,
plus the three operator-observed close-bar legs (P-KR1) that no overnight can close by
design, and the still-armed ATTENDED live-camera check from card 5 (closeout 20260905).
Derived from the live roadmap card states read this sitting, not from HANDOFF prose.

**Recommended additions: nothing worth adding.** Every remaining overnight-able card
(E, F) is refused by the per-goal spike gate — no spikes are authored for those goals,
and there is no waiver path (§15cf.260). The unlock is attended: Activity 0's facts +
accounts, then `/nc-spike-open` on E and F. Tonight's one-card narrowness is structural,
not timidity — the queue refills the evening those sittings run.

## Execution notes

- **Serial, single card:** dispatch card 1, G6 it fresh-context, merge, closeout. The
  riskiest card is trivially last.
- **Two-session split:** not applicable — one card.
- Run mechanics inherited by reference from `reference/overnight-run-plan-20260707.md`
  (standing rules, gates G1–G6) — not restated here.
