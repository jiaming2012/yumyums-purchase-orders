# Merge conflict log — `overnight-20260729-2`

> Required by DESIGN §15ad.66. **Every merge to the run branch gets an entry — clean or
> conflicted.** Clean merges get a one-line entry, so an empty log never reads as "no conflicts"
> when what it actually means is "the logging never ran."
>
> Run: `overnight-20260729-2` · Slate: `slate-20260729-2.md` (signed 2026-07-28, 5 cards)
> Dispatch: **CONCURRENT**, three tracks.

## Known collision predicted at planning

**The next migration number (`0072`).** A1 `app-timezone-unify-new-york` takes it (timezone column
defaults on `0037`/`0042`). B2 `sync-rxdb-row-visibility-rls` renumbers at merge **if** it created
one. This is an ordinary conflict — resolve at merge, **do not park**.

## Entries

_(appended in merge order; each entry records the cards involved, files and hunks, the
merge-intent notes read, the resolution taken, and the gate result after it)_

### 1 · H1 `test-harness-fail-loud` → `overnight-20260729-2` — **CLEAN**

**Merged:** `card/h1-test-harness-fail-loud` @ `76e35ef` (7 commits). First card to land; nothing
else was on the branch, so no collision was possible.

**Files:** `Taskfile.yml` (one `deps:` line + comment), five `TestMain`s
(`workflow/stable_identity_test.go`, `receipt/worker_test.go`, `inventory/period_summary_test.go`,
`auth/permission_test.go`, `purchasing/scheduler_cron_test.go`), three helpers
(`recipes/helpers_test.go`, `sync/access_test.go`, `sync/jwtbridge_test.go`), plus two new files
outside the stated footprint — `backend/internal/testdb/testdb.go` and
`scripts/verify-test-harness.sh` — both disclosed in the merge-intent note.

**Intents read:** H1's note only; no other card was on the branch to read against. It declares
`Taskfile.yml` as the shared surface every other card tonight asserts untouched, and states both
out-of-footprint files with rationale. Two lines were struck in place under B-11 (the pre-
implementation promise of a dead-port default, contradicted by the WSL2 SYN black-holing; and the
`sync.spec.js:1198` "also passed" claim, struck because `:1198` names nothing).

**Resolution:** none required — automatic merge, no conflicted hunks.

**Gate after merge:** see the merge commit body. G1 green, harness verifier `RAW EXIT=0` on the
merged tree, skip-on-unset preserved at exit 0 across nine packages.

**Note for the next merge:** `Taskfile.yml` now carries `bdd:gen` in `test:`'s `deps`. Any card
that also edited `Taskfile.yml` conflicts here — resolve toward keeping `bdd:gen`, since removing
it reinstates B-09.

### 2 · C1 `sync-rxdb-conflict-notice-mockup-amendments` → `overnight-20260729-2` — **CLEAN**

**Merged:** `card/c1-sync-rxdb-conflict-notice-mockup-amendments` @ `e8e21d8` (7 commits).

**Files:** 35 paths — 33 under `.planning/phases/sync-rxdb-conflict-notice/` (UI-SPEC.md,
mockup.html, 32 screenshots), plus `.night-crew/knowledge/roadmap.md` and the merge-intent note.
**Zero production code.**

**The one shared surface: `roadmap.md`.** Git auto-merged it against H1's edit. The two cards
touch disjoint regions — H1 flips its own card bullet at `:528`, C1 rewrites the
conflict-notice block at `:1068`+ and strikes the superseded r1 claims at `:1169`-`:1190`.
**Verified after the merge rather than assumed:** `test-harness-fail-loud` reads DONE at `:528`,
`sync-rxdb-conflict-notice-mockup-amendments` reads DONE at `:1068`, and
`sync-rxdb-conflict-notice-ui` still reads `🛑 PLANNED — ATTENDED-BLOCKED AGAIN` at `:1123`.
Both flips survived; the block did not get lifted by the auto-merge.

**Intents read:** C1's note (self-consistent, three lines struck in place across two repair
rounds) and H1's (already on the branch). Neither claims the other's region of `roadmap.md`.

**Resolution:** none required — no conflicted hunks.

**Gate after merge:** none owed. `git diff` on `version.go`, `package.json`, `sw.js`, `Taskfile.yml`
is empty, so neither the service worker nor the suite can move.

🛑 **The literal full-suite trigger DID fire and is recorded rather than passed over.** The
card's slate entry says it pays the full Playwright suite "if it somehow touches a file outside
`.planning/`", and it touched `roadmap.md`. **Orchestrator decision: the suite is not owed.** The
clause's stated reason is "zero production code, so the SW and the suite do not move" — a
knowledge-doc status flip changes no shipped byte and cannot alter a test outcome, and *every*
card flips the roadmap, so reading the clause literally would make every card owe two suites. The
final-tree suite at closeout covers the merged result regardless. Recorded here so triage can
overrule it.

### 3 · B1 `sync-rxdb-collections-and-table-contract` → `overnight-20260729-2` — **CLEAN**

**Merged:** `card/b1-sync-rxdb-collections-and-table-contract` @ `c15d591` (8 commits).

**Files:** `sync-schema/collections.js`, `sync-schema/sql/0001_sync_tables.sql`,
`sync-schema/package.json` (three keys, zero deps, solely `"type":"module"` — a disclosed
footprint widening, because the root has no `"type"` field and Node parsed the ESM as CJS,
killing 20 of 28 tests with `SyntaxError: Unexpected token 'export'`), `tests/sync-schema.spec.js`,
plus `roadmap.md` and the merge-intent note.

**Shared surface: `roadmap.md`** — third card to touch it, auto-merged again. **Verified after
the merge, not assumed:** all three landed cards read DONE in the file, and
`sync-rxdb-conflict-notice-ui` still reads `🛑 ATTENDED-BLOCKED AGAIN`. No card's flip was lost
and the block was not lifted.

**Intents read:** B1's note (self-consistent; the "package.json untouched" line struck in place
when `sync-schema/package.json` was added, and two further claims struck in the repair round),
against H1's and C1's already on the branch. No overlap — B1 adds only new files plus its own
roadmap bullet, and explicitly touches neither `workflows.html` nor `sync.js`.

**Resolution:** none required — no conflicted hunks.

**Gate after merge:** G1 (`go build` + `go vet`) green on the merged tree. No Go source moved, so
the Go suite is unaffected; `sync-schema/**` matches none of `build-sw.js`'s `globPatterns`
allow-list, so the precache manifest structurally cannot move.

**Carried forward for the A1 merge:** A1 is the only remaining card that touches `sync.js`,
`workflows.html`, and `purchasing.html`, and it regenerates `sw.js`. B1 touches none of those, so
no collision is expected from this direction. The predicted `0072` migration collision is between
A1 and B2 only.
