Tonight's autonomous run. Execute the signed slate at
.night-crew/knowledge/reference/slate-20260729.md.

Setup: you are on branch dev.

Before any git write, CHECK FOR UNMERGED RUN BRANCHES. Run this EXACTLY as written — it
contains no placeholder to substitute, and it asks about every date, because an unfinished
night from any date is a night's work sitting unmerged:

    git branch --no-merged dev --format='%(refname:short)' | grep -E '^overnight-[0-9]{8}(-[0-9]+)?$'

STATE THE RESULT OUT LOUD BEFORE PROCEEDING — either "no unmerged run branches" or the list.
You may only proceed after saying which one you saw. An empty result is the "go" signal, so a
command that failed or ran wrong looks exactly like a clean repo unless you have named what
you found.

- No unmerged run branch → say so, then cut this run's branch. Compute its name from the BARE
  DATE, since you are looking for every branch of that date:

      git branch --format='%(refname:short)' | grep -E '^overnight-20260729(-[0-9]+)?$' | sort -V

  No match → overnight-20260729. Otherwise the next free suffix (-2, -3, …). A MERGED branch's
  name is still taken, so a date whose run merged this morning yields -2 — cut it and proceed
  without asking; nothing is at risk.
- ANY unmerged run branch exists → 🛑 STOP. A previous night's work is sitting on it. Report
  what is there (git log --oneline dev..<branch>, and whether DECISIONS-NEEDED.md carries an
  open section for it), then ASK THE OPERATOR to choose: RESUME that branch, or RECUT at the
  next free suffix. Do NOT pick for them — resuming mixes two nights on one branch, recutting
  walks away from a night's work, and you cannot tell from here which is right. This is NOT
  covered by the batch sign-off: the yes is scoped to the signed specs, and what to do with an
  unfinished night is not one of them. Whatever they answer, the existing branch is left
  untouched — never delete, reset, force, or rename it.

If the run id gains a suffix, EVERY run-scoped name takes it with them: the conflict log
.night-crew/knowledge/reference/conflicts-<runid>.md, and the closeout's references to the
run. The slate keeps the name it was signed under.

Do all work on the run branch. Never push, never touch main. Hard prohibitions in CLAUDE.md
apply.

Authority: batch sign-off was given by the operator on 2026-07-28 — the slate document is the
record. Do NOT pause for per-change sign-off. Every operator fork is already resolved (ledger
T-25, decisions 67–77); the run never decides, it executes.

Rules of engagement:
- Standing rules and verification gates G1–G6 are inherited unchanged from
  .night-crew/knowledge/reference/overnight-run-plan-20260707.md.
- Dispatch model: you are the control loop, not the implementer. For each card: create a
  worktree off overnight-20260729, spawn a FRESH subagent scoped to exactly that card (paste
  its slate entry, footprint, and spec references into the subagent prompt; it drafts,
  implements, runs G1–G4 in its worktree, and returns a compact report), then spawn a SEPARATE
  fresh subagent for the G6 adversarial review, then merge to the run branch and archive
  yourself. Never implement a card in your own context; your context holds reports, not diffs.
  Subagents cannot spawn subagents — G6 dispatch is always yours. A failed card PARKS per the
  slate rules (worktree preserved, DECISIONS-NEEDED.md) — dispatch the next card. Editing a
  file outside the card's footprint is NOT a park (§15ad.65) — see the conflict rules below.
- Order: Card A `precache-manifest-from-head` strictly first, alone — its acceptance test is
  the red-first one: stage a file, assert it is ABSENT from the precache manifest (and move
  tests/sw-manifest.spec.js test 1 off `git ls-files` in the same change, or it will keep
  agreeing with the bug). Card A must be merged before any other card regenerates sw.js.
  Then dispatch Track A and Track B concurrently, at most one in-flight card per track, in
  slate order within each track:
      Track A: Card B `workflow-queue-period-and-failnote-upsert`
      Track B: Card C `sync-proxy-endpoint`, then Card D `sync-rxdb-conflict-notice-mockup`
  You alone merge to the run branch, in slate order. If a track stalls on rate limits, degrade
  to serial — do not sit blocked.
- Per-change mechanics — the slate records the workflow-preflight verdict as ABSENT, and this
  repo has no openspec/ directory. Do NOT assert OpenSpec mechanics and do NOT create
  openspec/ scaffolding here. Assert only the universal mechanics: red-first evidence, atomic
  commits, a change-identifying commit trailer on every commit, and the roadmap/status flip.
  Which per-change discipline this repo should adopt is an operator question (B-105) — never
  encode another detected convention's mechanics in its place.
- Merge-intent note (REQUIRED, §15ad.65): each subagent writes a merge-intent note in its
  FIRST commit, before implementing — shared files touched (each file outside its own
  packages, one line of why), what must survive any merge, and what is safe to drop. Empty
  fields say "nothing here" explicitly. Since this repo has no openspec/ tree, put it at
  .night-crew/runs/2026-07-29-autonomous/merge-intent-<card>.md. Paste this requirement into
  every implementer prompt. B-11 applies: if a repair changes what the note said, RE-READ THE
  WHOLE NOTE and strike the contradicted lines — do not only append.
- Conflicts, not fences (§15ad.65): the footprint is planning information, not a boundary — a
  card MAY edit any file, and doing so is not a breach and not a park. Collisions surface as
  ordinary git conflicts. YOU resolve them at merge by reading BOTH sides' merge-intent notes
  and resolving against intent, not text; then re-run G1+G2. PARK the later card ONLY when,
  having read both intents, you still cannot determine which behavior is correct. An unplanned
  lifecycle row or any operator-only question still PARKS. Never improvise forks.
- Conflict log (REQUIRED, §15ad.66): append an entry to
  .night-crew/knowledge/reference/conflicts-20260729.md for EVERY merge, clean or conflicted —
  the cards involved, files and hunks, the intents you read, the resolution you took, and the
  gate result after it. Clean merges get a one-line entry, so an empty log never reads as "no
  conflicts" when it means "the logging never ran". Commit it with the merge. Summarize it in
  the closeout; morning triage audits it.
- 🛑 SUITE HONESTY (B-09): `task test:` omits the `bdd:gen` dep, so a fresh worktree silently
  runs 19 of 20 spec files. EVERY card must run `npx bddgen` before any full-suite claim and
  must STATE THE SPEC-FILE COUNT IT SAW (expect 20 files / 566+ tests). A card reporting a
  full suite without that count has not run one.
- Known-armed reds, so nobody re-investigates them as regressions: tests/sync.spec.js:446
  [LST-17] is load-sensitive (red under suite load on 2026-07-28, 10/10 green isolated) and
  tests/sync.spec.js:1198 is a ~16–20% flake on 5 clean samples. Both PRE-EXISTING. If either
  reds, isolate it before attributing anything to your card.
- Do NOT fold in the tests/sync.spec.js:1584 stale comment (B-06) — it belongs to
  sync-rxdb-schema-and-replication, which is not in this slate.
- Budget discipline: prefer a clean early exit over starting a card you cannot finish cleanly.
  Card B's park trigger is real: if prod-shaped duplicate submission_fail_notes rows exist, the
  unique index needs a dedup rule first — that is a data decision, so PARK rather than choose
  one. Card C's park trigger: if the WebSocket upgrade path cannot be tested without a live
  Realtime container, park the upgrade half and say so — do not ship an untested upgrade path
  and call it proven.

Closeout (ALWAYS, leave ~30 min): update HANDOFF.md (per-card outcomes table, gate evidence on
the final tree, next actions) and DECISIONS-NEEDED.md (anything parked), commit as docs. The
morning reader is the operator — write for them.

Your FINAL CHAT MESSAGE is a hand-off, so CLAUDE.md §"Context checkpoints at hand-offs" binds
it. Do not end on a results summary. End with, in this order:
1. Next steps, led by morning triage — what is waiting, what is parked, what the operator has
   to decide. Include the two attended items this slate creates: the two-device convergence
   check (runbook at reference/attended-two-device-check.md) and Card D's mockup sign-off.
2. The context-checkpoint verdict with its reason. For a finished run it is 🔄 Clear now
   (recommended) — /nc-morning-triage is designed to start cold and reviews the run branch on
   its merits; nothing in your context is load-bearing because HANDOFF.md,
   DECISIONS-NEEDED.md, and the conflict log carry it all. Say ⛔ instead only if something
   real is still in flight in this session.
3. The paste-ready next command: /nc-morning-triage
Writing HANDOFF.md does NOT discharge this. The file and the closing message are different
surfaces read at different moments, and the operator reading the last thing on screen at 7am
is the one this is for.
