---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - purchasing.html
  - index.html
  - sw.js
autonomous: true
requirements: [quick-restructure-pwa]
must_haves:
  truths:
    - "Opening index.html shows a grid of tool tiles (Purchasing, Payroll, Scheduling, Hiring, BI)"
    - "Tapping the Purchasing tile navigates to purchasing.html"
    - "purchasing.html contains the full existing purchasing mockup unchanged"
    - "Launcher matches existing design language (same CSS variables, dark mode, mobile-first)"
    - "Service worker caches both index.html and purchasing.html"
  artifacts:
    - path: "purchasing.html"
      provides: "Existing purchasing mockup relocated"
    - path: "index.html"
      provides: "Launcher home screen with tile grid"
    - path: "sw.js"
      provides: "Updated cache with both pages"
  key_links:
    - from: "index.html"
      to: "purchasing.html"
      via: "anchor tag on Purchasing tile"
      pattern: "href.*purchasing\\.html"
---

<objective>
Restructure the PWA from a single purchasing app into an ops console shell. index.html becomes a launcher with a grid of tool tiles. The existing purchasing mockup moves to purchasing.html.

Purpose: Transform single-purpose app into an internal tools portal that can grow.
Output: New launcher index.html, relocated purchasing.html, updated sw.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@index.html (current purchasing mockup — will become purchasing.html)
@sw.js (service worker — needs cache update)
@manifest.json (PWA manifest — start_url stays index.html)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate purchasing mockup and create launcher home screen</name>
  <files>purchasing.html, index.html</files>
  <action>
Step 1 — Create purchasing.html:
- Copy the ENTIRE current index.html content to purchasing.html verbatim.
- Change the title from "Yumyums Purchasing" to "Yumyums Purchasing" (keep it, it's correct for the subpage).
- Update the apple-touch-icon href from "icons/icon-192.png" to "icons/icon-192x192.png" (the old file was deleted; new icons use the NxN naming).
- Keep all CSS, JS, mock data, service worker registration exactly as-is.

Step 2 — Create new index.html as the launcher:
- Same DOCTYPE, charset, viewport meta (with max-scale=1, user-scalable=no, viewport-fit=cover).
- Same theme-color (#0c447c), apple-mobile-web-app metas.
- Link to manifest.json, apple-touch-icon href="icons/icon-192x192.png".
- Title: "Yumyums HQ".
- Reuse the EXACT same CSS variables from the current index.html (:root and dark mode media query) so the design language matches.
- Body styling: same font-family, background, color, padding as current.
- .app container with max-width:480px, margin:0 auto, padding-top:12px (same as current).

Launcher content:
- An h1 "Yumyums HQ" styled at font-size:20px, font-weight:600, margin:0 0 4px.
- A subtitle p "Internal Tools" in --mut color, font-size:13px, margin:0 0 20px.
- A grid container using CSS grid: grid-template-columns: repeat(2, 1fr), gap:10px.
- Five tiles, each an anchor tag styled as a card (background:var(--card), border-radius:12px, padding:20px 16px, text-decoration:none, color:var(--txt), text-align:center, border:0.5px solid var(--brd)). Add a subtle hover/active state using var(--info-bg).

The five tiles with emoji, label, and description:
1. Emoji: a]a (shopping cart), Label: "Purchasing", Desc: "Weekly food orders", href: "purchasing.html"
2. Emoji: a]c (money bag), Label: "Payroll", Desc: "Staff pay runs", href: "#" (placeholder)
3. Emoji: a]d (calendar), Label: "Scheduling", Desc: "Shift planning", href: "#" (placeholder)
4. Emoji: a]b (briefcase), Label: "Hiring", Desc: "Applications", href: "#" (placeholder)
5. Emoji: a]e (bar chart), Label: "BI", Desc: "Reports & metrics", href: "#" (placeholder)

Tile inner structure:
- Emoji in a div with font-size:32px, margin-bottom:8px.
- Label in a div with font-size:14px, font-weight:500, margin-bottom:4px.
- Description in a div with font-size:11px, color:var(--mut).

Placeholder tiles (href="#") should have opacity:0.5 and pointer-events:none to indicate they're coming soon. Add a small "Coming soon" note via a span with font-size:9px, color:var(--mut), displayed below the description.

Add the same dblclick prevention and service worker registration script at the bottom.

Use these actual emoji characters (not the placeholders above):
- Purchasing: the shopping cart emoji
- Payroll: the money bag emoji
- Scheduling: the calendar emoji
- Hiring: the briefcase emoji
- BI: the bar chart emoji
  </action>
  <verify>
    <automated>test -f /Users/jamal/projects/yumyums/hq/purchasing.html && test -f /Users/jamal/projects/yumyums/hq/index.html && grep -q "purchasing.html" /Users/jamal/projects/yumyums/hq/index.html && grep -q "CATS=" /Users/jamal/projects/yumyums/hq/purchasing.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>purchasing.html contains full original purchasing mockup. index.html is a launcher with 5 tiles in a 2-column grid. Purchasing tile links to purchasing.html. Other 4 tiles are visually disabled placeholders. Both pages share the same CSS variables and design language.</done>
</task>

<task type="auto">
  <name>Task 2: Update service worker cache</name>
  <files>sw.js</files>
  <action>
Update sw.js:
- Bump CACHE version from 'yumyums-v2' to 'yumyums-v3'.
- Add './purchasing.html' to the ASSETS array. Final array: ['./', './index.html', './purchasing.html', './manifest.json'].
- Keep all other logic (install, activate, fetch) exactly the same.
  </action>
  <verify>
    <automated>grep -q "yumyums-v3" /Users/jamal/projects/yumyums/hq/sw.js && grep -q "purchasing.html" /Users/jamal/projects/yumyums/hq/sw.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Service worker caches both index.html and purchasing.html. Cache version bumped so existing installs refresh.</done>
</task>

</tasks>

<verification>
- Open index.html in browser: should show "Yumyums HQ" header with a 2-column grid of 5 tiles.
- Purchasing tile is clickable and navigates to purchasing.html.
- purchasing.html shows the original purchasing mockup (Form/Locked/PO tabs).
- Dark mode works on both pages (toggle system dark mode).
- Placeholder tiles are visually muted and not clickable.
- sw.js includes purchasing.html in cache list with bumped version.
</verification>

<success_criteria>
PWA opens to a launcher home screen with tool tiles. Purchasing tile navigates to the existing mockup. Design language is consistent across both pages. Service worker caches all pages.
</success_criteria>

<output>
After completion, create `.planning/quick/260412-c3f-restructure-pwa-into-ops-console-shell-w/260412-c3f-SUMMARY.md`
</output>
