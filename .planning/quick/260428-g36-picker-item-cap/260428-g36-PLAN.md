---
phase: quick
plan: 260428-g36
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/purchasing.spec.js
  - purchasing.html
autonomous: true
must_haves:
  truths:
    - "Item picker renders ALL catalog items, not just 30"
    - "Grouped items (by store_location) all appear with correct headers"
    - "Unassigned items section renders fully when >30 total items exist"
  artifacts:
    - path: "tests/purchasing.spec.js"
      provides: "Regression test proving >30 items all render"
    - path: "purchasing.html"
      provides: "renderPickerList without 30-item cap"
  key_links:
    - from: "purchasing.html renderPickerList()"
      to: "ALL_ITEMS array"
      via: "filter + forEach (no count cap)"
      pattern: "filtered\\.forEach"
---

<objective>
Remove the hard-coded 30-item cap in the purchasing item picker's `renderPickerList()` function that causes the list to appear frozen/truncated when the catalog exceeds 30 items.

Purpose: The food truck catalog can exceed 30 items. The picker must render all of them since the container already supports scrolling via `overflow-y:auto`.
Output: Regression test + fix in purchasing.html
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@purchasing.html (lines 919-978 — renderPickerList function)
@tests/purchasing.spec.js (existing test patterns, invApiCall helper at line 498)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write regression test proving 30-item cap bug</name>
  <files>tests/purchasing.spec.js</files>
  <behavior>
    - Test: Seed 35 items via API (all with store_location set), open picker, count rendered `.picker-row` elements — expect 35, currently gets 30 (RED)
  </behavior>
  <action>
Add a new test inside the existing `test.describe('Item picker store_location enforcement')` block (which already has `invApiCall` helper and the `beforeEach` login+navigate).

The test should:
1. Fetch groups via `invApiCall(page, 'GET', 'groups')` and grab the first group ID
2. Create 35 items in a loop: `invApiCall(page, 'POST', 'items', { description: 'Cap Test Item NN ' + Date.now(), group_id: gid })` — then for each, set store_location via `invApiCall(page, 'PUT', 'items', { id: created.id, description: created.description, group_id: gid, store_location: 'TestLoc' })`
3. Open picker: click `[data-action="open-picker"]`, wait for `#item-modal.open`
4. Clear search to show all: ensure `#picker-search` is empty
5. Count `.picker-row` elements: `const count = await page.locator('.picker-row').count()`
6. Assert `count >= 35` (there may be pre-existing items too, so >= not exact)

Name the test: `'item picker renders all items when catalog exceeds 30'`

Run the test — it MUST FAIL (proving the 30-item cap bug exists). The failure will show count === 30 instead of >= 35.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/yumyums/hq && npx playwright test tests/purchasing.spec.js -g "item picker renders all items when catalog exceeds 30" 2>&1 | tail -20</automated>
  </verify>
  <done>Test exists and FAILS with count === 30, proving the bug</done>
</task>

<task type="auto">
  <name>Task 2: Remove 30-item cap from renderPickerList</name>
  <files>purchasing.html</files>
  <action>
In `purchasing.html`, function `renderPickerList()` (around lines 919-978), remove all four `count >= 30` guards and the `count` variable:

1. Line 942: Delete `var count = 0;`
2. Line 944: Delete `if (count >= 30) return;` (inside `sortedKeys.forEach`)
3. Line 947: Delete `if (count >= 30) return;` (inside `groups[loc].forEach`)
4. Line 957: Delete `count++;` (end of grouped item loop body)
5. Line 960: Change `if (unassigned.length > 0 && count < 30)` to `if (unassigned.length > 0)`
6. Line 963: Delete `if (count >= 30) return;` (inside `unassigned.forEach`)
7. Line 973: Delete `count++;` (end of unassigned loop body)

Do NOT change any other logic. The filtering, grouping, sorting, and HTML generation all remain identical.

After the fix, run the regression test — it MUST PASS.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/yumyums/hq && npx playwright test tests/purchasing.spec.js -g "item picker renders all items when catalog exceeds 30"</automated>
  </verify>
  <done>Test passes with all 35+ items rendered. No count cap remains in renderPickerList.</done>
</task>

<task type="auto">
  <name>Task 3: Run full purchasing test suite for regressions</name>
  <files></files>
  <action>
Run the full purchasing test suite to confirm no regressions from removing the count cap.

If any pre-existing tests fail, investigate whether the failure is related to the count removal (it should not be — the cap was purely a rendering limit, not a functional constraint).
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/yumyums/hq && npx playwright test tests/purchasing.spec.js</automated>
  </verify>
  <done>All purchasing tests pass including the new regression test</done>
</task>

</tasks>

<verification>
- `renderPickerList()` in purchasing.html contains zero references to `count >= 30` or any item-count cap
- Regression test seeds >30 items and asserts all render
- Full purchasing test suite green
</verification>

<success_criteria>
Item picker renders the complete catalog regardless of size. Regression test guards against future re-introduction of arbitrary caps.
</success_criteria>

<output>
After completion, create `.planning/quick/260428-g36-picker-item-cap/260428-g36-SUMMARY.md`
</output>
