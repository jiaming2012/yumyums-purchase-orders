---
phase: quick
plan: 260421-im4
type: execute
wave: 1
depends_on: []
files_modified:
  - inventory.html
  - tests/inventory.spec.js
  - CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "First tab in inventory.html reads 'Purchases' instead of 'History'"
    - "Tab label with count reads 'Purchases (N)' instead of 'History (N)'"
    - "All existing inventory tests pass with updated assertions"
    - "CLAUDE.md documentation reflects 'Purchases' tab name"
  artifacts:
    - path: "inventory.html"
      provides: "Renamed tab button and label function"
      contains: "Purchases"
    - path: "tests/inventory.spec.js"
      provides: "Updated test assertions"
      contains: "Purchases"
    - path: "CLAUDE.md"
      provides: "Updated architecture docs"
      contains: "Purchases"
  key_links: []
---

<objective>
Rename the "History" tab in inventory.html to "Purchases" and update all related test assertions and documentation references.

Purpose: The tab shows purchase events and pending reviews — "Purchases" is a more accurate, intuitive label.
Output: Renamed tab across UI, tests, and docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@inventory.html
@tests/inventory.spec.js
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename History to Purchases in inventory.html</name>
  <files>inventory.html</files>
  <action>
In inventory.html, make these changes:

1. Tab button text (line ~173): Change `>History</button>` to `>Purchases</button>`

2. `updateHistoryTabLabel()` function (line ~495-499): Change the text content logic from:
   `btn.textContent=n>0?'History ('+n+')':'History';`
   to:
   `btn.textContent=n>0?'Purchases ('+n+')':'Purchases';`

Do NOT rename any JS function names (loadHistory, renderHistoryList, updateHistoryTabLabel, waitForHistoryContent) or HTML element IDs (history-list, t1). Those are internal identifiers — only user-visible text changes.
  </action>
  <verify>
    <automated>grep -n "Purchases" inventory.html | head -5</automated>
  </verify>
  <done>Tab button shows "Purchases" and count label shows "Purchases (N)" — no remaining user-visible "History" text in inventory.html tab area</done>
</task>

<task type="auto">
  <name>Task 2: Update test assertions and CLAUDE.md docs</name>
  <files>tests/inventory.spec.js, CLAUDE.md</files>
  <action>
In tests/inventory.spec.js:

1. Line ~112: Change test name from `'shows 4 tabs: History, Trends, Stock, Cost'` to `'shows 4 tabs: Purchases, Trends, Stock, Cost'`
   Note: inventory actually has 5 tabs (Purchases, Trends, Stock, Cost, Setup) — but keep the existing test name pattern, just swap History for Purchases.

2. Line ~113: Change assertion from `toContainText('History')` to `toContainText('Purchases')`

3. Line ~119: Change test name from `'History tab is active by default'` to `'Purchases tab is active by default'`

4. Lines ~125-137: Update test names and comments that say "History tab" to say "Purchases tab":
   - `'History tab loads purchase events from API'` -> `'Purchases tab loads purchase events from API'`
   - `'History tab shows empty state when no purchases exist'` -> `'Purchases tab shows empty state when no purchases exist'`
   - Comment `// HIST-01: History tab loads purchase events from API` -> `// HIST-01: Purchases tab loads purchase events from API`

Do NOT rename the `waitForHistoryContent` helper function — it is an internal test utility.

In CLAUDE.md, update the two references:

1. In the "### Architecture" inventory description: Change `5-tab layout (History / Trends / Stock / Cost / Setup)` to `5-tab layout (Purchases / Trends / Stock / Cost / Setup)`

2. In the "### inventory.html Key Concepts" section: Change `History (purchase events + pending review)` to `Purchases (purchase events + pending review)`

Run `task sw` after changes to rebuild the service worker with updated inventory.html hash.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/yumyums/hq && npx playwright test tests/inventory.spec.js --headed=false -x 2>&1 | tail -20</automated>
  </verify>
  <done>All inventory tests pass with "Purchases" in tab assertions. CLAUDE.md references updated from "History" to "Purchases".</done>
</task>

</tasks>

<verification>
- `grep -c 'History' inventory.html` returns 0 for user-visible tab text (function names like loadHistory are fine)
- `grep 'Purchases' inventory.html` shows tab button text
- All inventory.spec.js tests pass
- CLAUDE.md grep for "History / Trends" returns 0 matches
</verification>

<success_criteria>
- Tab reads "Purchases" in the UI (both base label and count label)
- All 170+ Playwright tests pass (no regressions)
- CLAUDE.md accurately describes the tab as "Purchases"
</success_criteria>

<output>
After completion, create `.planning/quick/260421-im4-rename-history-tab-to-purchases-in-inven/260421-im4-SUMMARY.md`
</output>
