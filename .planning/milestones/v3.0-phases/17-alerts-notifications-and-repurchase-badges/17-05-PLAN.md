---
phase: 17-alerts-notifications-and-repurchase-badges
plan: 05
type: execute
wave: 2
depends_on: ["17-04"]
files_modified:
  - backend/internal/db/migrations/0046_user_timezone.sql
  - backend/internal/users/db.go
  - backend/internal/users/handler.go
  - backend/internal/purchasing/scheduler.go
  - users.html
  - inventory.html
  - sw.js
autonomous: true
gap_closure: true
requirements: [ALRT-05, ALRT-06]

must_haves:
  truths:
    - "Each user has a configurable timezone (defaults to America/New_York)"
    - "Scheduler uses per-user timezone for reminder alert timing"
    - "Low-stock check uses admin's timezone for week boundary (not hardcoded America/Chicago)"
    - "Badge reset config UI displays the admin's timezone"
  artifacts:
    - path: "backend/internal/db/migrations/0046_user_timezone.sql"
      provides: "timezone TEXT column on users table with default America/New_York"
    - path: "backend/internal/users/db.go"
      provides: "Timezone field on UserRow, exposed in API responses"
    - path: "users.html"
      provides: "Timezone dropdown in user edit form"
  key_links:
    - from: "backend/internal/purchasing/scheduler.go"
      to: "backend/internal/users/db.go"
      via: "GetUsersForAlerts returns timezone per contact"
      pattern: "Timezone"
    - from: "users.html"
      to: "PATCH /api/v1/users/{id}"
      via: "timezone field in PATCH body"
      pattern: "timezone"
---

<objective>
Close UAT gap 3: per-user timezone setting instead of hardcoded America/Chicago.

Purpose: The scheduler uses a hardcoded timezone for reminder timing and low-stock week boundaries. Users in different timezones get alerts at wrong local times. Timezone should be a per-user setting defaulting to America/New_York.
Output: Migration adding timezone column, Go code using per-user timezone, timezone dropdown in Users tab edit form.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-alerts-notifications-and-repurchase-badges/17-01-SUMMARY.md
@.planning/phases/17-alerts-notifications-and-repurchase-badges/17-04-SUMMARY.md

<interfaces>
<!-- After Plan 17-04, UserRow.NotificationChannel is []string (notification_channels in JSON) -->
<!-- NotificationPreferenceContact.NotificationChannels is []string -->

From backend/internal/users/db.go (after Plan 04):
```go
type UserRow struct {
    // ... other fields ...
    NotificationChannel []string `json:"notification_channels"`
    // Plan 05 adds: Timezone string `json:"timezone"`
}

type NotificationPreferenceContact struct {
    UserID               string
    Email                string
    DisplayName          string
    NotificationChannels []string
    // Plan 05 adds: Timezone string
}
```

From backend/internal/purchasing/scheduler.go:
```go
// runLowStockCheck currently uses hardcoded America/Chicago at line 251:
//   loc, _ := time.LoadLocation("America/Chicago")
// Should use cutoff_config.timezone (admin-configured) instead.

// runReminderCheck already uses config.Timezone from cutoff_config — this is correct.
// But reminder alerts could be timezone-aware per recipient in the future.
```

From backend/internal/purchasing/types.go:
```go
type CutoffConfig struct {
    ID         string    `json:"id"`
    DayOfWeek  int       `json:"day_of_week"`
    CutoffTime string    `json:"cutoff_time"`
    Timezone   string    `json:"timezone"`    // e.g. "America/Chicago"
    UpdatedAt  time.Time `json:"updated_at"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — add timezone column to users, update Go types, fix scheduler hardcode</name>
  <files>
    backend/internal/db/migrations/0046_user_timezone.sql
    backend/internal/users/db.go
    backend/internal/users/handler.go
    backend/internal/purchasing/scheduler.go
  </files>
  <action>
1. Create migration `0046_user_timezone.sql`:
   - UP: `ALTER TABLE users ADD COLUMN timezone TEXT NOT NULL DEFAULT 'America/New_York';`
   - DOWN: `ALTER TABLE users DROP COLUMN IF EXISTS timezone;`
   - No CHECK constraint needed — `time.LoadLocation` validates at runtime; constraining to IANA names in SQL is impractical.

2. Update `backend/internal/users/db.go`:
   - Add `Timezone string` field to `UserRow` struct with json tag `"timezone"`.
   - Add `Timezone string` field to `NotificationPreferenceContact` struct.
   - Update ALL SELECT queries that scan UserRow to include `u.timezone` in the column list and Scan call. There are at least two: the single-user query and the list-all query.
   - Update `GetUsersForAlerts` SELECT queries (both cutoff_reminder and shopping_complete variants) to include `u.timezone` in columns and scan into `c.Timezone`.
   - Add `Timezone *string` to `UpdateUserInput`. In `UpdateUser`, when non-nil, validate with `time.LoadLocation(*input.Timezone)` — return error if invalid IANA timezone. Add to SET clause.

3. Update `backend/internal/users/handler.go`:
   - `UpdateUserHandler` PATCH body accepts `timezone *string` field. Pass through to `UpdateUserInput.Timezone`.

4. Fix hardcoded timezone in `backend/internal/purchasing/scheduler.go`:
   - `runLowStockCheck` (line ~250-251): Replace `loc, _ := time.LoadLocation("America/Chicago")` with: load the cutoff_config via `GetCutoffConfig`, use its `Timezone` field. If no cutoff config exists, fall back to `"America/New_York"`. This makes the low-stock week boundary timezone-aware using the admin-configured cutoff timezone (since low-stock is an admin concern, not a per-user one).
   - The cutoff config is already loaded in `runCutoffCheck` and `runReminderCheck` — `runLowStockCheck` should load it similarly.

5. Verify: `cd backend && go build ./cmd/server/` and `go vet ./...` must pass.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/yumyums/hq/backend && go build ./cmd/server/ && go vet ./...</automated>
  </verify>
  <done>
    - Migration 0046 adds timezone column with America/New_York default
    - UserRow has Timezone field, exposed in JSON API responses
    - NotificationPreferenceContact has Timezone field
    - UpdateUser accepts and validates timezone (must be valid IANA timezone via time.LoadLocation)
    - runLowStockCheck uses cutoff_config timezone instead of hardcoded America/Chicago
    - go build and go vet pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend — timezone dropdown in Users edit form + badge reset timezone display</name>
  <files>
    users.html
    inventory.html
    sw.js
  </files>
  <action>
1. **users.html — Add timezone dropdown to edit form:**
   - Add a `<select id="f-timezone">` below the alert channel checkboxes (added in Plan 04).
   - Label: "Timezone"
   - Options: Include the most common US timezones plus UTC. Use value=IANA name, display=friendly name:
     ```
     America/New_York — Eastern
     America/Chicago — Central
     America/Denver — Mountain
     America/Los_Angeles — Pacific
     America/Anchorage — Alaska
     Pacific/Honolulu — Hawaii
     UTC — UTC
     ```
   - In `editUser(u)`: Set `document.getElementById('f-timezone').value = u.timezone || 'America/New_York';`
   - In `saveUser()`: Read timezone value and include `timezone: document.getElementById('f-timezone').value` in the PATCH body.

2. **inventory.html — Badge reset config timezone display:**
   - The badge reset config card (rendered by `renderBadgeResetConfig()`) currently shows timezone as a static display (from Plan 03 it shows the config timezone). This is fine — the admin configures the reset timezone in the PUT body. But update the default timezone display if the config has no timezone set: show "America/New_York" instead of "America/Chicago".
   - Find where the repurchase reset config handler defaults timezone (line ~645 in handler.go): change `input.Timezone = "America/Chicago"` to `input.Timezone = "America/New_York"`.
   - In `renderBadgeResetConfig()` in inventory.html: if showing timezone text, ensure it reads from the config object (already does from Plan 03) — no change needed unless it shows a hardcoded default.

3. **Rebuild service worker:** Run `node build-sw.js` to regenerate `sw.js`.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/yumyums/hq && node build-sw.js && grep -c "f-timezone" users.html && grep -c "America/New_York" backend/internal/purchasing/handler.go</automated>
  </verify>
  <done>
    - users.html has timezone dropdown with common US timezones
    - editUser() pre-selects timezone from user API response
    - saveUser() includes timezone in PATCH body
    - Badge reset config handler defaults to America/New_York (not America/Chicago)
    - sw.js rebuilt with new content hashes
  </done>
</task>

</tasks>

<verification>
- Backend compiles: `cd backend && go build ./cmd/server/`
- `grep -n "Timezone" backend/internal/users/db.go` shows field on UserRow and NotificationPreferenceContact
- `grep -n "America/Chicago" backend/internal/purchasing/scheduler.go` returns 0 matches (hardcode removed)
- `grep -n "f-timezone" users.html` shows dropdown exists
- `grep -n "America/New_York" backend/internal/purchasing/handler.go` shows updated default
</verification>

<success_criteria>
- Each user has a timezone setting that persists via the API
- Default timezone is America/New_York (not America/Chicago)
- Users tab edit form shows timezone dropdown with common US options
- Scheduler runLowStockCheck uses cutoff_config timezone for week boundary calculation
- Badge reset config handler defaults to America/New_York
- Backend builds and passes vet
- Service worker rebuilt
</success_criteria>

<output>
After completion, create `.planning/phases/17-alerts-notifications-and-repurchase-badges/17-05-SUMMARY.md`
</output>
