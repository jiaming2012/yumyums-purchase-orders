---
phase: 999.2-per-menu-item-cogs-attribution-via-recipe-bom-mapping
plan: 02
subsystem: backend
tags: [recipes, endpoint, service-token, menu-cogs, cogs, sales-processor-contract]

# Dependency graph
requires:
  - phase: 999.2-01
    provides: internal/recipes types (MenuCOGSResponse, MenuCOGSRow, IngredientAlloc, UnallocatedBreakdown, UnallocatedDetail) + repository scaffolding + helpers_test.go (setupTestDB, seedMenuItem, seedPurchaseItem, seedRecipe)
  - phase: 21-cogs-in-sales-processor-report-receipt-completeness-gate-bef
    provides: auth.ServiceTokenMiddleware + Phase 21 PeriodSummaryHandler error-envelope contract (D-18 byte-for-byte mirror)
  - phase: 22-hq-toast-ingest-sftp-fetcher-menu-items-daily-sales-aggregat
    provides: menu_items + daily_menu_sales tables (units_sold source per D-13)
  - phase: 12-inventory-photos-tile-permissions
    provides: purchase_items + purchase_events + purchase_line_items (cost source for window-spend CTE)
provides:
  - GET /api/v1/inventory/menu-cogs endpoint (service-token-authenticated)
  - MenuCogsHandler factory exported from internal/recipes
  - Package-local writeJSON/writeError helpers in internal/recipes
  - 9 integration tests covering auth (401/503), validation (400), happy path summary, happy path breakdown, units_sold=0 → null, Cache-Control header
affects:
  - 999.2-03 (Recipes tab handlers — handler.go now exists, CRUD handlers Plan 03 adds to the same file with the same writeJSON/writeError already declared)
  - 999.2-04 (drift scheduler — endpoint surface is stable; banner can deep-link via deployed URL)
  - 999.2-06 (SALES-PROCESSOR-CONTRACT — endpoint is live and verifiable; contract can be authored against actual behavior)

# Tech tracking
tech-stack:
  added:
    - "(none — uses existing pgx/v5 + pgxpool + chi + auth.ServiceTokenMiddleware)"
  patterns:
    - "Per-package writeJSON/writeError helpers (Phase 21 inventory, Phase 22 toast, now Phase 999.2 recipes) — project convention is duplication over httpx shared package"
    - "Service-token endpoint pattern: route inside chi.Group with auth.ServiceTokenMiddleware; handler never checks Bearer/env — 503/401 handled upstream"
    - "Tax-inclusive cost math: (qty*price) * COALESCE(pe.total / NULLIF(pe.total - pe.tax, 0), 1) — Pitfall 4 guard against subtotal=0 keeps the multiplier at 1× instead of NULLing the whole line"
    - "Dual-shape response field via separate omitempty pointers: UnallocatedCogs (*float64) for default mode, Unallocated (*UnallocatedBreakdown) for breakdown mode — exactly one is non-nil"
    - "ingredient_cost_per_unit = NULL via CASE WHEN units=0 — preserved through the *float64 scan target so JSON serializes to literal 'null' (Pitfall 5)"
    - "Test fixtures use DELETE-FIRST seedVendor since Wave 1's setupTestDB TRUNCATE intentionally stops short of vendors; helpers_test.go contract preserved for Plan 04"

key-files:
  created:
    - backend/internal/recipes/handler.go
    - backend/internal/recipes/menu_cogs_test.go
    - .planning/phases/999.2-per-menu-item-cogs-attribution-via-recipe-bom-mapping/999.2-02-SUMMARY.md
  modified:
    - backend/cmd/server/main.go

key-decisions:
  - "writeJSON/writeError duplicated per-package (NOT imported from internal/inventory) — keeps internal/recipes free of cross-package imports per Wave 1 bounded boundary"
  - "Lexicographic from > to compare is safe because YYYY-MM-DD time.Parse rejects everything else upstream (Pitfall 1)"
  - "loadBreakdown + loadUnallocatedBreakdown extracted as private helpers in handler.go (not separate files) — they're handler-scope concerns, not repository-level"
  - "Three separate CTE queries (summary / total unallocated / breakdown detail) rather than one mega-CTE — keeps each query intent obvious, no cost penalty at this cardinality (T-999.2-11 risk register)"
  - "Tests use chi.NewRouter() + auth.ServiceTokenMiddleware wrapper to exercise 401/503 paths exactly as main.go production wiring — gives strong contract assurance vs. mocking the middleware"

patterns-established:
  - "Endpoint adds INSIDE existing service-token chi.Group at main.go:338-342 (one new line + comment) rather than spawning new group — keeps routing topology flat"
  - "Per-test seedVendor cleanup via DELETE WHERE name = $1 — pattern Plan 04 should follow rather than mutating Wave 1's helpers_test.go TRUNCATE list"

requirements-completed: []
requirements_note: "Backlog phase — REQUIREMENTS.md mapping deferred per Plan frontmatter."

# Metrics
duration: "12min"
completed: 2026-06-04
---

# Phase 999.2 Plan 02: menu-cogs endpoint Summary

**GET /api/v1/inventory/menu-cogs ships as a service-token-authenticated peer of Phase 21's period-summary, with byte-for-byte D-18 error envelope mirroring, tax-inclusive window-spend math (D-11), HQ-as-truth units_sold (D-13), dual-shape response per breakdown flag (D-17), and 9 integration tests passing against hq_test.**

## Performance

- **Duration:** ~12 min (one auto-fix loop for vendor-uniqueness in tests)
- **Started:** 2026-06-04T14:26:55Z
- **Completed:** 2026-06-04T14:38:37Z
- **Tasks:** 2 (Task 1 — handler.go; Task 2 — tests + main.go route registration)
- **Files modified:** 3 (2 new, 1 modified)

## Accomplishments

- `backend/internal/recipes/handler.go` ships (288 lines) — MenuCogsHandler factory + package-local writeJSON/writeError + loadBreakdown + loadUnallocatedBreakdown helpers
- Endpoint mounted at `/api/v1/inventory/menu-cogs` inside the existing service-token chi.Group (main.go:339-342), peer with `/inventory/period-summary`
- `HQ_INVENTORY_SERVICE_TOKEN` WARNING log updated to mention BOTH endpoints will 503 when unset
- 9 integration tests in `menu_cogs_test.go` all pass against hq_test:
  - TestMenuCogs_HappyPath_SummaryMode — verifies ingredient_cost_total=$11.00, per-unit=$2.7500, unallocated_cogs=$11.00 (a 22.00 tax-prorated spend × 50% recipe at units=4)
  - TestMenuCogs_HappyPath_BreakdownMode — verifies ingredients[] populated, Unallocated{total, by_ingredient[]} struct present, reason contains "partial allocation"
  - TestMenuCogs_UnitsSoldZero_PerUnitIsNull — string-level + struct-level confirmation of literal `null` JSON serialization
  - TestMenuCogs_FromMissing_400 — verbatim "from must be YYYY-MM-DD" envelope (D-18)
  - TestMenuCogs_FromAfterTo_400 — verbatim "from must be <= to" envelope (D-18)
  - TestMenuCogs_CacheControlHeader_OnSuccess — verifies `private, max-age=3600` on 200
  - TestMenuCogs_BearerMissing_401 — chi router + ServiceTokenMiddleware integration verifies "unauthorized" envelope
  - TestMenuCogs_ServiceTokenUnset_503 — verifies "service_token_not_configured" envelope (fail-closed when env unset)
  - TestMenuCogs_BearerWrong_401 — verifies wrong-Bearer rejection via constant-time compare
- Package boundary preserved: `grep -E "yumyums/hq/internal/inventory" backend/internal/recipes/handler.go` returns nothing
- All 8 Wave 1 TestRepository_* tests still pass alongside the new 9 TestMenuCogs_* (full package run: 17 tests, ~15s)

## Task Commits

Each task was committed atomically:

1. **Task 1: MenuCogsHandler + package helpers** — `b283f5f` (feat)
2. **Task 2: integration tests + main.go route registration** — `9f28197` (feat)

## Files Created/Modified

- `backend/internal/recipes/handler.go` (new, 288 lines) — Handler factory, three CTE queries (summary / total unallocated / breakdown detail), package-local writeJSON+writeError, loadBreakdown+loadUnallocatedBreakdown helpers
- `backend/internal/recipes/menu_cogs_test.go` (new, 354 lines) — 9 integration tests + local fixture helpers (seedVendor with DELETE-first cleanup, seedPurchaseEvent, seedPurchaseLineItem, seedDailyMenuSales)
- `backend/cmd/server/main.go` (modified, 3-line diff) — import recipes package; add menu-cogs route inside existing service-token group; update WARNING log to mention both endpoints

## Decisions Made

- **writeJSON / writeError duplicated per-package** — internal/toast/handler.go:12-23 documents this is the project convention. Sharing via an httpx package would create a cross-package import for a 5-line helper; the codebase prefers the duplication tradeoff.
- **Three separate CTEs, not one mega-CTE** — summary / total unallocated / breakdown detail are conceptually distinct; combining them would require correlated subqueries that obscure intent. At the projected cardinality (recipes in the low thousands, window spend over 7 days), three round-trips are negligible.
- **chi router + ServiceTokenMiddleware in tests** — exercises the actual middleware code path. Mocking the middleware would risk drift if Phase 21's auth code changes.
- **Per-test DELETE-first seedVendor over modifying setupTestDB TRUNCATE** — Wave 1's helpers_test.go has a stable contract that Plan 04 builds on (per Plan 01 SUMMARY "Next Phase Readiness"). Extending the TRUNCATE list to include vendors would have been a remote change with subtle blast radius; the per-test DELETE keeps the change tightly scoped to menu_cogs_test.go.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] vendors not truncated by setupTestDB**
- **Found during:** Task 2 (initial test run)
- **Issue:** Wave 1's `setupTestDB` TRUNCATEs `drift_check_results, recipes, daily_menu_sales, purchase_line_items, purchase_events, menu_items, purchase_items` — but NOT `vendors`. My tests seed `vendors("Acme")` repeatedly, hitting the UNIQUE(name) constraint on the second test in a single `go test` run. Could not modify `helpers_test.go` because Plan 01 SUMMARY's "Next Phase Readiness" section explicitly cites the existing TRUNCATE list as the stable Plan 04 contract.
- **Fix:** `seedVendor` now does `DELETE FROM vendors WHERE name = $1` before INSERT. Safe because setupTestDB already cleared the FK-referencing `purchase_events` rows via CASCADE TRUNCATE. Per-test cleanup keeps the change tightly scoped to `menu_cogs_test.go`.
- **Files modified:** backend/internal/recipes/menu_cogs_test.go (seedVendor helper)
- **Verification:** Two consecutive full-package test runs pass (no flakes). TestMenuCogs_* (9 tests) + TestRepository_* (8 tests) all pass.
- **Committed in:** 9f28197

### Smoke Test (for SALES-PROCESSOR-CONTRACT, Plan 06)

After deploy with `HQ_INVENTORY_SERVICE_TOKEN` set:

```
# Summary mode
curl -s -H "Authorization: Bearer $HQ_INVENTORY_SERVICE_TOKEN" \
  "https://hq.yumyums.kitchen/api/v1/inventory/menu-cogs?from=2026-05-25&to=2026-05-31"
# Expected: 200, JSON with from/to/menu_items[]/unallocated_cogs

# Breakdown mode
curl -s -H "Authorization: Bearer $HQ_INVENTORY_SERVICE_TOKEN" \
  "https://hq.yumyums.kitchen/api/v1/inventory/menu-cogs?from=2026-05-25&to=2026-05-31&breakdown=true"
# Expected: 200, JSON with menu_items[].ingredients[] populated and unallocated{total, by_ingredient[]} object

# Missing Bearer → 401
curl -s "https://hq.yumyums.kitchen/api/v1/inventory/menu-cogs?from=2026-05-25&to=2026-05-31"
# Expected: 401, {"error":"unauthorized"}

# Invalid date → 400
curl -s -H "Authorization: Bearer $HQ_INVENTORY_SERVICE_TOKEN" \
  "https://hq.yumyums.kitchen/api/v1/inventory/menu-cogs?from=not-a-date&to=2026-05-31"
# Expected: 400, {"error":"from must be YYYY-MM-DD"}
```

When `HQ_INVENTORY_SERVICE_TOKEN` is UNSET on the server, all the above paths return 503 `{"error":"service_token_not_configured"}` — fail-closed behavior per Phase 21's contract.

### Phase 21 Envelope Format Deviations

**Zero.** All four error envelope strings match Phase 21 byte-for-byte:
- `"from must be YYYY-MM-DD"`
- `"to must be YYYY-MM-DD"`
- `"from must be <= to"`
- `"internal_error"`

Confirmed via `grep -E '"(from|to) must be|internal_error|<= to"'` on both `backend/internal/inventory/handler.go` (Phase 21 PeriodSummaryHandler) and `backend/internal/recipes/handler.go` (this plan's MenuCogsHandler).

## Issues Encountered

- **Worktree base mismatch on startup** — actual base was `619597fe...`, expected `0f660f4...`. Hard-reset to expected base before any work; reset succeeded after `git fetch --all`.
- **PLAN.md / RESEARCH.md / PATTERNS.md missing from worktree base** — same as Wave 1: the worktree was branched from a commit prior to the phase planning being committed. Copied the three files from the main repo working tree into the worktree planning dir (no source files affected, planning artifacts only).
- **`task test-db-setup` is not a defined task** — Plan's verify block invokes it but it's a no-op (the actual Taskfile target is `task db-test`). Migrations were already applied to hq_test from Wave 1, so tests ran directly. No source/test changes required.
- **vendors not truncated** — see Deviation #1 above.

## User Setup Required

None. The endpoint is wired and will become live on next deploy. `HQ_INVENTORY_SERVICE_TOKEN` is already established by Phase 21 — no new env var, no new secret rotation.

## Next Phase Readiness

- **Plan 03 (Recipes tab CRUD handlers):** Can add `ListIngredientsHandler`, `CreateRecipeHandler`, `UpdateRecipeUsagePctHandler`, `DeleteRecipeHandler`, `MergeMenuItemHandler` to the same `backend/internal/recipes/handler.go` file. `writeJSON`/`writeError` already declared (no duplicate-definition risk). The handler factory pattern (`func XHandler(pool *pgxpool.Pool) http.HandlerFunc`) is now the in-package convention.
- **Plan 04 (drift scheduler + banner):** Endpoint surface is stable; deep-link from the banner can target `https://hq.yumyums.kitchen/inventory.html#recipes`. The Wave 1 alertSink + helpers_test.go stay intact (no modification by this plan).
- **Plan 05 (Recipes tab UI):** Frontend can call `/api/v1/inventory/menu-cogs` via existing cookie-auth fetch (NOT — this is service-token only). The UI side will consume the read-side via the existing inventory routes Plan 03 ships, not the menu-cogs endpoint (which is exclusively for sales-processor).
- **Plan 06 (SALES-PROCESSOR-CONTRACT):** The endpoint is live and testable. Plan 06 can author the contract document against verified behavior; smoke-test commands above are the seed material for the document.

## Self-Check: PASSED

**Files exist:**
- FOUND: backend/internal/recipes/handler.go
- FOUND: backend/internal/recipes/menu_cogs_test.go
- FOUND: .planning/phases/999.2-per-menu-item-cogs-attribution-via-recipe-bom-mapping/999.2-02-SUMMARY.md

**Files modified:**
- FOUND: backend/cmd/server/main.go (route registered, import added, WARNING updated)

**Commits exist:**
- FOUND: b283f5f (Task 1 — handler.go)
- FOUND: 9f28197 (Task 2 — tests + main.go)

**Build passes:**
- FOUND: `cd backend && go build ./cmd/server` exits 0
- FOUND: `cd backend && go build ./...` exits 0
- FOUND: `cd backend && go vet ./internal/recipes ./cmd/server` clean

**Tests pass:**
- FOUND: 9/9 TestMenuCogs_* PASS in 9-10s against hq_test (DB_TEST_URL=postgres://yumyums:yumyums@100.70.200.55:5433/hq_test)
- FOUND: 8/8 TestRepository_* (Wave 1) still PASS — no regression

**Package boundary preserved:**
- FOUND: `grep -E "yumyums/hq/internal/inventory" backend/internal/recipes/handler.go` returns nothing

**Route registration:**
- FOUND: 1 occurrence of `recipes.MenuCogsHandler` in backend/cmd/server/main.go (per plan acceptance criterion)
- FOUND: WARNING log line on missing HQ_INVENTORY_SERVICE_TOKEN mentions both `/inventory/period-summary` AND `/inventory/menu-cogs`

---

*Phase: 999.2-per-menu-item-cogs-attribution-via-recipe-bom-mapping*
*Completed: 2026-06-04*
