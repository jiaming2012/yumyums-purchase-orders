---
phase: 22.1-hq-toast-ingest-re-architecture-spaces-first-ingest-worker-n
verified: 2026-06-03T09:42:00Z
status: passed
score: 8/8 must-haves verified (programmatic)
overrides_applied: 0
human_verification:
  - test: "Run cmd/migrate-toast-archive against a real sales-processor archive with valid DO_SPACES_* creds"
    expected: "Walks YYYYMMDD subdirs, uploads each ItemSelectionDetails.csv + sidecar to toast/YYYYMMDD/ in DO Spaces, skips dates whose CSV key already exists (HeadObject hit), prints `done. uploaded=N skipped=M missing=K warned=W`. Second run without --overwrite shows uploaded=0 and skipped=N (idempotent)."
    why_human: "Requires live DO_SPACES_* credentials + actual archive on operator's machine; programmatic verification can only confirm the binary exits with --source required and the code calls HeadObject/PutObject correctly."
  - test: "Boot the server with valid DO_SPACES_* + TOAST_SFTP_KEY_PATH and observe a full worker tick"
    expected: "Log shows `toast worker: starting (interval=12h, window=7d, backfill=90d)`. After one tick: `toast sync: wrote YYYYMMDD ...` lines for dates present on Toast SFTP, `toast sync: skip YYYYMMDD (not in SFTP)` for missing dates, and one final `toast ingest: dates=[...] items_upserted=N sales_rows_upserted=M duration=... synced=K` line. Verify cache files appear under backend/cache/toast/YYYYMMDD/ AND keys exist in DO Spaces at toast/YYYYMMDD/."
    why_human: "Requires live SFTP creds + DO Spaces creds + Postgres; cannot validate end-to-end pipeline without running the real worker against real services."
  - test: "Simulate Spaces failure (e.g., revoke key or block the endpoint) and run 3 worker ticks"
    expected: "Each tick: `ERROR toast sync: YYYYMMDD: ...` per date, then `toast worker: consecutive Spaces failures = 1`, `... = 2`, `... = 3`. On the 3rd tick, exactly one Zoho Cliq message is sent to the purchaseandinventory channel containing `Toast sync degraded: 3 consecutive failed ticks talking to DO Spaces. Last error: ...`. On the 4th and 5th failing tick: no additional Cliq message. After Spaces is restored on the 6th tick: `toast worker: Spaces recovered after 5 consecutive failures` and counter resets to 0."
    why_human: "Requires manipulating live Spaces auth + observing the Zoho Cliq channel — the in-process alert latch logic is wired correctly but only a live test confirms the Cliq dispatch lands in the right channel."
  - test: "Boot the server with DO_SPACES_* env vars unset"
    expected: "Log line `WARNING: toast worker: DO Spaces not configured — ingest disabled (server continues)` appears once at boot. Receipt worker, HTTP API, login, and all other subsystems continue functioning normally. No panic, no log.Fatalf, no exit. The /api/v1/inventory/menu-items endpoint still serves any pre-existing rows from menu_items."
    why_human: "Requires running the actual server process and confirming HTTP endpoints stay responsive while the toast worker is dormant."
---

# Phase 22.1: HQ Toast ingest re-architecture — Spaces-first ingest worker Verification Report

**Phase Goal:** Replace Phase 22's SFTP-direct ingest with a Spaces-first pipeline. HQ owns the SFTP→DO Spaces sync (with local forensic cache), the Spaces→DB ingest, and a one-time migration CLI that seeds the bucket from sales-processor's historical Toast archive. Server keeps running when Spaces is unreachable; degraded operation alerts via Cliq after 3 consecutive failed ticks.
**Verified:** 2026-06-03T09:42:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

Derived from the phase goal (ROADMAP has empty `success_criteria` array; REQUIREMENTS.md does not exist in this repo; all plan frontmatter `requirements:` are empty arrays — must-haves come from the ROADMAP goal text plus PLAN frontmatter `must_haves.truths`).

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SFTP→DO Spaces sync exists in HQ (HQ-owned, not sales-processor) | VERIFIED | `backend/internal/toast/sync.go:36` exports `SyncDate(ctx, cfg, date)`. Body calls `dialWithRetry` (sync.go:124) → `client.Download` (sync.go:60) → `cfg.SpacesClient.PutObject` (sync.go:82). Worker calls it per-date (worker.go:116). |
| 2 | Local forensic cache writes alongside Spaces (atomic dual-write) | VERIFIED | sync.go:71-78 writes cache file FIRST with `os.WriteFile(cachePath, csvBytes, 0o644)`, then sync.go:82-90 uploads to Spaces; on PutObject failure, `os.Remove(cachePath)` (sync.go:88) restores the `cache ⊆ Spaces` invariant. |
| 3 | Spaces→DB ingest is the read path; SFTP removed from ingest | VERIFIED | ingest.go:40 calls `cfg.SpacesClient.GetObject`; grep returns 0 hits for `client.Download`, `dialWithRetry`, and `os.ReadFile(cfg.SFTPKeyPath)` in ingest.go. NoSuchKey gracefully skipped at ingest.go:45-50. |
| 4 | One-time migration CLI exists for sales-processor archive | VERIFIED | `backend/cmd/migrate-toast-archive/main.go:49` `func main()`; flags `--source` (required, line 50) and `--overwrite` (line 51); idempotent via `toast.SpacesKeyExists` (line 129); writes CSV + sidecar with `MetaSourceMigration` (line 161). Zero pgx/DB references (grep returns 0). |
| 5 | Cliq alerting fires after 3 consecutive Spaces failures | VERIFIED | worker.go:30 declares `consecSpacesFails`; worker.go:35 declares `alertFiredAtThreshold` latch; worker.go:143-149 increments on systemic error and fires once at threshold via `fireDegradedAlert` (worker.go:175); message format matches exactly: `Toast sync degraded: 3 consecutive failed ticks talking to DO Spaces. Last error: %v` (worker.go:180). Routed via `alerts.ChannelZohoCliq` (worker.go:182). |
| 6 | Server degrades gracefully when Spaces unreachable | VERIFIED | worker.go:57-60 — `if cfg.SpacesClient == nil \|\| cfg.SpacesBucket == ""` logs `WARNING: toast worker: DO Spaces not configured — ingest disabled (server continues)` and returns. Pre-Plan-05 main.go variant would not crash. Counter reset on clean tick at worker.go:163-164. |
| 7 | cmd/server wires Spaces fields + SetAlertQueue | VERIFIED | main.go:540-543 assigns `toastCfg.SpacesClient`, `SpacesBucket`, `SpacesEndpoint`, `CacheDir` (with `envOrDefault("TOAST_CACHE_DIR", "backend/cache/toast")`); main.go:546 calls `toast.SetAlertQueue(alertQ)` BEFORE main.go:551 `toast.StartWorker(ctx, toastCfg)`. `alertQ` constructed at main.go:520 — in scope. `envOrDefault` helper exists at main.go:570. |
| 8 | cmd/sync-toast standalone path reads from Spaces | VERIFIED | cmd/sync-toast/main.go:88-94 builds `*s3.Client` via `photos.NewSpacesClient(photos.SpacesConfig{...})`, injects into `cfg.SpacesClient`/`SpacesBucket`/`SpacesEndpoint`/`CacheDir` (lines 88-97), then calls `toast.RunIngest(ctx, pool, cfg, fromDate, toDate)` at line 107. Zero `client.Download` or `dialWithRetry` references in this CLI. |

**Score:** 8/8 truths verified programmatically. (Live behaviors — actual Cliq message landing in the right channel, real DO Spaces uploads, graceful boot in production — require operator validation; see Human Verification Required.)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/internal/toast/types.go` | Config struct extended with SpacesClient/Bucket/Endpoint/CacheDir | VERIFIED | Lines 27-30 add all four fields; `s3 "github.com/aws/aws-sdk-go-v2/service/s3"` imported at line 6. |
| `backend/internal/toast/config.go` | LoadConfigFromEnv preserves SFTP key fail-fast; no DO_SPACES_* read | VERIFIED | Line 25-31 keeps the `TOAST_SFTP_KEY_PATH is required` fail-fast verbatim. No `DO_SPACES_*` references in the file. |
| `backend/internal/toast/spaces.go` | Key builders + HeadObject existence check | VERIFIED | Exports `SpacesCSVKey` (line 19), `SpacesMetaKey` (line 25), `CacheCSVPath` (line 31), `CacheMetaPath` (line 36), `SpacesKeyExists` (line 43). Path format `toast/%s/ItemSelectionDetails.csv` matches D-02. |
| `backend/internal/toast/meta.go` | Sidecar JSON struct + constants | VERIFIED | `MetaSidecar` (line 19) with snake_case JSON tags `original_filename`, `downloaded_at_iso`, `source`; constants `MetaSourceSFTP="sftp"` (line 11) and `MetaSourceMigration="migration"` (line 12); `NewMetaSidecar` uses `time.RFC3339`. |
| `backend/internal/toast/sync.go` | SyncDate + ErrSFTPMiss + moved dialWithRetry | VERIFIED | All present; atomicity via cache-first + os.Remove on Spaces fail; ErrSFTPMiss sentinel at line 22; dialWithRetry at line 124 with 5s/15s/30s schedule. |
| `backend/internal/toast/ingest.go` | RunIngest reads from Spaces only; dialWithRetry deleted | VERIFIED | RunIngest at line 27; reads via `cfg.SpacesClient.GetObject` (line 40); NoSuchKey graceful at line 45-50; SQL preserved verbatim (lines 109-138); zero SFTP touch points (grep confirms). |
| `backend/internal/toast/worker.go` | Combined runCycle + alert dispatch + SetAlertQueue | VERIFIED | `SetAlertQueue` at line 21; `consecSpacesFails`/`alertFiredAtThreshold` at lines 30/35; `StartWorker` at line 54 (signature preserved); `runCycle` at line 94; `fireDegradedAlert` at line 175 with exact D-06 message format; graceful boot guard at line 57-60. |
| `backend/cmd/migrate-toast-archive/main.go` | Standalone CLI for one-shot historical seed | VERIFIED | `func main()` at line 49; flags `--source` (required) and `--overwrite`; uses `photos.NewSpacesClient`, `toast.SpacesKeyExists`, `toast.SpacesCSVKey`, `toast.SpacesMetaKey`, `toast.MetaSourceMigration`; ZERO DB references (grep on `pgx\|pgxpool\|DB_URL` returns 0). |
| `backend/cmd/server/main.go` | Worker wiring block with Spaces injection + SetAlertQueue | VERIFIED | Lines 528-553 contain the updated block; comment block says "Phase 22.1" (line 528); all four Spaces field assignments present; `toast.SetAlertQueue(alertQ)` precedes `toast.StartWorker`; `TOAST_SYNC_INTERVAL=0` branch preserved (line 548); `log.Fatalf("toast worker: %v", err)` preserved (line 537). |
| `backend/cmd/sync-toast/main.go` | Standalone CLI updated to read from Spaces | VERIFIED | Builds Spaces client via `photos.NewSpacesClient` (line 88); injects all four config fields (lines 88-97); preserves `--from`/`--to` flags (lines 48-49); calls `toast.RunIngest` (line 107); LoadConfigFromEnv still called (line 73) — D-12 preserved. |
| `backend/.gitignore` | cache/ + /migrate-toast-archive entries | VERIFIED | Line 13 `cache/`; line 16 `/migrate-toast-archive`; existing entries `bin/`, `.env`, `/sync-toast`, `/import-notion`, `/seed`, `/server` preserved (lines 2-10). `git check-ignore backend/cache/toast/20260530/ItemSelectionDetails.csv` reports the path as ignored. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `cmd/server/main.go` | `toast.SetAlertQueue` | call before StartWorker | WIRED | main.go:546 `toast.SetAlertQueue(alertQ)`; alertQ constructed at main.go:520 — happens-before goroutine spawn (REVIEW.md WR-02 notes the memory-model nuance but the lexical order is correct). |
| `cmd/server/main.go` | `toast.Config` Spaces fields | direct field assignment | WIRED | main.go:540-543 — four assignments verified. |
| `cmd/server/main.go` | `toast.StartWorker` | call after SetAlertQueue + field injection | WIRED | main.go:551 — block-scoped; precedes only the post-toast log line. |
| `cmd/sync-toast/main.go` | `photos.NewSpacesClient` | client build before RunIngest | WIRED | sync-toast/main.go:88 — call returns `*s3.Client`, assigned to `cfg.SpacesClient`. |
| `cmd/sync-toast/main.go` | `toast.RunIngest` | call after Spaces injection | WIRED | sync-toast/main.go:107 — passes `cfg` with SpacesClient set. |
| `cmd/migrate-toast-archive/main.go` | `toast.SpacesKeyExists` | idempotency check | WIRED | line 129 — guards PutObject call. |
| `cmd/migrate-toast-archive/main.go` | `toast.MetaSourceMigration` + `NewMetaSidecar` | sidecar source distinction | WIRED | line 161 — `toast.NewMetaSidecar("ItemSelectionDetails.csv", toast.MetaSourceMigration)`. |
| `cmd/migrate-toast-archive/main.go` | `photos.NewSpacesClient` | client construction with UsePathStyle:true | WIRED | line 83 — reuses existing factory; no duplicate client setup. |
| `toast.SyncDate` | `toast.dialWithRetry` | SFTP fetch | WIRED | sync.go:51 — only caller; moved from ingest.go in Plan 02. |
| `toast.SyncDate` | `cfg.SpacesClient.PutObject` | upload after cache write | WIRED | sync.go:82-90 — CSV upload; sync.go:108-115 — sidecar upload (best-effort). |
| `toast.SyncDate` | `os.Remove(cachePath)` | cache cleanup on Spaces fail | WIRED | sync.go:88 — preserves `cache ⊆ Spaces` invariant (REVIEW.md WR-01 flags os.Remove failure window — non-blocking warning). |
| `toast.RunIngest` | `cfg.SpacesClient.GetObject` | per-date Spaces read | WIRED | ingest.go:40-43 — only data source for the ingest path. |
| `toast.RunIngest` | `s3types.NoSuchKey` | graceful 404 handling | WIRED | ingest.go:45-50 — `errors.As(err, &nsk)` then `continue`. |
| `toast.worker.runCycle` | `toast.SyncDate` | per-date sync | WIRED | worker.go:116 — loops fromDate..toDate inclusive. |
| `toast.worker.runCycle` | `toast.RunIngest` | post-sync single ingest call | WIRED | worker.go:133 — once per cycle over the full window. |
| `toast.worker.fireDegradedAlert` | `alerts.Queue.Enqueue` | Cliq dispatch | WIRED | worker.go:181 with `Channel: alerts.ChannelZohoCliq`; nil-safe guard at worker.go:176. |
| `toast.runCycle` | `toast.isColdStart` | window selection | WIRED | worker.go:95 — preserved from Phase 22 D-02. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `toast.SyncDate` | csvBytes | SFTP `client.Download(remotePath)` then `io.ReadAll(r)` (sync.go:60-69) | Yes — actual Toast CSV bytes streamed from SFTP and uploaded to Spaces verbatim | FLOWING |
| `toast.RunIngest` | resp.Body → AggregatedRow rows | `cfg.SpacesClient.GetObject` → `parseItemSelectionDetails` → `upsertDayInTx` (ingest.go:40-70) | Yes — Spaces response body parsed into AggregatedRow, written to menu_items + daily_menu_sales via SQL upserts | FLOWING |
| `toast.worker.runCycle` | result.ItemsUpserted/SalesRowsUpserted/Duration | Returned from RunIngest, printed in D-13 log line (worker.go:167-169) | Yes — real counters from upserts | FLOWING |
| `migrate-toast-archive` | uploaded/skipped/missing/warned counters | Per-entry directory walk + Spaces PutObject (line 100-179) | Yes — real archive walk results | FLOWING |
| `toast.worker.fireDegradedAlert` | msg → alerts.Alert.Message | `lastSyncErr` propagated from sync/ingest phase (worker.go:126/139) | Yes — real error chain via `%v` (REVIEW.md confirms aws-sdk error formatter strips signatures) | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Whole backend tree builds | `cd backend && go build ./...` | exit 0 | PASS |
| go vet clean on touched packages | `cd backend && go vet ./internal/toast/... ./cmd/server/... ./cmd/sync-toast/... ./cmd/migrate-toast-archive/...` | exit 0, no output | PASS |
| Existing toast tests pass | `cd backend && go test ./internal/toast/... -count=1` | `ok github.com/yumyums/hq/internal/toast 0.352s` | PASS |
| migrate-toast-archive CLI usage | `go build -o /tmp/mta ./cmd/migrate-toast-archive/ && /tmp/mta` | Prints Usage + `--source is required (no default — see D-08)`, exit non-zero | PASS |
| sync-toast CLI usage | `go build -o /tmp/st ./cmd/sync-toast/ && /tmp/st` | Prints Usage + `--from and --to are required`, exit non-zero | PASS |
| `cache/` is gitignored | `mkdir -p backend/cache/toast/20260530 && touch backend/cache/toast/20260530/ItemSelectionDetails.csv && git check-ignore backend/cache/toast/20260530/ItemSelectionDetails.csv` | prints the path → ignored | PASS |
| Phase 22 SQL contract preserved | `grep -c "ON CONFLICT (master_id) DO UPDATE" backend/internal/toast/ingest.go` and `grep -c "GREATEST(menu_items.last_seen" backend/internal/toast/ingest.go` | both return 1 | PASS |
| Cliq alert message format exact | `grep -c "Toast sync degraded: 3 consecutive failed ticks talking to DO Spaces. Last error:" backend/internal/toast/worker.go` | returns 1 | PASS |

### Requirements Coverage

REQUIREMENTS.md does not exist in this repo; all five plan frontmatter `requirements:` are empty arrays. No requirement IDs to cross-reference. No orphaned requirements detected.

### Anti-Patterns Found

Scanned `backend/internal/toast/{types,config,spaces,meta,sync,ingest,worker}.go`, `backend/cmd/migrate-toast-archive/main.go`, `backend/cmd/server/main.go` (toast block only), `backend/cmd/sync-toast/main.go`.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `backend/internal/toast/sync.go` | 88 | `_ = os.Remove(cachePath)` swallows error | Info | REVIEW.md WR-01 — cache-orphan window when Remove fails; documented in REVIEW with proposed fix. Not blocking; ingest never reads from cache so the invariant matters only for operator forensics. |
| `backend/internal/toast/sync.go` | 124-144 | dialWithRetry doesn't honor ctx (time.Sleep uncancellable) | Info | REVIEW.md IN-01 — caught by reviewer; deferred from scope of this phase. |
| `backend/internal/toast/sync.go` | 127, 138 | 30s slot in `backoffs` slice never reached | Info | REVIEW.md WR-05 — comment says "5s/15s/30s schedule" but only 5s and 15s are ever slept; dead third element. Documentation/code mismatch only. |
| `backend/internal/toast/worker.go` | 17, 22 | `var alertQueue *alerts.Queue` written from main, read from worker goroutine without sync | Info | REVIEW.md WR-02 — lexical ordering (SetAlertQueue precedes StartWorker) makes this race-free today but the contract is implicit. Acceptable; documented in REVIEW. |
| `backend/cmd/migrate-toast-archive/main.go` | 161-175 | Sidecar-only failures become sticky on next idempotent run | Info | REVIEW.md WR-04 — operator must use `--overwrite` to repair. Not blocking; sidecar is operational metadata only (D-03 explicitly says ingest doesn't read it). |
| `backend/internal/toast/worker.go` | 101-105 | Cold-start 90-day backfill stampedes SFTP if `daily_menu_sales` is truncated | Info | REVIEW.md WR-03 — 90 sequential SSH dials per tick when isColdStart=true. Acceptable for one-shot bootstrap; flagged for follow-up phase. |

No blockers. No FIXME/XXX/HACK/PLACEHOLDER comments in any of the touched files. No empty return-statement stubs. No hardcoded test/mock data flowing into production paths.

### Human Verification Required

Four items require live operator testing — see frontmatter for full detail. Summary:

1. **migrate-toast-archive against real archive** — programmatic checks confirm flag parsing, idempotency wiring, sidecar source, and zero-DB invariant. Live DO Spaces upload + HeadObject idempotency on a second run can only be confirmed by an operator with credentials and the actual sales-processor archive.

2. **Full worker tick against real SFTP + Spaces + DB** — pipeline correctness is wired correctly but end-to-end (SFTP → cache → Spaces → DB upserts) requires running the actual server.

3. **3-consecutive-Spaces-failure Cliq alert** — counter logic, latch, and message format are verified programmatically. Live test by simulating Spaces failure across 3+ ticks and confirming exactly one Cliq message lands in the purchaseandinventory channel (and no message on ticks 4+).

4. **Graceful boot with DO_SPACES_* unset** — `WARNING` log + early return are in place. Live test confirms the rest of HQ (HTTP API, receipt worker, login, etc.) remains functional with the toast worker dormant.

### Gaps Summary

None. All 8 observable truths derived from the phase goal + plan frontmatter map to verified artifacts and wired key links. All 11 required artifacts exist with substantive content. All 17 key links resolve. The full backend tree builds cleanly, go vet is silent, and the existing toast test suite passes.

The status is `human_needed` rather than `passed` because four behaviors (live SFTP/Spaces/DB end-to-end, real Cliq channel delivery, real PutObject idempotency, and operational graceful-boot) cannot be confirmed without operator credentials and a live environment. The review (22.1-REVIEW.md) flagged 5 warnings + 6 info items, none of which are gaps against the phase goal — they are refinement opportunities (concurrency annotations, dialWithRetry slot 3, sidecar repair flow, cold-start guardrails) appropriate for a follow-up phase.

---

_Verified: 2026-06-03T09:42:00Z_
_Verifier: Claude (gsd-verifier)_
