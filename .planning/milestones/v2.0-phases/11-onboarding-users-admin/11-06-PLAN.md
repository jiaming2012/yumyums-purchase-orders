---
phase: 11-onboarding-users-admin
plan: 06
type: execute
wave: 4
depends_on: [11-03, 11-05]
files_modified:
  - tests/onboarding.spec.js
  - tests/users.spec.js
autonomous: true
requirements: [ONBD-04, USER-03]

must_haves:
  truths:
    - "Onboarding E2E tests run against real Go server with test database"
    - "Users admin E2E tests validate invite, edit, permissions, and accept-invite flows"
    - "All tests pass with `task test`"
    - "No references to mock data in test files"
  artifacts:
    - path: "tests/onboarding.spec.js"
      provides: "E2E tests for onboarding.html against real API"
      contains: "test("
    - path: "tests/users.spec.js"
      provides: "E2E tests for users.html and login.html against real API"
      contains: "test("
  key_links:
    - from: "tests/onboarding.spec.js"
      to: "/api/v1/onboarding/*"
      via: "Playwright page interactions triggering API calls"
      pattern: "onboarding"
    - from: "tests/users.spec.js"
      to: "/api/v1/users/*"
      via: "Playwright page interactions triggering API calls"
      pattern: "users"
---

<objective>
Rewrite onboarding E2E tests against the real Go server and create new users admin E2E tests. Delete all mock-data-dependent tests.

Purpose: Per D-27, existing 35 onboarding tests must be rewritten against the real API. Users.html has no existing E2E tests and needs them. This ensures both tools are covered before the phase checkpoint.
Output: tests/onboarding.spec.js rewritten, tests/users.spec.js created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-onboarding-users-admin/11-CONTEXT.md
@.planning/phases/11-onboarding-users-admin/11-RESEARCH.md
@.planning/phases/11-onboarding-users-admin/11-03-SUMMARY.md
@.planning/phases/11-onboarding-users-admin/11-05-SUMMARY.md

<interfaces>
<!-- Test infrastructure from Phase 10 -->
playwright.config.js has webServer config that starts Go test server
apiCall(page, method, path, body) helper pattern from workflows.spec.js
Test database created/destroyed per test run via `task test`
Admin login: jamal@yumyums.kitchen (superadmin email from config)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite onboarding.spec.js against real API</name>
  <files>tests/onboarding.spec.js</files>
  <read_first>
    tests/onboarding.spec.js,
    tests/workflows.spec.js,
    playwright.config.js,
    onboarding.html
  </read_first>
  <action>
    Rewrite tests/onboarding.spec.js from scratch (D-27). Delete all existing tests that reference mock data. Write new tests against the real Go server.

    **Setup:** Use the same test infrastructure as workflows.spec.js:
    - `beforeEach`: Navigate to onboarding.html, login as admin (via API call or page login).
    - Helper: `apiCall(page, method, path, body)` using `page.evaluate` to make authenticated fetch calls.
    - Seed: The Go server seeds "Kitchen Basics Training" template on startup (from Plan 04 seed.go). Tests can rely on this existing seed data.

    **Test suite structure** (organized by tab):

    **My Trainings tab:**
    1. `test('shows empty state when no templates assigned')` — No assignments yet, verify "No trainings assigned" text visible.
    2. `test('shows assigned template with progress after assignment')` — Assign template via API (POST /assignTemplate), reload, verify template name appears in My Trainings list.
    3. `test('checkbox progress persists after page reload')` — Open training, check a checkbox item, wait for auto-save (check network idle or small wait), reload page, reopen training, verify checkbox is still checked. (Per CLAUDE.md persistence test requirement.)
    4. `test('video part watched state persists after page reload')` — Open training, mark a video part as watched, reload, verify it's still marked.
    5. `test('section unlocks after completing all items in previous section')` — Complete all items in section 1, verify section 2 becomes active (not locked).
    6. `test('FAQ section shows questions and answers')` — Open training, navigate to FAQ section, verify Q&A pairs are visible.

    **Manager tab:**
    7. `test('manager sees hire with assigned training')` — Create a second user via API, assign template, switch to Manager tab, verify hire appears with progress.
    8. `test('sign-off form requires notes and rating')` — Complete a section as a hire (via API calls for speed), switch to Manager tab, open hire's training, tap "Sign Off Section", verify Confirm button is disabled until notes and rating are filled.
    9. `test('sign-off records attribution on hire view')` — Perform sign-off via Manager tab (fill notes + select rating + confirm), switch to hire's My Trainings view, verify "Signed off by {name}" text appears.

    **Builder tab:**
    10. `test('shows existing seed template in Builder list')` — Switch to Builder tab (admin), verify "Kitchen Basics Training" appears.
    11. `test('can create a new template via Builder')` — Create template with name "Test Template", add a section with a checkbox item, save. Verify it appears in template list.

    Target: ~11 tests covering the core flows. Use `test.describe` blocks for grouping.

    **Important:** Per CLAUDE.md conventions:
    - Tests block service workers: `serviceWorkers: 'block'` in playwright config.
    - Run only new tests during iteration: `npx playwright test tests/onboarding.spec.js -g "test name"`.
    - Do NOT use `MOCK_OB_TEMPLATES` or `MOCK_HIRES` in any test.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/yumyums/hq && npx playwright test tests/onboarding.spec.js --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/onboarding.spec.js does NOT contain "MOCK_OB_TEMPLATES" or "MOCK_HIRES"
    - tests/onboarding.spec.js contains at least 9 test() calls
    - tests/onboarding.spec.js contains `api` or `apiCall` or `fetch` for API interactions
    - tests/onboarding.spec.js contains "Kitchen Basics Training" (seed data reference)
    - tests/onboarding.spec.js contains "Sign Off" in at least one test
    - tests/onboarding.spec.js contains a persistence test (reload + verify state)
    - All tests in onboarding.spec.js pass
  </acceptance_criteria>
  <done>Onboarding E2E tests rewritten against real API. All tests pass. Mock data references eliminated.</done>
</task>

<task type="auto">
  <name>Task 2: Create users.spec.js E2E tests for users admin + invite flow</name>
  <files>tests/users.spec.js</files>
  <read_first>
    tests/workflows.spec.js,
    users.html,
    login.html,
    playwright.config.js
  </read_first>
  <action>
    Create tests/users.spec.js with E2E tests for the users admin and invite acceptance flows.

    **Setup:** Same infrastructure as onboarding.spec.js — login as admin, use apiCall helper.

    **Test suite:**

    **User List:**
    1. `test('shows admin user in user list on load')` — Navigate to users.html, verify the logged-in admin user appears in the list.
    2. `test('shows skeleton loading state')` — Intercept the /api/v1/users request to delay it, verify skeleton rows appear, then let it resolve.

    **Invite Flow:**
    3. `test('can create new user via invite form')` — Tap "Add Crew Member", fill first name "Test", last name "User", email "test@yumyums.kitchen", role "Member", submit. Verify invite link panel appears with a URL. Verify user appears in list with "Invited" badge.
    4. `test('invite link copy button works')` — After invite creation, tap "Copy Link", verify button text changes to "Copied!" temporarily.

    **Accept Invite Flow:**
    5. `test('accept-invite page shows welcome heading')` — Create user via API, get invite path, navigate to login.html with ?token= param. Verify "Welcome, Test" heading appears. Verify password fields are visible. Verify email field is NOT visible.
    6. `test('accept-invite sets password and redirects to index')` — On the accept-invite page, enter matching passwords, submit. Verify redirect to index.html. Verify user can now log in with new credentials.
    7. `test('expired token shows error on accept-invite page')` — Navigate to login.html?token=invalid_token. Verify error message "This invite link has expired" appears.

    **Edit User:**
    8. `test('can edit user name and role')` — Create user via API, open edit form, change first name, save. Verify updated name appears in list.
    9. `test('nickname collision shows 409 error')` — Create two users. Edit second user, set nickname to first user's display_name. Verify inline error appears under nickname field with "already taken" message.

    **Destructive Actions:**
    10. `test('force logout revokes user sessions')` — Create and activate a user. Open edit form, tap "Force Logout", confirm dialog. Verify no error.
    11. `test('delete user removes from list')` — Create user via API, verify in list, open edit, tap delete, confirm. Verify user removed from list.

    **Password Reset:**
    12. `test('reset password generates new token link')` — Create and activate user, open edit, tap "Reset Password". Verify reset link panel appears with heading "Password Reset Link".

    Target: ~12 tests. Use `test.describe` blocks.

    **Important:** For accept-invite tests, the test needs to:
    1. Create user via admin API (POST /api/v1/users/invite) and capture the invite_path from response.
    2. Navigate to that path (login.html?token=...) in a new context or after logging out.
    3. Set password and verify redirect.

    Handle auth context carefully — the admin is logged in, but accept-invite needs an unauthenticated page. Use `page.context().clearCookies()` before navigating to accept-invite URL, or use a separate browser context.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/yumyums/hq && npx playwright test tests/users.spec.js --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/users.spec.js exists with at least 10 test() calls
    - tests/users.spec.js contains "Add Crew Member" or "invite" in test names
    - tests/users.spec.js contains "accept-invite" or "Welcome" in test names
    - tests/users.spec.js contains "Force Logout" in at least one test
    - tests/users.spec.js contains "nickname" or "collision" in at least one test
    - tests/users.spec.js does NOT contain "const USERS = [" mock data
    - All tests in users.spec.js pass
  </acceptance_criteria>
  <done>Users admin E2E tests cover invite, accept-invite, edit, permissions, force logout, delete, and password reset flows. All pass.</done>
</task>

</tasks>

<verification>
- `npx playwright test tests/onboarding.spec.js` — all pass
- `npx playwright test tests/users.spec.js` — all pass
- No mock data references in either test file
- `task test` runs full suite without failures
</verification>

<success_criteria>
- ~11 onboarding tests cover: My Trainings, progress persistence, section unlock, sign-off, Builder
- ~12 users tests cover: list, invite, accept-invite, edit, nickname collision, force logout, delete, reset password
- All tests run against real Go server with test database
- Zero references to mock data arrays
</success_criteria>

<output>
After completion, create `.planning/phases/11-onboarding-users-admin/11-06-SUMMARY.md`
</output>
