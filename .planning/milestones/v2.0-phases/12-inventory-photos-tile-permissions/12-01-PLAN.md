---
phase: 12-inventory-photos-tile-permissions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/db/migrations/0024_inventory.sql
  - backend/internal/db/migrations/0025_pending_purchases.sql
  - backend/internal/inventory/types.go
  - backend/internal/inventory/handler.go
  - backend/internal/inventory/service.go
  - backend/config/fixtures/purchase_item_groups.yaml
  - backend/cmd/server/main.go
autonomous: true
requirements: [INVT-01]

must_haves:
  truths:
    - "Vendors, item_groups, tags, purchase_items, purchase_events, purchase_line_items tables exist in Postgres"
    - "pending_purchases table exists for the receipt review queue"
    - "YAML seed data populates vendors, item_groups, tags, and purchase_items on startup"
    - "GET /api/v1/purchases returns purchase events with nested line items"
    - "GET /api/v1/vendors returns vendor list"
    - "GET /api/v1/stock returns stock levels computed from purchase data"
    - "hq_apps table contains an 'inventory' row"
  artifacts:
    - path: "backend/internal/db/migrations/0024_inventory.sql"
      provides: "Inventory schema (vendors, item_groups, tags, purchase_items, purchase_events, purchase_line_items)"
      contains: "CREATE TABLE vendors"
    - path: "backend/internal/db/migrations/0025_pending_purchases.sql"
      provides: "Pending purchases review queue table"
      contains: "CREATE TABLE pending_purchases"
    - path: "backend/internal/inventory/handler.go"
      provides: "HTTP handlers for inventory CRUD"
      exports: ["ListPurchaseEventsHandler", "ListVendorsHandler", "GetStockHandler", "CreatePurchaseEventHandler"]
    - path: "backend/internal/inventory/types.go"
      provides: "Domain types for inventory"
      contains: "type PurchaseEvent struct"
    - path: "backend/internal/inventory/service.go"
      provides: "Seed function, stock computation"
      contains: "func SeedInventoryFixtures"
    - path: "backend/config/fixtures/purchase_item_groups.yaml"
      provides: "YAML seed data for vendors, item groups, tags, purchase items"
  key_links:
    - from: "backend/cmd/server/main.go"
      to: "backend/internal/inventory/handler.go"
      via: "route registration"
      pattern: "inventory\\.List"
    - from: "backend/internal/inventory/service.go"
      to: "backend/config/fixtures/purchase_item_groups.yaml"
      via: "YAML embed and load"
      pattern: "SeedInventoryFixtures"

user_setup:
  - service: do-spaces
    why: "Photo and receipt file storage"
    env_vars:
      - name: DO_SPACES_KEY
        source: "DO Dashboard -> Spaces -> Manage Keys"
      - name: DO_SPACES_SECRET
        source: "DO Dashboard -> Spaces -> Manage Keys"
      - name: DO_SPACES_BUCKET
        source: "Create bucket 'yumyums-hq' in DO Dashboard -> Spaces"
      - name: DO_SPACES_REGION
        source: "Region where bucket was created (e.g. nyc3)"
    dashboard_config:
      - task: "Set CORS policy on bucket to allow PUT from app origin"
        location: "DO Dashboard -> Spaces -> yumyums-hq -> Settings -> CORS"
---

<objective>
Create the inventory database schema, Go CRUD handlers, YAML seed data, and route wiring so that purchase events, vendors, and stock levels are persisted in Postgres and accessible via API.

Purpose: Replace all mock data in inventory.html with real Postgres-backed endpoints — this is the foundation for the inventory API swap and receipt pipeline.
Output: Two goose migrations, inventory Go package (types, handlers, service), YAML fixtures, routes registered in main.go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-inventory-photos-tile-permissions/12-CONTEXT.md
@.planning/phases/12-inventory-photos-tile-permissions/12-RESEARCH.md

@backend/cmd/server/main.go
@backend/internal/workflow/handler.go
@backend/internal/db/migrations/0023_multi_role.sql
@backend/internal/db/seed.go

<interfaces>
<!-- Existing patterns the executor needs -->

From backend/internal/workflow/handler.go:
```go
func ListTemplatesHandler(pool *pgxpool.Pool) http.HandlerFunc { ... }
func CreateTemplateHandler(pool *pgxpool.Pool) http.HandlerFunc { ... }
// All handlers: accept pool, return http.HandlerFunc, use auth.UserFromContext
```

From backend/cmd/server/main.go:
```go
// Route registration pattern:
r.Route("/api/v1", func(r chi.Router) {
    r.Use(auth.SessionMiddleware(pool, cfg.CookieDomain))
    r.Route("/workflow", func(r chi.Router) {
        r.Get("/templates", workflow.ListTemplatesHandler(pool))
        // ...
    })
})
```

From backend/internal/db/seed.go:
```go
func SeedHQApps(ctx context.Context, pool *pgxpool.Pool) error { ... }
// Pattern: INSERT ... ON CONFLICT (slug) DO NOTHING
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create inventory DB migrations and types</name>
  <files>
    backend/internal/db/migrations/0024_inventory.sql,
    backend/internal/db/migrations/0025_pending_purchases.sql,
    backend/internal/inventory/types.go
  </files>
  <read_first>
    backend/internal/db/migrations/0023_multi_role.sql,
    backend/internal/db/migrations/0021_ob_signoffs.sql,
    backend/internal/workflow/handler.go
  </read_first>
  <action>
    Create migration 0024_inventory.sql with goose Up/Down markers and BEGIN/COMMIT wrappers (per D-11). Tables:
    - vendors (id UUID PK, name TEXT UNIQUE NOT NULL, created_at TIMESTAMPTZ DEFAULT now())
    - tags (id UUID PK, name TEXT UNIQUE NOT NULL)
    - item_groups (id UUID PK, name TEXT NOT NULL, par_days INTEGER)
    - item_group_tags (group_id UUID FK, tag_id UUID FK, PK composite)
    - purchase_items (id UUID PK, description TEXT UNIQUE NOT NULL, group_id UUID FK nullable)
    - purchase_events (id UUID PK, vendor_id UUID FK NOT NULL, bank_tx_id TEXT UNIQUE NOT NULL, event_date DATE NOT NULL, tax NUMERIC(10,2) DEFAULT 0, total NUMERIC(10,2) NOT NULL, receipt_url TEXT nullable, created_at TIMESTAMPTZ DEFAULT now())
    - purchase_line_items (id UUID PK, purchase_event_id UUID FK NOT NULL ON DELETE CASCADE, purchase_item_id UUID FK nullable, description TEXT NOT NULL, quantity INTEGER NOT NULL, price NUMERIC(10,4) NOT NULL, is_case BOOLEAN DEFAULT false)
    - Indexes: purchase_events(vendor_id), purchase_events(event_date DESC), purchase_line_items(purchase_event_id)
    - Also INSERT INTO hq_apps: ('inventory', 'Inventory', '📦', true) ON CONFLICT (slug) DO NOTHING

    Create migration 0025_pending_purchases.sql:
    - pending_purchases (id UUID PK, bank_tx_id TEXT NOT NULL, bank_total NUMERIC(10,2) NOT NULL, vendor TEXT NOT NULL, event_date DATE, tax NUMERIC(10,2), total NUMERIC(10,2), total_units INTEGER, total_cases INTEGER, receipt_url TEXT, reason TEXT, items JSONB NOT NULL DEFAULT '[]', confirmed_at TIMESTAMPTZ nullable, confirmed_by UUID FK nullable, discarded_at TIMESTAMPTZ nullable, created_at TIMESTAMPTZ DEFAULT now())
    - Use JSONB `items` column for line items array instead of denormalized rows — cleaner for the review queue
    - Index on bank_tx_id

    Create backend/internal/inventory/types.go:
    - Vendor struct {ID, Name, CreatedAt}
    - Tag struct {ID, Name}
    - ItemGroup struct {ID, Name, ParDays, Tags []Tag}
    - PurchaseItem struct {ID, Description, GroupID}
    - PurchaseEvent struct {ID, VendorID, VendorName, BankTxID, EventDate, Tax, Total, ReceiptURL, CreatedAt, LineItems []LineItem}
    - LineItem struct {ID, PurchaseEventID, PurchaseItemID, Description, Quantity, Price, IsCase}
    - PendingPurchase struct {ID, BankTxID, BankTotal, Vendor, EventDate, Tax, Total, TotalUnits, TotalCases, ReceiptURL, Reason, Items json.RawMessage, ConfirmedAt, ConfirmedBy, DiscardedAt, CreatedAt}
    - StockItem struct {Description, GroupName, TotalQuantity, TotalSpend, LastPurchaseDate, AvgPrice}
    - Input structs: CreatePurchaseEventInput, ConfirmPendingInput
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/yumyums/hq/backend && go build ./internal/inventory/...</automated>
  </verify>
  <acceptance_criteria>
    - File 0024_inventory.sql contains "CREATE TABLE vendors" and "CREATE TABLE purchase_events" and "CREATE TABLE purchase_line_items"
    - File 0024_inventory.sql contains "INSERT INTO hq_apps" with slug 'inventory'
    - File 0025_pending_purchases.sql contains "CREATE TABLE pending_purchases"
    - types.go compiles with `go build ./internal/inventory/...`
    - All tables use UUID PKs with gen_random_uuid() default
    - All migrations have -- +goose Up and -- +goose Down markers
  </acceptance_criteria>
  <done>Two migration files and types.go compile. Schema matches D-11 requirements with vendors, item_groups, tags, purchase_items, purchase_events, purchase_line_items, and pending_purchases tables.</done>
</task>

<task type="auto">
  <name>Task 2: Create inventory handlers, service, seed data, and route wiring</name>
  <files>
    backend/internal/inventory/handler.go,
    backend/internal/inventory/service.go,
    backend/config/fixtures/purchase_item_groups.yaml,
    backend/cmd/server/main.go
  </files>
  <read_first>
    backend/internal/inventory/types.go,
    backend/internal/workflow/handler.go,
    backend/internal/db/seed.go,
    backend/cmd/server/main.go,
    /Users/jamal/projects/yumyums/baserow-revenue-api/fixtures/purchase_item_groups.yaml
  </read_first>
  <action>
    Create handler.go with RPC-style handlers matching existing workflow pattern (accept *pgxpool.Pool, return http.HandlerFunc, use auth.UserFromContext):
    - ListVendorsHandler: GET /api/v1/inventory/vendors — returns all vendors ordered by name
    - ListPurchaseEventsHandler: GET /api/v1/inventory/purchases — accepts ?vendor_id and ?page query params, LIMIT 50 per page, returns events with nested line items via LEFT JOIN, ordered by event_date DESC
    - GetStockHandler: GET /api/v1/inventory/stock — aggregates purchase_line_items grouped by purchase_item description: total qty, total spend, avg price, last purchase date. Joins item_groups for group name.
    - CreatePurchaseEventHandler: POST /api/v1/inventory/purchases — accepts CreatePurchaseEventInput with vendor_id, bank_tx_id, event_date, tax, total, line_items[]. Inserts event + line items in a transaction.
    - ListPendingPurchasesHandler: GET /api/v1/inventory/purchases/pending — returns pending_purchases WHERE confirmed_at IS NULL AND discarded_at IS NULL, ordered by created_at DESC
    - ConfirmPendingPurchaseHandler: POST /api/v1/inventory/purchases/confirm — accepts {id, vendor_name, line_items corrections}, creates real purchase_event + line_items from pending data, sets confirmed_at/confirmed_by on pending row
    - DiscardPendingPurchaseHandler: POST /api/v1/inventory/purchases/discard — accepts {id}, sets discarded_at on pending row

    Create service.go:
    - SeedInventoryFixtures(ctx, pool) — loads purchase_item_groups.yaml via embed.FS, inserts vendors/tags/item_groups/purchase_items with ON CONFLICT DO NOTHING. Follow SeedHQApps pattern.
    - Use //go:embed ../../../config/fixtures/purchase_item_groups.yaml

    Create purchase_item_groups.yaml (per D-15) with real food truck data:
    - Vendors: Restaurant Depot, US Foods, Sysco, Costco, Local Farm
    - Item groups with tags: Proteins (salmon, chicken, beef), Produce (lettuce, tomatoes, onions), Dairy (cheese, cream, butter), Dry Goods (rice, oil, spices), Packaging (containers, napkins, bags)

    Wire routes in main.go:
    - Add import "github.com/yumyums/hq/internal/inventory"
    - Add r.Route("/inventory", func(r chi.Router) { ... }) inside the /api/v1 group
    - Call inventory.SeedInventoryFixtures at startup (after SeedHQApps call)
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/yumyums/hq/backend && go build ./cmd/server/...</automated>
  </verify>
  <acceptance_criteria>
    - handler.go exports ListVendorsHandler, ListPurchaseEventsHandler, GetStockHandler, CreatePurchaseEventHandler, ListPendingPurchasesHandler, ConfirmPendingPurchaseHandler, DiscardPendingPurchaseHandler
    - service.go exports SeedInventoryFixtures
    - main.go contains `r.Route("/inventory"` inside the /api/v1 group
    - main.go calls inventory.SeedInventoryFixtures
    - purchase_item_groups.yaml contains at least 5 vendors and 5 item groups
    - `go build ./cmd/server/...` succeeds
  </acceptance_criteria>
  <done>Inventory CRUD API is compiled and routed. GET /vendors, GET /purchases, GET /stock, POST /purchases, GET /purchases/pending, POST /purchases/confirm, POST /purchases/discard all have handlers. Seed data loads on startup.</done>
</task>

</tasks>

<verification>
- `go build ./cmd/server/...` compiles without errors
- Migration files exist at 0024 and 0025 with proper goose markers
- All 7 inventory route handlers are registered in main.go
- YAML fixtures file exists with food truck vendor/item data
</verification>

<success_criteria>
- Backend compiles with all inventory types, handlers, service, and route wiring
- Two goose migrations define the complete inventory schema
- Seed function loads YAML fixtures idempotently on startup
- hq_apps table will contain an 'inventory' row after migration
</success_criteria>

<output>
After completion, create `.planning/phases/12-inventory-photos-tile-permissions/12-01-SUMMARY.md`
</output>
