# Phase 10.2: Reactive Sync Framework - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-17
**Phase:** 10.2-reactive-sync-framework
**Areas discussed:** File structure, Migration scope, Store API surface, Backend /ops endpoint

---

## File Structure

| Option | Description | Selected |
|--------|-------------|----------|
| Single shared file (store.js) | ~200 LOC, Store + LamportClock + WS + submitOp + idb in one file | |
| Single sync.js file | ~300 LOC, same contents but named for purpose. Page-specific stays inline. | ✓ |
| Multiple files (lib/) | store.js + sync.js + idb.js in lib/ directory, 3 script tags per page | |

**User's choice:** Single sync.js file (Option B)
**Notes:** Matches existing ptr.js shared-file pattern. No build step needed.

---

## Migration Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Big-bang rewrite | Gut workflows.html in one pass, fix all tests at once | |
| Extract-then-migrate | 3 steps: create sync.js, wire workflows.html, add POST /ops. Green tests at each step. | ✓ |
| Facade pattern | Old globals become thin wrappers around Store. Zero risk but throwaway code. | |

**User's choice:** Extract-then-migrate (Option B)
**Notes:** Each step must have all 166 tests passing before moving to the next.

---

## Store API Surface

| Option | Description | Selected |
|--------|-------------|----------|
| Pure store, no computed | Subscribers calculate derived values themselves. Store stays ~100 LOC. | ✓ |
| Store with computed/derived | store.computed() with dependency tracking and caching | |

**User's choice:** Pure store (Option A)
**Notes:** User noted A and C (derive helpers) were identical. Went with A — extract helpers naturally when reused.

---

## Backend /ops Endpoint

| Option | Description | Selected |
|--------|-------------|----------|
| Single POST /ops replaces all | Remove old endpoints, only POST /ops exists | Tech debt |
| Keep old + add /ops alongside | Two write paths coexist indefinitely | |
| POST /ops primary, old become wrappers | Old endpoints delegate to OpHandler internally | ✓ |

**User's choice:** Option C — POST /ops primary, old endpoints as thin wrappers
**Notes:** Option A (remove old endpoints) noted as tech debt for future cleanup.

---

## Claude's Discretion

- Store class internals
- IndexedDB version strategy
- api() wrapper placement
- SW rebuild timing
- Test organization

## Deferred Ideas

- Fine-grained key-level subscriptions (Store Option A from earlier discussion)
- Remove old write endpoints entirely
