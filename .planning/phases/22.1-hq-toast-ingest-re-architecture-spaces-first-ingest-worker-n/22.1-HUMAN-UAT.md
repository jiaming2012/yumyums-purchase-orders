---
status: resolved
phase: 22.1-hq-toast-ingest-re-architecture-spaces-first-ingest-worker-n
source: [22.1-VERIFICATION.md]
started: 2026-06-03T09:45:00Z
updated: 2026-06-03T13:10:00Z
---

## Current Test

[all tests resolved — 3 passed, 1 skipped by operator decision]

## Tests

### 1. migrate-toast-archive against a real sales-processor archive
expected: Walks YYYYMMDD subdirs, uploads each ItemSelectionDetails.csv + sidecar to toast/YYYYMMDD/ in DO Spaces, skips dates whose CSV key already exists (HeadObject hit), prints `done. uploaded=N skipped=M missing=K warned=W`. Second run without --overwrite shows uploaded=0 and skipped=N (idempotent).
result: passed
notes: |
  Ran via `task backend:migrate-toast:90-days` (90-day window matching worker backfill).
  First run: uploaded=64 skipped=0 missing=0 warned=0 (64 date dirs from 2026-03-05 → 2026-05-31).
  Second run: uploaded=0 skipped=64 missing=0 warned=0 — idempotence via HeadObject confirmed.
  Note: only the CSV idempotence path was exercised — the sidecar gap from REVIEW.md WR-04
  has not been triggered.

### 2. Boot the server with valid DO_SPACES_* + TOAST_SFTP_KEY_PATH and observe a full worker tick
expected: Log shows `toast worker: starting (interval=12h, window=7d, backfill=90d)`. After one tick: `toast sync: wrote YYYYMMDD ...` lines for dates present on Toast SFTP, `toast sync: skip YYYYMMDD (not in SFTP)` for missing dates, and one final `toast ingest: dates=[...] items_upserted=N sales_rows_upserted=M duration=... synced=K` line. Verify cache files appear under backend/cache/toast/YYYYMMDD/ AND keys exist in DO Spaces at toast/YYYYMMDD/.
result: passed
notes: |
  Final tick line: `toast ingest: dates=[20260527..20260603] items_upserted=63 sales_rows_upserted=63 duration=2.425259041s synced=7`.
  Fresh SFTP writes for 7 dates (20260527→20260602) + skip for 20260603 (today, not in SFTP, not yet in Spaces).
  Two follow-up improvements shipped during verification:
    1. Parser fix: embedded duplicate header rows in sales-processor's concatenated daily files
       were crashing every ingest. Defensive skip + regression test in parser_test.go.
       (commit b24375e)
    2. Server boot now ends with `=== Yumyums HQ ready — accepting connections ===` after
       the listener is bound, eliminating the race with background worker startup logs.
       (commit c12f614)
  SFTP-miss log lines were also enriched to distinguish archived-in-Spaces from truly-missing
  during this UAT cycle (commit 9d35c3a).

### 3. Simulate Spaces failure across 3+ ticks
expected: Each tick: `ERROR toast sync: YYYYMMDD: ...` per date, then `toast worker: consecutive Spaces failures = 1`, `... = 2`, `... = 3`. On the 3rd tick, exactly one Zoho Cliq message is sent to the purchaseandinventory channel containing `Toast sync degraded: 3 consecutive failed ticks talking to DO Spaces. Last error: ...`. On the 4th and 5th failing tick: no additional Cliq message. After Spaces is restored on the 6th tick: `toast worker: Spaces recovered after 5 consecutive failures` and counter resets to 0.
result: skipped
notes: |
  Skipped by operator decision (2026-06-03). Rationale:
  - Code-review (22.1-REVIEW.md) explicitly reviewed the alert latch logic and marked it
    "correct as written" — threshold check `>= 3 && !alertFiredAtThreshold` fires exactly
    once per failure storm; counters reset on clean tick.
  - Live test requires either (a) ~36h at 12h interval, or (b) a controllable Spaces front
    (MinIO container, traffic block) — both higher friction than the risk warrants for a
    failure-path alert.
  - To revisit: shrink TOAST_SYNC_INTERVAL to 15s, set DO_SPACES_SECRET to invalid string,
    observe 3 ticks. Recovery line still requires a mid-process Spaces flip.
  Tracked behavior remains a follow-up candidate if a real Spaces outage occurs in the wild.

### 4. Boot the server with DO_SPACES_* env vars unset
expected: Log line `WARNING: toast worker: DO Spaces not configured — ingest disabled (server continues)` appears once at boot. Receipt worker, HTTP API, login, and all other subsystems continue functioning normally. No panic, no log.Fatalf, no exit. The /api/v1/inventory/menu-items endpoint still serves any pre-existing rows from menu_items.
result: passed
notes: |
  All criteria met on live test (2026-06-03T13:04:55Z boot):
  - WARNING toast worker line printed exactly once
  - Receipt worker continued: `processed 12 transactions, 0 auto-created, 0 pending review, 12 already cached`
  - Ready sentinel `=== Yumyums HQ ready — accepting connections ===` fired
  - Login returned valid session for superadmin
  - /api/v1/inventory/menu-items returned real rows (e.g., Soda master_id=5740053574711822,
    units_sold_this_week=5) — confirms the full migrate→sync→ingest pipeline data
    survives a Spaces-degraded boot
  - No panic, no log.Fatalf, no process exit

## Summary

total: 4
passed: 3
issues: 0
pending: 0
skipped: 1
blocked: 0

## Gaps
