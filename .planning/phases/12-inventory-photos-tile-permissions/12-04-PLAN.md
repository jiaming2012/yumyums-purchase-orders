---
phase: 12-inventory-photos-tile-permissions
plan: 04
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - inventory.html
  - sw.js
autonomous: true
requirements: [INVT-02]

must_haves:
  truths:
    - "All 8 MOCK_ arrays deleted from inventory.html"
    - "History tab shows purchase events from GET /api/v1/inventory/purchases"
    - "Stock tab shows stock levels from GET /api/v1/inventory/stock"
    - "Vendor filter dropdown populated from GET /api/v1/inventory/vendors"
    - "Loading shows skeleton state; errors show inline retry"
    - "Empty states match UI-SPEC copywriting contract"
  artifacts:
    - path: "inventory.html"
      provides: "API-backed inventory UI"
      contains: "api/v1/inventory"
  key_links:
    - from: "inventory.html"
      to: "/api/v1/inventory/purchases"
      via: "fetch in loadHistory"
      pattern: "api/v1/inventory/purchases"
    - from: "inventory.html"
      to: "/api/v1/inventory/stock"
      via: "fetch in loadStock"
      pattern: "api/v1/inventory/stock"
    - from: "inventory.html"
      to: "/api/v1/inventory/vendors"
      via: "fetch in loadVendors"
      pattern: "api/v1/inventory/vendors"
---

<objective>
Big-bang swap of inventory.html from mock data to real API. Delete all 8 MOCK_ arrays and replace with fetch calls to the inventory API endpoints created in Plan 01.

Purpose: History, Stock, Trends, and Cost tabs will show real data from Postgres instead of hardcoded arrays (per D-12, D-13).
Output: Fully API-backed inventory.html with skeleton loading, error states, and pagination.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-inventory-photos-tile-permissions/12-CONTEXT.md
@.planning/phases/12-inventory-photos-tile-permissions/12-RESEARCH.md
@.planning/phases/12-inventory-photos-tile-permissions/12-UI-SPEC.md
@.planning/phases/12-inventory-photos-tile-permissions/12-01-SUMMARY.md

@inventory.html

<interfaces>
<!-- API endpoints from Plan 01 -->
<!-- GET /api/v1/inventory/vendors → [{id, name, created_at}] -->
<!-- GET /api/v1/inventory/purchases?vendor_id=X&page=N → [{id, vendor_id, vendor_name, bank_tx_id, event_date, tax, total, receipt_url, created_at, line_items: [{id, description, quantity, price, is_case}]}] -->
<!-- GET /api/v1/inventory/stock → [{description, group_name, total_quantity, total_spend, last_purchase_date, avg_price}] -->
<!-- GET /api/v1/inventory/purchases/pending → [{id, bank_tx_id, bank_total, vendor, event_date, ...}] -->

From workflows.html (api() wrapper pattern to reuse):
```javascript
async function api(path, opts) {
  const res = await fetch(path, opts);
  if (res.status === 401) { window.location.href = '/login.html'; throw new Error('unauthorized'); }
  if (res.status === 204) return null;
  const data = await res.json();
  if (!res.ok) throw Object.assign(new Error(data.error || 'api_error'), {status: res.status, data});
  return data;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all mock data with API calls in inventory.html</name>
  <files>inventory.html</files>
  <read_first>
    inventory.html,
    workflows.html
  </read_first>
  <action>
    Big-bang mock-to-API swap for inventory.html (per D-12). This is the same pattern used in Phase 10 (workflows) and Phase 11 (onboarding/users).

    1. DELETE all 8 MOCK_ arrays: MOCK_TAGS, MOCK_VENDORS, MOCK_ITEM_GROUPS, MOCK_PURCHASE_ITEMS, MOCK_PURCHASE_EVENTS, MOCK_PURCHASES, MOCK_MENU_ITEMS, MOCK_SALES

    2. ADD the api() wrapper function (identical to workflows.html pattern — handles 401 redirect, 204 short-circuit, JSON error parse)

    3. Replace data sources for each tab:

    **History tab:**
    - Add loadHistory() async function: fetch GET /api/v1/inventory/purchases with ?vendor_id filter and ?page pagination
    - Show .skeleton while loading
    - On success: render purchase event cards using existing .event-card pattern, with nested line items
    - On error: show .inline-error "Couldn't load purchases. [Retry]"
    - Empty state: "No purchases yet" / "Purchase events will appear here once the receipt pipeline syncs." (per UI-SPEC)
    - Add "Load more" button at bottom for pagination (page param)
    - Pending review items render at TOP of list with .approval-badge "Needs Review" (fetch from /purchases/pending)
    - Pending count shown in tab label: "History (N)" using warn color

    **Stock tab:**
    - Add loadStock() async function: fetch GET /api/v1/inventory/stock
    - Show .skeleton while loading
    - On success: render stock items with existing stock status badges (high/medium/low based on quantity vs par days)
    - On error: show .inline-error with retry
    - Empty state: "No stock data" / "Stock levels are computed from purchase history. Add purchase events to see stock." (per UI-SPEC)

    **Vendor filter:**
    - Add loadVendors() async function: fetch GET /api/v1/inventory/vendors
    - Populate .filter-select dropdown with vendor names
    - On vendor select: re-fetch /purchases with vendor_id filter

    **Trends / Cost tabs:**
    - Keep .coming-soon state — sales data deferred (per D-13). Display "Coming Soon" as before but update copy to: "Trends require sales data integration. Coming in a future update."

    4. Call loadVendors() + loadHistory() + loadStock() on DOMContentLoaded. Each tab loads independently (no waterfall).

    5. Add auth check: call api('/api/v1/me') at page load — redirect to login.html if 401 (same pattern as other pages).
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/yumyums/hq && grep -c "MOCK_" inventory.html && grep -c "api/v1/inventory" inventory.html</automated>
  </verify>
  <acceptance_criteria>
    - MOCK_TAGS, MOCK_VENDORS, MOCK_ITEM_GROUPS, MOCK_PURCHASE_ITEMS, MOCK_PURCHASE_EVENTS, MOCK_PURCHASES, MOCK_MENU_ITEMS, MOCK_SALES — all 8 deleted (grep MOCK_ returns 0)
    - inventory.html contains fetch calls to /api/v1/inventory/purchases, /api/v1/inventory/stock, /api/v1/inventory/vendors, /api/v1/inventory/purchases/pending
    - inventory.html contains api() wrapper function
    - "No purchases yet" empty state text exists
    - "No stock data" empty state text exists
    - .skeleton loading class used
    - .inline-error with retry pattern used
    - "Needs Review" badge text exists for pending items
  </acceptance_criteria>
  <done>All mock data deleted. History tab shows real purchase events with pagination and pending review badges. Stock tab shows computed stock levels. Vendor filter works. Loading skeletons and error states implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild service worker</name>
  <files>sw.js</files>
  <read_first>build-sw.js</read_first>
  <action>
    Run `node build-sw.js` to regenerate sw.js with updated content hash for inventory.html.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/yumyums/hq && node build-sw.js && grep "inventory.html" sw.js</automated>
  </verify>
  <acceptance_criteria>
    - sw.js contains precache entry for inventory.html with updated revision hash
  </acceptance_criteria>
  <done>Service worker rebuilt with updated inventory.html hash.</done>
</task>

</tasks>

<verification>
- Zero MOCK_ arrays remain in inventory.html
- All 4 API fetch calls present (/purchases, /stock, /vendors, /purchases/pending)
- SW rebuilt with new hashes
</verification>

<success_criteria>
- inventory.html is fully API-backed with no mock data
- History tab shows purchase events with pagination and pending review badges
- Stock tab shows computed stock levels
- Proper loading, error, and empty states per UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/12-inventory-photos-tile-permissions/12-04-SUMMARY.md`
</output>
