---
phase: 11-onboarding-users-admin
plan: 05
type: execute
wave: 3
depends_on: [11-04]
files_modified:
  - onboarding.html
  - sw.js
  - build-sw.js
autonomous: true
requirements: [ONBD-02, ONBD-03, ONBD-04]

must_haves:
  truths:
    - "onboarding.html loads templates from GET /api/v1/onboarding/myTrainings — no mock data"
    - "Checking a training item auto-saves to server via POST /api/v1/onboarding/saveProgress"
    - "Watching a video part marks it complete via saveProgress"
    - "Section state (locked/active/complete/signed_off) reflects server-computed state"
    - "Manager can sign off on a section with notes + rating via inline form"
    - "Sign-off attribution (name + timestamp) shown inline on hire's view"
    - "Builder tab creates/edits templates via API"
    - "Progress persists across page refreshes"
  artifacts:
    - path: "onboarding.html"
      provides: "Onboarding UI wired to real API"
      min_lines: 900
    - path: "sw.js"
      provides: "Updated service worker with new file hashes"
  key_links:
    - from: "onboarding.html"
      to: "/api/v1/onboarding/myTrainings"
      via: "api() fetch wrapper on tab load"
      pattern: "api\\('GET',\\s*'/api/v1/onboarding/myTrainings'"
    - from: "onboarding.html"
      to: "/api/v1/onboarding/saveProgress"
      via: "api() POST on checkbox/video tap"
      pattern: "api\\('POST',\\s*'/api/v1/onboarding/saveProgress'"
    - from: "onboarding.html"
      to: "/api/v1/onboarding/signOff"
      via: "api() POST on sign-off confirm"
      pattern: "api\\('POST',\\s*'/api/v1/onboarding/signOff'"
---

<objective>
Big-bang mock-to-API swap for onboarding.html. Replace MOCK_OB_TEMPLATES, SECTION_STATES, MOCK_HIRES, OB_CHECKS, and FAQ_VIEWED with API calls to the onboarding backend. Add sign-off form inline UI. Rebuild service worker.

Purpose: Implements ONBD-04 (onboarding.html fetches from API) plus the frontend parts of ONBD-02 (progress saving) and ONBD-03 (sign-off journal display).
Output: onboarding.html fully API-backed, rebuilt sw.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-onboarding-users-admin/11-CONTEXT.md
@.planning/phases/11-onboarding-users-admin/11-RESEARCH.md
@.planning/phases/11-onboarding-users-admin/11-UI-SPEC.md
@.planning/phases/11-onboarding-users-admin/11-04-SUMMARY.md

<interfaces>
<!-- API endpoints from Plan 04 -->
GET /api/v1/onboarding/myTrainings → [{template_id, template_name, progress_pct, sections_complete, sections_total}]
GET /api/v1/onboarding/hireTraining/{hireId}?templateId={id} → {template, sections: [{id, title, items, state, signoff: {manager_name, signed_off_at} | null}]}
GET /api/v1/onboarding/managerHires → [{user_id, display_name, assigned_templates: [{template_id, template_name, progress_pct}]}]
POST /api/v1/onboarding/saveProgress {item_id, progress_type, checked} → 200
POST /api/v1/onboarding/signOff {section_id, hire_id, notes, rating} → 200
GET /api/v1/onboarding/templates → [{id, name, role, sections: [...]}] (for Builder)
POST /api/v1/onboarding/createTemplate {name, role, sections: [...]} → {id}
PUT /api/v1/onboarding/updateTemplate/{id} {name, role, sections: [...]} → 200
POST /api/v1/onboarding/assignTemplate {hire_id, template_id} → 200
POST /api/v1/onboarding/unassignTemplate {hire_id, template_id} → 200
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Big-bang mock-to-API swap for onboarding.html</name>
  <files>onboarding.html</files>
  <read_first>
    onboarding.html,
    workflows.html,
    .planning/phases/11-onboarding-users-admin/11-UI-SPEC.md
  </read_first>
  <action>
    Rewrite the `<script>` block in onboarding.html to replace all mock data with API calls. Follow the Phase 10 workflows.html big-bang swap pattern (D-26).

    **1. Delete mock data:** Remove `MOCK_OB_TEMPLATES`, `MOCK_HIRES`, `SECTION_STATES`, `OB_CHECKS`, `FAQ_VIEWED` — all of them.

    **2. Add api() wrapper** (identical to workflows.html and users.html pattern):
    ```javascript
    async function api(method, path, body) {
      const opts = { method, headers: { 'Content-Type': 'application/json' } };
      if (body) opts.body = JSON.stringify(body);
      const res = await fetch(path, opts);
      if (res.status === 401) { window.location.href = '/login.html'; throw new Error('unauthorized'); }
      if (res.status === 204) return null;
      const data = await res.json();
      if (!res.ok) throw data;
      return data;
    }
    ```

    **3. Add state variables:**
    ```javascript
    let CURRENT_USER = null;
    let MY_TRAININGS = [];    // from /myTrainings
    let CURRENT_TRAINING = null; // from /hireTraining
    let MANAGER_HIRES = [];   // from /managerHires
    let TEMPLATES = [];        // from /templates (Builder)
    ```

    **4. Skeleton screens** (per UI-SPEC):
    - `renderSkeletons(containerId, count)`: insert `count` `.skeleton-row` divs.
    - Add CSS `.skeleton-row` (same as users.html: height 44px, brd background, pulse animation).

    **5. My Trainings tab (Tab 1 — hire view):**
    - `loadMyTrainings()`: call GET /api/v1/onboarding/myTrainings → render list of assigned templates with progress bar. Skeleton while loading.
    - Empty state: "No trainings assigned" / "Your manager will assign training when you're ready." (per UI-SPEC)
    - Tapping a template: call GET /api/v1/onboarding/hireTraining/{currentUser.id}?templateId={id} → render training detail view with sections and items.

    **6. Training detail view** (replaces the existing openChecklist flow):
    - Render sections in order. Each section header shows title + state badge.
    - Section states from server: 'locked' (grayed out, "Complete the previous section first."), 'active' (expanded, items interactive), 'complete' (collapsed, green checkmark), 'signed_off' (collapsed, "Signed off by {name} — {date}" attribution per D-06 and UI-SPEC).
    - Checkbox items: tapping toggles check and calls POST /api/v1/onboarding/saveProgress with `{item_id: itemId, progress_type: 'item', checked: newState}`. Optimistic UI — toggle immediately, revert on error.
    - Video series: show each part. Tapping "Watch video" opens the URL. A "Mark as watched" button for each part calls saveProgress with `{item_id: partId, progress_type: 'video_part', checked: true}`.
    - FAQ section: render Q&A pairs. No progress tracking for FAQ items (type='faq' is excluded from completion count).
    - After saving progress, re-check if section is now complete (compare checked count vs total non-faq items). If complete and section has requires_sign_off=true, show "Waiting for sign-off" indicator.

    **7. Manager tab (Tab 2):**
    - `loadManagerHires()`: call GET /api/v1/onboarding/managerHires → render list of hires with their progress.
    - Empty state: "No active hires" / "Add a crew member in Users to get started." (per UI-SPEC)
    - Tapping a hire: show their assigned templates. Tapping a template: call GET /api/v1/onboarding/hireTraining/{hireId}?templateId={templateId} → render read-only view of hire's progress.
    - Sign-off button: shown on sections where requires_sign_off=true AND section is complete AND no signoff exists yet. Button: "Sign Off Section" (per UI-SPEC).
    - **Sign-off form** (per D-05, UI-SPEC — inline, not modal):
      - Appended below section header when "Sign Off Section" is tapped.
      - Heading: "Sign Off: {section_name}"
      - Notes textarea (label: "Notes", placeholder: "What went well? Any concerns?")
      - Rating selector — three radio buttons or button group: "Ready", "Needs Practice", "Struggling"
      - Confirm button: "Confirm Sign-Off" (disabled until both notes non-empty and rating selected)
      - Cancel button: "Never Mind" (removes form, restores button)
      - On confirm: POST /api/v1/onboarding/signOff with {section_id, hire_id, notes, rating}. On success: replace button with "Signed off" pill + attribution line.

    **8. Builder tab (Tab 3) — admin/manager only:**
    - `loadTemplates()`: call GET /api/v1/onboarding/templates → render template list for editing.
    - Template editing follows the same pattern as existing Builder code but saves via API:
      - "Save" button: PUT /api/v1/onboarding/updateTemplate/{id} with full template structure (per D-04).
      - "Create New Template" button: POST /api/v1/onboarding/createTemplate.
      - "Discard" button: reload template from API, discarding local edits (per D-04).
    - Keep the existing SortableJS drag-to-reorder for sections and items within the Builder.
    - Assignment management within Builder: show "Assign to..." dropdown per template. POST /assignTemplate and /unassignTemplate on change.

    **9. Page init:**
    ```javascript
    (async function init() {
      try { CURRENT_USER = await api('GET', '/api/v1/me'); } catch(e) { return; }
      const isManager = CURRENT_USER.role === 'admin' || CURRENT_USER.role === 'manager' || CURRENT_USER.is_superadmin;
      // Show/hide Manager and Builder tabs based on role
      loadMyTrainings();
      if (isManager) { loadManagerHires(); loadTemplates(); }
    })();
    ```

    **10. Event delegation:** Maintain single click listener + single input listener on appropriate containers, routing via `data-action` attributes. Keep the existing event delegation pattern.

    **11. Auto-save error handling** (per UI-SPEC): On saveProgress failure, show `.field-err` inline: "Couldn't save. Tap to retry." Do NOT revert checkbox state until retry fails twice.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/yumyums/hq && grep -c "MOCK_OB_TEMPLATES" onboarding.html && grep -c "api('GET'" onboarding.html && grep -c "api('POST'" onboarding.html</automated>
  </verify>
  <acceptance_criteria>
    - onboarding.html does NOT contain `MOCK_OB_TEMPLATES` (grep count = 0)
    - onboarding.html does NOT contain `MOCK_HIRES` (grep count = 0)
    - onboarding.html does NOT contain `SECTION_STATES = ` assignment (grep count = 0)
    - onboarding.html does NOT contain `OB_CHECKS = ` assignment (grep count = 0)
    - onboarding.html contains `async function api(method, path, body)`
    - onboarding.html contains `api('GET', '/api/v1/onboarding/myTrainings'`
    - onboarding.html contains `api('GET', '/api/v1/onboarding/hireTraining/`
    - onboarding.html contains `api('POST', '/api/v1/onboarding/saveProgress'`
    - onboarding.html contains `api('POST', '/api/v1/onboarding/signOff'`
    - onboarding.html contains `api('GET', '/api/v1/onboarding/templates'`
    - onboarding.html contains "Sign Off Section" button text
    - onboarding.html contains "Confirm Sign-Off" button text
    - onboarding.html contains "Never Mind" cancel text for sign-off form
    - onboarding.html contains rating options "Ready", "Needs Practice", "Struggling"
    - onboarding.html contains `skeleton-row` class
    - onboarding.html contains "No trainings assigned" empty state text
  </acceptance_criteria>
  <done>onboarding.html fully API-backed with no mock data. My Trainings shows real progress. Manager tab shows hire progress with sign-off form. Builder tab saves via API.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild service worker with updated file hashes</name>
  <files>sw.js</files>
  <read_first>
    build-sw.js
  </read_first>
  <action>
    Run `node build-sw.js` from the project root to regenerate sw.js with updated content hashes for the modified HTML files (onboarding.html, users.html, login.html).

    This ensures the PWA picks up the new files on next visit. The Workbox generateSW handles cache busting via content hashes automatically.

    After generation, verify the output sw.js contains precache entries for onboarding.html, users.html, and login.html with updated revision hashes.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/yumyums/hq && node build-sw.js && grep "onboarding.html" sw.js && grep "users.html" sw.js && grep "login.html" sw.js</automated>
  </verify>
  <acceptance_criteria>
    - sw.js exists and contains precache entries for onboarding.html, users.html, login.html
    - sw.js contains updated revision hashes (different from before this plan)
    - `node build-sw.js` exits with code 0
  </acceptance_criteria>
  <done>Service worker rebuilt with updated content hashes for all modified HTML files.</done>
</task>

</tasks>

<verification>
- onboarding.html has zero mock data — all data from API
- Sign-off form works inline with notes + rating
- Service worker rebuilt with fresh hashes
- `node build-sw.js` succeeds
</verification>

<success_criteria>
- onboarding.html loads real data from API for all three tabs
- Auto-save works for checkbox and video part progress
- Manager sign-off form captures notes + rating and shows attribution
- Builder tab CRUD works via API with save/discard
- Service worker updated
</success_criteria>

<output>
After completion, create `.planning/phases/11-onboarding-users-admin/11-05-SUMMARY.md`
</output>
