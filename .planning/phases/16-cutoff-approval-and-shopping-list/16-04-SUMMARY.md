---
phase: 16-cutoff-approval-and-shopping-list
plan: "04"
subsystem: ui
tags: [purchasing, purchasing.html, po-tab, cutoff, approve, shopping-list, vanilla-js]

# Dependency graph
requires:
  - phase: 16-02
    provides: Lock/unlock/approve state machine + cutoff scheduler + GET /orders?status=locked endpoint
  - phase: 16-03
    provides: Shopping list CRUD, check-off, vendor completion endpoints
provides:
  - PO tab (s3) in purchasing.html with locked PO view grouped by vendor_name
  - Cutoff config pill on Order tab header (admin-interactive, non-admin read-only)
  - Admin Edit toggle enabling/disabling steppers in locked PO view
  - Approve button calling POST /orders/{id}/approve with 409 error handling
  - Inline cutoff config form with day/time dropdowns and PUT /cutoff save
affects:
  - 16-05 (Shopping tab build — PO tab is now functional, full purchasing.html context needed)
  - 17 (Alerts — approve button creates shopping list that triggers notifications)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "LOCKED_PO loaded independently from GET /orders?status=locked — separate from PO_STATE (draft)"
    - "Parallel init() loads: /me, /cutoff, /orders?status=locked via Promise.all"
    - "PO_EDITING toggle pattern: boolean state flag + re-render on toggle for admin edit mode"
    - "Vendor grouping via line_item.vendor_name (not group_name) — null vendor_name falls to 'Unassigned'"

key-files:
  created: []
  modified:
    - purchasing.html

key-decisions:
  - "LOCKED_PO loaded from GET /orders?status=locked independently — not derived from PO_STATE status checks"
  - "Items grouped by vendor_name field on POLineItem (from LEFT JOIN in Plan 02 backend), not by item group_name"
  - "Cutoff config form is inline (expands below pill) rather than a modal — consistent with mobile-first dense layout"
  - "Non-admin sees cutoff pill as read-only text only (D-02); admin gets toggle-cutoff-config data-action"

patterns-established:
  - "data-action event delegation for admin controls (toggle-cutoff-config, save-cutoff, cancel-cutoff, po-edit-toggle, approve-po)"
  - "409 from approve endpoint → inline message 'Complete last week's shopping list before approving this one'"

requirements-completed: [CUT-01, CUT-02, CUT-04]

# Metrics
duration: 35min
completed: 2026-04-22
---

# Phase 16 Plan 04: Cutoff Config and PO Tab Summary

**PO tab with vendor-grouped locked view, admin edit toggle + approve button, and interactive cutoff config pill wired to PUT /cutoff**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-04-22
- **Completed:** 2026-04-22
- **Tasks:** 1 auto + 1 human-verify (approved)
- **Files modified:** 3 (purchasing.html, sw.js, sw.js.map)

## Accomplishments

- PO tab (s3) renders LOCKED_PO with items grouped by vendor_name, disabled steppers for non-admin
- Admin Edit toggle (PO_EDITING flag) activates steppers in locked view; changes persist via savePOItems()
- Admin Approve button calls POST /orders/{id}/approve; handles 409 with "Complete last week's shopping list" message
- Cutoff config pill on Order tab: admin can tap to expand inline form (day + time dropdowns, PUT /cutoff), non-admin sees read-only
- LOCKED_PO, CUTOFF_CONFIG, USER_ROLE all loaded in parallel in init() via Promise.all

## Task Commits

Each task was committed atomically:

1. **Task 1: Add cutoff pill config and PO tab rendering** - `5a6fc0d` (feat)
2. **Task 2: Verify PO tab and cutoff config** - human-verify checkpoint, approved (no code commit)

## Files Created/Modified

- `purchasing.html` - Added CUTOFF_CONFIG/USER_ROLE/PO_EDITING/LOCKED_PO state, renderPOTab(), renderCutoffForm(), saveCutoff(), vendor grouping logic, admin controls, approve flow
- `sw.js` / `sw.js.map` - Regenerated by build-sw.js after purchasing.html changes

## Decisions Made

- LOCKED_PO loaded from dedicated `GET /api/v1/purchasing/orders?status=locked` — not derived from PO_STATE status checks, per plan spec
- Items grouped by `vendor_name` on POLineItem (populated via LEFT JOIN in Plan 02 backend), not by item group_name — per D-09
- Cutoff form is inline (expands below pill) rather than a modal — keeps the Order tab header compact on mobile
- Non-admin sees cutoff pill as static read-only text (D-02); admin gets `data-action="toggle-cutoff-config"` on the pill

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- PO tab is complete and approved; purchasing.html now has all four tabs (Order, Shopping, History, PO)
- Plan 16-05 adds the Shopping tab with check-off, photo capture, and location notes — purchasing.html context is required
- Note: Per STATE.md decision, Plan 05 was later executed incorporating 16-04 work in a single pass rewrite. This summary documents the 16-04 frontend work as originally planned and committed.

---
*Phase: 16-cutoff-approval-and-shopping-list*
*Completed: 2026-04-22*
