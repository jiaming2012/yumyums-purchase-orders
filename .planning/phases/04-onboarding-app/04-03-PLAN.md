---
phase: 04-onboarding-app
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - index.html
  - sw.js
  - users.html
autonomous: false

requirements:
  - D-01

must_haves:
  truths:
    - "Onboarding tile appears on HQ launcher and links to onboarding.html"
    - "Service worker caches onboarding.html for offline PWA use"
    - "Onboarding app appears in Users permission management"
  artifacts:
    - path: "index.html"
      provides: "Active Onboarding tile in launcher grid"
      contains: "onboarding.html"
    - path: "sw.js"
      provides: "Cache entry for onboarding.html"
      contains: "onboarding.html"
    - path: "users.html"
      provides: "Onboarding entry in APPS array"
      contains: "onboarding"
  key_links:
    - from: "index.html"
      to: "onboarding.html"
      via: "tile href"
      pattern: "href=\"onboarding.html\""
    - from: "sw.js"
      to: "onboarding.html"
      via: "ASSETS array"
      pattern: "onboarding.html"
---

<objective>
Wire onboarding.html into the HQ ecosystem: activate the Hiring tile on index.html to link to onboarding.html, add onboarding.html to the SW cache, and add the onboarding app to the Users permission APPS array. Includes a human-verify checkpoint.

Purpose: Makes onboarding.html discoverable and usable as part of the installed PWA.
Output: Updated index.html, sw.js, users.html + human verification of full flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-onboarding-app/04-CONTEXT.md
@.planning/phases/04-onboarding-app/04-RESEARCH.md

<interfaces>
From index.html — current Hiring tile (lines 59-64, will be converted):
```html
<div class="tile soon">
  <div class="badge">Soon</div>
  <div class="tile-icon">👥</div>
  <div class="tile-title">Hiring</div>
  <div class="tile-desc">Candidates and onboarding</div>
</div>
```

From sw.js — current cache config (lines 1-2):
```js
const CACHE = 'yumyums-v39';
const ASSETS = ['./', './index.html', './purchasing.html', './users.html', './login.html', './workflows.html', './ptr.js', './manifest.json'];
```

From users.html — current APPS array (line 138):
```js
const APPS=[
  {slug:'purchasing',name:'Purchasing',icon:'🛒'},
  {slug:'payroll',name:'Payroll',icon:'💰'},
  {slug:'scheduling',name:'Scheduling',icon:'📅'},
  {slug:'hiring',name:'Hiring',icon:'👥'},
  {slug:'bi',name:'BI',icon:'📊'},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate onboarding into HQ launcher, service worker, and user permissions</name>
  <files>index.html, sw.js, users.html</files>
  <read_first>
    index.html (find the Hiring tile div to convert — around lines 59-64)
    sw.js (find CACHE version string and ASSETS array — lines 1-2)
    users.html (find APPS array — around line 138)
  </read_first>
  <action>
**index.html — Convert Hiring tile to active Onboarding tile:**

Replace lines 59-64 (the Hiring "Soon" tile):
```html
<div class="tile soon">
  <div class="badge">Soon</div>
  <div class="tile-icon">👥</div>
  <div class="tile-title">Hiring</div>
  <div class="tile-desc">Candidates and onboarding</div>
</div>
```

With:
```html
<a class="tile active" href="onboarding.html">
  <div class="tile-icon">🎓</div>
  <div class="tile-title">Onboarding</div>
  <div class="tile-desc">New hire training</div>
</a>
```

**sw.js — Add onboarding.html to cache and bump version:**

Change line 1 from:
```js
const CACHE = 'yumyums-v39';
```
To:
```js
const CACHE = 'yumyums-v40';
```

Change line 2 from:
```js
const ASSETS = ['./', './index.html', './purchasing.html', './users.html', './login.html', './workflows.html', './ptr.js', './manifest.json'];
```
To:
```js
const ASSETS = ['./', './index.html', './purchasing.html', './users.html', './login.html', './workflows.html', './onboarding.html', './ptr.js', './manifest.json'];
```

**users.html — Add onboarding to APPS array:**

Find the APPS array (around line 138) and add a new entry after the existing entries (before the closing `]`):
```js
{slug:'onboarding',name:'Onboarding',icon:'🎓'},
```

The resulting APPS array should be:
```js
const APPS=[
  {slug:'purchasing',name:'Purchasing',icon:'🛒'},
  {slug:'payroll',name:'Payroll',icon:'💰'},
  {slug:'scheduling',name:'Scheduling',icon:'📅'},
  {slug:'hiring',name:'Hiring',icon:'👥'},
  {slug:'bi',name:'BI',icon:'📊'},
  {slug:'onboarding',name:'Onboarding',icon:'🎓'},
```
  </action>
  <verify>
    <automated>grep -q "onboarding.html" index.html && grep -q "yumyums-v40" sw.js && grep -q "onboarding.html" sw.js && grep -q "onboarding" users.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `href="onboarding.html"` (tile links to onboarding page)
    - index.html contains `tile active` wrapping the onboarding link (not `tile soon`)
    - index.html contains the emoji and title `Onboarding`
    - index.html does NOT contain a `tile soon` div with `Hiring` title (it was converted)
    - sw.js line 1 contains `yumyums-v40` (cache version bumped)
    - sw.js line 2 contains `onboarding.html` in the ASSETS array
    - users.html contains `slug:'onboarding'` in the APPS array
  </acceptance_criteria>
  <done>
    Onboarding tile is active on HQ launcher, service worker caches onboarding.html with bumped version, and onboarding appears in Users permission management.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete onboarding flow</name>
  <files>onboarding.html, index.html, sw.js, users.html</files>
  <action>
Human verifies the complete onboarding tool end-to-end. No automated action — this is a visual and functional verification checkpoint after Plans 01, 02, and 03 are all complete.
  </action>
  <verify>Human confirms all 19 verification steps pass or describes issues.</verify>
  <done>User types "approved" confirming the onboarding tool works correctly across all flows.</done>
  <what-built>Complete onboarding tool: My Trainings tab (checklist list, runner with sequential sections, checkboxes, video parts, FAQ gate, sign-off request) + Manager tab (hire cards, read-only drill-down, approve/send-back, assign training) + HQ integration (launcher tile, SW cache, user permissions).</what-built>
  <how-to-verify>
    1. Open index.html in browser — verify Onboarding tile shows with graduation cap icon and links to onboarding.html
    2. Tap Onboarding tile — onboarding.html loads with "My Trainings" tab active
    3. Verify checklist cards show with progress bars (e.g., "0 of X tasks complete")
    4. Tap a checklist card — runner opens with sections; first section is active, later sections show "Locked"
    5. Check some items in the first section — progress updates
    6. Find a video part — tap "Watch Video" — verify it opens a URL in new tab (checkbox does NOT auto-check)
    7. Manually check video part checkbox after watching
    8. Complete all items in first section — if requiresSignOff, "Request Sign-Off" button appears
    9. Tap Request Sign-Off — section shows "Pending Sign-Off", next section should NOT unlock yet
    10. Switch to Manager tab — verify Active sub-tab shows hire cards with progress bars
    11. Tap a hire card — verify read-only checklist (checkboxes NOT tappable)
    12. Find the pending sign-off section — tap "Approve Section" — verify section shows signed off and next section unlocks
    13. Test "Send Back" on another pending section — verify section returns to active
    14. Back to Manager list — tap "+ Assign Training" — verify template picker appears
    15. Assign a template — verify it appears in hire's checklist list
    16. Go back to My Trainings — open the checklist with a FAQ section — verify "View Required" badge
    17. Expand FAQ section — verify badge changes to "Viewed"
    18. Verify dark mode works (toggle system preference or use DevTools)
    19. Navigate back to index.html via the "HQ" back link
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- index.html shows active Onboarding tile
- sw.js has version v40 and includes onboarding.html in ASSETS
- users.html APPS array includes onboarding entry
- Full onboarding flow works end-to-end from HQ launcher
</verification>

<success_criteria>
Onboarding tool is fully integrated into HQ ecosystem. User can navigate from launcher to onboarding, complete crew workflows, and use manager features. SW caches the page for offline PWA use.
</success_criteria>

<output>
After completion, create `.planning/phases/04-onboarding-app/04-03-SUMMARY.md`
</output>
